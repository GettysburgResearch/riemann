# T-105107 boundary-optimal Blaschke–Pick selectors

This checkpoint replaces avoidable polynomial selector growth by the exact
minimum-norm bounded analytic selector on a supplied regular window.

The zeros forced by the complete pole manifest form a finite inner factor
\(I_0\).  Every admissible top-jet selector is

\[
W=I_0H,
\qquad
H(c)=\gamma_c/\beta_c.
\]

The remaining problem is ordinary finite Nevanlinna–Pick interpolation.  Its
optimal norm is the first \(u\) for which

\[
\left[
\frac{u^2-y_j\overline y_k}{1-c_j\overline c_k}
\right]\succeq0.
\]

The extremal has constant boundary modulus, preserves every L-105105 residue,
and transports to rectifiable simply connected Jordan windows.  Intrinsic
pseudohyperbolic clustering and all Xi cofinal estimates remain open.

Exact replay:

    python -B experiments/X-105107-blaschke-pick-jet-selectors/tests/test_verify.py
    python -B -O experiments/X-105107-blaschke-pick-jet-selectors/tests/test_verify.py

Expected:

    PASS_T105107_BLASCHKE_PICK_JET_SELECTORS
    17/17 focused tests in normal and optimized Python
    digest 82747e320365a97bf9824dd84960f00bbcb77cd738040c2c5214be844dfc738c

No heavy computation is used.  Xi manifests, rectangle conformal
coordinates, cofinal Pick-norm estimates, quotient edge estimates, strict
coherence, RCMV104530, and RH remain open.
