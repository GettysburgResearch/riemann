## Hyperbolic continuation of the native beta reset

This add-only continuation is rebased on the live PR #759 head

```text
d1b8aa57b08db1ba9f2edf3b68238c2a129b7c33
```

It retains the individual-zeta source

```text
beta=(delta_1-delta_67)*mu.
```

**RH remains unproved.**

### Relation to the new causal barrier

The live branch now proves that no nonzero compact causal detector can obtain an exponentially small Fourier tail, and hence source-blind exact quadratic Nyquist rank is impossible. T-107020 deliberately crosses that theorem by using a fixed noncompact smoother and paying physical aliases.

### New fixed smoother

The Fourier multiplier

```text
exp(1-cosh t)
```

has zero-free entire bilateral Laplace multiplier

```text
exp(1-cos s).
```

Its frequency tail is double-exponential and its physical kernel has
exponential tails in every strip of width below `pi/2`. The noncausal physical
tail and negative-time transform correction are paid explicitly.

### New compression

Poisson sampling with period `O(log X)` has every-power alias error. The
frequency tail beyond `loglog X+O_A(1)` is every-power small. Therefore

```text
RH <=> HNBV107020,
```

where HNBV is the norm bound for one literal native beta vector of dimension

```text
O(log X loglog X).
```

No source completion, owner surrogate, family average or X-dependent detector
is used.

### Status

```text
zero-safe hyperbolic smoothing              PROVED
noncausal Landau correction                 PROVED
Poisson alias bound                         PROVED
frequency tail beyond O(loglog X)           PROVED
feature rank O(log X loglog X)              PROVED
HNBV107020                                  OPEN / RH-EQUIVALENT
Riemann Hypothesis                          UNPROVEN
```

### Replay

```text
PASS_T107020_HYPERBOLIC_NATIVE_BETA_COMPRESSION
```

### Hostile self-audit

The self-audit corrected the initial direction of the negative-time holomorphy half-plane. The correction is holomorphic in `Re(s)<1`, which contains the required detector strip. No conclusion changed. See `reports/gpt56-pro/2026-08-27-hostile-audit-hyperbolic-native-beta.md`.
