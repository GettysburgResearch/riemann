# M-96000 — Hostile reconstruction protocol

1. Reconstruct `L-94200.1--.6` from the canonical row definition.
2. Search for a negative finite initial-prime row.
3. Demand an explicit enumeration of every divisor-cube frontier template.
4. Verify that no positive reservoir is reused by two paths.
5. Check the full Möbius-to-initial-prime identity in `L-94201`.
6. Recompute the substitution \(X=kY\) and the factor \(k^{-s-1/2}\).
7. Recompute the fixed-row Mellin transform and the zeta-pole cancellation.
8. Recompute
   \[
   P_j(z)=-\frac{z(z+1)}{1-z}j^{-z-1}
   +O_z(j^{-\Re z-2}).
   \]
9. Check the hypotheses and orientation of Landau's theorem.
10. Verify that no endpoint benchmark or RH-equivalent estimate is imported.

Immediate falsifiers:

```text
one negative finite sieve row;
one double-owned reservoir;
a missing k^(-s-1/2) factor;
a zero of every P_j at one open-strip point;
a positive-real singularity of the continued transform;
a sign-changing f_j;
a hidden native benchmark assumption.
```
