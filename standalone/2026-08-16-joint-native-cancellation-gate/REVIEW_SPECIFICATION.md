# Review specification

Review in this order:

1. Verify `L-92920.7` / the exact child identity `-R_X`.
2. Verify `L-91379.3` and the positive `m=67` ordinary term at `q=2`.
3. Verify that the physical packet class has nonnegative ordinary response.
4. Reconstruct `R-92940` and reject every branchwise positive child placement.
5. Check that `L-92940` claims necessity only.
6. Treat `M-92940` as an open construction target.
7. Treat `T-92940` as conditional; do not infer the antecedent.

Immediate falsifiers:

```text
actual oriented child called a positive capacity;
separate nonnegative child row preserving negative ordinary response;
rough lift used as native;
109/1200 used outside its full-rough-lift hypotheses;
signed finite comparison called positive source;
RH reported proved.
```
