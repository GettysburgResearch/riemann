# Review specification — T-99250 scalar SHARP Harnack route

The candidate is rejected immediately if any of the following fails:

1. PR #642's identity `Q=T*kappa` or the proof `kappa>0`.
2. The derivative of `T(Y/t)/T(Y)` in `L-99250`.
3. The exact Radon–Nikodym child identity.
4. The compact Hall margin `>7/20` at all required prefixes.
5. The local coefficient dictionary `(1,-2,1)` at 67.
6. The all-real endpoint semantics of the directed scan.
7. The independent reconstruction of the interval at `201^-`.
8. The exact Mellin transform.
9. Analyticity on the positive real axis.
10. Noncancellation of an off-line zero by `1-67^(-rho)`.

The unbounded statement

```text
mathfrak H_67(x) >= 0 for x >= 100000001
```

is not certified and must not be inferred from the finite margin or a fitted
trend.
