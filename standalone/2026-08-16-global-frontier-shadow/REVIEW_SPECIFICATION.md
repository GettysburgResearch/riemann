# M-96100 — Hostile review protocol

1. Reproduce the exact `(j,P,n)=(3,6,24)` fixed-product failure.
2. Verify `q_j=C_j+h_j` and the rough-reservoir identity `L-96100.6`.
3. Expand every first-crossing owner in `L-96101`; no phrase such as "the
   remaining templates are analogous" is acceptable.
4. Check that complete and partial multiplicative blocks are half-open and
   source-disjoint.
5. Check the log-barycentre of every butterfly.
6. Attempt a negative row search at every integer activation knot; between
   knots the row is affine in `log Y`.
7. Recompute the full-row finite specialization.
8. Recompute the Mellin substitution `X=kY`, including `k^{-s-1/2}`.
9. Recompute the large-j coefficient `-rho(rho+1)/(1-rho)`.
10. Check the exact hypotheses of Landau's theorem.

Immediate rejection conditions:

```text
fixed-product proof silently reused;
duplicate rough reservoir owner;
unpaid shoulder or partial block;
negative finite initial-prime row;
missing real-endpoint cell;
reciprocal-zeta pole cancelled in every row;
hidden RH-strength arithmetic estimate.
```
