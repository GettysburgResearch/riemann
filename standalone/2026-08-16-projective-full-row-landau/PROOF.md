# Canonical projective paired-source positivity and a two-row Mellin-Landau criterion

The proof candidate is exactly the concatenation of `R-96300`, `L-96300`,
`L-96301`, `L-96302`, `L-96303`, and `T-96300`.

The central mathematical identity is

\[
 c_X
 =\sum_{\ell}\omega_\ell g_\ell
  +\sum_u\eta_uc_{x_u},
\]

where every `g_l` is a complete canonical Target-Lorenz leaf row, every
`c_(x_u)` has `x_u<67`, and all coefficients are nonnegative.  This identity is
formed before any physical observation of an intermediate rough child.

The final analytic contradiction uses only rows two and three:

\[
 \int_1^\infty c_X(j)X^{-s-1}dX
 ={C_j\over s^2}+{P_j(s+1/2)\over s^2\zeta(s+1/2)},
\]

and

\[
 P_2=P_3=0\Longrightarrow -3(x-1)(x-2)=0,
 \qquad x=2^{-z},
\]

which is impossible in the open critical strip.  Landau's theorem then gives
the proposed RH conclusion.

The review must not replace the general source induction with the finite
registry fixture or the MPFR tail with its printed decimal margins.
