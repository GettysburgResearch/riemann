# Review specification — T-107300

Review in this order:

1. verify that the marked residue map factors right-to-\(\ell\) and
   left-to-\(\rho\);
2. verify the rank-one coefficient form on the declared native rectangles;
3. rederive residue aggregation and tensor-product quadratic factorization;
4. check the rectangular omission identity;
5. verify the augmentation splitting and Frobenius equivariance;
6. verify that Adams is applied on source orbits before pushforward;
7. independently recompute the ten-history pattern and four tensor grades;
8. check the equal-history opposite-sign calibration;
9. check the rank-one residue-only counterexample;
10. retain every open global trace, gluing and principal-binding flag.

Do not infer that every complete live fibre is already a disjoint rectangular
union or that the finite graded object supplies a uniform sheaf/conductor
theorem.
