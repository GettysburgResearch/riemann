# Hostile reconstruction specification — T-102960 / T-102970

Reviewed source:

```text
PR #751
8743ba4230097c296dee1dec40d09b59cdf56599
```

Review deposit: PR #719.

## Mandatory checks

1. Expand `M(x)^2` and verify that its difference from `1-x` begins at `x^2`.
2. Reconstruct Boolean restriction functoriality coefficientwise.
3. Reconstruct the Boolean Vaughan identity without ordinary overlapping
   Dirichlet factors.
4. Verify the squarefree lattice reindexing and its owner exclusions.
5. Check the `l1` operator transfer before using the pointwise lattice bound.
6. Verify the minimum-owner rule and `lambda^2<=a` on literal Boolean cores.
7. Verify that every balanced core exceeds `U^2`.
8. Reconstruct common-square extraction and the `Y^(1+o(1))G^(-3+o(1))`
   physical bound.
9. Check that equal-core and one-sided sectors are absolutely subpower at
   `G~Y^(1/3)`.
10. Reconstruct the physical-product centered phase theorem with `n`, not an
    erased square-core variable.
11. Distinguish scalar phase energy from owner-incidence physical collapse.
12. Verify the finite labelled-to-physical counterfixture.
13. Identify the exact source weights consumed by any proposed incidence
    transport.
14. Keep co-owner, marked-67, carrier, shell and renewal coordinates.
15. Recombine all chaos levels before a negative part.
16. Confirm `ICPR102970`, `OICP102960`, T-106080 and RH remain unproved.

## Immediate falsifiers

```text
using ordinary Vaughan while claiming Boolean support;
allowing overlap between Boolean factors;
treating owner exclusion as changing the universal balanced coefficient;
using only a pointwise Type-I estimate without the Hilbert/source transport;
erasing the owner multiplier from the physical phase;
using the scalar same-family kernel as an owner-collapse theorem;
keeping owner fibres orthogonal through the physical conclusion;
collapsing labels source-blindly;
spending one internal prime weight in two Cauchy steps;
splitting a non-absolutely-controlled all-chaos packet;
claiming the replay proves ICPR102970 or RH.
```
