# Parity-covariant single-scalar v2

This is the hostile-reconstruction successor to paper PR #558.

It accepts the exact odd-history obstruction of PR #561. The v1 leafwise
factor-67 terminalization is therefore withdrawn: a terminal theorem proved in
canonical parity cannot be installed after an odd rough history.

The v2 paper retains the exact analytic finish and compresses the two fixed rows
to the unique positive scalar

\[
\mathcal R_X=5c_X(2)+3c_X(3).
\]

It proves the scalar dictionary, reciprocal-zeta Mellin transform, zero-safe
numerator, global finite Lorenz primal/dual, source-correct prime-prefix
recurrence, eventual positivity for every fixed finite prime prefix, and the
bottom-contact carry identification.

The exact remaining all-scale producer can be stated in any of three equivalent
forms:

```text
SGHL  global parity-aware scalar Hall/Lorenz feasibility;
SCPS  moving-prefix source-correct prefix shadow positivity;
PICR  positive interior carry realization of the bottom-contact scalar.
```

None of these uniform producers is represented as proved. Consequently this is
an unconditional theorem-and-reduction paper, not an accepted proof of RH.

```text
v1 leafwise factor-67 terminalization  REFUTED
5:3 scalar algebra and Mellin finish    EXACT
fixed finite prime prefixes             EVENTUALLY POSITIVE
uniform moving-prefix producer          OPEN / RH-BEARING
Riemann Hypothesis                       UNPROVED
```
