# Parity-covariant gluing and fixed-depth obstructions - T96500

This standalone arXiv-style manuscript hostilely reconstructs PR #550 under the
complete rough-prime recursion.

It proves:

```text
rough placement preserves coefficient magnitude and every row activation;
rough history contributes the sign cocycle (-1)^depth;
the canonical one-prime Target-Lorenz map is not parity invariant;
E_T(71,13)-O_T(71,13)>17 on an explicit odd-history leaf;
every fixed depth has an exact source expansion;
every fixed even-depth positive current reset is eventually impossible;
the depth-two current is negative in rows 2 and 3 at X=200000;
global parity-aware common-source gluing has an exact primal-dual theorem;
GPHT23 implies two-row positivity and then RH by Mellin-Landau.
```

Exact scientific boundary:

```text
full-row / two-row nonnegativity: not refuted, still open
fixed-depth local repair: refuted
GPHT23 global producer: open / RH-bearing
Riemann Hypothesis: unproved
```

Replay and compile:

```bash
./replay.sh
```
