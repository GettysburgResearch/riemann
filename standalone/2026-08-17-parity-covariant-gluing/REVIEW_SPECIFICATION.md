# Hostile review specification

Review in this order:

1. Verify the signed observation convention and `O(S^m P)=(-1)^m O(P)`.
2. Reconstruct the path `X=61841`, incoming history `(67)`, terminal `(71,13)`.
3. Run the exact rational interval verifier and independently recompute the 239
   active `P61` target atoms.
4. Verify that the canonical Target-Lorenz map is oriented from even supply to
   odd demand and cannot be reversed when `E_T>O_T`.
5. Reconstruct the exact depth-`L` direct-sum identity.
6. Check the finite-Euler principal term and the fixed-order reciprocal
   almost-prime estimates in the fixed-depth no-go theorem.
7. Rerun the directed MPFR depth-two certificate at `X=200000`.
8. Check the global common-source primal and its separation alternative.
9. Re-derive the fixed-row Mellin transform and rows-2/3 noncancellation.
10. Do not interpret the paper as a proof of `GPHT23`, two-row positivity, or RH.
