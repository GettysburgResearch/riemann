# Hostile review specification

Reject at the first failure of:

1. the exact negative witness remains strictly negative;
2. the successor never forms a derivative-fibre causal difference;
3. the anchored finite source is realized with one literal coefficient vector in every row/ordinary coordinate;
4. the bulk direct row is exactly the retained-cell Volterra integral;
5. the hybrid discrete/continuum identity is cellwise exact;
6. the retained-cell error bound is valid on the moving bulk interval;
7. the terminal relative estimate uses the moving top anchor rather than a hidden omission;
8. ordinary `q` and `4q` are observed on the same row before detail;
9. all `q<K` columns are included;
10. the sparse `Y_4` arithmetic gives `<1` mismatch cost;
11. the finite-dual inequality has orientation `F_Lambda<=native deficit`;
12. no `J_Lambda-4sqrt(X)` estimate enters;
13. PR #509 is not cited as independent confirmation;
14. all imported SHAs match the lock.

The first independently reconstructed open input is the anchored finite Target-Lorenz leaf theorem at the frozen PR #508 head. The final independently reconstructed open input is the frozen prime-square/Mellin-Landau consumer.
