# Hostile review specification — T-102890

Review the live PR #719 branch at the exact head recorded by the final source
lock.  This packet is a source-typing and regime reduction, not an RH proof.

## Mandatory reconstruction

1. Reconstruct the stopped-prime Vaughan identity in `L-102869`; do not replace
   the monoid `P^+(n)<q` by the unrestricted integer lattice.
2. Verify the positive-square main and one-sided Type-I estimate in `L-102880`.
3. Verify that the smooth boundary remains in the final coherent owner sum even
   though its fixed-squareclass coefficient cost is polylogarithmic.
4. Reconstruct the exact gcd extraction and all representation factors.
5. Recompute the crude physical Gram bound in `L-102887`; confirm that the
   conclusion-facing threshold is `X^(1/3)(log(3X))^2`, not `X^(1/4)`.
6. Check the three-case source partition of `L-102886` coefficientwise.
7. Verify the internal Ramanujan identities and the reciprocal normalization
   factors in `L-102888`.
8. Preserve the equal-core and unequal-core packets until complete all-chaos
   recombination; `R-102869` is binding.
9. Confirm that only independently absolute regions are removed before taking
   the negative part.
10. Confirm that `SGIC102890` and RH remain explicitly unproved.

## Immediate falsifiers

```text
using the unrestricted Type-I lattice as the literal stopped source;
spending the favorable Type-I square twice;
using a free/Fock norm as a physical restriction theorem;
separating equal-core and unequal-core negative parts;
choosing an internal phase not determined by one reduced source core;
reintroducing a principal internal frequency;
using an X^(1/4) large-gcd threshold for the physical Gram packet;
dropping the smooth-boundary row from the coherent final scalar;
claiming RH from the finite replay.
```
