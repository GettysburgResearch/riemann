# Hostile review specification for T-99940

A reviewer should reconstruct, in order:

1. the labelled duplicate-67 Euler expansion;
2. the one-label removal ratio and the exact proof `S_2<1`;
3. the activation jump `beta(n)n^(-3/2)`;
4. the derivative identity
   `y d/dy [T(y)^2/y] = 3T(y)/y`;
5. the finite ordinary-variation estimates;
6. the exact negative-part measure identity;
7. the dyadic mutation separating ordinary and weighted variation;
8. the Mellin transform and cancellation at `s=1/2`;
9. noncancellation at every hypothetical off-line zero;
10. the specialized Landau alternative;
11. both partial-summation estimates under RH;
12. the conclusion that the remaining condition is RH-equivalent, not proved.

Immediate falsifiers:

- any use of ordinary BV as a substitute for the weighted variation;
- any claim that positivity for `m=2` continuously implies the `m=1` sign;
- any cancellation of an off-line pole by `1-67^(-rho)`;
- any statement that the replay proves RH.
