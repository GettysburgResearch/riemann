# Parity-safe root-Julia single-scalar packet

This standalone packet isolates the unique `5:3` scalar as the boundary defect of a positive reciprocal Julia channel. It does not claim RH.

Review `R-97100`, then `L-97100--L-97103`, then `T-97100`.
