# Execution and review boundary

## Completed executions

- interval_source.py: the actual ten-dimensional dyadic matrix, ordinary
  Python and Python -O; both complete compact JSON records match.
- verify.py: 448 distinct finite controls per mode, including all ten actual
  source pivot signs; complete ordinary/optimized result JSON equality.
- verify.py --refusals: five intended record mutations per mode were rejected:
  an RH flag, total-count type alias, floating-point dimension alias,
  all-rank flag, and a negative replacement final pivot. These reruns are
  not counted as additional distinct algebra controls.
- An independent 160-decimal-digit mpmath differentiation lies in all twenty
  jet enclosures. This is NON_DIRECTED_HIGH_PRECISION, not a proof premise.

The new certificate uses 640-bit-grid outward rational arithmetic, N=256,
M=48 Euler--Maclaurin, a 128-step Gamma shift and a complex radius 1/4.
The source implementation is independent of the #792/#793 interval code.
CERTIFICATE.md describes every analytic tail bound. A prepublication audit
widened the Gamma remainder by a conservative factor two and regenerated
all final outputs before publication.

A combined execution command hit its orchestration timeout while starting
the final optimized refusal run. The ordinary and optimized verification
records and ordinary refusals had completed. The optimized refusal run was
then executed separately, completed successfully, and its full output was
compared with the ordinary result. No timed-out invocation is reported as a
completed test. Temporary output files are not part of the scientific packet.

## What the finite checks prove and do not prove

The ten pivot signs certify this exact finite source matrix SUBJECT TO the
stated interval algorithms and analytic remainder bounds. The remaining
finite controls check the confluent matrix formula, elementary arithmetic,
Laplace test identity, Fourier endpoint normalization, Gamma moment forms,
and constants used in the analytic proof. They do not turn finite samples
into the all-X or infinite-index theorems; those have separate prose proofs.

No actual prime sum was numerically evaluated in the signed-tail theorem.
No new zero census, interval zero certificate, earlier whole-branch suite,
Lean build, remote CI success, independent referee verdict, or external
priority is claimed. The final all-rank positivity statement is still open.

## Priority independent review

1. Source budget x>1 and sum 1/x<1, with invariant multiplicities retained.
2. The finite-cutoff signed density and its normalization, including delta_c.
3. Dense rational Laplace tests and both infinite inertia statements.
4. Core/exterior estimates and the uniform N>=4096(L+1)^3 quantifier.
5. Exact full-minus-cutoff sign in the prime tail and the factor 1/128.
6. Cauchy bounds on analytic remainders, especially complex Gamma error.
7. Exact invariant Taylor composition and confluent matrix indexing.
8. Original-basis LDL versus eigenvalues in the Hilbert metric.
9. The explicit separation between a low-center signed tail result, a
   finite positive matrix, and the unresolved full signed matrix form.

Reference identities were checked in NIST DLMF 5.4, 5.11, 25.2 and 25.11.
The unconditional explicit formula is the same source contract as the
parent (Chirre--Goncalves, Mathematische Zeitschrift 300, Proposition 5).
This pass uses that theorem, not the source paper's RH-conditional results.
