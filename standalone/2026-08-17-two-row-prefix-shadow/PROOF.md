# Proof extract

The full proof extract is resident in the claim files.

The two decisive exact calculations are:

\[
\omega_{P=6,j=3}(24)=-1,
\]
which invalidates fixed-product convex packets, and
\[
\sum_{\substack{2\le m<10\\(m,30)=1}}m^{-1/2}
=7^{-1/2}<1/2<2<\sqrt2\log5,
\]
which invalidates the all-integer block as a rough-store capacity.

The corrected conclusion-producing implication is
\[
c_X(2),c_X(3)\ge0\ \text{eventually}
\Longrightarrow \mathrm{RH},
\]
because their reciprocal-zeta Mellin numerators have no common zero in
\(\Re z>0\).
