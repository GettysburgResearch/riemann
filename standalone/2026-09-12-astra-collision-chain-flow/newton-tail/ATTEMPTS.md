# Research outcome, attempted endings, and exact boundary

The user requested an end-to-end attack on all three routes, not another
unqualified inference from finite data. This record is the mathematical
outcome of the present continuation.

## What changed after reading the live branch

The previously delivered six-moment packet was published as PR875 by an
uploader. The branch then acquired CJB26, including an eight-moment connected
chain at a specified positive correlation box, a nonzero tenth-moment error,
and a joint finite-moment/gamma-defect criterion. The present work starts
AFTER that continuation. It neither republishes those achievements as new
nor treats the original PR body as the full current mathematical state.

PR876's exact fixed-point response is a useful newer comparison. Its positive
response lives in Laplace coordinates; the complex collision expression still
contains oscillatory real parts. We did not infer a favorable complex sign
from the positive measure or repeat an unpriced derivative argument.

## Analytic completion attempt

The target was to use the finite Ising construction to close gamma defect
extinction directly, avoiding an independent universal sign theorem for the
gamma current. CJB26 already states this bridge. Its displayed budget pays
the model's infinite Taylor tail and minimum modulus with a variance-only
Gaussian bound, which incurs a radius-squared exponent.

The concrete new observation is that admissible real-zero models cannot
hide an arbitrary higher Taylor tail after a small fitted terminal moment:
Newton log-concavity controls all subsequent factorial-scaled coefficients.
The proof in NJT1 makes that exact. The transferred bound on G(2iR) then pays
its entire far-zero product and near-zero count in NJT2. NJT3--NJT4 complete
the resulting conditional zero-exclusion and full-defect implication, with a
smaller explicit schedule and no extra low-frequency premise.

This does NOT produce models satisfying that schedule at unbounded orders.
The positive-coupling eight-moment model is not silently promoted to such a
model, and local solvability at its current jet is not a global continuation
or target-containing induction.

We also checked the obvious alternative, fitting a single native gamma stage
forever. The imported F5 off-axis zero makes that impossible. The new bound
improves a rigorous sufficient forbidden degree to512, with a specified
nonzero error tolerance. The native zero integral was not rerun; only the
new consequence and rational budgets were reconstructed.

No new ten-moment infinite-chain fit, ordinary numerical scout, signed-current
integral, complete moment matrix, or native large-degree feasibility experiment
was executed in this packet. Prior conversation contained possible numerical
experiments; they are not counted as completed work here.

## Arithmetic completion attempt

We examined whether optimizing the artificial completion could make the
signed covariance favorable without first bounding the native annular energy.
NJT-A1 proves the full norm comparison for EVERY finite reciprocal-balanced
completion of the fixed Möbius prefix. NJT-A2 then solves the resulting energy
tradeoff exactly, using a completion that knows the future Möbius coefficients.

It can kill the entire quadratic annular output, but only by paying exactly
one quarter of the native annular energy. This explicitly tests and blocks a
false finish in which the energy is merely moved into the completion or the
extended source is inserted without paying its norm. Its coefficients are
computable; it is their energy bound, not numerical access, that is missing.
The bound is sharp, not an unsupported
claim that all completions must have large output.

For the original cheap native crossing collar, CJB26's stronger bounded-error
identity remains the relevant special case. We did not prove its covariance
upper bound. The all-completion theorem says why changing coordinates or
optimizing an uncharged collar cannot supply that missing arithmetic input.

## What would constitute the actual completion

A proof of the literal native covariance bound would close the arithmetic
consumer at its inherited scope. Alternatively, an unbounded feasible joint
sequence in NJT4 would close the two analytic routes simultaneously. The
comparison class can include verified real-zero polynomial products as well
as genuine ferromagnets; positive coefficients alone are insufficient.

No claim here evaluates the unknown off-axis defect, proves a new zeta
zero-free region, or identifies the constructed chain with theta. The
remaining source-specific feasibility or cancellation theorem is not a
routine omitted reviewer check. The packet supplies actual component proofs
and conditional endings, not a complete proof of RH.
