# Executed validation and publication boundary

## Complete reconstruction

The full accepting reconstruction succeeds in ordinary and optimized Python,
using fresh COMPLETE theta integrals and complete infinite chain-tail bounds.
The canonical results.json SHA-256 is:

    ef30252c8b99690180c23ee6eba7443fb2a8aa5c5cd7b4e848125b1efcf56c59

## Completed test partitions

All SEVEN test methods passed in EACH Python mode, in five named partitions:

    python -B test_checks.py --partition unit
    python -B test_checks.py --partition source
    python -B test_checks.py --partition cli-a
    python -B test_checks.py --partition cli-b
    python -B test_checks.py --partition integrity

All five commands also passed with -O. Each mode completed one pristine actual
CLI acceptance and NINE actual CLI refusals, with no failures, errors or skips.
The source partition rebuilt both full meshes (1/128 and 1/160) and proved the
same exact chain-root box from each. These are two meshes of ONE backend,
not independent numerical implementations or mathematical review.

The refusals cover false RH status, Boolean/integer alias, false degree-eight
matching scope, altered whole-box residual, duplicate JSON, floating JSON,
a changed-and-resealed native theta coefficient, an unsealed proof edit, and
an unexpected file. The resealed coefficient reaches fresh source calculation
and fails the variance boundary. Proof/inventory refusals are integrity tests,
not numerical certificates.

Finite coverage: 144 squarefree-coprime injection tuples; all native crossings
Y<=17; 21 exact finite Markov models; 14,028 ordered fourth-cumulant terms;
96 sixth-cumulant partition cases; 570 positive density-jet identities;
540 score-product identities; 16 Gaussian-rational root-current identities;
and 18 positive-score panels with full infinite tails. The exact head-root
certificate includes all 16 rational inverse identities and the whole parameter
box. These are bounded checks, not counts of infinite theorems.

## Interrupted orchestration and subsequent complete partitions

An initial combined suite timed out in its CLI portion. Two diagnostic
combined/CLI runs were also interrupted. A concurrent partition orchestration
later timed out after finishing all normal partitions and the optimized source
partition. The unfinished optimized CLI and integrity partitions were then run
separately and passed. Only completed retained outputs are counted above.
Streaming container sessions were unavailable; no background validation service
or unfinished process is offered as evidence.

## Final delivery replay

After writing this record and resealing the manifest, both complete accepting
modes were rerun. An add-only minimal Git fixture patch replay preserves an
unrelated sentinel and reruns both modes. A clean extracted archive is also
replayed. External evidence records those executions without a circular packet
hash dependency. These are packet/fixture checks, NOT a full Riemann checkout,
repository-wide validator run, or a fixture falsely labeled as the real main.

## Mathematical limits

No all-crossing covariance upper bound, all-order Ising realization, explicit
positive-q radius, gamma production sign, actual new gamma zero/root track,
proof-assistant build, remote CI, second independent backend or independent
mathematical acceptance is claimed. All infinite analytic arguments, including
the fixed-step endpoint uniformization, remain proposed for independent review.
The complete eighth-moment mismatch is retained: the new chain is NOT Xi.

## Publication boundary

The GitHub connector exposed no write action. Plugin discovery did not provide
another usable writer, and direct Git failed DNS. No branch, commit or PR was
published by this session. Existing #869 is separate concurrent work, not this
session's publication. The supplied script prepares ONE add-only draft PR from
the pinned main in a temporary worktree, without force-pushing or editing main.

Runtime: 3.13.5 / Linux.
