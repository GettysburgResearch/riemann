# The three conditional endings and the attempted missing steps

**RH has not been proved in this packet.** This document separates proved
component proposals, finite evidence, imported results and the missing global
estimates. It does not assign those estimates to a reviewer as routine work.

## A. Native collision covariance

The new all-scale lower bound and the inherited upper bound identify the
complete diagonal D_Y at order (log Y)^4, rather than merely providing a loose
majorant. The entire energy obeys F_((Y+1)^2-1)<=F_Y+8/Y+2D_Y+2C_Y.

An upper bound C_Y<=K D_Y for fixed finite K on unbounded ACTUAL crossings,
together with XCC26's common energy exponent, forces Theta=1/2. Reflection
then gives RH. This covers arbitrary sparse successful crossings and does not
require an eventually nonpositive covariance or a prescribed squaring ladder.

Attempt made: preserve Liouville-coherent signs, inject actual coprime
squarefree product collisions, and estimate the full diagonal without throwing
away its local fibers. This proves a lower bound, not an upper bound on C_Y.
Taking absolute values over different products still loses the essential signed
cancellation. Generic sign-coherent countermodels from #848 remain applicable.
The missing statement is the native covariance-to-diagonal bound or an
alternative subpower/subquadratic energy gain.

## I. Growth-calibrated infinite ferromagnets

The exact four-parameter seed matches the complete theta cumulants 2,4,6 and
calibrated head mass. The analytic implicit-function theorem keeps these
constraints at positive q. Every resulting finite restriction is a true
nearest-neighbor ferromagnet; variance bounds give entire compactness and
Lee–Yang/Hurwitz give real zeros for its complete infinite transform.

To finish, produce a SEQUENCE of such admissible constructions, not necessarily
one fixed chain, that matches actual theta moments through orders tending to
infinity with errors tending to zero. Matching the variance bounds all entire
Taylor tails by the Lee–Yang product estimate. Thus all moments plus that
bound identify Phi, and the real-zero property passes to Phi without a
simplicity assumption. No positive mixture of candidate transforms is used.

Attempt made: multiple finite block searches; then infinite connected heads;
then a four-group head with the exact logarithmic tail and calibration. This
produced a complete-source six-moment theorem. A complete eighth-moment
calculation proves that the obtained small-q neighborhood is NOT theta.
Its error is negative, not zero. Local submersion or a nonsingular finite jet
does not show that the next native target is reachable, let alone all orders.

The independent q=0 root is a useful analytic base point, not a proposed
independent-spin completion. #854's independent-spin obstruction and #863's
bounded-lattice-cluster restriction are respected. #871's existing framework
is credited explicitly. No complete realization or universality theorem is
claimed for the new head architecture.

## G. Native gamma defect extinction

The source-exact shape path gives a formula for Delta_(N+1)-Delta_N, with
complete source score and all finite-step exceptional zero branches. Its
endpoint radius depends on N. The inherited uniform weighted-zero tail and
whole-source compact convergence identify lim Delta_N=Delta_Xi.

A cofinal block contraction
Delta_(N_(j+1))<=(1-a_j)Delta_(N_j)+b_j, with sum a_j=infinity and b_j/a_j->0,
would force that limit to vanish. All multiplicities survive. This gives RH
through the existing weighted defect equivalence. Absolute approximation,
finite-stage eventual real zeros, or convergence of successive differences
does not supply the contraction.

Attempt made: derive the literal nonperturbative gamma generator, then remove
the parameter-dependent gamma normalization through a positive-series score,
and transport the weighted roots including collisions. This obtains a balance,
not a favorable sign. No actual new root or current integral was computed.
The certified native N5 defect in #858 prohibits assuming that every finite
centered stage is real-rooted. #869's known-zero Bessel anchor addresses an
initial-baseline issue along its different path; it does not establish the
missing signed-production estimate either.

## A concrete connection between the analytic routes

For an Ising law, differentiation of a field-free edge parameter J_ij gives
partial_J E f=Cov(f,sigma_i sigma_j). For the native gamma projection the new
identity is partial_u E f=Cov(f,S_u). In both cases preserving lower moments
means imposing exact covariance constraints before asking the next observable
to move in a favorable direction. This suggests using the same constrained
Jacobian/annihilator methodology to study the gamma score, rather than treating
its formal positive source terms as a positive zero current.

This is a proposed methodological transfer, not a proof that either covariance
has the required sign. The arithmetic covariance is a different quadratic
form over ordinary product fibers; no equality between these three objects is
asserted. Their useful common lesson is to preserve the exact source relations
before taking an absolute value or replacing the source by a convenient model.
