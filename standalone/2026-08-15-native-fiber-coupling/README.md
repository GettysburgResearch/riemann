# Standalone review packet — native-fibre physical coupling

This packet is a successor to frozen PR #500. Read:

1. `R-91870`;
2. `L-91870` through `L-91873`;
3. `T-91870`;
4. `O-91870`;
5. the exact replay;
6. `IMPORT_MANIFEST.json` and the publication lock.

The load-bearing new statement is not another abstract Hall theorem. It is the
source marginal identity: the coupling begins from the paired native Möbius
Volterra measure, not from the enlarged finite-Euler rough lift.
