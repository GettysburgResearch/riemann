# Proof extract

The bulk source of the coupling is

\[
 d\Sigma_X^\pm(s,k)=\frac2s\mathbf1_{\mu(k)=\pm1}
 \ell_{X/s}(k)\,ds.
\]

The anchored source is the literal finite native Möbius source on the complementary integer cells. The exact finite row is anchored observation plus bulk observation plus the retained signed quadrature defect.

Restrict the source into bulk and anchored sectors. On the bulk, all active
colours are below 67 and share one positive infinitesimal packet; complete-graph
Hall cancellation uses every negative mass once and leaves residual mass
`L(X/s)`. On the anchored sector, the exact `P_61` stopping line, unique least
rough owner and complete Target-Lorenz leaf theorem produce positive rows with
one source owner per occurrence.

Place every owner once in the common anchored/bulk target. Apply
`I_anc direct-sum Q_bulk` and one scalar thinning. The result is one nonnegative
row. The finite defect and collar stay in the signed observation ledger.

The all-column estimates include `q<K`; ordinary responses are summed at `q`
and `4q` before detail is formed. The resulting detail complement is
nonnegative, ordinary feasibility follows by positive inversion, and the exact
native dual prices the slack below 60989. The one-sided finite dual and frozen
endpoint consumer give the proposed RH implication.
