# Review specification — T-102600

Frozen base:

```text
PR #715
99cc94c48bafd9b96141f7cef9f1e7aa83012747
```

Mandatory checks:

1. Verify every finite prefix identity in `L-102600`.
2. Keep the two labelled copies of 67 distinct.
3. Verify the logarithmic phase derivative and geometric series.
4. Recompute the higher-prime-power `O(log Y)` bound.
5. Derive both finite and infinite native/squared Dirichlet multipliers.
6. Check holomorphy of the squared term for `Re(s)>0`.
7. Verify the polylogarithmic `L1` bound from PR #715 `L-102505`.
8. Reconstruct the Duhamel identity coefficientwise.
9. Reject any argument that deletes the Duhamel current.
10. Confirm `AR-DEFECT102600` and RH remain unproved.

11. Verify the `1/|S|` Duhamel owner coefficient and distinguish same-product multiplicity from distinct-product near-collision.
