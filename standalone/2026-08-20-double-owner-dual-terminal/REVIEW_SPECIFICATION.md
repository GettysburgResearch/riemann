# Review specification — T100700

A valid reconstruction must distinguish:

```text
exact coefficient decomposition;
convex Hilbert inequality;
deterministic long-block normal form;
centered short-block energy;
final negative-part gluing.
```

Immediate rejection conditions:

- a block is assigned to both `SCME100704` and `LRNM100704`;
- the empty/root source is reintroduced for `X>8`;
- finite squaring touches an endpoint owner;
- a positive inverse is used after scalar collapse;
- the divisor sign is replaced by unsigned mass;
- a root-containing Hardy square is substituted for the centered moment tower;
- pairwise or finite-prime diagnostics are promoted to either terminal theorem.

The proposal is complete as an implication matrix, not as a proof of the two
terminal estimates. RH remains unproved.
