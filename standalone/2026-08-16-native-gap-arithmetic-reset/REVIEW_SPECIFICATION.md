# Review specification

A hostile reviewer should reconstruct:

1. the finite adjoint reindexing in `L-94200`;
2. the distinction between \(P_\Lambda\) and \(J_\Lambda\);
3. the explicit \(X=3\) counterexample and its rational inequalities;
4. the optimization separation in `L-94201`;
5. the logical scope of NEDB after the correction;
6. the exact PR #531 source lock;
7. the replay from clean standard-library Python.

Reject the packet if any theorem treats the cubic tail, First-Hermite pointwise
sign, Brownian stability, or RH as proved.
