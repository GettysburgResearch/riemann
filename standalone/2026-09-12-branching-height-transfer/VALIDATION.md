# BHT26 execution record

The analytic arguments are proposed paper proofs. No actual xi value, zero
location, contour integral, parent quadrature, or new finite-height census is
computed in this continuation. The claimed all-N conclusions depend on the
written inductions and analytic estimates, not extrapolation from finite tests.

The accepting checker reconstructs native moment and inverse-moment constants,
local Euler/Binet/Harnack guards, dyadic height/radius prescriptions, the two
all-N induction guards, a complete-remainder rational interval for the comparison
speed, and the exact polynomial in the synthetic nonclosure control. It imports
no parent numerical code and uses no third-party arithmetic package.

On the final source at this stage, all four commands below completed successfully.
Both accepting checker modes reconstructed fingerprint
`587e9a350155281b5d32af492f71a1ba3e563f51b8b3f32bcac051e6592def78`.
Each five-method test suite passed with zero failures/errors/skips, one pristine
copied CLI acceptance and ten executed CLI refusals. These are separate
executions of the SAME implementation, not independent mathematical reviews.
The final delivery receipt separately records clean-extraction and Git-fixture
replays.

```
python -I -S -B check.py --check result.json
python -I -S -B -O check.py --check result.json
python -I -S -B test_check.py
python -I -S -B -O test_check.py
```

The tests use one pristine copied-package CLI acceptance and ten actual altered
CLI refusals per mode. Resealed result changes include false RH and confinement
flags, a radius exponent, the inverse moment, and numeric aliases. Separate
cases reject duplicate JSON keys, proof drift, extra files, and missing files.
No symlink/platform test is claimed in this packet. Canonical typed comparison
is separate from byte authentication; arbitrary malicious replacement of the
whole proof/checker/manifest is not purportedly detected by those same files.

Local receipt production is labeled producer-only. SHA256SUMS authenticates
all eight other packet files and the inventory rejects extras. Readability
receipts are not accepted solely because they have been rehashed: the expected
results are recomputed. These controls do not machine-prove Jensen, gamma
bounds, the source order, or the cluster theorem.

The principal parent manuscript is matched to its live Git blob and byte count.
The supplied parent archive is used only as preserved context for an add-only
Git application test. No complete real repository checkout was obtained.

Publication was attempted by discovering create actions in the GitHub connector
(none returned), checking the installed GitHub plugin, and trying git ls-remote.
The latter failed DNS resolution. No remote write was performed. The current
parent was read through the authenticated connector; this is not a new research
PR or a verified head for this continuation.

Clean extraction and minimal Git application are file/replay checks, not a
whole-repository build. Exact outcomes and file hashes are in the external
execution/publication receipt. No Lean, remote CI, independent implementation,
mathematical acceptance, Windows run, or unbounded zero-confinement conclusion
is claimed.
