# Targeted review specification

The open estimate DCP26.OPEN1 is not part of the claimed proof. Do not infer
it from finite tests, totality of a changed family, or a Gram eigenvalue floor.

Load-bearing checks:

1. Recompute both rational transforms and the local finite identity d*h=f.
   Confirm that the only sacrificed target zero is real z=1/2 and that it
   excludes no nontrivial zeta zero. Confirm f is not advertised as outer.
2. Verify h_T is cut in TIME, not just at a Dirichlet coefficient index with
   unretained kernel tails. Verify y_T is in the original source space and
   equals f on its complete initial interval, but has a retained future tail.
3. Recompute the full cutoff correlation (12)--(18), both exact norms,
   and its diagonal bound. The untruncated correlation is not a replacement
   for the finite-L correlation.
4. Verify the entire future constraint Gram, conjugations, multiplicities,
   and asymptotic constant. Its sharpness is for the selected constraints,
   not a claim that the actual y_T minimizes every constraint simultaneously.
5. Check the dense-damping proof in both Hilbert weights. Its failure of
   contraction is for the changed output assignment, not for the true Xi
   convolution. Inspect the commutator and coefficient-cost distinction.
6. Recompute the Lagrange interpolation lower bound and explicit cutoff
   constants. This is a large-cost finite conditioning contract, not a
   convergence theorem for inverse-Gram projection values.
7. Verify that RH is used only in the reverse direction of the energy
   equivalence, through the explicitly imported Mertens consequence.
8. Replay the actual 63-cell directed finite calculation, preserving all
   dyadic interval directions. It bounds the inverse input norm, not the
   exact full output norm or its asymptotic growth.

No external novelty or independent acceptance is asserted. A correction
should receive a new version and identity rather than overwrite this freeze.
