# M-96200 — Hostile review protocol for the hardened affine–Volterra packet

Reject any claimed closure at the first failed item.

1. Verify the PR #541 identity \( \langle Y_4,\Omega-\Xi_d\rangle=P_\Lambda-\mathcal H(d)\).
2. Verify \(F_\Lambda=J_\Lambda-P_\Lambda\) and retain the \(X=3\) negative witness.
3. Reconstruct the affine cell identity independently.
4. Verify the hybrid row identity before any inequality.
5. Confirm ordinary \(q\) and \(4q\) are taken from one row.
6. Recompute the physical `<3457` bound as \(P_\Lambda-\mathcal H\), not \(J_\Lambda-\mathcal H\).
7. Reject the old T-94000 endpoint consumer.
8. Run the MPFR smoke test; reject singleton irrational intervals.
9. Run the complete MPFR 65-row workflow before accepting the tail theorem.
10. Reconstruct the actual anchored endpoint/source/path marginal; numerical tail positivity is not a source compiler.
11. Do not import PR #537's FRONTIER-CHAIN positivity without a global cross-product reservoir ledger.
12. Treat RH as unproved unless either the complete arithmetic gap or the fixed-row positivity theorem is independently established.
