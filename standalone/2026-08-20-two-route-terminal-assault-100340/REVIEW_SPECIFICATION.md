# Hostile review specification

Review in this order:

1. Verify the hereditary removal inequality in `L-100320`.
2. Verify the largest-label matching and duplicate-67 handling.
3. Check the right-continuous jump sign in `L-100321`.
4. Reconstruct the Cauchy phase kernel in `L-100330`.
5. Check the `X=mu` Jacobian and the factor `m^(1-2sigma)`.
6. Verify gcd extraction and the common-core Euler product.
7. Confirm that no statement marks `CDTG100320`, `PRBC100330`, or RH proved.
8. Reject any review that replaces the primitive pair by absolute values.
