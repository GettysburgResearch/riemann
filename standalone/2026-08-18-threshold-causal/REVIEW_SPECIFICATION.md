# Hostile review specification

Review in this order:

1. convolution identity identifying the root with the native annular scalar;
2. edgewise residual `(r-a)+a=r` in the paired source;
3. one-prime `r-2r^2` correction;
4. the elementary-symmetric lower bound in `R-97700`;
5. the Mertens comparison `z_W=z_X-log(2k)+o(1)`;
6. application to the exact schedule of PR #578;
7. high-least-prime expansion and its two-prime cutoff;
8. only then the conditional Mellin-Landau conclusion.

Reject any inference from owner uniqueness, endpoint descent, finite replay, or
contracted mass to scalar positivity.
