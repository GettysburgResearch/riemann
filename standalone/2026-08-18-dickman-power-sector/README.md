# Dickman power-sector paper

Build:

```bash
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

The paper proves the fixed-power least-prime sector and explicitly leaves the
subpower critical core, full CSHT67, root RBLPTE67, and RH open.
