# Hostile review specification

Review in this order:

1. Verify the simplex differentiation `u I_k'(u)=I_(k-1)(u-1)`.
2. Verify the uniform half-weight estimate for all fixed degrees.
3. Verify that the finite-P61 constant and remainder contribute only
   `o_theta(sqrt(Y))`.
4. Verify uniform prime-harmonic weak convergence on `[1,1/theta]`.
5. Confirm that the root threshold `67` is outside the fixed-power contract.
6. Reject any promotion from scalar transport to target-preserving CSHT67.
7. Confirm that RH is not claimed.
