# Validation and assurance boundary

This packet contains proposed written mathematical arguments and one complete
source-driven interval reconstruction. Same-author arithmetic agreement is
not independent mathematical review. The analytic IFT/Banach/Lee--Yang and
all-order implications are not machine-proved by these controls.

## Primitive reconstruction

The accepting command reconstructs the literal theta density on all 84 stated
cells and pays every omitted theta index and all four infinite time tails.
It encloses raw normalization and moments 2,4,6,8,10,12,14, plus the three real
Fourier arguments in PROOF (18). Source Taylor degree is 120 and dyadic precision
is 256 bits. The original interval primitives and old four-weight scalar-root
method are adapted from IMR26; this is not a second independent backend.
The previous result.json is neither read nor trusted by acceptance.

The six-by-six rational inverse is reconstructed, with all 36 inverse identities.
A whole-box Jacobian bound and full source-moment intervals prove the contraction
conditions. The positive-coupling statement uses the paper's uniform covariance
and cumulant bounds on the COMPLETE 270-spin law, not an enumeration or a
numerically evaluated positive-coupling root. The current physical weights are
exactly defined by those root problems, not rounded display values.

The old 28-spin root is freshly enclosed from the same source integrals. All
seven one-edge compensated derivatives are enclosed with strict negative sign.
The tripling separation and the 512-degree finite-obstruction remainder are
checked as exact rational inequalities after the Fourier enclosure. Moments
above order 14 are NOT computed; the whole real safe MGF bounds their tail.
Seventy-nine additional bounded rational controls check cumulants by direct
finite sign sums, Gaussian lift identities, triple-angle algebra and a genuinely
interacting two-spin witness. They are controls, not theorem counts.

## Commands and receipt

Final normal and optimized reconstruction commands:

```
python -I -S -B verify.py --check result.json
python -I -S -B -O verify.py --check result.json
python -I -S -B test_rejections.py --part 1
python -I -S -B test_rejections.py --part 2
python -I -S -B test_rejections.py --part 1 --optimized
python -I -S -B test_rejections.py --part 2 --optimized
```

The delivery receipt outside the repository packet records their actual exits
and output identities. The accepting path validates the exact inventory and
SHA256 manifest and then reconstructs its primitive result. JSON duplicate
keys, noninteger numeric spellings, and Python bool/int equality aliases are
not accepted. Tests include real resealed producer mutations changing the
native theta coefficient 4 to 5 and reversing the compensated derivative sign.
Those cases fail mathematical reconstruction, not just hashes. Hash/inventory
and Linux symlink controls have a separate, more limited purpose. Acceptance
does not use Python assert. The producer --emit intentionally has no approval
meaning. This is not a security audit of running arbitrary untrusted Python.

## Exploration and failures disclosed

Ordinary SciPy/mpmath scouts suggested multiplicities and approximate roots.
A Gaussian-plus-spin fit suggested replacing its Gaussian part with 256 small
spins. The final candidate is instead validated by the explicit polynomial
contraction proof. No scout output is accepted as evidence. Single-pair and
Curie--Weiss moment-14 searches did not yield a certified solution; their
floating failures do not refute those models. A few exploratory long scans
were interrupted and are not counted as completed campaigns.

The first trigonometric reduction chose a floor rather than a nearest integer;
the explicit |remainder|<=1 check refused it. The integer reduction was corrected
before any retained output. A first compensated-derivative implementation used
the opposite interpolation sign; the strict numerical sign check refused it.
The exact root-polynomial sign was corrected and is separately exercised by
the refusal suite. Neither failed prototype is represented as a passed check. A combined normal/optimized refusal-test command exceeded the execution timeout after the normal partition had completed; both optimized partitions subsequently completed in separate calls. The interrupted combined orchestration is not counted as a successful command.

No parent full test campaign, all-order model, external numerical zero census,
new zero-free region, full repository checkout/build, Lean/Comparator, native
Windows, remote CI or independent referee acceptance is claimed. The numerical
zero argument is only an IVT real zero and nonvanishing at its triple, not a
claim that the zero is first or simple. The bound m=256 and the total-coupling
constant 10^-24 are deliberately loose and not claimed optimal.
