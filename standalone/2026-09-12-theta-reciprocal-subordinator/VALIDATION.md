# Validation and limits

Status: same-author computational support for proposed paper theorems, not
independent mathematical acceptance. The executed arithmetic does not prove
Bernstein representation, the entire-xi product, the low-height completeness
input, or complete monotonicity of H.

## Primitive calculation

Both accepting modes freshly reconstruct three native theta Fourier enclosures
at 0,14,15. Each uses the entire 4096-cell/4097-node Simpson cover, four explicit
theta indices, exact derivative-polynomial recurrence and the full omitted
index and physical-time tails. All arithmetic is outward 192-bit integer/Fraction.
There is no binary64 calculation, zeta/zero evaluator, gamma oracle or numerical
heat-trace truncation in acceptance. The mean-square/Levy identities are paper
arguments, not numerical contour experiments.

The two modes also reconstruct the four exact all-time endpoint budgets,
the integer-residue spectral control, its -8 quadratic witness, the negative
reciprocal-factorial determinant, eight elementary Laplace moment identities,
seven exponential reciprocal controls and two exact origin controls. These
are bounded controls of formulas, not a count of independent infinite theorems.

A separate 2048-cell reconstruction gives overlapping complete enclosures at
all three Fourier arguments. This is a second mesh of the SAME implementation,
not an independent transcendental backend. The 4096-cell receipt remains the
accepting numerical record. Only a sign change in (14,15) is asserted; first-
zero status, simplicity and census completeness are not established by these
three integrals.

## External theorem

Platt--Trudgian Theorem1 is explicitly imported for completeness and centrality
of all nontrivial zeros through height30. Its much larger published verification
is not repeated here. Its theorem statement and Turing explanation were read in
parsed text and the PDF page image. A repository bounded-window certificate is
not substituted for that published premise. Above30 all zeros remain unknown
and are paid by the complete analytic tail bounds in PROOF Sections2--3.

## Acceptance and altered inputs

Commands:

    python -I -S -B check.py --check result.json
    python -I -S -B -O check.py --check result.json

Both complete normally, with byte-identical reconstruction. Each mode has one
pristine full acceptance followed by SEVEN actual changed-copy CLI refusals:
false RH status, false complete-monotonicity status, Boolean/integer alias,
changed native interval, duplicate JSON key, resealed producer change of a
literal theta coefficient, and an unsealed manuscript change. The semantic
cases are resealed and then evaluated by fresh numerical reconstruction;
the unsealed manuscript is rejected by authentication. The native producer
change is rejected by the changed source sign/result, not only by its checksum.
No `assert` implements the accepting contract.

Eight regular files and seven manifest hashes are required. Producer `--emit`,
mesh-only and controls-only modes do not authenticate and must not be reported
as substitutes for full `--check`. Output logs and mutation orchestration are
outside the eight-file research packet and included in the delivery archive.

## Delivery scope

The add-only patch is replayed in a fresh minimal Git fixture, preserves an
unrelated sentinel, matches every payload byte and runs the normal full checker.
A clean ZIP extraction runs the optimized full checker. These are bounded packet
roundtrips, not a complete riemann checkout, repository-wide validation, a Windows
run, remote CI or formal proof.

The active connector supplies repository reads but no discovered write action.
Plugin discovery found only the already installed GitHub provider for publication;
direct Git transport failed DNS resolution. The packet is prepared locally,
not represented as a pushed commit. The delivery receipt records the last
verified remote source and the actual local file hashes separately.

## Interrupted orchestration

An initial combined validation wrapper exceeded its tool wall-time allowance
and produced no complete adverse-test receipt; it is not counted as a completed
suite. Subsequent separately logged ordinary and optimized orchestrations each
completed all seven refusals. The final sealed packet was then replayed again.
The interruption is not represented as a mathematical failure or acceptance.

## Not executed or not proved

No high zero was computed; no new verification height, independent referee review,
all-order Ising realization, unweighted Hankel sign, translated all-center heat
sign or complete monotonicity theorem is claimed. The new global inequalities
are paper proofs with the explicitly imported finite census and new sign
certificate. Positivity of a Levy density is not the missing Thorin property.
