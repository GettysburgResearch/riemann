# Hostile review specification — T-92891

Freeze the successor head before review. Review the proof in the order below.

## Required reconstructions

1. Recompute every factor-67 Hall prefix and verify the target-normalized score
   and component-row monotonicity used by `L-92890`.
2. Verify the endpoint density is positive and that the retained support consists
   entirely of whole integer cells.
3. Reconstruct the measurable Hall integration with source labels and one global
   positive quantizer.
4. Confirm that every causal child row remains an internal colour and that the
   controlling files never replace `Xi(child)` by `Omega(child)`.
5. Recompute the retained-cell ordinary estimates at `q` and `4q`, then form
   detail; include every column `2<=q<K`.
6. Recompute the terminal overfill `<4452 X^-3/2` and omission reserve
   `>5033 X^-3/2`.
7. Audit the eight correction classes in `L-92892`. A single operation that
   actually requires state completion falsifies the zero-port theorem.
8. Recompute the four `Y_4` costs and the total `60989<61000`.
9. Reconstruct the finite dual inequality
   `F_Lambda <= J_Lambda-H(d)`.
10. Reconstruct the prime-square moat, exact Mellin poles and Landau converse.

## Immediate falsifiers

Reject the proposal if any of the following occurs:

```text
a child response is promoted to full child capacity;
a recursive child is exported by the controlling endpoint row;
numerical slack is assigned positive source mass;
one correction class has a nonzero unaccounted matrix demand;
detail is formed before ordinary q and 4q identities are summed;
a retained partial endpoint cell appears;
the terminal margin is nonpositive;
the native cost uses J_Lambda-4sqrt(X)=O(log X);
an off-line Mellin pole is cancelled or a positive-real pole is overlooked.
```

## Replay scope

The packet replay authenticates constants, paths, pins, and negative controls.
It does not replace the analytic or number-theoretic reconstruction above.
