# Standalone proof — live native arithmetic coupling

## 0. Frozen setting and status

The frozen predecessor is PR #509 at

```text
e01daee9cdfea35d2a7d2591f1df6c8080084119
```

and is not modified. This successor accepts the objections in reviews #501, #503 and #504 and imports PR #508 only at its directed AVLT/typed-leaf scope. It is a candidate-complete proposal on frozen inputs. RH remains unproved pending independent reconstruction.

For integer `X`, put

\[
K=\lfloor X/67\rfloor+1,\qquad W=10000,
\]

and let

\[
\mathcal I_X=\{K+2,\ldots,X-W-3\}.
\]

The exact native finite row is denoted `c_X`; its ordinary response, radix-four response and literal benchmark are

\[
C(c_X)=w_X,\qquad \Xi(c_X)=\Omega_X,\qquad \mathcal H(c_X)=J_\Lambda(X).
\]

No positivity of `c_X` is assumed.

## 1. Exact native two-sector source

Write the native divergence as

\[
d_X^\star(m)=\sum_{k\le X/m}\frac{\mu(k)}{\sqrt{km}}\log\frac{X}{km}.
\]

Split it into the literal anchored cells and retained complete cells:

\[
d_X^\star=d_{X,A}^\star+d_{X,I}^\star,
\qquad d_{X,I}^\star=\mathbf1_{\mathcal I_X}d_X^\star.
\]

Let `R` be the exact finite component-row map. Let `c_(X,A)` be the finite row generated by the anchored cells, `bar c_(X,I)` the Volterra row generated by the same retained cells and `E_X^I` their genuine finite/continuum defect. Then

\[
\boxed{c_X=c_{X,A}+\overline c_{X,I}+\mathcal R E_X^I.}
\tag{1.1}
\]

The terms have different types:

```text
c_(X,A):       literal finite native Möbius source;
bar c_(X,I):   native Volterra Möbius source;
R E_X^I:       signed observation comparison, never positive source.
```

### 1.1 Anchored occurrence measure

An anchored native occurrence is

\[
\alpha=(m,k,\varepsilon),\qquad
 a_\alpha=\frac1{\sqrt{km}}\log\frac X{km}>0,
\qquad \varepsilon=\mu(k)\in\{\pm1\}.
\]

Parity is a label, not a signed mass.

### 1.2 Volterra occurrence measure

Use the tagged complete-cell target

\[
\mathcal T_{X,B}=\coprod_{n\in\mathcal I_X}\{n\}\times[0,1).
\]

For `s=n+u`, `x=X/s` and

\[
\ell_x(k)=k^{-1/2}(2\sqrt{x/k}-1),
\]

define positive paired measures

\[
d\Sigma_{X,B}^{\pm}(s,k)=\frac2s
\mathbf1_{\mu(k)=\pm1}\ell_{X/s}(k)\,ds.
\tag{1.2}
\]

Every colour multiplies the same positive infinitesimal row `p_s`. Since `1<x<67`, every active root colour has all prime factors at most `61`; the bulk has no rough first owner.

Applying the exact native observations to (1.1) gives before estimates

\[
w_X=C(c_{X,A})+C(\overline c_{X,I})+v(E_X^I),
\]

\[
\Omega_X=\Xi(c_{X,A})+\Xi(\overline c_{X,I})+\mathcal D_4v(E_X^I),
\tag{1.3}
\]

and the analogous literal-score identity. The canonical `P_61` rough lift is not the parent marginal.

## 2. Exact PR #503 firewall

At

\[
(p,y,s,j)=(67,15,1005,14)
\]

the forbidden infinitesimal causal difference is

\[
D_{\rm inf}=p_{1005}(14)-67^{-1/2}p_{15}(14).
\]

Its exact radical form is

\[
4+\frac{15\sqrt{14}}{13}-\frac{15\sqrt{15}}7
-\frac1{91\sqrt{1005}}
-\frac{15\sqrt{14}-14\sqrt{15}}{13\sqrt{67}}.
\]

Directed rational square-root enclosures give

\[
\boxed{-\frac{184291}{10^9}<D_{\rm inf}< -\frac{184290}{10^9}<0.}
\tag{2.1}
\]

Thus a positive Volterra packet is not hereditary under that causal subtraction.

The successor excludes the operation by type:

```text
bulk p_s: terminal current packet, no rough owner, no causal subtraction;
rough arithmetic: anchored finite stopping tree and Target-Lorenz leaf.
```

For the same arithmetic parameters `(67,15)`, the lightweight replay finds Target-Lorenz cutoff `133`, score reserve `>2.88`, and minimum row reserve `>0.0095` at row `66`. This is diagnostic; the proof input is PR #508's directed AVLT.

## 3. Bulk rank-one incidence coupling

For a retained `s`, write `x=X/s`, `a_k=ell_x(k)`,

\[
P_+(x)=\sum_{\mu(e)=1}a_e,\qquad
P_-(x)=\sum_{\mu(o)=-1}a_o.
\]

Define

\[
\pi_s(o,e)=\frac{a_oa_e}{P_+(x)},
\qquad
r_s(e)=a_e\frac{P_+(x)-P_-(x)}{P_+(x)}.
\]

Then

\[
\sum_e\pi_s(o,e)=a_o,
\qquad
\sum_o\pi_s(o,e)+r_s(e)=a_e.
\tag{3.1}
\]

Every matched edge contributes `+pi_s p_s-pi_s p_s=0` simultaneously in target, score, every component row, ordinary `q`, ordinary `4q`, detail and intrinsic boundary coordinates. The residual observation is `L(x)p_s>=0` on the factor-67 window.

Let `E_s(dt)` be the physical endpoint placement of `p_s` and `Q_X^B(t,dj)` the one bulk B-spline kernel. The positive bulk output coupling is

\[
\boxed{d\Gamma_{X,B}(s,e,t,j)=
\tau_K\frac2s r_s(e)E_s(dt)Q_X^B(t,dj)\,ds,}
\tag{3.2}
\]

where

\[
\tau_K=\frac{\sqrt K}{\sqrt K+130}.
\]

The edge measure `2 pi_s ds/s` remains in the source audit and has zero physical output. No bulk record has a rough owner or a child.

## 4. Anchored stopping paths and typed leaves

Apply the exact paired `P_61` stopping line before physical observation. At a node of endpoint `Y`, the outgoing terms are the complete finite forcing and, for each `d|P_61` and least rough prime `p>=67`, the source-disjoint child of endpoint `Y/(dp)` and coefficient `(dp)^(-1/2)`. Repeat only on nonterminal children.

A finite terminal path is labelled

\[
\omega=(m;k;d_0,p_1,d_1,\ldots,p_r,d_r;\varepsilon;c)
\]

and has exact coefficient

\[
\boxed{\omega_X=a_\alpha
\prod_{h=1}^r(d_hp_h)^{-1/2}
\prod_{h=0}^r\kappa_h(c_h).}
\tag{4.1}
\]

Every factor is applied once. The label records the anchored cell, native colour and parity, ordered rough history, unique first owner, every small divisor, every current/child decision and terminal leaf type.

At a terminal one-prime leaf let `E_omega,O_omega` be the complete positive even and odd packets. PR #508 supplies literal Target-Lorenz coefficients

\[
0\le u_{\omega,e}\le1,
\qquad
\sum_eu_{\omega,e}T_\omega(e)=T_\omega(O).
\]

Use the identical `u_(omega,e)` in target, declared score and every row. Put

\[
U_\omega=\sum_eu_{\omega,e}E_{\omega,e},
\qquad \nu_\omega=E_\omega-U_\omega,
\]

\[
B_\omega=R(U_\omega)-R(O_\omega)\ge0,
\qquad
\sigma_\omega=S(O_\omega)-S(U_\omega)\ge0.
\]

The typed leaf is

\[
\boxed{G_\omega=(\nu_\omega;B_\omega;\sigma_\omega).}
\tag{4.2}
\]

It satisfies

\[
T(\nu_\omega)=T(E_\omega)-T(O_\omega),
\]

\[
S(\nu_\omega)=S(E_\omega)-S(O_\omega)+\sigma_\omega,
\]

\[
R(\nu_\omega)+B_\omega=R(E_\omega)-R(O_\omega).
\tag{4.3}
\]

`B_omega` is current-only and has zero source target by type. It is never exported.

Choose a positive incidence coupling with negative marginal `O_omega` and positive marginal `U_omega`. Then

\[
(\Pi_\omega)_-=O_\omega,
\qquad
(\Pi_\omega)_++\nu_\omega=E_\omega.
\tag{4.4}
\]

The residual and bonus use the composed same-index placement of their path. The scalar `omega_X` is applied once at placement entrance.

Define

\[
\Gamma_\omega^{res}=\omega_X\operatorname{Place}_\omega(R(\nu_\omega)),
\qquad
\Gamma_\omega^{bon}=\omega_X\operatorname{Place}_\omega(B_\omega),
\]

and

\[
\boxed{\Gamma_{X,A}=\tau_K\sum_{\omega}
(\Gamma_\omega^{res}+\Gamma_\omega^{bon}).}
\tag{4.5}
\]

The same `omega_X` multiplies target, score, every row and both ordinary columns.

## 5. Actual live coupling and sole quantizer

Use the disjoint physical target

\[
\mathcal T_X=\mathcal T_{X,A}\dotplus\mathcal T_{X,B}
\]

and exactly one Markov operator

\[
\boxed{Q_X^{nat}=I_A\oplus Q_X^B.}
\tag{5.1}
\]

It depends only on the physical target point, never on parity, Hall edge, Target-Lorenz cutoff, first owner, rough history or child label.

The live coupling is

\[
\boxed{\Gamma_X=\Gamma_{X,A}+\Gamma_{X,B},}
\tag{5.2}
\]

and its finite row is the output marginal

\[
\boxed{d_X(j)=\Gamma_X(\mathcal Z_X\times\mathcal T_X\times\{j\}).}
\tag{5.3}
\]

For each sign, retained incidence plus the explicit discard `(1-tau_K)Sigma_X^pm` equals the original native marginal. Ownership follows from (3.1) and (4.4), not from the uncoloured output row.

## 6. Coefficient fidelity through every coordinate

Every output record has exactly one scalar coefficient:

```text
bulk residual:       tau_K (2/s) r_s(e) ds;
anchored leaf:       tau_K omega_X;
leaf residual:       additionally (1-u_(omega,e));
leaf used incidence: u_(omega,e), only in the incidence/bonus identity.
```

No observation replaces these scalars. The score coordinate is a superordination with named surplus `sigma_omega`, not a false target-null positive equality packet.

Ordinary response is evaluated on the same total row:

\[
C_q(d_X)=\int C_q(Q_X^{nat}(t,\cdot))\,d\Gamma_X(t),
\]

and separately at `4q`. Only afterward define

\[
\boxed{\Xi_q(d_X)=C_q(d_X)-2C_{4q}(d_X).}
\tag{6.1}
\]

Thus the same source coefficient is visible in target, score, every row, `q`, `4q` and detail.

## 7. Signed comparison, all columns and native charge

The retained-cell defect, intrinsic collar and terminal comparison form a signed observation vector `e_X`. Positive thinning and literal omissions supply unused capacity `u_X`. They are separate ledgers.

For every `q>=2`, including `2<=q<K`, the frozen estimates give

\[
|v_q(E_X^I)|<\frac{57}{2q\sqrt K},
\]

\[
|\mathcal D_4v_q(E_X^I)|<\frac{171}{4q\sqrt K},
\]

\[
|\mathcal D_4v_q(C_X-E_X^I)|<\frac{971}{4q\sqrt K}.
\]

For `2<=q<=X/4`,

\[
\tau_K\left(1+\frac{129}{\sqrt K}\right)
=\frac{\sqrt K+129}{\sqrt K+130}<1.
\]

The top omission leaves terminal reserve

\[
(5033-4452)X^{-3/2}=581X^{-3/2}>0.
\]

Therefore

\[
\Xi_q(d_X)\le\Omega_X(q),
\qquad
C_q(d_X)\le w_X(q)
\quad(q\ge2),
\tag{7.1}
\]

the ordinary inequality following by positive radix-four inversion.

Put

\[
r_X^{(4)}=\Omega_X-\Xi(d_X)\ge0.
\]

The exact native dual gives

\[
\boxed{J_\Lambda(X)-\mathcal H(d_X)=
\sum_qY_4(q)r_X^{(4)}(q).}
\tag{7.2}
\]

Only four external classes are priced:

```text
common thinning                  <12012
nonterminal signed comparison       <4
terminal signed comparison       <48972
literal positive omissions          <1
--------------------------------------
native deficit                    <60989
```

Thus, for `X>=10^12`,

\[
\boxed{0\le J_\Lambda(X)-\mathcal H(d_X)<60989=o(\log^2X).}
\tag{7.3}
\]

No estimate of `J_Lambda(X)-4sqrt(X)` is used.

## 8. Endpoint composition

The exact finite dual has the one-sided orientation

\[
F_\Lambda(X)\le J_\Lambda(X)-\mathcal H(d_X).
\tag{8.1}
\]

Hence `F_Lambda(X)=o(log^2 X)`. On the frozen prime-square moat and Mellin-Landau inputs, the resulting eventual endpoint sign excludes every zero with real part greater than one half; the functional equation gives the proposed RH conclusion.

Those analytic inputs and the present coupling remain independent reconstruction obligations. Publication does not establish RH.