# Standalone proof extract — live arithmetic coupling

## Native source

The input is the exact hybrid native Möbius source of PR #509: literal finite
anchored occurrences plus complete-cell Volterra occurrences. The signed
retained-cell defect is a second observation ledger.

## Bulk

At a bulk endpoint all colours multiply the same positive infinitesimal packet.
The complete-graph Hall incidence has the exact odd and even marginals and
leaves residual mass `L(X/s)`. The packet is placed directly. It is never sent
through a causal difference.

## Anchored

The paired P61 stopping line retains the rough-prime orientation bit. Each
native occurrence has a finite path with one coefficient and one first owner.
At a terminal leaf the directed Target-Lorenz theorem supplies a single cutoff
vector `u`, residual source `nu`, score surplus `sigma` and current-only row
bonus `B`. The explicit incidence coupling has odd marginal `O` and positive
marginal `U`; residual plus selected mass equals `E`.

## Physical output

All anchored outputs and bulk residuals enter one disjoint target. One
block-diagonal quantizer—identity on anchored rows and the martingale kernel on
bulk cells—is applied after all labels are retained. The common thinning has an
explicit discard marginal.

The same product coefficient occurs in target, score, every component row,
ordinary q, ordinary 4q and all boundary coordinates. Detail is formed from the
same complete row only after q and 4q have been summed.

## Native consumer

The signed finite comparison is dominated by the unused capacity from thinning
and omissions. The exact detail complement is nonnegative, positive inversion
gives ordinary feasibility, and direct Y4 pairing costs less than 60989. The
one-sided endpoint consumer is then the frozen final step.
