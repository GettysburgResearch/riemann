# Hostile review addendum — gauge-covariant defect current

Review the new continuation in this order:

1. `L-102706` — verify both local homotopies and the exact ratio.
2. Check that `g_tau-1` and `g_tau^(-1)-1` have no constant or linear term.
3. Verify that the global transfer costs only `prod_p(1+C/p)`.
4. `L-102707` — reconstruct the square-root geodesic and its root-free tangent.
5. Verify `Phi_*=(2D-1)(A*_M A)` and the polarized Hankel formula.
6. `L-102708` — audit the tensor-product derivative energy, including the parallel derivative term of size `O(loglog Y)`.
7. `L-102709` — check that every regional projection is fixed independently of the homotopy parameter and source-owned.
8. `R-102701` — reproduce both prime-carrier asymptotics and reject separate-field Cauchy closure.
9. `T-102710` — confirm that `PHDNC102710` remains open and that no RH claim is made.

Immediate falsifiers:

```text
using a regional absolute value before gauge/carrier recombination;
claiming F_- or F_+ has subpower physical L2 energy;
forgetting the second labelled 67;
using an endpoint-dependent detector;
claiming the polylog gauge transfer proves the polarized current sign;
marking PHDNC102710 or RH as proved.
```