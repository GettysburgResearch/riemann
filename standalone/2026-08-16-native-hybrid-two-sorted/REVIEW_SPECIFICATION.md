# Review specification

Reject immediately if:

```text
q=2 edge score obstruction is absent;
any Hall bonus is promoted to a complete packet;
any Volterra p_s row is causally split;
row-first rough lift replaces the native marginal;
inner finite occurrences are not exhaustive;
AVLT leaves use different target/score/row coefficients;
row bonus receives a rough owner or child;
outer rank-one colour features differ;
top omission differs from the packet used in terminal pricing;
identity channels contribute finite/continuum error;
q<K is omitted;
q and 4q are assembled on different rows;
Y4 pricing uses a different row identifier;
endpoint inequality is reversed;
the benchmark bridge is imported.
```

Recompute the exact finite replay, but do not infer the analytic theorems from it.
