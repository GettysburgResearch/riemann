# Proof extract

The exact native row is split as

\[
c_X=c_{X,in}+\bar c_{X,ret}+\bar c_{X,top}+\mathcal R E_{X,out}.
\]

The finite inner term is represented by exact stopping-line leaves. Each leaf is a positive residual source plus a row-only bonus; only the residual has children. The outer colours all multiply the same positive infinitesimal row, so rank-one incidence cancellation leaves `L(X/s)p_s>=0` and no edge bonus. The Volterra row is never causally split.

One block operator realizes the inner row by identity and the retained outer row by one label-blind B-spline kernel. The top Volterra row is omitted positively. The exact comparison is

\[
c_X-d_X^0=\bar c_{X,top}+\mathcal R(E_{X,out}-C_{X,ret}).
\]

After one common thinning, all-column and terminal estimates give one nonnegative detail complement. The exact native dual prices that complement below `60989`; the frozen one-sided endpoint consumer yields the candidate RH conclusion. RH remains unproved pending reconstruction.
