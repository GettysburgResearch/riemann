# Native hybrid two-sorted factor-67 successor

This standalone front door reconstructs the arithmetic source before the physical row:

```text
exact inner finite source tree
 + exact outer paired Volterra source
 + signed adjacent defect
 -> positive residual sources and row-only bonuses
 -> rank-one positive outer row
 -> one block realization and one thinning
 -> one all-column response complement
 -> direct native Y4 price
 -> one-sided endpoint consumer.
```

Start with `R-91880`, then `L-91880` through `L-91883`, then `T-91880`, the import manifest, and `X-91880`.
