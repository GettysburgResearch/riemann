# Validation and limitations

Executed in the shared isolated research worktree, under this gamma directory:

```text
python -I -S -B check.py --write results.json
python -I -S -B check.py
python -I -S -B -O check.py
```

All three completed successfully. Normal and optimized verification reconstruct the same exact JSON content. This is one arithmetic implementation, not two independent reviews.

The script uses Python integers and fractions only. Its finite controls cover five rational evaluations of the head factorization, the exact tilted gamma Fisher integral and coarse whole-path constants, three genuinely positive finite Radau scale measures, the first lost cumulant sign and tangent coefficient, the complete remainder-law first-moment bound on those finite controls, the native M=2 reflected tangent polynomial, five collision panels with a corner in the defect, the inherited GE4 disk's rational contribution to the defect, and the optional published finite-height theorem's fourth-power-tail arithmetic consequence.

The native GE4 root center and radius are read as exact rational decimals. They are an IMPORTED disk specification; the program does not reconstruct its Fourier integral, its interval quadrature, or its Rouché margin. It verifies only the new inequality deduced from that disk. The Platt–Trudgian height is also an imported theorem input; its zero verification is not replayed.

No new native Fourier/Jensen computation, large-order Radau node solve, root census, full predecessor certificate campaign, formal proof, Lean build, repository build, or remote CI run is represented by these checks. There are no ordinary floating scouts in this packet. The analytic generator, weighted square-root chain rule, Sturm anchor, and the regularized integrated Jensen theorem are written proofs awaiting independent mathematical review.

The main output remains a research result with the explicit unresolved condition OPEN-GJV. Passing the bounded controls cannot turn that condition into a theorem.
