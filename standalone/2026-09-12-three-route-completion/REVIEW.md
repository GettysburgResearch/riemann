# Component scrutiny during this research pass

Status: same-team mathematical and implementation review of proposed work.
This is not external referee acceptance, formal verification or promotion to
the integrated record. Classical and predecessor inputs retain their stated
review boundaries. Each complete source remains available in this packet.

## Arithmetic

The root reviewer and the ferromagnetic-route reviewer independently checked
the Fourier/Chebyshev parity and degree budget, the smooth cutoff remainder,
the exponent condition `(1-b)r>1+3b/2`, the every-large-N quantifier, and the
Theta=1 endpoint. They checked the entire-trace Hilbert--Schmidt split, the
effective-rank inference, the odd/all-integer Mobius conversion and the finite
adapter constants. No defect was found in those components. The classical
generalized Littlewood implication remains a named input, reconstructed in
the frozen XCC26 source; neither review evaluated Theta.

The root also checked the retained-boundary paper: the exact safe-even-zeta
tail, its integer activation cancellation, all mixed terms in the surrogate,
the third-derivative constant, and the fixed-r coverage exponent. Its finite
checker was replayed in the combined run. No unbounded covariance estimate
was inferred from the finite data.

## Ferromagnetic graft

The root checked the exact edge-merger law and both preserved marginals,
Vandermonde determinant factors, component-polynomial row elimination,
dimer normalization and the compensated cumulant-to-moment derivative.
The six directed signs were reconstructed in the combined run, alongside
the independent finite-configuration and rational linear-solve controls.
The native application still assumes the exact root-existence theorem at
#863's quoted head; its theta quadrature was not repeated.

The opposite signs supplied by #863 and new #867 were not treated as a
connectedness theorem. Both the certified initial motion and failed longer
scouts retain their distinct scopes.

## Gamma

The root checked the anchored Laplace factorization and Bessel/Sturm scale,
the weighted Fisher and Poisson constants, reciprocal normalization, Jensen
coefficients, complete tail error and native reflected Mellin tangent.
The ferromagnetic-route reviewer independently scrutinized Sections 2--5.

That review identified an incomplete justification: uniform integrated
derivative bounds alone did not justify passing through finite-jump
approximations. The author replaced it by uniform convergence of the
generators and convolution semigroups on C_0(R), establishing the actual
continuous parameter derivative first. The product square root is then
regularized on compact t intervals, the actual integrable majorant permits
dominated convergence, and the R+1 weighted estimate removes the tails.
The reviewers reread the repair; no further defect was found in that scope.
The moving support endpoint and the order of the epsilon/R limits are
explicit. This does not establish the missing signed-production estimate.

The imported native-N=5 disk and optional published finite-height theorem
were not newly certified; only the stated rational consequences were checked.

After #868 appeared, the root separately checked LATEST_CONNECTION.md: the
same-G marginal derivative (without calling that coupling a martingale), both
Fisher bounds, Gamma(6,rate9) constant 4374, endpoint-support integration,
normalization, and cancellation of intermediate spectral tails in the
common-radius cumulative budget. No defect was found. This review does not
reproduce #868's local collision integral or identify its zero's lineage.

## LP feasibility bridge

The arithmetic-route reviewer checked the integer-occupancy argument,
Gaussian reserve, Jacobi probability construction, finite moment match and
open exclusion neighborhood, then replayed the exact checker. No mathematical
defect was found. A checker issue was found and fixed: Python's loose JSON
equality accepted Boolean/numeric aliases and duplicate keys. The checker now
rejects duplicate keys and compares type-preserving canonical encodings;
three mutation controls exercise the repair in both Python modes.

The synthetic source is explicitly separate from theta. Its finite
obstruction is not presented as an obstruction to the native Ising route.

## Freeze and execution binding

The final validation record includes SHA-256 values of the route proofs,
code and artifacts at replay. The PR identifies the frozen publication head.
These reviews concern the displayed load-bearing arguments and stated finite
checks; optional scouts do not acquire proof status through this review.
