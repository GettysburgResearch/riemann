# Factor-67 two-row parity-resummed paper v2

This packet supersedes the leafwise parity composition of paper v1. It retains
the complete grouped `P_61` source and introduces a reserve-preserving grouped
Hall theorem plus a finite positive-operator parity resummation.

```text
accepted proof: no
RH established: no
primary review target: L-96651 reserve-preserving grouped Hall
```

Build reproducibly:

```bash
./build.sh
```

Primary artifacts:

```text
main.tex
factor67-two-row-parity-resummed-v2.pdf
THEOREM_LEDGER.md
REVIEW_SPECIFICATION.md
```
