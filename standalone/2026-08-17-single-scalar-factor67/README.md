# Standalone single-scalar factor-67 proposal

The controlling document is `PROOF.md`. The packet is candidate-complete in
text, but the first hostile reconstruction target is the atomwise source
identity `L-97001.7`. RH is unproved pending review.
