# A finite paired-source decomposition for one zero-safe Möbius parabolic scalar

Define
\[
\mathcal R_X=5c_X(2)+3c_X(3).
\]
Its canonical base dictionary is `(15,6,3,6,6,...)`, hence positive. Its exact
Mellin transform is
\[
\frac6{s^2}-
\frac{3(1-2^{-z})(2-2^{-z})}{s^2\zeta(z)},
\qquad z=s+\tfrac12,
\]
and its numerator has no zero in `Re z>0`.

The positive parity-labelled native source is decomposed before signed
observation by the exact factor-67 causal identity. Each unresolved positive
child loses a factor at least 67 in scale, so finite rank induction terminates.
Unique factorization and the least-unused-rough-prime owner give one terminal
owner for each squarefree source occurrence and preserve its coefficient.

Complete grouped terminal leaves lie in the compact-plus-MPFR Target--Lorenz
domain. One common coefficient vector gives nonnegative rows 2 and 3, hence a
nonnegative `5:3` scalar. Applying the scalar observation to the finite terminal
source equality gives `mathcal R_X>=0`.

Landau's theorem then excludes every zeta zero with real part greater than one
half; functional-equation symmetry yields RH.

This is a proposed proof. Independent reconstruction must begin with the exact
atomwise finite source identity and terminal-domain match.
