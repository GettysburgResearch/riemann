# Review specification — T-102740

Mandatory reconstruction:

1. Expand `P_2 W_z` independently for `z=-1` and `z=-1/2`.
2. Verify positivity on every dyadic cell.
3. Reconstruct the boundary scaling constants `5/3` and `2`.
4. Verify the directed prime-mass bound including the second labelled `67`.
5. Recompute all finite cells and stationary points in `X-102740`.
6. Reconstruct the greatest-owner adjacent-level pairing.
7. Verify that the completion tangent is a positive combination of shifts of the native source.
8. Derive the general identity
   `JP2 Q_z=P2 Q+(2z-3)G+(z^2-1)P2 a`.
9. Reproduce both filtered-current inequalities.
10. Reproduce `R-102721` and reject any claim that two rays prove the full Lorentz current.
11. Confirm `FLC102740` and RH remain unproved.

Immediate falsifiers:

```text
omitting the second 67 label;
using a grid instead of cell extrema;
applying P2 to an arbitrary positive function;
subtracting the two ray inequalities;
claiming a second independent S-lemma reserve;
claiming RH from the replay.
```
