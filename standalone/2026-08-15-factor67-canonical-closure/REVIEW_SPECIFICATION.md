# Reconstruction specification

1. Confirm `R-91750` against PRs #482 and #484.
2. Reconstruct the full Möbius native normalization `L-91377/L-91378`.
3. Reconstruct the factor-67 positive labelled root decomposition.
4. Verify label preservation through the single positive quantizer.
5. Verify the decomposition-blind all-column realization from PR #479.
6. Reconstruct every term in the direct `Y_4` cost ledger of `L-91752`.
7. Confirm that no child colour is re-realized in `L-91753/T-91751`.
8. Independently reconstruct the prime-square moat and Mellin/Landau consumer.

Reject immediately upon any use of the old equality-score recurrence, any
`J_Lambda-4sqrt(X)` bridge, an uncovered physical column, duplicated source or
port, a second child quantizer, or a recursive replacement hidden inside the
one-shot row.
