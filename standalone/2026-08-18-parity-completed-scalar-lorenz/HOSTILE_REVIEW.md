# Hostile review appendix

## Immediate rejection tests

Reject the route upon any of the following:

1. a duplicate or missing first owner;
2. a source occurrence whose coefficient changes from `k^-1/2`;
3. an activation side omitted at a real knot;
4. an oriented child observed separately as a positive row;
5. canonical Hall applied after an odd rough history;
6. TP2 or Cauchy–Binet promoted to total target capacity;
7. scalar exactness promoted to simultaneous row-2/row-3 positivity;
8. target-active scalar-zero atoms omitted from the finite LP;
9. a terminal certificate used outside canonical orientation;
10. a finite diagnostic promoted to uniform `CPSL67`;
11. the Mellin substitution written with any factor other than `k^(-s-1/2)`;
12. the unit contribution `6/s^2` omitted;
13. positive-real removability or arbitrary zero multiplicity ignored;
14. eventual scalar positivity assumed under another name.

## Required reconstructions

1. Rebuild the completed-parity atom table directly from the literal Möbius source.
2. Verify the odd witness at `X=61841` and identify its global payer in any proposed certificate.
3. Recompute the finite Lorenz optimizer and one-parameter dual.
4. At a failed endpoint, publish the exact minimizing dual threshold and separator.
5. Reconstruct the compact/tail terminal certificate independently, including the `py=166000` ownership boundary.
6. Verify real-cell coverage and both one-sided activation limits.
7. Recompute the complete scalar Mellin transform and Landau hypotheses.

## Computation boundary

The included executable verifies exact finite algebra, exact rational LP identities, selected directed certificate inputs and finite literal-source diagnostics. It does not prove a universal quantifier. A long positive scan is evidence only; a single exact separator is a mathematical refutation of `CPSL67`.
