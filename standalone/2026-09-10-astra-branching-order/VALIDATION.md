# BOR26 validation and review boundary

The unbounded theorems are proposed written proofs, not consequences of finite
Python PASS markers. Independent review is required. RH and the Gamma(5/2)
orbit's complex zero-preservation property remain unproved.

## Numerical theorem and accepting primitives

The single numerical theorem is Section6's simple zero of the literal ONE-STEP
map from Gamma(1,1). The center and radius are exact rationals. Acceptance
reconstructs the defining beta/uniform expectation through all degrees0–600
and bounds the entire remaining series, and its s derivative, by
100*3^50*(3/4)^601. All 601 centered moments are computed afresh as Fractions;
no saved scout moments enter the computation. Shifted gamma and digamma use
23 Bernoulli terms, the complete order24 remainders, and64 recurrence factors.

Arithmetic is outward integer intervals at512 bits, compressed outward to256
bits for the retained rectangles. All elementary primitives are reconstructed
by series and integer square roots. PROOF.md supplies every analytic error
budget, including the sup-disk second derivative. There is no zeta oracle,
scout root verdict, quadrature estimate, float or assertion removed by -O in
acceptance. Integer gamma and complex recurrence controls exercise the backend;
they are not an independent implementation of it.

The source series, both reflected summands and the normalizing denominator are
kept distinct. The proof certifies the zeros of the numerator G; its positive
constant normalization does not change them. The reported point is NOT an xi
zero and says nothing adverse about the different Gamma(5/2) orbit.

## Bounded controls

- Beta/uniform moments0–12 and their exact matching through degree2;
  the density-crossing thresholds and beta–gamma factorization moments.
-33 exact Gamma(5/2) moment stages0–32 through order6, with the exact third
  moment/rate and an independently checked fourth-moment upper induction.
- Six finite rational third-order pairs, all piecewise Peano intervals,
  and their full transformations under an explicitly DECLARED three-atom
  scale law. The scale is synthetic in these controls, not substituted for
  the uniform in the numerical counterexample or paper theorem.
-13 low-order mixing moments reconstructed again from beta integrals;
  the root calculation independently extends the same specified law to600.
- Five elementary exp/log/sqrt controls, four integer gamma cases and four
  complex gamma/digamma recurrence cases, plus pi/trigonometric checks.

These are finite coverage units, not counts of analytic theorems. The exact
all-depth order uses interpolation and beta–gamma structure, not moment fits.

## Executed command record

The retained final command outcomes and mathematical fingerprint are also in
the external delivery receipt. The reproduction commands are

```
python -I -S -B check.py --check result.json
python -I -S -B -O check.py --check result.json
python -I -S -B test_check.py --part fast
python -I -S -B test_check.py --part full
python -I -S -B -O test_check.py --part fast
python -I -S -B -O test_check.py --part full
```

The two full accepting commands completed with identical canonical fingerprint
`bb4503ccee933477857bb8aa77ab2f185756f08489e9e1f19ac71827545976d1`.
The fast partition completed four test methods, one pristine CLI acceptance
and twelve actual CLI refusals per mode. The full partition completed one
method, one pristine full acceptance and two full-reconstruction refusals per
mode. Thus all five methods passed in each mode, with zero failures, errors or
skips; fourteen actual CLI refusals per mode were observed, not inferred.
The parent suite was not rerun. These executions use one numerical backend,
not independently authored implementations.

The test runner prints structured counts from unittest's actual result and
exits nonzero on any failure/error; a symlink privilege skip is reported and
not counted as a refusal. This avoids the parent's reported Windows issue.

There are nine regular files and eight checksum entries. Authentication
rejects extra/missing files, symlinks, hash drift, parent identity drift and
malformed manifests. The JSON reader rejects duplicates, floating/nonfinite
numbers and nonobjects. Canonical typed comparison rejects Boolean/integer
aliases. Resealed primitive modifications exercise mathematical reconstruction,
not merely matching a checksum. Two altered full reports are compared with
fresh complete root reconstructions. The tests do not prove resistance to an
adversary replacing both the entire trusted checker and its trust root.

## Reconnaissance and failed prototype commands

Ordinary scipy/mpmath scouts investigated the first Gamma(5/2) iterate and
several other gamma shapes. A finite search did not reveal an off-central
Gamma(5/2) first-iterate root; that is NOT a zero-free theorem or evidence for
an all-depth invariant. A monotone-modulus-ratio test failed numerically, so
it is not used as an induction premise. Density-grid/FFT higher-depth scouts
were unvalidated and are excluded from every theorem. The gamma1 root center
was selected with a high-precision mpmath calculation, then certified anew by
the defining expectation above. No scout values define accepting constants
other than the openly prescribed trial center.

A combined archive/patch delivery orchestration later timed out; it is not
counted as a completed command. Delivery checks were split into separately
logged commands, with their final outcomes in the external receipt.

Three prototype errors were detected and repaired before the final packet:
(1) an exact1/3 atanh radius guard rejected its outward rounded representation;
the final17/50 guard has its own complete series remainder;
(2) a real-interval/complex dispatch error was corrected without changing the
mathematical expression; (3) an extra hand-entered low-order mixing control
incorrectly expected17/60 instead of the exact11/40. The two independent beta
moment formulas already gave11/40; the erroneous test constant was corrected.
No failed command is counted as an executed PASS.

The preliminary direct root reconstruction succeeded, then the complete
producer including all low-order controls succeeded. Final sealed replays,
tests, clean extraction and patch checks are recorded separately in the
external receipt rather than silently inferred from these prototypes.

## Review checklist

1. Verify the three density switches and the interpolation sign on the full
   interval, including the discontinuity at1/4.
2. Check order preservation uses independent children but one shared U; retain
   moment matching and uniform integrability before passing to the fixed law.
3. Check the exact ideal-metric normalization1/6 and inverse-moment constant.
4. Check the complete complex Peano identity, two marginal replacements, and
   changing normalization in H_n. No innerness premise is available.
5. Independently examine all interval primitives, the full mixing-series
   derivative remainder, gamma phase/normalization, and Rouché disk margin.
6. Keep the Gamma1 counterexample separate from the prescribed Gamma5/2 orbit.
   The zero property of that latter orbit remains a substantive open theorem.

## Explicit unperformed work

No all-depth zero theorem, full Gamma(5/2) zero census, parent executable replay,
new actual-xi value, infinite spectral computation, large probability-law
campaign, complete Riemann checkout/build, Lean verification, remote CI result,
Windows execution, or independent referee acceptance is claimed. No original
source, branch, canonical/formal status, workflow or setting is modified by
this add-only packet. The original source identities and classical imports
remain at their stated mathematical scopes.
