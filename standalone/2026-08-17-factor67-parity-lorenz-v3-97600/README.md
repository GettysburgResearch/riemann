# Parity-completed factor-67 Lorenz reconstruction

This is the standalone source for the hostile reconstruction of PR #566 against
mandatory adversarial inputs PR #574 and PR #575.

The manuscript proves the complete owner/parity ledger, the alpha-recursion
coefficient defect, the finite scalar Lorenz primal/dual, and the full
Mellin-Landau consumer. It does **not** prove the uniform arithmetic producer
`CPSL67` and therefore does not prove RH.

See `IDENTIFIER_MIGRATION.md` for the transparent `97400` to `97600`
repository migration and the provenance of the exact author-rendered PDF.

Build:

```bash
latexmk -pdf -interaction=nonstopmode main.tex
```
