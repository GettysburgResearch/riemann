# Validation and evidence boundary

**PROPOSED; independent mathematical and implementation review remains required.** This packet contains a native-source phase refutation, not an RH proof.

## Final executed scope

The final normal and optimized commands are:

```
python -S -B check.py --full --self-test
python -S -B -O check.py --full --self-test
```

Both reconstruct the same complete typed result from the explicit sinh source and companion recursion and compare every reported ball/tail/parameter. They authenticate the packet's twelve SHA256 entries. The complete proof-producing evaluation covers 4092 consecutive degree-80 cells, the degree-384 origin expansions, both entire future tails, the special-function normalization and the final strict integer inequality. Arithmetic is standard-library integer/Fraction complex-ball arithmetic at 512 bits. The final code does not import NumPy, SciPy, mpmath or a zeta/gamma oracle.

In each mode the bounded controls also check:

- 720 Gaussian-rational enclosure instances, including diamond vertices, multiplication, sums and inverses;
- 26 exact source moments from independent reciprocal-series and Bernoulli formulas;
- 247 native linearized-mode coefficient identities, 25 neutral size-bias identities, 25 delay identities and 50 moment majorants;
- nine elementary-function special-value/functional-identity controls.

The finite mode-coefficient digest is `4a70c071d20d9e6e90acc8d940cfda19154c4f573653114cb9f1a5b955f3a61e`.

Eight altered receipts and one duplicate-key JSON string are refused in each self-test. They exercise the actual strict contract/canonical comparison functions, using the expected result from the full replay when `--full` is present. This is not nine separate CLI numerical replays. No assert implements acceptance. Typed canonical serialization prevents Boolean/number equality aliases.

## What the certificate establishes

The complete source is retained, but only **one phase value** is enclosed. The strict scaled interval lies inside (-4700000,-4600000). That suffices to refute the parent's universal positive phase target. It supplies no off-line zeta zero: the evaluated A is enclosed away from zero. It neither contradicts RH nor proves it.

The complete-source finite certificate and the infinite analytic derivations are distinct. The bounds in PROOF.md and CERTIFICATE.md, the source normalization, gamma remainder and code all require review. The same author wrote the producer and checker. An exact arithmetic execution is not a Lean/kernel proof or independent peer review.

## Retained development history

Three exploratory floating configurations agreed on a negative sign near 59.5+0.2i. They used integral recursions on rotated rays beyond the Laplace half-plane and have no certified error bound here. Their full author-session JSON/NPZ outputs are retained in the downloadable archive; selected data and limits appear in SCOUTS.json. They are not certificate inputs.

The first directed prototype used mpmath.iv.log and did not consistently convert complex absolute remainders to L1 radii. It is excluded from accepting evidence. The final implementation replaces that dependency with rational reductions/series and pays a factor two for every relevant complex remainder. Its complete new run still gives a strongly negative interval. The first foreground launch was interrupted by an execution timeout. A scout-summary schema mismatch was corrected before the final manifest; two preliminary packet jobs were stopped and restarted with the complete inventory. Only completed final runs count.

Higher-mode and neutral-derivative floating tests were exploratory, not certified refutations of those other modes. The integer-mode theorems are proved in the manuscript and tested through finite Taylor order; no infinite expansion completeness is asserted.

## Publication and unexecuted scope

The original parent proof was read and authenticated to Git blob `6bdbee938a521f118b0e9b2b7f4029f10bed7e13`. Its checker was not rerun. The new packet is confined to its own directory and branch; parent files, main, canonical/formal claims, workflows and settings are not modified.

The uploaded packet tree and all blob identities are compared with locally tested bytes before publication. A fresh archive extraction is checked separately. Publication readback is recorded outside this immutable packet in the delivered PUBLICATION.json.

No full authenticated Riemann checkout, repository-wide validator, remote workflow, Lean build, independent-author implementation, exhaustive external literature audit or independent mathematical acceptance was executed or claimed.
