# BFC26 validation boundary

Analytic claims are proposed paper proofs requiring independent review. The
checker does not machine-prove stochastic comparison, differentiability,
Jensen, zero continuation, or RH.

The exact bounded reconstruction reports 380 identity checks in its defined
counter, seven parameter panels through moment 14, response moments through
order 11, and 48 synthetic fold panels. Some predicates are grouped; these
are not 380 independent mathematical theorems.

Commands to execute on the final packet:

```
python -I -S -B check.py --check result.json
python -I -S -B -O check.py --check result.json
python -I -S -B test_check.py
python -I -S -B -O test_check.py
```

Expected mathematical fingerprint:
`e2c54fde456799747c6ce50fb7197344cc405a4bc683c9f9e1ad14e8fd2684a0`.

There are six unittest methods. Each full suite executes one pristine copied
CLI and twelve actual altered copies. The producer-fold and producer-response
mutations are rehashed and fail independently expressed algebraic identities.
The numerical assertions use Python integers/Fractions only: no floating
acceptance, quadrature, zeta oracle, root search or parent executable.

Execution status and exact logs are in the external delivery receipt, so
adding post-run platform records does not modify the authenticated packet.
No actual new zero, source homotopy density, collision, or global defect value
was evaluated. No parent numerical campaign, whole-repository build, Lean,
remote CI, Windows run or independent analytic acceptance is claimed.

The manifest protects the delivered byte inventory, not against arbitrary
coordinated replacement of the checker, manifest and trust anchor. No symlink
capability test is counted; the checker contains member-symlink refusal, but
its platform-specific operation is not part of these executed portable cases.

A minimal add-only Git fixture and a fresh archive extraction are delivery
checks, not complete repository validation. Their separately completed
commands, outcomes and file identities belong to the external receipt.
