# Independent review request — components only

RH is not proved in this packet. Reviewers are not being asked to supply the
missing complex zero-production sign as a routine final lemma.

## Load-bearing proof checks

1. **Fixed law and source conventions.** One multiplier acts on the sum of TWO
independent copies. Both multiplier laws have moments (1,1/2,7/24). Verify
normalization, unique mean-one fixed points, moment existence, and the exact
Brownian/pi scaling. Parameter theta=1 is not discrete iteration depth one.

2. **Order and full Peano kernel.** Reconstruct the three density switches;
matching moments through degree two; the sign of the third divided difference;
`integral kappa=1/960`; and the entire bound
`kappa(v)<=1024 v^(9/2)/(945 pi)` on [0,1]. No sampled sign is a premise.

3. **Response differentiability.** In the difference quotient retain the
changing multiplier law AND both changing children. Check the factor 2,
the bias c^3, the named C_b space, operator-norm convergence, and the uniform
tail `A_theta(t)<=16/(15t)`. The inverse norm is `80/(50-theta)`, not a
putative spectral norm of a Hilbert--Polya operator. Check mass
`chi_theta=56/(50-theta)^2` and the geometric random recursion.

4. **Complex response.** Pay both integral endpoints and the complete
inverse moment of X+Z. The first branch uses a THIRD-size-biased pair; the
continuing branch uses the c^3-biased multiplier. Check the bound 400 for
inverse exponents 2..4, the gamma ratio sign, and normalization derivatives.

5. **Whole-height spectral accounting.** Verify the larger strip, the
upper/lower transform bounds 4 and 1/20, the tangent conformal map, all
multiplicities, and the factor 5/16 in the omitted-zero tail. The weight
vanishes at the vertical critical-strip edges. Its continuity does not assert
that zeros never enter through those edges or from high heights.

6. **The attempted closing test.** At a real double zero in z coordinates,
`D'=-2 F_theta/F_zz=16 chi A/Q`. Positive D' is DEATH of a nonreal pair as theta
increases; negative D' is birth. Check both signs. No actual inequality A*Q>=0
is proved. Higher collisions and a full signed budget require separate work.

## Numerical and novelty boundaries

The replay is exact finite algebra, not a contour integration, root certificate,
source distribution computation or independent proof verification. The two
endpoint moment reconstructions and the response recurrence are independently
expressed but share an author and Python's Fraction implementation.

Classical beta/gamma, BPY, stochastic order, Banach/resolvent and Jensen machinery
is credited. A scoped PR search did not identify this specific continuation,
but it is not an exhaustive priority search. In particular #872's separate
fixed-source perturbation spectrum is not a new input or a zero spectrum here.

The earlier #859/#860 variants, #870's height-30 certificate and #874's
zero-cluster transfer remain unchanged and keep their prior proposed scopes.
No assertion about this new path is derived merely by moving one of those
certificates to a different source.
