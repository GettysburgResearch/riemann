# Hostile review specification

Reject the packet upon the first failure of any of:

1. the `P_61` Hall verifier omits a causal activation or uses a formal source above `py`;
2. the analytic prime-tail monotonicity is invoked without positive full reciprocal prefix and child margin;
3. separate target and score Hall flows are identified as one flow;
4. a rough integer has two least-prime ownership paths;
5. a reservoir fiber is counted in current and child capacity;
6. radix-four detail is inferred from ordinary positivity rather than the two-column identity;
7. endpoint correction is applied before the common parent sum or once per color;
8. `L-91364` is used beyond its frozen row range;
9. an imported endpoint estimate is promoted without reconstruction;
10. the downstream endpoint-to-RH sign is reversed.

Required next proof objects:

```text
exact finite row-gain certificate on the live P61 matrix;
exact native owner matrix including ports and Y4 weights;
end-to-end replay of the native—not canonical—packet.
```
