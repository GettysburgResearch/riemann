# Review specification

The first review target is `R-97700`: verify the product-safe cutoff
`Y=X^(1/(2r))`, the repeated-coordinate collision bound for the elementary
symmetric sum, the Mertens comparison `A_Y=Lambda_X-log(2r)+O(1)`, and the final
odd-layer sign at `L=O(log log log X)`. Then reconstruct the exact largest-prime
owner expansion in `L-97700`. The Type-II sum in `L-97701` must retain `mu(v)`
and `P^+(v)<p`; replacing it by an absolute norm does not review the theorem
stated here.
