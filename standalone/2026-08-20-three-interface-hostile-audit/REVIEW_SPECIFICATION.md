# T99600 hostile review specification

Reject the packet if any of the following occurs:

1. a compact Hall threshold below `67` is omitted;
2. square-root rounding is not outward and sign-aware;
3. a raw support indicator is substituted for the RN ratio;
4. the RN cocycle is broken across two successive children;
5. the alpha-child coefficient is called native after the one-prime separator;
6. future primes are discarded from the first-owner paired current;
7. cumulative parity is erased before signed observation;
8. a Bellman coboundary uses a child operator different from the native source;
9. the large-row asymptotic is treated as necessary after the two-row exact
   elimination;
10. `FCHD67`, Harnack negative mass, or RH is marked proved.

The packet is an exact hostile audit and corrected frontier, not a completed
proof of RH.
