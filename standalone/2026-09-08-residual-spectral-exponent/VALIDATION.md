# Execution and trust boundary

The analytic assertions RG1--RG4 are paper proofs requiring independent
mathematical review. No finite test below proves a reciprocal-zeta estimate,
a contour shift, an infinite norm bound, a limit, Theta=1/2, or RH.

## Bounded verifier

Python standard library only. Formal polynomials have exact Fraction
coefficients and independent prime-log symbols. Interval logarithms use
finite atanh series with explicit rational remainders and binary range
reduction. Periodic weights use outward 112-bit integer rounding of every
summand and a complete analytic infinite-tail allowance. Final reported
intervals are additionally rounded outward to denominator 2^112.

Seven named groups reconstruct 319 bounded fixtures per interpreter mode:

- 128 independent sieve versus trial-factor Mobius values;
- 27 exact formal jet and coefficient-norm identities at nine prefixes;
- 97 exact integer-cell horizon identities;
- 36 directed coefficient/moment/diagonal bounds at those prefixes;
- 20 rational strict-strip and tail exponent budgets;
- nine two-point Hardy identities at synthetic rational complex nodes;
- two complete periodic norm replays at Y=2 and Y=4, with all 4 and 24
  period cells and 4,096 summands per weight plus the full remaining tail.

The last examples are the unoptimized endpoint polynomials already present
in the earlier positive-residual packet. They are NOT the parent's four
optimized minima and are not new positive-range certificates. Their replay
checks the new source conventions, not the asymptotic theorem.

## Commands

Run with the exact sibling parent manuscript present:

```
python -I -S -B verify.py --check result.json
python -I -S -B -O verify.py --check result.json
python -I -S -B test_rejections.py
python -I -S -B test_rejections.py --optimized
```

The two verification outputs must be byte-identical. Each rejection suite
first accepts a pristine copied packet, then rejects ten modifications via
the actual command line: false RH and edge claims, Boolean/float count
aliases, changed parent reference, duplicate JSON, changed proof, extra
file, changed parent bytes, and a missing manifest. Result mutations are
resealed to exercise primitive reconstruction rather than only hashes.
Integrity rejection is not a mathematical verdict on a changed proof.

The exact nine-file inventory and eight manifest hashes are checked. The
parent's SHA-256 and Git blob identity are authenticated before mathematics
is reconstructed. The new verifier executes NO parent Python. Ordinary and
optimized final runs, all refusal controls, a minimal-context Git add-only
patch roundtrip, and clean archive replay are recorded in the delivery
receipts; completion is reported only for commands that actually finish.

## Limits

No new finite optimized solve, large-prime sweep, numerical zero, zeta
oracle, contour quadrature, external computation, remote CI, full repository
checkout/build, Lean/Comparator, independent referee verdict, or external
priority assessment is claimed. The broad web scan and Maier--Rassias
statement-page reading did not provide a native arithmetic upper bound.
The latter paper's numerical constants and proof are not inputs here.

The initial result writer used exact rational endpoint serialization; it
was changed to a final outward dyadic serialization for smaller delivery.
Both describe the same enclosing intervals, not a change to the mathematical
acceptance rule. Only final frozen output is counted as the execution record.
