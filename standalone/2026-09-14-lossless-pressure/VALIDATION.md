# Executed validation and scope

Date: 14 September 2026.

The following commands completed successfully on the local packet:

```
python -I verify.py --check results.json
python -I test_verify.py
python -I -O verify.py --check results.json
python -I -O test_verify.py
```

Both checker outputs reproduced the retained result exactly. Both seven-method suites passed. The ordinary suite took approximately 0.76 seconds and the optimized suite approximately 0.77 seconds; these times are environment-specific and not mathematical evidence.

The new accepting calculations use Python integers and `fractions.Fraction`, with explicit failure checks rather than `assert`. The trigonometric constants are enclosed by adjacent alternating rational sums. The majorant is a complete interval polynomial proof via seven positive Bernstein coefficients, not sampling. The spectrum and pair budgets are strict exact rational inequalities.

Tests also cover a longer independent truncation length of the same series, invalid majorants, an altered source capacity, partition implementation over 4,096 exact seven-point input lists, a Gram where clipping genuinely loses energy, and a changed retained-result file refused by the real CLI. Testing the partition implementation is not the proof of its all-size behavior; that proof is in the manuscript. Agreement at two truncation lengths is a consistency check, not independent arithmetic-backend validation.

## Explicit exclusions

- No new universal six-dimensional pressure search was executed.
- No replay of the 9,412,168-box case-J source certificate, the eight auxiliary source certificates, or the repository's older 713,315-node pressure search was executed.
- The source paper's case-J assertion is an inherited computational theorem requiring its own validation; checking the input weights and capacities does not establish it.
- No full checkout of the riemann repository, whole-repository build, or Lean run was performed.
- No independent mathematical reviewer participated in this session.
- No RH proof, new xi zero certificate, zero-verification-height record, or established worldwide priority is claimed.

## Reading scope

Main was frozen at `f99d9e3908dde4865377c75d9ca051c1f545bf4f`. The prior Pade continuation was confirmed in PR #885 at `26e54e14e8648dbc2cde6bf17058ca508e42dc70` by its current PR description; that full packet was not re-audited. The actual seven-point inequality and block deduction in `reviews/A/supplement/REPORT.md` (S01-S02) and the multiplicity bridge at the pinned #726 source were read. Thirty recent PR descriptions were surveyed, not all proofs audited.

The author-uploaded full text of Gebendorfer's draft v0.3, including its finite spectral estimates, analytic transfer, and Appendix A, was read. The requested Zenodo record and API did not load, so its full archive could not be rerun. The primary Lamzouri PDF's fixed-function pair-correlation and unweighting formulas were inspected, including rendered formulas; the base 2024 pair-correlation proof was not independently audited. The inspected Alpoge-Furman sections and the more limited Shi attribution are itemized in the manuscript.

The new argument does not use the previous gamma, branching, theta-Ising, or similarity-energy conjectural completions. It is a separate quantitative contribution based on existing proved/claimed-proved inputs, with their statuses kept explicit.
