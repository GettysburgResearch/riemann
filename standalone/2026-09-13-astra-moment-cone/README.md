# MCE26 — native matrix certificates instead of integer-root fitting

**Proposed component proofs and computer-assisted finite inequalities. Independent
mathematical/code review required. RH and the unbounded-dimension native estimate
are unproved.** Add-only continuation for the SAME PR #875, frozen parent
`177cf75e93b5614c5d5f0db1e4721ac69727e5ce`.

## Main outcomes

The native centered gamma stage F5 has a seven-by-seven shift-two cumulant
Hankel form with inertia (6,1), freshly certified from its complete defining
integral. A rational negative witness remains negative throughout the whole
coefficient box allowed by E14(32)<=2^-72. Thus that fixed approximant cannot
match an admissible real-zero product through raw degree 28 at that tolerance.
This improves the previous sufficient degree-512 obstruction and does not use
any numerical zero or its earlier certificate. It is NOT an obstruction to
actual Xi or RH.

Both size-eight native theta matrices are positive. A complete source-coupling
bound transfers this to EVERY centered gamma stage N>=2^192, at the same FIXED
matrix size. Neither near-optimality of this stage threshold nor unbounded
matrix size is asserted.

Two positive finite matrices construct a degree-eight rational logarithmic-
derivative comparator by exact rational linear algebra. Its poles/residues
have the correct signs. Its integrated fractional product need not be entire;
RATIONAL_BRIDGE.md gives a different, legitimate residual-based zero-exclusion
consumer, not an illicit application of the previous entire-product theorem.
No native successful residual window is claimed.

The full ending can be stated without integer root multiplicities or a
prescribed fit tolerance: cofinal native positive-semidefinite shift-two
matrices with BOTH stage and dimension tending to infinity are equivalent to
RH. This uses a classical moment mechanism, not a newly claimed qualitative
RH criterion. Its native unbounded-dimension premise remains open.

## Reading and execution

Read PROOF.md, then RATIONAL_BRIDGE.md and NUMERICS.md. SOURCES.json records
source pins and reading limits; VALIDATION.md separates actual full replays,
receipt-only checks, finite tests and analytical dependencies.

From this directory:

```bash
python -S -B check.py --check result.json --progress
python -S -B -O check.py --check result.json --progress
python -S -B test_checks.py
python -S -B -O test_checks.py
```

The first two commands freshly reconstruct BOTH complete sources. The suites
recompute an alternate complete theta mesh, bounded synthetic algebra, and
actual CLI refusals. Their pristine CLI control is explicitly receipt-only;
it does not repeat the gamma quadrature. `--receipt-only` always prints that
weaker scope. `--emit PATH` is producer-only and creates no acceptance claim.
No third-party Python dependency is needed.

All files in THIS sibling packet are authenticated by MANIFEST.json. It is not
placed inside CCF26's sealed root directory, whose original inventory rejects
previous nested additions already present in #875. No old checker, old source,
canonical/formal status, workflow or main-branch file is changed.

## What did not advance to a theorem

There is no new native arithmetic covariance upper bound. #875's sharp source-
completion energy payment remains in force. There is no new all-order Ising
realization, no global gamma-defect extinction theorem, no actual Xi zero
computed, and no independent referee or formal-proof acceptance. Positive
factorial-weighted matrices from #842 do not supply the unweighted matrices
required here. The full mathematical gap is stated in PROOF.md Section 6.
