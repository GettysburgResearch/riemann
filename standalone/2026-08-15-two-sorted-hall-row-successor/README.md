# Two-sorted Hall-row successor

This packet hostilely reconstructs frozen T-91821. It accepts the exact `x=2` obstruction: a target-null Hall edge bonus can be row-positive while its declared-score correction is negative. The bonus is therefore routed as a direct current physical row, not as a complete endpoint/source packet.

The target-bearing Hall residual alone receives rough first ownership, causal splitting, internal child placement, and the bulk B-spline quantizer. The bonus and exact finite anchors use identity channels. One block operator and one common thinning produce one final row. All-column complements, terminal reserve, and `Y_4` pricing refer to that same row.

RH remains unproved pending independent reconstruction.
