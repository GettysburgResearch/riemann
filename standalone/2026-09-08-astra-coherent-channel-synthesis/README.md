# Coherent-channel synthesis: exact root cost and the missing native estimate

Status: PROPOSED COMPONENT THEOREMS; independent mathematical/code review needed.
The requested full unconditional RH proof is NOT obtained. No canonical result,
review verdict or source branch is altered by this separate research packet.

## Start here

Read [SYNTHESIS.md](SYNTHESIS.md) for the selected post-integration comparison,
[PROOF.md](PROOF.md) for the new sharp anchored-inverse theorem, and
[COHERENT_SOURCE.md](COHERENT_SOURCE.md) for the exact native source adapter and
its still-open final estimate. Source versions/depths are in SOURCE_LOCK.json;
commands and unperformed work are in VALIDATION.md.

## Positive all-scale result

On the complete squarefree divisor box generated by all primes <=P, the optimal
ANCHORED Poincare constant is

    A_P=(6/pi^2)log log P+O(1),

whereas the CENTERED constant on the same graph is exactly 2/(3log2).
For every finite box the anchored constant and extremizer have a complete
one-dimensional monotone secular formula; no large matrix inverse is required.
Its root Green statistic K_P differs from A_P by O(1/K_P). A further corollary
gives a uniformly asymptotically exponential rooted survival law at scale A_P.
These are proved with elementary squarefree counts and positive Euler estimates,
not with PNT, RH or numerical zeta information. The logarithmic loss in #825's
all-support ANCHORED theorem is therefore of the right order. This does not
refute a constant CENTERED gap for a larger class of supports.

## Exact connection to the unresolved arithmetic

For the literal vertex field v_n=mu(n) S_(log n) exp(-t/2), the original native
norm is EXACTLY the harmonic projection killed by the graph gradient:

    ||Pi_N v||^2=J_N/H_N,
    J_N=sum_(k<N)M(k)^2/[k(k+1)]+M(N)^2/N.

Its COMPLETE prime-power edge energy is (6/pi^2)N log N+O(N), unconditionally.
Thus the graph gap does not bound J_N from above. This diagnoses the attempted
cross-route composition on the actual same source, not merely on a counterfeit.
A complete delayed-Laplace argument shows that a subpower unbounded subsequence
of J_N would imply RH. That upper bound, equivalently the stated signed-work
estimate, remains unproved. Neither positivity nor exact tail certification
is substituted for it.

## Bounded replay

```bash
python -I -S -B check.py
python -I -S -B -O check.py
python -I -S -B test_check.py
python -I -S -B -O test_check.py
```

`check.py --emit` is a PRODUCER that deliberately does not authenticate the
package. Default checking authenticates the exact ten-file inventory, all nine
manifest entries, literal source-lock keys, and an independently reconstructed
result. Results include six full finite anchored-root intervals, four product
spectral panels, primitive Mobius/divisor checks through256, and thirteen native
full-energy panels. All finite tails in those native norms are exact.

Classical product Markov chains, Green functions, rank-one algebra, Euler
products and Mobius inversion are credited. No exhaustive novelty/priority
assessment, independent referee acceptance, full-repository audit, Lean build,
broad campaign or RH proof is claimed.
