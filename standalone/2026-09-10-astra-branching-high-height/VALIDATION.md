# BHH26 validation and exact evidence boundaries

**The all-depth analytic conclusions are proposed paper proofs. The checker
validates finite identities and delivery, not the analytic theorem or RH.**
Source freeze and classical imports are in SOURCES.json.

## Primitive reconstruction

Python standard library only: integers, Fraction, hashlib and JSON. No floating
arithmetic, zeta/gamma evaluator, supplied zero, eigenvalue solver, contour
quadrature, or parent numerical receipt enters acceptance. No assert controls
acceptance. The receipt is reconstructed from the primitive recurrence.

Four groups are retained separately, not combined into a theorem count:

* Six raw branching moment updates through order12, independently compared to
  the Taylor coefficient identity for the differential-dilation equation:
  78 exact coefficient identities.
* Thirteen leading-polynomial rows n=0,...,12, independently normalized by
  rational local ODE coefficients. Seventy Gaussian-rational evaluations
  test the stated modulus and logarithmic-derivative consequences. The all-n
  coefficient theorem is proved analytically, not inferred from these rows.
* Both complete negative-power systems at depthone, including the derivative
  of the first logarithmic term. The total leading-comparison value5/64 is
  explicitly distinguished from the complete pair value1.
* Forty synthetic gamma rising-factorial ratio identities. These are declared
  finite algebra controls, not numerical actual-source asymptotic evaluations.

Strict acceptance rejects duplicate JSON, floating/Boolean numeric aliases,
nonfinite numbers, missing/extra files, malformed/duplicate hashes, symlinks,
source drift and any reconstructed-result discrepancy. SHA256SUMS covers the
seven files other than itself. A fully malicious rewrite of both producer and
checker is outside this self-authentication contract; independent mathematical
and code review remains required.

## Commands

From this directory, the intended accepting commands are:

```
python -I -S -B check.py --check result.json
python -I -S -B -O check.py --check result.json
python -I -S -B test_check.py
python -I -S -B -O test_check.py
```

`check.py --write result.json` is producer-only and does not authenticate a
package. It must not be counted as an accepting check.

The test suite contains four methods. Its CLI method first accepts a pristine
copied package, then invokes eleven actual refusals: seven resealed semantic
receipt changes, one resealed duplicate-key receipt, one resealed producer
mutation changing the shared-uniform coefficient2 to3, one unsealed proof
change, and one extra file. The producer mutation is caught by independent
raw-moment/differential identity reconstruction, not merely by a changed hash.
The process exit status is determined by unittest success; failed subtests do
not print a blanket success count. There is no symlink capability subtest.

## Discovery, interruptions and unperformed work

Exploratory mpmath80/90 calculations used400 exact rational mixing coefficients
from the first-depth law to inspect phase, a few root starts, and the ratio to
the proposed leading asymptotic at heights40,...,140. The observed ratios were
compatible with a one-over-height error but are NOT evidence for that bound.
An attempted root scout was interrupted by the command timeout; it is not a
zero census or an accepting computation. Parent moment helpers were used for
that scout only; no parent numerical accepting suite was rerun.

The proof's contour argument is written and estimates the complete ray. It
was NOT evaluated numerically. No finite T_n, gamma phase margin at a specific
high height, or actual high zero was computed. No numerical result is used to
infer eventual or unbounded-depth confinement.

No full Riemann checkout/build, predecessor suite, actual xi-zero computation,
large moment campaign, Lean/Comparator, Windows run, remote CI success,
independent referee acceptance, or external originality survey is claimed.
The source-defined comparison in Section4 is not a probability approximation:
its mass differs from one and its full remainder is retained.

## Final executed outcome

Both listed check.py modes completed and reconstructed identical fingerprint
`e0aefda524f0a2841b12f59d2078d402109cf4cb0854a18b07975653a948b9a8`.
All four unittest methods completed in each mode with zero failures, errors or
skips. Each mode accepted one pristine copied package and executed all eleven
refusals. The polynomial group contains78 coefficient-growth comparisons,
104 independently normalized leading-ODE comparisons, and70 Gaussian-rational
points; overlapping finite assertions are not added as independent theorems.

The external execution/publication receipt records the final clean archive
extraction, add-only temporary-Git roundtrip, and remote byte readback. Those
operations are not a full repository checkout, and no hypothetical test run
is counted as performed. Publication receipts themselves are not analytic
acceptance.
