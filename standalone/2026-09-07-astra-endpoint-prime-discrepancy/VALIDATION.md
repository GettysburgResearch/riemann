# Executed validation and limits

Arithmetic class: EXACT_RATIONAL. The bounded checker uses Python's standard
library only. The paper arguments use classical Fourier, Hardy and prime-
counting facts stated in SOURCES.json; they are not machine-verified.

## Executed

- Both normal and optimized Python reconstruct identical complete outputs:
  five named groups, 115 bounded checks. The 72 Laguerre controls are finite
  normalization cases, not 72 different zero theorems.
- The multiplier integral is independently assembled from three exact
  polynomial pieces; its all-parameter constant is verified coefficientwise.
- Sixteen signed rational atomic packets test the full physical Gram against
  its tail-square factorization. One exact normalization check at X=2 uses
  the actual prime-2 amplitude squared. No higher-X actual discrepancy integral
  or actual nonlinear entropy integral is evaluated.
- A prepublication edge-case pass changed the band lemma to a non-strict
  inequality at the zero measure and added an exact zero-measure fixture.
  The actual-source theorems and constants are unaffected.
- The actual CLIs reject four altered results and four changed packages in
  each Python mode: sixteen subprocess refusals, plus four pristine controls.
- Combined replay/rejection commands exceeded the command time limit,
  including after the zero-measure addition. Their incomplete outputs are
  not PASS records. Final rejection runs were partitioned by Python mode;
  both completed, and their combined record matched the retained results.
- SHA256SUMS covers exactly the other thirteen files in the fourteen-file
  packet. Missing, extra, changed or symlinked members reject.
- The two parent manuscript bytes were read and checked during authoring:
  EFB26 PROOF.md is blob 9fd9ead5d3d765ea1a6213ac931a216a6ac49b3f;
  OEC26 PROOF.md is blob f8e74b694038176a2d5bc426832158fcde12d094.
  Their SHA256 identities and lengths are in SOURCES.json. This validator
  authenticates the NEW packet, not a remote parent checkout.

## Not performed or claimed

No full repository checkout, Lean build, formal/comparator run, remote CI,
large prime/zero campaign, numerical entropy quadrature, independent referee
acceptance, or exhaustive novelty assessment is claimed. The classical
PNT constants used qualitatively are not numerically certified here. No
unconditional subpower upper bound or full RH proof is supplied.

A direct Git read failed DNS resolution; publication, when completed, uses
GitHub Git-data objects and a non-force branch update. The final remote head,
comparison and tree readback are recorded in the external publication receipt,
not invented inside this self-authenticating packet.

Any patch roundtrip is a temporary bounded Git fixture, not a full repository
replay. The parent mathematical checkers are not freshly run in this pass.
