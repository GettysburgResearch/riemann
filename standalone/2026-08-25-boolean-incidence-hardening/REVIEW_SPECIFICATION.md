# Hostile review specification — T-102980/T-102990

## Frozen research targets

```text
PR #719 packet parent:
  da01ed97e6901ea70d7ff5c8c3b6073d4ec8f7e1

corrected PR #751:
  dbf78e950130a8bbfb00c4e1b03645954485b929

F1/Pluecker PR #730:
  d385097bfcd19ab6f9015b522eacd5b97bc7e1b5
```

## Mandatory reconstruction

1. Derive `a>Y^(1/3)` from the two disjoint Boolean factors `r,s>floor(Y^(1/6))`.
2. Reconstruct the exceptional-sector contradiction and verify the finite cutoff `Y<=64`.
3. Check `P<=a`, `Q<=b`, and then derive `Q<=2a`, `P<=2b` from the ratio-eight support.
4. Recompute the masked one- and two-family phase identities with complex masks.
5. Verify that the divisor-count proof uses only `|mask|<=1` and does not assume masks are independent of the modulus.
6. Reconstruct the asymmetric normalization `s_(ell,rho)` and prove both the positive-moment and transpose-signature identities.
7. Recompute the minimum/equal-pair star and cycle energies.
8. Verify the identification of the star with radial owner currents and the cycle with row-zero Pluecker rectangles at exact source scope.
9. Check that every cofinal Boolean balanced label is completion-safe before accepting the equal-pair bypass.
10. Verify that endpoint phase placement is source-owned; do not use a pair-dependent phase selector as though it were a linear source partition.
11. Confirm that `COCURL102980`, `BCI102990`, and RH remain unproved.

## Immediate falsifiers

```text
retaining an exceptional co-owner tail after L-102957;
using only the smaller owner when the full product is already core-paid;
removing owner masks before physical observation;
spending the selected owner weight in both a fixed-pair and a coherent Cauchy step;
claiming that the total positive moment is necessary for a one-sided Gram bound;
claiming that minimum/equal pair transfer is subcritical merely because it is polylogarithmic;
endpoint-placing a phase label selected from the other physical term without an exact incidence partition;
claiming BCI102990 or RH from either finite replay.
```

## Scientific boundary

This packet proves source geometry, masked analytic transport, and exact owner-gauge normal forms. It does not prove the coherent coprime Boolean core estimate or RH.