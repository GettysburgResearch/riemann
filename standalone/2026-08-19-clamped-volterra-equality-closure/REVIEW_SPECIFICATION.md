# T99550 review specification

Review in this order:

1. `L-99550`: recompute the Euler operator on \(y\log y\) and
   \(\sqrt y\log y\).
2. Solve the two activation equations and confirm uniqueness of
   \((-12,12)\).
3. Reintegrate the Green kernel against \(2\sqrt t-1\).
4. Check the scaling law \(V_x[\Phi(x/n)]=n^{-1/2}V\Phi(x/n)\).
5. Verify that zero extension creates no derivative-jump atom.
6. Recompute the lower-boundary coefficients from `L-99230.3`.
7. Confirm the physical endpoint packet is double clamped.
8. Reject any inference that the packet replayed inherited Hall, terminal, or
   Mellin campaigns.

A failure at steps 1--7 retracts the local theorem. Step 8 is a scope firewall.
