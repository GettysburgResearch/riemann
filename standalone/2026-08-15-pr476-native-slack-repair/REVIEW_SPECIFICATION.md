# Reconstruction specification

1. Confirm `R-91726` against PR #480.
2. Reconstruct `L-91726` with the actual SHARP target and complete child labels.
3. Reconstruct the common-parent packet identity `L-91730` and slack cocycle
   `L-91727`.
4. Reconstruct PR #479's adjacent-cell owner split, all-column reserve,
   activation-knot collars and common port.
5. Audit every sparse-`Y_4` bound and the native thinning cost in `L-91728`.
6. Verify `L-91729` in the same normalized space as the complete correction map.
7. Reconstruct the one-sided endpoint consumer independently.

Reject immediately upon any use of the old equality-score recurrence, any
uncovered `q<K` column, grouping across incompatible `U` maps, omission after
the split, interpolation across an activation knot, port duplication, or use of
`L-92114` before RH.
