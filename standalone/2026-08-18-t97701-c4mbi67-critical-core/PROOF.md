# The C4MBI67 critical core

The annular `5:3` scalar is

\[
\mathcal A_X=6H_X(1)+
\sum_{n\ge1}\frac{\mu(n)}{\sqrt n}
\left[-6H_X(n)+\frac9{\sqrt2}H_{X/2}(n)-\frac32H_{X/4}(n)\right].
\]

Finite Fubini converts the Möbius term to the four exact bands in `L-97702`.
The Bellman identity converts root BLPTE67 to `A_X>=0`, so the remaining sign is
exactly C4MBI67.  `L-97703` gives a coefficient-exact prime-Möbius owner form.
`R-97701` prohibits replacing its signed covariance by source-blind unsigned
information.

The sign remains open.  RH is unproved.
