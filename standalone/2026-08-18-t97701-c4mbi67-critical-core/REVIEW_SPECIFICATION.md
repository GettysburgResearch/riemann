# Review specification

Reject the packet if any coefficient in the four-band vector is wrong, if the
unit boundary atom is omitted, if logarithmic owner weights do not sum to one,
if a state-wise theorem is inferred from the root equivalence, or if unsigned
norm information is substituted for the signed Möbius covariance.
