# T-97701 C4MBI67 critical-core reconstruction

This is the actual publication of the T-97701 work announced but absent from PR
#590. It contains the claim files, exact checker, complete TeX source, readable
manuscript, and a canonical compact PDF with exact repository bytes.

```text
four-band identity                    exact
prime-Mobius owner decomposition      exact
BLPTE67 root equivalence              exact
C4MBI67 sign                          open / RH-bearing
RH                                    unproved
```

Verify and inspect the canonical repository PDF:

```bash
./build.sh
```

The full conference-style source remains in `main.tex`. The committed compact
PDF is deliberately ASCII-safe so its Git blob, Drive mirror, sandbox artifact,
and checksum ledger can be compared byte-for-byte without a binary transport
ambiguity.
