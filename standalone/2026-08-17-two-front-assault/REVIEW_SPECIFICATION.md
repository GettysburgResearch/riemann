# M-96300 — Hostile review protocol for the two-front assault

Reject or narrow the packet at the first failure below.

1. Re-derive the scaled second-difference kernel and verify all exponential factors.
2. Check that `kappa_h` is nonnegative and supported exactly on `[0,2h]`.
3. Verify the arithmetic atom-minus-cell formula from the signed renewal measure.
4. Verify the Mellin multiplier `(1-e^{-hs})^2` and its nonvanishing in `Re(s)>0`.
5. Recompute the row-2 and row-3 base dictionaries from the canonical row map.
6. Verify `q_*=(15,6,3,6,6,...)` and no hidden negative coefficient.
7. Verify `5P_2+3P_3=-3(2^{-z}-1)(2^{-z}-2)`.
8. Check the single-row Landau hypotheses, including compact initial corrections.
9. Reindex the exact convolution `R_X=sum q_*(m)m^{-1/2}V(X/m)`.
10. Verify the reciprocal-potential Mellin transform and the delay equation distributionally.
11. Verify the Volterra-density Mellin transform and differential bridge.
12. Verify the signed affine moment cone in both directions, including `M=0`.
13. Reconstruct the uniform large-row Euler--Maclaurin remainder.
14. Do not promote diagnostics for `ACTQ_h`, `SPRP`, `VRP`, or `AMCP` into proofs.
15. RH remains unproved unless one complete producer and its analytic consumer independently pass.
