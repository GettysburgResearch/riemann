# Validation and explicit nonclaims

This file separates a paper theorem, a complete finite integral certificate,
finite algebra, exploratory computation and publication. No accepting marker
means that RH or the infinite endpoint theorem was machine-proved.

## Exact source and numerical object

The arithmetic module is byte-identical to #855 at
0e19b74fe6dfb39bef69a5edd3f0b3098b10a6ad, blob
36d6341b574fe5a512196bfa0d66fbae897365a6. The new finite density has precisely
the five squared integer rates and the exact omitted-tail mean. The center is
an exact terminating rational pair; its existence as a root is not assumed.

The final default checker authenticates all eleven manifest entries in the
closed twelve-file packet, reconstructs every bounded identity, then freshly
computes all 785 Taylor cells and the complete endpoint and Rouché budget.
`results.json` contains exact dyadic and rational enclosures. Its flags explicitly
exclude an xi zero, complete census, numerical exterior threshold and RH proof.

## Final commands and scope

The following normal and optimized commands are the complete accepting runs:

```
python -I -S -B check.py --check results.json
python -I -S -B -O check.py --check results.json
```

Their canonical reconstructed outputs agree with the same retained receipt.
The separate `test_check.py` suite runs six methods, including twelve ACTUAL
changed-copy CLI refusals per mode, with no skipped Linux symlink test. Its
pristine CLI control uses `--quick`, not the full integral; output names that
boundary. A changed numerical endpoint is separately tested against a full
fresh reconstruction, rather than being rejected only by a stale checksum.
Execution logs and the delivery receipt outside this packet record completed
runs. No combined timeout is counted as success.

Finite algebra in `bounded.json` covers 104 complete gamma moments by partial
fractions versus independent cumulant recursion; every density-origin coefficient
through two orders beyond its first nonzero term for N=1,...,8; 72 abstract
endpoint jets; 36 phase-sign panels; 72 finite-index controls with their COMPLETE
geometric positive tail; 60 tail identities; and 785 contiguous partition cells.
The last group is integration coverage, not 785 independent theorems. Multiplicity
weights and zero-diagonal symmetric inertia are tested explicitly. The index
models are synthetic spectra, not computed actual gamma or theta spectra.

## Exploratory history and a numerical implementation repair

Before certification, nondirected Gauss--Jacobi scans used scipy/mpmath and then
two mpmath quadratures at 88 and 128 nodes. They located possible centered-stage
nonreal zeros; only the exact N=5 center selected here receives a zero-count
certificate. These scouts are not accepting inputs, independently directed
backends, or an exhaustive census. Other scouted stages remain unproved findings
and are not included as component claims.

Two preliminary defining-integral implementations used Taylor coefficients in
the unscaled time variable. They correctly FAILED the Rouché guard because
interval dependency near tiny endpoint cells produced enormous enclosures.
They did not certify a wrong root. Re-expanding in v=(t-c)/rho and integrating
with 2d(d/rho)^k/(k+1) fixed that numerical conditioning while retaining the same
analytic error bound. The final source includes every corresponding rho factor.
The unscaled failures are not counted as completed accepting runs. The proof's
analytic endpoint theorem did not depend on those failed numerical enclosures.

The final exact source guards were then strengthened; the final sealed bytes
are the ones used for reported normal/optimized accepting commands. A temporary
diagnostic output used during development is removed from the final producer.
No hidden cached integral or trusted scout result enters acceptance.

## Delivery and independent-review boundary

The add-only Git fixture and clean ZIP extraction preserve the parent packet
and unrelated sentinels; the delivery receipt records byte and replay checks
separately. Such fixtures are NOT a complete Riemann checkout or repository-wide
validator run. Git object verification authenticates publication, not mathematics.

No parent numerical campaign, actual xi value or zero, fresh zero census, Lean
build, remote CI result, native Windows test, independent-author review, computed
exterior radius, first detecting Hankel order or N-uniform confinement is claimed.
The parent full-strip convergence is inherited as proposed mathematics at its
exact source. Classical Watson/Rouché/Hadamard mechanisms are credited and the
source-specific hypotheses and tail arguments are supplied for paper review.
