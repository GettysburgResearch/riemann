# Hostile review specification

## Freeze

Verify exact heads for PRs #496, #500 and #495 and every blob in `imports/t92930/IMPORT_MANIFEST.json`.

## `q=2` scope

Reconstruct `Omega_X(2)` and the rough-lift response. Verify the conditional `X=10^16` arithmetic. Confirm that the document withdraws, rather than silently retains, the unproved application to PR #496/#500.

## Native source identity

Reconstruct the paired least-prime source tree and its parity/endpoint observation. Verify that actual source-tree children retain channel and first-owner labels. Reject any substitution of the row-first rough lift.

## Physical coupling

Check both Hall input marginals, residual source, Hall-edge bonus, actual child marginal, complete tagged cells, label-blind quantizer and thinning discard. Confirm that children are exported before root finite realization.

## Two ledgers

Verify the exact current-plus-actual-child ideal identity. Check every ordinary coordinate at `q` and `4q` before detail. Confirm `r_X=u_X-e_X>=0` from all-column domination, not source positivity of `e_X`.

## One-shot specialization

Verify that the quantizer is label-blind and acts after actual child physical
placement.  Confirm that linear label erasure preserves the native source-tree
input marginal and does not substitute the rough lift.

## Terminal children

Verify each canonical child row against the child's own typed capacities. Reject any promotion to a standard `Omega(Y)` unless separately proved. Check target-mass contraction and the `6039/8` bound.

## Consumer

Reconstruct the native dual, prime-square moat and Mellin–Landau endpoint chain. Reject the benchmark bridge.
