# Native source-tree / rough-lift firewall review front door

Base: PR #500 exact head `d73c1e7a1a482cac31581211a84db43cc34c824e`.
Comparison: PR #496 exact head `96f8a6b3cc3d474217633e16d4caa490a0aae518`.
Proposal: `T-92930`.
Status: candidate proposal; RH remains unproved pending independent reconstruction.

## Review order

1. `R-92930` — exact `q=2` separator and withdrawal scope.
2. `L-92930` — native source tree versus row-first rough lift.
3. `L-92931` — physical coupling with actual exported children.
4. `L-92932` — terminal realization against actual capacities.
5. `L-92934` — one-shot internalization of actual children.
6. `L-92933` — terminal-child fallback cost.
7. `T-92930` — endpoint composition.
8. `X-92930` — exact arithmetic and mutation replay.
8. import manifest and lock.

## Immediate falsifiers

- an input marginal equal to the full rough lift rather than the native source tree;
- one source occurrence with two owners;
- a child actual response promoted to `Omega(Y)` without a completion theorem;
- a signed comparison treated as positive source;
- a missing `q` or `4q` ordinary identity before detail;
- root corrections applied to a terminal child;
- native deficit at least `61744`;
- use of `J_Lambda-4sqrt(X)`;
- failure of the frozen endpoint consumer.
