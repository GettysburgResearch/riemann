# Executed validation and limits

## Exact accepting computation

The complete standard-library reconstruction passed in ordinary and optimized
Python, with identical semantic SHA256:

```
511b843638ca032427b8f103fd867e15561322c54d615e6bb2ee233af5b7ab23
```

Both six-method `test_verify.py` suites also passed, with no skips. The suites
include exact signed-rounding controls; two independent small Mobius sieves;
eight source-metadata/encoding/baseline refusals; a corrupted native divisor
source; the strict balanced-Newton endpoint control; short-shift and
same-product-sign controls; typed result changes and duplicate-key refusals.
These are in-process tests, not a claim of many independent CLI campaigns.

Actual full replay scope:

- authoritative compressed primitive SHA256 checked before any calculation;
- every decoded source coefficient is ternary; exact prefix total, fixed
  baseline's two moments and 23/15 energy reconstructed;
- actual prefix mean and full canonical balancing price enclosed;
- all 4,096 synthetic midpoint sums agree between ordered fibers and a
  separate hyperbola implementation;
- complete between-midpoint variation and every baseline/tail interaction paid;
- all 1,048,575 positive native Mobius entries compared between a linear sieve
  and an independently written square-removal sieve;
- 1,023 native gain predicates, one at each cutoff 1 through 1023;
- six full divisor-first Newton reconstructions, 1,118,478 coefficient
  comparisons INCLUDING overlaps;
- four prime-bank reconstructions, each checking every one of the 65,280
  cells of the Y=255 annulus, with complete diagonal/cross budgets.

The model's output interval is certified by analytic envelopes; billions of
output cells were not individually computed. The enclosing source is synthetic,
not a high-height zeta calculation or a giant Mobius-prefix computation.

A fresh ZIP extraction also passed the complete ordinary reconstruction and
all six tests. The optional scout freshly reproduced the authoritative source
bytes on this platform. All README-local file links resolve. An initial simple
link scanner mistook an indented mathematical expression for a Markdown link;
that scanner result was not a mathematical or replay failure.

## Development computations, not extra certificates

A preliminary NumPy/binary64 scan generated native energies through four
million and tested 1,998 gain steps. It helped select the smaller exact replay,
but its decimals and a fitted asymptotic are not acceptance inputs.

An ordinary-floating logarithmic-wave scout selected the source. Its rational
second-difference intermediate was rounded in cumulative coordinates to
produce the exact ternary word. A temporary C++ integer product-sum helper
accelerated scouting. The published checker freshly replaces it by TWO Python
integer formulas at every midpoint. No undocumented helper or binary is needed.

The floating scout is included for discovery reproducibility, not treated as
an independent numerical backend or as proof of any sine/logarithm value.
The accepting word, not the mathematical history of its selection, is the
finite primitive. The separate all-scale theorem is a written analytic proof.

## What a pass does not establish

There was no full Riemann repository checkout, whole-repository validator,
predecessor checker replay, Lean build, remote CI run, independent referee,
Windows/macOS test, or native unbounded estimate. Ordinary and optimized runs
use the same exact arithmetic library and author. Two formulas and two sieve
implementations are useful cross-checks, not independent mathematical acceptance.

The source/certificate bounds depend on PROOF.md's envelope and support
arguments. A successful JSON comparison cannot validate those infinite or
continuum arguments by itself. No theorem status is promoted here.

Publication preservation/readback is recorded in the GitHub continuation
comment, separately from these local execution statements.
