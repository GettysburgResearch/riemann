# Review specification

## Frozen input

```text
PR #508
4ae97dffd1f76ed3244b8f3028560ffa80663caf
```

Do not rerun the full directed tail merely to review this packet.  Verify that
the new source files never alter its claims or hashes.

## Mandatory reconstruction

```text
endpoint root x=X/s;
continuum E/R/T/S colours;
literal finite log occurrences;
P61/rough factorization and first owner;
paired path coefficient and parity;
real terminal leaf and beta/gamma weights;
q2 negative-child separator;
common q/4q coefficient contract;
JNTLC primal/Farkas scope.
```

## Reject immediately if

```text
an actual oriented child is represented by a separate nonnegative row;
a finite log atom is identified coefficientwise with E or T;
source ownership is inferred only from an uncoloured row equality;
different target/score/row/ordinary coefficients are used;
detail is formed before the common q and 4q sums;
a signed comparison vector is called positive source;
JNTLC is asserted feasible from the current replay;
RH is stated as proved.
```
