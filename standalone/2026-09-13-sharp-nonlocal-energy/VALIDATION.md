# Validation scope

Status: proposed component proofs, not independent mathematical acceptance.

## Bounded reconstruction

The standard-library Fraction checker reconstructs 33 whole-block identities,
165 rational geodesic values, 33 first/second-derivative and normalized-gradient
panels, 24 global scalar-minimum brackets, 63 triangular-partition controls,
60 complete continuous Volterra-kernel panels, nine fixed-similarity
embedding controls, and 18 complete primitive-error transport panels. Each panel has several assertions; these are NOT numbers
of independently proved infinite theorems or native theta calculations.

The direct full-matrix trace and separated block formulas are different algebraic
routes. The geodesic Laurent coefficients are compared with direct derivatives
of the inverse matrix. Scalar problems have exact nontrivial global minima.
The Volterra panels integrate all within-cell triangles and between-cell
rectangles; there is no finite-cutoff future or unpriced continuum remainder.
The normal spectral control is a declared synthetic six-dimensional real matrix.

## Executed commands

Both ordinary and optimized isolated commands reconstruct byte-identical semantic
JSON. Both six-method suites pass with zero failures, errors or skips on this
Linux host. In each mode one pristine copied CLI is accepted, ten actual
nonsymlink mutated-copy CLI runs are refused, and a separate symlink substitution
is refused. The symlink case explicitly skips, rather than claiming execution,
on a future host that cannot create symlinks.

Eight nonsymlink mutations are resealed: false RH, false native energy,
reduced coverage, Boolean/integer alias, duplicate key, floating alias,
removed coupling primitive and wrong gradient primitive. The last two fail
independent exact matrix reconstruction. Unsealed proof drift and an extra
file are separately refused. No acceptance check depends on Python assert.

## Deliberate exclusions

No new complete theta Iw/J/V/matrix primitive enclosure, optimized native
metric, spectral mass, actual zero, infinite-dimensional gradient trajectory,
or dimension-asymptotic bound was computed. Full theta integrals in the
mathematical objective are required data, not supplied by the synthetic tests.

Preliminary numerical scouting used binary64 or nondirected multiprecision,
finite theta/time grids, and an asymptotic rather than bounded high-end Iw
tail. Its reported finite energies are not accepted results. One intermediate
JSON basis-conversion field was inconsistent with its metric convention.
No scout code/value is included as proof evidence or adopted into results.json.
The exact matrix identities here were reconstructed separately with Fractions.

Ringrose's named compact triangularization theorem is imported. The continuous-
nest norm limit, geodesic calculations, regularizer coercivity and optimizer
sequence are written proofs requiring review. The external PDF statements were
read as text; the two page-image requests failed. No full audit of the original
1962 article or priority survey is claimed. Izumi's infinite-dimensional warning
is retained; its matrix theorem is not extrapolated to theta.

## Delivery and publication

An add-only local Git fixture and clean ZIP extraction are checked separately
in the accompanying delivery receipt, retaining the unchanged seven-file parent
packet and an unrelated sentinel. Those are not authenticated full Riemann
checkouts. The complete repository validator, trusted Lean build, native Windows
run, remote CI, predecessor numerical campaigns and independent referee review
were not performed in this authoring pass.

Remote publication is recorded only after reading back the actual new branch,
PR head and complete file-tree identities. No main merge or canonical status
promotion is requested by this research packet.
