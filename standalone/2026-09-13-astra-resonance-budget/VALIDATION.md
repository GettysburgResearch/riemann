# RBR26 evidence, execution and non-replay boundaries

All mathematical claims are PROPOSED pending independent review. The finite
checker does not prove the analytic annular limit, complete taper theorem,
RH, or the native filtered-energy upper bound. It uses no actual zeta zero,
zero derivative, zero table or claimed absence of an off-line zero.

## The accepting calculation

There is ONE standard-library program with alternative exact finite
reconstructions, not two independent implementations. It uses integers,
Fractions, Gaussian rationals, and outward dyadic logarithm intervals. Normal
and optimized Python execute the same mathematical implementation. Neither
mode is an independent referee or a proof-assistant build.

The declared corpus is fixed before accepting its report:

- 16 adjoint windows, multiplicities 1 through 8. Right-end integration and an
  independent left-polynomial formula verify all joining jets, normalization,
  the sharp moment/norm identity, and an orthogonal test perturbation.
- 54 SYNTHETIC rational convolution systems with two imaginary-axis poles of
  multiplicities 1 through 6. Four Gaussian-rational evaluations reconstruct
  their partial fractions. Their leading annular constants are compared with
  the derivative formula. These are not zeta zeros.
- 12 stable-cascade input panels over four banks, with one to four factors.
  The Lyapunov solution is obtained by exact Gaussian-rational elimination;
  all principal minors test P>=0 and S-P>=0. An independent modal expansion
  integrates the entire stored-state tail and reconstructs the transfer
  residues. Every intermediate input/output energy is retained. The modal
  fixtures have distinct stable poles; the written state-space theorem does
  not require that condition.
- 48 rational interior-retention controls and 72 rational Fourier numerator
  controls. These test exact algebra; the exponential budget comes from the
  written logarithmic inequality, not rounded exponent evaluations.
- 12 exact off-line annular-buffer controls for hypothetical rational real
  parts, none asserted to be a zero. The leakage exponent and positive
  lower-constant algebra are retained; the analytic limit is not a numeric
  zero test.
- 18 whole scalar norms for stable exponential inputs.
- 104 taper forcing panels, with banks of up to six factors, repeated
  frequencies and complex polynomial coefficients. Exact polynomial
  integration verifies the complete forcing norm, every joining derivative,
  the R^-1 bound, and the zero-output triangular state matrix. The subsequent
  full output estimate is the written stable Young inequality. No sampled
  native tail trajectory or arbitrary-continuation optimization is claimed.
- Nine rational identities for the complete energy of a matched finite
  boundary wave, including its post-cutoff tail. The variable q represents
  exp(-aT); these check algebra, not an exponential quadrature.
- Nine filtered prime-dephasing panels, 42 signatures, at X=16,32,64. Each
  Euler quotient and its inverse reconstruct all coefficients through X-1.
  The scalar a=1/2 filter's character-average energy is compared with every
  shifted rough-source cutoff as an exact rational plus rational prime-log
  expression. Source norm comparisons use outward rational bounds for B_S.
  These check the new coordinate bridge, not native subpower dephasing.
- 36 NATIVE panels Y=1,...,32,63,127,255,511. A Mobius sieve is compared at every
  index with trial factorization. F, E and the terminal state are rational.
  The zero-frequency a=1/2 output is reconstructed by direct cell integration
  plus its terminal tail and by the exact passive loss identity. Both whole
  and prefix-only energy intervals are retained. This is not a nonzero-zeta-
  frequency filter search or a cofinal bound.

Native logarithms use log((k+1)/k)=2 atanh(1/(2k+1)) with the full positive
geometric remainder. Each interval is outward-rounded at 192 bits, then all
weighted sums remain exact rational operations. Published bounds use denominator
2^40. No binary64 value enters the accepting reconstruction.

## Strict acceptance and adverse cases

`--emit` writes an unauthenticated producer report. `--check` first requires
MANIFEST.json, exactly the declared flat set of regular files, no symlinks,
and each listed SHA256. It then independently RECOMPUTES the entire expected
finite report and compares canonical typed JSON, rather than trusting its
internal identities or checksum. Duplicate keys, floating numbers and
nonfinite JSON values are rejected. Boolean/integer aliases fail the canonical
comparison. All acceptance predicates use exceptions and remain active under
-O; no assert implements acceptance.

After a pristine reconstruction, the self-test changes and RESEALS twelve
reports: false RH status; wrong native maximum/count/type; removed adjoint
case; missing terminal state; altered cascade tail; wrong sharpness constant;
false nonzero-zeta-frequency scope; bad numerical bound; erased taper cost;
and a false uniform unfiltered-input-cost claim. Each passes through the same
acceptance function and is required to fail. A duplicate-key parser file is a
separate refusal. These are not twelve full CLI subprocess reconstructions.

Immutable source pins and the publication manifest remain separate trust
boundaries. Replacing both the checker and its manifest with malicious code
cannot be authenticated by that checker alone.

## Executed commands

[validation.json](validation.json) records the actual final normal and optimized
commands, return codes, timings and stdout; it also records producer-byte
comparisons against the fixed report. MANIFEST.json binds the final receipt
and all other packet files. The external DELIVERY.json records fresh ZIP and
patch-application runs separately, without rewriting the packet's receipt.

## Development and excluded exploration

A concurrent packet at head 9ddd3dd was detected in the closing remote check.
Its disjoint prime-dephasing files must remain unchanged. Its finite Euler
comparison and character identities are credited; Section6.5 independently
rederives their proposed bridge in the new filtered-prefix coordinates.

An earlier exact report did not include the subsequently derived finite taper
compiler. The final report adds its explicit forcing and zero-dynamics controls;
no earlier numerical result is promoted to validate that new analytic theorem.

A small noncertifying binary64 scout considered a single width-1/2 filter on
native prefixes 63,255,1023,8191 over a frequency grid. The best sampled frequency
was zero and the retained energy stayed approximately 73--76 percent of the
unfiltered energy. This does not show the desired subpower capture. Those
floating displays are excluded from result.json, all theorem premises and
strict acceptance; the optional script is supplied outside the sealed packet.
No sampled actual zero coordinates are used as source data.

The theorem-level novelty claim is deliberately limited: passive filters,
Lyapunov identities, moment extremizers and weak-Mertens resonance ideas are
classical. The proposed contribution is their source-specific combination,
annular multiplicity implication and constructive budgeted full-future
completion. A comprehensive external priority survey was not performed.

## Not performed

No complete repository checkout/validator, parent producer or certificate
campaign, external formal build, high-order actual zero calculation, full
finite filter-grid optimizer, directed nonzero-frequency native simulation,
all-scale capture proof, or independent mathematical acceptance was obtained.
GitHub reads succeeded. Write discovery exposed no publishing action, and a
direct Git read failed DNS resolution; publication is not claimed by this
receipt. A later publisher must append its own actual execution and remote-head
receipt without rewriting these authoring-session facts.
