# Hostile review specification — T-101000

Mandatory reconstruction:

1. Check every alias against the exact PR head and path.
2. Reprove the Hardy implication in `L-101000`, including the dyadic tail.
3. Verify both exact decompositions of `G_mu`.
4. Check the `L1(dX/X)` integrability of both harmless errors.
5. Verify the negative-part Lipschitz inequality used in `L-101001`.
6. Reconstruct the signed Hasse/minimal-wavelet dictionary.
7. Reconstruct the phase-root decomposition from PR #688.
8. Run the hypergraph checker and verify the minimal cut sets.
9. Reject any edge that uses only a theorem number without PR/path/SHA.
10. Confirm that the matrix contains no verified-only route to RH.

The replay validates logic and finite algebra.  It does not establish any open
middle estimate or RH.
