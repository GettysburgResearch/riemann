# Hostile review specification

## Freeze checks

- record PR #488 base head and PR #489 proposal head;
- verify the full remote branch ref;
- verify every dependency blob in `IMPORT_MANIFEST.json`;
- verify `T92880_CONTENT_SHA256SUMS`.

## Mathematical reconstruction

### Gate A

Reconstruct the deterministic target Hall flow on every root activation cell. Verify target equality, the score-ratio direction and every active row-profile direction using the same coefficients.

### Gate B source identity

Reconstruct the labelled source through support restriction, knot collar, thinning, refinement, Hall, first-owner split, causal split, endpoint integration, actual-mass grouping and one global quantizer. Confirm one owner per source occurrence.

### Physical columns

Evaluate ordinary response at `q` and `4q` before forming detail. Reconstruct the all-column reserve, including `q<K_X`, the terminal annulus and zero response above support.

### Port

Instantiate each branch's projective demand and available port. Check `tau_p<1/9`, PSD domination, aggregate one-use ownership and zero `Y_4` coordinate in the direct-sum model.

### Native cost

Show that every slack vector charged in `L-92883` is the observation of a positive unused source packet and that their total target mass is below the retained root mass. Reject any signed mismatch treated directly as positive.

### Consumer

Reconstruct `L-91378`, `T-91313` and the frozen prime-square/Mellin–Landau chain. Reject any hidden use of RH or a bound for `J_Lambda-4sqrt(X)`.

## Verdict rule

The packet passes only if every interface above is reconstructed at the frozen blobs. A finite replay is authentication, not a substitute for the analytic proof.
