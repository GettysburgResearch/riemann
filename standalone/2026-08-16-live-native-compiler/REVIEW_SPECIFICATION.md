# Hostile review specification

## Freeze

Verify PR #507 remains at
`dfaa70cd2eefcabbf6717e3da060792904c7f357`, then verify every path/blob in
`imports/t93900/IMPORT_MANIFEST.json`.

## Exact type separator

At `x=2`, reconstruct the two active colours and reject any single common source
feature purporting to carry both SHARP target and the Volterra row scalar.
Verify the unique two-channel inverse formulas.

## Bulk source

For each quotient cell, reconstruct the actual Möbius occurrences and the two
complete-graph couplings. Check both input marginals, both positive residuals,
target, score, component row, ordinary `q`, and ordinary `4q` before detail.

## Anchored leaf

Run the live generator for `(p,y)=(67,13)`. Check all 229 source occurrences,
the actual child coefficient, equality/reserve split, one Target-Lorenz
coefficient per occurrence, target equality, score surplus, all 870 rows, and
all 870 ordinary/detail columns.

Then reconstruct the universal coefficient signs from the complete AVLT rather
than extrapolating from the sentinel.

## Root composition

Check the anchored/Volterra hybrid identity, one owner for every source and
actual child occurrence, the identity-plus-B-spline quantizer, explicit discard
marginals, and the separation of signed finite comparison from positive source.

Reject any rough-lift parent, full child-capacity promotion, row-specific source
coefficient, branchwise detail comparison, label-dependent quantizer, repeated
root correction, or hidden auxiliary port.

## Endpoint

Reconstruct the all-column and terminal estimates, the exact positive radix-four
inverse, the sparse `Y_4` pairing, and the one-sided endpoint orientation. The
packet passes only if the complete native deficit is below 60989 without the
benchmark bridge.
