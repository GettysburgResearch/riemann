# Factor-67 Source-Owned Native Thinning and Realization

This standalone packet presents the candidate complete factor-67 proof of
**Source-Owned Native Thinning and Realization (`SONTR`)**.

## Front door

Read in this order:

1. `claims/lemmas/L-91690-factor67-target-hall-is-a-source-owned-root-thinning.md`
2. `claims/lemmas/L-91691-factor67-finite-realization-has-one-use-native-capacity.md`
3. `claims/theorems/T-91660-factor67-endpoint-frame-sontr-resolution-proposal.md`
4. `claims/observations/O-91690-factor67-is-the-natural-source-owned-reset-window.md`
5. `reports/gpt56-pro/2026-08-14-factor67-target-hall-sontr-closure.md`
6. `experiments/X-91690-factor67-sontr/`
7. `integration/2026-08-14/t91660-factor67-sontr-lock.json`

## New mechanism

Choose

\[
 K=\lfloor X/67\rfloor+1.
\]

Then every root quotient is strictly below `67`, so the root Hall source is
exactly the finite `P_61` source and every rough prime remains in the unique
first-owner reservoir partition.

The new directed certificate proves

```text
root target-Hall prefix margin       > 7/20;
equality-state minimum               > 159/500;
reserve-state minimum                > 2/5;
finite mismatch constant C67         < 19;
interior relative correction         < 177/K;
terminal possible overfill           < 4452 X^-3/2;
fixed omission reserve               > 5033 X^-3/2;
recursive coefficient mass           < 1/sqrt(67) < 1/8.
```

Target Hall is score-superordinate and simultaneously gives a nonnegative bonus
in every component row. Rough first-owner labels and the causal reset partition
every source occurrence once. One global safety thinning and one fixed top
omission absorb the finite/continuum error without asserting the false equality
between the finite seed and its continuum model.

## Classification

```text
SONTR                         CANDIDATE COMPLETE / REVIEW REQUIRED
Native-Root Capacity Theorem CANDIDATE COMPLETE / REVIEW REQUIRED
Riemann Hypothesis            UNPROVED PENDING INDEPENDENT RECONSTRUCTION
```

The packet is intentionally fail-closed. The replay certifies its new finite
inequalities, not every imported theorem or RH.
