# Review specification — positive common-parent response-complement packet

## Reconstruction target

Reconstruct the exact chain

```text
native compact source fibre
 -> Hall residual plus edge-owned Hall bonus
 -> unique rough ownership
 -> inner survivor/current/child colours
 -> whole-cell positive direct integral
 -> I_anc plus one labelled bulk quantizer
 -> one common thinning
 -> one nonnegative row
 -> nonnegative detail and ordinary complements
 -> direct Y4 price
 -> one-sided endpoint consumer.
```

## Mandatory checks

1. Verify target demand exhaustion and positive-source non-overdraw for the actual deterministic Hall map.
2. Verify that each edge bonus is a member of the complete positive typed packet cone, not only coordinatewise favorable data.
3. Verify source disjointness through Hall edges, first rough ownership and inner colours.
4. Verify that the retained support is a union of complete cells and that anchored packets pass through the identity.
5. Verify exactly one bulk quantizer and one common scalar thinning.
6. Reconstruct the retained-cell mismatch identity with no cutoff atom and include every `q<K` physical column.
7. Recompute the `129/sqrt(K)` bound and the strict denominator `130`.
8. Recompute the terminal `5033`, `4452` and `581` constants in the same normalization.
9. Verify the finite positive radix-four inverse and ordinary complement identity.
10. Pair every signed comparison directly with `Y_4`; do not use a positive-source mass theorem for it.
11. Verify the thinning bound `<4290 log(2X)` without importing `J_Lambda-4sqrt(X)`.
12. Reconstruct the finite-dual orientation, prime-square moat, Mellin pole audit and Landau theorem.

## Fail conditions

Any one of the following rejects the proposal:

```text
negative/unrealized Hall edge packet;
duplicate source owner;
coordinate-dependent Hall coefficients;
child-specific physical quantization or correction;
signed comparison in positive source;
omitted q<K column;
nonpositive terminal margin;
negative inverse coefficient;
unnamed Y4 charge;
continuum-benchmark circularity;
reversed endpoint inequality.
```

## Machine replay scope

The exact replay is a finite regression and mutation firewall only.  Its passing verdict is not evidence for the unreplayed analytic theorems and does not prove RH.
