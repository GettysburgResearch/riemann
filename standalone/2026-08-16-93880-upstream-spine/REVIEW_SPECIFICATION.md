# Hostile review specification for T-93880

Reject the proposal at the first failed item.

## Source

1. Derive the one-colour Volterra identity by direct integration.
2. Verify finite Fubini and the exact finite/continuum defect.
3. Replay `L(x)>159/500` on all root cells.
4. Verify complete-graph rank-one cancellation uses each colour once.
5. Check anchored/bulk cell disjointness.

## Anchored Target-Lorenz

6. Rebuild the exact target-order coefficients.
7. Replay all compact real cells.
8. Audit the directed zeta primitive.
9. Replay the analytic tail and compact/tail join.
10. Check rows `2..66` and the separate `j>66` frontier construction.
11. Verify all current-only bonuses are target-null.

## Realization

12. Check `I_anc direct-sum Q_bulk`.
13. Verify whole-cell support and absence of cutoff atoms.
14. Check label-blind ownership.
15. Confirm no signed defect enters the positive source cone.

## Capacity

16. Derive the retained-cell ordinary response for every `q`.
17. Recompute constants `19/2`, `57/2`, `171/4`, `971/4`, `129`.
18. Verify the common thinning.
19. Recompute terminal `4452 < 5033`.
20. Verify positive radix-four inversion.

## Native cost

21. Derive the `Y4` recurrence and support.
22. Recompute both elementary sums.
23. Reprove `psi(x)<4 log(2)x` and the `J_Lambda` bound.
24. Recompute `12012+4+48972+1=60989`.

## Endpoint

25. Prove the prime-square PNT Riemann sum and exceptional-set error.
26. Evaluate the constant as `-1-zeta(1/2)>0`.
27. Derive the Green-kernel quadratic moat.
28. Check real-endpoint interpolation.
29. Derive the Mellin transform.
30. Audit every pole and the cancellation at `s=1`.
31. Apply Landau only after verifying eventual one-sidedness and exponential order.

## Forbidden repairs

A downstream claim may not repair a failed upstream premise. Shared ancestry
between PRs #508 and #509 is not evidence. Numerical results without source,
compiler flags, checksums and outward error bounds are insufficient.
