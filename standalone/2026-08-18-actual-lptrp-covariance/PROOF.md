# Actual large-prime two-row certificate and phase-locked covariance spectrum

## The static advance

The two-row consumer uses coefficients filtered by the Euler factors at two and
three. The old diagnostic did not scan those rows. For the actual coefficients,
define the prefix derivatives `A_2` and `A_3^sharp`. On every activation cell,
the logarithmic derivative of the Riesz row is exactly the corresponding
prefix. An exact `10^18`-denominator square-root enclosure certifies all
`200,000,000` row coordinates through `N=10^8` and keeps both prefixes strictly
positive. This proves the actual large-prime rows positive for every real
`X<100000001`.

For any fixed finite set of large primes, all coefficients beyond `(j+1)P` are
exactly the indicator of coprimality to `P`. Hence the derivative and row are
eventually positive. The still-open regime is necessarily one with unbounded
sieve depth.

## The carrier advance

The phase-locked synthesis denominator and the centered safe-line field contain
opposite copies of the cubic multiplier. Parseval cancels them exactly, leaving
one Gaussian phase-lock kernel convolved with the pole-subtracted safe-line
logarithmic derivative. Every invertible fixed safe-line preconditioner cancels
in the same way.

The resulting covariance has positive atomic carrier autocorrelation spectrum
and mean energy `O_m(q)`. This is a complete unconditional carrier-average
theorem. A one-atom model proves that positive spectrum and finite mean square
do not imply the pointwise `SCID_PL` sign.

## Exact boundary

The packet proves a large finite segment of the actual producer and a corrected
positive spectral theorem. It does not prove the growing-prime tail, the
pointwise signed covariance, or RH.
