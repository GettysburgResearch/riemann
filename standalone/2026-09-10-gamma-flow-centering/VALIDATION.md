# GFC26 validation scope

This is new proposed research. The two finite-source zero certificates and the
all-N written arguments have different evidence boundaries. No all-order zero
confinement or RH conclusion is claimed.

## Complete accepting numerical calculation

`check.py --expect results.json` reconstructs both entire cosine-transform root
certificates: rates (1,4,9,16), and rates (1,4,90/7). It evaluates all 256 real cells
on [0,2], at Taylor degree 40, for each source and each of I,I',I''. Outward
integer intervals use scale 2^-512. Explicit complex-disk nonvanishing and Cauchy
bounds pay every omitted Taylor coefficient. A separate analytic bound pays
[2,infinity); there is no omitted numerical future or zero-table input.

Both Rouche disks have radius 10^-6 and exactly one simple zero. The N4 disk is
outside the critical strip. The u=7/10 disk is strictly inside 0<Im z<1/2. These
are **changed finite sources**, not xi zeros. The smallest slope is still bounded
strictly away from zero with the full interval and tail errors included.

Pristine acceptance requires strict typed equality with a fresh complete
reconstruction, not just a hash. The packet manifest is a byte/inventory check,
not a source of mathematical truth; an independently audited copy of the code
and written remainder arguments is the trust boundary. An arbitrary malicious
replacement of the whole checker and its manifest is not claimed detectable by
the checker itself. Numeric receipt mutations are checked against fresh math.

## Executed final checks

Both complete seven-method test suites passed in ordinary and optimized Python,
with zero skips and identical summary JSON. Each mode executed one pristine full
CLI reconstruction, twelve actual receipt refusals and three copied-source
refusals. Their mathematical controls include 171 rational interval cases, 33
finite density cases and 168 coefficient-budget panels. The complete reconstructed
receipt has SHA256
`19ca98a75f99add66fdec030cfe8a7b125477c19d48f63d0868185f1dad253ef`.
These are bounded controls, not a count of independent analytic theorems.

The executable commands are:

```sh
python -I -S -B check.py --expect results.json
python -I -S -B -O check.py --expect results.json
python -I -S -B test_check.py
python -I -S -B -O test_check.py
```

The seven test methods include rational interval arithmetic, integer-root and
trigonometric/exponential controls, finite density/Laplace/initial-jet identities,
and the elementary-symmetric collision bound on finite rate lists. A real CLI
method performs pristine acceptance plus twelve altered-receipt refusals,
including a false numerical endpoint and missing complete-tail field that reach
fresh reconstruction. Three copied-source probes check an unsealed proof edit,
an extra file, and a symbolic-link substitution. A passed finite check is not
machine proof of the analytic all-N Cauchy/deconvolution theorem.

The optimized integer endpoint division produces exactly the same two raw
certificate rows as the first, slower Fraction-based endpoint implementation.
These are two formulations of endpoint arithmetic, not independent defining-
integral implementations or independent referee votes.

## Exploratory calculations kept separate

`scout.py` probes the new centered sources with mpmath, exact prescribed rates
and the exact mean formula. The endpoint substitution t=T sin(theta) and a
positive truncated density series reduce cancellation. Neither the quadrature
nor that small-x truncation has an outward remainder in the scout. Its root
tracks are not complete or ordered zero censuses. The two N5 node counts share
one backend. The suggested centered N5 nonreal zero is **not certified** by this
packet. It is a warning against claiming every centered stage is real-rooted.

Earlier exploratory raw-flow tracks suggested a collision in the 2-to-3 step.
They motivated the exact rational u=7/10 certificate. No certified collision
parameter, uniform parameter-interval continuation, winding census, or first
collision claim is made.

## Interrupted and unperformed work

A FLINT installation attempt failed because DNS was unavailable. No FLINT/Arb
calculation is used. An initial short-budget N4 orchestration and an initial
combined producer run timed out; they are not completed checks. Subsequent
separate completed runs produced the retained rows. The algebraic interval
division was then optimized and exact equality of both full row payloads checked.

No fresh parent numerical scout, parent package suite, external zero computation,
complete repository checkout validator, native Windows run, Lean build, remote
CI result or independent mathematical acceptance is claimed. Any temporary-Git
or clean-ZIP roundtrip checks the local packet and preserved sentinels, not the
whole Riemann repository. Actual publication identifiers are reported only after
remote readback; this file does not predict a future commit SHA.
