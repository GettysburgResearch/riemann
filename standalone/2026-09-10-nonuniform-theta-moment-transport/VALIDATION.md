# Validation record and limits

The mathematical status is proposed component proof plus computer-assisted
finite existence; independent analysis/code review is required. No infinite
argument is machine-proved by these finite tests.

## Accepting numerical contract

Standard-library integer/Fraction arithmetic and 512-bit outward dyadic
endpoints. Machin pi with complete alternating remainders; exponential Taylor
range reduction with full remainder; no binary64, special-function evaluator,
zeta zero, numerical quadrature library or optimizer output enters acceptance.

The complete source reconstruction uses 128 midpoint Taylor cells on [0,2],
derivative order 24 and the first eight literal theta summands. Order-24 L1
bounds, every later theta index and the whole t>2 future are included. It
reconstructs raw and standardized moments through order ten. All 1170 weighted
magnetization states of the 73-spin graph are included with exact multiplicity;
this is an exact compression of 2^73 configurations, not that many separate
executed configurations. Four complete parameter-box derivatives and the
rational preconditioner give the strict contraction and inward bounds.

`--check` first authenticates the ten-file inventory and nine manifest entries,
checks source/status conventions, then reconstructs the complete numerical
result and compares typed JSON. Duplicate keys, floating numeric literals,
NaN/infinity and the relevant Boolean/integer aliases are refused. `--emit`
is producer-only, with no claim of delivery authentication.

## Executed finite controls

Both normal and optimized accepting reconstructions give identical canonical
JSON. The separate test program passes in both modes:

- Five positive/negative exponential pairs against independent 300-term rational
  series and remainders.
- Three formal source-derivative compositions through order 24, independent of
  the producer's derivative-polynomial recurrence.
- Twenty-eight exact integrated monomials, including the complete L1 remainder.
- Six entire small individual-spin graphs (1344 configuration instances), with
  disagreement weights, all moments and all analytic derivatives compared to
  the separately compressed formula.
- Eight full-rank cumulant panels via formal log-cosh and exact elimination,
  plus three source-tail/graph-count panels. These are not infinitely many
  theorem proofs or an all-order numerical campaign.

In EACH mode, one pristine copied FULL CLI accepts and eight actual changed-copy
CLI calls reject: false RH claim; Boolean schema alias; duplicate JSON key;
claiming moment twelve; resealed removal of cross interactions; resealed source
coefficient mutation; singular preconditioner; extra undeclared file. The two
producer mutations reach the fresh mathematical reconstruction, rather than
failing solely at a hash mismatch. All mutations are confined to temporary
copies; no published source is altered.

## Delivery checks

A minimal temporary-Git add-only patch preserves an unrelated sentinel and
reproduces every new file byte. Both complete accepting modes are replayed in
that fixture. A clean ZIP extraction also replays both modes. These are packet
checks, NOT a full authenticated Riemann checkout, repository-wide navigation
validation, proof-assistant build or remote CI run. The publication receipt
outside this payload records exact Git identities and execution output hashes.

## Discovery and inherited scope

Non-directed NumPy/SciPy/mpmath scouts found two-block eight-moment and
block-plus-hub ten-moment candidates, including failed restricted fits. Their
parameters were rounded to rational data, then checked independently of their
reported residuals. A scout's twelfth moment was neither accepted nor promoted.
The source intervals above are newly regenerated, not taken from a scout or
from the old seed certificate. Interval primitives were adapted from the
Git-blob-authenticated earlier source; no earlier full seed checker was rerun.

The primary journal HTML Lee--Yang/product/closure statements were read;
no new proof of that external theorem is supplied. Relevant live PR descriptions
were orientation, not a full post-integration source audit. Paper arguments,
finite computations, imported classical theorems and global unproved targets
remain separate. No external novelty or independent acceptance is asserted.
