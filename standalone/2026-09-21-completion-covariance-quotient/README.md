# CQT32 — source completion and complete covariance

Proposed research for independent review. **No full covariance or RH resolution.**

This packet preserves the preceding chat's three-point, square-reservoir and graph-energy constructions, then continues them with exact full-output accounting and a mixed-source microscopic upper bound. It is additive and stacked on draft #904; do not merge or promote canonical claims automatically.

## Read

1. `PREVIOUS_PASS.md`: complete mathematical reconstruction of the preceding chat, scoped publication check, original motivating question, all constructions and limitations. Historical execution figures are labeled, not promoted to fresh receipts.
2. `PROOF.md`: full covariance dependence on completion; exact joint source/output optimum; eight-denominator source-attributed closure; positive square-endpoint packet; rectangular microscopic theorem and native adapter.
3. `VALIDATION.md`, `results.json`, `check.py`, and `test_check.py`: fresh exact arithmetic, ordinary/optimized execution, controls and boundaries.

## Main results and their scope

For every balanced perturbation h supported after a native prefix Y,

    Q_(c+t h)(k)=Q_c(k)+2t m_h(k), k<(Y+1)^2.

Thus the whole covariance is quadratic, not quartic, in completion variables, and the bulk after the completion ends is invariant. This does not bound its unknown native value.

The large-prime replacement pair closes into eight source-attributed divisibility amplitudes. Their full sum is zero before pr, but equals 2/(pr) on pr<=k<(Y+1)^2. The combined closed packet has energy asymptotic to a positive constant times Y^2/(log Y)^4, by classical PNT. This does not refute signed compensation by the remaining products.

For two separately balanced sources with support product L_c L_d<=8X, the complete smooth microscopic band satisfies

    sum_(k=X)^M |U_cd(k)|^2<=2^21 H^4 E_c E_d,
    X>=8H, X<=M<2X.

This extends MCB31 to a genuinely rectangular support condition and two localized source energies. It remains a microscopic, critical-exponent estimate, not complete-frequency control.

## Replay

From this directory, using Python 3.11 or later and no third-party dependencies:

```sh
python -I -S -B check.py --check results.json
python -I -S -B -O check.py --check results.json
python -I -S -B test_check.py
python -I -S -B -O test_check.py
```

There are 44,268 counted exact predicates in each full reconstruction. Both eight-method suites pass; each includes a pristine CLI reconstruction and seven actual malformed/altered-report refusals. All accepted arithmetic is integer/Fraction. The analytic microscopic inequality and PNT asymptotic are written arguments, not numerically certified by these predicates.

The finite optimum at Y=63 cancels all 29 primes R<p<=R^2 with R=12. Its perturbation energy is about 0.00256271425 rather than the square reservoir's 0.16646079004. These are exact finite graph calculations with descriptive decimals, not a uniform spectral-gap theorem or decrease of the invariant full covariance.

## Publication

Frozen base: `aaea3f9605a430600bea0189397d93ca315b5f98` (#904).
Branch: `research/astra/20260921-completion-covariance-quotient`.
First preservation commit: `084eb9298fc02108720bd440d106df8b3dd82b7e`.
The PR publication receipt records the final read-back head and file verification. Main, #904's branch, other agents' packets, canonical statuses and workflows are unchanged.
