# Executed validation and source boundary

## Fresh execution

Linux / Python 3.11+ compatible standard-library code. `check.py --write results.json` executed; the final complete reconstruction also passed with `--check` normally and under `-O`. Result JSON SHA-256:

    e88a6a62273583519f431eb0e3acff7011feb7e43e9d5e0f9c0580c550d82c9b

Each full reconstruction executes **44,268 counted predicates**. The JSON records the per-label counts, exact rational values, panel sizes and scope flags. A predicate may compare several values; this count is not a count of independent theorems or implementations.

Fresh panels include:

- Original and modified cap-three Newton reconstruction at Y=7,15,31,63, with the largest complete endpoint 4,095. Complete source differences and their whole-annulus energies are compared exactly.
- Square-reservoir completions at Y=7,15,31,63,127,255; every protected U_p through the declared support is checked. Pure rough prime/pair convolution checks are smaller panels, Y<=31.
- Exact minimum-energy graph problems at Y=7,15,31,63, including feasibility, dual cost, orthogonality, Pythagorean minimum certificate, reconstructed coefficients and full-source energy. Largest matrix is 29 by 29.
- Native completion bilinearity and full covariance polynomials at Y=3,7,15,31, for five real rational perturbation scales. An explicit first-failure check at the square endpoint is retained.
- Source-attributed closed-ray packet identities at Y=15,31,63,127, largest complete observation endpoint 16,383. This is not full Mobius reconstruction to 16,383. Four individual rays also have their full harmonic expression checked before, at and after activation.
- Rectangular native identities for five prefix pairs, through endpoint 1,023 at the largest pair. The transcendental microscopic band is NOT numerically evaluated in this checker.
- Three exact joint source/output minimizers, explicitly future-dependent diagnostics rather than source-only constructions.

The square-reservoir descriptive decimals at Y=31,63,127,255 match the rounded preceding-chat table. This is a fresh reconstruction, not recovery of that prior session's code, receipts or comparison counts.

## Regression controls

`test_check.py` passes all **eight methods**, both ordinarily and under `-O`, with zero failures, errors or skips. Each mode runs one actual pristine report-checking CLI and seven actual refusing CLIs: altered proof status, validator status, three-point energy, closed-packet value, rectangular endpoint, graph optimum, and duplicate JSON key.

Other methods compare a separately ordered direct harmonic double sum, reject a sign-flipped three-point completion, verify the strict endpoint really fails, check all eight nonzero attributed divisibility amplitudes, and reject a singular linear system. Unittest methods remain active under optimized Python; the producer has no assertion statements.

An initial isolated test invocation failed because a plain sibling-module import is excluded by `-I`. The final test runner loads its exact local checker via importlib; both complete suites were then rerun successfully. No accepting mathematical test was weakened.

## Limits

One same-author integer/Fraction implementation plus alternate summation and mutation controls is not independent mathematical review or formal verification. Written proofs, not these bounded panels, carry the universal identities and inequalities. No angular-band numerical certification, infinite-PNT error certification, full predecessor replay, full repository validator, Lean build, remote CI, other-platform test or RH/GRH proof was completed.

The original publication search was scoped to the recent PR inventories, selected branches and searches described in PREVIOUS_PASS.md. Its negative result does not certify that no unidentified duplicate exists anywhere. Direct container Git/network access failed DNS; authenticated GitHub connector writes were used instead.

## Sources and attribution

MCB31, `standalone/2026-09-20-microscopic-composite-band/PROOF.md`, at #904 head aaea3f9605a430600bea0189397d93ca315b5f98: exact centered tails, unreduced smooth kernel, derivative bounds, cap-three completion and native/localized energy framework. Section 4 of this packet extends the same proof to separate rectangular sources, not a new classical summation-by-parts method.

DMC31, `standalone/2026-09-20-anchored-composite-covariance/MELLIN_DENSE.md`, at #905 head 98cf588261473724178231c667595fc09cc216fe: dense high-Mellin projection, retained low-Mellin limitation. That projection is not identified with the angular complement here.

DCN26, `standalone/2026-09-20-divisor-closed-covariance/PROOF.md`, at #903 head f80aa1346bbc457d5726977c0b720162c05bb70e: concurrent complete divisor-fibre and near-ratio results. Our source-ray closure is in its explicitly stated harmonic observable, not a transfer of another kernel's sign.

Huxley--Watt, *Mertens Sums requiring Fewer Values of the Mobius function*, arXiv:1807.05890: classical short-prefix inversion antecedent. Its primary abstract was consulted; no full external-paper or external-priority audit is claimed. The ordinary prime number theorem, Dirichlet convolution algebra, minimum-norm linear algebra, and Cauchy--Schwarz are classical inputs, not claimed as new results.
