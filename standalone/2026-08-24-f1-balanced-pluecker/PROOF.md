# Shared-owner renewal and the balanced F1 Plücker trace

Start with the source-complete radial owners from T-105440. After the literal
singleton and carrier ledgers are recombined, transfer the higher-degree source
to its two largest actual prime owners. Every completed product is uniquely
`p q a^2`.

If two owner pairs share a prime `ell`, both physical vectors contain the same
factor `ell^(-1/2) U_ell`; their Gram term is `1/ell` times the reduced one.
The common incidence is removed permanently. Iteration over distinct common
primes has Euler mass `prod(1+C/p)`, hence only polylogarithmic loss. Shared
owners are therefore recursive, not terminal.

The clean direct sector has four owners. A nonzero phase modulo one owner from
each side gives two prime Ramanujan sums, each `-1`; their product is `+1`.
This is the tensor of two augmentation local systems on the corresponding
Frobenius divisors. It is the local-system realization of the four-label
Plücker rectangle.

The fixed-quadruple phase energies cancel every owner weight. The centered
outer kernel has zero square-lattice moment, and Vaughan decomposition removes
all Type-I core terms. What remains is one coherent signed balanced sum over
clean owner quadruples.

That statement is `F1BPT105450`, exactly `BQSP102870`. It implies the frozen
outer-current negative-mass theorem and hence RH. It is not proved here, and RH
remains unproved.
