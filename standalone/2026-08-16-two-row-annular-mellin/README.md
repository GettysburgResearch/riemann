# Two-row annular Mellin--Landau successor

This standalone front door records the hardened route:

```text
exact annular component formula
 -> integer endpoint reduction
 -> fixed-row reciprocal-zeta Mellin transform
 -> exact rows-2/3 noncancellation
 -> Landau
 -> TAP4 implies RH.
```

The old `J_Lambda/P_Lambda/F_Lambda` endpoint composition is not used.

The remaining gate is

```text
TAP4:
a_2(N)>=0 and a_3(N)>=0 for every integer N>=1.
```

The retained scans support TAP4 but do not prove it. RH remains unproved.
