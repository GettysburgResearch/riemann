# Hostile review specification for T-92920

## Freeze

- verify PR #496 remains at `96f8a6b3cc3d474217633e16d4caa490a0aae518`;
- verify PR #500 base is `d73c1e7a1a482cac31581211a84db43cc34c824e`;
- verify every dependency blob in `imports/t92920/IMPORT_MANIFEST.json`;
- verify both SHA-256 ledgers and the retained replay.

## `q=2` theorem

Reconstruct:

```text
Omega_X(2)=log(4)/sqrt(2) for X>=8;
rough m=67 contribution = Omega_X(2)/sqrt(67);
tau_K retained excess >7/75 at X=10^16;
all frozen downward allowances <1/400;
remaining normalized excess >109/1200.
```

Reject any attempt to apply this separator without first proving that the
physical input marginal is the full rough lift.

## Paired normalization

Verify the signed observation `Sigma(v+,v-)=v+-v-` and `Sigma S=-Sigma`.
Reconstruct the `P_61` stopping line and confirm:

```text
finite forcing signed observation              rough lift;
actual rough-child signed observation           minus rough reservoir;
complete paired source signed observation       native packet.
```

The extra least-rough-prime swap must be present in every child label.

## Physical coupling

Verify odd-demand and even-capacity marginals, first-owner uniqueness, oriented
actual child placements, complete tagged cells and one label-blind quantizer.
Reject full native child-capacity promotion or parity erasure.

## Native feasibility and cost

Reconstruct ordinary `q` and `4q` before detail, every `q<K` column, the
terminal margin, positive inverse, and the four-class `Y_4` ledger below
`60989`.  Signed comparison data must stay outside the positive source cone.

## Endpoint consumer

Reconstruct the exact finite dual, prime-square moat, endpoint orientation,
Mellin continuation and Landau step.  Reject the forbidden
`J_Lambda(X)-4sqrt(X)` bridge.

## Verdict rule

T-92920 passes only if every interface above reconstructs on the frozen blobs.
The finite replay authenticates algebra and constants; it is not a substitute
for the analytic proof.
