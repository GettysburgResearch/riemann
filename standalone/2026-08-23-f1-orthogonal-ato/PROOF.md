# Orthogonal F1 primitive coordinates and the exact cofinal trace

The positive prime-box Green density realizes every finite native Euler
numerator without changing its labelled coefficients. On the three-ray Hodge
surface, setting `B=G-A` diagonalizes the primitive norm:

\[
\mathcal H_{\rm prim}=A^2/24+2B^2.
\]

The conclusion current is `4A-B`, and the exact Hodge inequality is

\[
(4A-B)^2\le(769/2)\mathcal H_{\rm prim}.
\]

The residual is `(A+192B)^2/48`, proving optimality.

After carrier subtraction, `A` and `B` are compact ratio-eight observations of
one literal completion-tangent source. Their block energy is the exact Gram
sum

\[
\sum_{n,m}\sigma(n)\sigma(m)(nm)^{-1/2}\mathcal K_T(n,m).
\]

The kernel is PSD and vanishes outside `1/8<n/m<8`. Diagonal and same-product
sectors are already subpower on the frozen PR #719 inputs. The remaining
arithmetic theorem is the distinct-product off-diagonal upper bound
`F1ATO105405`. It implies the primitive-energy estimate and RH; it is not
proved here.
