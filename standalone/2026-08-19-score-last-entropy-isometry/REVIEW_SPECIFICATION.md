# Hostile review specification — T-99050

The first question is not a numerical bound. It is whether the causal source
tree is genuinely an exact finite tree of the same physical row.

Mandatory reconstruction:

```text
one-node causal identity;
nonnegative current row;
finite endpoint depth;
unique current/terminal ownership;
Hall bonus current-only;
score applied after complete expansion;
endpoint integration of the exact row;
top omission before realization;
one common thinning;
all-column capacity for the same final row;
endpoint consumer normalization.
```

Reject immediately if a declared source score is substituted for literal
physical entropy in the conclusion-producing chain.
