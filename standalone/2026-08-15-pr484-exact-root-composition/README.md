# PR #484 exact-root composition recovery

This standalone packet recovers the unpublished `91840` composition work and
stacks it on PR #487. Read in order:

1. `L-91840` through `L-91844`;
2. `T-91840`;
3. the experiment and retained verification result;
4. `REVIEW_SPECIFICATION.md` and `CLAIM_STATUS.md`.

RH remains unproved pending independent reconstruction.
