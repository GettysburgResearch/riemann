# Independent review specification — T-92900

## Review order

1. `R-92900`
2. `L-92900`
3. `L-92901`
4. `L-92902`
5. `T-92900`
6. `X-92900`
7. dependency lock and this specification

## First decisive checks

### A. Type audit

Confirm from the frozen files that the retained finite/continuum datum is signed
and is controlled by total variation. Reject any inference that makes it a
positive source packet without a separate positive realization.

### B. Two-ledger algebra

Check

\[
P=P^{cur}+\sum_b\beta_bU_bP_b+U^{src}
\]

in the positive source space, then independently check the signed observation
identity

\[
r_X=u_X-e_X\ge0.
\]

The correction owner and source owner are different notions.

### C. Root constants

Reconstruct the exact imported estimates:

```text
thinning       <12012
nonterminal    <4
terminal       <48972
omissions      <1
port/base      0
```

Do not accept a benchmark bridge or a positive-source mass payment for the
signed error.

### D. Terminal children

Verify:

\[
M_X<6039/2,\qquad
\sum_b\beta_b<1/8,\qquad
\Delta(P_b)\le2M_X,
\]

and therefore

\[
\sum_b\beta_b\Delta(P_b)<6039/8.
\]

### E. Endpoint consumer

Freeze and reconstruct the prime-square asymptotic, finite dual, Mellin pole
audit and Landau one-sign theorem at the exact imported blobs.

## Immediate rejection conditions

```text
signed comparison classified as positive source;
one source occurrence assigned twice;
raw coefficient count substituted for target mass;
child capacity dropped before canonical-row insertion;
root correction repeated on a child;
J_Lambda-4sqrt(X) used as an antecedent;
endpoint dependency SHA/path mismatch.
```

## Replay scope

The finite checker verifies only algebra, rational constants and mutations. It
does not authenticate the imported analytic theorems or prove RH.
