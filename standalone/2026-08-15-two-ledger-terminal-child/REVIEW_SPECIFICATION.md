# Hostile review specification

## Freeze checks

- verify PR #488 remains at `9acd381fa168db02a03646ab16851daebbf4d0fd`;
- verify PR #489 remains at `0bb487c8a0782f601be0a3041743b357ad93726a`;
- record PR #490 review head `6f46c2cf4e84d51c744263f7e92a0e1743de5148`;
- verify every dependency commit and blob in `imports/t92900/IMPORT_MANIFEST.json`;
- verify `FACTOR67_92900_SHA256SUMS`.

## Type reconstruction

Confirm that the retained finite/continuum comparison is a signed observation
datum and not a positive source packet. Reject the successor if any target-mass
theorem is used directly on this signed defect.

## Positive source reconstruction

Reconstruct the `P_61` stopping line, root Hall flow, exact endpoint
integration, first-owner allocation and actual target-mass child grouping.
Confirm that every source occurrence is current, one child or genuinely unused.

## Native capacity reconstruction

At ordinary \(q\) and \(4q\) before detail, identify:

```text
u_X  positive unused native capacity;
e_X  signed finite-realization excess use;
r_X  = u_X - e_X.
```

Verify the exact identity and \(e_X(q)\le u_X(q)\) for every physical column.
Do not infer slack positivity from a coordinatewise complement alone.

## Root cost

Reconstruct the five nonzero charges:

```text
thinning                         <12012
nonterminal signed comparison       <4
terminal signed comparison       <48972
positive omissions                  <1
port/base                             0
```

Reject duplicated correction ownership or any use of
\(J_\Lambda(X)-4\sqrt X\).

## Children

Verify actual target mass \(M_X<6039/2\), coefficient sum below \(1/8\), and
canonical child deficit at most \(2M_X\). Confirm that children are realized
once and receive no root correction.

## Port

The successor uses the zero-port specialization. Reject any hidden reliance on
the uninstantiated six-class Schur demand from PR #489.

## Consumer

Reconstruct the positive \(Y_4\) dual, the one-sided endpoint consumer, the
prime-square term, Mellin transform and Landau one-sign theorem. The finite
Python replay is authentication, not a proof of these analytic inputs.

## Verdict rule

The proposal passes only if every interface above is reconstructed at the
frozen commits and blobs. RH remains unproved until that review succeeds.
