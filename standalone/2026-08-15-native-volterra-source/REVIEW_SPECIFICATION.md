# Independent review specification

Freeze the successor commit and reconstruct in this order:

1. Verify the explicit one-colour integral identity `L-91760.12` by direct antiderivatives.
2. Sum over `k` and check every Fubini/support boundary in `L-91760.14`.
3. Verify the retained infinitesimal endpoint-row positivity used in `L-91760.9`.
4. Check `c_X=bar c_X+R E_X` and both native response identities.
5. Recompute `L(x)>159/500` on `1<=x<67` at the frozen directed source.
6. Verify the rank-one transport exhausts every negative source and never overdraws a positive source.
7. Confirm root labels are below `67` and rough first-owner labels begin at `67`.
8. Verify `L-91763`: the positive causal source ledger is integrated before the signed finite comparison, and check `e_X<=u_X`.
9. Substitute the explicit source into `L-91754--L-91756`; reject if any closing estimate used a row-dependent Hall bonus rather than the total row.
10. Reconstruct the exact `Y_4` dual and the one-sided endpoint-to-RH orientation.

Immediate rejection conditions:

```text
finite and continuum seeds are identified;
a boundary atom is dropped at an activation point;
Möbius colours do not share the same endpoint feature after the order swap;
source transport duplicates a positive occurrence;
a rough prime is consumed in the retained root source;
the signed mismatch is inserted as a physical positive row;
any frozen reserve is applied per colour rather than once to the total row.
```
