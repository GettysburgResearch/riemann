# Validation contract and execution record — BPW26

## Exact source and arithmetic

The accepting computation uses the unmodified theta integral for Xi and the
proved geometric all-future bound for the prescribed branching orbit. No zeta,
gamma, root-location oracle, floating quadrature or supplied zero list defines
an accepting value. Rational centres select test squares; their zero counts
are proved, not assumed. There is no numerical evaluation of H_n at n>=32.

The interval backend is standard-library integer/Fraction code at 256 bits.
All three literal theta time cutoffs, the full n>=4 tail, Simpson complex
Peano error, the full Taylor remainder, cubic contour interpolation, coefficient
rounding and the all-depth perturbation are included. The 57,347 term/node
evaluations and 3,220 complete contour segments are coverage units, not counts
of independent theorems or independent implementations.

## Retained numerical outcome

The complete outer winding is 3 and each inner winding is 1. Its positive
margins and all 121 interval coefficients are regenerated by check.py. The
three real derivative enclosures pay Cauchy model error separately and give
the stated geometric root-motion bounds. Written all-depth proofs still need
independent review; executable reconstruction does not formalize them.

## Executed commands

The complete accepting check passed in ordinary and optimized isolated Python.
Both eight-method suites passed in two partitions (six fast, two full), with
zero skips, failures or errors on this Linux host. Each mode executed thirteen
actual CLI refusals after a pristine full acceptance. Twelve are format,
coverage, integrity or primitive-derivative controls. The thirteenth changes a
positive numerical contour margin and reseals the whole manifest; it is refused
ONLY AFTER a complete fresh source reconstruction. This is not merely a hash
check. The same implementation runs in the two modes.

Commands (working directory is this packet):

    python -I -S -B test_check.py --part fast
    python -I -S -B -O test_check.py --part fast --optimized
    python -I -S -B test_check.py --part full
    python -I -S -B -O test_check.py --part full --optimized

The full partitions include `check.py --check result.json` as a real subprocess.
The canonical mathematical output of every successful reconstruction is the
retained JSON, compared byte-for-byte after typed canonicalization. The final
package also has separately retained clean-extraction/Git-fixture replay receipts
outside this immutable source directory. Those replays are not independent
numerical implementations or additional zero windows.

The numerical outcome is unchanged by the subsequent early-placement of the
independent derivative-primitive guard or by bibliographic/execution prose. Final
package replays authenticate the resulting complete inventory.

## Initial attempts and exclusions

An early orchestration timed out while a full producer was running. A subsequent
producer completed the mathematics but failed receipt writing because of an
argparse attribute typo; that typo was fixed before accepting execution. Neither
failed command is counted as success. Adding the explicit derivative-slope
certificate triggered another complete producer run, which succeeded.

Exploratory nondirected calculations suggested some first-iterate noncentral
zeros OUTSIDE the critical strip and supplied familiar approximate centres and
working precision choices. No such scout is a certified zero, a counterexample
to critical-strip confinement, or an input to acceptance beyond selecting exact
rational test domains. The entire accepting integral is reconstructed anew.

No broad campaign, parent accepting suite, independent numerical implementation,
full repository build, Lean/kernel proof, native Windows run, remote CI success,
or independent mathematical acceptance is claimed. Normal and optimized modes
run the same source. Symlink tests explicitly skip on incapable platforms and
must be reported as unperformed there.

## Integrity and limitations

The package has eleven regular files and ten SHA256 entries. The checked receipt
is compared using typed canonical JSON after primitive reconstruction. Resealing
a changed receipt does not bypass that numerical comparison. Integrity checks
do not authenticate an arbitrarily replaced checker-plus-manifest without an
external trusted blob/archive identity. No safety claim is based on asserts.

The add-only patch and ZIP checks are packet fixtures, not a full committed
Riemann checkout. All remote publication claims belong in a separately verified
uploader receipt. This author's session has no exposed write action; direct
Git access failed DNS. No branch, setting, permission or main change occurred.
