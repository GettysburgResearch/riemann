# Review specification — T-106210/T-106213

1. Reconstruct the formula
   \[
   f_U(gc)=2^{-\omega(c)}
   [f_U(g)-(1-(-1)^{\omega(c)})h(g)]
   \]
   for a completely \(U\)-rough reduced core.
2. Verify the rank-two formula for \(b_U(gc)\) and the cofinal
   \(\omega(c)\le3\) bound.
3. Verify that repeated labelled copies of one numerical prime are combined
   before phase optimization. Then verify the harmonic selector, its
   exponential-race interpretation, and the sharp cost \(1/\sum_{p\in\Delta}(p-1)^{-1}\).
4. Confirm that coefficientwise optimality does not itself estimate the
   globally aggregated selector fields.
5. Reconstruct the Witt coefficients and the local identity
   \(1-x/2=\prod_m(1-x^m)^{\gamma_m}\).
6. Verify the principal/quadratic resonance classification.
7. Check the function-field coefficient proof, especially the analytic
   remainder beyond the \(m=1\) factor and the quadratic \(m=2\) resonance.
8. Verify the explicit family-size inequality in `T-106213`.
9. Keep incomplete physical shells, incidence masks, `HSMALL106210`,
   `HROUGH106210`, `WKSFSC106150`, `BCI102990`, and RH open.
