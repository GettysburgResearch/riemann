# Review specification — factor-67 compact reserve closure

Reject the packet on the first failure of any item below.

## A. Hall transparency

1. Reconstruct the target-Hall demand and capacity equations.
2. Verify the identity
   
   \[
   \sum_eT_e\rho(e)-\sum_oT_o\rho(o)
   =\sum_er_e\rho(e)+
    \sum_{o,e}t_{o,e}[\rho(e)-\rho(o)].
   \]
3. Verify that the second term is the same current row bonus used by the
   factor-67 source theorem.
4. Apply ordinary maps at `q` and `4q` separately before forming detail.
5. Reject any use of an approximate Hall atom map in place of the exact finite
   row formulas.

## B. Equality density

1. Verify that the endpoint quantizer acts on the equality-row density `L`, not
   on the SHARP target density `Psi`.
2. Replay all 66 strict-window arithmetic cells.
3. Verify
   
   ```text
   159/500 < L(x) < 183/100 < 2.
   ```
4. Confirm the hostile witness `Psi(2-) > 2`; a proof which silently substitutes
   `Psi` into the `M=2` collar theorem fails.

## C. Interior and terminal arithmetic

1. Recompute `C67<19` with directed rational logarithm intervals.
2. Verify the ordinary and detail mismatch constants `57/2` and `285/8`.
3. Verify the relative interior constant `5655/32`.
4. Verify the exact margin
   
   \[
   178-5655/32=41/32.
   \]
5. Verify terminal overfill `4452`, omission reserve `5033`, and margin `581`.
6. Verify triangular zero response above the retained endpoint support.

## D. Source and port ownership

1. Check the support start `K+2` and strict root quotient `<67`.
2. Ensure the bottom width-two strip is unused source, not added to a child.
3. Check that top Hall is trivial on `1<=X/s<2` and that omitted mass is
   positive equality source.
4. Sum the branch Schur matrices before observing the common port.
5. Use only the `P_61/67` Schur reserve needed for the aggregate inequality;
   do not import the historical open colored-to-physical projection.
6. Apply one causal coefficient list to the global thinned packet and group
   children by rough prime.  Reject a fresh coefficient list per endpoint
   fibre.

## E. Scope

The replay authenticates the new compact theorem.  It does not independently
reconstruct all frozen endpoint-frame, causal, port, dual or endpoint-consumer
claims.  A successful review may promote the compact reserve obligation and the
SONTR proposal on those inputs; it may not declare RH established without the
full dependency reconstruction.
