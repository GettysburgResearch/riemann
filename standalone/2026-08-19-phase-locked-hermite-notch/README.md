# T99100 — Optimal phase-locked Hermite notch

This packet replaces the false uniform-center fractional heat estimate by the sharp fixed-center problem left after the Bohr no-go.

The notch preserves a hypothetical zero exactly, kills the safe-line saddle, and is optimized at order

```text
M ~ 2(1/2-delta)^2 T.
```

It localizes the surviving arithmetic to

```text
log n=2delta T+O(sqrt T)
```

and proves that every magnitude-only proof misses the pole by the exact amplitude exponent

```text
delta(1-2delta).
```

The new open theorem `PLHAC99100` asks the exact fractional owner martingale to supply that phase saving. RH remains unproved.
