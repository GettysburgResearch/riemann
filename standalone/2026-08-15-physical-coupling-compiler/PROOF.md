# Standalone proof extract — source-owned physical coupling compiler

## Native datum

The frozen full Möbius row has exact ordinary response `w_X`, detail response `Omega_X` and literal score `J_Lambda(X)`. Its coefficient sign is not assumed.

## Positive root conversion

On every complete factor-67 quotient fiber, the deterministic Hall coupling exhausts odd target demand and uses each even target capacity at most once. The unused even marginal and the matched-edge profile differences are nonnegative. Their sum is exactly the signed component row before ordinary observation.

The proof object retains:

```text
odd-demand marginal;
even-used marginal;
even-residual marginal;
Hall edge label;
rough first owner;
current/child label;
physical same-index placement;
tagged endpoint cell.
```

## Common physical coupling

Residual source is split by the causal kernel. Every child is physically placed at the same row indices. Hall bonuses remain current. All labels are summed in one common tagged endpoint target and passed through one quantizer which depends only on physical endpoint state.

The output row is therefore one positive marginal, while the input Hall marginals remain independently auditable. Common thinning has a named discard marginal.

## Signed comparison

The retained finite/continuum defect and collar are a signed observation vector `e_X`. Positive thinning and omissions provide unused native capacity `u_X`. The all-column and terminal estimates prove `e_X<=u_X`; hence `r_X=u_X-e_X>=0` and `Omega_X=Xi(d_X)+r_X`.

## Native cost and consumer

Sparse `Y_4` pairing gives

```text
thinning       <12012
nonterminal    <4
terminal       <48972
omissions      <1
total          <60989
```

Thus `0<=J_Lambda-H(d_X)<60989`. The frozen finite dual and endpoint consumer would imply RH. That implication and every frozen analytic producer are review obligations; RH is not established by publication.
