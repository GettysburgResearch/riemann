# HBR30 execution and evidence boundary

The numerical claim is only the complete theta-integral sign at 14 and at 15.
The all-time, all-real-p and all-rank theorems are written analytic arguments,
not conclusions extrapolated from the finite checker.

## Final reproducible commands

```sh
python -I -S -B check.py --check result.json --self-test
python -I -S -B -O check.py --check result.json --self-test
```

Each command fully reconstructs both native anchor values. Each self-test runs
one pristine copied CLI and eight actual refusal processes: false RH status,
increased parameter bound, altered numerical endpoint, a Boolean/integer alias,
duplicate JSON, a resealed theta-source coefficient mutation, unsealed proof
change, and an extra packet member. The theta mutation is rejected by an
independent formal-source calculation, not only a checksum. Duplicate and
inventory failures reject before numerical reconstruction. We do not claim
all eight corruptions rerun the native quadrature.

The numeric path uses only standard-library exact arithmetic and an inherited
512-bit outward complex L1 ball implementation. Every one of 128 cells, every
retained degree-24 Taylor term, all later theta indices and both full-line time
tails are included. A complete absolute Cauchy remainder is charged. No zeta,
gamma or zero-list evaluator enters acceptance. Only two signs, not uniqueness,
simplicity or a low-zero census, follow from this computation.

The exact algebra groups reconstruct the literal source derivatives by formal
composition, full polynomial tail-antiderivative identities, finite Gaussian-
rational gamma-moment identities, Bernoulli/gamma-product factors and centered
variances, congruence scaling, and the synthetic negative Hankel witness. Finite
instances are not independent proofs of the infinite theorems.

## What is not claimed

Platt--Trudgian's finite-height completeness theorem is an EXTERNAL mathematical
input. Its campaign is not rerun. The preceding #842/HBR29 numerical packages,
other agents' tests, an actual powered heat trace, any native fractional-gamma
matrix, an unknown zero, a new high verification height, all-p feasibility,
remote CI, a whole-repository validator and a Lean build are not computed.

Normal and optimized executions use one backend and one authorship. Hashes and
corruption controls do not provide independent referee acceptance or protection
against an adversary replacing the entire checker and trust record.

A preliminary ordinary mpmath scout evaluated the two anchor signs before the
new outward calculation was written. Those approximate values are not retained
as inputs or compared for acceptance. Direct command-line Git access failed
DNS; GitHub connector reads and publication are separate from a full checkout.
The first implementation of the bounded tail controls duplicated an algebra
identity across an unrelated parameter; it was replaced by explicit polynomial
differentiation and redundant moment repetitions were removed before sealing.
No mathematical conclusion uses those earlier test counts.

## Delivery checks

Final command logs, clean-archive replays, the temporary add-only Git fixture,
file/tree identities, and separately verified remote publication are recorded
in the OUTER delivery receipt. They are not an authenticated full Riemann
checkout or a scientific acceptance of every source on the parent branch.
The packet is sealed by SHA256.json; the semantic result is separately
reconstructed on every acceptance. The outer receipt is not a proof premise.
