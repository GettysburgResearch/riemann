# Third pass: source moments, signed Laguerre energy, and fixed-matrix capture

**Status: PROPOSED COMPLETE PROOFS with bounded exact certificates. Independent mathematical review remains required. RH and the conclusion-facing arithmetic signs remain unproved.**

All three routes remain active. This packet continues PR #793 at `f22b67db113d1aa4f986fe35a2fc0321fdff7d47`, adding only this directory. The earlier files, their normalization choices, certificates, and manifests remain unchanged.

## What stepping back changed

The fresh reading of #790's joint heat/rational-ray proof and #792's prime-energy/boundary proof identifies two tasks that should NOT be confused with closure. An early-time asymptotic does not reach the fixed-ray regime where an individual off-line zero appears. A polynomial diagonal bound, even on the same arithmetic support, does not control the full signed quadratic form. Fractional integrability of meromorphic boundary values does not establish holomorphy.

The present attack uses that work in three different ways: move the theta determinant construction to source log moments; build an exact reciprocal-zeta Laguerre energy with its own proved diagonal and falsifiers; and discretize the full Hardy operator in a fixed basis with a proved global tail rate. None is advertised as a shortcut around the remaining arithmetic.

## Route 1 — postpone integer multiplicities to the meromorphic limit

Read [R1_MOMENTS.md](R1_MOMENTS.md).

At the fixed safe center s=2, define

    f(u)=xi((1+sqrt(9+4u))/2)/xi(2),
    f'(u)/f(u)=sum_(j>=0) (-1)^j p_(j+1)u^j.

The all-order unshifted Hankel signs `[p_(i+j+1)]>=0` suffice for a positive trace-class determinant. Positive-axis zero-freeness supplies support on the positive axis; meromorphic residues supply integer multiplicities; the actual xi growth removes a possible exponential escape. No finite-stage integer quadrature weights, shifted-Hankel premise, or compatible matrix sequence is separately needed. This is an explicitly proved moment/Stieltjes refinement of the existing architecture, not a novelty claim for Hankel RH criteria.

The actual arithmetic source now gives SIX rigorously enclosed log moments. Six strict rational interval signs make both three-by-three moment matrices positive definite. They produce a positive three-node resolvent quadrature matching all six moments. This is not a rank-three ordinary determinant: its finite weights need not be integer spectral multiplicities. The preceding minimal ranks 15 and 32 concern another center and another constraint and are not superseded.

**Open:** the unbounded-order sign of the one fixed source Hankel stream. No finite sign is extrapolated.

## Route 2 — actual all-degree diagonal, exact same-diagonal countercontrol

Read [R2_MOBIUS_LAGUERRE.md](R2_MOBIUS_LAGUERRE.md).

For ordinary Laguerre polynomials define

    a_n=sum_(k>=1) mu(k)1_(67 does not divide k) k^(-3/2)L_n(2log k).

Every coefficient is an absolutely convergent source sum. Its generating germ is reciprocal zeta under the Cayley map `w=1/2+(1+z)/(1-z)`. The complete degree energy `E_N=sum_(n<=N)|a_n|^2` is subexponential exactly at the RH endpoint, with the exact horizontal pole-growth exponent retained.

For EVERY degree n the literal diagonal satisfies

    1<=Delta_n<=2+2sqrt(n).

No PNT or zero verification is required. The complete energy has an explicit Christoffel--Darboux kernel with every signed cross term present. Replacing only mu_67 by mu_67^2 preserves this diagonal EXACTLY at every degree, but gives coefficient root growth 3. This is a rigorous sign-blind obstruction, not a counterfeit claimed to be zeta.

A new transfer warning matters: a polynomial bound `E_N=O(N^B)` also imposes `2m-1<=B` on EVERY critical-line zero multiplicity m. Therefore #792's necessary polynomial bound for a logarithmic derivative cannot be copied to a reciprocal-zeta sequence. The correct stated endpoint here is subexponential, not an unproved claim that RH bounds all multiplicities uniformly.

**Open:** the signed subexponential off-diagonal comparison. No new Mobius cancellation upper bound is claimed.

## Route 3 — fixed safe-source matrices with a complete quantitative tail

Read [R3_LAGUERRE_CAPTURE.md](R3_LAGUERRE_CAPTURE.md).

Fix the parent operator at a=1, b=3/2 and use `ell_n(t)=sqrt(2)e^-t L_n(2t)`. Its exact matrix A is generated by

    sum A_(mn) z^m w^n=2[c(z)+c(w)]/(2-z-w),
    c(z)=Laplace(W_(3/2))(2/(1-z)).

The scalar c is given by safe gamma-plus-prime data at s=2 and s=5/2. The recurrence has rank-two displacement, NOT rank two or automatic positivity. Each finite A_N is defined without a zero list or target-dependent sampling frame.

A complete unconditional basis-tail estimate gives

    ||T-Q_N TQ_N||_1=O(log(N+2)/sqrt(N)),

with constants explicitly expressed through the stated coarse zero-count bound. Thus every negative direction would be caught by some predetermined finite matrix. Arithmetic-entry errors and basis-tail errors have separate budgets. Under RH the lost trace is asymptotic to `log N/(8sqrt(pi N))`, so the rate cannot be improved in order for this basis. This asymptotic is conditional calibration, not a positivity proof.

Finally, `Tr T_b=2(d/dv)log F_invariant(v)`, v=b^2-1/4, binds Route 1's moments to this literal resolvent family. It does not identify T's eigenvalues with reciprocal xi zeros or its determinant with xi.

**Open:** an all-N arithmetic lower bound `A_N>=-eta_N I`, eta_N->0. Convergence alone does not supply its sign.

## Replay and scope

With the unchanged adjacent pass2 source present:

```sh
python source_moments.py
python -O source_moments.py
python verify.py
python -O verify.py
python verify.py --tests
python -O verify.py --tests
sha256sum -c SHA256SUMS
```

The new suite reconstructs 622 distinct finite controls, including the six actual-source interval signs. Unit/rejection tests are separate. `MOMENTS.json` and `RESULTS.json` are recomputed, not trusted by their stored PASS flags. The optional `regression.py` reproduces the non-directed 70-digit cross-check; it is not a proof dependency.

See [REVIEW_AND_SOURCES.md](REVIEW_AND_SOURCES.md) for the exact read scope, parent lock, mathematical dependencies, execution boundary, and self-audit. No independent referee acceptance, Lean build, broad zero/prime scan, or remote CI success is claimed.

## The next substantive attack

The common target is not another change of coordinates. It is source-specific control of the connected log moments, the signed Laguerre cross terms, or the finite Hardy matrix's negative part. The new interfaces eliminate auxiliary compatibility, target-selection, and unpriced truncation requirements; none eliminates that arithmetic. All three routes are retained for further work.
