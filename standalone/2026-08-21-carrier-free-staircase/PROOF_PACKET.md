# A carrier-free staircase matrix for the remaining RH producer

The owner side is reduced by an exact survival-weighted mixed coboundary, a monotone staircase telescope, and unconditional positivity for all intervals `q<=p^A`, `A<e^(16/17)`.

The detector side is reduced by a common Mellin-safe filter, one positive five-box spline, and a vector Vaughan decomposition with integrable Type-I error.

The sole named gate is `CFBB102100`: a strict 2x2 Perron contraction on the balanced two-coordinate packet. That estimate is open. RH is unproved.
