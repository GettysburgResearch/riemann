# NM26 validation and review boundary

Status: proposed analytic components with bounded author verification. No RH
proof, independent referee acceptance, external novelty, or full repository
validation is claimed. This packet is self-contained relative to its named
classical Hardy/zeta inputs; the older LOCAL seed handoff is not required to run.

## Reconstructed mathematics

The standard-library checker uses exact integers/Fractions only. It reconstructs
256 Mobius values with independent linear-sieve and trial-factor algorithms;
323 finite deformation-profile values; six complete small period means;
30 exact compact-filter transform identities; eighteen finite Dirichlet-delay
panels; and48 rational polynomial-continuity budgets. These681 instances are
finite checks, not681 analytic theorems.

One actual logarithmic seed is evaluated on EVERY1,048,575 integer cell before
H=2^20, by direct floor summation. The pairwise gcd mean and the proved complete
Fourier-period remainder cover the entire infinite future. All logarithms use
100-term range-reduced atanh sums with full remainders and outward128-bit
dyadic endpoints. Exact real-logarithmic normalization is retained through an
explicit full-norm rationalization error. This freshly reproduces one earlier
seed bound, not the earlier six-prefix campaign. The seed coefficients themselves
were inherited from that earlier trial; they are not a newly computed optimizer.

The three-seed error bound is then analytic via a complete profile bound. No
actual zero, artificial-zero location, controller, feedback full norm, theta
potential, or global residual optimum is numerically evaluated here. The
recurrence, cyclic-space, and all-horizon assertions are PAPER proofs.

## Commands and acceptance

    python -I -S -B verify.py --check result.json
    python -I -S -B -O verify.py --check result.json
    python -I -S -B test_rejections.py
    python -I -S -B test_rejections.py --optimized

Each checker mode reconstructs the same canonical mathematical JSON. Each
adversarial run executes one pristine COMPLETE acceptance and ten actual CLI
refusals: false RH flag, duplicate key, Boolean alias, float alias, wrong seed
energy, missing file, extra file, symlinked proof, resealed wrong primitive
coefficient, and altered proof bytes. Semantic result/producer mutations are
resealed; they do not pass or fail only because an old checksum was left behind.
The missing/extra/symlink cases exercise inventory before execution. Linux
symlinks are tested; no Windows outcome is claimed. No assert implements
acceptance. --emit is explicitly an unauthenticated producer mode.

The ten-file inventory and nine SHA256 entries authenticate the complete
package. A temporary minimal Git fixture and a clean ZIP extraction are used
for add-only roundtrip/replay, preserving unrelated bytes. These are not a
full Riemann checkout, historical-suite replay, Lean/kernel proof, or remote CI.
Exact execution receipts and publication identities are delivered outside the
self-hashed payload, so this document does not contain its own future commit.

## Disclosed exploration and failures

A separate NumPy float scout formed truncated residual powers through2^20;
its tiny high-power partial integrals do not cover future energy and are
excluded from all acceptance. The old local seed blob was queried remotely
and returned404; no remote publication of that earlier package is inferred.
No parent checker is imported or counted as a fresh successful run.

## Review priorities

Check native index convention n<=Y; integer Dirichlet delay versus real causal
shifts; recurrence near a simple safe seed zero; pole removal at s=1; all
multiplicities in the common divisor; absence of singular inner factors;
compact signed density and its operator norm; and the exact coefficient/degree
quantifiers in NM4. Absence of common zeros is not a bounded Bezout theorem.
The open full-norm upper bound is neither proved nor assigned to reviewers.
