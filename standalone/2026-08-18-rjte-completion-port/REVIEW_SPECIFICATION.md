# Review specification

Begin with the exact overshoot certificate. It is the binding test that prevents the positive reciprocal-Julia state from being mistaken for a normalized scalar boundary.

Then reconstruct:

1. the five finite formulations;
2. the annular integral identity;
3. the bulk/parity split and Bellman recurrence;
4. the trace-only counterexample;
5. the quotient-profile prime update;
6. the real-`X` Mellin--Landau compositions.

Do not accept finite verification through `8192` as proof of FCBI. Do not treat PR #564's one observation file as a theorem packet.
