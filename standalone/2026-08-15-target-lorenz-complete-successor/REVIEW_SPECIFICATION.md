# Hostile review specification — Target-Lorenz complete successor

## Frozen target

```text
origin PR:             #468
origin head:           9e2ae6d26a7920055e1f11327dc0ddeb7b855c61
normative content:     9421eb45bcd10f0b7cdf4881a7adddbc295c8b64
main at cutoff:        9c7538559d7f56c2914b39aed5a1fb3fbf7ce131
```

## Required reconstruction order

1. Re-derive `L-93600` from Hurwitz Euler--Maclaurin, including the
   differentiated periodic-Bernoulli remainder.
2. Rebuild `X-93600` and verify every event state, derivative enclosure, child
   correction and the final unbounded interval.
3. Reconstruct the compact/tail join at `py=166000` and the determinant-to-row
   implication `L-91780`.
4. Export the actual stopped-leaf weights in `L-93603.2`; verify one source
   owner and one common coefficient vector in every coordinate.
5. Verify formula `L-93603.5` is the actual common parent and not an existential
   complement.
6. Check that no mismatch, collar or terminal comparison is placed in the
   positive source measure.
7. Reconstruct every physical column, including `2<=q<K`, and define native
   slack only afterward.
8. Recompute all five native-cost charges and confirm the port/base terms are
   literally zero.
9. Reconstruct the one-sided endpoint orientation and the frozen
   prime-square/Mellin--Landau consumer.

## Immediate rejection conditions

Reject the proposal if any of the following occurs:

```text
one compact or tail determinant cell is nonpositive;
the event sweep misses a real activation interval;
rounding uncertainty reaches the published margin;
leaf weights or first owners are not explicit and source-disjoint;
row/ordinary/detail coordinates use different source coefficients;
a signed observation error is treated as positive source;
a full child capacity replaces an actual child response;
any q>=2 exceeds Omega_X;
root port or exported recursion is nonzero;
Y4 cost reaches 61000;
the endpoint implication imports the forbidden benchmark bridge.
```

## Scope

A passing replay authenticates the deposited finite/analytic proof objects. It
does not by itself accept the frozen dependencies or establish RH.
