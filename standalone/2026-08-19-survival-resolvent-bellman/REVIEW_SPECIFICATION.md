# Hostile review specification — T-99030

Review in this order:

1. Reconstruct `m(P_x)=Psi(x)` from Hall target conservation.
2. Verify `dnu=2L(x)dx/x` is the actual endpoint measure.
3. Recompute the sixty-six cell integral and `M_67<16`.
4. Derive `L-99031.6` by moving the causal survival term to the left.
5. Verify every `delta(C_i)<=0` statement is literal-score faithful.
6. Check target-mass nonexpansivity for every child placement.
7. Verify terminal `delta_+<=2m` and the optional-stopping limit.
8. Confirm the Hall row bonus has no declared-score or child coordinate.
9. Insert the `<32` debt into the exact row produced by T-99020.
10. Reconstruct all-column feasibility and the endpoint consumer independently.

Immediate falsifiers:

```text
Hall residual target mass differs from Psi(x);
endpoint measure differs from 2L(x)dx/x;
current difference has positive declared-minus-literal debt;
same-index placement enlarges target mass;
Hall bonus reaches a child;
root arithmetic debt exceeds 2M_67;
finite benchmark or endpoint transform uses RH implicitly.
```

Scientific status: proposed hardening; RH unproved pending review.
