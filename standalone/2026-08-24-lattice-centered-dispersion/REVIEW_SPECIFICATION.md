# Hostile review specification — T-102890

Frozen continuation parent:

```text
PR #719
f3e74bbec2f2463bb42e0264e5e709a1a5f69809
```

Mandatory reconstruction:

1. Recompute the nonzero moment of `R_L` and reject the whole-magnitude claim in historical `L-102867`.
2. Integrate the three exact pieces of `K_L` and verify its logarithmic moment is zero.
3. Prove the Euler sum-minus-integral bound `sum m^-1 K_L(Z/m^2)=O(Z^-1/2)` from bounded variation.
4. Reconstruct the `Y^-1/6` unrestricted derivative Type-I bound.
5. Retain `P^+(m)<q` in every stopped lattice and balanced convolution variable.
6. Reconstruct the one- and product-modulus centered phase identities, including every `-1` principal-frequency subtraction.
7. Check the dyadic coherent energy theorem from the centered divisor counts.
8. Verify that the selected owner weights in `L-102883` are used once only.
9. Reconstruct the smooth-boundary physical coefficient and squarefree kernel.
10. Separate source diagonal/equal products from distinct-product cross correlations.
11. Confirm `SLCD102890` remains open.
12. Reject any claim that the replay or the long-core phase theorem establishes RH.

Immediate falsifiers:

```text
using the unrestricted Type-I magnitude as an error;
dropping the q-smooth cutoff;
reintroducing h=0 inside the phase energy;
spending owner weights once in fixed-pair Cauchy and again in coherent packing;
charging a favorable Type-I square as adverse debt;
claiming that source diagonal control bounds physical cross correlations.
```
