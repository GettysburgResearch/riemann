# Independent-review request

## Requested disposition

Review as a component manuscript and a failed attempted RH finish. Do not describe it as a claimed complete RH proof. The explicitly unproved statement is OPEN, equation (27). Proving the implications that consume OPEN does not prove it.

## Proof priorities

| Object | Exact scope | First material check |
|---|---|---|
| CR1, equations (12)--(16) | All finite REAL completions with fixed prefix; exact complete signed functional | Verify residue, support threshold 2Y^2, sign in contour shift, and distinction between real bilinear and Hermitian forms |
| CR2, equation (18) | Uniform full absolute tail for arbitrary finite polynomial | Expand mean square before bounding; retain N log N loss and both linear/quadratic terms |
| CR3, equations (20)--(22) | Fixed support/energy constants, fixed nu>2/3, positive epsilon smaller than 3nu/2-1 | Check uniformity over p and m, all dyadic tails, and no inference at nu=2/3 |
| Native check (23)--(24) | m=2 only; all prime powers through 16 | Independently derive rational log coefficients and atanh remainder; no contour quadrature is claimed |
| Section 7 | Conditional OPEN => RH | Check input predicates, square-cell measure, full zero supremum, actual residue and Landau argument; mark OPEN unproved |

The parent bounded completion and spectral/curvature components are author dependencies at exact sources, not independently accepted results. The new CR1--CR3 do not require the old logarithmic-operator domain theorem or the numerical length-one positivity certificate. No lemma about an arbitrary positive comparison function is substituted for the actual source.

## Attempts that did not close

- Minimize the full signed functional over tail corrections: CR1 gives the exact minimum D(m)-1/4 and exposes the unknown intercept.
- Minimize only the truncated signed functional in a uniform bounded-norm class: CR3 allows only a vanishing change.
- Infer a nonnegative signed integral from j(t)>0: the native m=2 calculation refutes this stronger assertion.
- Apply the generic mean-value estimate all the way down to t=O(1): its N log N term does not vanish; no power saving for bad values follows.

None of these statements refutes special Mobius-sensitive estimates, optimization of a separate absolute majorant, or an argument using additional arithmetic structure. Enlarging the coefficient support/energy requires a new tail budget rather than inheriting CR3.

## Extraction

CR1/CR2/CR3 may be extracted as source-qualified proposed lemmas after independent review. The m=2 check is bounded native evidence for its stated sign only. The conditional end-to-end consumer must retain OPEN as an open premise. No canonical RH endpoint, main-branch modification, or independent acceptance is authorized by this submission.
