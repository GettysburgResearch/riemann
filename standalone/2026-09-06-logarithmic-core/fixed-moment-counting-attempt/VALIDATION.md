# Validation and limitations

The accepting script is Python standard library integer/Fraction arithmetic.
It contains no optimizer, floating point, special-function oracle, remote call,
or assertion whose removal under -O could change acceptance.

Eight named groups were executed in ordinary and optimized isolated Python:
weight integrals; derivative variation; 23 rational lattice-error fixtures
j=2,...,24; four exact native Gram polynomial expansions M=2,...,5;
finite Markov controls; three resonant Fourier controls through moment eight;
the summable centering constant; and growing-order factor algebra.

The native Gram fixtures retain all integers through 100 and every prime-power
weight as a formal logarithm of its underlying prime. The two routes to the
Gram identity are independently assembled. They are identities for all real
values of those formal logarithms, not numerical zeta/prime-sign certificates.
The rational lattice-only fixtures have source indices through 2304.
The Fourier controls have arbitrary small integer frequencies and rational
amplitudes; none is claimed to be a zeta zero.

Ordinary and optimized verification reconstruct byte-identical result.json.
There is no large arithmetic campaign and no parent producer replay.
Five deliberate corruptions per mode test the actual CLI: a false count-proof
flag, a Boolean/numeric alias, a duplicate JSON key, a modified manuscript,
and an added unexpected file. The result mutations are rehashed to exercise
semantic reconstruction in addition to the manifest. No resealed-manuscript
mathematical verifier is claimed.

SOURCES.json pins the inspected parent manuscript identities. The corresponding
uploaded local source bytes were hashed and agree with their remote Git blob
identities. The new algebra does not execute parent code. The two context
manuscripts in the ZIP are not part of the new repository commit.

A SHA256 manifest binds the eight-file packet. A clean temporary-Git add-only
patch roundtrip was also checked byte for byte and replayed in both modes.
That minimal temporary repository is not the full Riemann checkout. Publication
readback, when completed, is recorded separately in the delivery receipt.

The analytic PNT deduction, spectral limits, infinite passages, and the
fixed-moment obstruction are paper arguments, not machine-proved results.
The requested count bound and RH are not proved. No Lean/Comparator/kernel
build, remote CI success, independent acceptance, or fresh actual-source
positivity certificate is claimed.
