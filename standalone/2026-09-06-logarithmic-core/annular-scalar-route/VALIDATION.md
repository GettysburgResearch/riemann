# Validation contract

The proof of the unbounded arithmetic inequality was NOT obtained. Execution
checks the finite identities and 63 actual scalar instances, not an RH proof.
No new all-functions continuum window sign is certified in this packet.

## Arithmetic

`verify.py` uses standard-library integers and Fraction. Interval endpoints are
outward dyadics with denominator 2^160. Every multiply and reciprocal rounds
outwards. Logarithms use exact power-of-two reduction and 80 atanh-series terms
with a proved positive tail bound. Pi uses Machin's identity and 96 terms per
arctangent with absolute alternating tails. Euler's constant uses the elementary
harmonic sandwich proved in PROOF.md, at N=1024. The C0 certificate compares
rational endpoints to 1/25 and 1/20; decimal displays are not accepting inputs.

The actual prime-power table is made by a sieve and repeated integer powers.
Independent trial division checks the table through 256. Every source prime
logarithm is regenerated by the accepting log routine. The instances are exactly
m=2,...,64, using all prime powers through 4*64^2=16384 as needed. No zero table,
optimizer, arbitrary coefficient data, or numerical special-function library
is used. Individual weights are nonnegative and the endpoints are exact.

## Commands

    python -B validate.py
    python -B -O validate.py
    python -B verify.py --check result.json
    python -B -O verify.py --check result.json
    python -B test_rejections.py
    python -B -O test_rejections.py

Normal and optimized reconstruction have byte-identical JSON payloads. The
result includes separate false flags for RH, the terminal unbounded estimate,
and a new full-window sign. Its samples_digest fingerprints the full freshly reconstructed rational
sample intervals; the ZIP also retains those intervals outside the repository
packet. The source checker does not trust those intervals as inputs. Neither a digest nor a successful comparison proves a missing theorem.

The twelve named groups contain 603 bounded controls in total, including 63
source-sign tests and 63 complete nonnegative-weight rows. Individual zero
weights outside the annulus are not padded into a headline check count.
Eight intentional invalid inputs are rejected in each interpreter mode:
false RH flag, floating-point alias, removed sample count, wrong source interval,
duplicate JSON key, changed proof, missing source lock, and extra delivery file.
The result mutants are run through fresh arithmetic reconstruction, not only a
manifest check. Delivery mutations exercise the actual delivery validator.

The initial combined normal/optimized refusal orchestration exceeded its outer
time limit after the normal driver completed. The interrupted optimized driver
is not counted; a separate full optimized rerun completed all eight cases.

## Integrity and limitations

SHA256SUMS covers all nine other files and exact ten-file inventory is required.
The parent source proof was Git-blob-authenticated against its frozen source;
none of its large predecessor suites was rerun or relabeled a fresh result.
The archive includes execution receipts and an add-only patch outside the
repository packet. Publication readback authenticates file bytes, not remote CI.

The symbolic/finite arithmetic checks do not machine-prove the paired Hadamard
product, Laplace Landau passage, square-sampling theorem, source-preserving
continuous countermodel, or all-length equivalence. These have written proofs
and require independent mathematical review. PNT is imported only for the
continuous countermodel, not for the new RH equivalence. No Lean, Comparator,
independent kernel, large zero campaign, external novelty audit, or unbounded
search was executed.
