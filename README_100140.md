# T100140 — Poisson-tail equivalence and the terminal owner-packing audit

This collision-safe supplement is stacked on PR #671 at exact head

```text
2889071e9ebdc412b94cf2cfe74f1fd5142b2857
```

The original theorem reconstruction froze PR #659 at
`83c17b32a99ac9e1aa5aec3168535550eb286636`. PRs #666 and #671 now preserve
the positive tail-square identity and RH-equivalence direction. The distinct
content retained here is the signed Poisson/phase-defect identity, the frozen
source-specific `OCE67` corollary, and the adjacent-block firewall.

It proves that the apparent final estimate `GPMOC99800` is not a weaker
auxiliary lemma:

```text
GPMOC99800  <=>  Riemann Hypothesis.
```

The key identity writes the Cauchy–Poisson norm as the weighted square of
**every coefficient tail**. It also gives a signed Poisson identity showing
that the gap between the physical square and the positive norm is exactly the
phase/tail defect.

The packet further proves that PR #660's source-specific frozen-orbit `OCE67`
contract is equivalent to RH on its already-proved polylogarithmic labelled-
energy inputs, and supplies an
exact source-blind counterexample to any diagonal or free-labelled-energy
shortcut.

```bash
python3 experiments/X-100140-poisson-tail/verify.py \
  --output /tmp/x100140.json
cmp /tmp/x100140.json \
  experiments/X-100140-poisson-tail/results/verification.json
sha256sum -c T100140_CONTENT_SHA256SUMS
```

Expected:

```text
PASS_X_100140_POISSON_TAIL_EQUIVALENCE
```

**RH remains unproved.**
