## Purpose

Continue the exact T-97701/C4MBI67 route without renaming the open sign as a
proof. This packet reconstructs the complete chain at primitive-prefix level,
proves the specialized analytic consumer internally, and establishes an exact
rational certificate through `10^9`.

**RH remains unproved.**

## Intended base

```text
PR #604
research/gpt56-pro/97701-c4mbi67-publication-recovery
24212c02fab728bb6c419ed8f5a2429fdce07397
```

## New exact reduction

For the sparse source dictionary `a=q*mu`, define

```text
C(t)=sum_(n<=t) a(n)/sqrt(n).
```

Then, for every real `X>0`,

```text
A_X = integral_[X/4,X] C(t) dt/t.
```

Moreover

```text
C(t)=6-[6B(t)-9/sqrt(2) B(t/2)+3/2 B(t/4)].
```

Thus eventual `C(t)>=0` is a stronger, pointwise producer for C4MBI67.

## New structural form

`C(t)/6` is the expected Euler characteristic of a random induced
multiplicative-threshold complex with three dyadic labels and one label per odd
prime. Adding a label `(p,r)` obeys the exact recurrence

```text
G_new(X)=G(X)+r(H_X(p)-G(X/p)).
```

A finite threshold-26 fixture is exactly negative, proving that independent
pointwise Schur/PSD ports do not compose through the half-order boundary.

## Exact finite theorem

The outward-rounded `2^40` scanner proves

```text
C(t)>=0 for every real 1<=t<1,000,000,001.
```

The exact lower enclosure is minimized at `t=48433`:

```text
1226685126915 / 1099511627776
 = 1.115663623672844551...
```

The full from-source rerun is byte-identical to the retained transcript.

## Self-contained analytic chain

The manuscript includes proofs of:

- the exact Mellin transform;
- the needed nonnegative-density Landau theorem;
- the zeta Euler product and reciprocal Möbius series;
- Poisson summation for the Gaussian;
- theta inversion and the zeta functional equation;
- reflection of zeros and the conditional implication to RH.

## Exact boundary

```text
primitive-prefix/window identity          PROVED EXACT
three-band and four-band identities       PROVED EXACT
prime-Mobius ownership                    PROVED EXACT
weighted threshold-complex form           PROVED EXACT
windowed Bellman recurrence               PROVED EXACT
pointwise Schur-port obstruction           PROVED EXACT
Mellin/Landau/functional-equation consumer PROVED IN MANUSCRIPT
prefix positivity below 10^9+1            PROVED COMPUTER-ASSISTED
global primitive-prefix positivity        OPEN / RH-BEARING
global C4MBI67                             OPEN / RH-BEARING
Riemann Hypothesis                         UNPROVED
```


## Publication layout

```text
README_99000.md
PACKET_METADATA_99000.json
T99000_CONTENT_SHA256SUMS
standalone/2026-08-19-t99000-c4mbi67-primitive-prefix-billion/
```

This successor is add-only relative to PR #604. The canonical PDF and TeX are
committed directly; no external publication workflow or placeholder is used.


## PDF publication note

The repository carries a deterministic compressed core-font rendering
so its bytes can be published and read back through the connected GitHub data
API. The complete modular TeX source is committed beside it; the richer LaTeX PDF is retained
in the deterministic fallback ZIP.


The repository core omits only the derived `MANUSCRIPT.txt`, reproducible LaTeX auxiliary files, and the duplicate `main.pdf`; the deterministic fallback retains the original richer LaTeX PDF and complete push tooling.
