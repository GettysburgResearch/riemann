# Current formalization status — source fidelity before build status

**No unconditional Lean theorem proves RH. No new complete formal release is made by this integration.** The historical formal-v0.1 code and its execution receipts are preserved, but current interpretation must incorporate C's source audit and C4's separately proposed repairs.

## The actual-Xi input defect

The old totalized product `s(s-1) completedZeta(s)/2` vanishes at s=0 and s=1. At the centered half-node its logarithmic-derivative expression is totalized to zero, contradicting a required strictly positive diagonal. The old input type is therefore empty. A theorem with that explicit impossible hypothesis may compile without kernel inconsistency, but it is not an inhabited implementation of the intended actual-Xi source theorem.

Independently, an injective `Nat -> OffLineOrbit` forces infinitely many off-line orbits and excludes empty and finite spectra. The correct source must permit arbitrary countable index types or a complete once-only optional enumeration, with actual analytic multiplicities and disjoint reserve accounting.

## Replacement mathematical source

At the pinned convention `Lambda = Lambda0 - 1/s - 1/(1-s)`, use

```text
xi_ent(s) = 1/2 + s(s-1) Lambda0(s)/2.
```

This function is entire, agrees with the usual product away from the removable endpoints and has value 1/2 at both. C4's paper argument reconstructs complete C2 source assembly, constant one-use reserve allocation, scalar monotonicity/curvatures and order-three PSD for the corrected source. This integrator independently reconstructed its function normalization, reserve argument and finite kernel algebra. It is not a compiled replacement of shared ChallengeDeps and consumers, and it does not internally discharge the actual zero/growth/height inputs. Scientific safe-domain PSD and correct formal source implementation are distinct obligations.

## Mellin interfaces

C4 supplies paper proofs for the exact tail Landau and subpower logarithmic-negative-mass propositions. The old total-function `not AnalyticAt` predicate is weaker than punctured-germ nonextendibility; the old full-neighborhood transfer lemmas remain valid in their own scope. A future implementation must keep those meanings distinct, handle the entire/finite-abscissa cases, and use the correct analytic reciprocal value at zeta's pole.

## Next formal release requirements

Implement the new entire source and complete admissible spectra jointly in statements, inputs and consumers; derive rather than assume the prefix fields; provide the exact imported source bindings; regenerate locks, registry and blueprint; then run the pinned build, comparator/type checks and axiom audit. Keep candidates outside trusted imports until that whole change is checked.

No candidate Lean file from a reviewer archive is promoted by this integration. A clean axiom list for a theorem with impossible hypotheses does not establish the intended mathematical instance. The [paper reconstruction](research/integrated/2026-09-06/MATHEMATICS.md), [review evidence](reviews/C/pass4-math-completion/REPORT.md) and [execution record](integration/2026-09-06/VALIDATION.md) give the exact boundaries.
