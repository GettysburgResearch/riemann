# T104550 — fixed-order Laguerre orientation bridge

This packet gives the exact route by which Conrey's `alpha_3>0.9873` theorem
would genuinely descend to `xi''`.

```text
Xi''' line zero
  -> sign of Xi'''^2-Xi''Xi''''
  -> good/wrong extremum of Xi''
  -> exact reverse-Rolle count
  -> alpha_2 >= (2q-1) alpha_3.
```

The target Laguerre expression is exactly the Fourier transform of one
nonnegative correlation kernel built from `u^2 Phi(u)`.  The missing theorem is
positive definiteness of that correlation kernel.  Pointwise positivity of the
source kernel is not enough.

```text
LAG2XI104550  OPEN
RH             UNPROVED
```
