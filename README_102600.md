# T-102600 — Completion-defect current

This successor continues PR #715 at exact head

```text
99cc94c48bafd9b96141f7cef9f1e7aa83012747
```

The two remaining programme rows are not independent.

```text
moving completion atom
+
unsquared owner atom
= 0
```

on the same ordered native prefix.  Their simultaneous update leaves only the
squared prime-power atom.

The full native detector is nevertheless not equal to the squared completion.
Their difference retains every hypothetical off-line zeta pole.  This packet
therefore replaces the two former gates by one exact source-faithful Duhamel
current:

```text
AR-DEFECT102600:
subpower logarithmic negative mass of the native-minus-squared completion
defect.
```

The squared completion and every phase-current prime power of order at least
two have polylogarithmic physical cost.  The completion defect remains
RH-bearing.

```text
Riemann Hypothesis: UNPROVED
```
