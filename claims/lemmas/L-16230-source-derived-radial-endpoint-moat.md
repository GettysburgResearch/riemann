# L-16230 — Source-derived radial and endpoint fields decay cofinally

Claim ID: `L-16230`  
Status: **PROVED TRANSFER FOR THE SOURCE FIELDS; COMPLETE ALIAS GRAM OPEN**  
Authoring agent: `gpt56-pro-12`  
Created: 2026-08-01

## Statement

Let the repaired packet use modes `0,4,8,12`. Choose the even-Legendre Jacobi
cutoff so that the normalized coefficient tail is at most
`epsilon_lambda`. Fourier unitarity transports this directly to the radial
profile error. Multiplication by the frequency variable and the horizontal
strip cost only fixed polynomial factors in

```text
gamma=2*pi*lambda^2.
```

Consequently, for every fixed `A>0`, the cutoff may be chosen cofinally so that

```text
tail_l2_sq=O(lambda^-A),
derivative_tail_l2_sq=O(lambda^-A),
horizontal_tail_l2_sq=O(lambda^-A).
```

On the quadratic-logarithmic mode schedule, the uniform Hermite window gives

```text
||f_lambda^(4)||_1 <= polylog(lambda).
```

The exact fourfold endpoint ledger therefore yields

```text
point remainder
 =O(polylog(lambda)/lambda^4),

L2 remainder squared
 =O(polylog(lambda)^2/lambda^7).
```

Both tend to zero. The first positive-ray leakage samples are orthogonal before
the exact radical repair. For the repaired two-column packet their cross term
tends to zero by the fixed defect hierarchy. Hence the first-alias profile Gram
tends to the identity.

## Production block at gamma=4096

The source-bound Jacobi replay gives the outward ceilings

```text
tail L2 squared                  <10^-4182
derivative tail L2 squared       <10^-4175
horizontal tail L2 squared       <10^-4181
source fourth-derivative L1      <5000
endpoint point remainder         <10^-6
endpoint L2 remainder squared    <10^-10
first-alias cross magnitude      <10^-8.
```

The exact large rational producer output is bound by SHA-256 in `X-16207`.

## Complete-profile boundary

These statements close every source-derived deterministic field. They do not
bound the complete Poisson higher-alias synthesis operator. A full profile-Gram
moat still requires either

```text
||B_alias|| < first_alias_gram_lower
```

or a direct Loewner enclosure of the complete arithmetic Gram.

`X-16207` derives the deterministic-error and first-alias Gram fields from the
source ceilings and fails closed when the complete alias operator enclosure is
absent. No additional support block should be promoted before this remaining
operator moat is certified.

No RH proof is claimed.
