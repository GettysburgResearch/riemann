# L-91881 — Native incidences, first owners, residual-only causal children, and physical placements form one one-use compiler

Claim ID: `L-91881`  
Status: **CANDIDATE-COMPLETE SOURCE-OWNERSHIP COMPILER ON FROZEN ARITHMETIC INPUTS — REVIEW REQUIRED**  
Created: 2026-08-16  
Depends on: `L-91880`; exact `P_61` stopping line; unique least-rough-prime ownership; canonical causal generator; same-index placement  
RH status: **unproved**

## 1. Anchored occurrence tree

Every anchored native occurrence carries

\[
\omega=(n,d,\varepsilon,p,h,\chi),
\]

where:

```text
n              anchored endpoint cell;
d|P61          complete small-prime part;
epsilon        parity channel;
p              unique least rough prime, if present;
h              remaining rough history;
chi            Hall residual, Hall bonus, causal current, or causal child.
```

The exact finite stopping line partitions every source occurrence into one finite forcing occurrence or one actual rough child. Unique factorization assigns one least rough owner. No row-first rough-lift reservoir is introduced.

## 2. Residual-only causal split

Only a target-bearing Hall residual source is eligible for the causal identity

\[
P
=
s_kP+
\sum_i\lambda_i(P-r_iU_{p_i}P_{/p_i})
+
\sum_i\alpha_iU_{p_i}P_{/p_i},
\tag{L-91881.1}
\]

with

\[
s_k+\sum_i\lambda_i=1,
\qquad
\sum_i\alpha_i<\frac18.
\tag{L-91881.2}
\]

Every displayed source packet is positive on the canonical causal theorem.

Hall row bonuses satisfy instead:

```text
one Hall-edge owner;
identity physical placement;
current generation only;
no rough owner;
no causal split;
no recursive child;
no declared-score packet coordinate.
```

The retained Volterra bulk residual also receives no causal split, by the exact firewall in `R-91880`.

## 3. One-use incidence laws

For the anchored target Hall coupling,

\[
(\Pi_A)_-=O_A,
\qquad
(\Pi_A)_++R_A=E_A.
\tag{L-91881.3}
\]

For the retained bulk rank-one coupling,

\[
(\Pi_B)_-=\Sigma^-_{X,I},
\qquad
(\Pi_B)_++R_B=\Sigma^+_{X,I}.
\tag{L-91881.4}
\]

At every causal source node, outgoing current and child coefficients sum to the incoming residual-source coefficient. These identities are incidence equalities; they do not identify scalar output mass with total absolute input mass.

After common thinning by `tau`, retained incidence plus the explicit discard marginal equals the original incidence in every anchored and bulk source class.

## 4. Physical placements

Every positive output has exactly one physical placement:

```text
anchored exact finite source row         identity;
anchored Hall row bonus                  identity;
canonical rough child                    same-index placement;
canonical causal current difference      same-index placement;
retained Volterra residual               endpoint-state bulk placement.
```

Each arithmetic coefficient is applied once. Same-index placement transports every component, ordinary, detail, literal-score, and child-owned boundary coordinate by the same scalar.

## 5. Exact observation statement

Before labels are erased, the output component row equals the native anchored-plus-retained ideal row. Target is exact on the source sort. Declared score of the source sort is superordinate, and the row-only bonus has nonnegative literal physical score.

Thus the ideal total row has literal score at least the retained native score. This is the fact used when the common thinning is priced; no declared-score value is assigned to a Hall bonus.

## 6. Boundary

```text
native occurrence partition                   exact
least rough owner                              unique, many-to-one allowed
bulk causal split                              forbidden
anchored residual-only causal split            exact on frozen inputs
child arithmetic coefficient                   once
Hall row bonus placement                       identity/current-only
signed comparison                              no source incidence
Riemann Hypothesis                             unproved
```
