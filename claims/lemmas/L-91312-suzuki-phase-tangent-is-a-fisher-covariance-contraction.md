# L-91312 — Suzuki's complete boundary phase tangent is a contraction of an explicit completed Fisher covariance kernel

Claim ID: `L-91312`  
Status: **PROVED EXACT FULL-CARRIER FISHER FACTORIZATION; MODEL-SPACE/DELAY COMPRESSION OPEN**  
Created: 2026-08-12  
Depends on: `L-91309`, `L-91301`, `L-91305`  
RH status: **unproved**

## 1. Completed exponential-tilt source

Retain the fixed positive Riemann density realization

\[
 E(z)=\xi\left(\frac12+z\right)
 =\int_{\mathbb R}e^{-zy}Q(y)dy
\]

and, for `a>0`,

\[
 dP_a(y)=E(a)^{-1}e^{-ay}Q(y)dy.
\]

Write

\[
 \varphi_a(t)=\mathbb E_a[e^{itY}]
 =\frac{E(a-it)}{E(a)},
\tag{L-91312.1}
\]

\[
 m_a=\mathbb E_aY,
 \qquad
 \sigma_a(Y)=Y-m_a.
\tag{L-91312.2}
\]

Then

\[
 \mathbb E_a\sigma_a=0,
 \qquad
 \|\sigma_a\|_{L^2(P_a)}^2
 =\operatorname{Var}_a(Y)
 =\partial_a^2\log E(a).
\tag{L-91312.3}
\]

On the real boundary, Suzuki's all-pass phase is

\[
 \boxed{
 \Theta_a(t)=\frac{\varphi_a(-t)}{\varphi_a(t)}.
 }
\tag{L-91312.4}
\]

In the unconditional safe range `a>1/2`, `varphi_a(t)` has no real zero and all
expressions below are defined.

## 2. The centered phase feature

For real `t`, define

\[
 \boxed{
 h_{a,t}(Y)
 =\frac{e^{-itY}}{\varphi_a(-t)}
  -\frac{e^{itY}}{\varphi_a(t)}.
 }
\tag{L-91312.5}
\]

Because each normalized character has expectation one,

\[
 \boxed{
 \mathbb E_a h_{a,t}=0.
 }
\tag{L-91312.6}
\]

Moreover `h_(a,t)` is purely imaginary on the real source space and

\[
 h_{a,-t}=-h_{a,t}.
\tag{L-91312.7}
\]

The family

\[
 \mathcal H_a(s,t)
 =\mathbb E_a\left[
  h_{a,s}\overline{h_{a,t}}
 \right]
\tag{L-91312.8}
\]

is therefore a positive semidefinite kernel on every finite carrier packet.

## 3. Exact phase-tangent factorization

Differentiating the normalized characteristic function gives

\[
 \partial_a\varphi_a(t)
 =-\mathbb E_a[\sigma_a(Y)e^{itY}].
\tag{L-91312.9}
\]

Hence

\[
\begin{aligned}
 \partial_a\log\Theta_a(t)
 &=\partial_a\log\varphi_a(-t)
   -\partial_a\log\varphi_a(t)\\
 &=-\frac{\mathbb E_a[\sigma_a e^{-itY}]}
          {\varphi_a(-t)}
   +\frac{\mathbb E_a[\sigma_a e^{itY}]}
          {\varphi_a(t)}.
\end{aligned}
\]

Thus

\[
 \boxed{
 \partial_a\log\Theta_a(t)
 =-\mathbb E_a[\sigma_a(Y)h_{a,t}(Y)].
 }
\tag{L-91312.10}
\]

The complete boundary phase tangent is one fixed-source Fisher covariance, not
an independently chosen output derivative.

## 4. Fully polarized kernel domination

For a finite carrier packet `t_1,...,t_N` and coefficients `c_j`, equation
(L-91312.10) and Cauchy--Schwarz give

\[
\begin{aligned}
 \left|
  \sum_jc_j\partial_a\log\Theta_a(t_j)
 \right|^2
 &\le
 \operatorname{Var}_a(Y)
 \mathbb E_a\left|
  \sum_jc_jh_{a,t_j}
 \right|^2.
\end{aligned}
\tag{L-91312.11}
\]

Equivalently, in Loewner order,

\[
 \boxed{
 \left[
  \partial_a\log\Theta_a(t_i)
  \overline{\partial_a\log\Theta_a(t_j)}
 \right]_{i,j}
 \preceq
 \operatorname{Var}_a(Y)
 \left[
  \mathcal H_a(t_i,t_j)
 \right]_{i,j}.
 }
\tag{L-91312.12}
\]

Every carrier cross term is retained.  This is the Schur complement of the
positive Gram block generated by the score `sigma_a` and the phase features
`h_(a,t)`.

Since

\[
 \partial_a\Theta_a(t)
 =\Theta_a(t)\partial_a\log\Theta_a(t)
\]

and `|Theta_a(t)|=1`, unitary diagonal congruence gives

\[
 \boxed{
 \left[
  \partial_a\Theta_a(t_i)
  \overline{\partial_a\Theta_a(t_j)}
 \right]
 \preceq
 \operatorname{Var}_a(Y)
 \left[
  \Theta_a(t_i)\overline{\Theta_a(t_j)}
  \mathcal H_a(t_i,t_j)
 \right].
 }
\tag{L-91312.13}
\]

The same statement holds for the logarithmic radial tangent `a partial_a` after
multiplying both sides by `a^2`.

## 5. Explicit covariance kernel

Expanding (L-91312.8) gives the completely explicit formula

\[
\boxed{
\begin{aligned}
 \mathcal H_a(s,t)={}&
 \frac{\varphi_a(t-s)}
      {\varphi_a(-s)\varphi_a(t)}
 -\frac{\varphi_a(-s-t)}
      {\varphi_a(-s)\varphi_a(-t)}\\
 &-\frac{\varphi_a(s+t)}
      {\varphi_a(s)\varphi_a(t)}
 +\frac{\varphi_a(s-t)}
      {\varphi_a(s)\varphi_a(-t)}.
\end{aligned}}
\tag{L-91312.14}
\]

Although the right side is not manifestly positive term by term, its positivity
is automatic from the resident feature map (L-91312.5).

On the diagonal,

\[
 \boxed{
 \mathcal H_a(t,t)
 =2\left[
  \frac1{|\varphi_a(t)|^2}
  -\operatorname{Re}
   \frac{\varphi_a(2t)}{\varphi_a(t)^2}
 \right]\ge0.
 }
\tag{L-91312.15}
\]

This is a completed, source-specific phase-fluctuation variance.

## 6. Fixed Hilbert-space interpretation

In the fixed source Hilbert space of `L-91309`, the Fisher tangent is

\[
 \partial_aq_a=-\frac12\sigma_aq_a.
\]

Equation (L-91312.10) says that the boundary phase tangent is obtained by
observing this same score against the centered normalized character difference
`h_(a,t)`.  After normalizing the score, the map

\[
 h_{a,t}
 \longmapsto
 -\frac{\partial_a\log\Theta_a(t)}
        {\sqrt{\operatorname{Var}_a(Y)}}
\tag{L-91312.16}
\]

extends contractively from the span of all carrier features to the scalar
output.

Thus the completed source already contains enough positive metric to dominate
the complete **boundary phase derivative**.

## 7. Relation to the model-space tangent reserve

`L-91301` identifies the actual Hardy output leakage as

\[
 \mathcal J_a
 =\sqrt2\,M_{a\partial_a\Theta_a}^*P_a.
\]

The present theorem controls the fully polarized multiplier symbol
`a partial_a Theta_a` by an explicit completed Fisher Gram.  It does not yet
prove that the same contraction intertwines:

```text
model-space projection P_a;
delayed causal and anti-causal outputs;
the bridge coordinate;
all cross-delay and cross-orientation terms.
```

Passing (L-91312.13) through that structured compression is precisely the fixed
scattering observation required by `T-91008` and `L-91305`.

## 8. Strategic consequence

The remaining Suzuki theorem is now narrower:

```text
completed Fisher score and carrier phase features       EXPLICIT;
boundary phase-tangent contraction                      EXACT;
model-space/delay/bridge compression of that contraction OPEN.
```

No new positive source curvature has to be invented.  The load-bearing step is
to prove that Suzuki's canonical Hardy compression is the observation of the
resident Fisher covariance, rather than an unrelated per-scale realization.

## 9. Proof boundary

```text
completed score variance                           EXACT POSITIVE
centered normalized phase feature                  EXACT
phase derivative = Fisher covariance               EXACT
full finite-carrier kernel domination              EXACT
explicit source covariance kernel                  EXACT
boundary multiplier tangent controlled             EXACT
model-space projection intertwining                OPEN
full delayed screw/Weil Gram                       OPEN / RH-EQUIVALENT
Riemann Hypothesis                                 UNPROVED
```
