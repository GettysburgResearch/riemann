# L-92931 — The physical coupling compiler may export actual native source-tree children without promoting them to rough-lift capacities

Claim ID: `L-92931`
Status: **CANDIDATE-COMPLETE SOURCE/PHYSICAL COMPILATION ON FROZEN INPUTS — REVIEW REQUIRED**
Created: 2026-08-15
Primary inputs: `L-91850`; `L-91362`; `L-91760/L-91761`; two-ledger theorem `L-92900`; all-column estimate `L-91733`
RH status: **unproved**

## 1. Exact native source marginal

Use the native paired stopping-line source of `L-92930.2`, not the row-first rough lift. On every retained root fibre the finite forcing is sent through the Hall coupling of `L-91850`; all odd demand is exhausted, even source is matched or residual exactly, and Hall bonuses are edge-owned.

The complementary least-prime packets from the same stopping-line identity are retained as **actual child source marginals**. Let their complete typed observations be `A_b`. Each `A_b` carries its own target, declared benchmark, literal score, component row, ordinary response, radix-four response and descendant boundary coordinates.

Before finite realization the source identity is

\[
\boxed{
 P_X^{\rm nat}
 =P_X^{\rm cur,ideal}
 +\sum_b A_b
 +U_X^{\rm src},
}
\tag{L-92931.1}
\]

where all terms are source-disjoint and `U_X^src` contains only literal positive restriction, omission and thinning source.

No `A_b` is replaced by the full native capacity of an endpoint having the same scale. No `A_b` is identified with a first-owner rough-lift slice `C_i` from `L-91688`.

## 2. Actual-mass grouping

Group the children by complete provenance/placement type and write

\[
 A_b=\beta_bU_b\widetilde P_b.
\tag{L-92931.2}
\]

The factor-67 actual-mass theorem gives

\[
 \beta_b\ge0,
 \qquad
 \sum_b\beta_b<\frac18,
 \qquad
 m(\widetilde P_b)=M_X,
 \qquad
 M_X<\frac{6039}{2}.
\tag{L-92931.3}
\]

These are identities for the actual source-tree children. The external index `b` does not erase their internal endpoint or channel labels.

## 3. Current physical coupling

Apply the PR #500 coupling compiler to the current root marginal:

```text
one Hall edge/residual marginal;
one current label for every Hall bonus;
one tagged complete-cell target;
one label-blind martingale quantizer;
one common scalar thinning and explicit discard marginal.
```

Let `d_X^cur` be its nonnegative output row. Children are exported before the root quantizer and therefore receive no root mismatch, collar, top omission, base packet or port.

The alternative implementation which places every child internally remains available, but it is not used in this theorem; exporting the actual marginals makes the native normalization independently visible.

## 4. Signed observation ledger

Let `u_X>=0` be the unused native capacity observed from genuine positive restriction, omission and thinning. Let `e_X` be the signed finite/continuum, intrinsic-collar and terminal excess-use vector. It is current-owned observation data, not source-positive.

Applying ordinary response at `q` and `4q` before detail to (L-92931.1) gives the exact ideal native identity. The frozen all-column and terminal estimates prove

\[
 e_X(q)\le u_X(q)
 \qquad(q\ge2).
\tag{L-92931.4}
\]

Put

\[
 r_X=u_X-e_X\ge0.
\tag{L-92931.5}
\]

Then the two-ledger theorem gives

\[
\boxed{
 \Omega_X
 =\Xi(d_X^{\rm cur})
  +r_X
  +\sum_b\beta_bU_b\Theta_b,
 \qquad r_X\ge0,
}
\tag{L-92931.6}
\]

where

\[
 \Theta_b:=\Xi(\widetilde P_b)
\]

is the child's **actual complete detail-capacity coordinate**. It is not silently replaced by `Omega_(Y_b)`.

The corresponding ordinary identity is obtained before radix-four subtraction and positive inversion.

## 5. `q=2` normalization regression

Equation (L-92931.6) is native-normalized because it descends from (L-92931.1). If the current-plus-child ideal marginal is replaced by the full rough lift, `R-92930` gives a strict `q=2` excess. The exact regression is therefore:

```text
source-tree actual child marginal              accepted;
row-first full rough lift                      rejected at q=2;
full native child capacity substituted by scale rejected unless separately proved.
```

## 6. Boundary

```text
native input marginal                          explicit source-tree identity
Hall/source ownership                          physical coupling marginals
actual children                                exported with complete types
rough-lift substitution                        forbidden
signed finite comparison                       separate observation ledger
all-column root slack                          u_X-e_X >=0
full-child-capacity promotion                  absent
auxiliary Schur port                           zero / not used
Riemann Hypothesis                             unproved
```
