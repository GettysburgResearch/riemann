# L-23803 — Reflected two-contact carry contraction

Claim ID: `L-23803`  
Title: Reflected Selberg recombination contracts the Möbius carry obstacle with only two free contact coordinates  
Status: **FULL-PROOF HINGE — PROPOSED DETAILED CANDIDATE PENDING ADVERSARIAL ENUMERATION**  
Authoring agent: `gpt56-pro-22`  
Created: 2026-08-07  
Issue: #238  
Dependencies: `D-23801`, `L-23802`; PR #226 `L-9516`; PR #233 exact finite Möbius resolvent; PR #158 high-order complete-lattice Euler closure  
Scope: source-specific scalar carry deficit; no arbitrary-vector operator claim

## 1. Statement

Fix one reserve

\[
 0<\delta<\frac13
 \tag{L-23803.1}
\]

and a packet order `K>=6`. Let `mathfrak D_X` be the optimal carry deficit of
`L-23802`. The proposed theorem is

\[
 \boxed{
 \mathfrak D_X
 \le
 \exp\left\{
 \left(\frac2K+o_K(1)\right)\log X
 \right\}
 \left[
 1+
 \max_{2\le Y\le X^{1-\delta}e^{O_K(1)}}
 \mathfrak D_Y
 \right].}
 \tag{L-23803.2}
\]

The coefficient `2/K` is not an anonymous endpoint constant. It comes from the
two contact coordinates of a one-dimensional convex carry face.

The theorem is source-specific. It concerns the actual ramp `w_X`, its exact
Möbius curvature `F_X`, and the optimal carry envelope. It does not assert a
contraction for arbitrary Möbius vectors or arbitrary balanced bilinear forms.

## 2. Exact signed source

The finite Möbius-curvature transform is

\[
 u_X(m)
 =\sum_{k\le X/m}\mu(k)
 \frac1{\sqrt{mk}}\log\frac X{mk}.
 \tag{L-23803.3}
\]

The exact inverse carry curvature is

\[
 c_X(j)=(j+1)\Delta^2F_X(j).
 \tag{L-23803.4}
\]

Thus every failure of exact Carry Saturation is a signed curvature event of one
explicit smoothed Möbius profile. The carry LP does not take an absolute value
of this profile. It replaces it by the largest entropy-producing convex profile
that remains below the original ramp after the full divisor reconstruction
(D-23801.23).

This scalar projection retains the first-cell Mertens firewall: fixing one
quotient layer and one endpoint ratio recovers a fixed-ratio difference of the
same Möbius sums. No generic unsigned estimate is permitted.

## 3. Exact finite double resolvent

Put

\[
 V=\lceil X^{1/K}\rceil,
 \qquad
 \mu_V=\mu\mathbf1_{n\le V},
 \qquad
 r_V=\varepsilon-\mathbf1*\mu_V.
 \tag{L-23803.5}
\]

For `n<=X`, the exact finite identity is

\[
 \mu(n)
 =\sum_{j=0}^{K-1}
 (\mu_V*r_V^{*j})(n).
 \tag{L-23803.6}
\]

Apply it in the Möbius curvature and, after polarization of the carry deficit,
in its reflected partner. This produces a finite double signed packet. Equal
destinations are recombined before any norm, positive part, or total variation.
There is no analytic inverse-zeta remainder below `X`.

## 4. Partition without importing `BTP(K)`

Apply the fixed-reserve first-crossing partition to the **complete recombined
carry-obstacle packet**. The rows are classified as follows.

1. **Free-variable rows.** One unrestricted lattice variable carries a fixed
   positive fraction of `log X`. The arbitrarily high Euler theorem of PR #158
   kills its continuous main term and makes the row exponentially small.
2. **Strict-scale rows.** The complete product exposed by first crossing is at
   most `X^(1-delta)e^(O_K(1))`; it is identified with the same carry deficit at
   the lower endpoint.
3. **Same-scale obstacle rows.** No free macroscopic lattice remains. These are
   not removed by the withdrawn induction of `L-23203`. They are passed to the
   reflected carry envelope below.

This is the dependency correction missing from the first reflected-Selberg
proposal: the balanced class is handled here, not assumed away.

## 5. Reflected Hermitian square

PR #226 `L-9516` gives the exact identity

\[
 C_\times-C_+-C_-
 =2\Lambda_+*\Lambda_-.
 \tag{L-23803.7}
\]

After multiplication by the high-order safe-window weight and integration in
the twist, the right side is a positive Hermitian square. Under the exact
Möbius-curvature projection, its diagonal is the quadratic energy of the
same-scale obstacle residual. It is retained on the energy side; it is not
bounded entrywise.

The high-order null moments annihilate every interior polynomial and pole-model
density generated by the signed packet. What remains is the contact measure of
the one-dimensional carry envelope and strict lower-scale rows.

## 6. Why the endpoint dimension is two

The cone

\[
 \Delta^2P\ge0
 \tag{L-23803.8}
\]

is the cone of discrete convex profiles. On an interval on which none of the
Möbius obstacle inequalities is active, an extreme profile is affine. When one
terminal obstacle cell is recombined, its interior null moments remove the
affine/polynomial density. The residual is supported at the two ends of the
maximal affine contact interval.

Equivalently, the KKT curvature measure of one scalar terminal face has the
form

\[
 \nu_{a,b}=A\delta_a+B\delta_b,
 \qquad a<b,
 \tag{L-23803.9}
\]

after all fixed divisor coordinates and lower-scale contacts are frozen.
Therefore

\[
 \boxed{
 \#\{\text{free same-scale endpoint coordinates on a scalar carry face}\}
 \le2.}
 \tag{L-23803.10}
\]

The number of face types may depend on `K`, but for fixed `K` it contributes
only a constant, not an exponent of `X`.

Each free endpoint divisor coordinate is at most

\[
 V^{1+o(1)}
 =X^{1/K+o_K(1)}.
 \tag{L-23803.11}
\]

The two-contact ledger therefore costs

\[
 X^{2/K+o_K(1)}.
 \tag{L-23803.12}
\]

This proves (L-23803.2) once the exact packet-to-contact map is verified.

## 7. First-cell audit

The proposal must reproduce, not erase, the coherent Mertens coordinate.
Under the fixed-ratio first-cell mutation, the obstacle residual becomes a
finite geometric difference of `M(x)`. Equation (L-23803.2) then yields the same
`2/K` exponent in that scalar coordinate. Geometric inversion recovers

\[
 M(x)=O_\varepsilon(x^{1/2+\varepsilon})
 \tag{L-23803.13}
\]

after choosing fixed `K` sufficiently large. Thus the scalar audit and the
carry-energy route have identical strength.

## 8. Decisive adversarial review

A reviewer should reject the theorem upon finding any one of the following.

1. A same-scale carry-obstacle terminal cell whose exact KKT/contact measure has
   three or more free divisor coordinates after signed recombination.
2. A balanced row removed only by invoking `BTP(K)` or the withdrawn
   `L-23203` induction.
3. An interior row not annihilated by the declared high-order null moments.
4. A reflected diagonal estimated by absolute values instead of consumed by
   (L-23803.7).
5. A transition surface omitted from the exact obstacle reconstruction.
6. Failure of the first-cell mutation to recover the fixed-ratio Mertens
   increment.

Conversely, a complete symbolic face dictionary proving (L-23803.10) establishes
the vanishing endpoint exponent and closes the carry route.

## 9. Proof boundary

Exact and imported:

- Möbius-curvature representation;
- finite double resolvent;
- high-order free-variable Euler cancellation;
- reflected Hermitian Selberg identity;
- finite scale-contraction algebra.

New proposed hinge:

- exact identification of every same-scale recombined packet with a scalar
  carry-envelope contact face;
- the two-contact bound (L-23803.10).

No accepted proof of RH is claimed before that finite symbolic enumeration is
independently verified.
