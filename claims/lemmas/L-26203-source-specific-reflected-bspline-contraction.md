# L-26203 — Source-specific reflected B-spline contraction

Claim ID: `L-26203`  
Title: A graph-norm contraction for the exact aligned boundary-spline source closes the carry route without conditional-Hankel positivity  
Status: **PROPOSED CLOSING THEOREM — OPEN / LOAD BEARING**  
Authoring agent: `gpt56-02-r`  
Created: 2026-08-08  
Dependencies: `L-26201`, `L-26202`; the two-frequency reflected Selberg coefficient identity; exact fixed-ratio shell Hardy transfer  
Scope: one source-specific estimate; this file does not claim it has been proved

## 1. Differential graph state and strict block scale

For integers `R>=0` and `M>=1`, retain the exact boundary-spline signal `U_R`
of `L-26201`. Define

\[
 \mathcal D_0=I,
 \qquad
 \mathcal D_j=
 \prod_{\ell=0}^{j-1}
 (\partial_t+\ell+\tfrac12)
 \quad(1\le j\le M).
 \tag{L-26203.1}
\]

Fix once and for all

\[
 \boxed{
 B=\frac14\log2,
 \qquad
 \delta=\log2-B=\frac34\log2>0.}
 \tag{L-26203.2}
\]

Use the short-block graph energy

\[
 \boxed{
 \mathcal E_{R,M}(J)
 =\sum_{j=0}^M16^{-j}
  \int_J^{J+B}|\mathcal D_jU_R(t)|^2dt.}
 \tag{L-26203.3}
\]

Every nonforcing column in `L-26202.16` is shifted by at least `log 2`.
Therefore its image of the block `[J,J+B]` lies entirely below

\[
 J-\delta.
 \tag{L-26203.4}
\]

This strict delay is load bearing. A unit block would be longer than `log 2`
and is not used here.

The weights `16^{-j}` match the exact Peano formula: every finite difference
beginning at `n=2` carries both the Euler coefficient `2^{-j}` and the geometric
derivative factor `(2+u)^{-j}`.

## 2. Exact boundary operator

Equation `L-26202.16` defines an exact causal operator

\[
 U_R=Q_R^{\partial}+\mathscr T_{R,M}U_R,
 \tag{L-26203.5}
\]

where

\[
\begin{aligned}
 (\mathscr T_{R,M}U)(t)={}&
 \sum_{j=0}^{M-1}2^{-j-1}
 \Delta^j[n^{-1/2}U(t-\log n)]_{n=2}\\
 &+2^{-M}\mathcal A\!\left(
 \Delta^M[n^{-1/2}U(t-\log n)]_{n\ge2}
 \right).
\end{aligned}
 \tag{L-26203.6}
\]

The finite forcing `Q_R^partial` is generated by the fixed polynomial `D_R` and
has no unbounded arithmetic source.

## 3. Proposed contraction theorem

The replacement closing statement is:

> **Boundary-Spline Reflected Contraction, `BSRC(R,M)`.** There are fixed
> integers `R,M`, constants `C,A>0`, and one number `0<theta<1`, independent of
> `J`, such that the actual aligned inverse-zeta source satisfies
>
> \[
> \boxed{
> \mathcal E_{R,M}(J)
> \le C(1+J)^A
> +\theta\left[
>  1+\max_{u\le J-\delta}
>  \mathcal E_{R,M}(u)
> \right]}
> \tag{L-26203.7}
> \]
>
> for every sufficiently large block `J`.

The theorem is source specific. It is not required, and is generally false, for
arbitrary coefficient vectors in the same spline space.

An asymptotic version is also sufficient: one may take `M -> infinity` provided

\[
 \frac{\log C_M}{M}\longrightarrow0,
 \qquad
 \theta_M\le C_M2^{-M}<1
 \tag{L-26203.8}
\]

along an unbounded sequence, with the same fixed delay `delta`.

## 4. Permitted proof mechanism

The proposed proof of (L-26203.7) has three exact channels.

### Boundary channel

Use the Peano representation `L-26202.18`. The nonnegative one-variable
B-spline weights and the fixed support of `mathcal B_R` reduce every finite
Euler column to finitely many endpoint traces and graph energies on blocks
strictly below `J-delta`. No kernel `f(x+y)` is introduced.

### Digital channel

Use

\[
 \varepsilon*b_R=d_R
 \tag{L-26203.9}
\]

before taking a norm. The forcing is supported on finitely many powers of two,
and the cumulative eta coefficients are `0` or `1`. Every endpoint charge must
be carried by this exact digital ledger.

### Reflected source channel

Expand the `L2` norm of the **actual** remainder `mathscr R_(R,M)` only after
assembling the two independent frequency twists. Apply the reflected Selberg
identity to the aligned pair `A_R,B_R`:

\[
 2\left|\frac{A_R'}{A_R}(\sigma+it)\right|^2
 =\mathcal C_{R,\times}(\sigma,t)
  -\mathcal C_{R,+}(\sigma,t)
  -\mathcal C_{R,-}(\sigma,t).
 \tag{L-26203.10}
\]

The inverse coefficients and generalized prime weights are nonnegative by
`L-26201`. The proof must retain the complete `b_R` pair until the modulus
square is formed. A scalar analytic square or rowwise absolute value is not
admissible.

The desired reserve is the factor `2^{-M}` from exact Euler transformation,
combined with the fixed strict delay and a reflected remainder constant that is
subexponential in `M`.

## 5. Finite proof-object form

At fixed `R,M,J`, every source sum is finite because `mathcal B_R` is compact.
A proof-producing ledger should emit:

```text
complete aligned coefficient manifest b_R;
finite forcing polynomial d_R;
all Peano B-spline cells and endpoint traces;
all two-frequency reflected source pairs;
complete same-cell and cross-cell Gram matrices;
strictly lower block destination for every delayed column;
exact graph metric and one strict final LMI;
first fixed-ratio Mertens projection.
```

The final finite inequality is

\[
 \boxed{
 \mathsf G_{R,M,J}^{\rm bdry}
 +\mathsf G_{R,M,J}^{\rm refl}
 \preceq
 \theta\,\mathsf G_{R,M,J}^{\rm past}
 +C(1+J)^A\mathsf I.}
 \tag{L-26203.11}
\]

All matrices in (L-26203.11) are generated from the declared source. The symbol
is a compact notation for a finite ledger, not an unspecified ambient operator.

## 6. Why this hinge is different from the rejected one

The failed PR #243 mechanism asserted positivity of a Hankel kernel generated
by one scalar profile. `BSRC` instead uses:

1. a positive **convolution** B-spline with an explicit factorization;
2. an exact finite Euler/Peano boundary expansion;
3. the actual eta-aligned Möbius source;
4. a reflected modulus square at the coefficient level;
5. a strict lower-scale graph recurrence.

No conditional-Hankel statement is a dependency.

## 7. Mandatory mutations

A review must reject `BSRC` if any of the following occurs:

1. the determinant `-233/64` from `R-26201` reappears as an assumed PSD kernel;
2. an Euler finite difference is replaced by its unsigned total variation;
3. the `n=2` lower-scale boundary column is omitted;
4. the reflected modulus square is replaced by `H(z)^2`;
5. the eta forcing is widened before the finite polynomial `d_R` is formed;
6. a noncoprime source chain is deleted;
7. the first fixed-ratio Mertens shell is absent from the source projection;
8. the claimed constant `theta` depends on `J` or the lower destination is not
   separated by the fixed `delta`.

## 8. Status boundary

`BSRC(R,M)` is the sole new hinge of the replacement proposal. It has not been
proved in this branch. The exact boundary-spline and renewal identities reduce
it to a sharply specified source LMI, but they do not establish its sign.
