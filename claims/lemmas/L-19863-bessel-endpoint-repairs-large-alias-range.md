# L-19863 — The Bessel endpoint layer controls every alias with `k>R`

Claim ID: `L-19863`  
Status: **PROPOSED COMPLETE REPAIR OF THE LARGE-ALIAS RANGE — NORMALIZED PROFILE BOUNDS INHERITED**  
Authoring agent: `gpt56-pro-09-r`  
Created: 2026-08-07  
Dependencies: normalized Bessel endpoint bounds of `L-19829`; collective endpoint Parseval bounds `L-19850`; stationary geometry of `L-19853`  
Scope: replaces the nonuniform `k^{-5/2}` stationary-phase claim for `k>R`

## 1. The defect in the previous proof

For the first-versus-`k` equal-sign phase, the unique stationary point obeys

\[
 z_{jnk}^2-1\asymp k^{-2},
 \qquad
 |\phi_{jnk}''(z_{jnk})|\asymp k^3.
 \tag{L-19863.1}
\]

When `k>R`,

\[
 z_{jnk}-1\ll R^{-2},
 \tag{L-19863.2}
\]

and the nominal stationary width satisfies

\[
 (Rk^3)^{-1/2}\big/(z_{jnk}-1)
 \asymp\sqrt{k/R}>1.
 \tag{L-19863.3}
\]

Thus the stationary point is inside the Bessel simple-pole layer and an ordinary
interior stationary-phase expansion is not uniform. The individual
`O(k^{-5/2})` assertion in the frozen ledger is withdrawn.

## 2. Endpoint layer mass

Let

\[
 \mathcal E_R=\{1<z<1+cR^{-2}\}
 \tag{L-19863.4}
\]

with fixed sufficiently large `c`. The canonical unit radial leakage profile
satisfies

\[
 |\rho_{n,R}(z)|
 \le C(\log R)^C
 \min\bigl(R^{1/2},(z^2-1)^{-1/4}\bigr).
 \tag{L-19863.5}
\]

Consequently,

\[
 \boxed{
 \int_{\mathcal E_R}|\rho_{n,R}(z)|^2dz
 \le C R^{-1}(\log R)^C.}
 \tag{L-19863.6}
\]

The same estimate holds for a polylogarithmic signed packet in its whitened
coefficient metric.

## 3. Collective higher-alias endpoint aggregate

Let

\[
 H_{>R}(z)=\sum_{k>R}\rho_{n,R}(kz)
 \tag{L-19863.7}
\]

with every incoming/outgoing branch and retained endpoint coefficient included.
The far-field expansion has leading coefficients `1/k`; one support derivative
introduces at worst `(log k)/k` after the rapid phase is extracted. The exact
Parseval identities

\[
 \int_0^{2\pi}
 \left|\sum_{k>K}{e^{ik\vartheta}\over k}\right|^2d\vartheta
 =2\pi\sum_{k>K}{1\over k^2},
 \tag{L-19863.8}
\]

and

\[
 \int_0^{2\pi}
 \left|\sum_{k>K}{\log k\over k}e^{ik\vartheta}\right|^2d\vartheta
 =2\pi\sum_{k>K}{(\log k)^2\over k^2}
 \tag{L-19863.9}
\]

control the complete leading endpoint channel before any absolute summation.
All higher endpoint jets and radial remainders are absolutely summable.
Therefore the normalized endpoint ledger gives

\[
 \boxed{
 \|H_{>R}\|_{L^2(\mathcal E_R)}
 +R\|\partial_RH_{>R}\|_{L^2(\mathcal E_R)}
 \le C(\log R)^C.}
 \tag{L-19863.10}
\]

No individual stationary expansion for `k>R` enters this estimate.

## 4. Endpoint cross bound

Cauchy--Schwarz, (L-19863.6), and (L-19863.10) give the complete large-alias
endpoint contribution

\[
 \boxed{
 \left|
 \int_{\mathcal E_R}
 \rho_{j,R}(z)\overline{H_{>R}(z)}\,dz
 \right|
 \le C R^{-1/2}(\log R)^C.}
 \tag{L-19863.11}
\]

The same estimate holds in operator norm on the complete
`O((log R)^2)` packet after increasing the logarithmic exponent.

## 5. Exterior of the endpoint layer

Every stationary point belonging to `k>R` lies inside `mathcal E_R`. On
`z>=1+cR^-2`, the phase derivative is bounded below by its distance from that
unique point. A dyadic partition followed by one integration by parts gives

\[
 \boxed{
 \sum_{k>R}
 |I_{jnk}^{\rm ext}(R)|
 \le C R^{-1}(\log R)^C.}
 \tag{L-19863.12}
\]

The endpoint value generated by this integration is already contained in the
collective channel of Section 3 and is not charged a second time.

## 6. Complete corrected alias estimate

For `2<=k<=R`, the ordinary stationary calculation of `L-19829/L-19853` remains
valid and gives

\[
 \sum_{2\le k\le R}|I_{jnk}(R)|
 \le C R^{-1/2}(\log R)^C.
 \tag{L-19863.13}
\]

Combining (L-19863.11)--(L-19863.13),

\[
 \boxed{
 \sum_{k\ge2}|I_{jnk}(R)|
 \le C R^{-1/2}(\log R)^C.}
 \tag{L-19863.14}
\]

The Mellin frequency fold remains a separate Airy/cubic channel of order
`R^-1/3 log^C R`; the radial endpoint itself is purely Bessel.

## 7. Proof boundary

- The geometric diagnosis and the endpoint-layer argument are correct in the
  declared normalized Bessel profile.
- Production must independently verify the uniform complex-strip and support-
  derivative versions of (L-19863.5) and (L-19863.10) in the exact CCM
  normalization.
- The withdrawn individual `k>R` stationary estimate is not used anywhere in
  the corrected proposal.