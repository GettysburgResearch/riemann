# L-96601 — The paired rough-prime stopping line terminates at finite depth

Claim ID: `L-96601`  
Status: **PROPOSED COMPLETE UNCONDITIONAL SOURCE THEOREM — REVIEW REQUIRED**

Use the exact identity of `L-96600` on the complete parity-labelled source. A state of local scale `Z` has rank

```text
rank(Z)=min{n>=0: Z/67^n<67}.
```

- a causal edge with child scale `Z/p<67` is terminal;
- every unresolved edge or contracted child has local scale at most `Z/67`;
- the survival coefficient is the aggregate first-hazard tail beyond the last active prime and is a zero-child terminal packet in rows two and three;
- later rough primes are retained inside a common positive history packet; no oriented intermediate child is physically observed;
- ordered histories give one first owner per source occurrence.

Substituting only unresolved source states yields a finite direct sum of terminal leaves `(p,y,d,h)` with

```text
p>=67, 1<=y<67, d|P61,
```

and outer terminal packets of local scale `<67`. The depth is at most `rank(X)`.
