# L-91411 — The square-root and constant rough-renewal laws have a maximal common canonical coupling

Claim ID: `L-91411`  
Status: **PROVED EXACT ROUGH-RENEWAL COUPLING THEOREM**  
Created: 2026-08-13  
Depends on: rough renewal `L-91113`; diagonal mode factors `L-91335`; aggregate regeneration `L-91410`  
RH status: **unproved**

## 1. Two Euler survival laws

For an ordered finite active rough-prime set `p_1<...<p_k`, put

\[
 q_i^X=1-p_i^{-1},
 \qquad
 q_i^Y=1-p_i^{-1/2}.
\]

The two positive Euler renewal modes have survival and first-active-prime laws

\[
 s_\infty^\sigma=\prod_{i=1}^kq_i^\sigma,
 \qquad
 h_i^\sigma=(1-q_i^\sigma)\prod_{r<i}q_r^\sigma,
 \qquad \sigma\in\{X,Y\}.
\]

They are probability laws on

```text
current survival;
first active prime p_1;
...
first active prime p_k.
```

Indeed

\[
 s_\infty^\sigma+\sum_i h_i^\sigma=1.
\]

These are the two mode-dependent coefficients generated by the positive rough
Euler kernels `m^{-1}` and `m^{-1/2}` in the delayed renewal.

## 2. Maximal common branch law

Define

\[
 \alpha_i=\min(h_i^X,h_i^Y),
 \qquad
 \Theta=\sum_i\alpha_i.
\]

Then `alpha_i` is the largest coefficient which may be placed simultaneously
in both mode branches at prime `p_i`. The common branch law is substochastic:

\[
 \boxed{\sum_i\alpha_i=\Theta\le1.}
\]

The unmatched mode masses are nonnegative.

## 3. Canonical current state

Add all unmatched branch masses to the corresponding survival mass. In either
mode the resulting current coefficient is

\[
 s_\infty^\sigma+
 \sum_i(h_i^\sigma-\alpha_i)
 =1-\Theta.
\]

Thus both mode laws have the same current coefficient after their maximal common
branch law is extracted.

For every canonical two-channel packet `P`,

\[
 \boxed{
 P=(1-\Theta)P+\sum_i\alpha_iP_i,
 }
\tag{L-91411.1
}

where `P_i` is the same canonical packet transported through the exact prime
`p_i` child functor and retains tail index `i+1`.

The equality is source-measure-valued. It is not obtained by comparing the two
signed physical observations branch by branch.

## 4. Infinite active sets

For a countable active prime set, apply the finite construction to the first
`N` active primes. The common coefficients increase pointwise to their infinite
values, while all measures are nonnegative. Monotone convergence gives
(L-91411.1) in the countable case.

## 5. Review firewall

For one prime and a pure reserve state, the reviewed coordinatewise branch has
mode coefficients `p^{-1}` and `p^{-1/2}`. The common child coefficient is

\[
 \alpha=p^{-1}.
\]

The remaining `p^{-1/2}-p^{-1}` mode mass is not asked to accompany that child.
It is combined with survival and becomes part of the canonical current packet.

Hence the PR #431 counterexample remains correct for an isolated branch and is
exactly neutralized by the aggregate coupling.

## 6. Consequence

The rough tail may be processed recursively without:

```text
reintroducing a fixed finite prime block;
normalizing a restricted branch as a native packet;
or carrying a noncanonical hidden residual.
```

The remaining proof obligations are the finite positive entry, the exact affine
child packet functor, and the packet-loss/local-debt normalization used by
`T-91404`.

```text
two rough renewal probability laws         EXACT
maximal common branch coefficients          EXACT
canonical current coefficient               EXACT
source-valued coupling                      EXACT
infinite tail extension                     EXACT
factor-54 physical producer                 SEPARATE
Riemann Hypothesis                          UNPROVED
```
