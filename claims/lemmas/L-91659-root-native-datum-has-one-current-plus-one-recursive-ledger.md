# L-91659 — The native root datum has one explicit current ledger and one recursive certificate

Claim ID: `L-91659`  
Status: **PROPOSED COMPLETE ROOT-DATUM LEDGER — FROZEN IMPORT RECONSTRUCTION REQUIRED**  
Created: 2026-08-13  
Depends on: `L-91340`, `L-91341`, `L-91110`, `L-91111`, `L-91114`, `L-91115`, `L-91316`, retained `L-91112.25--26`, `L-90027`, `L-90029`, `L-91653`, `L-91658`  
RH status: **unproved**

## 1. Native optimization datum

For an integer endpoint `X`, define the complete native datum

\[
\mathcal N_X=
(\ell_X,J_\Lambda(X),w_X,\Omega_X,q_X,\Gamma_X,\Xi_X,b_X),
\tag{L-91659.1}
\]

where:

- `J_Lambda(X)` is the exact parabolic benchmark;
- `w_X(q)=q^{-1/2}\log(X/q)1_(q<=X)` is the ordinary target;
- `Omega_X=D_4w_X` is the positive radix-four target;
- `q_X` is the exact finite equality/component-row datum, using only the retained formulas `L-91112.25--26`;
- `Gamma_X` and `Xi_X` are the exact ordinary and radix-four responses;
- `b_X` is the direct sum of the finite endpoint-boundary channels needed by the root construction.

The feasible cone is the cone of nonnegative rows whose ordinary, detail and boundary uses are bounded coordinatewise by `(w_X,Omega_X,b_X)`. Its deficit is

\[
\Delta_X(\mathcal N_X)
=J_\Lambda(X)-\sup_{d\in\mathcal F_X(\mathcal N_X)}\operatorname{Score}(d).
\tag{L-91659.2}
\]

## 2. Recursive Hall certificate

On the fixed factor-54 window, let `t_(o,e)>=0` be the no-upward score-mass transport of `L-91340`, and put

\[
r_e=W_S(X,e)-\sum_ot_{o,e}\ge0,
\qquad
\nu_X(e)=\frac{r_e}{W_S(X,e)}.
\tag{L-91659.3}
\]

Then

\[
0\le\nu_X(e)\le1.
\tag{L-91659.4}
\]

Let `E_X` denote the active even source nodes in the fixed window. Define the free recursive certificate

\[
\boxed{
\widehat Z_X=\sum_{e\in E_X}\nu_X(e)\widehat A_{X,e}^{(67)}.
}
\tag{L-91659.5}
\]

The superscript records that every future rough child begins with tail-prime index `67`; the finite small-prime block is not reintroduced later. Since `|E_X|<55`, its certificate mass satisfies

\[
\boxed{
\widehat m(\widehat Z_X)=\sum_e\nu_X(e)\le54.
}
\tag{L-91659.6}
\]

Write

\[
\mathcal R_X=R_X\widehat Z_X
\tag{L-91659.7}
\]

for its complete physical realization.

## 3. Current datum is the coordinatewise complement

Work in the real vector space generated by complete endpoint data and define

\[
\boxed{
\mathcal C_X=\mathcal N_X-\mathcal R_X.
}
\tag{L-91659.8}
\]

This is an exact identity of complete data:

\[
\boxed{
\mathcal N_X=\mathcal C_X+R_X\widehat Z_X.
}
\tag{L-91659.9}
\]

The content of the root theorem is that `C_X` is an admissible current datum with uniformly bounded positive deficit. Its coordinates are not left implicit:

\[
J(\mathcal C_X)=J_\Lambda(X)-J(\mathcal R_X),
\tag{L-91659.10}
\]

\[
w(\mathcal C_X)=w_X-w(\mathcal R_X),
\qquad
\Omega(\mathcal C_X)=\Omega_X-\Omega(\mathcal R_X),
\tag{L-91659.11}
\]

\[
q(\mathcal C_X)=q_X-q(\mathcal R_X),
\quad
\Gamma(\mathcal C_X)=\Gamma_X-\Gamma(\mathcal R_X),
\quad
\Xi(\mathcal C_X)=\Xi_X-\Xi(\mathcal R_X),
\tag{L-91659.12}
\]

\[
b(\mathcal C_X)=b_X-b(\mathcal R_X).
\tag{L-91659.13}
\]

The benchmark coordinate may be signed. Admissibility requires nonnegative physical capacities and an explicit feasible current row, not positivity of the benchmark scalar.

## 4. One-use construction of the current row

Define

\[
d_X^{\rm cur}
=d_X^{\rm edge}+d_X^{\rm outer}+d_X^{\rm port}.
\tag{L-91659.14}
\]

The three terms are:

1. `d_edge`: the nonnegative interval/butterfly rows generated by the matched Hall transports `t_(o,e)`; `L-91340/L-91341` give exact score transport, target subordination, and every-row nonnegativity.
2. `d_outer`: the single B-spline-quantized outer producer after one global interior safety factor and one terminal omission; `L-91110/L-91111/L-91114/L-91115` give nonnegative endpoint weights, exact bulk modes, the positive collar, every physical outer-column inequality, and bounded score charge.
3. `d_port`: the two positive endpoint channels of the common Euler/Schur boundary port; `L-91316` gives strict positivity and uniformly bounded critical mass.

The construction order is mandatory:

```text
sum every Hall/current contribution;
apply the interior safety factor once to the total outer producer;
omit the top endpoint packet once;
apply the common two-channel port once;
leave all unused target/detail/boundary capacity as slack.
```

No correction is copied to each rough color or each recursive child.

## 5. Coordinate verification ledger

The frozen imports give the following coordinate statements.

| Coordinate | Recursive use | Current complement and proof source |
|---|---:|---|
| score benchmark | `J(R_X Z_X)` | exact difference (L-91659.10) |
| SHARP/ordinary target | target-subordinate | Hall residual plus unused positive target, `L-91340` |
| exact finite rows | nonnegative | Hall edge row bonuses plus residual row, `L-91341` |
| ordinary columns | exact linear response | positive telescope from detail, `L-90029` |
| radix-four columns | exact linear response | `L-90027`, `L-91111`, `L-91114`, `L-91115` |
| quantization collar | none recursive | one positive width-three current seed, `L-91111` |
| finite/continuum mismatch | none recursive | one global safety charge, `L-91114` |
| terminal annulus | none recursive | one top omission, `L-91115` |
| common endpoint port | none recursive | one two-channel current port, `L-91316` |

Consequently the capacity coordinates in (L-91659.11)--(L-91659.13) are nonnegative and

\[
\boxed{
d_X^{\rm cur}\in\mathcal F_X(\mathcal C_X).}
\tag{L-91659.15}
\]

The statement uses `L-91112` only for its explicitly retained positive component formula and finite equality-row identity; the refuted Volterra equality is not imported.

## 6. Uniform root debt

The Hall score is exact and the Hall target is subordinate. The edge/butterfly score is nonnegative. The collar safety loss, finite mismatch loss, fixed-width top omission, and common port each have an absolute score charge by the frozen imports. Therefore there is one absolute constant `C_root` such that

\[
\boxed{
J(\mathcal C_X)-\operatorname{Score}(d_X^{\rm cur})
\le C_{\rm root}.
}
\tag{L-91659.16}
\]

Equivalently,

\[
\boxed{
\Delta_X(\mathcal C_X)\le C_{\rm root}.
}
\tag{L-91659.17}
\]

The constant is the sum of four current charges, each applied once; it is not multiplied by the number of rough primes or children.

## 7. Root deficit inequality

Let `d_rec` be any feasible packing for `R_X Z_X`. By (L-91659.9), (L-91659.15), and linear feasibility,

\[
d_X^{\rm cur}+d_{\rm rec}
\in\mathcal F_X(\mathcal N_X).
\]

Taking suprema gives the exact subadditive root inequality

\[
\boxed{
\Delta_X(\mathcal N_X)
\le C_{\rm root}+\Delta_X(R_X\widehat Z_X),
\qquad
\widehat m(\widehat Z_X)\le54.
}
\tag{L-91659.18}
\]

This is the complete root identity requested by PR #443.

## 8. Review boundary

The algebraic datum identity (L-91659.9), coefficient mass (L-91659.6), and implication (L-91659.18) are exact once the imported coordinate statements are accepted. Independent review must reconstruct every cited finite inequality at the locked blob and verify that the datum named `N_X` matches the endpoint consumer normalization.

```text
root datum defined in every coordinate             EXACT
recursive certificate and mass <=54               EXACT
current datum defined by exact complement          EXACT
one-use correction order                          EXPLICIT
current feasibility/debt                          PROPOSED FROM FROZEN IMPORTS
root deficit inequality                           EXACT CONDITIONAL ON IMPORTS
Riemann Hypothesis                                UNPROVEN
```
