# L-96651 — Reserve-preserving grouped P61 Hall for the parity-resummed scalar

Status: **PROPOSED COMPLETE SOURCE THEOREM — PRIMARY HOSTILE TARGET**

The complete P61 source is grouped before Hall.  Safe child packets are extracted from the exact parent hazard modes with coefficient `alpha=lambda/sqrt(p)`.  Every child occurrence maps to an activated parent occurrence at the same physical quotient, with a complete owner label `(d,history,p,m,side)`.  The extracted reserves are disjoint.  Hall is performed on the complement, and matched reserve removal preserves demand conservation and target barycenter.  The positive 5:3 dictionary makes each reserve scalar nonnegative and gives

```text
g_v >= sum_w alpha(v,w) g_w.
```

No unrestricted rough comparison sum and no separate-color positivity are used.
