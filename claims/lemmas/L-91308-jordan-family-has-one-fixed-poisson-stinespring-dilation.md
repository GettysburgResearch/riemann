# L-91308 — The complete generalized-prime Jordan family has one fixed nested-Poisson Stinespring dilation

Claim ID: `L-91308`  
Status: **PROVED EXACT FIXED-COLLIGATION THEOREM FOR THE JORDAN FACTOR; COMPLETED SUZUKI TRANSPORT OPEN**  
Created: 2026-08-12  
Depends on: `L-91037`, `L-91305`  
RH status: **unproved**

## 1. The normalized Jordan multiplier

Fix `c>1`.  For `a>0`, put

\[
 Q_a(s)=\frac{\zeta(s)}{\zeta(s+2a)}
\]

and normalize the safe vertical multiplier by

\[
 \boxed{
 \varphi_a(t)=\frac{Q_a(c+it)}{Q_a(c)}.
 }
\tag{L-91308.1}

The Euler logarithm gives

\[
\begin{aligned}
 \log\varphi_a(t)
 =\sum_{n=p^k}
 \frac{1-n^{-2a}}{k}n^{-c}
 (e^{-it\log n}-1).
\end{aligned}
\tag{L-91308.2}

Every coefficient is nonnegative and the total mass is finite because `c>1`.
Thus `varphi_a` is the characteristic function of a compound-Poisson law.

## 2. One common marked Poisson process

Let

\[
 \mathfrak X=(0,\infty)\times\mathcal P^*
\]

and define the sigma-finite measure

\[
 \boxed{
 d\mu_c(r,n)
 =2\Lambda(n)n^{-c-2r}\,dr
 }
\tag{L-91308.3}

with counting measure in the prime-power coordinate.

For every finite `a`,

\[
\begin{aligned}
 \mu_c((0,a]\times\{n\})
 &=2\Lambda(n)n^{-c}\int_0^a n^{-2r}dr\\
 &=\frac{1-n^{-2a}}{k}n^{-c},
\end{aligned}
\tag{L-91308.4}

because `Lambda(p^k)=log p=(log n)/k`.

Let `N` be one Poisson random measure on `mathfrak X` with intensity `mu_c`.
Define the nested compound-Poisson process

\[
 \boxed{
 X_a
 =\int_{(0,a]\times\mathcal P^*}
  \log n\,N(dr,dn).
 }
\tag{L-91308.5}

Then the exponential formula for a Poisson random measure gives

\[
\begin{aligned}
 \mathbb E[e^{-itX_a}]
 &=\exp\left\{
   \int_{(0,a]\times\mathcal P^*}
   (e^{-it\log n}-1)d\mu_c
  \right\}\\
 &=\boxed{\varphi_a(t)}.
\end{aligned}
\tag{L-91308.6}

All radial scales therefore live on one probability space and are coupled by
independent increments in the mark `r`.

## 3. Fixed Stinespring dilation

Let

\[
 \mathcal S=L^2(\mathbb R_t\times\Omega,dt\,d\mathbb P),
 \qquad
 \mathcal H=L^2(\mathbb R_t,dt).
\]

Define

\[
 (V_af)(t,\omega)=e^{-itX_a(\omega)}f(t).
\tag{L-91308.7}

Each `V_a:H->S` is an isometry.  Let the fixed contraction

\[
 C:\mathcal S\to\mathcal H
\]

be conditional expectation over `Omega`:

\[
 (CF)(t)=\mathbb E[F(t,\cdot)].
\tag{L-91308.8}

Then

\[
 \boxed{
 CV_a=M_{\varphi_a}
 }
\tag{L-91308.9}

for every `a>0`.  This is one `a`-independent Stinespring observation map for
the complete Jordan radial family.

No per-scale dilation, arbitrary metric, or zero data are used.

## 4. Common first-chaos curvature space

The compensated Poisson first chaos is canonically

\[
 \mathcal K_c=L^2(\mathfrak X,\mu_c).
\]

For `Re(s)>1/2`, define

\[
 \boxed{
 w_{a,s}(r,n)
 =\frac2a\,1_{0<r<a}
  \sqrt{r\Lambda(n)\log n}\,n^{-s-r}.
 }
\tag{L-91308.10
}

Although `mu_c` is the probability-space intensity at one fixed vertical line,
(L-91308.10) is naturally read in the common counting/Lebesgue first-chaos
coordinates of `L-91037`.  Its Gram is

\[
 \boxed{
 \langle w_{a,s},w_{a,t}\rangle
 =\mathscr C_a^{\rm J}(s+\overline t),
 }
\tag{L-91308.11
}

where

\[
 \mathscr C_a^{\rm J}(z)
 =-\partial_a\left[a^{-1}\log Q_a(z)\right].
\]

Thus the positive normalized Jordan curvature at every `a` is resident in one
nested first-chaos space.  The dependence on `a` is only the cutoff
`1_(0,a)` and the explicit radial normalization.

## 5. Exact increment coupling

If `0<a<b`, then

\[
 X_b=X_a+X_{(a,b]},
\]

where the increment is independent of the sigma-field generated by `X_a`.
Consequently

\[
 \varphi_b(t)
 =\varphi_a(t)\,arphi_{a,b}(t)
	ag{L-91308.12}

with `varphi_(a,b)` another compound-Poisson characteristic function.

At the first-chaos level,

\[
 L^2((0,b)\times\mathcal P^*)
 =L^2((0,a)\times\mathcal P^*)
  \oplus
  L^2((a,b)\times\mathcal P^*).
\tag{L-91308.13}

This is the exact radial product-system structure needed by a fixed
colligation.  It removes the possible connection curvature caused by choosing
unrelated source realizations at different scales.

## 6. What remains in the completed family

The multiplier `varphi_a` is the normalized generalized-prime contraction on a
safe vertical line.  Suzuki's completed object is the inner multiplier

\[
 \Theta_a(z)
 =\frac{\xi(1/2-a-iz)}{\xi(1/2+a-iz)},
 \qquad a>1/2.
\]

The gamma/pole completion converts a contractive characteristic-function
channel into a boundary inner scattering channel.  `L-91035` closes this at
zeroth order separately for each `a`.

The only remaining fixed-colligation step is now:

> construct one `a`-independent completion/observation of the common Poisson
> product system (L-91308.12)--(L-91308.13), together with the gamma/pole
> channels, whose observed boundary family is `M_(Theta_a)`.

Once such a fixed completion exists and its source normal curvature is the sum
of (L-91308.11) and the explicit gamma/pole curvature, `L-91305` gives the
required tangent domination and positive auxiliary reserve.

## 7. Proof boundary

```text
Jordan characteristic-function family                 EXACT
one common nested Poisson probability space             EXACT
fixed conditional-expectation Stinespring observation   EXACT
common radial first-chaos curvature                      EXACT
independent-increment/product-system coupling             EXACT
fixed gamma/pole-to-Suzuki completion                    OPEN / RH-BEARING
completed source-normal curvature identification         OPEN
Riemann Hypothesis                                       UNPROVED
```
