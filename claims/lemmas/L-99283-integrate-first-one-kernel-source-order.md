# L-99283 — Integrate first, recurse second: the one-kernel source order avoids derivative-fibre heredity

Claim ID: `L-99283`  
Status: **PROVED ORDER-OF-OPERATIONS AND COMMON-KERNEL THEOREM; ACTUAL FRAME LEDGER EXPLICITLY SEPARATED**  
Created: 2026-08-20  
Depends on: the one-colour Volterra identity, `L-99280`, and the random-key
source theorem  
RH status: **not assumed**

## 1. Forbidden order

The operation

\[
p_s-p^{-1/2}p_{s/p}
\]

is not positive in general. The exact review-503 witness

\[
(p,y,s,j)=(67,15,1005,14)
\]

must remain a fail-closed regression. Therefore no proof may apply the rough
causal operator directly to the infinitesimal Volterra fibre.

## 2. Correct order

For each Möbius colour \(k\), first use the exact one-colour identity

\[
\frac1{\sqrt k}
\int_n^{X/k}
t^{-1/2}\log\frac{X}{kt}\,dt
=
\int_n^{X/k}
\frac2s\ell_{X/s}(k)g_s(n)\,ds.
\tag{L-99283.1}
\]

Apply the finite component-row map and perform finite Fubini. This produces the
already-integrated canonical endpoint packet. Only after this integration is
the compact Hall source partition applied.

`L-99280` proves that the target Hall flow is simultaneously valid in every
component row. Represent each positive Hall target by a literal interval,
partition matched source and residual source, and put only the residual source
into the rough-prime stage.

For the rough stage, use the endpoint-nested vector measure and a single random
key. Genuine alpha-child cylinders are disjoint because their total density is
below \(1/8\). The complement is the exact positive causal current. The same
source partition is used for target, the selected component row, provenance,
and the signed calibration label.

## 3. Common-kernel conclusion

At every typed node there is one positive child kernel \(\kappa_Y\), not one
kernel per coordinate. In a fixed component row,

\[
E_Y=J_Y+\int E_Z\,d\kappa_Y(Z)+C_Y,
\tag{L-99283.2}
\]

where `J_Y>=0`, only alpha-children occur in `kappa_Y`, and `C_Y` is the
source-owned finite/continuum calibration.

Thus the exact source order is

```text
one-colour Volterra integration
 -> compact Hall source partition
 -> residual-only random-key child partition
 -> positive current + one signed calibration
 -> alpha-only resolvent.
```

This theorem removes derivative-fibre heredity and separate-coordinate
couplings. The remaining repository-specific obligation is to write the actual
finite/continuum and anchored terms into `C_Y` and verify that they belong to
the subpower class of `L-99281`.
