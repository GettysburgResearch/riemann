# L-99252 — The SHARP Harnack defect squares the 67 factor and is positive through one hundred million

Claim ID: `L-99252`  
Status: **PROVED EXACT ALGEBRA + COMPUTER-ASSISTED DIRECTED ALL-REAL CERTIFICATE**  
Created: 2026-08-19  
Depends on: `L-99251`  
RH status: **unproved**

## 1. Exact local square

Define

\[
\beta_{67}(n)
:=\mu(n)-\mathbf1_{67\mid n}\mu(n/67).
\tag{L-99252.1}
\]

Finite reindexing gives

\[
\boxed{
\mathfrak H_{67}(x)
=
\sum_{n\le x}\frac{\beta_{67}(n)}{\sqrt n}T(x/n).
}
\tag{L-99252.2}
\]

Write `n=67^f m` with `(m,67)=1`. Then

\[
\boxed{
\beta_{67}(n)=c_{67}(f)\mu(m),
\qquad
c_{67}(0,1,2)=(1,-2,1),
}
\tag{L-99252.3}
\]

and the coefficient is zero for `f>=3`. Thus the dilation multiplies the
reciprocal-zeta source by one additional `(1-67^{-z})`, giving a squared local
factor.

## 2. Cell normal form

Put

\[
A_\beta(N)=\sum_{n\le N}\frac{\beta_{67}(n)}n,
\qquad
B_\beta(N)=\sum_{n\le N}\frac{\beta_{67}(n)}{\sqrt n}.
\]

On every half-open integer cell `[N,N+1)`,

\[
\boxed{
\mathfrak H_{67}(x)
=4\sqrt x\,A_\beta(N)-3B_\beta(N).
}
\tag{L-99252.4}
\]

It is monotone on the cell. Therefore an all-real proof needs only the left
endpoint and the pre-activation right limit of every cell.

## 3. Directed arithmetic contract

The retained scanner uses `S=2^50` and maintains outward integer intervals for
both prefix sums:

\[
\frac{\lfloor S/n\rfloor}{S}
\le\frac1n\le
\frac{\lceil S/n\rceil}{S},
\]

and the unique integer `q_n` satisfying

\[
q_n^2n\le S^2<(q_n+1)^2n
\]

gives the enclosure of `1/sqrt(n)`. At a cell endpoint `x`, an independently
corrected integer square root encloses `sqrt(x)`. Sign-aware interval
multiplication then encloses (L-99252.4) with denominator `S^2=2^100`.

The Möbius values are generated by a segmented radical sieve excluding the
already explicit 67-adic factor. Every floating estimate in the producer is
followed by an exact `unsigned __int128` inequality correction; no ordinary
floating comparison enters the acceptance decision.

## 4. Exact finite theorem

The complete scan proves

\[
\boxed{
\mathfrak H_{67}(x)>0
\qquad
(67\le x<100{,}000{,}001).
}
\tag{L-99252.5}
\]

Together with the independently replayed compact base, this proves the defect
positive for every real `1<=x<100,000,001`.

The global directed minimum is attained at the pre-activation right limit of
the cell `[200,201)`:

\[
 x=201^-.
\]

Its exact enclosure is

\[
\boxed{
\frac{1626923303441334483980962730376}{2^{100}}
\le
\min\mathfrak H_{67}
\le
\frac{1626923303449543135335825015140}{2^{100}}.
}
\tag{L-99252.6}
\]

Numerically this is

\[
1.28341618987788997
<\min\mathfrak H_{67}
<1.28341618988436545.
\]

The producer encounters `60,806,035` nonzero local-square coefficients through
`10^8`.

## 5. Finite consequences

Dilation descent gives

\[
\Psi(x)\ge0
\qquad(1\le x<100{,}000{,}001).
\tag{L-99252.7}
\]

Using the positive kernel of PR #642 then gives

\[
c_X(j)\ge0
\qquad
(1\le X<100{,}000{,}001,\ j\ge2).
\tag{L-99252.8}
\]

These are finite theorems only.

## 6. Replay and boundary

```bash
cd experiments/X-99250-sharp-harnack
python3 verify.py
./replay.sh --full
```

The full mode recompiles the producer, rescans all `10^8` cells, and requires
byte-identical stdout.

```text
local-square coefficient formula        exact
compact SHARP Hall base                 exact directed replay
all-real Harnack sign below 10^8+1      exact directed certificate
global tail at and above 100000001      open / RH-bearing
Riemann Hypothesis                       unproved
```
