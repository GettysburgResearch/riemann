# L-91692 — Target Hall is total-row transparent and the factor-67 compact reserve is explicit

Claim ID: `L-91692`  
Status: **PROVED EXACT/DIRECTED COMPACT-RESERVE THEOREM ON FROZEN INPUTS — BROADER IMPORTS REQUIRE REVIEW**  
Created: 2026-08-14  
Depends on: `L-91107`, `L-91110`, `L-91111`, `L-91114`, `L-91115`, `L-91320`, `L-91378`, `L-91650`, `L-91674`, `L-91682`, `L-91690`, `L-91691`, `T-91312`  
Replay: `X-91692-factor67-reserve-closure`  
RH status: **unproved**

## 1. Statement

Put

\[
K=K_X=\left\lfloor\frac X{67}\right\rfloor+1,
\qquad W=10000,
\tag{L-91692.1}
\]

and realize the factor-67 outer endpoint frame only on

\[
K+2\le s\le X-W-2.
\tag{L-91692.2}
\]

The bottom width-two strip and the top width-`W` strip are retained as explicit
unused source.  They are not copied to a child.

On the frozen endpoint, Hall, port and causal interfaces, the construction of
`L-91690/L-91691` has all of the following properties:

1. the target-Hall operation creates **zero** finite/continuum error in the
   total physical row;
2. the B-spline collar is controlled by the equality-row density
   `L(x)`, which remains strictly below two on the complete window `x<67`;
3. the global factor
   \[
   \sigma_K=\frac{K}{K+178}
   \tag{L-91692.3}
   \]
   leaves a strict explicit reserve in every interior native detail column;
4. the fixed top omission leaves a strict explicit reserve in every terminal
   column;
5. all branch ports aggregate into one common `P_61/67` port;
6. after the global thinning, causal children may be grouped by rough prime,
   so the outer coefficients still sum to less than `1/8` rather than being
   repeated once per source fibre.

Thus the abstract atom-error and correction-operator-norm gate isolated in the
previous compact-reserve proposal is unnecessary.  The remaining physical
errors are bounded directly, coordinate by coordinate.

## 2. Pointwise target-Hall transparency

Fix `1<=x<67`.  For the positive target atoms of `L-91690`, write

\[
T_e=T_x(e),\qquad T_o=T_x(o),
\]

and let `t_(o,e)>=0` be the no-upward target-Hall flow.  Put

\[
r_e=T_e-\sum_ot_{o,e}\ge0.
\tag{L-91692.4}
\]

For one physical component row `j`, define

\[
\rho_j(k)=\frac{A_{x,j}(k)}{T_x(k)}
=\frac{Q_{x/k}(j)}{4\sqrt{x/k}-3}.
\tag{L-91692.5}
\]

Frozen `L-91682`, as used in `L-91690`, gives

\[
e\le o\quad\Longrightarrow\quad \rho_j(e)\ge\rho_j(o).
\tag{L-91692.6}
\]

Demand conservation and (L-91692.4) give the exact identity

\[
\begin{aligned}
\sum_eT_e\rho_j(e)-\sum_oT_o\rho_j(o)
={}&\sum_er_e\rho_j(e)\\
&+\sum_{o,e}t_{o,e}[\rho_j(e)-\rho_j(o)].
\end{aligned}
\tag{L-91692.7}
\]

The second term is coefficientwise nonnegative.  It is precisely the current
row bonus of `L-91690`.  Therefore

\[
\boxed{
\text{signed equality row}
=
\text{positive Hall residual row}
+
\text{positive Hall bonus row}.
}
\tag{L-91692.8}
\]

This is an identity **before** ordinary or radix-four observation.  Hence every
linear ordinary response preserves it.  Applying the ordinary maps at `q` and
`4q` separately and only then subtracting proves the same identity in every
radix-four coordinate.

Consequently

\[
\boxed{
\varepsilon_{\rm Hall,row}=0.
}
\tag{L-91692.9}
\]

There is no `188` or `10152` amplification in the total physical row.  Those
stability constants remain useful for approximate atom maps, but the present
factor-67 construction uses the exact finite source formulas and the exact row
identity (L-91692.7).

## 3. The quantizer sees the equality density, not the SHARP target density

The continuum endpoint inverse of `L-91107` is

\[
L(x)=2\sqrt x\sum_{n\le x}\frac{\mu(n)}n
     -\sum_{n\le x}\frac{\mu(n)}{\sqrt n}.
\tag{L-91692.10}
\]

Although target Hall is normalized by

\[
\Psi(x)=4\sqrt x\sum_{n\le x}\frac{\mu(n)}n
        -3\sum_{n\le x}\frac{\mu(n)}{\sqrt n},
\]

equation (L-91692.8) says that the total row presented to the endpoint
quantizer is still the original equality row generated by `L`, not a row
generated by `Psi`.

On one arithmetic cell `N<=x<N+1`, put

\[
A_N=\sum_{n\le N}\frac{\mu(n)}{\sqrt n},
\qquad
B_N=\sum_{n\le N}\frac{\mu(n)}n.
\]

Then

\[
L(x)=2B_N\sqrt x-A_N.
\tag{L-91692.11}
\]

It is monotone on the cell, with direction determined by the rational number
`B_N`.  Directed evaluation of the two one-sided endpoints of all `66` cells
proves

\[
\boxed{
\frac{159}{500}<L(x)<\frac{183}{100}<2
\qquad(1\le x<67).
}
\tag{L-91692.12}
\]

The maximum is the left limit at `x=2`, namely `2sqrt(2)-1`.  Thus the
`M=2` collar bound of `L-91111` remains valid unchanged on the enlarged
factor-67 window.

This distinction is load-bearing: `Psi` itself exceeds two near `x=2`, but it
is a target normalization, not the density quantized in the equality row.

## 4. Support-compatible source thinning

Since `K>X/67`, every retained endpoint fibre in (L-91692.2) satisfies

\[
\frac Xs\le\frac X{K+2}<67.
\tag{L-91692.13}
\]

Hence root Hall still sees only `P_61`.  Starting at `K+2` also places the
width-three B-spline collar entirely at integer nodes at least `K+1`, exactly as
required by `L-91110/L-91111`.

The omitted bottom strip `[K,K+2)` is positive and has fixed width.  It is
labelled unused thinning.  Omitting it can only improve every capacity
inequality and has bounded score cost.  The exact inner recursive packet of
`L-91690/L-91691` remains at endpoint at most

\[
K\le X/67+1;
\]

the bottom omission is not added to that child and therefore does not enlarge
the recursive scale.

## 5. Explicit interior reserve

Let

\[
E_X=b_X^\star-\overline b_X^\star
\]

be the genuine nonzero finite/continuum discrepancy and let `C_X>=0` be the
B-spline collar.  As in `L-91691`, define

\[
C_{67}
=\sum_{k\le67}\frac{|\mu(k)|}{\sqrt k}
 \left(1+\frac12\log\frac{67}{k}\right)<19.
\tag{L-91692.14}
\]

The adjacent quadrature identity gives, for `q>=K`,

\[
|v_q(E_X)|<\frac{57}{2}q^{-3/2},
\tag{L-91692.15}
\]

\[
|\mathcal D_4v_q(E_X)|<\frac{285}{8}q^{-3/2}.
\tag{L-91692.16}
\]

Equation (L-91692.12) permits the unchanged collar estimate

\[
|\mathcal D_4v_q(C_X)|<\frac{200}{q\sqrt K}.
\tag{L-91692.17}
\]

For `K<=q<=X/4`, one has

\[
\Omega_X(q)=\frac{\log4}{\sqrt q}>\frac4{3\sqrt q}.
\]

Therefore

\[
\begin{aligned}
\frac{|\mathcal D_4v_q(C_X-E_X)|}{\Omega_X(q)}
&<\frac{855}{32q}+\frac{150}{\sqrt{qK}}\\
&\le\frac{5655}{32K}.
\end{aligned}
\tag{L-91692.18}
\]

After multiplication by `sigma_K=K/(K+178)`, the worst possible used detail is
at most

\[
\frac{K+5655/32}{K+178}\,\Omega_X(q)
=
\left(1-\frac{41}{32(K+178)}\right)\Omega_X(q).
\tag{L-91692.19}
\]

Hence every interior column has the strict reserve

\[
\boxed{
 s_X(q)
 \ge
 \frac{41}{32(K+178)}\Omega_X(q)>0.
}
\tag{L-91692.20}
\]

This is the requested explicit reserve inequality.  No unspecified atom
constant, correction norm or compact maximum remains.

By the positive radix-four inverse, ordinary feasibility follows from the same
single detail comparison.

## 6. Explicit terminal reserve

For `q>X/4`, the factor-67 collar contributes less than

\[
512\sqrt{67}\,X^{-3/2}<4224X^{-3/2},
\tag{L-91692.21}
\]

and (L-91692.15) contributes less than

\[
228X^{-3/2}.
\tag{L-91692.22}
\]

Thus possible terminal overfill is strictly below

\[
4452X^{-3/2}.
\tag{L-91692.23}
\]

On the omitted top interval one has `1<=X/s<2`, so

\[
L(X/s)=2\sqrt{X/s}-1\ge1.
\tag{L-91692.24}
\]

There is no odd root source on this cell; target Hall is the identity.  The top
packet is therefore literal source-owned equality mass.  Frozen `L-91115`
proves that its omission removes more than

\[
5033X^{-3/2}
\tag{L-91692.25}
\]

from every terminal column which can overfill.  The remaining strict margin is

\[
\boxed{
(5033-4452)X^{-3/2}
=581X^{-3/2}>0.
}
\tag{L-91692.26}
\]

For `q>=X-W`, triangular support makes the retained response zero.  The global
factor `sigma_K<1` only improves all terminal inequalities.

## 7. One aggregate `P_61/67` port

For one positive labelled branch `b`, let

\[
M_b=
\begin{pmatrix}
\mathcal V_b&\mathcal B_b\\
\mathcal B_b&\mathcal V_b
\end{pmatrix}.
\]

The `P_61/67` theorem `L-91320` gives

\[
M_b\succeq\frac19\mathcal V_b I_2,
\qquad
\tau_{p_b}\mathcal V_b<\frac19\mathcal V_b,
\tag{L-91692.27}
\]

where `tau_p=p^(-1/2)(1-p^(-1/2))`.  Let `beta_b>=0` be arbitrary source-owned
branch weights.  Summing before port observation gives

\[
\sum_b\beta_bM_b
\succeq
\frac19\left(\sum_b\beta_b\mathcal V_b\right)I_2
>
\left(\sum_b\beta_b\tau_{p_b}\mathcal V_b\right)I_2.
\tag{L-91692.28}
\]

Thus one aggregate port covers the aggregate correction.  There is no port per
leaf and no port per child.  The exact normalized mass remains bounded by

\[
\prod_{p\le61}\left(1+\frac1p\right)<\frac{14}{3}.
\tag{L-91692.29}
\]

The argument uses only the Schur-reserve part of `L-91320`; the old colored
physical projection is not imported.

## 8. Thin once, then group the causal children

Let `Q_XP_X` denote the single positive packet remaining after the bottom/top
omissions and the global factor `sigma_K`.  Keep all endpoint, small-divisor
and rough-owner labels inside this one packet.

Apply `L-91650` to the **aggregate packet**, not independently with a new outer
coefficient list on every source fibre.  For the ordered active rough primes,

\[
Q_XP_X
=s_kQ_XP_X
+\sum_i\lambda_i
 \left(Q_XP_X-r_iU_{p_i}Q_{X/p_i}P_{X/p_i}\right)
+\sum_i\alpha_iU_{p_i}Q_{X/p_i}P_{X/p_i}.
\tag{L-91692.30}
\]

Linearity permits all fibrewise child contributions having the same rough prime
`p_i` to be absorbed into the single positive packet `P_{X/p_i}` inside the
coefficient `alpha_i`.  The outer coefficients are therefore not repeated:

\[
\boxed{
\sum_i\alpha_i<67^{-1/2}<\frac18.
}
\tag{L-91692.31}
\]

The Hall row bonus remains current.  The current causal terms plus the grouped
children equal the one already feasible packet `Q_XP_X` in source, row,
ordinary, detail and port coordinates.  Hence the causal split creates no new
capacity or slack problem.

## 9. Score cost

The global thinning loss obeys

\[
1-\sigma_K=\frac{178}{K+178}<\frac{178}{K}<\frac{11926}{X}.
\tag{L-91692.32}
\]

Multiplying the frozen polynomial score majorant for the outer equality
producer by (L-91692.32) gives a bounded, in fact vanishing, safety-factor cost.
The bottom and top omissions have fixed endpoint width, the martingale
quantizer is score-favourable, and the aggregate port has bounded normalized
mass.  Therefore the compact realization adds one absolute score charge.

Together with the causal target-mass debt and subcritical envelope already
recorded in `T-91660`, this gives

\[
J_\Lambda(X)-\operatorname{Score}(d_X)
\le4\log X+C
=o(\log^2X).
\tag{L-91692.33}
\]

The exact radix-four dual identifies the left side with the `Y_4`-weighted
native slack.

## 10. What has been closed

The previous unresolved compact obligation asked for:

```text
an atom-error constant A_atom;
a correction-operator norm A_fin;
a support-compatible reserve denominator;
a global M independent of X;
one-use correction and port ownership.
```

For the exact factor-67 root construction these are replaced by stronger facts:

```text
Hall-induced total-row atom error             exactly zero;
equality-row density                          159/500 < L < 183/100;
interior reserve                               41 Omega/[32(K+178)];
terminal reserve                               581 X^(-3/2);
zero-denominator terminal columns              zero retained response;
common port                                    one aggregate Schur inequality;
recursive coefficient list                    grouped once, sum <1/8.
```

Accordingly the compact finite-root reserve obligation is closed on the frozen
interfaces.

```text
target Hall total-row transparency             EXACT
factor-67 equality-density upper bound         DIRECTED EXACT
support-compatible bottom thinning             EXACT
interior native reserve                        EXACT
terminal native reserve                        EXACT
one aggregate P61/67 port                      EXACT
thin-once grouped causal reset                  EXACT
compact reserve proof obligation               CLOSED ON FROZEN INPUTS
full SONTR composition                         CANDIDATE COMPLETE / REVIEW
Riemann Hypothesis                             UNPROVEN
```
