# L-91379 — The finite-Euler row is the native row plus an exact rough reservoir

Claim ID: `L-91379`  
Status: **PROVED EXACT SOURCE/ROW/CAPACITY/ENTROPY DECOMPOSITION**  
Created: 2026-08-14  
Depends on: unique factorization and `L-91377`  
RH status: **unproved**

## 1. Rough monoid

Let

\[
 P=P_{61}=\prod_{p\le61}p
\]

and let

\[
 \mathcal R_{67}
 =\{m\ge1:p\mid m\Rightarrow p\ge67\}
\]

be the multiplicative monoid generated by the rough primes, including `m=1`.
Let

\[
 \mu_P(n)=\mu(n)\mathbf1_{n\mid P}.
\]

The Dirichlet-convolution identity is

\[
\boxed{
 \mu_P=\mathbf1_{\mathcal R_{67}}*\mu.
}
\tag{L-91379.1}
\]

Indeed, factor `n=dm` into its small-prime and rough parts. The rough divisor
sum of `mu` vanishes unless the rough part is one, while repeated small primes
are killed by `mu`.

## 2. Exact row decomposition

The canonical finite-Euler row is

\[
 D_{P,X}(j)=\sum_{d\mid P}\frac{\mu(d)}{\sqrt d}Q_{X/d}(j).
\]

Using (L-91379.1) and finite rearrangement,

\[
\boxed{
 D_{P,X}(j)
 =\sum_{\substack{m\in\mathcal R_{67}\\m\le X/j}}
  \frac1{\sqrt m}\,c_{X/m}(j),
}
\tag{L-91379.2}
\]

where `c` is the full native Möbius row of `L-91377`.

This is an identity of signed rows; no positivity of the summands is asserted.

## 3. Simultaneous physical coordinates

Every relevant physical map is linear. Applying ordinary columns, radix-four
columns, and literal entropy to (L-91379.2) gives

\[
\boxed{
 C_{P,X}(q)
 =\sum_{m\in\mathcal R_{67}}m^{-1/2}w_{X/m}(q),
}
\tag{L-91379.3}
\]

\[
\boxed{
 \Theta_{P,X}(q)
 =\sum_{m\in\mathcal R_{67}}m^{-1/2}\Omega_{X/m}(q),
}
\tag{L-91379.4}
\]

and

\[
\boxed{
 \mathcal E_P(X)
 =\sum_{m\in\mathcal R_{67}}m^{-1/2}J_\Lambda(X/m).
}
\tag{L-91379.5}
\]

The `m=1` terms are exactly the native root datum. The remaining terms are the
positive **capacity and benchmark reservoir**, although their native rows are
signed before a physical producer is supplied.

## 4. Least-prime form

Every rough `m>1` has a unique least prime `p>=67`; write `m=pu` with all prime
factors of `u` at least `p`. Therefore, with

\[
 H_{\ge p}(Z)
 =\sum_{\substack{u\le Z\\P^-(u)\ge p}}
  u^{-1/2}\log(Z/u),
\]

one obtains

\[
\boxed{
 H_P(Z)=\log Z+
 \sum_{\substack{p\ge67\\p\le Z}}
 p^{-1/2}H_{\ge p}(Z/p).
}
\tag{L-91379.6}
\]

The same least-prime partition holds in detail and benchmark coordinates.
No source atom occurs twice.

## 5. Exact meaning of the open theorem

Equation (L-91379.2) explains both the power and the limitation of the canonical
row:

```text
D_P may be coefficientwise positive;
its native term c_X is still signed;
the rough reservoir dominates the sign obstruction;
subtracting recursive certificates requires an actual positive row allocation.
```

The remaining Native-Root Capacity Theorem must convert the exact signed
identity (L-91379.2) into a positive physical disintegration while preserving
(L-91379.3)--(L-91379.5) and the one-use least-prime ledger.
