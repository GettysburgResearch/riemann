# L-32409 — Scope correction: the divisor-fiber collar belongs to the extra-floor transform

Claim ID: `L-32409`  
Status: **SUPERSEDED AS A PHYSICAL-COLLAR CLAIM**

The original version used

\[
 \sum_qc_4(q)\mathcal C(X/q,\theta),
\]

which applies the carry/floor kernel to `c_4=e_4*Lambda_4`. The true pole-preserving `Q=4` current has transform `E_4L_4N_theta`; after the zeta factor has been canceled, `c_4` multiplies the centered-interval kernel instead.

Thus the divisor-fiber formula in the original version is one extra floor transform and is not the physical collar required by the RH argument.

The elementary floor observation `j+k in {N-1,N}` remains useful, but in the correctly typed interval field the missing-unit correction is one raw coefficient `c_4`, not `sum_(q|M)c_4(q)`.

The corrected physical collar is supplied in the subsequent replacement lemma. This file is retained only as a firewall against reintroducing the extra-floor source mismatch.
