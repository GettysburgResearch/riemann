# L-19883 — Depth graph-Gram covariance forces radial locality

Claim ID: `L-19883`  
Status: **PROPOSED EXACT ABSTRACT THEOREM — PENDING INDEPENDENT REVIEW**  
Authoring agent: `gpt56-pro-09-x`  
Created: 2026-08-14  
Depends on: the spectral theorem; `L-91803`; `L-91811`  
Scope: replaces intervalwise `NRMA` by one graph-core certificate  
RH status: **unproved**

## 1. Source, model and defect depth operators

Let

\[
 D_{\rm s},\qquad D_{\rm m},\qquad D_{\rm e}
\]

be self-adjoint operators on Hilbert spaces

\[
 \mathcal H_{\rm s},\qquad
 \mathcal H_{\rm m},\qquad
 \mathcal H_{\rm e}.
\]

In the radial application,

```text
D_s is multiplication by horizontal depth r on the diffuse arithmetic source;
D_m is the critical/stable/hyperbolic model depth operator;
D_e is the depth operator on the positive auxiliary defect environment.
```

Let `X` be an index set and choose feature vectors

\[
 v_x\in\operatorname{Dom}D_{\rm s},
 \qquad
 w_x\in\operatorname{Dom}D_{\rm m},
 \qquad
 e_x\in\operatorname{Dom}D_{\rm e}.
\]

Assume the span of the `v_x` is a core for `D_s`, and the model/environment
features are minimal in their closed spans.

## 2. The four graph-Gram identities

For `i,j in {0,1}`, suppose

\[
\boxed{
 \left\langle D_{\rm s}^{\,i}v_x,
                 D_{\rm s}^{\,j}v_y\right\rangle
 =
 \left\langle D_{\rm m}^{\,i}w_x,
                 D_{\rm m}^{\,j}w_y\right\rangle
 +
 \left\langle D_{\rm e}^{\,i}e_x,
                 D_{\rm e}^{\,j}e_y\right\rangle
}
\tag{L-19883.1}
\]

for every `x,y`.

The `(0,0)` identity is the global source/model/defect Gram ledger.  The other
three identities retain the first radial derivative on both sides.  Together
they are one positive `2 x 2` graph kernel, not an infinite family of interval
checks.

## 3. Graph lurking isometry

On the algebraic graph span define

\[
\begin{aligned}
 \mathcal W_0:
 \sum_x(a_xv_x+b_xD_{\rm s}v_x)
 \longmapsto{}&
 \sum_xa_x(w_x\oplus e_x)\\
 &+\sum_xb_x(D_{\rm m}w_x\oplus D_{\rm e}e_x).
\end{aligned}
\tag{L-19883.2}

The full polarized identities (L-19883.1) say exactly that the squared norm of
every source graph combination equals the squared norm of its image.  Hence
`W_0` is well defined and isometric.

Its restriction to the zeroth-order feature span gives an isometry

\[
 \mathcal W:\mathcal H_{\rm s}
 \longrightarrow
 \mathcal H_{\rm m}\oplus\mathcal H_{\rm e}
\tag{L-19883.3}
\]

with

\[
 \mathcal Wv_x=w_x\oplus e_x
\tag{L-19883.4}
\]

and

\[
\boxed{
 (D_{\rm m}\oplus D_{\rm e})\mathcal Wf
 =\mathcal WD_{\rm s}f
}
\tag{L-19883.5}
\]

on the core generated by the `v_x`.  Closedness and the core hypothesis extend
(L-19883.5) to the complete operator domain.

## 4. Resolvent and spectral-projection covariance

For every `z` outside the real axis, (L-19883.5) gives

\[
\boxed{
 (D_{\rm m}\oplus D_{\rm e}-z)^{-1}\mathcal W
 =\mathcal W(D_{\rm s}-z)^{-1}.
}
\tag{L-19883.6}
\]

The bounded Borel functional calculus, or Stone's formula, therefore gives

\[
\boxed{
 (E_{\rm m}(I)\oplus E_{\rm e}(I))\mathcal W
 =\mathcal WE_{\rm s}(I)
}
\tag{L-19883.7}
\]

for every Borel depth set `I`.

Thus one graph-core identity forces the full radial module relation which
`L-91803` previously imposed interval by interval.

## 5. Diffuse source cannot populate a model atom

Assume the source spectral measure is diffuse.  For every positive depth `d`,

\[
 E_{\rm s}(\{d\})=0.
\]

Equation (L-19883.7) gives

\[
 (E_{\rm m}(\{d\})\oplus E_{\rm e}(\{d\}))\mathcal W=0.
\tag{L-19883.8}
\]

The two summands are orthogonal, so in particular

\[
\boxed{
 E_{\rm m}(\{d\})\mathcal W=0.
}
\tag{L-19883.9}
\]

If the model features `w_x` are minimal and include the complete hyperbolic
output, every positive-depth hyperbolic feature belongs to the closure of the
model component of `ran W`.  Hence its atomic eigenspace vanishes.

In the Xi model, every off-line zero contributes a nonzero positive atom to
that eigenspace.  Therefore the graph-Gram certificate excludes all off-line
zeros crossed by the horizontal deformation.

## 6. Concrete replacement for `NRMA_a`

For the arithmetic/model stack, it is enough to prove the following
**Depth-Graph Gram Completion (`DGGC_a`)** on every finite carrier/delay packet:

\[
\boxed{
 K_{\rm src}^{ij}
 =K_{\rm crit}^{ij}
  +K_{\rm st}^{ij}
  +K_{\rm hyp}^{ij}
  +K_{\rm aux}^{ij},
 \qquad i,j\in\{0,1\},
}
\tag{L-19883.10}
\]

where `K^(ij)` is the polarized Gram with `i` radial depth operators on the
left feature and `j` on the right.

The zeroth kernel is the global completed source ledger.  The first-depth
kernels are determined by:

```text
the exact prime radial density and the native-radial dictionary;
the eta/bridge/gamma Hellinger derivatives;
the completed Xi logarithmic radial cocycle;
the de Branges--Weyl derivative bridge on the model side.
```

Once (L-19883.10) is established on a graph core, no separate rational-interval
allocation, measurable fibre selection, or post hoc direct-integral theorem is
needed.

## 7. Firewall

The `(0,0)` Gram identity alone is insufficient.  A global unitary may mix two
depth intervals or send a diffuse vector into an atomic target coordinate.
The derivative rows in (L-19883.1) are load bearing.

Conversely, checking only the first mixed moment

\[
 \langle Dv_x,v_y\rangle
\]

without the `(1,1)` graph norm does not prove that the lurking isometry maps the
operator graph into the target graph.  All four polarized blocks are required.

## 8. Boundary

```text
four graph-Gram blocks -> graph lurking isometry          EXACT
operator intertwining -> all Borel depth projections      EXACT
diffuse source -> no atomic model output                   EXACT
intervalwise NRMA as separate construction burden         REMOVED UNDER DGGC
actual Xi depth-graph Gram completion DGGC_a               OPEN / RH-BEARING
Riemann Hypothesis                                         UNPROVED
```
