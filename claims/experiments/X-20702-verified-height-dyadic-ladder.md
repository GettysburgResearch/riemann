# X-20702 — Pure-integer 320-bit verified-height conditional-frame ladder

Claim ID: `X-20702`  
Status: `DIRECTED EXACT FINITE COMPUTATION; FIXED-HEIGHT NONCOFINAL`  
Authoring agent: `gpt56-03-r`  
Created: 2026-08-01

## Result

A standard-library fixed-point verifier with denominator \(2^{320}\) evaluates

\[
M_c
=
2\sum_{j=1}^{20}v_{\gamma_j}(c)v_{\gamma_j}(c)^{\mathsf T}
-\eta_c I
\]

for the actual \(N=1\) even D-0001 evaluation rows. The first twenty
critical-line ordinate balls are imported from the retained 320-bit Arb
certificate `X-17204`. The unseen-zero radius is the off-line-safe
verified-height bound

\[
\eta_c
=
\frac{192(13/80)\sqrt c}{H\log c}(\log H+1),
\qquad
H=3{,}000{,}175{,}332{,}800.
\]

Every unlisted zero below \(H\) is discarded as positive semidefinite mass.
Every zero above \(H\), including a hypothetical off-line zero, is covered by
the negative scalar radius.

The first zero row defines the graph kernel. The other nineteen rows give the
conditional second frame. Exact Schur elimination certifies positive levels at

```text
5, 10, 20, 50, 100, 200, 500, 1000, 2000, 5000, 10000,
10241, 12830, 13680, 14249, 15000, 15390.
```

Adjacent controls

```text
10242, 12829, 13681, 14248, 15391, 100000
```

are unresolved by this lower model.

At \(c=15390\),

\[
\lambda_{\min}(M_c)
>
1.4677756009791674054\times10^{-12}.
\]

The conditional graph-kernel margin is

\[
>1.4677760263198035653\times10^{-12}.
\]

Therefore the finite certificate has

\[
\Lambda_c(\overline\nu_c-\underline\sigma_c^2)_++\delta_c=0.
\]

## Arithmetic

The verifier uses only Python integers. It constructs outward intervals for:

- \(\pi\), from Machin's identity and alternating arctangent series;
- logarithms, from the positive atanh series and an explicit tail;
- square roots, by integer `isqrt`;
- sine, by certified \(\pi/2\) range reduction and Taylor remainder;
- every matrix and Schur operation, by outward fixed-point rounding.

No binary floating-point number enters a sign decision.

## Reproducibility

```text
proof object
ed22bdaf5480ffe25b1c66f70d836c3f21d1ee323e94a5a75f6e9f3914ba0edd

source SHA-256
0f02c19edba3147dd27ce86757056642ec6602b3c0d5aea2a7240f7c2da0c995
```

The full 45,696-byte certificate is deterministically regenerated by the
committed source and is bound by
`artifacts/certificate-p320-manifest.json` and
`artifacts/SHA256SUMS-verified-height-320`.

## Correction to the preliminary session summary

The preliminary response printed several conditional margins from an earlier
scratch normalization. The committed exact replay above is authoritative.
The pass/fail classification is unchanged, and the independently highlighted
Euclidean lower endpoint at \(c=15390\) is reproduced exactly.

## Scope

This is a genuine finite zeta-data result. It cannot become cofinal at one
fixed verified height: the selected finite zero-frame energy is
\(O(1/\log c)\), while the off-line-safe unseen-zero radius grows like
\(\sqrt c/\log c\).
