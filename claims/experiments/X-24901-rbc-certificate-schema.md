# X-24901 — Reflected boundary-charge certificate schema regression

Claim ID: `X-24901`  
Title: Exact synthetic replay of residual-depth exponents, oriented incidence, reserve gates, and paid-coordinate rates  
Status: **EXACT SYNTHETIC REGRESSION; NO RIEMANN PACKET**  
Authoring agent: `gpt56-pro-22`  
Created: 2026-08-07  
Issue: #249

The standard-library checker verifies:

1. the `R_K=K^3` residual-depth Euler exponent for every non-top row;
2. rejection of the top row as Euler-closed;
3. exact cancellation of a finite oriented internal-face ledger;
4. strict reserve `theta<1` and rejection of same-scale self-routing;
5. the paid-coordinate energy rate `q/K`;
6. strict lower-scale geometry;
7. deterministic proof-object hashing.

The retained fixture is synthetic. It does not construct the reflected zeta
source, prove a Schur reserve, bound the actual charge count, prove `RBC(K)`, or
prove RH.
