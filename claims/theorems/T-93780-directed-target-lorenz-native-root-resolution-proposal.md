# T-93780 — Directed Target-Lorenz native-root resolution proposal

Claim ID: `T-93780`  
Status: **HARDENED CANDIDATE-COMPLETE NATIVE-ROOT PROPOSAL — HOSTILE REVIEW REQUIRED**  
Created: 2026-08-15  
Frozen base: PR #497 at `bd2a3c32ab50d8a8cec39c4b51ccd63349b23a74`  
New inputs: `R-93780`, `L-93780--L-93785`  
RH status: **unproved pending independent reconstruction**

PR #497 remains the controlling proposal. This theorem changes neither its
Target-Lorenz optimizer nor its one-shot construction. It replaces the last
unprotected numerical and typing interfaces by:

1. directed primitive enclosures for `zeta(1/2)` and `zeta'(1/2)`;
2. directed interval evaluation of all `51,118,080` tail events and the final
   unbounded interval;
3. a finite row-66 extremality certificate and exact half-open compact/tail
   join;
4. an explicit typed leaf compiler `(nu;B;sigma)` with one owner and one common
   coefficient vector;
5. one concrete common parent, an empty exported child family, zero port,
   separate signed observations and direct native `Y_4` pricing.

On the frozen compact, endpoint-frame, all-column and terminal inputs, for every
integer `X>=10^12` the construction supplies one finite row `d_X` with

\[
\boxed{d_X\ge0,}
\]

\[
\boxed{
C_{d_X}(q)\le w_X(q),
\qquad
\Xi_{d_X}(q)\le\Omega_X(q)
\quad(q\ge2),
}
\]

and

\[
\boxed{
0\le J_\Lambda(X)-\mathcal H(d_X)<61000.
}
\]

The replay authenticates the directed tail and exact compiler algebra. The
frozen compact theorem, positive endpoint-frame density, all-column analytic
estimate, terminal estimate and endpoint consumer remain independent review
obligations. This is a candidate proof proposal, not a declaration that RH is
established.
