# T-91302 — Historical first version of the Triune Adelic Scattering proposal

Claim ID: `T-91302`  
Status: **SUPERSEDED IN PART / DO NOT PROMOTE**  
Created: 2026-08-12  
Corrected: 2026-08-12  
RH status: **unproved**

## Supersession notice

This file records the first version of the three-route proposal. Two of its
constructive claims were subsequently disproved on the same branch:

1. `R-91303` proves that the fixed phase-locked `p=2` filter does **not**
   regularize Suzuki's hard-range integer singularities.
2. `R-91304` proves that the log-odds Sturm--Liouville cell realizes the
   two-copy difference coordinate `Delta`, not the one-copy BPY variable used
   in the Xi impedance.

The corrected proposal is

```text
T-91303 — One-Vector Global Jordan Optical Completion.
```

The current minimal endpoint is

```text
L-91313 — One-Node Arithmetic Exhaustion (ONAE_a).
```

## Mathematics retained from this version

The following parts survive:

1. `L-91307`: the Schur, positive-real, de Branges, canonical-system,
   conservative-colligation, and DtN formulations are congruent realizations
   of one target kernel.
2. `L-91309`: the positive generalized-Jordan source has an explicit fully
   polarized bosonic Fock product system.
3. A source-ordered optical identity would imply innerness and then RH along
   any sequence of positive scales tending to zero.
4. The proposal is unconditional in its definitions but is not a proof.

## Corrected architecture

The fixed local filter is retained only as a zero-safe finite boundary port.
The correct Suzuki repair is the global all-prime deconvolution of `L-91311`
followed by the one-Green primitive of `L-91312`.

The Brownian/theta route must retain the coupled coordinates

\[
 S=\frac12\log(A^2-D^2)+2C,
 \qquad
 \Delta=\operatorname{artanh}(D/A),
\]

together with the exact half-size tilt.

The hierarchy of sufficient targets is now

```text
AOT_a   full source/output kernel equality;
OVOT_a  one completed Hardy-vector norm identity;
ONAE_a  one interior Cauchy-vector norm exhaustion.
```

`ONAE_a` is the smallest visible conclusion-producing theorem. It remains
open and RH-bearing.

## Exact status

```text
first triune architecture                   HISTORICAL / PARTLY RETAINED
fixed p=2 hard-range regularization         FALSE
one-copy log-odds DtN identification        FALSE AS STATED
corrected one-vector proposal               T-91303
minimal one-node endpoint                   L-91313
Riemann Hypothesis                          UNPROVED
```
