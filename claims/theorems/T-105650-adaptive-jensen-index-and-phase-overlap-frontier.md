# T-105650 — Adaptive Jensen index and the degree-zero phase-overlap frontier

Claim ID: `T-105650`  
Status: **MAJOR EXACT CROSS-PROGRAM REDUCTION; DEGREE-ZERO PHYSICAL DEFECT OPEN**  
Created: 2026-08-25  
Depends on: `T-105640--T-105646`; `L-105647--L-105649`; sibling `L-106431`, `L-106506`, `L-106512--L-106514`, `T-106540`  
RH status: **unproved**

## 1. The signed index has an exact physical interior coordinate

For every finite reduced inner quotient

\[
U=\omega{A\over B},
\]

define

\[
\mathfrak J_y(U)
={1\over4\pi y}
\int_{\mathbb R}\log|U(x+iy)|^2dx.
\]

`L-105647` proves

\[
\boxed{
\mathfrak J_y(U)
=
\sum_{b\in Z(B)}\min\left(1,{\operatorname{Im}b\over y}\right)
-
\sum_{a\in Z(A)}\min\left(1,{\operatorname{Im}a\over y}\right).
}
\tag{T-105650.1}

Therefore

\[
\boxed{
\lim_{y\downarrow0}\mathfrak J_y(U)
=-\operatorname{wind}U.
}
\tag{T-105650.2}

Moreover, the first and second scale derivatives of
`y mathfrak J_y` recover respectively the complete signed depth-counting
function and the complete signed vertical divisor.  Multiplicity and
confluence are automatic.

Thus the microscopic endpoint collar is no longer an unrepresented integer.
It is an exactly resolved signed interior logarithmic flow whose sampling line
must adapt to the shallowest factor.

## 2. The soft current price is a random-scale signed index

Let

\[
d\mu(s)={2s\over(1+s)^3}ds.
\]

For the signed soft-depth functional

\[
\mathfrak D_H(U)
=
\sum_{b\in Z(B)}{2\operatorname{Im}b\over H+2\operatorname{Im}b}
-
\sum_{a\in Z(A)}{2\operatorname{Im}a\over H+2\operatorname{Im}a},
\]

`L-105648` gives the exact identity

\[
\boxed{
\mathfrak D_H(U)
=
\int_0^\infty
\mathfrak J_{Hs/2}(U){2s\over(1+s)^3}ds.
}
\tag{T-105650.3

Hence

\[
\boxed{
\lim_{H\downarrow0}\mathfrak D_H(U)
=-\operatorname{wind}U.
}
\tag{T-105650.4

The exponential Paley--Wiener depth price is therefore a universal positive
random-scale average of exact signed indices.

For the actual Xi current profile, `L-105649` proves a second exact resolution:
its one-zero weighted charge is a positive Stieltjes mixture of the hard-band
fractions

\[
1-e^{-2\delta L}
\]

with the complementary endpoint fractions

\[
e^{-2\delta L}.
\]

The current source, adaptive bandwidth, and endpoint index are now one
predeclared positive scale system rather than three unrelated transfer errors.

## 3. Exact closure of the former owner/collar normal form

The following rows are now closed at finite divisor and one-factor source
scope:

```text
one anti-inner crossing -> source-visible plus endpoint charge     EXACT
soft depth price -> random-scale clipped index                      EXACT
monotone actual current -> adaptive hard-band mixture               EXACT
signed index -> logarithmic interior limit                          EXACT
vertical divisor -> logarithmic scale coarea                        EXACT
```

Accordingly:

```text
HOWNXFER105644
BCOLLAR105643
MICROCOLLAR105640
```

are no longer separate normalization mysteries.  Their common normal form is
the signed logarithmic scale flow `mathfrak J_y`, together with the exact
source/endpoint split at every scale.

This statement does **not** claim that the full Xi physical theorem is proved.
The unresolved part survives after the signed index has been extracted.

## 4. The surviving object is degree-zero phase overlap

For the same quotient, the sibling endpoint programme proves

\[
\|H_U\|_{\mathcal S_2}^2
=m_- -\operatorname{tr}(P_-P_+),
\]

where `P_+` and `P_-` are the numerator and denominator model-space
projections.  Since

\[
\lim_{y\downarrow0}\mathfrak J_y(U)=m_--m_+,
\]

one obtains the exact decomposition

\[
\boxed{
\|H_U\|_{\mathcal S_2}^2
=
\lim_{y\downarrow0}\mathfrak J_y(U)
+
\underbrace{
\left[m_+-\operatorname{tr}(P_-P_+)\right]
}_{\displaystyle\mathfrak P_0(U)}.
}
\tag{T-105650.5

Equivalently,

\[
\boxed{
\mathfrak P_0(U)
=\|H_{U^{-1}}\|_{\mathcal S_2}^2
\ge0.
}
\tag{T-105650.6

The signed logarithmic flow carries every integer.  The sole remaining
finite-quotient quantity is the **degree-zero reverse-oriented phase energy**
`mathfrak P_0`.  It is the same object seen elsewhere as:

```text
canonical-correlation deficit between K_(B_+) and K_(B_-);
reverse-oriented Hankel charge;
favorable phase energy discarded by an adverse-only estimate;
boundary phase-angle overlap defect;
weighted current collision after source normalization.
```

`R-105647` proves that `mathfrak P_0` can be strictly positive even when the
signed index and every sufficiently fine logarithmic index are zero.

## 5. Relation to the actual Turán flow

For a real entire `F`, the balanced parent/derivative current is generated by

\[
{F'^2-FF''\over FF'}
=\partial_z\log{F\over F'}.
\]

Thus the signed logarithmic divisor flow in Sections 1--3 is exactly the
integrated parent-minus-derivative Turán spectral flow of `T-105645`.  For
`F=Xi`, the numerator `Xi'^2-Xi Xi''` already has the unconditional positive
exterior-square Fourier source.

After the new reduction, the remaining physical theorem is:

```text
D0PHASE105650 — degree-zero physical phase overlap

In the cofinal actual-Xi parent/derivative and endpoint quotients, absorb the
reverse-oriented degree-zero phase energy mathfrak P_0 by the positive
current/Turan reserve, with point evaluation, common-zero confluence and the
single endpoint ledger retained.
```

At operator strength this supplies the pointwise/Pick route.  At trace strength
it supplies the balanced shell and proportion routes.  The fifth-endpoint
scalar `ORIENTEDANGLE106540` is one concrete finite-window instance of the same
physical defect.

## 6. Why this is a genuine reduction rather than a renamed RH equivalent

The signed integer and its entire depth distribution are no longer hypotheses:
they are explicit logarithmic integrals with an exact adaptive scale law.  The
remaining term is positive, degree zero, and can be nontrivial in a quotient
with no signed index at all.  It is therefore logically and geometrically
separate from the RH conclusion.

The new implication graph is

\[
\boxed{
\begin{array}{c}
\text{actual positive current/Turan hierarchy}\\
+\ \text{exact adaptive signed logarithmic flow}\\
+\ \text{control of }\mathfrak P_0
\end{array}
\Longrightarrow
\text{physical balanced spectral flow}
\Longrightarrow
\text{reverse-Rolle descent}.
}
\]

The first two rows are proved.  The third is open.

## 7. Exact status

```text
logarithmic clipped signed index                    PROVED EXACT
logarithmic coarea recovers vertical divisor        PROVED EXACT
soft depth = universal random-scale index            PROVED EXACT
actual monotone current = adaptive hard-band mix     PROVED EXACT
former collar/owner normalization problem            CLOSED AT NORMAL FORM
zero signed index -> zero phase energy               REFUTED
D0PHASE105650 degree-zero physical overlap           OPEN / RH-BEARING
POINTID105630 pointwise actual-Xi evaluation          OPEN / RH-BEARING
cofinal endpoint/common-zero ledger                  OPEN
fixed-width terminal derivative theorem              PROPOSED / REVIEW
Riemann Hypothesis                                    UNPROVEN
```

## 8. Scope

Finite Blaschke products and finite canonical-product windows are exact.  The
entire Xi passage must subtract the exponential carrier and retain horizontal
endpoints.  The theorem does not bound `mathfrak P_0`, prove the pointwise
microscope sign, authenticate the proposed moving-saddle terminal theorem, or
prove RH.
