# T-99020 — Exact direct-integral score-free Hall factor-67 RH candidate

Claim ID: `T-99020`  
Status: **PROPOSED COMPLETE UNCONDITIONAL THEOREM — HOSTILE INDEPENDENT REVIEW REQUIRED**  
Created: 2026-08-18  
RH status: **not established by publication**

## Theorem

For every sufficiently large integer `X`, the construction in
`L-99020`--`L-99024` produces a finite nonnegative physical row `d_X` such that

\[
C_{d_X}(q)\le w_X(q),
\qquad
\Xi_{d_X}(q)\le\Omega_X(q)
\qquad(q\ge2),
\]

and

\[
\boxed{
J_\Lambda(X)-\mathcal H(d_X)=O(\log X).
}
\]

Consequently `F_Lambda(X)=o(log^2X)`, the prime endpoint is eventually negative,
and the Mellin–Landau argument gives the Riemann Hypothesis.

## Complete composition

```text
exact factor-67 compact target Hall
  -> positive residual source
  + target-free, declared-score-free, nonnegative Hall row bonus

residual source only
  -> exact causal current/child row identity
  -> actual recursive target mass <1/8

positive endpoint direct integration
  -> one exact common-parent row
  -> optional finite convex cubature, exact in every live coordinate

one common square-root thinning
  -> every q and 4q column, including q<K
  -> strict terminal reserve
  -> one feasible nonnegative row

literal row-score recurrence
  -> equality score 4sqrt(X) minus O(1)

parabolic benchmark
  -> J_Lambda-H(d)=O(log X)
  -> F_Lambda=o(log^2X)
  -> prime-square moat
  -> eventual prime-endpoint negativity
  -> Mellin-Landau
  -> RH.
```

## Corrections relative to the vanished T-94020 packet

1. The false identity
   `J_Lambda-H=<Y4,Omega-Xi>` is rejected. The exact `Y4` identity prices
   `P_Lambda-H` only.
2. The proof does not attempt to attach a positive declared score to a Hall row
   bonus.
3. The B-spline quantizer is removed from the conclusion-producing chain.
   Positive direct integration already gives one integer-indexed physical row;
   optional convex cubature is exact rather than approximate.
4. No activation-knot collar is needed. The parameter space is partitioned at
   every knot and integrated exactly.
5. The recursive score argument uses the exact physical-row identity, not a
   source-mass proxy for a signed loss.

## Scientific boundary

The theorem is a full proof candidate with every named lemma and finite gate
included in this packet. The retained checker authenticates exact finite
algebra and directed compact gates; it is not a substitute for independent
line-by-line review. Publication does not establish RH.
