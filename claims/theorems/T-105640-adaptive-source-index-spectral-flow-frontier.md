# T-105640 — Adaptive source/index spectral flow is the correct first-crossing frontier

Claim ID: `T-105640`  
Status: **MAJOR EXACT FIRST-CROSSING REDUCTION; MICROSCOPIC COLLAR OPEN**  
Created: 2026-08-25  
Depends on: `T-105620--T-105630`; `L-105640--L-105642`; `R-105640`; sibling `L-106415--L-106431`  
RH status: **unproved**

## 1. Correct natural safe domain

Let

\[
\beta_1=\sup\{\operatorname{Im}\rho:\Xi'(\rho)=0\}.
\]

For every base `b>=0` and scale `h>0` with total analytic height

\[
H=b+h>\beta_1,
\]

the shifted derivative all-pass

\[
U_H(x)={\Xi'(x-iH)\over\Xi'(x+iH)}
\]

is inner.  The standard Xi kernel is strictly log-concave, so the actual
current-normalized profile

\[
r_{b,h}(\xi)
={h e^{-H\xi}\Lambda_2(\xi)\over j_H(\xi)}
\]

is nonincreasing.  Hence the causal weighted contraction

\[
\boxed{
V_H^*M_{r_{b,h}}V_H\preceq M_{r_{b,h}}
}
\tag{T-105640.1}
\]

holds throughout the complete region `b+h>beta_1`.  The load-bearing parameter
is total height, not the base by itself.

This strengthens the conservative `b>=beta_0` wording retained in earlier
summaries.  It still does not imply the pointwise differential microscope.

## 2. What happens below beta one

At arbitrary height, the shifted derivative phase has the canonical form

\[
U_H=\Theta_H/B_H,
\]

where `Theta_H` is inner and `B_H` is the inner Blaschke product generated by
those Xi-prime zeros lying above height `H`.  The exact signed kernel is

\[
\boxed{
B_H(z)\overline{B_H(w)}K_{U_H}(z,w)
=K_{\Theta_H}(z,w)-K_{B_H}(z,w).
}
\tag{T-105640.2}
\]

Thus safe descent can fail only by the entrance of an explicit anti-inner
model space.  There is no diffuse new carrier or numerator error at that
moment.

## 3. First crossing conservation law

For one first simple factor

\[
b=\alpha+i\delta,
\]

in an inner background `A`, the adverse Hankel operator is rank one and

\[
H_U^*H_U=|A(b)|^2P_{\phi_b},
\qquad
\phi_b(\xi)=\sqrt{2\delta}e^{-\delta\xi}e^{-i\alpha\xi}.
\]

For source bandwidth `L`,

\[
\boxed{
\begin{aligned}
\text{source-visible charge}
 &=|A(b)|^2(1-e^{-2\delta L}),\\
\text{signed endpoint complement}
 &=|A(b)|^2e^{-2\delta L}.
\end{aligned}
}
\tag{T-105640.3}
\]

The two pieces sum exactly to the full first-crossing charge.  Hence source
energy and endpoint index are not alternative estimates.  They are a conserved
spectral-flow pair.

## 4. The adaptive collar

Fix `c>0`.  Every first-crossing factor lies in exactly one of two regions:

```text
source-visible:   delta L >= c,
                  endpoint fraction <= exp(-2c);

index collar:     0 < delta < c/L,
                  exact signed endpoint term retained.
```

The cofinal physical problem is therefore reduced to a moving vertical collar
of width

\[
\boxed{O(1/L).}
\tag{T-105640.4}
\]

Absolute model-space coverage is neither true nor needed.  The correct theorem
must combine source contraction on deeper factors with signed spectral flow in
this microscopic collar.

## 5. The weighted charge is soft but its flux is not

The actual current weight obeys

\[
0\le r_{b,h}(\xi)\le {h\over H}e^{-H\xi}.
\]

A first factor at depth `delta` therefore has current charge

\[
\boxed{
\mathfrak q_{b,h}
\le {2h\delta\over H(H+2\delta)}.
}
\tag{T-105640.5}
\]

It vanishes linearly at contact.  However

\[
{d\over d\delta}\mathfrak q_{b,h}(0+)
=2\int_0^\infty r_{b,h}(\xi)d\xi,
\]

and the unweighted index jumps by one.  The missing unit escapes to source
frequencies of order `1/delta`; it is not destroyed.

## 6. Revised conclusion-facing gates

The former broad `MCTPHYS105610/ENDIDX105630` interface now separates into:

```text
POINTID105630
  identify the actual two-trace point evaluation with a legitimate positive
  consumer; abstract causal energy alone is insufficient;

MICROCOLLAR105640
  control the signed Xi-prime spectral flow in the collar
  0 < Im(rho)-H = O(1/L), including confluent events;

ADAPTBANK105640
  choose a cofinal source bandwidth and endpoint gauge so that deeper factors
  are source-visible and the collar complement telescopes exactly;

PARENTATOM105640
  couple the derivative spectral-flow ledger to the fixed Clark/zero-height
  atom created by a positive-height Xi zero.
```

A proof of these coupled statements would extend the physical microscope below
`beta_1`; combined with the exact zero-height variational theorem it would give
RH.  None is claimed here.

## 7. Exact status

```text
actual current/Turan hierarchy                    PROVED EXACT
standard Xi-kernel strict log-concavity            PROVED / REVIEW
source contraction whenever b+h>beta_1             PROVED / REVIEW
inner/anti-inner factorization                     PROVED EXACT
first simple crossing rank-one law                 PROVED EXACT
source/index exponential conservation              PROVED EXACT
fixed-band source-only descent                     REFUTED
adaptive microscopic collar reduction              PROVED EXACT / SIMPLE EVENT
POINTID105630                                      OPEN / RH-BEARING
MICROCOLLAR105640                                  OPEN / RH-BEARING
ADAPTBANK105640                                    OPEN
PARENTATOM105640                                   OPEN / RH-BEARING
Riemann Hypothesis                                UNPROVEN
```

## 8. Scope

The theorem is a first-crossing and proof-architecture result.  It does not
bound the number of Xi-prime zeros in the microscopic collar, sum several
nonorthogonal factors by scalar addition, or exclude a parent Xi boundary
zero.  Its contribution is to replace a false fixed-band transfer target by an
exact adaptive source/index conservation law.
