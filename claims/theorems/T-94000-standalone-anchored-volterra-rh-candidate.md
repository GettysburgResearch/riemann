# T-94000 — Standalone anchored/affine-Volterra candidate for the Riemann Hypothesis

Claim ID: `T-94000`  
Status: **UNCONDITIONAL FULL PROOF CANDIDATE — PENDING INDEPENDENT ADVERSARIAL REVIEW**  
Date: 2026-08-16  
RH is not treated as established before that review.

For every integer `X>=10^12`, construct the row `d_X` in `L-94001` and `L-94002`.

1. `d_X` is a finite coefficientwise nonnegative row.
2. Every ordinary column satisfies `C_(d_X)(q)<=w_X(q)`.
3. Every radix-four column satisfies `Xi_(d_X)(q)<=Omega_X(q)`.
4. The exact native deficit satisfies
   \[
   0\le J_\Lambda(X)-\mathcal H(d_X)<3457.
   \]
5. The complete endpoint consumer of `L-94003` yields RH.

The proof does not use:

```text
Volterra derivative-fibre causality;
L-91763 or T-92910;
a rough-lift parent marginal;
PR #509 as confirmation;
an exported child family;
a matrix port;
a B-spline bulk quantizer;
a benchmark bridge involving 4sqrt(X).
```

The exact PR #503 witness is embedded in every replay. The main new proof-producing step is `L-94000`: the continuum outer source is compressed exactly, cell by cell, into finitely many actual nonnegative rows. The only large external certificate is the directed anchored Target–Lorenz theorem, imported at one exact SHA with its checksum ledger.
