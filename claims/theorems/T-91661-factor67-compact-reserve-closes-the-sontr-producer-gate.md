# T-91661 — The explicit factor-67 compact reserve closes the remaining SONTR producer obligation

Claim ID: `T-91661`  
Status: **PROVED CONDITIONAL COMPOSITION ON FROZEN INPUTS — REVIEW-READY SONTR PROPOSAL, RH UNPROVED**  
Created: 2026-08-14  
Primary new input: `L-91692`  
Retained producer inputs: `L-91688`, `L-91690`, `L-91691`, `L-91674`, `L-91320`, `L-91650`, `L-91654`  
Retained consumers: `L-91375`, `L-91378`, `T-91312`, `T-91313`, `T-91314`, `T-91660`  
Replay: `X-91692-factor67-reserve-closure`  
RH status: **unproved pending independent reconstruction of the frozen stack**

## 1. Producer statement

For every sufficiently large integer endpoint `X`, put

\[
K=\left\lfloor\frac X{67}\right\rfloor+1.
\tag{T-91661.1}
\]

On the frozen interfaces listed above there exist:

1. a coefficientwise nonnegative current row `d_X^cur`;
2. source-disjoint positive recursive packets `P_i` with coefficients
   `alpha_i>=0`;
3. one nonnegative native detail-slack vector `s_X`;
4. one labelled source partition into current, child, terminal stop, finite
   correction, and unused thinning;

such that

\[
Y_i\le K\le X/67+1,
\qquad
\sum_i\alpha_i<\frac18,
\tag{T-91661.2}
\]

\[
C_{d_X^{\rm cur}}(q)+
\sum_i\alpha_i C_{P_i}(q)
\le w_X(q),
\tag{T-91661.3}
\]

\[
\Xi_{d_X^{\rm cur}}(q)+
\sum_i\alpha_i\Xi_{P_i}(q)
=
\Omega_X(q)-s_X(q)
\le\Omega_X(q),
\tag{T-91661.4}
\]

for every physical column, with the analogous one-use inequality in every
retained boundary and common-port coordinate.  Moreover

\[
\sum_qY_4(q)s_X(q)
\le4\log X+C_*,
\tag{T-91661.5}
\]

for one absolute constant `C_*`.  In particular the weighted slack is
`o(log^2 X)`.

This is Source-Owned Native Thinning and Realization (`SONTR`) on the frozen
producer stack.

## 2. Exact source-owned root entry

Use the outer endpoint support

\[
K+2\le s\le X-10002.
\tag{T-91661.6}
\]

Then `X/s<67`, so the deterministic target Hall theorem `L-91690` sees only
squarefree divisors of `P_61`.  It assigns every small-divisor source occurrence
once to a matched edge or a positive residual coefficient.  The same target
flow is score-superordinate and gives a nonnegative bonus in every component
row.

Independently, `L-91688` assigns every rough monomial to its first rough-prime
owner.  The two ownership labels are disjoint because the root Hall window is
strictly below `67` while every rough owner is at least `67`.

The bottom width-two endpoint strip and the fixed top strip are retained as
explicit unused positive source.  They are neither hidden in a coordinatewise
complement nor copied into a child.

## 3. Hall creates no physical approximation error

For one Hall edge, write `rho_j(k)=A_(x,j)(k)/T_x(k)`.  The exact identity of
`L-91692` is

\[
\sum_eT_e\rho_j(e)-\sum_oT_o\rho_j(o)
=
\sum_er_e\rho_j(e)
+
\sum_{o,e}t_{o,e}[\rho_j(e)-\rho_j(o)].
\tag{T-91661.7}
\]

The second term is the nonnegative current row bonus.  Hence the Hall residual
plus bonus is exactly the original equality row before physical observation.
Every ordinary response preserves this identity, and radix-four equality
follows by applying the ordinary maps at `q` and `4q` before subtraction.
Therefore

\[
\boxed{\varepsilon_{\rm Hall,row}=0.}
\tag{T-91661.8}
\]

The abstract compact estimate

```text
reserve > 10152 * ||C|| * epsilon
```

is consequently unnecessary in this exact construction.

## 4. The correct density and the explicit interior reserve

The total equality row is generated by

\[
L(x)=2\sqrt x\sum_{n\le x}\frac{\mu(n)}n
-
\sum_{n\le x}\frac{\mu(n)}{\sqrt n},
\tag{T-91661.9}
\]

not by the larger SHARP target density.  The directed factor-67 cell census in
`L-91692/X-91692` proves

\[
\frac{159}{500}<L(x)<\frac{183}{100}<2
\qquad(1\le x<67).
\tag{T-91661.10}
\]

Thus the `M=2` positive B-spline collar estimate remains valid.  The genuine
finite/continuum defect is not erased; its factor-67 constant satisfies

\[
C_{67}<19.
\tag{T-91661.11}
\]

For every interior column `K<=q<=X/4`, the total mismatch and collar use at most

\[
\left(1+\frac{5655}{32K}\right)\Omega_X(q).
\tag{T-91661.12}
\]

Multiply the one common positive parent endpoint measure by

\[
\sigma_K=\frac{K}{K+178}.
\tag{T-91661.13}
\]

Then the retained used detail is at most

\[
\left(1-\frac{41}{32(K+178)}\right)\Omega_X(q),
\]

so

\[
\boxed{
s_X(q)\ge
\frac{41}{32(K+178)}\Omega_X(q)>0
}
\tag{T-91661.14}
\]

on every interior column.  The positive radix-four inverse gives ordinary
feasibility from this same comparison.

## 5. Explicit terminal reserve

On `q>X/4`, the total possible overfill is less than

\[
4452X^{-3/2}.
\tag{T-91661.15}
\]

The fixed top omission is source-owned because on its quotient cell

\[
L(X/s)=2\sqrt{X/s}-1\ge1
\]

and there is no odd Hall demand.  The frozen terminal theorem gives omitted
response greater than

\[
5033X^{-3/2}.
\tag{T-91661.16}
\]

Consequently every terminal column has the strict reserve

\[
\boxed{581X^{-3/2}>0.}
\tag{T-91661.17}
\]

For columns above the retained endpoint support, the response is exactly zero.
There is therefore no zero-denominator exception.

## 6. One aggregate common port

For each positive labelled branch `b`, the `P_61/67` Schur theorem gives

\[
M_b\succeq\frac19\mathcal V_bI_2,
\qquad
\tau_{p_b}\mathcal V_b<\frac19\mathcal V_b.
\tag{T-91661.18}
\]

For arbitrary positive source weights `beta_b`, PSD linearity yields

\[
\sum_b\beta_bM_b
\succeq
\frac19\left(\sum_b\beta_b\mathcal V_b\right)I_2
>
\left(\sum_b\beta_b\tau_{p_b}\mathcal V_b\right)I_2.
\tag{T-91661.19}
\]

Thus one port, formed after all branches are summed, covers the aggregate
correction.  Its normalized mass is less than `14/3`.  No port is charged per
leaf or per child.

## 7. Thin once, then split causally

Apply the causal coefficient identity to the single globally thinned parent
packet, with causal zero extension for inactive fibre/prime pairs.  Group every
fibrewise child contribution having the same rough prime inside one positive
packet `P_i`.  Linearity gives

\[
Q_XP_X
=P_X^{\rm cur}+
\sum_i\alpha_iU_{p_i}P_i,
\tag{T-91661.20}
\]

simultaneously in source, row, ordinary, detail, score and port coordinates.
The outer coefficient list occurs once, not once per endpoint fibre, and

\[
\sum_i\alpha_i<67^{-1/2}<\frac18.
\tag{T-91661.21}
\]

Since the right side equals the one already feasible packet `Q_XP_X`, the causal
split creates no new capacity or slack obligation.  Hall bonuses stay current.

## 8. Debt and weighted slack

The martingale quantizer is score-favourable.  The global thinning fraction is

\[
1-\sigma_K<\frac{11926}{X},
\tag{T-91661.22}
\]

so its cost against the frozen polynomial score majorant is bounded and tends
to zero.  The bottom and top omissions have fixed endpoint width, and the one
aggregate port has bounded normalized mass.  Hence the entire compact physical
realization contributes one absolute current charge.

The causal current debt is bounded by twice exact target mass, and the grouped
recursive coefficient is below `1/8`.  The hereditary packet envelope therefore
gives

\[
\Lambda_{\rm eq}(X)
\le C_0+\rho\Lambda_{\rm eq}(X/67+1),
\qquad \rho<\frac18,
\tag{T-91661.23}
\]

and hence

\[
\Lambda_{\rm eq}(X)=O(1).
\tag{T-91661.24}
\]

Restoring the native benchmark gives

\[
J_\Lambda(X)-\operatorname{Score}(d_X)
\le4\log X+C_*.
\tag{T-91661.25}
\]

The frozen radix-four dual identifies this deficit with the `Y_4`-weighted
unused native detail, proving (T-91661.5).

## 9. Disposition of the unresolved proof obligation

The previous compact reserve gate requested an unspecified atom error, an
unspecified correction norm, a reserve denominator, and a global constant.
`L-91692` replaces these by exact values:

```text
Hall total-row error             0;
equality density                 159/500 < L < 183/100;
finite mismatch constant         C67 < 19;
interior reserve                 41 Omega/[32(K+178)];
terminal reserve                 581 X^(-3/2);
common port                      one aggregate PSD inequality;
recursive coefficients          one grouped list, sum <1/8.
```

Thus the compact finite-root reserve obligation is proved on the frozen
interfaces.  `T-91660` is upgraded from a proposal with an implicit reserve
step to a review-ready SONTR/NRCT composition with an explicit proof object.

This theorem does not declare RH established.  Promotion through `T-91313`
requires hostile independent reconstruction of every frozen endpoint-frame,
causal, port, dual, and endpoint-consumer input.

```text
compact reserve proof obligation                PROVED
source-owned one-use native realization         PROVED ON FROZEN INPUTS
SONTR / Native-Root Capacity Theorem             REVIEW-READY PROPOSAL
endpoint-to-RH chain                             FROZEN CONDITIONAL
Riemann Hypothesis                               UNPROVEN
```
