# T-91510 — Clark–Redheffer domain-wall completion would prove PDWT and RH

Claim ID: `T-91510`  
Status: **FULL CONDITIONAL RH PROPOSAL / ARCHIMEDEAN REDHEFFER COMPLETION OPEN**  
Created: 2026-08-12  
Depends on: `L-91510/L-91511`, `R-91510`; `T-91403`  
RH status: **unproved**

## 1. Prime parity sector is now source complete

At every safe scale used by the Cauchy recurrence, the prime endpoint pair has
an explicit positive-real realization:

\[
 E_{\rm p}
 \longrightarrow
 \mathcal G_{\rm p}^{\rm Clark}
 \oplus O_{\rm p}
 \oplus R_{\rm p},
 \tag{T-91510.1}

\]

where

```text
G_p^Clark   is the Herglotz/Clark connection of the returned prime channel;
O_p         is the ordered odd Julia-detail production;
R_p         is the coefficient-one returned Euler state.
```

The complete cross-carrier kernel and every finite-prime ordering are retained.
Prime-atom isolation is absorbed by `R_p`, rather than being compared against
an absolutely continuous measure.

## 2. Clark–Redheffer Domain-Wall Completion (`CRDWC_a`)

Construct a source-ordered conservative Redheffer interconnection

\[
\boxed{
 \mathfrak R_a:
 \mathcal G_{\rm p}^{\rm Clark}
 \oplus R_{\rm p}
 \oplus \frac{C+J}{\sqrt2}
 \oplus O_-
 \longrightarrow
 \mathcal G_a^\lambda
 \oplus T_{\rm sh}
 \oplus \frac{C-J}{\sqrt2}
 \oplus E_-
 \oplus \mathcal E_a
}
\tag{T-91510.2}

\]

with the following properties.

1. `mathfrak R_a` is contractive in positive Hilbert metrics.
2. Its prime restriction is exactly the Clark/Julia realization of
   `L-91510/L-91511`.
3. Its continuous restriction is the plastic-aligned parity domain wall of
   `L-91414`.
4. Its finite connection output has Gram `C_a^lambda` in the exact six-safe-jet
   normalization.
5. It intertwines compressed delays, both Hardy reflections, and the bridge.
6. The returned prime state is consumed once, with coefficient one, by the
   gamma/pole completion; no same-scale signed port remains.

This is `CRDWC_a`.

## 3. Consequence

The prime Clark identity supplies the prime odd details and returned state.
Contractivity of (T-91510.2) then gives the parity-domain-wall transfer
`PDWT_a` of `T-91403`. Hence

\[
 \mathcal N_a^{\rm par}
 \preceq
 \mathcal C_a^\lambda+\mathcal P_a^{\rm par},
\]

so

\[
 \mathbb K_a^{\rm del}\succeq0.
\]

The corrected fixed-scale form-core consumer then proves RH.

## 4. What has genuinely been removed

The remaining theorem no longer needs to invent a prime source map. The
following components are exact:

```text
prime local passivity;
all-prime safe cascade;
prime parity Cayley graph;
prime Herglotz connection;
prime atomic-isolation return;
coefficient-one prime state recursion.
```

Only the interconnection of that already-passive prime impedance with the
completed gamma/pole domain wall remains.

## 5. Preferred construction

The algebra suggests a Redheffer star product of three systems:

```text
safe prime Clark impedance;
positive gamma-ladder impedance;
finite dyadic/pole bridge and compensation connection.
```

The sibling eta/dyadic route supplies an explicit compact realization of the
last item. The sibling one-node route gives a scalar hostile test of every
candidate interconnection.

## 6. Rejection tests

Reject a claimed proof if it:

1. replaces the returned prime state by an absolutely continuous reserve;
2. drops the local Julia details at a prime resonance;
3. multiplies one-scale passive factors after applying the signed three-scale
   observation;
4. treats the pole channel as positive;
5. proves only boundary moduli and not the polarized kernel;
6. loses delay, reflection, bridge, or coefficient-one state return;
7. defines the connection by a square root of the target screw Gram.

## 7. Exact boundary

```text
prime source-ordered parity connection               EXACT
prime atom isolation                                 CLOSED
completed continuous/gamma/pole interconnection      OPEN
CRDWC -> PDWT -> CPPD                                 EXACT CONDITIONAL
CRDWC                                                 OPEN / RH-EQUIVALENT
Riemann Hypothesis                                    UNPROVED
```
