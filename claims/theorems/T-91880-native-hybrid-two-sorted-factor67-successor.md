# T-91880 — Native hybrid two-sorted arithmetic compilation gives a bounded-deficit factor-67 successor

Claim ID: `T-91880`  
Status: **CANDIDATE-COMPLETE RH PROOF PROPOSAL ON FROZEN INPUTS — INDEPENDENT RECONSTRUCTION REQUIRED**  
Created: 2026-08-16  
Base: frozen PR #506 at `3d44cf10f8b5f36fd06a57a745550a25142e3658`  
New claims: `R-91880`, `L-91880`–`L-91883`  
RH status: **proposed only; unproved pending review**

## 1. Construction

For every integer `X>=10^12`, the exact native Möbius source is split into:

```text
inner exact finite occurrences;
outer paired Volterra occurrences;
signed adjacent finite/continuum defect.
```

The inner finite occurrences are processed by the exact stopping-line and first-owner source tree.  Compact transition leaves and deep directed Target-Lorenz leaves produce a positive target-bearing residual source and a current-only nonnegative row bonus.  Only the residual source enters any causal child path.

The outer Volterra colours share one common positive row feature.  Their exact rank-one incidence cancels every matched positive/negative colour and leaves `L(X/s)p_s>=0`.  This row is never causally split.

The anchored inner rows, row-only bonuses and retained outer row are realized once by

\[
\mathcal Q_X^{\rm hyb}
=I_{\rm in}\oplus I_{\rm bonus}\oplus Q_{\rm bulk}
\]

and one common scalar thinning.  All actual children are already physical internal colours; the exported recursive family is empty.

## 2. Same-row native identity

The exact hybrid algebra is

\[
 c_X-d_X
 =(1-\tau_K)c_X
 +\tau_K\left[
  \overline c_{X,\mathrm{top}}
  +\mathcal R(E_{X,\mathrm{out}}-C_{X,\mathrm{ret}})
 \right].
\]

Thus source ownership, collar, top omission, finite comparison, all-column capacity, terminal reserve and native pricing concern one immutable row.

For every physical column,

\[
 C_{d_X}(q)\le w_X(q),
 \qquad
 \Xi_{d_X}(q)\le\Omega_X(q).
\]

Furthermore

\[
\boxed{
0\le J_\Lambda(X)-\mathcal H(d_X)<60989.
}
\tag{T-91880.1}

## 3. Endpoint consumer

The exact finite dual has the one-sided orientation

\[
 F_\Lambda(X)
 \le J_\Lambda(X)-\mathcal H(d_X).
\]

Hence `F_Lambda(X)=o(log^2 X)`.  On the frozen unconditional prime-square moat and Mellin--Landau endpoint inputs, the prime endpoint is eventually negative, every zero with real part greater than one half is excluded, and the functional equation supplies the proposed RH conclusion.

## 4. Mandatory regressions

Reject this proposal if any of the following fails:

```text
forced x=2 Hall score correction is negative;
Hall bonuses are row-only;
Volterra p_s is never causally split;
row-first rough lift is never used as native parent;
inner finite occurrences are exhaustive and source-disjoint;
deep AVLT compact/tail join covers every x>=67;
only residual sources receive causal children;
outer rank-one coupling has exact two marginals;
top omission is the same positive outer packet used by the terminal bound;
all q<K columns are included;
q and 4q are observed on the same row before detail;
Y4 pricing uses the same response complement;
endpoint orientation is F_Lambda<=Delta;
no J_Lambda-4sqrt(X) bridge appears.
```

The machine replay is a finite algebra/type/provenance regression and does not replace independent reconstruction of the directed AVLT, analytic endpoint estimates, prime-square theorem, Mellin transform or Landau theorem.
