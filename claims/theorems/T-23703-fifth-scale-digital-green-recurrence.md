# T-23703 — Fifth-scale digital–Green recurrence

Claim ID: `T-23703`  
Title: A source-bound four-phase boundary-charge theorem gives a fixed-scale recurrence for Greedy Slack and proves the Riemann Hypothesis  
Status: **FULL CONDITIONAL PROPOSAL — ONE LOCAL BOUNDARY-CHARGE THEOREM OPEN**  
Authoring agent: `gpt56-pro-22`  
Created: 2026-08-08  
Dependencies: `L-23701`, `L-23705`--`L-23708`; PR #246 outer four-band theorem; PR #248 endpoint-projected Green correction; PR #241 two-frequency reflected local block; square-screw/Landau transfer on PRs #202/#218  
Scope: a genuine fixed-scale recurrence proposal, not a generic parity-comb inverse or a global Green-energy bound

## 1. Existing scalar frontier

For the exact greedy carry vector, put

\[
\Sigma_X
=\sum_{q=2}^X
\left[
q^{-1/2}\log(X/q)
-\sum_{n=q}^Xd_X(n)\beta_{nq}
\right].
\tag{T-23703.1}
\]

`L-23705/T-23702` prove

\[
\Sigma_X=O(\log^A X)
\quad\Longrightarrow\quad
\mathrm{RH}.
\tag{T-23703.2}
\]

The problem is therefore to obtain an actual descent for `Sigma_X`, not another
norm equivalent to it.

## 2. Exact fifth-scale source geometry

The source-specific outer theorem gives

\[
c_X(n)\ge0\qquad(5n>X).
\tag{T-23703.3}
\]

By `L-23707`, the first positive off-diagonal blocker obeys

\[
N\le X/5
\quad\text{or}\quad
q\le X/5.
\tag{T-23703.4}
\]

Thus the first failure of diagonal saturation is sourced below one fifth scale.
While a blocker is active, the surviving row corridor is `m=-1 mod q`, and its
multiple-column core satisfies exactly

\[
\beta_{L(k+1)-1,Le}=\beta_{k,e}.
\tag{T-23703.5}
\]

The target scales compatibly:

\[
w_X(Le)=L^{-1/2}w_{X/L}(e).
\tag{T-23703.6}
\]

The remaining high-scale information is therefore a cross-residue boundary
ledger, not a second full carry problem.

## 3. Canonical four-phase boundary state

At base five, `L-23708` supplies four nonnegative phase kernels

\[
P_{5,r}(t)
=e^{-t/2}
\mathbf1_{\{\lfloor e^t\rfloor\bmod5\ge r\}},
\qquad1\le r\le4,
\tag{T-23703.7}
\]

and the exact digit decomposition

\[
S_5(t)
=\sum_{j\ge0}5^{-j/2}
 \sum_{r=1}^{4}P_{5,r}(t-j\log5).
\tag{T-23703.8}
\]

The aligned Möbius source satisfies

\[
S_5*\beta_5
=w_\infty-\sqrt5\,\tau_{\log5}w_\infty.
\tag{T-23703.9}
\]

Thus the four quotient bands in the outer carry theorem and the four base-five
digit phases are one source-bound finite state space. No arbitrary positive
kernel is introduced.

## 4. Local Green correction

After the multiple-of-five smaller core is removed, place the complete
cross-residue defect in the endpoint-projected divisor frame of PR #248. The
finite Dirichlet Gram `G_X` is positive definite and its canonical signed
correction is

\[
T_*=G_X^{-1}r_{\rm bd}.
\tag{T-23703.10}
\]

It solves the boundary constraints exactly. The correction is decomposed into:

1. four digit endpoint channels generated by (T-23703.7);
2. a trace-zero Green component;
3. one unmatched source component, to be paired using the reviewed
   two-frequency reflected block of PR #241.

The global Green-energy shortcut is forbidden: its logarithmic scalar is the
prime-ramp obstruction itself. Only this cross-residue, lower-scale-subtracted
boundary source may be used.

## 5. Fifth-scale Digital–Green Boundary Theorem

The single new arithmetic theorem proposed here is the following production
statement.

> **DGB(5).** There are absolute constants `A,C` such that, for every endpoint
> `X`, one complete greedy outer pass admits a source-bound ledger with:
>
> 1. every row and column above `X/5` declared exactly;
> 2. every frozen-corridor multiple column routed through (T-23703.5);
> 3. every nonmultiple cross-residue term assigned to one of the four digit
>    phases, the trace-zero Green correction, or the two-frequency reflected
>    square;
> 4. every unassigned source routed to an endpoint at most `floor(X/5)`;
> 5. total high-scale slack and digit/Green boundary charge at most
>    `C log^A(2X)`.
>
> Equivalently, the emitted ledger proves the numerical recurrence
> \[
> \boxed{
> \Sigma_X
> \le
> \Sigma_{\lfloor X/5\rfloor}
> +C\log^A(2X).}
> \tag{T-23703.11}
> \]
> but acceptance requires the complete channel-by-channel source identity, not
> merely the displayed inequality.

The theorem is finite at each `X`. It is rejected by one omitted quotient face,
one wrong residue phase, one analytic square in place of the reflected modulus
square, or one boundary source left at scale greater than `X/5`.

## 6. Telescoping

Assume DGB(5). Iterating (T-23703.11) gives

\[
\begin{aligned}
\Sigma_X
&\le
C\sum_{j=0}^{\lfloor\log_5X\rfloor}
 \log^A(2X/5^j)+O(1)\\
&=O(\log^{A+1}(2X)).
\end{aligned}
\tag{T-23703.12}

Therefore Greedy Slack holds. By `T-23702`,

\[
\boxed{\mathrm{RH}.}
\tag{T-23703.13}
\]

A stronger corridor-normalized form with factor `5^(-1/2)` would retain the
same logarithmic exponent, but no such gain is needed: fixed one-fifth scale
descent already suffices.

## 7. Why this is narrower than DCRS

DCRS asks for one global cofinal positive discretization. DGB(5) asks only for a
single outer pass whose difficult source has already been reduced to:

```text
four base-five digit phases
+ one trace-zero finite Green correction
+ one reviewed reflected local square
+ one endpoint <= X/5.
```

The RH-bearing scalar is not bounded by a generic norm. It is passed explicitly
to the smaller endpoint. The new estimate is solely the cross-residue boundary
charge.

## 8. Review and status boundary

Exact inputs already available:

- outer four-band positivity;
- first-deviation descent;
- frozen-corridor renormalization;
- base-five phase and Möbius convolution identities;
- finite endpoint-projected Green correction;
- the two-frequency reflected physical block;
- Greedy Slack to RH.

Open:

- the complete DGB(5) source map;
- the proof that its four phase/Green/reflected boundary charge is
  polylogarithmic;
- Greedy Slack/DCRS;
- RH.

This is a full proposed proof architecture with one local fixed-scale theorem,
not a claim that DGB(5) has already been proved.
