# T-91655 — Rejected proposal; superseded by `T-91656`

Claim ID: `T-91655`  
Status: **REJECTED AS A PROOF PROPOSAL AT ITS REVIEWED NORMALIZATION**  
Reviewed by: PR #457 at `e136fcf42fbab1195fc193e64ce4039d1a1d416c`  
Superseded by: `R-91659`, `L-91670`, `L-91671`, `T-91656`  
RH status: **unproved**

The historical conclusion depended on `L-91668`, whose balanced/reserve row
observation produces `3c_X`, not `c_X`. The native root-capacity entry and every
downstream recurrence based on that equality were therefore invalid.

The following independent pieces survive and are imported into the successor:

```text
L-91666 fixed-67 entropy theorem;
exact native ordinary/detail response identities;
same-index arbitrary-child replacement;
one-prime target/row-budgeted-score/row cocycle;
finite endpoint dual.
```

The normative successor uses one SHARP channel

\[
w_\Psi=3w_{4/3},
\qquad
\mathcal H_{\Psi,j}=Q_Y/(4\sqrt Y-3),
\]

which produces exactly one native row copy, and supplies an explicit
finite-seed/continuum-seed bridge and typed one-generation ledger. See
`T-91656`.

This file is retained only as a durable rejected stage and must not be treated
as a live theorem.
