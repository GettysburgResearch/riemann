# T-104640 — Binding disposition of the source-to-inner-factor completion attempt

Claim ID: `T-104640`
Status: **BINDING CORRECTION; THE NINETY-PERCENT LEDGER SURVIVES BUT ITS PROPOSED SOURCE-GRAM COMPLETION DOES NOT**
Created: 2026-08-30
Supersedes: the interpretation of `FRACKRYLOV104633` and
`SELFKRYLOV104636` as a finite source-to-inner-factor bridge
Depends on: `L-104639`, `R-104640`; retained ledgers `L-104630--L-104638`;
the microscopic companion analysis on PR #731
RH status: **unproved**

This theorem records the outcome of one direct attempt to close the last
source-to-inner-factor/model-space Gram estimate in the self-contained
ninety-percent programme.

## 1. What remains valid

The following statements survive unchanged:

```text
self-contained Conrey input alpha_5 > 49/50;
fifth-current generalized-Maxwell sandwich;
fractional Calderón resolution;
deep companion-height payment;
exact fifth-endpoint charge ledger;
frozen carrier Bézout source identity;
variable carrier error o(N) at source mean-square scope.
```

In particular, the conditional conclusion remains valid:

\[
\mathfrak I_{\rm sh}(T)
<
{13\over500}N(T,2T)
\quad\Longrightarrow\quad
{N_0(T,2T)\over N(T,2T)}
>
{225181\over250000}
=
0.900724
\]

in the declared limsup/liminf sense.

The issue is not the arithmetic of that ledger.  It is whether the displayed
premise has been reduced to a more accessible source estimate.

## 2. The fractional premise is already the microscopic charge

`L-104639` proves

\[
e^{-\delta_0}
\left(
\mathfrak C_{\rm sh}
-
m\varepsilon
\right)
\le
\mathfrak I_{\rm sh}
\le
\mathfrak C_{\rm sh}
\le
e^{\delta_0}\mathfrak I_{\rm sh}
+
m\varepsilon.
\tag{T-104640.1}
\]

Therefore the fractional-current residual and the shallow canonical-correlation
charge are equivalent up to exactly the tail already entered in the numerical
budget.

Moreover, if \(d\) is the unmatched denominator dimension, then

\[
d\le\mathfrak C_{\rm sh}
\le
e^{\delta_0}\mathfrak I_{\rm sh}+m\varepsilon.
\tag{T-104640.2}
\]

Thus the premise already contains the required new zero-count/topological
theorem.  Fractional resolution distributes that theorem over scales; it does
not produce it from diagonal source control.

## 3. The seven-grade projected Krylov certificate is tautological at grade zero

For the exact operators defined in `L-104633`,

\[
Z_{0,H}=P_{K_A}P_{K_B}M_H
\]

is the unrestricted primal minimizer.  Hence

\[
\inf_{c\in\mathbb C^7}
\left\|
P_{K_B}M_H-
\sum_{r=0}^6c_rZ_{r,H}
\right\|_{\mathcal S_2}^2
=
\operatorname{tr}
\left(Q_{A,B}\widetilde W^{(5)}_{a,H}\right).
\tag{T-104640.3}
\]

The ridge functional is the right side plus one nonnegative regularization
term.  Accordingly:

```text
FRACKRYLOV104633 with tau -> 0:
  the original microscopic charge in finite-matrix notation;

FRACKRYLOV104633 with tau bounded away from zero:
  a strictly stronger regularized microscopic charge;

neither version:
  a source-to-inner-factor theorem derived from the degree-six source module.
```

The polynomial degree-six observation remains true, but the definition has
already inserted the unknown inner projection \(P_{K_A}\).  It cannot be used
to prove that projection is small.

## 4. Exact source-module promotion is false in general

`R-104640` specializes the exact frozen source algebra to

\[
F_n(t)=c+\cos(nt),\qquad c>1.
\]

For that family:

\[
\mathbf n=\mathcal M(\mathcal X)\mathbf d
\]

holds with zero source residual and constant carrier, while the reduced
physical quotient has numerator inner degree zero, denominator inner degree
two, and

\[
\|H_{U_n}\|_{\mathcal S_2}^2=2
\]

per period.  Its nontrivial pole height is \((\log c)/n\to0\).

Therefore exact source transport and arbitrarily shallow poles do not control
the physical Gram.  Any completion must be Xi-specific.

## 5. Correct remaining theorem

Retire `SELFKRYLOV104636` as a purported *source-level completion mechanism*.
Its inequality remains a valid sufficient condition, but is no simpler than
the microscopic charge it was meant to bound.

The correct conclusion-facing target is:

```text
XIMICRO104640

For the literal fifth-endpoint Xi companion quotient on the predeclared
mesoscopic exhaustion, prove that the sum of

  (a) the positive unmatched shallow endpoint index, and
  (b) the matched degree-zero phase / truncated-Toeplitz nonnormality charge

is strictly below the applicable shallow allowance:

  7319/250000 N        for the self-contained alpha_5 > 49/50 ledger,

or

  21701/10^6 N         for the pinned alpha_5 > 0.997 ledger,

with common outer factors, confluence, endpoints and regular-window errors
retained.
```

Equivalently, one may prove the corresponding `SHALLOWCORR106591`,
`FRACTRANS104635`, or lossless small-regularization source-Pick estimate.  The
equivalence is now explicit; none is proved here.

The Xi-specific Fourier advance `L-106710` remains relevant to the balanced
phase component, but it does not pay the forced topological index and does not
by itself commute through the physical constant-scale quotient.

## Exact disposition

```text
self-contained >90% numerical ledger                 RETAINED EXACT
fractional current resolution                         RETAINED EXACT
fractional residual as weaker source theorem          WITHDRAWN
seven-grade projected Krylov source leverage          WITHDRAWN / GRADE-ZERO EXACT
frozen degree-six Bézout source transport              RETAINED EXACT
universal source-to-inner promotion                    REFUTED
XIMICRO104640                                          OPEN / >90%-BEARING
more than ninety percent                               UNPROVED
density one                                            UNPROVED
Riemann Hypothesis                                     UNPROVED
```
