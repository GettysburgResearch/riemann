# T-91860 — Two-sorted Hall/source-row realization gives a bounded native-deficit factor-67 successor proposal

Claim ID: `T-91860`  
Status: **CANDIDATE-COMPLETE SUCCESSOR PROPOSAL ON FROZEN INPUTS — HOSTILE REVIEW REQUIRED**  
Created: 2026-08-15  
Base: frozen PR #499 at `99d3983b57f82941131caa8d9c36e4947f1179a0`  
Accepted hostile review: PR #501 at `2f58a65097206c4b80142cead52282161302d10c`  
Compared implementation: PR #500 at `d73c1e7a1a482cac31581211a84db43cc34c824e`  
Depends on: `R-91860`, `L-91860--L-91863`, frozen endpoint consumer `T-91750` and exact pinned dependencies  
RH status: **proposed only; unproved pending independent reconstruction**

## 1. Corrected producer

For every integer `X>=10^12`, the factor-67 Hall fibre is interpreted in the two-sorted cone

\[
K_{\rm src}\oplus K_{\rm row}.
\]

The target-bearing Hall residual is a positive complete source packet. It alone receives rough first ownership, the causal split, and positive same-index internal child placements. The target-null Hall difference is retained only as a coefficientwise nonnegative, Hall-edge-owned current row. It carries no target or inherited declared-score packet coordinate.

After restriction to tagged complete cells, the construction integrates both sorts over the same fibres and applies the single block realization

\[
\mathcal Q_X^{(2)}
=I_{\rm anc}\oplus I_{\rm bonus}\oplus Q_{X,\rm bulk}^{\rm lbl},
\]

followed by one scalar thinning. All child colours are physically inserted and the exported recursive family is empty.

## 2. One final physical row

The result is one row `d_X>=0`. Collar removal, Hall/rough ownership, internal children, block realization, thinning, finite comparison, terminal comparison, response complements, and `Y_4` pricing all carry the same row identifier.

The signed finite/continuum and collar comparisons are observation data, not positive source. The identity Hall-bonus and anchored channels cancel exactly from those comparisons.

## 3. Capacity and native cost

On the frozen all-column and terminal estimates,

\[
\Gamma(d_X)\le w_X,
\qquad
\Xi(d_X)\le\Omega_X
\]

in every physical column, including `2<=q<K`. The exact nonnegative complements belong to the same `d_X` and satisfy

\[
\Gamma(d_X)+e_X^{\rm ord}=w_X,
\qquad
\Xi(d_X)+e_X^{(4)}=\Omega_X.
\]

Direct sparse-dual accounting gives

\[
\boxed{
0\le J_\Lambda(X)-\mathcal H(d_X)<60989
=o(\log^2X).
}
\tag{T-91860.1}

No estimate of `J_Lambda(X)-4sqrt(X)` is used.

## 4. Endpoint composition

The frozen one-sided endpoint theorem gives

\[
F_\Lambda(X)
\le J_\Lambda(X)-\mathcal H(d_X)
=o(\log^2X).
\]

On the frozen prime-square moat and Mellin--Landau pole audit, this yields the candidate RH conclusion. Those analytic inputs and the factor-67 Hall/profile/collar estimates remain independent review obligations.

## 5. Relation to frozen proposals

```text
PR #499:
    frozen unchanged;
    complete target-null Hall packet rejected;
    response-complement architecture retained after two-sorted repair.

PR #500:
    abstract source/output coupling retained as a useful model;
    synthetic live-marginal replay not used;
    many-to-one first ownership allowed;
    bonus physicalization made explicit as an identity row channel.

PR #494:
    actual-response one-shot terminalization is compatible;
    the present theorem supplies the missing Hall-bonus type separation.
```

## 6. Immediate falsifiers

Reject at the first occurrence of:

```text
one Hall bonus assigned positive target or inherited declared score;
one Hall bonus sent through rough ownership or child recursion;
one negative Hall residual or row bonus coefficient;
one source occurrence or Hall edge with two owners;
restriction applied to only one of the two Hall sorts;
a second label-dependent bulk quantizer;
a bonus row passed through the finite/continuum comparison instead of identity;
a comparison, terminal reserve, or Y4 vector with a different row identifier;
an omitted physical column, especially q<K;
a terminal margin <=0;
a native total >=60989;
the benchmark bridge J_Lambda-4sqrt(X);
the reversed endpoint inequality.
```

## 7. Boundary

```text
target-null complete Hall packet            refuted
positive Hall residual source               proposed exact on frozen inputs
direct nonnegative Hall-bonus row            exact
one two-sorted realized row                  proposed complete
all-column complements                       proposed complete on frozen bounds
native deficit                               <60989
endpoint implication                         frozen conditional / reconstruct
accepted proof of RH                         no
Riemann Hypothesis                           unproved pending hostile review
```
