# T-98070 — Negative-mass, Euler–Hurwitz, and vanishing-hinge closure frontier

Claim ID: `T-98070`  
Status: **UNCONDITIONAL REDUCTIONS; CONCLUSION-PRODUCING ESTIMATE OPEN**  
Created: 2026-08-18  
RH status: **unproved**

This packet proves or records under collision-safe IDs:

1. every fixed raw rough prefix changes sign infinitely often (`R-98070`);
2. the integer target Euler discrepancy is a strict Hausdorff moment sequence (`L-98070`);
3. Hurwitz interpolation isolates one fractional-cell reset (`L-98071`);
4. no finite dyadic target filter cancels both tail modes while staying nonnegative (`R-98071`);
5. subpower logarithmic negative mass of the native scalar implies RH (`L-98072`);
6. that negative mass is controlled by `(D-6)_+` (`L-98072`);
7. the log energy is one exact source-faithful Type-II kernel (`L-98073`);
8. a vanishing Lorenz hinge supplies the squarefree-shell reserve (`L-98074`);
9. fractional reciprocal roots give a positive Julia hierarchy (`L-98075`);
10. scalar and target zero hinges obey an exact two-adic bridge (`L-98076`).

The strongest sufficient targets are
\[
\int_1^T(\mathcal D(t)-6)_+\,dt/t=T^{o(1)}
\]
or, for one subpower `L(X)` with `L(X)^2=o(sqrt X)`,
\[
D_X^+(L(X)/\sqrt X)\ge0
\]
eventually (and even subpower logarithmic negative mass of that hinge suffices).

Equivalent quadratic work may target the one-sided off-diagonal kernel of
`L-98073`.

```text
raw-prefix eventual positivity              REFUTED
Euler knot Hausdorff reservoir               PROVED
fractional-cell reset identity               PROVED
finite positive dyadic closure               REFUTED
negative-mass Mellin criterion               PROVED
prefix-exceedance reduction                  PROVED
exact Type-II energy kernel                  PROVED
vanishing-hinge shell regularization         PROVED
fractional-root Julia hierarchy              PROVED
two-adic scalar-target bridge                PROVED EXACT / SIGN OPEN
DPE67 / VHL67 / cross-kernel estimate        OPEN / RH-BEARING
Riemann Hypothesis                           UNPROVEN
```
