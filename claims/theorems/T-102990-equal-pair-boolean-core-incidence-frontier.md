# T-102990 — Equal-pair Boolean core incidence frontier

Claim ID: `T-102990`  
Status: **MAJOR UNCONDITIONAL OWNER-GAUGE BYPASS; RH UNPROVED**  
Created: 2026-08-25  
Depends on: `T-102960--T-102980`; `L-102962--L-102963`  
RH status: **unproved**

`T-102980` sharpened the concentrated minimum-owner route to one star/Plücker curl. The new observation is that this curl is not intrinsic to the Boolean balanced source.

## 1. Symmetric ownership is cofinally legal

`L-102957` proves that the Boolean balanced packet contains no label above \(4\sqrt Y\) once \(Y>64\). Therefore every unordered pair is horizon-safe.

`L-102962` consequently permits the canonical equal-pair Duhamel gauge to be retained throughout the cofinal proof. No transfer to one deterministic minimum pair is required, and no star/cycle owner-gauge correction is created.

## 2. Phase labels are independent of pair owners

`L-102963` proves that source-owned Boolean, discrepancy, activation, or boundary labels may be placed at endpoint temperatures independently of the owner gauge. The additive Ramanujan identity requires only that the phase label divide one physical product and not the other; it does not require that label to be an unsquared owner.

All nonempty endpoint-color fluctuations begin at squared activity and remain in the established polylogarithmic ledger.

## 3. Physical incidence masks are analytically harmless

`L-102959` proves the one- and two-family centered phase estimates with arbitrary source-incidence masks. Therefore the insertion of the literal Boolean support, gcd stratum, internal discrepancy label and clean owner/core condition does not change the analytic divisor-kernel bound.

The only remaining issue is the arithmetic size of the complete two-sided core packet after its source-owned incidence decomposition. No owner-transfer or mask-versus-collapse theorem remains unnamed.

## 4. Closed sectors

The inherited exact reductions give:

```text
Boolean Type-I row                         power-saving;
finite and terminal rows                   closed;
equal reduced core                         subpower absolute;
one-sided reduced core                     subpower absolute;
very-large common square core              absolutely summable;
squared/higher-prime-power color variance  polylogarithmic;
equal-pair owner multiplicity              polylogarithmic.
```

Only the coprime two-sided Boolean core remains.

## 5. Canonical remaining theorem

Define

```text
BCI102990:
  in the canonical equal-pair owner gauge, after exact common-core extraction,
  primewise endpoint placement and incidence-masked centered phases, the
  complete coprime two-sided Boolean core has subpower logarithmic negative
  mass in the fixed derivative/outer observation.
```

Then

\[
\boxed{
\mathrm{BCI}_{102990}
\Longrightarrow
\mathrm{ICPR}_{102970}
\Longrightarrow
\mathrm{OICP}_{102960}
\Longrightarrow
\mathrm{HMO}_{102940}
\Longrightarrow
\mathrm{RH}.
}
\]

The concentrated minimum-owner route remains available:

```text
COCURL102980:
  star/Pluecker orientation in one deterministic owner gauge;

MOBOSM-NE106081:
  stronger positive-moment estimate.
```

But neither is required by the symmetric normal form.

## Exact boundary

```text
cofinal completion safety for every pair       PROVED
canonical equal-pair owner gauge                PROVED EXACT
owner/phase gauge decoupling                    PROVED EXACT
arbitrary physical incidence masks              PROVED
minimum-owner star/cycle current                 BYPASSED
coprime two-sided Boolean core BCI102990         OPEN / RH-BEARING
Riemann Hypothesis                              UNPROVED
```

The final arithmetic problem is now independent of the choice of owner gauge. It is the one-sided physical orientation of the literal coprime Boolean core.