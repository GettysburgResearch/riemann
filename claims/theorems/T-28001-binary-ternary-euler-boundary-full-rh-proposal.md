# T-28001 — Binary–ternary Euler boundary proposal for RH

Claim ID: `T-28001`  
Title: A finite factor-eighteen reflected source certificate controls the dyadic bottom charge and proves the Riemann Hypothesis  
Status: **FULL CONDITIONAL PROPOSAL — ONE SOURCE-IMAGE BOUNDARY RECURRENCE OPEN**  
Authoring agent: `gpt56-pro`  
Created: 2026-08-08  
Frozen parent: PR #277 at `d5be8262c80a1debcf86922b45a4d00a406803f1`

## 1. Full-problem reduction

Let `A_X(n)` be the exact equal binary–ternary producer of the critical carry
target

\[
w_X(q)=q^{-1/2}\log(X/q).
\]

`L-28001` proves the exact identity

\[
\boxed{
\mathcal C_2(X):=2A_X(2)+A_X(3)
=-\sum_{q=2}^{X}\frac{b_2(q)}{\sqrt q}\log\frac Xq.}
\tag{T-28001.1}
\]

An eventual nonnegative bottom charge `mathcal C_2` implies RH directly by
Landau's theorem.  Thus the full producer, its total variation, and every upper
row have been removed from the logical consumer.

This is not a finite-height criterion.  The source has Dirichlet series

\[
\frac{1-2^{-s}}{\zeta(s)},
\]

so every hypothetical off-line zero remains visible.

## 2. Binary–ternary Euler source

Use

\[
\omega_{2,3}
=b_2*(\varepsilon-\delta_3),
\qquad
\Omega_{2,3}(s)
=\frac{(1-2^{-s})(1-3^{-s})}{\zeta(s)}.
\]

`L-28002` proves:

```text
positive inverse a_(2,3)(n)=(v_2(n)+1)(v_3(n)+1);
nonnegative generalized von Mangoldt weights;
exact generalized Selberg square;
compact pointwise carry wavelet;
all binary–ternary source rows vanish for n>=18m-2;
positive synthesis of the actual generalized-prime profile;
uniform strict carry-feature Schur reserve.
```

The corresponding Riesz signals satisfy

\[
R_{2,3}(x)=R_2(x)-3^{-1/2}R_2(x/3).
\tag{T-28001.2}
\]

Hence a scale-stable upper envelope for `R_(2,3)` gives the same envelope for
`R_2` by a convergent ternary iteration.

## 3. Sole new theorem — Binary–Ternary Euler Boundary Certificate

For

\[
D_{2,3}(X)=\sup_{2\le x\le X}[R_{2,3}(x)]_+,
\]

the proposed production theorem is:

> **BTEBC.** There are absolute constants `A,C` such that every sufficiently
> large endpoint has a complete source-bound decomposition proving
> \[
> \boxed{
> D_{2,3}(X)
> \le C\log^A(2X)
> +\max_{2\le Y\le X/3+C}D_{2,3}(Y).}
> \tag{T-28001.3}
> \]
> The decomposition must be emitted before any absolute value and must contain
> the complete independent-frequency physical source.

Acceptance requires the following exact ledger.

1. **Source.** Every `omega_(2,3)` coefficient, every `2`- and `3`-adic sibling,
   and the stable map to `b_2`.
2. **Finite transition.** Every row in
   \[
   m\le n<18m-2,
   \]
   together with all endpoint and noncoprime cases.  Rows outside this band must
   vanish by the exact box identity, not by an estimate.
3. **Positive inverse synthesis.** The actual generalized-prime carry profile,
   with all cross terms in (L-28002.12).
4. **Physical block.** PR #241's two independent frequencies and every translate
   cross term.  A one-frequency vertical integral is not accepted.
5. **Reserve.** The physical-to-carry source map must preserve a strict fraction
   of the carry Schur reserve of `L-28002`.
6. **Boundary charge.** The only nonpositive destination is the explicit scalar
   `R_(2,3)` at an endpoint at most `X/3+C`; all other terms are nonnegative
   squares, digital endpoint atoms, or a declared polylogarithmic finite collar.
7. **Firewall.** The same object must reproduce the dyadic and `2/3` fixed-ratio
   Möbius shell mutations.

This is a finite-ratio source theorem, not a bounded-rank or generic operator
claim.

## 4. BTEBC implies a subpower bottom charge

Iterating (T-28001.3) gives

\[
D_{2,3}(X)=O(\log^{A+1}(2X)).
\tag{T-28001.4}
\]

Equation (T-28001.2) then gives

\[
[R_2(X)]_+
\le
\sum_{j\ge0}3^{-j/2}
D_{2,3}(X/3^j)
=O(\log^{A+1}(2X)).
\tag{T-28001.5}
\]

The upper-envelope form of Landau's theorem, equivalently the pole-tomography
argument already used in the square-screw and fixed-ratio shell programmes,
excludes every pole with positive real part.  Therefore no zeta zero has real
part greater than `1/2`; functional-equation symmetry gives RH.

Thus

\[
\boxed{
\mathrm{BTEBC}
\Longrightarrow
\mathrm{RH}.}
\tag{T-28001.6}
\]

A stronger exact sign decomposition would give `R_2<=0` and may use the simpler
one-sign argument of `L-28001`; it is not required.

## 5. Why this is a full-problem attack

The proposed proof no longer asks for:

```text
all producer coefficients to be positive;
BTF total variation;
ambient third- or fourth-Abel kernel positivity;
generic balanced Type II;
a bounded face or Bohr rank;
a monotone positive cover;
a whole Green-energy estimate.
```

It asks for one physical source map on a fixed finite ratio band and consumes it
through a reciprocal-zeta scalar which directly detects every off-line zero.

The branch also records exact counterexamples to the finite-order Abel detour,
so the production theorem cannot be replaced by a false ambient positivity
surrogate.

## 6. Relationship to the live routes

- PR #269 supplies the closest existing physical transition technology, but for
  the dyadic `omega_2` source and a factor-five band.
- PR #263 supplies parity-paired finite reconstruction into that transition.
- PR #241 supplies the correct physical block.
- PR #276 supplies the canonical WSTS equivalence and an independent scalar
  firewall.
- PR #277 supplies the exact binary–ternary producer.

BTEBC is the direct composition of those ideas around the source naturally
matched to both branching scales.

No unproved conclusion from those branches is imported as a theorem.

## 7. Automatic rejection conditions

Reject a proposed BTEBC proof if it:

1. invokes third-Abel or fourth-Abel kernel positivity;
2. omits one `2`- or `3`-adic sibling;
3. drops a generalized-prime cross term;
4. uses an analytic square in place of a reflected modulus square;
5. estimates the ambient vector space rather than the declared source image;
6. leaves a same-scale boundary charge outside the factor-eighteen manifest;
7. turns a carry-space reserve into a physical reserve without an explicit map;
8. loses the `b_2` or `2/3` Mertens mutation;
9. promotes finite sign reconnaissance to the cofinal theorem.

## 8. Exact status

```text
binary–ternary producer algebra             inherited proposed exact
dyadic bottom-charge collapse               proposed complete exact
factor-eighteen Euler source                proposed complete exact
positive inverse/generalized primes         proposed complete exact
carry-feature Schur reserve                 proposed complete
third/fourth Abel ambient closure            shown false
physical BTEBC source map                    OPEN / RH-BEARING
BTEBC -> dyadic bottom envelope -> RH        proposed complete
Riemann Hypothesis                          UNPROVEN
```
