# T-96302 — Two independent minimal producer contracts for RH

Claim ID: `T-96302`  
Status: **CONDITIONAL CLOSURE CONTRACT — BOTH PRODUCERS OPEN**  
Created: 2026-08-17

Either of the following independently implies RH:

### Front A — compact annular complete-gap quadrature

For one fixed `h>0`, prove

\[
F_\Lambda(X)-2F_\Lambda(e^{-h}X)+F_\Lambda(e^{-2h}X)
=O(\log^B(2X)).
\]

### Front B — one scalar reciprocal-zeta row

Prove

\[
5c_X(2)+3c_X(3)\ge0
\]

for every sufficiently large integer `X`.

A stronger sufficient scalar producer is

\[
\mathcal V(X)=
\sum_{d\le X}\frac{\mu(d)}{\sqrt d}\log(X/d)\ge0
\]

eventually.

The complete affine–Volterra physical geometry supplies a third, weaker source target: the signed moment-cone inequalities of `L-96303` on every endpoint cell. That target alone does not control the arithmetic gap `F_Lambda`; it is a bridge for constructing the scalar row or a direct reciprocal-state Lyapunov function.

```text
ACTQ_h          OPEN / RH-BEARING
SPRP            OPEN / RH-BEARING
VRP             OPEN / RH-BEARING
AMCP            OPEN / PHYSICAL SOURCE TARGET
Riemann Hypothesis UNPROVEN
```
