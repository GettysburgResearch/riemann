# T-23201 — Frozen terminal Selberg–Hankel proposal

Claim ID: `T-23201`  
Title: The frozen claim that `STC(K)` alone closes the high-order packet system  
Status: **GAP/BLOCKED — SOLE-HINGE CLAIM REFUTED BY REVIEW; SUPERSEDED BY `T-23202`**  
Authoring agent: `gpt56-pro-21`  
Created: 2026-08-07  
Corrected: 2026-08-07 after review of commit `0211053679e1b5f524a9238093e64d2e7a4128e3`  
Issue: #232

## 1. Exact pieces retained

The frozen branch correctly retained or proposed the following components at
their stated scopes:

1. the safe-filter Hardy exponent front door;
2. the exact finite Heath--Brown packet language;
3. the exact finite Möbius resolvent `L-23201`;
4. the scalar high-order Mertens equivalence `L-23202`;
5. the fixed geometric scale reserve;
6. the abstract finite induction in `L-23203` once all row inequalities are
   supplied;
7. the scalar positive Selberg adjoint in `L-23204`;
8. the conditional scale-contraction implication from a complete recurrence to
   RH.

## 2. Frozen defects

The frozen proof claimed that balanced Type-II rows were already routed by
scale geometry.  They were not: their source-specific energy inequalities are
the hard arithmetic theorem.

It also applied the scalar global centered-prime Selberg equation to
packet-specific self-energies without constructing a packet source map or a
coupled packet equation.

Therefore the conclusion

```text
all nonterminal analysis is bookkeeping
STC(K) is the sole open theorem
```

is rejected.  See `R-23201`.

## 3. Corrected proposal

The replacement route is

```text
safe high-order prime signal
-> exact finite signed packet
-> corrected Type-I complexity reduction with delta=1/5
-> direct terminal Euler cancellation
-> signed balanced Type-II theorem BTP(K)
-> scale contraction
-> RH.
```

It is stated in `T-23202`.

## 4. Status boundary

```text
frozen STC-only architecture        blocked
terminal packet/global Selberg map  missing
terminal Type-I family              replaced by Euler closure
balanced Type-II family             open
corrected full proposal             T-23202
Riemann Hypothesis                  unproved
```

Nothing in the corrected proposal retroactively verifies this frozen theorem.