# R-105420 — Two-sided observation has no strict phase gap

On `L^2(R)` or a periodic observation space, translations are unitary and have
numerical radius one. The lower bound of L-105422 collapses to zero. One-sided
support is load bearing.
