# R-97240 - Parity-blind terminal reuse and source-blind Julia promotion do not close RH

Claim ID: `R-97240`  
Status: **PROVED INTERFACE FIREWALL**  
Created: 2026-08-17

The parity cocycle of PR #561 remains binding. A terminal theorem proved in one canonical orientation cannot be applied after an odd rough history unless the reversed datum has its own positive realization. Therefore the parity-blind terminal compositions of the full-row and annular factor-67 proposals are not valid as written.

The Julia diagonal has physical Dirichlet symbol
\[
H(z)=3(2+2^{-z})\frac{\zeta(z)}{1-2^{-z}}-6.
\]
It retains the pole of `zeta(z)` at `z=1`. The positive eigenchannels `H+-A` therefore have real Mellin abscissa `1/2` in the shifted variable. Positive matrix gluing preserves parity, but its trace cannot by itself supply the Landau continuation to `Re s>0`. A trace-free arithmetic extraction is still required; `GABPT` is precisely that source-specific extraction theorem.
