# R-100210 — Unrestricted constant telescoping is not the physical Euler source

Claim ID: `R-100210`  
Status: **PROVED EXACT SCOPE FIREWALL**

The identity

\[
\prod_p(I-p^{-1}U_p)c=c\prod_p(1-p^{-1})
\]

is valid for a constant defined at every shifted endpoint.

The physical packet is zero below activation. Its exact image is instead the truncated Euler prefix

\[
c\sum_{p_A\le X}\frac{(-1)^{|A|}}{p_A}.
\]

The difference is the inactive-subset tail in `L-100210.4`. That tail is critical arithmetic data and cannot be dropped, assigned to Type I, or replaced by the full Euler product.

This firewall prevents the new largest-prime telescoping identity from being misrepresented as a proof of `BLPTE67`.