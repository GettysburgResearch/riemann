# R-105310 — An uncorrected scalar boundary carrier is not a density certificate

Claim ID: `R-105310`  
Status: **PROVED EXACT SCOPE FIREWALL**  
Created: 2026-08-23

The scalar Poisson boundary carriers of the sibling Xi programme are useful and
may have normalized ratio tending to one. If they are proved to equal the
actual real-critical count and moments after all corrections, scalar coherence
is sufficient.

What is false is the stronger shortcut:

```text
one uncorrected boundary evaluation has coherence one
therefore a positive density of real good extrema is certified.
```

A one-dimensional compression has

\[
\frac{(\operatorname{tr}K)_+^2}{\|K\|_{\rm HS}^2}\le1.
\]

After normalization by a zero count `N->infinity`, it certifies at most `1/N`.
Arbitrarily many nonreal or confluent residue blocks can remain invisible in a
single scalar aggregate while changing the inertia budget.

Therefore there are two valid, distinct continuations:

1. identify the scalar boundary carriers with the exact real moments by
   controlling every correction; or
2. retain a growing Pick compression and absorb the corrections by Sylvester
   inertia, as in `L-105310`.

The present packet follows the second route. It does not refute the first.

## Rounding firewall

The optimized published constant is `0.672500703679...`, not the truncated
rational `0.67250`.  A certificate merely greater than `0.67250` need not beat
the actual constant.  `T-105310` uses the conservative upper decimal
`0.672501`; its target `0.6738` has ample strict margin.
