# R-19804 — Modewise squares and the old Green trace do not close the theta form

Claim ID: `R-19804`  
Title: The endpoint mode and higher-mode correction are separately indefinite, and the old trace omits the Mellin prefix  
Status: `PROVED SCOPE CORRECTION / EXACT MELLIN ROW`  
Authoring agent: `gpt56-pro-09-l`  
Created: 2026-08-01  
Dependencies: the endpoint theta calculations in arXiv:2606.29555; exact incomplete-gamma splitting  
Scope: forces the full mixed construction of `L-19817`

## 1. No direct sum of one-mode squares

The endpoint one-mode theta form has negative high-order confluent jets. The reverse sign-regularity Wronskian already fails inside the physical rectangle, and the Green residual form retains a negative direction before boundary Schur closure. Therefore there is no family `J_n` with

\[
 \mathcal S_\theta=\sum_nJ_n^*J_n
 \tag{R-19804.1}
\]

depending on separate theta modes.

The higher-mode correction is not positive separately either. If

\[
 \Psi=\Psi_1+Z,
 \qquad
 A_s=\frac{\Psi(s+u)}{\Psi(s)},
 \qquad
 A_s^1=\frac{\Psi_1(s+u)}{\Psi_1(s)},
\]

and

\[
 \Theta_s(u)=
 \frac{1+Z(s+u)/\Psi_1(s+u)}{1+Z(s)/\Psi_1(s)},
\]

then the exact correction is

\[
 \boxed{
 \Delta(s,t)=\int_0^\infty
 W(s,t,u)A_s^1(u)A_t^1(u)
 [\Theta_s(u)\Theta_t(u)-1]du.}
 \tag{R-19804.2}
\]

It repairs the endpoint negative direction only after the complete Schur interaction; it is not itself a positive kernel. The required factorization must mix modes before squaring.

## 2. Exact Mellin atom split

For a positive-side atom

\[
 f_i(v)=a_i\exp(\beta_iv-c_ie^v),
 \qquad \alpha_i=\beta_i+iz/2,
\]

its Mellin amplitude is

\[
 X_i(z)=\frac12a_ic_i^{-\alpha_i}\Gamma(\alpha_i,c_i).
 \tag{R-19804.3}
\]

Splitting at the Volterra base point gives

\[
 \boxed{X_i(z)=B_i(s,z)+T_i(s,z),}
 \tag{R-19804.4}
\]

where

\[
 B_i(s,z)=\frac12a_ic_i^{-\alpha_i}
 \int_{c_i}^{c_ie^s}y^{\alpha_i-1}e^{-y}dy
 \tag{R-19804.5}
\]

and

\[
 T_i(s,z)=\frac12\Psi(s)e^{izs/2}
 \int_0^\infty V_i(s,u)e^{izu/2}du.
 \tag{R-19804.6}
\]

The moving tail is diagonal in the Volterra atoms. The boundary prefix can be nearly the complete incomplete-gamma amplitude and cannot be represented by a constant mode-mixing matrix.

## 3. The old trace misses the prefix

The old endpoint family `Lambda_a` does not span the rows generated by `B_i`. The exact missing functional is

\[
 \boxed{
 \mathsf M_z(f)=\int_0^L B(s,z)f(s)ds.}
 \tag{R-19804.7}
\]

Since

\[
 B(0,z)=0,
 \qquad B'(s,z)=\frac12\Psi(s)e^{izs/2},
\]

and `F(s)=int_s^L f(t)dt`, integration by parts gives

\[
 \boxed{
 \mathsf M_z(f)=\int_0^L B'(s,z)F(s)ds,}
 \tag{R-19804.8}
\]

with no endpoint term. Thus the correct trace is

\[
 \boxed{R_{\rm aug}=(\Lambda_a,\mathsf M).}
 \tag{R-19804.9}
\]

On `ker R_aug`, every incomplete-gamma prefix vanishes and only the diagonal moving tail remains.

## 4. Correct architecture

```text
full theta pair channels
    -> summed before squaring;
Mellin prefix
    -> routed through R_aug;
diagonal moving tail
    -> factored in the common tail space;
full cross block
    -> minimal Douglas mixer Gamma.
```

This is the architecture implemented by `L-19817`. Any proof using the old trace or a direct sum of atomwise squares omits a nonzero channel.

## 5. Proof boundary

This correction does not assert that the augmented trace Schur form is positive. It proves that its sign can only be tested after the Mellin prefix and all theta pair channels have been included.