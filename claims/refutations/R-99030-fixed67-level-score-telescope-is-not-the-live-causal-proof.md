# R-99030 — A deterministic `X/(d67^j)` score telescope is not the live arbitrary-prime causal proof

Claim ID: `R-99030`  
Status: **SUPERSEDED INTERFACE / SCOPE FIREWALL**  
Created: 2026-08-19

The live causal decomposition of `L-99021` exports children at endpoints
`Y/p_i` for their actual active rough primes `p_i>=67`. A proof written only on
the artificial chain

\[
Y_j=X/(d67^j)
\]

does not, by itself, prove score control for an arbitrary path

\[
Y_{j+1}=Y_j/p_j,\qquad p_j\in\{67,71,73,\ldots\}.
\]

This is not a counterexample to fixed-67 score inequalities. It is a
proof-interface warning: an explicit rebasing theorem would be required before
a deterministic `67` chain can be used for the live rough tree.

`L-99031` removes the issue. Dividing the exact causal score identity by the
killed survival mass `1-s_k` produces a discounted Markov kernel with discounts
`p_i^{-1/2}`. The resulting optional-stopping estimate works directly on every
actual rough-prime path and has no leaf-count factor.

```text
fixed-67 local score inequality         retained
fixed-67 artificial path as live tree   not inferred
actual-prime Bellman identity           L-99031
historical score conclusion             strengthened / recompiled
Riemann Hypothesis                      unproved
```
