# R-96000 — The prime-sieved row must not be closed through the stale zero-deficit endpoint normalization

Claim ID: `R-96000`  
Status: **EXACT SCOPE FIREWALL / ROUTE REPLACEMENT**  
Created: 2026-08-16  
Frozen parent: PR #537 at `2c2d4dd834ee61c54a6f8bdd7ba204a01896d593`  
RH status: **unproved**

The finite producer in `L-94200--L-94201` is logically separate from the
endpoint benchmark notation used in the parent conclusion. This packet retains

\[
 c_X(j)=\sum_{k\le X/j}\frac{\mu(k)}{\sqrt k}Q_{X/k}(j)\ge0
 \qquad(j\ge2)
\]

as the load-bearing finite theorem and consumes it directly by Mellin
transformation.

The native radix-four adjoint pairs physical slack with the prime-power score.
The complete native benchmark loss additionally contains the arithmetic gap

\[
F_\Lambda=J_{\rm nat}-P_\Lambda.
\]

Thus zero physical slack does not by itself imply zero native benchmark loss.
No conclusion in this successor uses that inference.

```text
prime-sieved row positivity             retained as finite producer
stale endpoint zero-deficit inference   rejected
native benchmark bridge                 not used
direct Mellin–Landau consumer           controlling route
```
