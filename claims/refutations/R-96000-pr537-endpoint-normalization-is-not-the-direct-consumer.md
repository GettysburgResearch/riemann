# R-96000 — The prime-sieved row must not be closed through the stale zero-deficit endpoint normalization

Claim ID: `R-96000`  
Status: **EXACT SCOPE FIREWALL / ROUTE REPLACEMENT**  
Created: 2026-08-16  
Frozen parent: PR #537 at `2c2d4dd834ee61c54a6f8bdd7ba204a01896d593`  
RH status: **unproved**

## 1. Retained result

The finite producer in `L-94200--L-94201` is logically separate from the endpoint benchmark notation used in the parent conclusion. The present packet retains the proposed theorem

\[
 c_X(j)=\sum_{k\le X/j}\frac{\mu(k)}{\sqrt k}Q_{X/k}(j)\ge0
 \qquad (j\ge2),
\]

and attacks its consequences directly by Mellin transformation.

## 2. Rejected inference

The native radix-four adjoint identifies the literal score of the full Möbius row with the smoothed prime-power quantity `P_Lambda(X)`. The complete native benchmark loss has the form

\[
 J_{\rm nat}(X)-\mathcal H(d)
 =F_\Lambda(X)+\langle Y_4,\Omega_X-\Xi_d\rangle,
 \qquad F_\Lambda=J_{\rm nat}-P_\Lambda.
\]

Consequently, zero physical slack for `c_X` yields

\[
 J_{\rm nat}(X)-\mathcal H(c_X)=F_\Lambda(X),
\]

not zero. Naming the prime-power score itself `J_Lambda` does not remove the gap. No conclusion in this packet uses that identification.

## 3. Replacement consumer

For each fixed component row `j`, the Mellin transform of `c_X(j)` contains `1/zeta(s+1/2)`. Positivity of the row and Landau's theorem therefore exclude every pole generated by an off-line zeta zero, provided the explicit row kernel does not cancel that zero. `L-96000--L-96001` prove the transform and noncancellation.

```text
prime-sieved row positivity             retained as finite producer
stale endpoint zero-deficit inference   rejected
native benchmark bridge                 not used
direct Mellin–Landau consumer           controlling route
```
