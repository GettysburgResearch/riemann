# R-101000 — No proven-only path to RH exists in the frozen graph

Claim ID: `R-101000`  
Status: **PROVED GRAPH/PROVENANCE FIREWALL**  
Created: 2026-08-20  
Review cutoff: 2026-08-20  
RH status: **unproved**

After importing the frozen claim statuses into the canonical implication
hypergraph, there is no directed or conjunctive path from a verified
source-faithful producer to `RH` whose every conclusion-bearing input is
verified.

Every live path crosses at least one of two structural middle classes:

```text
CV:
  critical one-sided variation / activation-envelope deficit;

XD:
  signed cross-core dispersion / minimal wavelet.
```

The following are not additional independent exits:

```text
HTOC / DGOC positive squares          contain the unknown root scalar;
phase-Hasse signed packing            equals the minimal wavelet;
LPMW and BVD                          differ by an L1 error;
ACAD                                  is downstream of AFCD;
minimal-wavelet L2                    is RH-equivalent;
complex quadratic positivity          admits a nonarithmetic countermodel.
```

## Numbering firewall

The live repository contains reused claim numbers on distinct branches,
including multiple `T-100400` and `T-100200` lineages.  The matrix therefore
uses canonical immutable aliases `IM-*`, each keyed by

```text
PR number;
exact head SHA;
path;
original claim ID.
```

An original theorem number without all four coordinates is not a unique proof
object and must not be used as a dependency.
