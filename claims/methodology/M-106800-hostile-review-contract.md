# M-106800 — Hostile review contract

Reject the packet if any proof:

1. replaces the classical Mertens zero-abscissa theorem by RH;
2. drops the epsilon slack at the boundary \(\Theta=1/2\);
3. uses an endpoint value where a maximal prefix is required;
4. lets the logarithmic frequency vary faster than the declared subpower
   phase-transport range;
5. drops the duplicate-67 inverse or the rough-coordinate conditioning mass;
6. uses low rank as an operator-norm bound;
7. ignores the critical lattice tail;
8. claims a fixed power saving, a new zero-free half-plane, RH, or GRH from
   the stretched-exponential estimate.
