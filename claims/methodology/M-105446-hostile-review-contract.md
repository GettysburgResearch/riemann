# M-105446 — Hostile review contract for the six-dimensional microscope frontier

Claim ID: `M-105446`  
Status: **BINDING REVIEW PROTOCOL**  
Created: 2026-08-24  
RH status: **unproved**

## 1. Differential algebra

Recompute from

\[
\mathcal C={1\over2}(h u_h-u),
\qquad u_{aa}+u_{hh}=0,
\]

that

\[
\mathcal C_{aa}+\mathcal C_{hh}-{2\over h}\mathcal C_h=0.
\]

Then verify that `C=h^3 W` gives

\[
W_{aa}+W_{hh}+{4\over h}W_h=0,
\]

the radial Laplace equation in `R x R^5`.

A sign error here reverses the Newton-source interpretation and is fatal.

## 2. Maximum-principle scope

The strong maximum principle applies only on a connected region in which
`m_r` is holomorphic and `h>0`. It excludes a finite smooth zero contact under
`C<=0`; it does not cross a denominator pole, the zero-height axis or spatial
infinity.

The strict contradiction uses the all-base Xi anchor

\[
C_(r,b)(0,h)<0.
\]

Review the two cases `U_r'(b+h)>=0` and `<0` separately.

## 3. Coarse-scale scope

The coarse negativity statement must be uniform in real centre and in the
stated bounded base range. It uses the completed-zeta safe-line expansion and
is not replayed by `X-105446`.

Do not promote it beyond the fixed derivative order for which the constants
are controlled.

## 4. Extremal-line atoms

At maximal zero height, verify:

```text
f_beta=m_r(.+i beta) is Pick;
g_beta=-1/f_beta is Pick;
a top zero of multiplicity n gives a g_beta atom of mass n;
a noncommon top critical pole gives an f_beta atom of mass -rho.
```

The packing inequalities are one-sided because continuous Herglotz measure may
consume the remaining source budget.

## 5. Strict extremal-line theorem

`L-105449` proves only exact-line extinction:

```text
new noncommon derivative zero on the parent top line: impossible;
common top multiplicity: drops by one.
```

It does **not** inherit a quantitative parent height gap. The earlier draft
claim `beta(F^(M))<=beta-delta` was withdrawn before publication of the current
blob. A derivative-zero sequence may remain strictly below while approaching
the same supremum at spatial infinity.

## 6. Gauss-law signs

In six dimensions,

\[
Delta |X|^{-4}=-4pi^3 delta_0.
\]

Thus a residue `rho` contributes source `-4pi^3 rho delta`. Nonpositive
residues correspond to nonnegative sources. The Herglotz measure has mass
`-rho`, and the Newton potential is negative.

For a nonreal critical point the lifted singular support is an off-axis
four-sphere. No scalar shell coefficient is claimed without the full conjugate
calculation; only the support/type firewall is normative.

## 7. Replay boundary

`X-105446` authenticates exact rational algebra and a finite dual-capacity
fixture. It does not replay:

```text
strong maximum principle;
completed-zeta coarse asymptotic;
Xi top-height attainment;
zero-height spatial-escape exclusion;
CRVH105330;
RH.
```

## 8. Current scientific boundary

```text
six-dimensional lift                         PROVED EXACT
finite smooth first contact                   EXCLUDED
attained top-edge source packing              PROVED EXACT
new noncommon top critical creation           EXCLUDED
Gauss-law coordinate for critical hierarchy   PROVED EXACT / SIMPLE STRATUM
zero-height or spatial escape                  OPEN / RH-BEARING
Riemann Hypothesis                             UNPROVEN
```