# M-102500 — Hostile reconstruction contract

A reviewer should reject the programme execution at the first failure of:

1. the Mellin transform of `Phi_0`;
2. compact support or `C^1` boundary matching of `Phi_*`;
3. the exact Smith–Bezout identity;
4. zero safety in `0<Re(s)<1/2`;
5. distinct labelled treatment of the two copies of `67`;
6. the relative-coordinate action of `Q=u-Lambda`;
7. the commutator `[P,Q]=-iI`;
8. the exact CV/XD Gram determinant;
9. carrier removal before every regional norm;
10. explicit moving-cutoff source atoms;
11. the fixed-detector pole audit.

The following are immediate overclaims and must fail closed:

```text
ordinary Euclidean phase stress has positive bulk;
labelled-to-physical collapse is contractive;
the shell energy theorem proves the arithmetic shell estimate;
a finite campaign proves either global arithmetic row;
the local determinant by itself proves RH.
```
