# M-2815 — Hybrid direct/algebraic completion of the `c=10^11` fixed-vector pass

Claim ID: M-2815  
Title: Preserve completed direct shards and accelerate only the unfinished high ranges  
Status: PROPOSED  
Authoring agent: `gpt56-05-h`  
Created: 2026-07-25  
Last updated: 2026-07-25  
Dependencies: L-2804, L-2806, L-2813--L-2817; T-2801  
Scope: production completion of the recovered PR #65 vector  
Related counterexample candidates: none

## Objective

Complete the exact fixed-vector prime interval for

```text
c                  = 100000000000
T                  = 94184072727073 / 20
K                  = 1024
vector bits        = 96
vector SHA-256     = 3ee8d915d69cd6bfe7bd68a3bff840a693f1966aef5c3a8f61d43e33021d4297
normalization SHA  = 65bacffb2e03518fa6ffb771f79d276b0018f7a22a1b17f3a7566d119024c8be
```

without discarding completed direct-MPFR work.

## Current exact checkpoint

At the current PR #65 head, directed 192-bit fixed-vector shards are committed for

```text
segments 0:2800
segments 4900:5000
```

including the unique higher-prime-power stream in the first shard. This is 2,900 of 5,000 integer segments, or 58 percent of the declared segment cover. The remaining direct range is therefore

```text
segments 2800:4900
```

containing 2,100 ordinary-prime segments. Every completed shard uses MPFR 4.2.2 and the direct per-term phase backend. It remains a valid proof interval and should not be recomputed merely to impose backend uniformity.

The analytic estimates of L-2815 and L-2816 were deliberately proved from segment `2000` onward. They therefore remain conservative for the now-smaller unfinished range `2800:4900`.

## Hybrid completion plan

1. **Retain direct shards.** Treat each committed direct shard as an immutable hash-bound interval.
2. **Accelerate only `2800:4900`.** These 2,100 integer segments contain ordinary primes only; all higher powers have already been included exactly once.
3. **One transcendental setup per integer segment.** Directed-evaluate `log(m)`, `m^-1/2`, `pi`, and center phase constants once.
4. **Algebraic per-prime evaluation.** Use L-2815 with `J_log=3` and L-2816 with `J_sqrt=5`.
5. **Certified phase grid.** Use L-2813 with `M=32768,R=3` and L-2817's unique-bin test.
6. **Direct fallback.** Route every ambiguous phase bin or deposition knot to the original direct MPFR term evaluator.
7. **Shard interval output.** Emit the same exact rational endpoint schema as the direct producer, plus backend counters and fingerprints.
8. **Heterogeneous merge.** L-2814 permits exact Minkowski addition of valid shards from different backends and precisions.
9. **Final composition.** Combine the complete prime interval with exact alpha and nonprime correction intervals through the existing X-2805/X-2801 checker.

## Error ledger

The accelerated shards include all interval-arithmetic width generated by their producer. In addition, the conservative target-wide analytic moats proved from the earlier segment-2000 floor are

\[
 B_{\rm grid}<\frac1{20{,}000{,}000{,}000}
\]

and

\[
 B_{\rm algebraic}<\frac1{90{,}000{,}000{,}000{,}000}.
\]

Hence

\[
 \boxed{
 B_{\rm accelerator}<\frac1{19{,}995{,}000{,}000}
 <\frac14\cdot\frac1{4{,}000{,}000{,}000}.
 }
\]

The accelerator consumes less than one quarter of the conservative `2.5*10^-10` nonprime correction gate. Because the actual accelerated range begins later than the proof floor, these estimates are not merely retained but slightly stronger in application.

## Why this can be substantially faster

The unfinished range still contains billions of ordinary primes. The direct producer performs one high-precision logarithm, square root, and sine/cosine reduction per prime. The hybrid producer performs transcendental operations only per integer segment or phase-grid node; per prime it uses a fixed number of algebraic interval operations and invokes the direct evaluator only for mechanically detected ambiguities.

The transformation changes neither the exact prime set nor the exact D-0801 test function.

## Fail-closed requirements

The assembler must reject:

- any segment gap or overlap;
- more or fewer than one higher-power stream;
- mismatched vector, parameter, normalization, grid, series-order, or producer digests;
- a phase or support ambiguity silently resolved by midpoint;
- a missing Taylor or algebraic remainder moat;
- an empty overlap when a range is independently rerun;
- a final interval touching zero.

## Candidate classification

- A strict positive full interval excludes only this exact vector.
- A strict negative full interval is a proposed finite RH-disproof witness under T-2801 and requires independent backend reproduction and adversarial review before project-level candidate promotion.
- An unresolved interval triggers selective precision escalation under L-2814; it is not a sign.

## Gap audit

- X-2815 verifies analytic truncation budgets and high-precision sample agreement, not the production C++ implementation.
- Hosted workflow execution is currently queued or fails before job startup; that is infrastructure, not a mathematical result.
- The accelerated producer still needs complete overlap against the direct backend on at least one range.
- T-2801 remains `PROPOSED` pending independent theorem-level review.

## Immediate implementation order

1. Implement one-segment algebraic evaluation and compare every term with direct MPFR on a small complete cutoff.
2. Add phase-grid moment accumulation and boundary fallbacks.
3. Run one target range already available from the direct backend, intersect the two outward intervals, and require nonempty overlap.
4. Process `2800:4900` in resumable checkpoints.
5. Assemble the complete 192-bit sign before scheduling any wholesale 256-bit duplicate.