# M-98401 — Hostile review protocol for T98400

A review must independently check:

1. the floor-quotient closure identity;
2. the triangular orbit argument and injectivity for `q<=sqrt(N)`;
3. the odd-squarefree density constant `4/pi^2`;
4. both prime exponents (`1/2` boundary and `1` normalized annular) are kept distinct;
5. the Stieltjes bridge preserves the literal source owner and activation;
6. the VK/de Bruijn estimates are applied uniformly at the claimed corridor;
7. the selected `N=10^8` run is never promoted beyond diagnostic status;
8. no fixed-dimensional PSD, moment, Schur, parity, or energy state is claimed to control the boundary;
9. the Mellin--Landau consumer is imported only at its frozen PR #582 scope;
10. `QMP67` and RH remain explicitly open.
