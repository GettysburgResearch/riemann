# M-99020 — Hostile reconstruction protocol for the direct-integral score-free Hall candidate

## Objective

Reconstruct the conclusion-producing chain without importing theorem status from any historical branch.

## Mandatory order

1. Verify `R-99020`: the positive radix-four dual prices `P_Lambda-H`, not `J_Lambda-H`.
2. Recompute every compact Hall prefix on `1<=x<67`.
3. Verify that the Hall edge bonus is target-free, declared-score-free, current-only and componentwise nonnegative.
4. Reconstruct the global normalized component-row profile and every arithmetic activation boundary.
5. Reconstruct the residual-only causal identity, same-index placement, positivity and actual child target-mass bound.
6. Reconstruct the equality endpoint measure and its exact score normalization.
7. Check that direct integration produces the same integer-indexed row used by the all-column comparison.
8. Check all `q`, especially `2<=q<K`, and the terminal top-omission ownership.
9. Reconstruct the all-depth fixed-P61 score telescope.
10. Reprove the elementary parabolic benchmark bound.
11. Reconstruct the proper-prime-power asymptotic, prime endpoint transform and Landau sign argument.

## Immediate falsifiers

```text
negative compact Hall prefix;
negative component-row Hall bonus;
bonus assigned target, declared score, port or child mass;
source occurrence with two owners;
actual recursive target mass >= parent/8;
wrong equality-frame score normalization;
partial endpoint cell silently treated as whole;
q<K overfill;
top source both output and omitted;
use of Y4 to price J-H directly;
nonzero cancellation of an off-line Mellin pole;
RH-strength benchmark input hidden upstream.
```

## Heavy-computation boundary

The packet's standard-library replay authenticates compact finite algebra, exact constants and hostile mutations. It does not replace independent analytic proof of the endpoint frame, all-real profile tail, prime-square asymptotic or Mellin-Landau consumer.

## Scientific status

```text
full theorem candidate    YES
accepted proof            NO
independent review        REQUIRED
Riemann Hypothesis        UNPROVEN BEFORE REVIEW
```
