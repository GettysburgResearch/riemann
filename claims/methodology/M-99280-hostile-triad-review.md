# M-99280 — Hostile review protocol for the triad-hardened route

Review in this order:

1. replay the exact rational compact Hall/profile certificate;
2. verify the derivative formula and right-endpoint reduction by hand;
3. ensure the review-503 derivative-fibre operation never appears;
4. verify one source kernel carries Hall, alpha-children, the selected row and
   calibration labels;
5. reconstruct the actual local identity `E=J+ET+C`;
6. prove each primitive calibration belongs to the subpower class per unit
   typed source;
7. verify only alpha-children recurse and total mass is `<1/8`;
8. rederive the fixed-row Mellin transform and the large-row numerator;
9. apply Landau only to the eventually nonnegative surrogate.

Immediate falsifiers:

```text
a negative compact profile derivative;
a coordinate-specific child coupling;
a derivative-fibre causal subtraction;
a primitive calibration of fixed positive power size;
a non-holomorphic defect transform in Re(s)>0;
a zeta zero cancelled in every sufficiently large row.
```
