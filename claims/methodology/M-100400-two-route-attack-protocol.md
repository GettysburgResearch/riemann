# M-100400 — Hostile protocol for the two surviving routes

Status: **METHODOLOGY / NO RH CLAIM**

## Quadratic route

1. Preserve the exact source `beta` and the full formula in `L-100400.5`.
2. Do not use positivity of `Q_c` as positivity of the envelope.
3. At `c=-1`, retain the signed ratio-`16/9` collar.
4. At `c=0`, retain the signed future atomic tail.
5. A proof must establish `QACG100400` for the literal atom-plus-collar sum.

## Minimal-wavelet route

1. Keep the compact kernel `K_0` and its three activation bands.
2. Assign every nonunit integer to its unique largest prime before estimating.
3. Remove only the genuinely smooth sector using `L-100410.2–3`.
4. Do not replace the remaining bilinear form by absolute values, a diagonal
   large sieve, or an unrestricted rough reservoir.
5. A proof must establish `LPMW100410` on the exact ordered source.

## Phase-Hasse firewall

Any use of the phase-Hasse symbol must retain the `-P_B/2` term from
`L-100401.1`.  Neutral-phase cancellation alone is not root removal.
