# M-105310 — Hostile review contract for the low-order Pick descent

Review in this order:

1. Verify the sign and affine subtraction in `L-105310.1--L-105310.4`.
2. Check that every observation is a congruence and cannot increase positive
   index.
3. Reconstruct the confluent real and nonreal block signatures in `L-105311`.
4. Verify the linear nuisance budget using multiplicity excess `E` and
   unprotected distinct count `U`; do not replace it by an unsigned residue
   norm.
5. Check the imported constants against the pinned Anthropic xi-prime theorem.
6. Verify the exact rational threshold arithmetic in `T-105310`.
7. Treat `LPRT105310` as open. Neither the gamma model nor a one-point Poisson
   carrier is the arithmetic Xi compression.
8. Do not promote the multiplicity-counted 0.6738 criterion to a simple-zero
   record without the explicit common-zero/multiple-parent upgrade.

Immediate falsifiers:

- a sign error in the divided-difference kernel;
- a nonreal pair with more than its stated positive index;
- a confluent block violating the `3E/4+U/2` charge;
- a hidden observation chosen after seeing residue signs;
- a claimed prime-side 1% estimate without an exact explicit-formula source
  ledger;
- any statement that RH or a new zero proportion has already been proved.
