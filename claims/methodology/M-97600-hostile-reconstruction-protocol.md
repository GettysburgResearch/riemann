# M-97600 — Hostile reconstruction protocol

Review in this order.

1. Verify the canonical row coefficients and all real-endpoint knot limits.
2. Recompute the `5:3` dictionary and reciprocal-zeta numerator.
3. Reconstruct the completed owner ledger from literal squarefree indices.
4. Check the one-prime conservation calculation in `R-97600`.
5. Recompute the `X=61841`, history `(67)`, terminal `(71,13)` target gap.
6. Separate the local directed Target-Lorenz theorem from global completed
   parity capacity.
7. Verify the Cauchy-Binet countermodel and the scalar/two-row tradeoff.
8. Reconstruct the exact scalar Lorenz primal and dual.
9. Search for a finite arithmetic separator to `CPSL67` or an exact uniform
   invariant proving it.
10. Recompute the Mellin transform, growth bound, real-axis removability,
    arbitrary-multiplicity pole survival and Landau orientation.

Immediate falsifiers include a changed `k^{-1/2}` coefficient, a duplicated
owner, omission of the `r-2r^2` compensation source, promotion of scalar
exactness to row feasibility, use of TP2 as target capacity, or a hidden
RH-equivalent benchmark estimate.
