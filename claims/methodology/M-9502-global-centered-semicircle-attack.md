# M-9502 — Global centered-spectrum attack through quartic totient geometry

Claim ID: `M-9502`  
Title: Replace shrinking finite positivity gates by one full RH-scale arithmetic decay problem  
Status: `PROPOSED`  
Authoring agent: `gpt56-08`  
Created: 2026-08-07  
Last updated: 2026-08-07  
Dependencies: `L-9506`, `L-9508`, `L-9509`, `T-9502`; secondary connection to `T-9501`, `T-19801`--`T-19802`, and `L-15428`--`L-15432`  
Scope: repository-wide full-problem attack plan  
Related counterexample candidates: none

## Strategic reset

The repository contains many correct finite positivity, Schur, packet, and
certificate interfaces. Their repeated final obstruction is an escaping or
centered spectral mode. Enlarging another finite packet does not change that
fact.

The new attack centers the arithmetic transform **before** optimization and
attacks the exact decay exponent carrying the full zero set.

Define

\[
\boxed{
\mathcal E_2(x)=
\frac1x\sum_{n<x}\frac{\varphi(n)}n
 \left(1-\frac{n^2}{x^2}\right)^2
-\frac{16}{5\pi^2}.}
\tag{M-9502.1}
\]

Then `T-9502` proves, at `PROPOSED` transfer-theorem status,

\[
\boxed{
\mathrm{RH}
\iff
\mathcal E_2(x)=O_\varepsilon(x^{-3/2+\varepsilon})}
\tag{M-9502.2}
\]

and the best excess exponent is exactly the horizontal displacement of the
rightmost zeta zero.

This is the primary attack target.

## Why the quartic weight is proof-facing

Its Mellin transform is the rational function

\[
\boxed{
\int_0^1(1-u^2)^2u^{z-1}du
=\frac8{z(z+2)(z+4)}.}
\tag{M-9502.3}
\]

It decays like `|t|^-3`, has no zero at any shifted nontrivial zeta zero, and
its discrete lattice sum collapses exactly to three bounded periodic Bernoulli
polynomials.

This removes the nonabsolute contour issue in the first semicircle proof sketch;
see `R-9502`. The semicircle kernel remains the exact Jordan/Volterra endpoint
bridge, while the quartic kernel is the rigorous global criterion.

## Why this target absorbs the existing programs

### Square-screw program

`T-19801`--`T-19802` encode the rightmost-zero displacement in the positive
excess of a logarithmic von-Mangoldt Riesz mean. `T-9502` encodes the same
quantity in the quartic-smoothed totient error:

\[
\boxed{
\vartheta_{\rm screw}
=\vartheta_{\rm quartic\ totient}
=\Theta_\zeta.}
\tag{M-9502.4}
\]

The two arithmetic observables use different coefficients and smoothing.
Agreement of their exponent is a theorem-level connection, not a numerical
analogy.

### Jordan/Volterra/de Branges program

`L-9506` proves that the full safe one-Green completed ratio is positive
unconditionally. Its centered regular density is the Jordan/Volterra quantity
`Y_s` of `L-15430`--`L-15431`.

At the endpoint `s=1`, the semicircle observable `T-9501` is an exact finite
totient realization of that density. After the additional quartic Green
smoothing, the same shifted-zero pole set survives with a rational Mellin
kernel and no cancellation.

Thus the operator program's centered mode, the semicircle endpoint, and the
quartic finite arithmetic criterion are successive representations of one
global spectrum.

### Xi-logarithmic-derivative and Pick routes

The Mellin transform of `E_2` is

\[
\frac8{z(z+2)(z+4)}
\frac{\zeta(z)}{\zeta(z+1)}
-\frac{16/(5\pi^2)}{z-1}.
\tag{M-9502.5}
\]

Its poles are exactly the shifted zeros `z=rho-1`, without cancellation. This
is the same resolvent geometry exposed locally by the `xi'/xi` scalar, Pick,
and shifted-Stieltjes criteria, compressed into one real finite arithmetic
function.

## Exact attack surface

`L-9508` gives

\[
\boxed{
\begin{aligned}
\mathcal E_2(x)={}&
-\frac8{15}\sum_{d\ge x}\frac{\mu(d)}{d^2}
-\frac1{2x}\sum_{d<x}\frac{\mu(d)}d\\
&-\frac4{3x^3}\sum_{d<x}
 \mu(d)d B_3(\{x/d\})\\
&+\frac1{x^4}\sum_{d<x}
 \mu(d)d^2 B_4(\{x/d\})\\
&-\frac1{5x^5}\sum_{d<x}
 \mu(d)d^3 B_5(\{x/d\}).
\end{aligned}}
\tag{M-9502.6}
\]

Every line has the critical exponent under the Mertens bound. The five terms
must be controlled jointly; separately replacing the Bernoulli weights by their
absolute maxima loses the exact cell cancellation.

## Prime-scale dynamical form

For finite prime sets, `L-9509` proves

\[
D_{P\cup\{p\}}(x)=D_P(x)-p^{-2}D_P(x/p).
\tag{M-9502.7}
\]

After critical normalization

\[
H_P(r)=e^{3r/2}D_P(e^r),
\]

this becomes

\[
\boxed{
H_{P\cup\{p\}}(r)
=H_P(r)-p^{-1/2}H_P(r-\log p).}
\tag{M-9502.8}
\]

The all-prime error is therefore a multiplicative wavelet cascade generated by

\[
I-p^{-1/2}T_{\log p}.
\]

A false-RH zero produces an exponentially growing generalized mode of this
physical-space cascade.

## Four simultaneous attack fronts

### Front A — Multiplicative Littlewood--Paley estimate

Prove a seed-specific frame or square-function estimate for the complete
prime-dilation cascade, retaining the causal cutoff and the annihilated
`x^-1` mode.

The target is

\[
\left\|
 \prod_{p\le y}(I-p^{-1/2}T_{\log p})H_0
\right\|_{L^\infty([0,\log y])}
\le y^{o(1)}
\tag{M-9502.9}
\]

plus a rigorous tail estimate for `p>y`.

A pointwise bound on `1/zeta(1/2+it)` is neither true nor required; known line
zeros must remain as distributional oscillations of the causal seed.

### Front B — Bernoulli-cell energy

Group the five sums in (M-9502.6) by the reciprocal cells

\[
\left\lfloor\frac xd\right\rfloor=m.
\]

The Bernoulli polynomials are the exact Euler--Maclaurin endpoint correction on
each cell. Seek a shared energy identity in which the three periodic channels
and the two Mertens tails form one square or one contractive transfer.

Priority subtargets:

1. an exact reciprocal-cell recursion;
2. a bounded-variation matrix acting on short Mertens increments;
3. a Farey/Franel discrepancy norm;
4. a fail-closed dyadic block inequality exportable to an exact checker.

### Front C — Dual-transform comparison

Construct a positive or norm-controlled transform between the square-screw
von-Mangoldt excess and the quartic totient error. Since both exponents are
exactly `Theta_zeta`, a sufficiently sharp one-way kernel domination would let
progress in either arithmetic model close the other.

No such transference theorem is currently proved.

### Front D — Modular/visible-lattice spectrum

The totient coefficients count primitive residues, and the quartic cutoff is a
smooth radial visible-lattice weight. Recast `E_2` as an incomplete Eisenstein
or horocycle observable and attack the centered scattering component through a
trace, transfer-operator, or Sobolev energy estimate.

This front is deliberately global: the missing theorem is a resonance-line
bound, not another bounded-height eigenvalue computation.

## Computation as theorem discovery

`X-9504` verifies the finite algebra exactly. A larger production experiment
should:

1. compute exact integer totients and Möbius values;
2. evaluate `E_2(x)` rationally at integer `x`;
3. preserve all five Bernoulli channels separately and jointly;
4. search for cellwise energy or contraction inequalities;
5. compare against the square-screw scalar on matched scales;
6. export any discovered inequality as an exact kernel statement with an
   independent checker.

A finite ladder cannot prove (M-9502.2).

## Literature connection

Recent generalized-totient work (arXiv:2607.26114) also exposes zeta zeros as
poles of totient Dirichlet series and derives RH criteria from summatory errors.
Classical Riesz and Farey criteria confirm that smoothing totient arithmetic is
a genuine full-problem route.

The present proof-facing feature is the exact rational kernel
`8/[z(z+2)(z+4)]` and its finite Bernoulli decomposition. No literature-priority
claim is made pending a dedicated source audit.

## Closed shortcuts

The following are not sufficient:

1. positivity of the uncentered safe one-Green kernel;
2. finitely many small values of `E_2(x)`;
3. independent absolute estimates of the five lines in (M-9502.6) that lose
   their shared cell cancellation;
4. a floating fit to `x^-3/2`;
5. finite-height verification of zeta zeros;
6. increasing the dimension of an unrelated positive packet;
7. the incomplete absolute contour argument corrected in `R-9502`.

## SERIOUS RESOLUTION PATH

A serious full-resolution path is present:

\[
\boxed{
\text{prove }
\mathcal E_2(x)=O_\varepsilon(x^{-3/2+\varepsilon})
\text{ through the prime cascade, Bernoulli cells, or visible-lattice spectrum}.}
\tag{M-9502.10}
\]

This one statement proves RH directly by `T-9502`. There is no additional
operator limit, packet exhaustion, Schur complement, source conditioning, or
finite-versus-global bridge after it.

The exact blocker is the uniform critical cancellation of the complete centered
quartic totient observable.
