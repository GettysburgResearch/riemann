# M-105560 — Hostile review contract

Reject the packet if any of the following fails.

1. The formal baseline must come from `ThmD/Mult.lean`, not from the similarly
   valued `N0star` theorem in `Final.lean`.
2. `S` must denote simple on-line zeros; `N` must count complete multiplicity.
3. The remainder positive-index bound must be `(N-S)/2`, not `N-S` and not an
   unqualified distinct-zero budget.
4. The sixfold overlap of an interior gap must be retained, giving `1/500`.
5. The seven-gap verifier must be pinned to the exact external commit and the
   exact interval/certificate blobs.
6. An independent second replay must not be claimed unless workflow evidence
   is actually read back.
7. The theorem is a simple-zero proportion theorem. It does not prove ninety
   percent, density one, or RH.
8. Mutating any denominator or the final `>673/1000` comparison must fail the
   exact replay.
