# M-26203 — Fail-closed review protocol for the critical digital bank

Claim ID: `M-26203`  
Status: `METHODOLOGY / REVIEW CONTRACT`  
Applies to: `L-26210`, `L-26211`, `X-26203`, `X-26204`, and any physical banked transition theorem

## 1. Freeze all sources

Record exact heads for:

```text
PR #263  critical bank and hyperbola split;
PR #236  critical-prefix scaling and digital Sobolev tail;
PR #241  independent-frequency physical block;
PR #269  omega_2 factor-five transition and carry reserve.
```

No later repair inherits the verdict on a frozen theorem.

## 2. Reconstruct the finite convolution

Independently verify

\[
c_2*\omega_2
=
\varepsilon-\frac52\delta_2+\delta_4.
\]

Check the local \(2\)-adic rows and odd squarefree cores. Reject any source that omits a sibling or replaces \(\omega_2\) by total variation.

## 3. Strict prefix convention

For

\[
C_{<Y}(s)=\sum_{n<Y}c_2(n)n^{-s},
\]

verify for every integer \(R\ge5\)

\[
\sum_{d<R}\omega_2(d)d^{-s}C_{<R/d}(s)
=1-\frac52\,2^{-s}+4^{-s}.
\]

Changing one strict inequality may create or delete a boundary product. The checker must mutate every boundary convention.

## 4. Critical lower frame

Reconstruct

\[
1-\frac5{2\sqrt2}z+\frac12z^2
=(1-\sqrt2z)(1-2^{-3/2}z)
\]

and prove on \(|z|=1\)

\[
|1-\sqrt2z|\ge\sqrt2-1,
\qquad
|1-2^{-3/2}z|\ge1-2^{-3/2}.
\]

The squared bank loss is \(O(R)\), not polylogarithmic. Reject any proof claiming a smaller source-uniform rate without declaring a tempered critical-line channel.

## 5. Hyperbola coverage

For \(R=N^2\), independently enumerate every pair \(dn<R\) and verify

\[
\mathcal H
=
\mathcal S_N+\mathcal L_N.
\]

The production manifest must prove:

```text
all pairs included;
overlap subtracted once;
all L_N monomials satisfy N<=dn<N^2;
no current-scale remainder survives.
```

The overlap-omission mutation must fail.

## 6. Pole-mode scaling

At a zeta zero \(\rho=\beta+i\gamma\), use only the reviewed Abel bound for the digital prefix. Reconstruct

\[
|\mathcal S_N(\rho)|
\ll_\rho N^{1-2\beta}\log N.
\]

Do not convert this pointwise pole calculation into a physical \(L^2\) theorem without a complete independent-frequency source map.

## 7. Production physical theorem

A valid banked transition artifact must include:

- the double-frequency kernel of PR #241;
- every \(\omega_2\)-weighted bank cross term;
- quotient cells \(2,3,4\);
- the generalized-prime wavelet synthesis of PR #269;
- the complete upper endpoint collar;
- every lower-scale destination;
- a tempered-channel declaration;
- a strict reserve or the critical-order observability inequality;
- the dyadic and \(2/3\) Mertens mutations.

The artifact fails if it takes an absolute value before source recombination, charges the lower remainder in total variation, suppresses a genuine critical-line mode, or hides target energy inside the tempered channel.

## 8. Status boundary

```text
finite convolution and bank identity       reviewable exact algebra
critical O(R) frame scale                  reviewable exact analysis
hyperbola lower-scale support              reviewable exact algebra
physical upper estimate for observations  open / RH-bearing
Riemann Hypothesis                         unproved
```
