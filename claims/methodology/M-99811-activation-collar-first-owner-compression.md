# M-99811 — Activation-collar first-owner compression program

Status: **OPEN PROGRAM; NO RH CLAIM**

`L-99811` proves every deep two-prime cube has the favorable mixed sign. Therefore only activation-collar cubes remain. Absolute reciprocal pair mass in the collar grows slowly and is not itself summable, so the collar may not be paid source-blindly.

The correct next state keeps the exact future Euler completion after the first two owner primes. For ordered `p<q`, write

\[
 C_{p,q}(Y)
 :=\Delta_p\Delta_q\prod_{r>q}(I-r^{-1/2}U_r)\Phi(Y).
\]

The target `APCL99810` is a source-faithful bound for the logarithmic negative mass of the collar part of `C_{p,q}` only. The allowed inputs are:

1. exact first-owner identity of PR #652;
2. hereditary mesoscopic Bellman corridor of PR #608;
3. exact factor-67 finite base and zero-free box potential;
4. deep pair-submodularity of `L-99811`.

No absolute sum over future primes, unrestricted reservoir, or alpha-child replacement is permitted.

A successful compression should split by the least future prime `r>q`. If `r` is mesoscopic relative to the residual child scale, PR #608 supplies the hereditary one-prime inequality. If `r` is not mesoscopic, the state remains in the compact activation collar and must be charged to the finite product-boundary ledger. The proof must show these two sectors exhaust every collar history with no double ownership.
