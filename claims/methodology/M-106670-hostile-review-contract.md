# M-106670 — Hostile review contract for the regularized Pick free-energy packet

Claim ID: `M-106670`  
Status: **REVIEW CONTRACT**  
Created: 2026-08-26

A valid review must check all of the following.

1. `Q=P_M(I-P_N)P_M|M` is a positive contraction and the relevant endpoint charge is `tr Q`.
2. The scalar chord inequality `tau*x <= -log(1-tau*x) <= x[-log(1-tau)]` is applied only for `0<=x<=1`, `0<tau<1`.
3. The determinant is `det(G-tau V*GV)/det G`; deleting `G` or replacing it by diagonal products is forbidden.
4. The common outer factor is not silently bounded below. Source values remain `R(b)/O(b)`.
5. Confluent zeros use derivative kernels and multiplication jets, or are charged to the explicit regularization ledger.
6. Exact gate equivalence uses a strict limsup threshold and `tau_T -> 0`; it does not assert equality at fixed `tau`.
7. The full determinant receives allowance `97/1000`. A finite cutoff `eta` receives only `97/1000-3/(4000 eta)` after the proved height payment.
8. The unregularized `tau=1` logarithmic Laplace identity is not used as the live consumer without the firewall in `R-106670`.
9. The replay is finite-dimensional only and must record the Xi determinant gate false, `90%=false`, and `RH=false`.
