# M-99100 — Fail-closed native interface contract for the common-parent route

The abstract common-parent theorem is usable in the factor-67 RH programme only if all of the following are reconstructed from the exact native producer.

## A. One source space before normalization

There must be one finite or sigma-finite labelled source `(Omega,P_X)`. Every child, omission, current row, Hall bonus, and endpoint correction must have one owner on this source or on the single random-key extension `Omega x [0,1]`.

## B. Literal weighted-child domination

For every active child, prove in the **same unnormalized source coordinate**

\[
 \alpha_jC_j\ll P_X,
 \qquad
 \sum_j\frac{d(\alpha_jC_j)}{dP_X}\le1.
\]

A label injection, scalar mass inequality, or isomorphism after independent normalization is not enough.

## C. Normalize only after the partition

Target, row, score, and endpoint observables must be additive before division by one common positive mass. Then `L-99101` applies. If distinct coordinates use different normalizations, a new interface theorem is required.

## D. Samplewise finite realization

If a finite physical producer is required, every sampled leaf must satisfy:

```text
one-use source ownership;
nonnegative physical rows;
every ordinary and radix-four capacity;
all activation and endpoint restrictions;
the claimed literal score bound.
```

`L-99102` supplies this automatically only for restrictions encoded in one integer network-flow polytope. An extra score half-space or nonlinear native constraint must be incorporated without destroying integrality or checked on every leaf.

## E. No sign-adaptive construction

The source partition, flow graph, and randomization law must be fixed from the input data before observing the RH-bearing root sign. Post hoc selection of a contraction, Gram, or leaf after reading that sign is circular.

## Current exact boundary

```text
common-parent random-key partition       CLOSED / L-99100
factor-67 child coefficient <1/8         CLOSED / L-99100
normalize-last barycentric identity      CLOSED / L-99101
integral network Hall decomposition      CLOSED / L-99102
exact native producer as such a face     OPEN / LOAD BEARING
samplewise native score preservation     OPEN / LOAD BEARING
Riemann Hypothesis                       UNPROVED
```
