# M-99700 — Hostile reconstruction contract for the canonical scalar/native-source spine

Review the packet in this order.

1. Recompute `beta(n)=mu(n)-1_(67|n)mu(n/67)` and `beta*g=epsilon` with
   `g(n)=v_67(n)+1`.
2. Re-derive the scalar Mellin transform and audit the removable point
   `s=1/2`, every positive-real point, and zero multiplicities.
3. Verify the sequential first-owner identity coefficient by coefficient.
4. Differentiate the symmetric homotopy and check its two endpoints.
5. Derive the tilted renewal and owner evolution by finite divisor switching.
6. Check the positive-work domination of `h_-`.
7. Recompute the integrated tilt-energy bound.
8. Enforce the squarefree-degeneracy firewall: no ordinary Doob inference.
9. Enforce the positive-Euler-smoothing firewall: no pole-cancelling
   completion.
10. Treat `TOCE67` as open unless a source-root/boundary Carleson proof is
    supplied with all quantifiers and constants.

Immediate falsifiers are:

```text
a lost factor q^(-tau-1/2) in the owner flow;
a child coefficient r^2 substituted for the native r;
a future Euler factor dropped from a first-owner current;
a smoothing factor that vanishes at a translated zeta zero;
an L2-to-L1 step after the source index has been summed;
a claim that finite positivity implies the unbounded tail;
TOCE67 or RH marked proved without a complete argument.
```
