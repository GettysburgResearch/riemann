# M-99980 — Hostile reconstruction protocol for the two-route assault

Review the routes independently.

## Route A

1. Recompute the kernel \(R\) from the completed quadratic leading term.
2. Verify the future-tail identity including the atomic tail coefficient.
3. Check cancellation at \(s=1\) and \(s=1/2\), and noncancellation at every
   off-line zero.
4. Reconstruct all five two-prime activation regimes.
5. Check the inactive-tail formula with the second labelled \(67\).
6. Do not infer FEAG from pairwise or fully active positivity.

## Route B

1. Average the priority flow directly over random keys and recover
   `L-99990.1`.
2. Verify all vertex marginals on arbitrary finite weighted cubes.
3. Expand \(B_i^\pm\) and check odd-subset selection.
4. Verify that the physical potential difference supplies exactly
   \(1-p_i^{i\gamma}\).
5. Keep both labelled \(67\) vertices separate.
6. Do not substitute HTOC/DGOC, diagonal energy, or a source-blind large sieve
   for PSCP.

Any proof claiming RH must prove one of the two final gates on the literal
source. A diagnostic scan, a finite-prime theorem, or a random-coefficient
fixture is not sufficient.
