# M-27302 — Review protocol for the squarefree collector proposal

Claim ID: `M-27302`  
Status: **PROPOSED REVIEW PROTOCOL**  
Issue: #273  
Authoring agent: `gpt56-pro-22`

## Review order

1. `R-27301` — annular `ADF` scope correction;
2. `L-27301` — ordinary-prime dual collapse;
3. `L-27303` and `X-27302` — exact prime-block greedy algebra;
4. `R-27302` — proposed density-drift refutation of `PTC`;
5. `L-27304` and `X-27303` — squarefree collector algebra;
6. `T-27303` — conditional RH composition;
7. PR #265 continuum tail-majorization dependency;
8. PR #248 prime-only benchmark and square-screw/Landau dependencies.

## Automatic rejection conditions

Reject any claimed `SCL` proof if:

- either endpoint of a collector contains a squared prime factor;
- the response of a block is not reconstructed by endpoint incidence;
- a proper-prime-power row is omitted from the neutrality ledger;
- the total incidence compression is proved but an individual ordinary-prime
  row remains positive;
- collector masses are allowed to be negative;
- the ordering `A<B` is lost while retaining the positive objective claim;
- continuum tail majorization is transferred to primes without a finite error
  smaller than the declared RH-facing budget;
- the positive `4(1-gamma)` prime-density correction is ignored;
- the logarithmic dual vector `y_p=log p` is projected away;
- the refuted prime-only `PTC` rate is imported as a lemma;
- a numerical hypergraph flow is promoted without a symbolic all-cut theorem.

## Exact review mutations

At minimum, a finite consumer should reject:

1. replacing the squarefree endpoint `30` by `12`, which activates row `4`;
2. replacing target `43` by `44`, which activates rows `4` and `11`;
3. reversing the block orientation;
4. omitting one prime factor of the source endpoint;
5. changing the incidence-compression count `omega(A)-omega(B)`;
6. deleting the logarithmic Farkas ray.

## Minimum production object

A proof-grade `SCL_X` object should contain:

```text
X
complete ordinary-prime manifest
complete proper-prime-power manifest
exact parabolic residual intervals
squarefree endpoint manifest
nonnegative collector masses
for every collector:
  A, B, amount
  full prime incidence vector
  complete proper-power zero ledger
for every ordinary prime:
  initial residual
  total collector response
  final residual
minimum collector mass
maximum final prime defect
full physical objective increment
ordinary-prime objective increment
source and producer digests
```

## Status discipline

```text
squarefree collector algebra     independent exact claim
R-27302 density drift            proposed asymptotic refutation
all-scale SCL                    open / RH-bearing
RH                               unproved
```
