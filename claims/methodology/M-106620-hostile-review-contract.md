# M-106620 — Hostile review contract

Reject the packet if any argument:

1. infers \(a_T<\delta_T\) only from \(a_T\to0\);
2. uses the exact variable Riemann--Siegel scale in the height theorem without
   a quantitative zero-cluster threshold;
3. treats coefficient-level carrier mismatch as a Hankel-charge estimate;
4. omits the \(\lambda_jN_{5,j}\) rank-one companion-height term;
5. lets the number of mesoscopic boundaries grow as a positive proportion of
   \(N(T,2T)\);
6. aggregates deep and shallow model-space charges without the exact
   orthogonal height split;
7. reports `MESORSGAUGE106620`, ninety percent, density one, or RH as proved.
