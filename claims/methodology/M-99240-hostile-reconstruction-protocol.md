# M-99240 — Hostile reconstruction protocol for holomorphic-defect fixed-row closure

Review in this order:

1. verify the fixed component-row map
   \(\mathcal Rb(j)=(j+1)\Delta^2[b(j)/(j-1)]\);
2. reconstruct the retained-cell identity and the exact constant-tail formula
   \(2\eta/[j(j-1)]\);
3. check the adjacent-cell bound \(19m^{-3/2}/2\);
4. place every unproved knot/anchor term in the signed defect, not in the
   positive current;
5. verify uniform total variation of the compact knot/anchor ledger;
6. verify that only positive residual \(\alpha\)-children recurse and their
   total mass is below \(1/8\);
7. check the common-parent source uses one owner and one physical row;
8. derive the fixed-row Mellin transform from the displayed row formula;
9. independently derive the large-\(j\) asymptotic for \(P_j(z)\);
10. apply Landau to the eventually nonnegative surrogate after removing the
    finite initial interval.

Immediate falsifiers:

```text
a fixed-row calibration term growing like X^theta for theta>0;
row-only Hall bonus copied to a child;
separate source couplings for different component coordinates;
a non-holomorphic defect transform in Re(s)>0;
cancellation P_j(rho)=0 for every sufficiently large j;
a real singularity of the surrogate transform at positive s.
```
