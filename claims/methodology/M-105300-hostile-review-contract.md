# M-105300 — Hostile review contract for the low-order Levinson–Pick packet

Review order:

1. `L-105300`: verify the quotient-algebra trace form, complex-factor
   signature, Pick-kernel congruence, and exact root-count signature.
2. `L-105303`: verify the contour orientation, evaluation isomorphism, hyperbolic nonreal blocks, compression inertia monotonicity, and both endpoint bits.
3. `R-105300`: check that the all-real cubic really has coherence `16/25`.
4. `L-105301`: verify that multiplication by `w` gives a congruence and that
   the rank--trace inequality is applied to the augmented matrix of dimension
   `n`.
5. `L-105302`: audit the external Anthropic source lock and every proportion
   constant; do not substitute the zeta theorem for the Xi-prime theorem.
6. Run the exact-rational replay and hostile mutations.
7. Audit `T-105300` scope. No finite polynomial result is to be promoted to an
   entire-function window without localization, exhaustion, and boundary
   control.

Immediate falsifiers:

- any use of `p/p''` without inversion modulo `p'`;
- a nonreal conjugate critical pair treated as positive semidefinite rather
  than signature `(1,1)`;
- a rank--trace denominator missing the full critical count (`n` globally or `M` in a window);
- treating a compression positive index as larger than the full form's positive index;
- claiming unweighted coherence is necessary;
- claiming `0.694912` unconditionally;
- feeding an endpoint-dependent polynomial truncation directly to a fixed
  Mellin/Landau consumer;
- omitting common-zero or multiplicity branches.

Scientific flags required in every replay:

```text
anthropic_record_beaten = false
LHRT105300_proved       = false
LEXI105301_proved       = false
rh_established          = false
```
