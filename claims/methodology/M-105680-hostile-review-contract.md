# M-105680 — Hostile review contract for determinant and rank extension

A review must independently verify:

1. the Cauchy determinant normalization in `L-105680.2`;
2. the determinant ratio `det A_H/det B_H` and every diagonal/off-diagonal
   factor in `mathscr D_H`;
3. the polynomial identity proving strict positivity of each complex pair;
4. passage to confluent derivative blocks only by a normalized limit;
5. the AM/GM spectral-flatness implication, without promoting it to Loewner
   order;
6. the projection update
   `P_r=P_(r-1)+u_r tensor u_r`, `Q_r=Q_(r-1)+v_r tensor v_r`;
7. the exact increment identity and the formula for the new deep residual;
8. the scalar trial bound `m_2^2/m_4`;
9. that `REC105681`, arbitrary-rank trace CTI, cofinal Xi passage, and RH are
   all still open.

The replay checks exact rational determinant factors and finite projection
increments. It does not prove return compensation or RH.
