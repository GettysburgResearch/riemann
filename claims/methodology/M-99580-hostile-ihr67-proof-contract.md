# M-99580 — Hostile contract for any claimed proof of IHR67

A successor claiming IHR67 must:

1. use the exact `5:3` scalar and duplicate-`67` normalization;
2. reproduce the relative-persistence identity coefficientwise;
3. explain why the six-equal-label separator is outside its hypotheses;
4. prove a source-specific global transport, total-positivity,
   owner-Carleson, or equivalent cancellation theorem;
5. preserve every explicit noncancellation factor at an open-strip zero;
6. avoid treating a positive real Mellin symbol as a positive inverse density;
7. avoid treating the positive inverse renewal as a sign theorem;
8. reject any argument whose only global input is a finite endpoint scan;
9. record `rh_established=false` until independent reconstruction succeeds.
