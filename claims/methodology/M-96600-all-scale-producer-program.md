# M-96600 — Review-facing programme for the actual all-scale producer

The next proof attempt should attack one of two exact objects.

## Scalar prefix

Prove `SCPS` from `T-96600` using the actual finite dictionary. Acceptable mechanisms include:

1. a finite-facet or quadratic invariant for `(mathcal R_N,M_*(N))` under every admissible odd-core packet;
2. a globally owned transport on the coefficients in (L-96601.3), with exact mass and logarithmic first moment;
3. a direct complete-monotonicity proof for `A(s)/s`;
4. a source-correct prime induction proving (L-96601.4).

An all-integer rough block or a fixed-product three-knot packet is inadmissible.

## Compact annulus

Prove `ACTQ_2` by comparing the actual prime-power atoms with the exact deterministic cell density on `[X/4,X]`. Because of `R-96600`, the proof must use arithmetic information or a nonlinear/matrix state; another zero-safe finite shift filter cannot close the sign.

## Acceptance

A complete successor must contain the producer proof itself. A finite scan, a renamed RH-equivalent inequality, or an imported unreviewed factor-67 source theorem is not sufficient.
