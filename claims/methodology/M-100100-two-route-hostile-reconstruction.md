# M-100100 — Hostile reconstruction order for the two-route synthesis

Status: **BINDING REVIEW PROTOCOL**  
Created: 2026-08-20

Review in this order.

## Shared geometry

1. Verify the explicit quadratic and cubic carrier kernels.
2. Recheck every activation sector in the one-prime Harnack inequalities.
3. Reconstruct the Euler-level removal double count and `1/r!` factor.
4. Verify the inactive-prime tail, including the noncompact cubic sector.
5. Check that `1+sqrt(2)` is a strict asymptotic corridor constant, not a
   claimed global threshold.

## Route A

1. Reconstruct the cubic future-prime recurrence coefficient by coefficient,
   including the two labelled `67` transitions.
2. Confirm the selected terminal state lies inside `L-100100`.
3. Audit finite backward induction.
4. Treat `DCE100100` as open unless the actual source-orbit ratio inequality is
   proved.  A universal positive-function Harnack bound is not a substitute.
5. Recheck PR #676's fixed cubic Mellin multiplier and Landau conclusion.

## Route B

1. Multiply every local Euler factor by the order-`k` geometric polynomial.
2. Recompute `Sigma_(k,Z)(X)` and the prime-zeta contribution.
3. Verify each finite multiplier is zero-free in `Re z>0`.
4. Reconstruct the PNT asymptotic for `log A_(Z,k)(z)` with a continuous
   logarithm.
5. Use the noncompact cubic carrier; do not import the compact-SHARP full
   residue without an activation correction.
6. Treat `QPET100101/UPE100101` as open until competing poles and the
   moving-cutoff contour are uniformly bounded.

## Firewalls

```text
infinite completion -> original detector             forbidden
finite corridor -> global sign                       forbidden
supercritical carrier positivity -> critical sign    forbidden
computer replay -> DCE or QPET                        forbidden
```

Only after one of `DCE100100` or `QPET100101` passes independent
reconstruction may the corresponding RH implication be promoted.
