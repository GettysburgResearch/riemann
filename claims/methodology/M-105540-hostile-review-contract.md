# M-105540 — Hostile review contract

A valid review must:

1. multiply both displayed block matrices in `L-105540` and check the inverse
   in a nonnormal operator fixture;
2. verify the Bezout identity `W0+(I+X/2)W1=I` and the exact left inverse;
3. distinguish bank-induced partial index from the Xi meromorphic partial
   index, applying `R-105540`;
4. rederive the independent two-boundary cubic numerator from `L-105530`;
5. verify that fixed-degree multiplication preserves the pinned `o(d_T)`
   source errors and does not import an endpoint-critical mean-value theorem;
6. keep `MATRIXLERC105541`, 90%, the public record, and RH false;
7. reject any claim that a horizontal identity term alone counts residues.
