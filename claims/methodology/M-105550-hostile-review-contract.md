# M-105550 — Hostile review contract

A review must reject this packet if it:

1. counts the unreduced common factor of `F` and `F'` as a companion zero;
2. confuses distinct-root count with multiplicity count;
3. changes the sign/orientation in `N_+-N_-=R`;
4. identifies Hankel Hilbert--Schmidt norm with Hankel rank;
5. omits the Gram conditioning or `|B_+(b_j)|` floor in `L-105552`;
6. treats the polynomial/global identity as a cofinal Xi estimate without the
   regular-window endpoint and exhaustion ledger;
7. sets `CPINDEX105550`, ninety percent, the public record, or RH to proved.

Required hostile fixtures:

- `p=(x^2-1)^2`, where common factors must be cancelled;
- `p=x^2+1`, with one companion zero in each half-plane;
- `u=B_a/B_b` with `b->a`, where rank stays one and energy tends to zero;
- a malformed sign convention for the upper-half-plane count;
- a source file with any scientific flag changed to true.
