# M-105250 — Hostile review contract

Reject or demote T-105250 if any of the following occurs:

1. `P_K` is treated as a pointwise zero-free entire multiplier rather than a source-algebra coordinate change.
2. A negative coefficient appears in `P_K^2/(1-x)` after degree `K`.
3. The source congruence is used without proving that `P_K(x)` is a unit in the represented algebra.
4. The inverse cost is stated as uniform in `K`; the proved cost is `c_K^(-1) ~ sqrt(pi K)`.
5. The PNT prime-simplex energy law is treated as replayed by the light checker.
6. Safe-line half-line representation is identified with the actual Xi contour compression without the missing taper/horizontal/archimedean map.
7. Fixed `99/101` transfer is claimed to imply density one; its exact ceiling is `9401/10201`.
8. The order of limits in the density-one implication is reversed without a uniform theorem. The proved statement takes height first for each fixed `K`, then `K->infinity`.
9. The conditional 92% statement is reported as an unconditional zeta proportion theorem.
10. Density one is reported as RH.

The light replay checks finite exact algebra only.
