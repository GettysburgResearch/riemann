# M-99900 — Phase-sensitive cross-core program after the cube parity barrier

The fully active Euler cube is now solved exactly, and independent cube
matching is quantitatively insufficient. The remaining power saving must come
from interactions between distinct squarefree cores.

The strongest successor is `EPXGC99900`, combining on one ledger:

```text
native labelled first-owner Littlewood–Paley identity;
phase-adaptive logarithmic-owner square;
direct one-label Hasse transport;
zero-free factor-67 logarithmic box;
three decorated 67 fibres;
physical same-product collapse only after conditional cancellation.
```

Two concrete attack routes are retained.

## Route A — Hardy/Poisson owner packing

Use the exact Cauchy–Poisson tail-square representation from the canonical
scalar spine. Prove a Carleson estimate for truncated native tails after
decomposing them by first owner. The required saving must occur in the
off-diagonal near-collision Gram; the diagonal and far-product terms are
already controlled.

## Route B — Node-split phase flow

Build a vertex-split prime-exchange network whose edge capacities are weighted
by phase-owner dissipation

\[
|1-p^{i\gamma}|^2
\]

and the box-potential drop. Prove that every min-cut either crosses enough
phase energy or compresses to a ratio-67 collar window. Treat the latter by
the exact native half-order window criterion.

Both routes preserve source coefficients and avoid the source-blind parity
barrier of `R-99900`.
