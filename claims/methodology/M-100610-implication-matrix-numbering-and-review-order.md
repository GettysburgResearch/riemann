# M-100610 — Implication-matrix numbering, supersession, and hostile review order

This note is normative for the `100600` implication-matrix packet.

## Canonical identifiers

```text
L-100610  exact double-owner / interval tensor identity
L-100611  two-parameter owner energy identity
L-100612  root, singleton, and empty-interior positivity
L-100613  ratio-eight interval positivity
L-100614  explicit positive carrier and collar localization
L-100615  fixed power-width interval positivity
L-100616  corrected joint min-max hazard and convex AND port

R-100616  unbalanced one-sided marginal energy diverges

T-100610  first matrix formulation; read through R-100616
T-100611  region/hypermatrix reduction
T-100612  canonical activation-endpoint/interior-Turan AND gate
```

## Supersession rule

The `FOCR100610/LOCR100610` formulation in `T-100610` deleted one outside
survival from each marginal.  `R-100616` proves that its row energy diverges.
It is therefore **superseded**, not an open theorem.

The normative interfaces are:

1. the joint min--max coefficient `pi_ij=r_i r_j L_i R_j` of `L-100616`;
2. the concrete witness pair `AEP100612` and `DNT100612` of `T-100612`;
3. any future left/right certificate pair must be coupled occurrence by
   occurrence before Cauchy--Schwarz is applied.

## Hostile review order

1. Reconstruct the direct double-owner coefficient identity.
2. Reconstruct the joint hazard identity and verify that all weights sum to
   one.
3. Verify root and singleton positivity for the centered cubic carrier.
4. Verify the explicit carrier subtraction and each proved interval region.
5. Reconstruct PR #679's cell derivative, activation jump, and unique interior
   minimum.
6. Verify the exact equivalence
   `AEP100612 AND DNT100612 <=> E_2>=0`.
7. Recheck the fixed Mellin multiplier and positive-real Landau interface.
8. Only then review any proposed arithmetic proof of AEP or DNT.

## Publication boundary

No `100600` theorem may be summarized as a proof of RH unless both long-collar
witness classes have independent proof objects and the fixed detector has been
reconstructed on their exact normalization.