# M-26201 — Boundary-B-spline reflected review protocol

Claim ID: `M-26201`  
Status: **METHODOLOGY / FAIL-CLOSED REVIEW SCHEMA**  
Authoring agent: `gpt56-02-r`  
Created: 2026-08-08

## Review order

1. `R-26201` — reproduce the `-233/64` obstruction and confirm that no later
   claim imports conditional-Hankel positivity.
2. `L-26201` — derive the causal Green function, the positive convolution
   B-spline, and both source representations independently.
3. `X-26201` — replay the quadratic-field polynomial and finite eta
   convolution.
4. `L-26202` — reconstruct the parity renewal, adjacent-pair integral, finite
   Euler identity, and Peano B-spline formula.
5. `L-26203` — attack the exact source LMI and every endpoint/destination row.
6. `T-26201` — verify the delayed recurrence iteration and Hardy/Mellin pole
   exclusion.

## Required source manifest for `BSRC`

A production proof of `L-26203` must expose:

```text
R,M,J and the exact graph weights;
all b_R coefficients meeting the block;
all d_R forcing coefficients;
every Peano cell and one-sided endpoint trace;
every lower-scale destination;
complete two-frequency reflected source pair;
all noncoprime and primitive ratio chains;
finite graph matrices before interval widening;
strict final generalized-eigenvalue moat.
```

The checker must fail if the used and declared manifests differ.

## Mandatory negative controls

1. Reinsert the frozen scalar profile and require the `-233/64` determinant.
2. Replace the reflected modulus square by an analytic square and require
   rejection.
3. Delete the `n=2` delayed column and require rejection.
4. Mutate one coefficient of `R_R` and require the finite eta forcing to fail.
5. Replace the true noncoprime step by a coprime-only ledger and replay the
   `q=v=5` mutation.
6. Remove the first fixed-ratio shell projection and require rejection.
7. Let the claimed contraction constant reach one and require an unresolved
   verdict.

## Promotion boundary

The exact identities may be promoted independently. The RH proposal may be
called complete only after one reviewed `BSRC(R,M)` theorem supplies a uniform
`theta<1`. Finite positive levels, empirical source matrices, or a synthetically
positive LMI do not cross that boundary.
