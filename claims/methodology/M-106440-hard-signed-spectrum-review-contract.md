# M-106440 — Hostile review contract for the corrected hard-band frontier

Reject any proof or publication statement that does one of the following.

1. Replaces the actual quotient coefficient `widehat U_T(-xi)` by the Fourier
   density of `N_T-D_T` without proving the meromorphic quotient transfer.
2. Uses the four-channel constant `<1/600` as a bound for
   `||H_(U_T)P_T||_HS^2` merely because the analytic source densities obey a
   pointwise ratio.
3. Uses a denominator-multiplied analytic source as a nonzero vector in
   `(ker H_U)^perp`.
4. Charges pole and zero complement traces separately after invoking the
   signed all-pass index, unless the resulting stronger loss is declared.
5. Drops residue weights, pole multiplicities, confluent derivatives,
   horizontal endpoints, common factors or finite-window seams from the
   residue/exponential Gram.
6. Identifies a hard-band Paley--Wiener projection with an arbitrary source
   frame without an explicit unitary/isometric feature map.
7. Reports `HARDSIGNED106440`, the actual visible `1/600` row, ninety percent,
   density one or RH as proved.

A valid continuation must estimate the literal expressions in
`L-106432/L-106433`, or prove a source-to-quotient physical-transfer theorem
whose conclusion is exactly those expressions.
