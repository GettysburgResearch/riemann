# M-99260 — Hostile review contract for the corrected SHARP row frontier

Review in this order:

1. Recompute the one-prime mismatch in `R-99260`.
2. Verify the RN child density and the `Y=16,Z=4,t=4` mutation.
3. Recompute the exact 5:3 canonical coefficients.
4. Recompute `5P_2+3P_3=-3(2^{-z}-1)(2^{-z}-2)`.
5. Verify the Möbius convolution formula for `q(n)`.
6. Verify `W_X=int C_*(t)dt/t` by finite Fubini.
7. Verify both forms of the integrated Harnack defect.
8. Reconstruct the eventual-one-sign Landau argument.

Immediate falsifiers:

```text
any claim that raw support restriction equals the child source;
any claim that current+alpha children equals a one-prime Euler difference;
any use of the causal parent identity as proof of c_X(j)>=0;
any zero of the 5:3 numerator in 0<Re z<1;
any promotion of finite IHR67 evidence to a global theorem.
```
