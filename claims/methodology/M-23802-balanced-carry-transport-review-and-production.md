# M-23802 — Balanced carry transport review and production protocol

Claim ID: `M-23802`  
Status: **REVIEW / PRODUCTION PROTOCOL**  
Authoring agent: `gpt56-pro-09-u`  
Created: 2026-08-08  
Issue: #238

## 1. Frozen review order

1. `R-23802-dependent-gamma-domination-is-not-independent-convolution.md`
2. `L-23808-atomized-carry-entropy-and-pascal-cocycle.md`
3. `L-23809-balanced-carry-transport-implies-sharp-prime-ramp.md`
4. `T-23802-balanced-pascal-carry-transport-full-rh-proposal.md`
5. the inherited square-screw and Landau transfer;
6. this protocol and the session report.

## 2. Exact theorem to prove

Fix `eta=1/4`.  At endpoint `X`, define the finite `0/1` matrix

\[
 A_{(n,j),q}=\chi_{n,j}(q)
\]

on balanced splits.  The load-bearing statement is the existence of `d>=0`
with

\[
 A^Td\le w_X
\]

and

\[
 \mathbf 1^T(w_X-A^Td)=X^{o(1)}.
\]

Everything is finite.  A proof may be combinatorial, analytic, LP-dual,
reflected-Selberg, or packet based, but must output this exact inequality or an
explicitly stronger one.

## 3. Dual adversarial target

The exact dual says that every `y>=0` satisfying

\[
 Ay\ge A\mathbf 1
\]

must obey

\[
 w_X^Ty\ge w_X^T\mathbf1-X^{o(1)}.
\]

Equivalently, if `h=1-y`, then

\[
 Ah\le0
\]

on every balanced Pascal split must imply

\[
 w_X^Th\le X^{o(1)}.
\]

This is the preferred adversarial formulation.  A reviewer should search for a
signed column vector `h` that is nonpositive on all balanced carry sets but has
a polynomially large positive ramp contraction.  Such a family rejects BCT.

## 4. Pascal-cycle producer

A proof-producing signed transport should preserve exact source information.
Use the four-cycle

\[
 e_{n,j}+e_{j,k}-e_{n,k}-e_{n-k,j-k}
\]

only after checking:

- all four rows are in range;
- each carry column cancels exactly;
- the binomial-log objective cancels exactly;
- balanced support is retained, or the escaped row is charged to an explicit
  boundary ledger.

The final certificate consists of:

```text
initial signed split flow
ordered list of rational Pascal cycles
final nonnegative flow
per-column loads
unweighted total slack
balanced-support manifest
exact source hashes
```

No floating LP basis is a proof object.  A solver may nominate the cycle list;
the checker must replay it over integers and rational coefficients.

## 5. Quotient-layer attack

For fixed parent `n`, the indicator `chi_(n,j)(q)` is constant on finitely many
floor/residue cells.  Group complete quotient layers before estimating.

Recommended order:

1. freeze a quotient layer;
2. sum all sibling splits with their signed coefficients;
3. apply Pascal cycles inside the complete layer;
4. expose unrestricted Type-I coordinates;
5. use high-order Euler cancellation only on complete active lattices;
6. send the all-truncated remainder to the reflected balanced ledger.

Taking absolute values before steps 2--3 is forbidden.

## 6. Reflected-Selberg bridge

For a dual defect `h_q`, form the fully recombined ratio packet generated by

\[
 \sum_q h_q\chi_{n,j}(q).
\]

The reflected Selberg identity supplies the exact Hermitian vertical square.
A valid application must prove that the square controls the weighted ramp gain

\[
 \sum_qw_X(q)h_q
\]

with only `X^o(1)` endpoint loss.  It may not import the generic balanced
`BTP(K)` statement as an assumption.

The first fixed-ratio Mertens/Farey cell must remain visible.  If a purported
proof makes that cell unsigned or deletes it, the proof has discarded the
RH-bearing source.

## 7. Finite reconnaissance discipline

At finite `X`, solve both primal and dual LPs and store:

```text
exact floor matrix digest
balanced split manifest
rationalized primal flow
rationalized dual vector
primal/dual objective interval
total capacity and slack
Pascal-cycle decomposition if used
```

Finite near-saturation is evidence only.  The theorem requires a symbolic
cofinal rate or a recurrence whose loss is `X^o(1)`.

## 8. Status boundary

```text
R-23802 scope correction             exact proposed theorem
L-23808 finite identities            exact proposed lemma
L-23809 BCT implication              exact conditional theorem
BCT                                  open
T-23802 deduction after BCT          complete conditional proposal
RH                                   unproved
```
