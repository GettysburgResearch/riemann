# M-97700 — Closure protocol for the explicit future-prime hinge barrier

Status: **METHODOLOGY / FAIL-CLOSED RESEARCH PROTOCOL**  
Created: 2026-08-18

The corrected producer in `T-97700` is not another scalar absolute estimate.
It is the construction of an explicit nonnegative subsolution

\[
B\le g^+ + R^2B
\]

for the exact completed-parity Lorenz hinge deficit.

The following protocol is binding for the next closure attempt.

## 1. Work at exact hinge breakpoints

For each finite endpoint/state:

1. fully expand the source owner ledger;
2. retain `(capacity,target,scalar,parity,first owner)` for every atom;
3. compute the exact ratios `theta=r/t`;
4. evaluate `D^+`, `D^-`, `d^+`, `d^-`, and `g^+=d^++Rd^-` at every
   breakpoint and on both affine tails;
5. retain an optimizing `lambda` whenever a deficit is negative.

No sampled `lambda` grid may be promoted to a theorem.

## 2. Search barriers in source coordinates

Candidate barriers should be built from quantities which update Markovly after
two rough primes, for example:

- endpoint and least unused rough prime;
- future reciprocal-prime profile;
- future square-root-prime profile only when it appears with a proved local
  margin;
- annular activation cell;
- target mass and scalar mass;
- one or two hinge moments relative to the active breakpoint;
- safe one-prime margin from `L-97700`;
- exact Q4/annular band labels if a Type-II bridge is invoked.

A candidate using the unknown completed value `D^+` as an input is rejected as
circular.

## 3. Split the Bellman error before absolute values

For a candidate `B`, define the exact error

\[
E_v(\lambda)
=g_v^+(\lambda)+(R^2B)_v(\lambda)-B_v(\lambda).
\]

The desired theorem is `E_v(lambda)>=0`.

Before applying Cauchy or total variation, partition `E` source-faithfully into:

1. diagonal / same-prime pieces;
2. near-boundary activation pieces;
3. Type-I unbalanced pieces;
4. balanced separated coprime Type-II pieces.

Every piece must keep the exact sign, first owner, and hinge breakpoint.

## 4. Use the Q4 lane only through an explicit bridge

PR #580 isolates a separated coprime Type-II form `SACF`.  It may be imported
only after proving a literal identity or inequality of the form

\[
E_v(\lambda)
=E_v^{\rm safe}(\lambda)
 +\sum_{j=1}^{J}c_j\,\mathcal S_j(X),
\]

where `E_safe>=0`, `J` is fixed, and every `S_j` is a source-matched SACF form
with all kernel coefficients and activation ranges displayed.

A statement that the two problems are "morally the same" is not a bridge.

## 5. Falsification priorities

Every proposed explicit barrier must first be attacked at:

- the moving state `p_0(X)>Z_X` from `R-97700`;
- the exact `X=184` source mismatch geometry from PR #587;
- the odd-history Hall witness around `X=61841` retained by PR #584;
- fixed even-depth negative states from `R-96501`;
- hinge parameters immediately to both sides of every Lorenz breakpoint.

## 6. Success criterion

A closure packet is complete only when it contains:

```text
explicit B formula
+ exact two-prime update
+ proof E>=0 at every state and every breakpoint
+ finite small-endpoint directed closure
+ GPC67 statement-to-use map
+ reconstructed Mellin-Landau consumer
+ independent hostile replay.
```

Until then, `FPHB67`, `GPC67`, and RH remain open.
