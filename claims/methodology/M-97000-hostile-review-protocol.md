# M-97000 — Hostile reconstruction protocol

Review in this order:

1. Recompute the `5:3` base dictionary and Mellin numerator.
2. Expand the root parity-labelled source and confirm its scalar marginal.
3. Verify the one-generation causal identity coefficient by coefficient.
4. Prove first-owner uniqueness for every squarefree colour.
5. Reconstruct the finite rank induction and the exact terminal equality
   `L-97001.7`.
6. Check that no oriented child is physically observed before terminal common
   coupling.
7. Reconstruct the compact and MPFR terminal theorem at the pinned SHA.
8. Apply the common terminal coefficients to rows two and three and then to the
   positive `5:3` functional.
9. Recompute the Mellin transform and Landau hypotheses.

Immediate falsifiers include a duplicate terminal owner, a missing colour, a
changed `k^{-1/2}` coefficient, a nondecreasing unresolved rank, a terminal leaf
outside the directed domain, a common-coupling failure, or a Mellin numerator
zero in `Re z>0`.
