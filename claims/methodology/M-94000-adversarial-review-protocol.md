# M-94000 — Adversarial review protocol for T-94000

Reject `T-94000` at the first failure of any item below.

1. Reproduce the PR #503 negative witness and confirm the proof never invokes the forbidden operator.
2. Verify `p_s=A_n-s^(-1/2)B_n` on every complete endpoint cell, including one-sided activation values.
3. Verify the exact two-node compression for arbitrary nonnegative cell weights.
4. Reconstruct the anchored Target–Lorenz typed-leaf theorem at PR #508 head `4ae97dffd...`, including directed transcendental rounding and all row events.
5. Verify the anchored and outer finite cell sets are disjoint and exhaustive.
6. Verify the hybrid identity `c_X=d_(X,A)+d_(X,I)+R E_X^I` before any inequality.
7. Verify ordinary `q` and `4q` are observations of the same row before forming detail.
8. Reconstruct the retained-cell error for all `q>=2`, including `q<K` and the moving terminal anchor.
9. Verify the strict factor `(sqrt(K)+23)/(sqrt(K)+24)<1` and zero-capacity support cases.
10. Recompute the `<3456` thinning and `<1` signed-mismatch `Y_4` charges.
11. Reconstruct the finite dual, prime-square asymptotic, Mellin pole survival, and every hypothesis of Landau's theorem.
12. Reject any hidden use of PR #509, a rough-lift parent, full child capacity, or `J_Lambda-4sqrt(X)`.

The deterministic verifier is a schema and exact-algebra regression. It does not replace items 4, 8, or 11.
