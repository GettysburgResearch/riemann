# M-99930 — Hostile closure contract for the critical Taylor route

Any successor claiming to finish T-99930 must prove the native critical
envelope, not only another supercritical inequality.

## Frozen arithmetic object

```text
beta(n) = mu(n)-1_(67|n)mu(n/67)
S(y)    = 4(sqrt(y)-1)_+
G2(X)   = sum beta(n)n^(-1/2) S(X/n)^2
C2(X)   = 16(1-67^(-3/2))/zeta(3/2) * X - G2(X)
```

The acceptable conclusion-producing statements are:

```text
C2(X)>=0 eventually;

or

integral_1^X C2(t)_- dt/t = X^o(1).
```

## Required source discipline

1. retain the two labelled copies of `67` until coefficient projection;
2. use the literal native coefficient `(1,-2,1)`;
3. preserve real activation sides and the zero `S(1)=0`;
4. never substitute the contracted alpha-child source;
5. do not infer an upper envelope from positivity of `G2`;
6. do not spend an absolutely positive completion whose Mellin transform
   introduces or cancels a positive-real carrier;
7. do not use a source-blind square-function collapse with an unbounded label
   multiplicity.

## Mandatory negative controls

A valid proof and replay must reject:

- a nonnegative primitive with a negative final window;
- mutation of one `67` label;
- replacing `beta` by `|beta|`;
- reversing the Taylor-remainder scaling inequality;
- deleting the compact correction in the Mellin transform;
- setting `critical_envelope_sign=true` from finite endpoint data.

## Useful equivalent coordinates

The following are legitimate exact representations of the same boundary:

```text
critical Taylor envelope C2;
last pole-lowering B-spline of the linear activation-zero scalar G1;
ratio-67 Möbius collar window of PR #664;
subpower negative mass of the zero-safe SHARP box.
```

Moving between them is allowed only through an explicit identity. Naming one of
them as a new theorem does not close the route.

RH remains unproved.