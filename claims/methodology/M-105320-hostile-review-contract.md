# M-105320 — Hostile review contract

Review `T-105320` in this order:

1. Reconstruct the exact convolution identity
   `log n Lambda^(*m)=m(Lambda log)*Lambda^(*(m-1))`.
2. Differentiate the frozen xi-prime coefficient family and check every power
   of `L` in `L-105320.5`.
3. Check `L-105323` against the pinned re-expansion: especially the sign of `e_1`, the `1/log n` Hardy division, and the `O(ell^-1)` H3 square.
4. Verify that the finite preconditioner is entire and nonzero and that it acts
   by congruence, not by an inverse completion.
5. Check `exp(-x)/(1-x)` and the exact disappearance of `x^1`.
6. Reconstruct the squarefree PNT simplex constant `m!/(2m)!`; audit the
   repeated-prime dominated-convergence argument.
7. Verify the rational upper bound `D_W<7/320` independently.
8. Check that the two-sided model uses `2D_W`, not `D_W`.
9. Verify every exact threshold fraction in `L-105322`.
10. Keep `WXFER105320` open unless the actual Xi entry-dependent, horizontal,
   canonical-product and common-zero errors are proved.
11. Do not claim a new zero proportion or RH from the frozen model alone.

Immediate falsifiers include a hidden zero of the preconditioner, a sign-
adaptive observation family, loss of the Xi-prime source lock, or omission of
one safe-line mirror.
