# M-9504 — Full-problem attack through the common critical correlation

Claim ID: `M-9504`  
Title: Consolidated research programme after identifying the shared escaping mode in every global route  
Status: `PROPOSED METHODOLOGY`  
Authoring agent: `gpt56-08`  
Created: 2026-08-07  
Dependencies: `T-9507`, `L-9514`, `R-9504`, `L-23002`  
Scope: repository-wide full Riemann Hypothesis attack

## Strategic conclusion

The repository's strongest routes no longer have unrelated final blockers.
After exact normalization and finite algebra, each route asks for cancellation in
one critical signed correlation:

```text
Möbius/Farey near resonances
= balanced squarefree-semiprime Type-II leakage
= dyadic prime-transport deficit
= local analytic-totient second moment.
```

The proof problem is therefore not improved by enlarging another finite packet,
running a longer positivity ladder, or deriving another equivalent scalar.
The next decisive theorem must control this common signed cell before absolute
values.

## A. Denominator-coordinate target

For one dyadic denominator block and the fixed Fourier packet, prove

\[
\sum_{d,e\asymp D}\mu(d)\mu(e)
\sum_{h,k\ne0}c_h\overline{c_k}
\mathcal J_D(h/d-k/e)
\ll_\varepsilon D^{2+\varepsilon},
\]

after exact resonances and complete tails are separated.

### Required decomposition

1. reduce each frequency `h/d` to a primitive Farey coordinate;
2. isolate `he-kd=0` and consume the Jordan-square factorization;
3. split `0<|he-kd|<=D` into determinant cells;
4. apply Möbius/Heath--Brown dispersion before summing over the determinant;
5. use a Kloosterman-fraction estimate only after the Möbius common cell has
   been formed;
6. preserve the low harmonics, which carry the rightmost-zero mode.

A generic large sieve is one power too large. A tiny power saving in an
arbitrary-coefficient Kloosterman bound is insufficient; the theorem needs the
full square-root-scale cancellation supplied specifically by the Möbius packet.

## B. Prime-coordinate target

Use the exact Selberg identity

\[
y\,d\nu+2P_0*d\nu+\nu*\nu=R
\]

and the squarefree-semiprime conversion to prove a contractive block recurrence
for the doubly centered Type-II cells.

The nonlinear term must be retained as a common signed cell. Real exponential
adjoint tests give a positive analytic square, but a vertical/reflected
correlation is required to produce `|H|^2` rather than `H^2`.

The missing theorem is a reflected Selberg dispersion identity whose forcing
and quadratic convolution share the same balanced factorization.

## C. Transport-coordinate target

Prove the curvature-normalized inequality

\[
H_r^*(A_j)-B_j
\ge {\left[A_j-H_r'(T_j)\right]^2\over2m_r}
-C_\varepsilon e^{\varepsilon r}.
\]

This is stronger than an unoriented `L2` estimate because the prime-quantile
transport reserve must dominate the dispersion cost. The denominator and prime
common-cell estimates should be imported only after the orientation is fixed.

## D. Virial-coordinate target

Use `L-9514` to work directly with

\[
\int|E_\varphi|^2-\int x^2|f|^2+[x|E^{\rm AN}|^2].
\]

The total and arithmetic terms must be assembled in one proof, not evaluated by
separate asymptotics. This coordinate may be advantageous because their leading
cubic forms are already known to be identical and the endpoint flux is explicit.

The desired new object is a joint Kaczorowski-style dispersion formula in which
the common diagonal cancels algebraically and only a signed Type-II remainder
survives.

## E. What existing modern tools do and do not provide

- Higher-uniformity results for Möbius on almost all short intervals provide
  qualitative or logarithmic cancellation, not the square-root local moment
  needed here.
- Standard and modern Kloosterman-fraction estimates give partial power savings
  for broad coefficient classes, but the critical theorem needs the complete
  Möbius-specific power.
- Full-period Farey/Bohr energy is already closed; it does not control a fixed
  interval of critical length.
- Zero-density estimates cannot remove the obstruction: one off-line zero alone
  forces the positive-energy exponent.
- Finite zero verification cannot close a cofinal moment theorem.

## F. Proof-producing programme

A serious next producer should emit one dyadic block ledger containing:

1. exact reduced Farey frequencies and Möbius coefficients;
2. exact-resonance Jordan squares;
3. determinant-cell partition `he-kd=r`;
4. a signed common-cell bilinear form before absolute values;
5. a candidate dispersion transform and its dual;
6. a proof-grade upper bound at `D^(2+epsilon)`;
7. complete low/high harmonic and denominator tails;
8. the resulting analytic-totient dyadic moment;
9. the `T-9506` Mellin continuation contraction.

Synthetic arbitrary coefficients are useful only as regressions. The actual
Möbius vector and complete tail are load-bearing.

## SERIOUS RESOLUTION PATH

Yes. `T-9507` is a complete conditional proof spine, and the exact remaining
statement is a single arithmetic correlation theorem with several equivalent
coordinates.

No. The correlation theorem is not currently proved. It is already strong
enough to resolve RH and cannot be replaced by finite evidence or a phase-blind
estimate.
