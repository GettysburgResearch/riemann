# M-97200 — Hostile review protocol for the RJTE completion-port packet

Review in this order:

1. Freeze PR #570 at `992404909d6c2940ae8ae849633fe5530ed79db0`.
2. Recompute the local factors of `G_diamond` and `B_diamond`.
3. Check every coefficient-one channel-swap identity and the martingale normalization.
4. Replay the 200-bit radical overshoot at `(P,N)=({3,5,7,11,13},26)`.
5. Confirm that the genuine moment matrix is PSD while the unit Schur matrix is not.
6. Reconstruct `A_(P,X)=integral M_P(y)dy/y` and the TFPE/ACBI recurrence.
7. Replay the `X=500` trace-only separator; do not misclassify it as a TFPE counterexample.
8. Check the quotient-profile prime recurrence and every directed interval operation.
9. Reject any claim that the `O-97120` metadata file itself proves the absent lemmas.
10. Reject any RH conclusion unless one of RJTE, TFPE/ACBI or FCBI is proved all-scale and the real-`X` Landau hypotheses are reconstructed.
