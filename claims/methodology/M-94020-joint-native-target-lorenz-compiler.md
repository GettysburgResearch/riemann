# M-94020 — Joint Native Target–Lorenz Compiler (`JNTLC`)

Methodology ID: `M-94020`
Status: **OPEN PRODUCER SPECIFICATION**
Created: 2026-08-16

The shortest honest continuation is to solve `L-94023` with a template family
that is demonstrably complete for the actual anchored endpoint marginal.

A valid implementation must:

1. generate the literal endpoint occurrences `a_(X;m,k)^fin`;
2. carry `x=X/m`, `d|P61`, rough history, first owner and parity;
3. generate the exact finite-forcing weights `beta_p` and child weights
   `gamma_p` rather than cloning the parent into each leaf;
4. permit finite-forcing/oriented-child cancellation inside one joint template;
5. use the same coefficient in target, score, rows and all ordinary columns;
6. form detail only after one total `q/4q` row;
7. return either a directed positive primal certificate or an exact Farkas
   separator;
8. retain `R-94020` as a mutation: a child-only nonnegative marginal must fail.

The first useful computational campaign should export sparse extreme points and
then prove stability on activation cells.  Numerical feasibility alone is not a
proof and a separator for an incomplete template list is not a refutation of
all native realizations.
