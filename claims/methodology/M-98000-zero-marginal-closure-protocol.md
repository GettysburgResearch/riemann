# M-98000 — Closure protocol after the eventual zero-marginal theorem

Methodology ID: `M-98000`  
Status: **SOURCE-FAITHFUL RESEARCH PROTOCOL**

`T-98000` proves that the completed scalar Lorenz optimization has no independent
interior-threshold obstruction at sufficiently large endpoints. Future work
should therefore not spend the main effort proving an all-`lambda` cone unless
that cone supplies a direct estimate of a zero hinge.

## Priority order

1. **Root scalar (`GPC67`).** Attack the exact native scalar through the
   largest-prime identity and the balanced large-prime Type-II form of PR #590,
   or the root-tail theorem `RBLPTE67` of PR #594.
2. **Root target (`GTC67`).** Treat it as a separate reciprocal-zeta Mellin
   criterion, not as free Hall capacity.
3. **One-switch diagnostics.** At a finite endpoint, sort no atoms. Use ordinary
   source-index order, find the unique target crossing, and emit that prefix as
   the exact Lorenz separator.
4. **Critical-saddle localization.** Use the fact that the zero-scalar shell
   pays all target beyond the positive-scalar prefix. Any remaining scalar
   obstruction is confined to indices `k<X/2`.
5. **Finite certificates.** Preserve real activation sides and directed
   transcendental primitives. A finite positive scan remains falsification only.

## Firewalls

```text
no arbitrary Lorenz sorting after L-98001;
no interior lambda treated as an independent asymptotic obstruction;
no target capacity assumed from scalar positivity;
no target shell used to pay scalar demand;
no LBP67 all-lambda cone called necessary;
no fixed positive mass aperture reintroduced after R-97900;
no balanced Type-II form replaced by absolute values;
no finite range promoted to RH.
```

## Minimal scalar estimate

The preferred conclusion-producing target remains the one-sided largest-prime
identity

\[
U_{\rm full}(X)
=U_Z(X)-
\sum_{Z<p\le X/2}{1\over p}U_{<p}(X/p),
\]

with the right side proved nonnegative by a source-specific balanced Type-II or
future-profile argument. `T-98000` shows that solving a larger all-threshold
Lorenz problem is not required for RH.