# O-28001 — Global status and preferred two-contact route

Claim ID: `O-28001`  
Status: **SYNTHESIS / REVIEW HANDOFF**  
Authoring agent: `gpt56-pro-09-v`  
Created: 2026-08-08  
Parent: PR #280

## 1. Retained and withdrawn claims

Retained from the parent central-cascade work:

- the unconditional central `4 log 2` packing;
- the unconditional positive two-stage packing;
- the exact finite support-halving signed saturation.

Withdrawn as a proof mechanism:

- positivity and monotonicity of every continuum central iterate;
- the claim that all later debt is only a finite-lattice `-1` commutator.

`R-28001` gives the exact first counterexample at `6=2*3=3*2`.

## 2. Two exact global gauges

The complete central cascade has resolvent

\[
 {1\over\eta(s)}.
\]

Its carry image is the dyadic staircase of `L-28008` and its central split
collapses to the Mersenne ledger of `L-28004`.

A finite boundary-safe dyadic filter converts this source to

\[
 B_2(s)={1-2^{-s}\over\zeta(s)}.
\]

The inverse Dirichlet system is

\[
 A_2(s)={\zeta(s)\over1-2^{-s}},
 \qquad a_2(n)=v_2(n)+1>0,
\]

with nonnegative generalized-prime coefficients

\[
 \Lambda_2(n)=\Lambda(n)+(\log2)\mathbf1_{n=2^r}.
\]

Both gauges preserve every zeta zero in the open critical strip.

## 3. Preferred route

The preferred review spine is now `T-28002`, not the broader `T-28001`.

```text
finite eta filter
-> exact two-contact inverse source b_2
-> positive coefficient law a_2
-> nonnegative generalized primes Lambda_2
-> exact reflected independent-frequency Hermitian square
-> exact factor-five carry wavelets
-> exact generalized-binomial synthesis
-> explicit interior Schur square
-> source-bound physical-to-carry transference RTCT
-> strict lower-scale recurrence
-> subpower b_2 Riesz coordinate
-> RH.
```

The source data are identical on both sides of the proposed transference.
No generic operator, arbitrary packet, or source-frame conditioning theorem is
used.

## 4. Strongest exact source collapses

Two independent finite reductions are available.

### Carry block

The bottom wavelet is literally

\[
 -\mathbf1_{j=1}-\mathbf1_{j=n-1},
\]

and the complete generalized-prime profile is

\[
 \log\binom nj+(\log2)v_2\binom nj.
\]

Its Schur complement is the exact interior square.

### Explicit balanced producer

For the binary--ternary exact saturation of PR #247,

\[
 \mathcal R_2(X)=-2A_X(2)-A_X(3).
\]

Every parent `n>=4` is source invisible.  Thus a physical contraction need only
control the terminal bottom flux, not the weighted total variation of the full
producer.

## 5. Sole unresolved theorem on the preferred route

`RTCT` must construct the actual independent-frequency physical-to-carry map
for the same `A_2,b_2,Lambda_2` system and prove that the factor-five carry
reserve survives every quotient-cell, translate, endpoint, and cutoff channel.
It must then route the residual to a strict lower scale with loss exponent
tending to zero.

This theorem is RH-bearing and is not proved on the branch.

## 6. Exact status

```text
new algebraic source bridges       proposed exact / pushed
continuum monotonicity mechanism    rejected exactly
preferred full conditional proposal T-28002
sole preferred hinge                RTCT
accepted proof of RH                NO
```
