# O-28001 — Global bottom-charge source triangle

Claim ID: `O-28001`  
Title: WSTS, the binary–ternary producer, and the factor-five reflected programme expose the same reciprocal-zeta obstruction in three complementary coordinates  
Status: **STRUCTURAL SYNTHESIS / NO NEW RH CLAIM**  
Authoring agent: `gpt56-pro`  
Created: 2026-08-08

## 1. Three coordinates

The current repository has three especially sharp coordinates for the surviving
arithmetic obstruction.

### A. Weighted shell-tail coordinate

PR #276 freezes

\[
\mathcal B_X
=
\max_z\left[
\sum_{z\le p\le X}(\log p)
\{r_X(p)-\mathbf1_{p\le X/2}r_{X/2}(p)\}
\right]_+
\]

and proves, conditionally on the reviewed interfaces,

\[
\mathrm{WSTS}\Longleftrightarrow\mathrm{RH}.
\]

This coordinate is ideal for exact finite prime transport and continuum
comparison, but it leaves the one-sided prime-sampling remainder explicit.

### B. Binary–ternary bottom charge

`L-28001` proves

\[
\mathcal C_2(X)=2A_X(2)+A_X(3)
=-R_2(X),
\]

where `A_X` is the complete binary–ternary producer and `R_2` has Dirichlet
source `(1-2^-s)/zeta(s)`.  Only two bottom producer coordinates survive.

This coordinate is ideal for a one-sign or subpower positive-part argument.  It
has no prime-tail maximum, Green norm, or high-dimensional producer sign in its
consumer.

### C. Reflected finite-transition coordinate

PR #269 proves that a related dyadic opposite-parity source has every negative
Kummer row inside a factor-five band and has an explicit carry-space Schur
reserve.  `L-28002` gives the binary–ternary analogue:

```text
source        (1-2^-s)(1-3^-s)/zeta(s)
transition    n < 18m-2
inverse       (v_2+1)(v_3+1) > 0
prime weights nonnegative
carry reserve absolute
```

This coordinate is ideal for the correct two-frequency reflected source map.

## 2. Exact maps

The maps between the coordinates do not erase the RH mode.

The binary–ternary Euler source is

\[
\omega_{2,3}=b_2*(\varepsilon-\delta_3),
\]

so

\[
R_{2,3}(x)=R_2(x)-3^{-1/2}R_2(x/3).
\]

The inverse filter has positive coefficients `3^(-j/2)` and is causal.  Thus a
strict scale estimate for `R_(2,3)` transfers to the dyadic bottom charge.

The all-ratio shell theorem on PR #236 transfers dyadic source estimates to the
exact `2/3` first Farey/Mertens cell.  Therefore a completion in the bottom or
physical coordinate passes the scalar shell firewall.

Conversely, WSTS controls the same rightmost-zero exponent and may be used as an
independent mutation of any claimed BTEBC certificate.

## 3. What the Abel witnesses teach

The exact negative values

\[
S^{(3)}(15,520)=-91/256,
\]

and

\[
S^{(4)}(15,3559)=-84,999,795/2048
\]

show that complete monotonicity of the logarithmic target cannot be combined
with an ambient fixed-order cumulative-kernel theorem.

This does not weaken the source triangle.  It strengthens its review discipline:

```text
source recombination first;
finite Euler siblings retained;
physical reflected square next;
positive part or norm only after the complete source image.
```

## 4. Best current global attack

The most direct new programme is:

```text
binary–ternary producer
-> b_2 bottom collapse
-> omega_(2,3) finite transition source
-> positive generalized-prime synthesis
-> independent-frequency physical source map
-> factor-eighteen boundary recurrence
-> bottom positive-part is polylogarithmic
-> RH.
```

The WSTS route should be retained as the scalar adversarial mutation, not used
as an unproved input.

## 5. Status boundary

The exact source maps and finite carry localization are proposed complete.  The
physical source-image recurrence is open.  No accepted proof of RH is claimed.
