# O-94200 — Endpoint packing and cubic dispersion are distinct frontiers

Claim ID: `O-94200`
Status: **ROUTE DISPOSITION / EXACT INTERFACE MAP**
Created: 2026-08-16

The research reset separates two finite objects that had been conflated.

## Physical frontier

The endpoint-detail compiler chooses a nonnegative row \(d\) and pays

\[
\mathfrak W_X(d)
=
P_\Lambda(X)-\mathcal H(d)
=
\langle Y_4,\Omega_X-\Xi_d\rangle
\ge0.
\]

This is a finite positive-cone optimization. Its coefficients may be certified
without estimating a signed Möbius or prime sum.

## Arithmetic frontier

The complete gap

\[
F_\Lambda(X)=J_\Lambda(X)-P_\Lambda(X)
\]

contains the RH-sensitive arithmetic. It is independent of the choice of
physical packing.

PR #531 supplies a different exact arithmetic entry point. For its double-zero
kernel \(W\),

\[
\mathcal L_W(X)
=
\sum_{\sqrt X<d\le X/2}\mu(d)F_W(X/d)+O(\log X),
\]

with every small divisor already paid. The surviving large-divisor Möbius tail
is a genuine arithmetic cancellation problem. It is not a native detail slack,
a child budget, or a port reserve.

## Route decision

The strongest honest continuation is therefore:

```text
retain endpoint compiler as finite feasibility infrastructure;
discard NEDB as a conclusion-producing bridge;
retain exact F_Lambda + physical-slack decomposition;
attack an arithmetic producer directly;
use PR #531's large-divisor tail / positive Peano potential as one live option;
keep First-Hermite and Brownian lanes independent rather than importing their
open pointwise gates as lemmas.
```

No equivalence between \(F_\Lambda\) and the PR #531 cubic tail is asserted
without an explicit transform. They are two arithmetic producer coordinates
with the same scientific burden: uncancelled off-line-zero information.
