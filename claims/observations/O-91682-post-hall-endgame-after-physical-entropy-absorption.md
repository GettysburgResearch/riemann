# O-91682 — Post-Hall endgame after physical-entropy debt absorption

Claim ID: `O-91682`  
Status: **CURRENT EXACT BOUNDARY / CROSS-BRANCH SYNTHESIS**  
Created: 2026-08-14  
Parent: PR `#462` at `8642e062b6c6f5a4c7d443a1ee5a6e9ecf3e4706`  
RH status: **unproved**

## 1. Review correction retained

PR `#463` confirms the exact stopped-leaf Hall obstruction from PR `#456`. The universal branchwise Hall implication is false at

```text
p=67, y=13, py=871, t=13,
```

and on an infinite tail. No theorem on this branch uses separate survival or hazard Hallization.

The complete canonical `P_61` row is also not a current-only bypass: its excess ordinary/detail response is the rough-child reservoir. Counting the complete row as current while exporting the child spends that reservoir twice.

## 2. What PR #462 contributes

`L-91680` gives the source-faithful target-proportional ray

\[
 \nu=\left(1-\frac{O_T}{E_T}\right)E.
\]

It preserves the signed target exactly and reduces each physical coordinate to

\[
 O_TE_\alpha-E_TO_\alpha\ge0.
\]

`L-91681/X-91681` close the divisor-ordered causal row-per-score profile for every `p>=500000`. The remaining profile corridor is finite.

## 3. New cross-branch closure

`L-91682` combines the target-proportional decomposition with the corrected direct-Euler score interface.

Once the row determinants hold, the residual source and nonnegative row bonuses assemble to the complete signed causal row. Applying the literal entropy functional gives exactly the signed arithmetic-row entropy, not the auxiliary declared score of the residual source.

For every

\[
 p\ge67,
 \qquad1\le y<67,
\]

`L-91682` proves

\[
 \operatorname{Ent}(C_{p,y})
 -\mathfrak S_{p,y}>\frac12.
\]

Therefore the target-proportional declared-score debt

\[
 \frac{[O_TE_S-E_TO_S]_+}{E_T}
\]

is absorbed at physical row assembly. It is not an all-generation debt and must not be charged a second time.

This removes Gate C of `O-91680` **after** the complete row determinants are proved. It does not make a source template score-superordinate before row assembly and does not relax the one-use root ledger.

## 4. Reduced exact frontier

The conclusion-producing factor-67 route is now reduced to three gates.

### Gate A — finite causal profile corridor

Certify the divisor order for

\[
 67\le p<500000,
 \qquad1\le y<67,
 \qquad d\mid P_{61},
 \qquad2\le j\le66.
\]

### Gate B — full physical determinants

For every stopped leaf and every literal row, ordinary response, radix-four detail response, and shared-port coordinate, prove

\[
 O_TE_\alpha-E_TO_\alpha\ge0.
\]

The score-Lorenz alternative instead uses its score-normalized full row determinant after Gate A.

### Gate D — one-use root equality realization

Identify the positive stopped-leaf output with the same finite root packet consumed by the equality front door. Current row, recursive child, collar, mismatch, omission, annulus, and shared port must each have exactly one owner.

No independent aggregate-score-debt theorem remains between B and D.

## 5. Current status

```text
branchwise stopped-leaf Hall                    FALSE / WITHDRAWN
canonical P_61 current-only bypass              FALSE / CHILD OVERDRAW
target-proportional source producer             EXACT / L-91680
causal profile for p>=500000                    EXACT / L-91681
P_61 literal entropy over signed score          EXACT / L-91682
aggregate target-proportional score debt        ABSORBED AFTER ROW ASSEMBLY
finite causal profile corridor                  OPEN / DIRECTED FINITE
full physical determinants                      OPEN / ARITHMETIC
one-use root equality realization               OPEN
Riemann Hypothesis                              UNPROVED
```
