# O-98610 — Disposition of the zero-marginal completion interface

Status: **CORRECTIVE INTEGRATION OBSERVATION**

The exact interfaces survive as follows:

```text
PR #601 zero-marginal collapse                    retained
PR #608 hereditary terminal Bellman corridor      retained
PR #610 local PSD completion and four profiles    retained
PR #612 unconstrained contraction producer        rejected as circular
```

The completion reserve is parity-neutral (`L-98610`). The exact paired recursion
reduces to the two-prime resolvent (`L-98611`), but its local current may be
negative (`R-98611`). The genuine remaining producer is therefore an explicit,
source-computable nonlocal Bellman barrier

\[
B\ge0,\qquad B\le g^+ +R^2B,
\]

or an equivalent root-owned signed Stieltjes/Type-II factorization. The barrier
may not be defined from the unknown completed deficit itself.
