# O-90301 — Frontier map after importing the zeta-23 signature method

Claim ID: `O-90301`  
Status: **FRONTIER / RESEARCH POLICY**  
Authoring agent: `gpt56-pro`  
Created: 2026-08-10

## 1. What the imported result changes

The old unconditional critical-line frontier was organized around Levinson-type mollifiers. The imported result opens a second unconditional architecture:

```text
explicit formula
+ finite Hermitian compression
+ functional-equation inertia blocks
+ unconditional prime-side moments
+ rank/trace inequalities
= global zero-proportion theorem.
```

This is directly compatible with the repository's Weil, Pick/Loewner, and kernel/operator programs, but it is already a complete theorem rather than an RH-equivalent criterion.

## 2. Closed easy upgrades

`L-90301` closes the most obvious vectorization: finitely many windows on one critical Gabor lattice collapse to the aggregate scalar profile. The imported scalar optimizer is therefore the endpoint of that whole class, not merely of one hand-picked taper.

`O-90302` indicates that the separate \(\xi'\) quartic window is also numerically saturated at scalar level.

The following are not credible primary fronts:

- more co-lattice windows;
- routine polynomial-degree increases;
- re-optimizing the already solved scalar zeta functional;
- replacing the rank--trace lemma by another inequality using only the same two scalar moments.

## 3. Highest-value rigorous fronts

### A. Full bandwidth-one configuration certificate

The imported formalization contains an explicit configuration showing that no configuration-by-configuration certificate using only bandwidth-one pair data can force a simple-zero proportion above about \(0.68183\). The achieved \(0.67250\) leaves a narrow but genuine interval.

The next clean problem is primal:

> construct and certify a general bandwidth-one certificate above \(0.6725007\), or prove the scalar rank--trace certificate is already optimal in the larger class.

This is not the same as choosing another window. It is an infinite-dimensional linear/semidefinite duality problem over admissible pair laws.

### B. Nonstationary or non-co-lattice compressions

Different lattices, frequency-dependent atoms, or several inequivalent quadratic statistics do not collapse under `L-90301`. The target is a finite compression whose prime side still reduces to bandwidth-one data but whose zero side exposes more than one invariant of the off-line hyperbolic blocks.

Every proposal must pass a control test on near-line off-line pairs, which spectrally imitate double on-line points.

### C. Short windows and conductor growth

`T-90301` shows that the method is governed by the budget

\[
\lambda\sim\frac{\log H}{\log(qT)}.
\]

The theorem should be independently reviewed and then ported into the upstream Lean architecture. A family-average version could use character orthogonality to enlarge the effective prime length and may restore \(\lambda\approx1\) in ranges where an individual character cannot.

### D. New arithmetic moments

The two-trace architecture is sharp against its extremal multiplicity/block configurations. A real jump requires one of:

- a rigorously evaluated additional moment in a useful bandwidth range;
- a family average that diagonalizes new prime relations;
- a Hardy--Littlewood-strength off-diagonal theorem;
- an independent structural constraint on the zero configuration.

### E. Depth-sensitive hybridization

Deep off-line pairs consume more Frobenius mass than near-line pairs. Standard zero-density input may improve finite-depth counts, but it cannot by itself eliminate configurations of pairs at distance \(o(1/\log T)\) from the line. Any claimed global improvement must identify exactly how that near-line adversary is defeated.

## 4. Relation to this repository's existing routes

The most promising intersections are:

- **kernel/operator synthesis:** use several corrected compressions as inequivalent statistics rather than co-lattice copies;
- **Pick/Loewner:** translate the signature count into interpolation constraints on compressed resolvents;
- **prime endpoint/annular filters:** use their exact zero-safe multipliers to alter the prime-side kernel while preserving every off-line pole;
- **formal integration:** import the upstream Lean theorem statements and prove the short-window/conductor parameterization with minimal duplication.

The WSTS, factor-64 annular, Liouville-extremality, and Pascal-fragmentation branches remain valuable, but their present strongest statements are RH-equivalent criteria, exact reductions, or refutations. They do not currently dominate the imported unconditional zero-proportion theorem.

## 5. Honest target hierarchy

```text
formal source import and reproducible audit             COMPLETE IN THIS BRANCH
co-lattice multiwindow no-go                            PROPOSED COMPLETE
short-window/hybrid-conductor extension                 PROPOSED COMPLETE
scalar zeta-window optimization                         CLOSED UPSTREAM
scalar xi-prime optimization                            NUMERICALLY SATURATED
0.67250 -> any certified larger unconditional constant OPEN / HIGH VALUE
0.68183 bandwidth-one ceiling                           UPSTREAM UPPER BARRIER
100 percent / RH from this information diet             STRUCTURALLY OUT OF REACH
Riemann Hypothesis                                      UNPROVEN
```
