# O-91800 — Current boundary after the radial spectral-type reset

Observation ID: `O-91800`  
Status: **CURRENT FAIL-CLOSED RESEARCH HANDOFF**  
Created: 2026-08-13  
RH status: **unproved**

## The sideways insight

Claude's finite compression treats an off-line reflected pair as one
signature-`(1,1)` block.  The present route retains the horizontal shift
parameter and observes a second invariant:

```text
the same pair is one Dirac atom at its exact horizontal depth.
```

The completed arithmetic Julia source is generated by continuous radial
births.  Therefore a positive map that is local under every radial refinement
cannot feed the atomic hyperbolic channel.

## Exact objects

```text
A_arith(dr)    diffuse positive arithmetic source OVM;
C_crit(dr)     critical output OVM;
S_st(dr)       stable output OVM;
H_hyp(dr)      pure-point crossed-zero OVM;
E_aux(dr)      positive auxiliary OVM.
```

The desired identity is

\[
 A_{\rm arith}(I)
 =C_{\rm crit}(I)+S_{\rm st}(I)+H_{\rm hyp}(I)+E_{\rm aux}(I)
\]

for every radial interval `I`.

## Exact implication

Because

\[
 0\preceq H_{\rm hyp}(I)\preceq A_{\rm arith}(I),
\]

one has

\[
 H_{\rm hyp}\ll A_{\rm arith}\ll dr.
\]

But `H_hyp` is pure point.  Hence it is zero and RH follows.

## What remains

1. Complete the gamma factor's continuous-product bookkeeping on arbitrary
   radial partitions.
2. Build the critical/stable model map before summing radial generations.
3. Prove interval naturality on a generating Borel algebra.
4. Keep coefficient one, all carriers, delays, both orientations and the
   bridge.
5. Pass to the minimal Fock/product-system morphism and invoke `L-91803`.

## Rejection tests

A proposed completion fails if it provides only:

```text
a global kernel lock;
a total entropy equality;
one fixed dyadic filtration;
a map after radial integration;
an arbitrary unitary rotation of source details.
```

## Preferred attack order

1. Refine the gamma beta source over a rational radial partition and prove an
   `O(|I|)` detail-norm bound.
2. Define source and model interval projections on the common carrier core.
3. Prove the cocycle map on adjacent rational intervals.
4. Use coassociativity to obtain all finite unions.
5. Extend by monotone convergence to the Borel algebra.
6. Apply the diffuse-versus-pure-point theorem.

## Exact boundary

```text
source diffuse radial factors             MOSTLY CLOSED
hyperbolic pure-point depth spectrum       CLOSED EXACTLY
abstract spectral-type theorem             CLOSED EXACTLY
completed interval-local map               OPEN / RH-EQUIVALENT
Riemann Hypothesis                         UNPROVED
```
