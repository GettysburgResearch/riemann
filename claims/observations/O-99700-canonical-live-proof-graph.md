# O-99700 — Canonical live proof graph after the native-coefficient audit

The post-PR #652 repository no longer supports a proof chain that promotes the
contracted alpha-child tree to the Möbius Euler source. PR #654 is correctly
marked superseded at that interface.

The strongest conclusion-facing graph is scalar:

```text
factor-67 coefficient beta
 -> exact scalar h=beta*T
 -> zero-safe reciprocal-zeta Mellin transform
 -> zero-free logarithmic smoothing OR subpower negative mass
 -> Landau
 -> RH.
```

The source-faithful arithmetic graph is:

```text
native sequential first ownership
 <-> symmetric random-order Euler homotopy
 <-> tilted positive inverse renewal
 <-> tilted prime-power owner flow
 -> TOCE67 source-root/boundary Carleson estimate
 -> subpower negative mass.
```

All Hall, Radon--Nikodym, Volterra, score, capacity, terminal-port, and
prime-square packets remain useful local mathematics and cross-checks. None is
required by the canonical scalar consumer.

The single honest research target is not another pointwise positivity theorem.
It is a critical, source-correlated boundary embedding that converts the exact
`O(log log X)` tilt-energy budget into subpower logarithmic negative mass
without using the degenerate within-chain variance and without cancelling the
zeta poles by positive Euler smoothing.
