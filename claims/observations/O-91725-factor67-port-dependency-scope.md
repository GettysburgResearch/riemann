# O-91725 — SONTR bypasses the old colored-capacity projection only through the physical causal generator

Observation ID: `O-91725`  
Status: **DEPENDENCY-SCOPE CLARIFICATION / NO RH CLAIM**  
Created: 2026-08-14

The older endpoint-port theorem `L-91320` has two logically distinct outputs:

1. an uncolored positive `2x2` root Schur port with normalized mass `<14/3`;
2. a colored affine Pascal rough-state lift whose colored-to-physical capacity
   projection remains open.

The factor-67 SONTR route may import the first output but not silently the
second.  Its escape is `L-91654`: every causal current generator is already a
complete physical row/ordinary/detail datum and every recursive child has zero
boundary/port coordinate.

`L-91725` proves that positive integration of the uncolored fiberwise port
inequalities yields one parent port, with total normalized mass `<252`.  Root
Hall bonuses are current, children have zero port, and all endpoint fibers are
summed before one global correction test.

```text
uncolored root matrix port                available
colored affine Pascal projection          open in old route
physical causal generator SONTR           does not use colored projection
one integrated common port                exact / L-91725
analytic correction estimates             still frozen review inputs
Riemann Hypothesis                         unproved
```
