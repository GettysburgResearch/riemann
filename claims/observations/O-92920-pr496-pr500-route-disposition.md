# O-92920 — Route disposition after the native/rough `q=2` audit

Status: **NON-LOAD-BEARING RESEARCH ORIENTATION**  
Snapshot: 2026-08-15

## PR #496

PR #496 correctly separates positive source ownership from signed realization
error and supplies a useful terminal-child fallback.  Its controlling recursive
identity nevertheless promotes actual children to full child capacities.  The
`q=2` audit shows why that promotion requires an explicit paired-orientation or
positive-completion theorem.  The unconditional `109/1200` claim was not in the
PR and is withdrawn.

## PR #500

PR #500 is stronger in the physical layer:

```text
Hall input marginals retained;
first owner in the coupling kernel;
actual children physically placed;
one label-blind quantizer;
signed comparison outside the source cone;
zero port;
direct root cost <60989.
```

Its missing bridge is narrower: the frozen packet does not explicitly certify
that the child orientation makes the precomparison output native rather than
the `P_61` rough lift.

## Successor

`L-92920--L-92923` add the missing paired orientation and turn `q=2` into a
normalization mutation test.  The preferred architecture is now:

```text
full paired native source;
exact P61 stopping line with rough-prime swap;
oriented Hall/causal/physical coupling;
actual children internal;
one common quantizer;
two-ledger finite comparison;
all-column native complement;
direct Y4 cost <60989;
frozen one-sided endpoint consumer.
```

The infinite recursive closure and first-generation terminal-child closure are
both unnecessary in the controlling route, though the latter remains a useful
fallback if an actual child cannot be included in the common physical target.

RH remains unproved pending independent reconstruction.
