# O-95100 — Carry terminal-control proof DAG

```text
root-completed critical target
  -> source-specific descending policy occupation
  -> exact internal divergence
  -> one terminal coordinate tau
  -> mod-12 monotone cutoff controller
  -> two positive flows bracketing tau=0
  -> exact positive CRCTP flow
  -> all negative debt localized to the dyadic root scalar
  -> one-sided subpower root estimate
  -> Cycle Debt -> sharp prime ramp -> RH.
```

Exact in this packet: all arrows through the finite two-policy interpolation.
Open: cofinal occupation/bracketing and the root-scalar estimate. RH remains
unproved.
