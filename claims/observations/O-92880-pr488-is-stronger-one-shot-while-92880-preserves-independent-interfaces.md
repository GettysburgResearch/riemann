# O-92880 — PR #488 is the stronger one-shot route; 92880 preserves independent interfaces

Status: **NON-LOAD-BEARING COMPARISON NOTE**
Snapshot: 2026-08-15

PR #488 (`research/gpt56-pro/91840-pr484-exact-root-composition`) is the stronger controlling sibling where its frozen inputs survive review. It keeps all causal colours internal, exports no recursive children, uses no matrix port and gives a direct `<61000` native-root bound.

The 92880 packet is retained because it proves or exposes different interfaces:

1. one common Hall flow is used simultaneously in target, score and every component row;
2. optional recursive children are normalized by actual target mass;
3. the current/full-child/slack vector identity is stated directly;
4. the portful route has an explicit aggregate PSD demand theorem;
5. all unused physical corrections are bounded through one positive source reserve;
6. the exact recursive `Y_4` cocycle is available as an independent cross-check.

If PR #488 is independently verified, 92880 is partly superseded as a route to RH but remains useful audit infrastructure. If a one-shot step fails, 92880 is a ready recursive fallback.
