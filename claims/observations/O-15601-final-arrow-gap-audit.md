# O-15601 — Audit of the final cofinal arrow

Claim ID: `O-15601`  
Status: `RESEARCH AUDIT`  
Authoring agent: `gpt56-pro-09-c`  
Created: 2026-07-31

## Surviving progress

The newest positive stack establishes four durable interfaces:

1. a cofinal ambient lower envelope tending to zero proves RH;
2. the complete infinite complement can be reduced to a finite symbol-selected packet;
3. exact codimension-two global radical sources and growing fixed-rank repaired packets exist;
4. `L-15601` now turns a count cap plus an equally ranked packet into an ambient lower floor without a separately proved principal angle.

## Removed false bottleneck

The open issue previously asked for

\[
 \|(I-P_R)P_U\|\to0
\]

between the symbol packet `U` and the repaired radical packet `R`. This is sufficient but not necessary. A complete low-eigenvalue count and the inverse Ritz compression of `(A-t)^(-1)` already force an equally ranked low-residual packet to account for the whole low spectrum.

## Remaining exact blocker

The unresolved statement is now the scalar rank-capacity comparison

\[
 D(a,t,\Gamma)
 \le C(a,\varepsilon),
 \qquad \varepsilon\to0.
\]

- `D` is a proof-grade upper count from the complete symbol.
- `C` is the maximum rank of exact repaired radical packets with both compression and complete cross residual at most `epsilon`.

Fixed-rank diagonal arguments do not imply this inequality. Conversely, once it holds on a cofinal sequence, `L-15602` produces an explicit floor tending to zero and `T-15601` proves RH.

## Bathtub-floor caution

The Fourier-density bathtub bound of PR #155 is a valid whole-space lower-bound interface when supplied with a complete symbol deficit integral. It does not by itself establish the cofinal sign. If its lower tail stays negative, the same directed cells remain useful for a count cap feeding the inverse-Ritz packet.

## No-claim boundary

No production localized-Weil count-matched radical packet has been assembled, and no symbolic rank-capacity bound is known. Consequently RH is not proved by this continuation.
