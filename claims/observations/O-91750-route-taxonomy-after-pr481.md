# O-91750 — Route taxonomy after live PR #481

Claim ID: `O-91750`  
Status: **PROPOSED INTEGRATION MAP — PENDING REVIEW**  
Created: 2026-08-15

## One arithmetic method on PR #481

The original repaired packet and the later audit supplement on PR #481 are not
separate RH methods. They share the same root Hall, native capacities,
first-owner rough source, all-column physical realization, square-root thinning,
causal colours and endpoint consumer. The later packet supplies a second
bookkeeping language and sharper constants.

## Two assembly implementations

There are nevertheless two ways to assemble that one arithmetic method:

```text
recursive packet assembly:
  reserve child packet capacities, insert recursively feasible rows, and use a
  packet-native slack cocycle;

one-shot total-row assembly:
  retain every child colour inside the single globally quantized parent row and
  estimate the final native slack directly.
```

PRs #482/#484 correctly identified unresolved gates in the recursive
composition. `L-91753/T-91751` use the one-shot assembly and therefore bypass
those gates rather than relabelling them.

The canonical merge policy is:

```text
retain local lemmas L-91726--L-91737;
supersede T-91724 and T-91725;
retain L-91750--L-91752 as compiler/cross-check infrastructure;
use L-91753 and T-91750--T-91751 as the unique conclusion layer.
```

## Independent methods elsewhere

The repository still contains genuinely independent programmes:

```text
completed depth graph-Gram / radial model allocation;
passive-string and near-cut complete-Bernstein realization;
First-Hermite heat resonance;
Q4 distinct-prime positive energy;
Cycle Debt / signed fragmentation.
```

They may cross-check the factor-67 endpoint conclusion or supply future
independent proofs, but none is logically required by `T-91751` and none was
completed by the extra commits on PR #481.

## Integration recommendation

Review the factor-67 route as one immutable arithmetic proof DAG with two
possible assembly implementations. Do not count duplicate formulations as
independent evidence. Conversely, do not close an independent route merely
because the factor-67 packet passes or fails.
