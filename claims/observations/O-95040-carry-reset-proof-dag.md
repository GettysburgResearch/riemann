# O-95040 — Carry/CRCTP reset proof DAG

```text
critical carry target
 -> exact multiples-Möbius divergence
 -> dyadic root completion
 -> signed interior decoder                 CLOSED by L-95040
 -> positive transverse producer            OPEN in general
      |- nonnegative-high-node tree cone    CLOSED by L-95041
      `- generic root-neutral shortcut      FALSE by R-95040
 -> root one-sided/subpower estimate         RH-BEARING / OPEN
 -> Cycle Debt -> sharp prime ramp -> RH     FROZEN CONDITIONAL CONSUMER
```

The reset separates algebra from positivity. Future CRCTP work should either:

1. construct a positive Green transform landing in the explicit interval cone of `L-95041`; or
2. emit a rational/directed Farkas separator for the actual target.

No prose-level root-completion claim may be promoted to positivity without one of those proof objects.
