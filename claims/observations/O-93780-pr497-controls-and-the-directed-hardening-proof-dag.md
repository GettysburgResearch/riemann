# O-93780 — PR #497 controls, and the directed hardening proof DAG

Observation ID: `O-93780`  
Created: 2026-08-15  
RH status: **unproved**

```text
unpublished 93420 packet
  -> claim-by-claim comparison
  -> PR #497 strictly controls; no duplicate publication

L-93600 analytic ramp expansion
  + L-93780 directed zeta primitives
  + exact uint128 event order
  -> L-93781 directed all-row tail
  -> L-93782 row-66 extremality and exact compact/tail join
  + L-91781 compact theorem
  -> complete AVLT

exact stopped tree + first ownership
  + L-93783 typed Target-Lorenz leaf (nu; B; sigma)
  -> L-93784 concrete common parent
  -> one thinning / one quantizer / internal actual child colours
  + separate signed observation ledger
  -> every ordinary and detail column feasible
  -> r_X = Omega_X-Xi(d_X) >= 0

sparse positive Y4 dual
  -> L-93785 native deficit <61000
  -> one-sided endpoint consumer
  -> proposed RH conclusion on frozen inputs
```

The first hostile-review targets are:

```text
Euler--Maclaurin component-row algebra in L-93600;
exact reproduction of PR #497's coefficient envelope by the directed source;
compact L-91781 replay and blob lock;
frozen endpoint-frame source identity;
all-column and terminal estimates;
prime-square, Mellin and Landau consumer.
```

No conclusion in this packet identifies a signed comparison with positive
source, reserves full child capacity, introduces a hidden port, or uses the
RH-bearing benchmark bridge.
