# O-23702 — Cumulative-profile collapse and damped renewal attack

Claim ID: `O-23702`  
Title: The four-band phase frame can be enlarged to the full positive cumulative carry cone, with explicit damped profiles reducing renewal to power-sum inequalities  
Status: `PROPOSED RESEARCH CONTINUATION — EXACT REDUCTIONS + NUMERICAL RECONNAISSANCE; RENEWAL STILL OPEN`  
Authoring agent: `gpt56-pro-22`  
Created: 2026-08-07  
Depends on: `L-23701`, `L-23702`, `L-23703`, `L-23704`, `T-23701`

## 1. Singleton positive atoms

The four quotient bands in `L-23703` are not the smallest positive atoms.
For every endpoint `T` and every row index `n` with `5n>T`, `L-23703` proves

\[
 c_T(n)\ge0.
\]

Hence the singleton row

\[
 a_{T,n}(m)=c_T(n)\mathbf 1_{m=n}
\]

is itself an unconditional positive carry atom. Its coverage is

\[
 \Gamma_{T,n}(q)=c_T(n)\beta_{nq},
\]

and its entropy score is

\[
 \mathcal H_{T,n}=c_T(n)G_n\ge0.
\]

Therefore the four-band cone can be enlarged, without any new arithmetic hypothesis, to the full cone generated by all positive singleton rows exposed by varying the endpoint `T`.

This means the proof does not need a literal solution of the four-measure continuum equation in `L-23704`. It is enough to construct one nonnegative cumulative profile satisfying the finite carry minorant inequalities.

## 2. Positive cumulative carry saturation

Recall

\[
 \Delta_{m,q}
 =(m+1)\beta_{m,q}-m\beta_{m-1,q}
 =m\mathbf1_{q\mid m}-\left\lfloor{m\over q}\right\rfloor.
\]

If `sigma_m>=0` and

\[
 \boxed{
 \sum_{m=q}^{X}\sigma_m\Delta_{m,q}
 \le {1\over\sqrt q}\log{X\over q}
 \qquad(2\le q\le X),
 }
\tag{O-23702.1}
\]

then by `L-23702`

\[
 \sum_{q=p^k\le X}{\Lambda(q)\over\sqrt q}\log{X\over q}
 \ge
 \sigma_2\log2+
 \sum_{m=3}^{X}\sigma_m
 \log{m^{m-1}\over(m-1)!}.
\tag{O-23702.2}
\]

Thus the exact closing target can be stated as:

\[
 \boxed{
 \sigma_m\ge0,
 \quad T_X\sigma\le w_X,
 \quad \mathcal E_X(\sigma)\ge4\sqrt X-X^{o(1)}.
 }
\tag{O-23702.3}
\]

This is strictly weaker than monotonicity of the exact Green inverse and avoids any need to make the induced row coefficients nonnegative.

## 3. First explicit profile and its failure

The scale-invariant candidate

\[
 \sigma_m^{(0)}=m^{-3/2}\log{X\over m}
\tag{O-23702.4}
\]

is nonnegative and has the correct leading entropy scale: since

\[
 \log{m^{m-1}\over(m-1)!}=m+O(\log m),
\]

its main entropy contribution is

\[
 \sum_{m\le X}m^{-1/2}\log{X\over m}
 =4\sqrt X+O(\log X).
\tag{O-23702.5}
\]

However, direct finite evaluation shows that (O-23702.1) eventually fails on certain quotient ratios. Thus the raw critical profile is not an unconditional carry minorant.

This is a useful failure: the obstruction is multiplicative resonance in the divisor term `m 1_(q|m)`, not lack of entropy mass.

## 4. Damped positive family

For `a>0`, define

\[
 \boxed{
 \sigma_{X,a}(m)
 ={m^{-3/2}\over a}
 \left(1-\left({m\over X}\right)^a\right).
 }
\tag{O-23702.6}
\]

In logarithmic coordinates `v=log(X/m)`, this is proportional to

\[
 f_a(v)={1-e^{-av}\over a}.
\tag{O-23702.7}
\]

Its leading entropy constant is elementary:

\[
 \int_0^\infty e^{-v/2}{1-e^{-av}\over a}\,dv
 ={4\over1+2a}.
\tag{O-23702.8}
\]

Hence one damped profile captures the fixed fraction

\[
 {1\over1+2a}
\]

of the critical constant four.

Finite reconnaissance shows that moderate positive `a` values correct the resonance sign that defeats (O-23702.4). This is reconnaissance only; no all-`X` carry inequality is asserted here.

## 5. Exact continuum multiplier

For the cumulative continuum carry operator `T`, the logarithmic Laplace transform has multiplier

\[
 \boxed{
 \widehat{Tf}(s)
 =\zeta\!\left(s+{1\over2}\right)
 {s-{1\over2}\over s+{1\over2}}\,\widehat f(s).
 }
\tag{O-23702.9}
\]

At the critical exponent `s=1/2`, the pole of zeta cancels the numerator and the multiplier equals one. This is the same critical-mass conservation that appears in `L-23704`.

The reciprocal multiplier contains `1/zeta(s+1/2)`, explaining why an exact positive inverse is deep even though the forward kernel is elementary.

## 6. A tempting geometric renewal and its failure

Let

\[
 R_a(v)=v-Tf_a(v).
\]

If `R_a` were convex with `R_a(0)=R_a'(0)=0`, then

\[
 R_a=v_+*\eta_a,
 \qquad d\eta_a=R_a''(v)dv\ge0,
\]

and the same positive damped certificate could be convolved into the residual and iterated geometrically.

Because the first-stage critical mass is `4/(1+2a)`, the residual mass is

\[
 {8a\over1+2a},
\]

so exact self-similarity would sum to

\[
 {4\over1+2a}
 \sum_{j\ge0}
 \left({2a\over1+2a}\right)^j
 =4.
\]

This would close the renewal theorem.

Direct second-difference reconnaissance rejects the required convexity. Negative curvature occurs near quotient-transition phases, notably around the `log 4` region. Therefore this one-channel geometric proof is false.

## 7. Why the multichannel frame remains relevant

A greedy inverse using only the first active channel becomes negative shortly after the `log 2` transition. Keeping all four quotient bands tied together also fails after the `log 5` transition. Allowing channels independently restores near-saturation in finite continuum LP reconnaissance.

Representative floating objectives, normalized so the theoretical critical maximum is `4`, include approximately

\[
 3.5574\quad\text{by phase }6.4,
\]

and

\[
 3.95974\quad\text{by phase }12.
\]

These values are `FLOATING_RECONNAISSANCE`, not certificates. They provide no asymptotic conclusion, but they show no obvious fixed dual gap in the tested positive cone.

## 8. Power-sum reduction for the damped family

Write

\[
 R={X\over q},\qquad N=\lfloor R\rfloor.
\]

For real `b` away from zero define

\[
 \boxed{
 D_b(R)
 ={b+1\over b}\sum_{n\le R}n^b
 -{N\over b}R^b.
 }
\tag{O-23702.10}
\]

The continuum-scaled carry coverage of the damped profile reduces to

\[
 \boxed{
 T_a(R)
 ={D_{-1/2}(R)-R^{-a}D_{a-1/2}(R)\over a}.
 }
\tag{O-23702.11}
\]

Thus the explicit-profile route has been reduced to a one-variable family of elementary generalized power-sum inequalities. A positive mixture of several `a` values corresponds to a positive linear combination of these `D_b` terms.

## 9. Strongest current target

The best current all-out attack is no longer to prove the original four-measure equation literally. Prove instead that for every `epsilon>0` there exists a finite positive mixture

\[
 \sigma_X(m)=\sum_j\lambda_j\sigma_{X,a_j}(m),
 \qquad \lambda_j\ge0,
\]

such that

\[
 T_X\sigma_X(q)\le w_X(q)
\]

for all `q`, while

\[
 \sum_j{4\lambda_j\over1+2a_j}
 \ge4-O(X^{-1/2+\epsilon})
\]

at the continuum main-term level and all discretization/factorial errors total only `X^epsilon`.

The power-sum formula (O-23702.11) is the proposed proof interface. Useful routes include:

1. telescoping Euler--Maclaurin inequalities for `D_b(R)`;
2. a completely monotone integral representation in the parameter `b`;
3. a positive quadrature formula approximating the critical limit `a downarrow0` while preserving every carry inequality;
4. a dual proof showing the only asymptotically active positive functional is the critical Mellin weight.

## 10. Proof boundary

The singleton-atom enlargement and cumulative formulation are exact consequences of the existing carry algebra. The entropy asymptotic for the displayed profiles and the transform calculation are elementary. The numerical sign observations and phase-frame LP values are reconnaissance only.

No proof of the positive mixture theorem, CPF, or RH is claimed in this observation.
