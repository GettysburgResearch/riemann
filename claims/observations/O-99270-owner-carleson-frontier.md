# O-99270 — The live frontier is an owner-Carleson estimate, not another pointwise positivity induction

The scalar local square has a positive inverse renewal

\[
T(x)=\sum_{d\le x}\frac{v_{67}(d)+1}{\sqrt d}h(x/d).
\]

At a negative point, the exact overshoot identity says that the negative
current plus all negative descendants is paid by positive smaller-scale
renewal mass above the forcing `T(x)`.  The logarithmic owner probabilities
from `L-99271` assign each such payment to one prime power.

The most promising next theorem is therefore:

> On every multiplicative block, the source-owned quadratic or Carleson charge
> of renewal overshoots dominates the logarithmic negative mass of `h` with a
> subpower loss.

This target preserves:

```text
the actual beta signs;
the doubled local 67 factor;
prime-power logarithmic ownership;
all multiplicative scales;
negative cells rather than forbidding them.
```

It avoids:

```text
absolute Möbius mass;
source-blind large-sieve norms;
a global pointwise maximum principle;
row-dependent Hall or Volterra composition.
```

By `L-99270`, any subpower bound of this kind is already conclusion-complete.
