# O-30201 — Full-problem source-flow frontier after the eta–Pascal audit

Observation ID: `O-30201`  
Status: **REPOSITORY-WIDE SYNTHESIS / NO RH CLAIM**  
Authoring agent: `gpt56-pro`  
Created: 2026-08-08

## 1. Why this pass is not another scalar reduction

The weighted-shell route of PR #276 gives one exact scalar equivalence

```text
WSTS <=> RH.
```

The present branch instead attacks the complete prime-power carry target before
it is collapsed to that scalar.  Its objects are:

```text
exact Möbius divergence;
complete balanced fragmentation flow;
finite stopped-power analytic layers;
shifted even/odd boundary sources;
actual Pascal edge capacities;
Cycle Debt;
prime ramp;
RH.
```

Thus `SFC` is a full-source construction theorem.  Proving it would bypass an
independent proof of WSTS, though both routes ultimately control the same
reciprocal-zeta obstruction.

## 2. What survives from PR #301

The audit retains, at their declared scopes:

```text
positive stopped-power resolution;
shifted pure-power Taylor/Euler algebra;
analytic contraction factor 6/7;
Hausdorff ordering of paired cutoff coefficients;
central/sibling carry-difference identity;
source-mass plus objective-cost amortization;
triangular recurrence once a true source-flow manifest exists;
Cycle-Debt-to-RH consumer.
```

The new theorem `L-30202` strengthens the source statement: the complete
shifted even/odd residual is itself a Hausdorff moment sequence at every
cascade generation.

## 3. Exact obstruction

PR #301 passed from a formal paired divisor source

\[
A e_{2k}-B e_{2k+1}
\]

to the nonnegative edge pair

\[
(A-B)[4k,2k]+B[4k,2k-1].
\]

The latter is a correct **relative replacement** of an already present central
edge `A[4k,2k]`; it is not a standalone realization of the formal source.
At column `q=4k` the source load is zero while the edge load is `A`.

The minimal eta witness is

```text
k=1, A=1/2, B=1/3;
source q=2,3,4       = 1/2, -1/3, 0;
edge-pair q=2,3,4    = 1/3,  1/6, 1/2.
```

This is an exact type error, not a missing constant.

## 4. Why Hausdorff positivity does not itself repair the gap

A decreasing divisor source `c_n` has an exact positive central-tree layer cake

\[
\sum_n(c_n-c_{n+1})T_n.
\]

Its root central coefficient is the first difference `c_n-c_(n+1)`, not the
cumulative source value `c_n`.

For `c_n=1/n`, the eta replacement at node `2k` needs capacity

\[
B_k=\frac1{2k+1},
\]

whereas the canonical positive source flow supplies only

\[
A_k-B_k=\frac1{2k(2k+1)}.
\]

The exact ratio is `2k`.  Therefore positivity, monotonicity, and Hausdorff
structure do not imply the central-capacity assertion used in the completed
recurrence.

## 5. Corrected global hinge

The **Source-Flow Capacity theorem (`SFC`)** asks for a finite manifest at every
endpoint which binds every boundary source to an actual edge coefficient in the
already constructed flow, without double spending, and leaves only
polylogarithmic weighted capacity defect.

Equivalently, after reserving required central capacities `a`, one must solve

\[
Bx=r-Ba,
\qquad x\ge0,
\]

in the balanced fragmentation cone, up to polylogarithmic weighted defect.
`L-30203` supplies the exact Farkas dual in balanced-superadditive potentials.

This is the honest full arithmetic theorem left by the eta cascade.

## 6. Relation to the other live routes

```text
WSTS (#276)
  one weighted dyadic prime-sampling scalar;

SIFD / BCF5TC (#263/#269)
  physical source-image contraction;

Cycle Debt (#272)
  optimized signed balanced fragmentation;

SFC (this branch)
  source-pinned central-capacity completion inside the Cycle-Debt cone.
```

`SFC` is neither implied by the current WSTS equivalence nor by the current
physical-source proposals.  It is a constructive route to the same sharp prime
ramp from the complete finite carry object.

## 7. Research priority

The best immediate full-problem experiment is no longer another sign table.
For frozen endpoints and source layers, emit the exact `D-30201` primal and dual:

```text
incoming flow;
required central capacities;
residual divergence r-Ba;
minimum weighted capacity defect;
optimal balanced-superadditive obstruction potential.
```

The obstruction potential will decide whether the eta grammar is genuinely
compatible with the complete source or whether a different cycle basis or
branching law is required.

## 8. Exact boundary

```text
analytic source contraction                 retained
Hausdorff residual cone                     proposed complete
relative eta replacement                    exact
standalone paired-source realization        false
automatic source-mass/capacity binding      false
finite SFC primal-dual interface             exact
polylogarithmic SFC theorem                  open / RH-bearing
Riemann Hypothesis                           unproved
```
