# O-91760 — The Volterra/Möbius order swap removes the common-parent source search

Claim ID: `O-91760`  
Status: **PROPOSED RESEARCH SYNTHESIS — PENDING REVIEW**  
Created: 2026-08-15  
RH status: **unproved**

## New structural point

The one-shot factor-67 reviews treated the common positive endpoint measure as an imported object.  The reason it looked difficult is that the Möbius sum was kept inside each component-row fibre, producing `k`-dependent rows and a simultaneous Hall problem.

Interchange the Möbius sum and the endpoint Volterra integral first.  Then every colour has the form

\[
 \mu(k)\ell_{X/s}(k)G_s,
\]

where `G_s` is independent of `k`.  The source fibre is rank one.  Its signed colours can be coupled exactly, leaving the positive residual `L(X/s)G_s`.

Thus the exact chain is

```text
finite native Möbius seed
 = continuum Volterra seed + finite mismatch
 -> rank-one mu-coloured endpoint source
 -> exact source-owned cancellation
 -> explicit positive common parent
 -> one global quantizer and one comparison reserve.
```

The finite mismatch remains visible and is paid by the already-reviewed reserve.  The source theorem does not revive the false equality `b_X^star=bar b_X^star`.

## Consequences

1. The fixed-window component-row Hall theorem is no longer load bearing for the common-parent source derivation.
2. No row-profile monotonicity is needed at this step; all typed coordinates share one feature.
3. The root threshold `67` is used only to certify `L(x)>0` and to separate small root colours from rough first owners.
4. The exact finite/native equality is recovered before reserves by adding the explicit mismatch response.
5. The remaining hostile review should focus on the one-colour Volterra formula, source coupling, and frozen reserve/endpoint stack.

## Boundary

```text
Volterra/Mobius order swap                   L-91760
rank-one source-owned cancellation           L-91761
rough lift -> explicit parent fibre            L-91762
one-shot composition                         T-92910
independent review                           required
Riemann Hypothesis                           unproved
```
