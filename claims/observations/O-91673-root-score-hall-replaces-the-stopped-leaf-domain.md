# O-91673 — Root score Hall replaces the refuted stopped-leaf domain

Claim ID: `O-91673`  
Status: **CURRENT PROOF DAG / REVIEW HANDOFF**  
Created: 2026-08-14  
RH status: **unproved**

## Freeze

```text
repository:   gfreund123/riemann
base PR:      #462
base branch:  research/gpt56-sol/91680-causal-proportional-tail
base SHA:     8642e062b6c6f5a4c7d443a1ee5a6e9ecf3e4706
head branch:  research/gpt56-pro/91683-root-score-hall-causal-closure
```

## New proof order

```text
finite equality endpoint frame
 -> certified positive outer quotient fibers x<=54.2192
 -> one fixed-window score-Hall transport per root fiber
 -> exact score identity + target slack + every-row bonuses
 -> integrate positive fibers in the common parent coordinate
 -> one global positive quantization and one-use finite correction
 -> positive provenance certificate
 -> causal reset at rough primes, child mass <1/8
 -> uniform equality deficit
 -> native loss O(log X)
 -> finite dual and frozen endpoint theorem.
```

## Mandatory firewall

The following old proof order is forbidden:

```text
least-prime tree
 -> stopped one-prime leaf py
 -> separate survival/hazard Hall at py
 -> sum leafwise positive outputs.
```

PR #456 gives an exact infinite counterfamily to that Hall assertion. The new route Hallizes only the root quotient window and never the stopped leaf.

## Relation to PR #462

`L-91680` and `L-91681` on PR #462 remain useful fallback mathematics: the target-proportional producer is exact, and the arithmetic causal order is closed for `p>=500000`. They are not needed as antecedents of `T-91657`, because the successor avoids post-Hall stopped leaves entirely.

## Independent reconstruction order

1. `R-91673` and the PR #456 counterexample.
2. Fixed-window score Hall imported by `L-91673`.
3. Score- and target-normalized row monotonicity on the root window.
4. The exact native ordinary/detail response identities.
5. `L-91674` formal endpoint-frame integration and one-quantizer theorem.
6. Frozen analytic endpoint density, quantization, mismatch, safety, and terminal estimates.
7. Exact causal coefficient identity, causal positivity, compact debt, and same-index child functor.
8. Packet envelope.
9. Native benchmark comparison, finite dual, and endpoint chain.

## Boundary

```text
new root algebra                         explicit and replayed
rough recursion                          exact on frozen inputs
load-bearing review target               analytic root realization
complete proposal                        T-91657
Riemann Hypothesis                       unproved
```
