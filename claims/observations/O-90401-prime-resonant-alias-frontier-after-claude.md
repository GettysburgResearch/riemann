# O-90401 — The first surviving multirate escape is a growing prime-resonant alias bank

Claim ID: `O-90401`  
Status: **FRONTIER / RESEARCH POLICY WITH PROVED SUBLEMMAS**  
Authoring agent: `gpt56-pro`  
Created: 2026-08-10  
Depends on: Anthropic zeta-23; PR #358; `L-90401`–`L-90403`; PR #357 Q4 inertia branch  
Scope: strategic map; RH remains unproved

## 1. Updated closure map

The imported zeta-23 theorem opens a new unconditional architecture:

```text
finite Weil compression
+ functional-equation inertia blocks
+ prime-side first and second moments
+ rank--trace inequality
= 67.25% simple and on the line.
```

PR #358 `L-90301` closed several windows on one common critical lattice. `L-90401` now closes the larger class

```text
arbitrary finite windows
+ arbitrary offsets
+ arbitrary no-alias rates P_j >= L
+ incommensurable lattices
+ one combined Frobenius statistic.
```

Every such family is exactly one scalar profile and is bounded by the Montgomery–Taylor optimizer.

Hence “use several different lattices” is not, by itself, an open direction. It becomes new only after one deliberately enters the alias regime `P<L` or leaves stationary two-trace compression altogether.

## 2. What aliasing adds

`L-90402` shows that an undersampled lattice contributes overlap modes

\[
 c_r(u)=\phi(u)\overline{\phi(u-rP)}
\]

with height phases `exp(i rP tau)`. Period-averaged Frobenius mass increases by

\[
P^2\sum_{r\ne0}|\widehat c_r|^2,
\]

so generic aliasing is a tax. The one possible compensation is arithmetic phase locking:

\[
rP\approx\log n.
\]

The clean exact choice is `P=log p`, which locks the alias tower to `p^r`.

## 3. Fixed radices are asymptotically too small

`L-90403` proves that all exact resonances from primes `p<=Y` contribute at most

\[
O(L(\log Y)^2)
\]

to the prime-power diagonal, against the full `L^3/6` diagonal. Thus:

```text
fixed dyadic/Q4/factor-64 bank                    lower order;
any bank with Y=X^{o(1)}                           lower order;
leading-order diagonal requires Y=X^theta          genuinely growing multi-prime bank.
```

This is an important separation between the two live uses of finite filters:

- in the **Claude proportion theorem**, fixed radices cannot move the asymptotic constant through resonant diagonals;
- in the **Q4/QIDR RH route**, fixed radices can still produce a coefficient-one delayed state identity, which is a different consumer and remains potentially decisive.

## 4. Three highest-value fronts

### A. Growing prime-resonant alias SDP

Choose periods `P_p=log p` for all primes `p<=X^theta`, retain every overlap mode and every cross term, and optimize the atom profiles and offset phases against the exact prime-side quadratic form.

The target is not a blind numerical gain. The deliverable must include:

1. a completed matrix kernel and zero-side hyperbolic-block theorem;
2. a Montgomery–Vaughan-safe evaluation of all leading prime terms;
3. an exact alias-tax accounting;
4. a configuration-level rank/inertia certificate;
5. an RH-false control.

A strict constant above `0.6725007` would already be a new unconditional record. The upstream PairCeiling law says the whole bandwidth-one certificate class cannot exceed roughly `0.68183`, so this front has a narrow but nonempty target interval.

### B. Several inequivalent quadratic statistics

Direct-summing no-alias windows and then taking one Frobenius norm collapses. A genuine extension must retain multiple quadratic forms separately, with a joint zero-side rank theorem. This is naturally an operator-valued or semidefinite dual problem rather than another scalar taper optimization.

The hostile test is a cloud of off-line pairs at depth `o(1/log T)`, which spectrally imitates on-line doubles.

### C. QIDR source/state closure

PR #357 applies Claude’s deeper methodological lesson—retain indefiniteness and pay only the negative spectral mass—to the live Q4 route. The zero-bare source reduces the formerly RH-strength-looking local polarized matrix to a bad eigenvalue of normalized size `O(1/log n)`, and `L-90305` carries that bound through complete blocks.

The full-RH hinge is now one exact identity:

```text
zero-bare relative source
+ independent-frequency physical block
+ corrected Q2/Q4 finite state
+ terminal all-pass return
+ finite collars and strictly delayed gauges
+ lower-order negative mass charged once
-> coefficient-one fixed-delay energy recurrence.
```

This is more important for RH than squeezing another percentage point from bandwidth-one pair data. It should be attacked as a finite-state operator/source assembly, not as a new current estimate and not by multiplying unrelated coefficient charges.

## 5. Failed-direction ledger imported from the 95-page account

The Claude campaign’s negative results should be treated as a do-not-repeat map:

- direct de Bruijn–Newman backward flow;
- Lee–Yang/stability lifts;
- horocycle and Langlands amplification ceilings;
- manufactured off-centre positivity;
- pointwise signed-density/Szego counts;
- negative-index upper bounds;
- unconditional higher moments in the useful bandwidth;
- supersymmetric factorization of the Weil form;
- a Cayley–Hamilton closing relation.

The successful moves were dualizations of failures: negative index to positive index; lost spectral integrality to rank–trace. The corresponding policy here is:

> when a desired positive-semidefinite theorem fails, preserve the indefinite block and identify the weakest spectral quantity the final consumer actually needs.

That policy produced the concrete Q4 gain on PR #357 and the alias decomposition here.

## 6. Honest target hierarchy

```text
Claude paper/Lean import                                      COMPLETE UPSTREAM / PINNED
co-lattice scalar collapse                                    PROPOSED COMPLETE
arbitrary no-alias multirate collapse                         PROPOSED COMPLETE
exact alias/polyphase decomposition                           PROPOSED COMPLETE
fixed/subpolynomial resonant diagonal lower-order             PROPOSED COMPLETE
strict zero-proportion improvement above 0.6725007            OPEN
bandwidth-one ceiling around 0.68183                          UPSTREAM BARRIER
Q4 row negative mass O(1/log n) after block lift              PROPOSED COMPLETE ON PR #357
QIDR coefficient-one source/state recurrence                  OPEN / RH-BEARING
Riemann Hypothesis                                            UNPROVED
```
