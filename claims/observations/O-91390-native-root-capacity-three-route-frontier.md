# O-91390 — Three-route native-root capacity frontier

Claim ID: `O-91390`  
Status: **CURRENT RESEARCH FRONTIER**  
Created: 2026-08-14  
RH status: **unproved**

## Controlling base

The controlling normalization is PR #468 at

```text
a41f81466f85d52597c97b41505756a8860698d0.
```

Later work must preserve:

```text
full Möbius row = exact native datum;
canonical P61 row = native datum + rough reservoir;
endpoint deficit = positive Y4-weighted native detail slack;
universal shell positivity is native-row hard.
```

## Route order

### Route A — PR #464 compiler

The root Hall finite inequalities are retained. Direct native compilation fails
the exact ordinary coordinate at `(X,q)=(136,2)` unless the rough reservoir is
removed from current ownership. `R-91316` is the controlling separator.

### Route B — target-Lorenz producer

PR #467 gives the complete monotone profiles and finite cutoffs. `L-91386`
removes basis search: the leftmost target fill is simultaneously best for every
row and for score. The remaining task is one finite activation-cell sign
campaign, with an exact row separator on failure.

### Route C — direct \(Y_4\) LP

Solve the native current/children/slack problem without an intermediate source
ansatz. The objective is exactly the endpoint deficit. \(Y_4\)-zero columns are
score-free repair directions.

## Stop rule

Do not publish another full RH composition until one route returns

```text
PASS_NATIVE_ROOT_CAPACITY_THEOREM
```

with a complete immutable certificate. If the declared generator cone fails,
publish the exact Farkas separator and enlarge only the coordinate exposed by
that separator.

```text
native normalization                CLOSED
subcritical consumer                CLOSED CONDITIONAL
root Hall direct compiler           EXACT SEPARATOR
target-Lorenz finite campaign       OPEN
direct native slack LP              OPEN
NRCT                                OPEN / FAIL-CLOSED
RH                                  UNPROVED
```
