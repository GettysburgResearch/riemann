# O-91418 — Reciprocal-limit shifted-eight transport has positive moment

Claim ID: `O-91418`  
Status: **EXACT FINITE CERTIFICATE; ASYMPTOTIC TRANSFER PROPOSED**  
Created: 2026-08-13

Normalize the fully activated causal score masses by `5 sqrt(py)`.  The limiting mass at a `P_61` source node `d` is `1/d`.

`X-91411` works in the exact common denominator

```text
P_61 = 117288381359406970983270
```

and certifies the radius-eight prefix margin

\[
\frac{3541661146745983}{204963260862830470}>\frac1{100}.
\]

For the canonical left-greedy transport, define

\[
M_\infty=\sum_{o,e}\pi_\infty(o,e)(\sqrt o-\sqrt e).
\]

Directed radical intervals prove

\[
\boxed{M_\infty>18.}
\]

The retained value is approximately `18.538552495926712`.  The exact flow has `131203` pieces and includes upward edges of displacement eight.

The fixed-row ratio expansion gives

\[
q_{p,j}(e)-q_{p,j}(o)
=\frac{(-\lambda_j)\log p}{5\sqrt{py}}
 (\sqrt o-\sqrt e)+O_j(p^{-1/2}),
\qquad -\lambda_j>0.
\]

Combining this with the exact edge ledger of `L-91415` yields the proposed tail law

\[
R_{sig}^{(j)}-R_{res}^{(j)}
=(-\lambda_j)M_\infty\log p+O_j(1).
\]

Thus Route C has a strictly positive limiting coefficient.  The remaining work is an explicit common perturbation bound, an effective transition height, and the compact directed replay.

```text
limiting prefix moat       EXACT
limiting moment >18        EXACT
positive tail coefficient EXACT ALGEBRA
uniform effective tail    OPEN
full route                 OPEN
```
