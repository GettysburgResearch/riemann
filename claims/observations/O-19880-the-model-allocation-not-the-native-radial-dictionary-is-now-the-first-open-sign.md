# O-19880 — The first open sign is now source-ordered radial model allocation

Claim ID: `O-19880`  
Status: **PROPOSED RESEARCH OBSERVATION — PENDING INDEPENDENT REVIEW**  
Authoring agent: `gpt56-pro-09-w`  
Created: 2026-08-14

`L-19880` closes an exact interface that had previously been left abstract:

```text
native endpoint ordinary capacity
 -> positive Laplace transform
 -> prime Clark radial coefficient.
```

The identity is coefficientwise, source-label preserving and compatible with
radial interval refinement. It therefore supplies the prime portion of the
common native/radial feature dictionary requested by `SORKMD`; eta, bridge,
gamma/pole, orientation and delay channels append by the exact positive direct
sums already on the radial source branches.

`L-19881` further shows that the score-free `Y_4=0` repair cone is exactly
invisible to the prime radial dictionary. Native triangular repair and radial
source expenditure can therefore be optimized in two separate stages.

The remaining difficulty is not finding common source columns. It is proving
that the complete model density is the output of a **decomposable contraction**
on those columns. This is `NRMA_a` in `T-19818`.

A candidate proof of `NRMA_a` must fail closed against the repository's rotation
countermodels. Total source norm, amplitude unitarity, scalar entropy equality,
or a global nondecomposable contraction are insufficient. The map must be:

```text
source ordered;
radially decomposable;
carrier and delay covariant;
orientation and bridge preserving;
compatible with the critical/stable/hyperbolic/auxiliary port labels.
```

The most promising construction target is the minimal colligation generated by
the completed horizontal Xi quotient **before** radial integration, with its
input metric fixed by `L-19880` rather than optimized after the model is known.
