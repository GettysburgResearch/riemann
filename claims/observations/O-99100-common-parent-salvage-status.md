# O-99100 — Where the common-parent idea fits the live factor-67 portfolio

Status: **RESEARCH SYNTHESIS; NO RH CLAIM**

The one-random-key theorem directly repairs a recurring ownership mistake: constructing each child marginal separately and then assuming simultaneous realizability. It is compatible with the residual-only factor-67 identity and the strict actual-mass budget below `1/8`.

The theorem does **not** by itself validate the conclusion-producing producer in PR #620. That candidate still needs an exact proof that its native source/row/score data satisfy the common unnormalized domination and that its finite realization is samplewise feasible in every native coordinate.

The strongest next attack is therefore not another marginal Hall calculation. It is to expose the complete native producer as a face or projection of one integral network-flow polytope, with the score coordinate either represented as a network potential or proved monotone on every integral vertex.

A success would compose as

```text
exact native unnormalized source
 -> L-99100 common-parent partition
 -> L-99101 normalize-last transfer
 -> L-99102 samplewise integral Hall leaf
 -> all-column and literal-score consumer
 -> endpoint Mellin-Landau chain.
```

Until that source-to-leaf interface is proved, RH remains unproved.
