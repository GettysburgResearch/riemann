# O-101100 — Live implication hypergraph after the latest closure wave

This is a hypergraph: several arrows require simultaneous inputs.

## Frozen source layer

```text
ordinary Möbius source mu
        | factor-67 difference (zero-free)
        v
beta_67 source
```

Both are literal Euler sources.  Positive reservoirs, contracted children,
and floor-kernel inversions are excluded by existing firewalls.

## Quadratic lane

```text
beta source
  -> globally positive quadratic packet H_2                  [proved]
  -> upper-envelope defect E_2                               [exact]
  -> cell/Hankel/activation coarea                           [proved exact]
  -> adaptive small-prime squaring, owner mass < 3/4         [proved on corridor]
  -> critical continuous-boundary estimate                   [open]
```

The important gain is summable/squared small-prime geometry.  The important
loss is the activation collar and the impossibility of positive desmoothing.

## Wavelet lane

```text
mu or beta source
  -> minimal ratio-eight compact wavelet                     [proved exact]
  -> largest-prime ownership                                 [proved exact]
  -> smooth histories negligible                             [proved]
  -> zero-moment Vaughan: carrier and Type-I closed          [proved]
  -> balanced signed cross-core                              [open]
```

The important gain is exact cancellation on large-prime/complete-lattice
pieces.  The remaining balanced term is source-sensitive and cannot be sent
to an unsigned large sieve.

## New exact bridge

```text
continuous quadratic boundary  ----\
                                  +--> compact beta wavelet --> Mellin/Landau --> RH
activation atomic boundary      ----/
```

More precisely,

```text
3 G_beta
 = compact calibration
 - J P_2 activation_measure
 - J P_2 (D-1)E_2.
```

Therefore the two lanes are not competing reformulations.  They are the two
coordinates of one detector before absolute values are taken.

## The genuine AND gate

Let `Q(Y)` and `A(Y)` be the negative masses of the continuous and atomic
bridge channels.  A pair of source-faithful estimates

```text
Q <= a Q + b A + subpower,
A <= c Q + d A + subpower
```

closes jointly whenever

```text
a<1, d<1, bc < (1-a)(1-d).
```

Neither row need close its own lane.  This is the principal new implication
matrix of T-101100.

## Where existing work enters the matrix

- adaptive Euler squaring is the natural source of a strict diagonal budget
  in the quadratic row;
- largest-prime/zero-moment Vaughan is the natural source of a strict
  activation-to-quadratic cross estimate;
- the exact bridge fixes normalization, signs, activation atoms, and the
  harmless compact calibration;
- the matrix absorption lemma permits signed fluxes to be used before their
  negative parts are separated.

The missing work is not another detector.  It is a proof that the two exact
arithmetic cross-channel estimates populate a uniformly subcritical matrix.
No such claim is marked proved here.
