## Purpose

Continue the fixed-order `Xi''' -> Xi''` programme with a radical weakening of
the terminal target.  Global pointwise positivity of

```text
L_2(t)=Xi'''(t)^2-Xi''(t)Xi''''(t)
```

is stronger than required.  At a real zero of `Xi'''`, the required
orientation is exactly horizontal minimum curvature of `|Xi''(t-ih)|` at
`h=0`.

**RH remains unproved.**

## Exact new results

```text
Z(z)=integral u^2 Phi(u)e^(zu)du
D(z)=Z Z''-(Z')^2
D(it)=L_2(t)

Q_t(h)=|Z(h+it)|^2=|Xi''(t-ih)|^2
Q_t''(0)=2L_2(t)
```

On the real axis `D` is exactly a positive variance determinant.  For every
weight with nonnegative Fourier transform, the complete finite horizontal
shift has nonnegative weighted bulk defect.

A positive atomic source proves that these bulk facts do not automatically
continue to individual imaginary stationary points.

## Direct fixed-order conversion

If a density `q` of Conrey's real `Xi'''` zeros is horizontally minimizing,
then

```text
alpha_2 >= (2q-1) alpha_3.
```

Hence `q>1/2` gives a positive unconditional `Xi''` proportion genuinely using
`alpha_3>0.9873`.

## Unconditional weighted converse Rolle

For the actual Xi function,

```text
sum_good |Xi''(c)| - sum_wrong |Xi''(c)|
  = (1/2) integral_R |Xi'''(t)|dt > 0.
```

Thus good extrema already carry more than half of the critical-value amplitude
mass.  If the normalized amplitude bias `rho_T` exceeds the coefficient of
variation `v_T` by `eta>0`, exact algebra gives

```text
alpha_2 >= eta alpha_3 > 0.9873 eta.
```

## Correct open gates

```text
SHMIN104580
  horizontal-minimum density q>1/2 at real Xi''' zeros;

CSAMP104580
  critical sampling of the proved positive-Fourier bulk finite-shift defect
  with loss <1/2;

AMPREG104580
  liminf(rho_T-v_T)>0 for the actual critical values |Xi''(c)|.
```

Any one of `SHMIN104580` or `AMPREG104580` gives a direct positive `Xi''`
proportion from Conrey's `Xi'''` theorem.  None is claimed proved.

## Replay

```text
PASS_T104580_STATIONARY_HORIZONTAL_MINIMUM_ALGEBRA
ae633641b5399aedad7ad97738cf411f10fe9ebf471f70dac1f321a8194f147c
```

The replay authenticates exact finite algebra only.  It records
`alpha2_from_alpha3_proved=false` and `rh_established=false`.

## Review order

1. `L-104537`
2. `R-104519`
3. `L-104539`
4. `L-104538`
5. `L-104540`
6. `L-104541`
7. `T-104580`
8. replay, report and handoff
