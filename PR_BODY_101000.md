## Purpose

Build the live RH research graph as a typed implication **hypergraph**, rather
than another list of equivalent criteria.

```text
base main:
677203992eb0168920365ee45ae9db76bfa97dcf
```

**RH remains unproved.**

## New exact translations

```text
AFCD -> ACAD
```

by a positive Hardy operator and a dyadic tail argument.

```text
LPMW <-> BVD
```

because the largest-prime and Vaughan terminal forms differ only by an
`L1(dX/X)` error.

```text
signed phase-Hasse packing = minimal-wavelet realization
```

at the physical scalar scope; the nonzero-phase symbol retains the Euler root.

## Matrix compression

After contracting exact equivalences, the live graph has two structural middle
classes:

```text
CV  critical one-sided variation;
XD  signed cross-core dispersion.
```

The strongest honest conjunctive packets are:

```text
finite small-prime squaring
+ depth-one large-prime geometry
+ oriented collar variation
-> CV -> RH;

minimal compact wavelet
+ smooth-sector removal
+ rough largest-prime / balanced Vaughan dispersion
-> XD -> RH.
```

## Replay

```text
PASS_T101000_IMPLICATION_HYPERGRAPH
```

The checker verifies that no proven-only path reaches RH and computes the two
minimal open cut classes.

## Exact boundary

```text
implication matrix and provenance aliases    PROVED
AFCD -> ACAD                                 PROVED
LPMW <-> BVD modulo L1                       PROVED
phase-Hasse -> wavelet contraction           PROVED
CV critical variation                        OPEN / RH-EQUIVALENT
XD cross-core dispersion                     OPEN / RH-EQUIVALENT
Riemann Hypothesis                           UNPROVED
```
