## T-105680 continuation — determinant and rank-extension frontier

Two new all-rank results sharpen the intermediate Cauchy problem.

1. The canonical-overlap determinant strictly dominates the current
determinant at every height. The gain is an explicit positive Cauchy product.
Trace CTI follows whenever this gain pays the current AM/GM spectral
dispersion.
2. The complete trace defect has an exact nested rank-one extension formula.
The only possible negative increment is one scalar reverse-moment deficit,
which can be paid by the projection of the new deep residual back into the old
shallow model space.

The resulting open gate is

```text
REC105681:
  old-space return >= positive scalar moment deficit
```

at each extension in one ordering.

Replay:

```text
PASS_T105680_CAUCHY_DETERMINANT_AND_EXTENSION
checks=231
proof object=75690ec42866d1a6620052a4534a6c790543e4bab340dda4c522c7c3ba9b6cd8
```

Arbitrary-rank trace CTI, the cofinal Xi passage, pointwise localization, and
RH remain unproved.
