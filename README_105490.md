# T-105490 — Corrected F1 bounded-detector and spectral frontier

This checkpoint performs a binding audit of the observation multiplier on
durable draft PR #730.

Frozen heads:

```text
PR #730  f534df7ad3a180c35a95b57cd7a91b6233702408
PR #719  426fe1c34a35d21b38a393456a7071c0902170f1
PR #751  98af0db6ec7f77d6333a77a3dac53c4698852f43
```

The exact bounded density symbol is

\[
\widehat K_L(s)=
\frac{4(s-1)(1-2^{-s})^2(1-\sqrt2\,2^{-s})}
{s(s-\tfrac12)}.
\]

It is related to the differential half-source square by

\[
(5s+\tfrac32)(1-\sqrt2\,2^{-s})\widehat K_L(s)
=
4P(s)\widehat A(s)^2.
\]

This corrects the old direct equality. The reflection differential current is
still a valid stronger sufficient route because the missing operator has a
stable anti-causal/causal inverse.

The old unregularized spectral weight is asymptotically constant, so its
integral against any nonzero finite Dirichlet polynomial is infinite. The
correct bounded-current weight is

\[
\Omega_K(t)
=
\frac{
16|P(\tfrac14+it)|^2r_A(t)^4
}{
((\tfrac{11}{4})^2+25t^2)
(1+\sqrt2-2\,2^{1/4}\cos(t\log2))
}
\asymp(1+t^2)^{-1}.
\]

Hence

```text
F1KFOURTH105493
  -> F1KASQ105492
  <=> F1GRAM105480
  <=> F1HCNC105481
  -> F1HARDY105470
  -> RH.
```

Every premise remains open. Corrected PR #719 independently gives

```text
QPTI103112 <=> BCI102990 -> RH,
```

with coherent owner collapse still open.

## Replay

```text
PASS_T105490_F1_BOUNDED_RESOLVENT
```

The replay verifies the exact symbols, resolvent branch, corrected weight,
decay, divergence firewall and hostile mutations. It proves none of the open
gates or RH.
