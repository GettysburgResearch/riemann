# T-108430 — source-weighted shared-fibre front door

The positive occupancy/Hellinger theorems on this branch remain valid, but
they solve a uniform operator problem that is stronger than the literal native
source requires.

For the actual source vector `z`,

\[
\langle z,(B_R)_+z\rangle
\le
\sum_a\left|\sum_{r(\omega)=a}z_\omega\right|^2.
\]

Thus same-cell multiplicity is harmless whenever the signed native
coefficients cancel. On a source-authorized rank-one rectangle, the right
side factorizes exactly into two one-sided coefficient-collision energies.

This checkpoint therefore supersedes `FROBMIX108420` as the canonical first
gate. `FROBMIX108420` remains a correct stronger sufficient condition.

The live target is now:

```text
COEFCELL108430:
  full native source, assembled before norms
    -> literal physical cell sums
    -> one coefficient-weighted squared norm
    -> subpower bound.
```

Together with the explicit quadratic/principal ledger `QRESBIND107300`, this
feeds the existing connected-Kummer consumer.

Replay:

```bash
python3 -B experiments/X-108430-source-weighted-quotient/verify.py
```

```text
PASS_T108430_SOURCE_WEIGHTED_POSITIVE_QUOTIENT
2a4ea86c850e5f9faf6b7c53985bfd8d131e9570d993f77032ecac8c82edcbdb
```

```text
literal source-vector quotient bound    PROVED EXACT
unweighted occupancy as necessary gate  REFUTED
COEFCELL108430                           OPEN
QRESBIND107300                           OPEN
RH / GRH                                 UNPROVED
```
