## T-107120 continuation — additive Chowla and square-root frequency normal forms

The stationary ratio-67 beta square now has two exact additional coordinates.

1. It is its diagonal plus twice one complete weighted additive two-point
   Möbius correlation aggregate. After grouping by `g=(n,h)`, the source is
   the primitive correlation `mu(a)mu(a+r)/(g sqrt(a(a+r)))`. The maximal
   aggregate has exact power exponent `2 Theta-1`.
2. It is the sinc-squared spectral integral of the literal q-free Möbius
   Dirichlet polynomial. The classical mean-value theorem removes every
   frequency beyond `Y^(1/2) log(2Y)^B`, `B>1/2`, with subpower error. The
   retained low-frequency integral has the same exponent `2 Theta-1`.

Replay:

```text
PASS_T107120_BETA_ADDITIVE_CHOWLA_NORMAL_FORM
checks=352
proof object=88309b68691f188333d950ae67bac9bb9d2970e46286004cef31b3a5b7820ddc
```

The low-frequency/additive signed estimate and RH remain unproved.
