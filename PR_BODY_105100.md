## Purpose

Continue the post-integration Xi reverse–Rolle programme with an exact,
lightweight second-moment reduction based on the draft PR #720 frontier.

**RH remains unproved.**

Base: main at 852d8aa05c701ea7818ce8a50543e68987fef5cc.

Proposed source head: freeze the live PR head after the checkpoint commit; the
live PR body and checkpoint comment carry that exact SHA because this tracked
template cannot self-reference its own commit.

## Exact result

For a finite monic polynomial under explicit simplicity hypotheses, the sum
of squared derivative-ratio residues at zeros of \(p'\), plus a cross-residue
debt at zeros of \(p''\), equals one explicit centered \(V_2,V_4\) root ledger.

This gives an exact decomposition of the real critical-residue second moment
into:

- a complete-root fourth-moment term;
- an off-real critical correction;
- a second-derivative cross-residue debt.

## Binding firewall

The polynomial \(x^4-2x^2+2\) has

    real critical-residue M2     = 9/32
    root V2/V4 ledger            = 37/432
    second-level debt            = -169/864

so the root ledger alone is not an upper bound for M2.

## Scope

The packet is proposed mathematics pending independent review. It does not
localize the global identity to a finite height window, prove
canonical-product passage, bound either correction, prove RCMV104530, or prove
RH.

Claim inventory:

- L-105100: finite second-residue balance;
- T-105100: Xi continuation frontier;
- R-105100: root-ledger-only firewall;
- M-105100: hostile review contract.

Preferred review order: L-105100, R-105100, exact replay and tests, T-105100
scope, then M-105100 and packet metadata.

## Replay

    python -B experiments/X-105100-critical-residue-second-moment/verify.py \
      --output experiments/X-105100-critical-residue-second-moment/results/verification.json

    python -B -m unittest discover \
      -s experiments/X-105100-critical-residue-second-moment/tests \
      -p "test_*.py" -v

Arithmetic class: EXACT_RATIONAL. No heavy computation was run.

Exact replay digest:

    1d2a75cebfc8946597d9485a2de737ddad10baedf5011261dac95af9a6cf7ea0

## Validation

    PASS_T105100_CRITICAL_RESIDUE_SECOND_MOMENT_LEDGER
    10/10 focused unit tests pass
    PASS_FINAL_SCIENTIFIC_INTEGRATION
    PASS_MELLIN_LANDAU_CONSUMER
    git diff --check passes

Known inherited baseline issue: the unchanged
X-99930-critical-taylor test suite errors because its test calls
last_step_firewall while the producer exports abstract_last_step_firewall.
This packet does not alter that historical source.
