## T105418 addendum — one complete phase edge is unconditional

For every derivative order `r>=0` and every `y>0`, the positive Xi Fourier
kernel gives

```text
Im [ Xi^(r)(iy) / Xi^(r+1)(iy) ] > 0.
```

The proof is direct. Depending on parity, the ratio is `i` times a quotient of
strictly positive `cosh/sinh` Fourier integrals. No saddle theorem or zero
information is used.

Consequently, under the signed-real-critical-pole hypothesis, the harmonic
outer-phase problem has favorable data on:

```text
the real axis away from poles;      Im(F/F')=0
the small upper pole detours;        Im(F/F')>=0
the complete positive imaginary axis; Im(F/F')>0
```

Parity and reflection pay the second quadrant. The remaining gate is only the
remote quarter-circle, or its functional-equation-folded safe-line image:

```text
QOPG105418:
  max(0,-inf_outer-quarter-arc Im(F/F')) -> 0.
```

Then

```text
CRVH105330 AND QOPG105418
 -> OPG105417
 -> BRP105220
 -> RH.
```

`CRVH105330` and `QOPG105418` remain open. RH is unproved.
