## Purpose

Dedicated shared-conductor relative-Frobenius continuation from exact PR
#765 head `a30276a5be049749ebb2147f30f000dd5659298b`.

This PR now contains three source-locked checkpoints:

```text
T-108100  exact positive occupancy quotient
T-108102  live factor-4225 history compression
T-108104  source-grouped positive Hodge descent
```

**RH and GRH remain unproved.**

## T-108100 — positive quotient

For

\[
 B_R=R^*SR-dI,\qquad
 D=RR^*,\qquad
 U=R^*D^{-1/2},\qquad
 Q_R=D^{1/2}SD^{1/2}-dI,
\]

the positive operator satisfies

\[
 (B_R)_+=U(Q_R)_+U^*.
\]

Thus no hidden positive block lives in the residue-kernel directions.

## T-108102 — live source compression

The source-locked one-hundred-history block has

\[
 \sum z_\omega=4w,
 \qquad
 \sum|z_\omega|^2=676|w|^2,
\]

inside one physical cell of multiplicity at least \(100\). Hence its
normalized quotient input is at most \(4|w|^2/25\), a factor \(4225\) below
its literal energy, while at least \(16896|w|^2/25\) lies exactly in the
\(-d\) block.

## T-108104 — source-grouped Hodge descent

Let \(A\) aggregate only the source-authorized representation histories,
let \(C\) aggregate the retained source groups into physical residue cells,
and retain

\[
 R=CA,
 \qquad
 M=AA^*=\operatorname{diag}(m_g),
 \qquad
 D=RR^*=\operatorname{diag}(N_a).
\]

Define

\[
 V=A^*M^{-1/2},
 \qquad
 T=D^{-1/2}CM^{1/2},
 \qquad
 U=R^*D^{-1/2}.
\]

Then \(V\) is an isometry, \(T\) is a coisometry, and

\[
 \boxed{U=VT^*.}
\]

Every literal vector has the exact orthogonal decomposition

\[
 \boxed{
 z=(I-VV^*)z+V(I-T^*T)V^*z+UD^{-1/2}Rz.}
\]

The first term is within-history fluctuation. The second is cancellation
among distinct retained source groups in the same residue cell. Both lie in
\(\ker R\) and are exact \(-d\) eigenspaces. Therefore

\[
 \boxed{(B_R)_+z=U(Q_R)_+D^{-1/2}Rz.}
\]

The complete energy ledger is

\[
 \|z\|^2=
 \sum_g\left(E_g-\frac{|Z_g|^2}{m_g}\right)
 +\left(\sum_g\frac{|Z_g|^2}{m_g}
 -\sum_a\frac{|s_a|^2}{N_a}\right)
 +\sum_a\frac{|s_a|^2}{N_a}.
\]

The first two nonnegative terms are paid at the exact negative eigenvalue
\(-d\). Consequently:

- the original positive operator is exactly invariant under authorized
  history aggregation;
- nonzero source-group currents may cancel inside one cell and have zero
  positive debt;
- the only remaining positive input is
  \(y_a=N_a^{-1/2}\sum_{g\mapsto a}Z_g\), with original multiplicities and
  all conductor/source labels retained.

## Next exact gate

```text
LIVEQUOTIENTCURRENT108106

Assemble the complete retained group currents Z_g and original
multiplicities m_g,N_a in every relevant live fibre. Form

  y_a=N_a^(-1/2) sum_(g->a) Z_g

before every absolute value. Then prove signed cross-conductor recombination,
or realize these currents in a compatible relative partial-Frobenius trace.
```

The existing live files certify exact subblocks rather than a complete fibre,
so no full occupancy census is claimed here.

## Replay and boundary

```text
PASS_T108100_SHARED_FIBRE_POSITIVE_QUOTIENT
PASS_T108102_LIVE_SIGNED_HISTORY_POSITIVE_COMPRESSION
PASS_T108104_SOURCE_GROUPED_POSITIVE_HODGE_DESCENT
```

The T-108104 producer authenticates six predecessor blobs and uses exact
integer/rational arithmetic. It replays the live factor-4225 ledger, a
nontrivial two-stage atom/group/cell fixture, two literal history lifts with
identical positive debt, and a cross-group zero-current vector in
\(\ker R\setminus\ker A\).

```text
positive occupancy quotient                         PROVED
live factor-4225 compression                         PROVED EXACT
source-grouped three-block Hodge decomposition       PROVED
history-aggregation invariance of positive part      PROVED
same-cell cross-group zero-current negative block    PROVED
complete live grouped-current census                 OPEN
signed cross-conductor quotient estimate             OPEN
global partial Frobenius / RELTRACE                   OPEN
principal binding                                    OPEN
RH / GRH                                             UNPROVED
```
