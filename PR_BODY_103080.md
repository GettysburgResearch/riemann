## Quarter-power Boolean closure proposal

This continuation targets the exact frozen frontier `BCI102990`.

```text
base PR #719:
4146f81e7237d41e2e4a0cb1737511266683e980

supporting PR #751:
98af0db6ec7f77d6333a77a3dac53c4698852f43
```

**RH is not announced as proved. The packet is a complete unconditional proof
proposal pending independent frozen-head reconstruction.**

### New observation

The historical sixth-root Boolean cutoff was chosen to make Type I
power-saving. The consumer needs only a subpower bound.

On each dyadic physical/owner block choose the quarter-power cutoff

```text
V=floor((2Y/A)^(1/4)).
```

Then:

```text
complete Type-I packet:  Y^o(1);
balanced packet:         exactly support-empty.
```

The exact cutoff-transfer identity gives

```text
B_old = B_V + T_V - T_old = T_V - T_old.
```

Both Type-I rows are subpower. Hence the complete historical balanced row, and
therefore `BCI102990`, has subpower logarithmic negative mass.

### Proposed conclusion

```text
quarter-power cutoff transfer
 -> BCI102990
 -> ICPR102970 / OICP102960 / HMO102940
 -> fixed derivative Mellin--Landau consumer
 -> RH.
```

The first hostile review target is the uniform extension of the complete
source/Hilbert Type-I transport in `L-102953` from the sixth-root cutoff to the
quarter-power endpoint. The new packet states and proves that extension in
`L-103070`; failure of that reconstruction rejects the proposal.

### Replay

```text
PASS_T103080_QUARTER_POWER_BOOLEAN_CLOSURE_ALGEBRA
```

The replay authenticates finite Boolean and support algebra only. It does not
validate the analytic inherited stack or establish RH.
