## Purpose

Continue PR #715 by attacking its two remaining arithmetic rows jointly.

**RH remains unproved.**

## Main exact reduction

For every ordered labelled prime prefix,

```text
completed native owner atom   = -p^(-1/2) U_p C_(<p)
moving completion atom        = +p^(-1/2) U_p C_(<p)
```

so they cancel exactly before physical collapse. The simultaneous update is the
squared atom

```text
-p^(-1) U_(p^2) C_(<p).
```

Every phase-current prime power of order at least two has polylogarithmic
physical cost.

## Binding boundary

The native source is not the squared completion:

```text
native:   (1-67^(-z))/zeta(z)
squared:  (1-67^(-2z))/zeta(2z).
```

The squared source is holomorphic in the Landau half-plane. Their difference
retains every hypothetical off-line pole.

The two former rows are therefore replaced by one source-faithful Duhamel
current:

```text
AR-DEFECT102600:
subpower logarithmic negative mass of the carrier-recombined
native-completion defect.
```

## Replay

```text
PASS_T102600_COMPLETION_DEFECT_REDUCTION
```

The replay checks finite source algebra only.

```text
AR-DEFECT102600   OPEN / RH-BEARING
RH                 UNPROVED
```
