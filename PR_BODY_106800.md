## Radical beta reset: a quantitative zero-abscissa theorem

This successor is stacked on PR #758 at frozen parent
`fad8ce2c4ce8633c5bb67356e6765800eac3d440`.

The new theorem upgrades the fixed beta harmonic from a binary RH-equivalent
gate to a quantitative zero sensor. If

```text
Theta = sup Re(rho), zeta(rho)=0,
```

then one fixed nonzero guarded beta harmonic and the complete maximal compact
beta energy both have exact power exponent

```text
2*Theta - 1.
```

Equivalently, every zero-free half-plane `Re(s)>sigma` is exactly the growth
bound `X^(2 sigma-1+epsilon)` for that one scalar observable.

The classical Vinogradov–Korobov Mertens estimate transfers through the full
critical lattice and gives the unconditional source-faithful bound

```text
maximal beta energy
  << X exp(-c (log X)^(3/5) (loglog X)^(-1/5)).
```

This is a stretched-exponential saving, not a fixed power. It proves no new
zero-free half-plane and does not prove RH.

The packet is fail-closed and records the load-bearing boundaries:

```text
maximal prefix cannot be replaced by the endpoint;
low rank cannot replace signed arithmetic cancellation;
rough deletion beyond the log^2 frontier is not free;
the classical Mertens and explicit VK inputs are source locked.
```
