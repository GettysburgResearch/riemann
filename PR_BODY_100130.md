## Purpose

Continue PR #674 at exact head `714efd9f837b5349e5295c5064237375ee69798e`, identify the precise spectral content of its final Hardy square, and audit the separate positive-floor-kernel proposal.

**RH remains unproved.**

## Exact spectral theorem

For the compact ordinary-Mobius wavelet, put

```text
F_gamma(X)=sum_n mu(n)n^(1/2-i gamma)K_0(X/n),
Q_X=int |F_gamma(X)|^2 P_1(gamma) dgamma.
```

The Mellin transform is exactly

```text
M F_gamma(s)=Khat_0(s)/zeta(s-1/2+i gamma).
```

The compact multiplier is nonzero at every off-line reciprocal-zeta pole. If
`Theta` is the supremum of real parts of nontrivial zeta zeros, the weighted
L2 abscissa is

```text
sigma_2=Theta+1/2.
```

Consequently

```text
int_1^Y Q_X dX/X^3 = Y^o(1)  <=>  RH.
```

More quantitatively, an upper bound `Y^(2 theta+epsilon)` implies
`Re rho<=1/2+theta` for every zero. Cauchy-Schwarz proves `RH -> MWOC99910`,
while PR #674 supplies `MWOC99910 -> RH`.

## Binding floor-kernel correction

For

```text
K_Q(x)=sum_j q_j floor(x/j), q_j>=0,
```

finite Möbius inversion gives exactly

```text
sum_m mu(m)K_Q(N/m)=sum_(j<=N) q_j.
```

Its Mellin transform is `Q(s)/s`: the floor-kernel zeta factor cancels the
Möbius reciprocal-zeta factor. Hence the T-100120 positivity is tautologically
true but detects no zeta zero.

## Replay

```text
PASS_T100130_MINIMAL_WAVELET_SPECTRAL_AUDIT
```

## Boundary

```text
minimal compact wavelet                      inherited exact
Poisson-Mellin identity                      proved exact
wavelet L2 abscissa = Theta+1/2              proved
critical cumulative energy <=> RH            proved
MWOC99910 <=> RH                             proved across #674 + this packet
positive floor-kernel RH shortcut             refuted
signed ordinary-Mobius cross-core estimate    open / RH-bearing
Riemann Hypothesis                            unproved
```
