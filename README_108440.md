# T-108440 — resonance disposition

The clean nonresonant shared-conductor geometry is now understood. The
remaining double quadratic physical mode is not another oscillatory error:
inside a fixed owner-class fibre,

\[
\widehat Z(\kappa_\ell,\kappa_\rho)
=
\sigma\tau\widehat Z(1,1).
\]

Thus its squared modulus is exactly the unphased physical-cell squared
modulus. It has no core-variable oscillation and cannot be removed by Deligne
or square-root trace cancellation.

This statement is made in the physical squareclass normalization. The old
`eta=chi^2` tensor-Gauss indexing has a separate root and weight convention;
no automatic historical weight ratio is claimed.

The programme has therefore reached its real boundary:

```text
clean nonresonant geometry           CLOSED
positive source-vector quotient      CLOSED
double quadratic physical alias      PROVED
mixed one-coordinate resonances      OPEN
principal/root normalization         OPEN / CONCLUSION-BEARING
RH / GRH                              UNPROVED
```

Replay:

```bash
python3 -B experiments/X-108440-principal-resonance/verify.py
```

```text
PASS_T108440_DOUBLE_QUADRATIC_PHYSICAL_ALIAS
eabf156b3f224769f9f984f192ad2658faedc901d642cf8b56ad0ffc65f3f285
```
