# Reviewer checklist — root Hall literal-row identity

The packet is **not** an RH proof packet.  Review the following equation first:

\[
c_X(j)=R_{s,X}(c_s)(j)+R_{h,X}(c_h)(j)+B_s(j)+B_h(j)
\quad(j\ge2).
\]

Reject unless all answers are yes.

1. Is `c_X` derived from the exact finite equality row, not a continuum surrogate?
2. Is every squarefree rough monomial assigned to exactly one least-prime node?
3. Is each raw residual paired with its unique exact raw child before control?
4. Is the controlled cocycle checked in target, native score and every row coordinate?
5. Are `c_s,c_h` coefficientwise nonnegative after Hall?
6. Are `B_s,B_h` coefficientwise nonnegative and target-null?
7. Is the Hall row-profile inequality proved on every used edge in the same normalization?
8. Does summing leaves preserve literal row equality, rather than only total target mass?
9. Are bonuses retained current-only and children retained once?
10. Is the source/tree limit justified by monotone convergence of positive outputs?

Only after this equation survives should the reviewer proceed to `L-91702`,
`L-91622`, `L-91634`, and the endpoint consumer.
