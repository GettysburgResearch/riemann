# Light replay for the direct-main delta audit

Run:

```bash
python3 verify_direct_main_delta.py
python3 -m py_compile verify_direct_main_delta.py
```

The replay checks the packet schemas, the exact 85-commit contiguous partition
through cumulative compare indices, required claim dispositions, and five small
exact symbolic fixtures. It does not rerun any broad endpoint, prime, MPFR,
matrix, high-zero, or formal campaign and does not validate the underlying
research proofs merely because the structural replay passes.
