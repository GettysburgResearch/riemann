# Cross-review follow-up — Reviewer C

## Freeze and scope

This follow-up consumes the two completed cross-reviews without changing their verdicts:

- Reviewer A cross-review of Reviewer B: PR #710 at `e91d6aa25d2e8576e82d26170127941eec430f75`;
- Reviewer B cross-review of Reviewer A: PR #711 at `27784928fbcecff975bc947e13c3a30d3b630ce0`;
- prior Reviewer C publication: PR #712 at `2b35d2b159a4c7af045ffca9214aebe02a423da4`;
- frozen main remains `677203992eb0168920365ee45ae9db76bfa97dcf`.

No research PR beyond #707 is admitted as a research claim. No heavy computation was rerun. **RH remains unproved.**

## Queue disposition

The two handoffs delegated 22 candidate groups to Reviewer C. The exact disposition is in `CROSS_REVIEW_FOLLOWUP.tsv`:

```text
NEWLY_EXTRACTED  10
EXACTIFIED         8
ALREADY_COVERED    4
TOTAL             22
```

“Exactified” means the original packet already had the family or PR-level disposition, but lacked one or more of: exact source head, exact claim path, precise first broken arrow, checker/result/hash ledger, or corrected standalone scope.

## New or materially sharpened conclusions

### Fixed Mellin detector

PR #649 supplies one genuinely fixed detector:
`5c_X(2)+3c_X(3)`. Its Mellin numerator is
`-3(2^{-z}-1)(2^{-z}-2)`, zero-free for `Re z>0`.
This is a verified analytic consumer node. The eventual sign or subpower
negative-mass estimate for the same scalar remains the open arithmetic producer.

### Older Q4/PIG dust from PRs #383 and #386

PR #383 contains several independent local theorems that must not be buried under
later Q4 endpoint classifications:

- exact carry Fourier/inverse-Laplacian energy;
- exact reflected-bridge Haar localization, with all fine scales closed;
- exact periodized Jordan-2 covariance/GCD-square model;
- corrected fourteen-row phase-locked carry image;
- corrected one-state Möbius renewal, including the mandatory column-one gauge,
  `u_1` term, and factor-two spectral correction.

These are `DELTA_VERIFIED_WITH_FIXES`, because their bare numerical IDs collide
inside PR #383 and because fixed-endpoint, periodized, Haar and Brownian objects
must remain path-qualified.

PR #386 supplies an exact singular cosine-antiderivative/weighted-Goldbach normal
form. Its sine-mode fixture proves that source-blind `ell^2` or Parseval control
loses one full factor of the endpoint. This is an operator-class no-go, not a
counterexample to the actual von-Mangoldt source.

### Q4 middle genealogy

PR #540 is now pinned as a real positive multiplicative compiler:
`g_4(n)=4^{floor(v_2(n)/2)}` with a coefficient-one divisor Markov recursion.
Its source-free logarithmic-derivative-to-reciprocal shortcut is exactly false.
No additive critical-capacity theorem follows.

PR #597 is retained as exact classification mathematics: reciprocal-zeta-square
gcd resonance, annular-kernel noncancellation, a SACF exponent dictionary, and a
classical subexponential gain. The actual signed SACF/UOSACF estimate remains
open.

PR #600 gives the exact finite-filter endpoint: arbitrary boundary zero moments
are available at polylogarithmic inverse cost, but a further zero at `z=1/2`
would force power-sized inverse cost. No finite stable filter closes Q4.

PRs #520, #563, #573 and #595 were already adequately represented in the first
Reviewer C packet; their verdicts are unchanged.

### Arithmetic/source-interface dust

The follow-up extracts or pins:

- PR #439 `L-91355`: exact nonduplication and total child mass below `1/8`;
- PR #454 `L-91671`: exact atomwise root-ledger sufficiency and finite Farkas
  alternative;
- PR #470 `L-91687`: exact Y4-zero repair LP and triangular score-free transfers;
- PR #472 `L-19881`: Y4-zero repair is a prime-radial null gauge;
- PR #521 `L-94023`: the actual live Target-Lorenz marginal is one joint finite
  primal/Farkas problem, with feasibility open;
- PR #638 `L-99230`: exact distributional Volterra Green formula, including both
  boundary modes and every activation-knot atom.

For the historical PR #620 full proposal, the exact first broken arrow is now
recorded. `L-99022.3/L-99024.1` assign literal score `4sqrt(X)` to the same
near-native physical row. PR #635 `R-99161` proves that literal score is locked
by the ordinary response and equals `P_Lambda` minus its explicit defect. The
positive direct-integral/cubature API survives conditionally; the full wrapper
does not.

## Heavy campaigns

The heavy inventory now records exact source/result/checker hashes for:

- PR #508’s 51,118,080-event Target-Lorenz campaign. The bytes are retained,
  but PR #516’s exact rounding-policy probe invalidates its directed-inclusion
  claim, so it is computational support only.
- PR #630’s exact H67 campaign through `2,000,000,001`, including the checker,
  verifier, result, segment and proof-object hashes. This is a retained finite
  computer-assisted theorem only.

Both rows state `HEAVY_CAMPAIGN_NOT_RE_RUN`.

## Remaining boundary

No new proven-only path reaches RH. The surviving conclusion-facing gates remain
source-faithful fixed-detector sign/negative mass, literal native common-parent
feasibility, signed Q4/UOSACF cancellation, critical Pick orders four and above,
and the other open gates already listed in the primary packet.
