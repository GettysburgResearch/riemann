# Reviewer B cross-review of Reviewer A

**Repository:** `gfreund123/riemann`  
**Frozen main:** `677203992eb0168920365ee45ae9db76bfa97dcf`  
**Audited review:** PR #709 at `55fe0b6f23d9163e2b602608da84194ba243d7c4`  
**Comparison only:** PR #708 at `eb1987502ef9043e782ac6ab1e19c47be9daef93`  
**Scientific status:** **RH remains unproved.**

## Overall quality

Reviewer A's packet is substantive, mathematically informed, and valuable for reconciliation. Most modern arithmetic/source classifications are directionally correct. It is not yet suitable as a stand-alone canonical integration authority because its graph is not fail-closed, selected provenance is malformed, several source-type premises are implicit, and two interval theorems need a uniformity lemma.

## Accepted classifications

Accepted, with the exact local qualifications in `VERDICT_DELTA.tsv`:

- fixed-row, rows-2/3 and fixed 5:3 Mellin consumers;
- specialized Landau, fixed zero-safe box and fixed holomorphic-defect transfer;
- compact SHARP Hall and RN child/cocycle;
- alpha-child native-promotion refutation, sequential first-owner identity, parity and `p^-1` firewalls;
- SHARP positivity for every real `m>=2`, critical wall and exact distributional identities;
- minimal ratio-eight wavelet, Abel-Mertens frame, floor-kernel and critical-inverse firewalls;
- PR #705 correction: old K0/K1 edge false, same-K1 coordinate valid modulo `L1(dX/X)`;
- double-owner/coboundary identities and corrected Vaughan/half-divisor/Haar/diagonal reductions;
- abstract Perron, Schur, matched-transfer and root/excess inequalities at conditional scope;
- critical CV/XD and BPOE/HHFE estimates as open or RH-equivalent, never proved.

## Classifications requiring fixes

The analytic consumers require explicit fixed-detector, convergence/continuation, multiplicity, positive-real removability, local-integrability and finite-abscissa wording. The minimal-wavelet spectral-abscissa theorem needs a complete Mellin-Plancherel statement, including the infimum boundary, a.e. phase, multiplicity, vertical reciprocal-zeta bounds and possible nonattainment of `Theta`.

The mesoscopic Dickman and hereditary Bellman theorems should be catalogued as unconditional results once exact classical VK/de Bruijn citations and ranges are pinned. Their deep fixed/small-prime block remains open.

## Weakened classifications

1. `ARITH.HYPERGRAPH.REGISTRY` is weakened to a gap as an integration artifact. The table uses semicolon-separated premises while `verify_review.py` splits only ampersand. Fifteen multi-premise edges are therefore misparsed, and a strict parse exposes twenty dangling nodes.
2. `L-101220/L-101221` are held conditional pending a joint uniformity lemma over varying endpoint ratio, moving prefix location, continuity points and tails.
3. The sparsity-times-energy inequality is valid, but the subpower bad-set-length premise is automatic because logarithmic length is at most `log Y`. It is not a genuinely independent key without power-saving or deep-excursion occupancy.
4. Root-return plus root-free packing is an exact API, but should not be counted as two independent arithmetic keys until root-free separation is proved before using the unknown detector.

No accepted Reviewer A theorem is overturned as mathematically false beyond the mechanism refutations Reviewer A already records. The stale PR #692 K0/K1 edge is correctly retained only as false historical provenance.

## Mellin producer/consumer typing

The analytic consumer is stable. The missing object is a fixed source-faithful arithmetic detector. Rows 2 and 3 are a fixed finite family; a hypothetical zero may determine which member detects it, but may not determine or change the family. The graph must explicitly represent simultaneous native row production. A positive auxiliary kernel, moving completion, signed observation or holomorphic calibration formula is not that producer.

## Native source typing

The RN child repairs endpoint location, not coefficient. The one-prime alpha mutation has net shifted coefficient zero and parity magnitude `2r^2`, not native `r`. The sequential first-owner identity repairs the coefficient algebra but leaves `FCHD67` open. The exact odd-history fixture at `X=61841`, the raw-cutoff ratio `1/5`, normalized `p^-1`, and actual-response/full-capacity distinction remain binding.

## SHARP descent

Global positivity for every fixed `m>=2` is accepted, but no uniform descent to `m=1` exists in the proof: the owner mass becomes prime-harmonic. Distributional descent and weighted downward variation are identities, not estimates. The final variation condition is RH-equivalent.

## Minimal wavelet and XD

The compact wavelet algebra survives. PR #705 controls genealogy: the old K0/K1 edge is false; only the same-K1 theorem is admissible. Positive floor kernels cancel the detector, and positive inverse desmoothing costs critical `sqrt(X)` mass.

## Vaughan and occupancy

Corrected Vaughan signs, squarefree/gcd constraints, ratio-four factorization, `eta*eta=1`, finite truncation before Hardy, one-field norm, Haar Gram and subpower diagonal survive. The remaining near-collision term is signed. Positive Gram or source/phase amplitude does not orient the physical scalar; `BPOE103300` remains open.

## Graph and provenance defects

`GRAPH_DEFECTS.tsv` and `SOURCE_PROVENANCE_FIXES.tsv` are integration-blocking. The fail-closed replacement is:

- encode premises as JSON arrays and require exact arity;
- reject mixed delimiters, empty and duplicate premises;
- require every premise/conclusion and alias target to exist;
- represent open/conditional/equivalent hypotheses as nodes;
- declare RH as a terminal node type;
- validate exact 40-character SHAs, paths and internal claim IDs before reachability;
- exclude open, conditional, empirical, refuted and RH-equivalent edges from proved-only reachability.

The strict cross-review parser finds no proved-only path to RH. That is a sanity result, not validation of every scientific edge.

## Likely omissions

Reviewer C owns breadth. Exact candidates are listed in `MISSED_CLAIM_CANDIDATES.tsv`, including score-free/direct-integral Hall, same-row calibration, Volterra boundary modes, causal packet budget, Y4-zero repair cone, finite primal/Farkas results, Target-Lorenz live marginal and heavy 2e9/51m computation families.

## Computation boundary

No billion-scale, `10^8`, `2x10^9`, Target-Lorenz, MPFR, broad prime or formal campaign was rerun. Only exact small fixtures, metadata, source paths and method contracts were checked.

## Exact blockers to integration

1. Repair graph parsing and dangling hypotheses.
2. Repair exact SHAs, paths and alias closure.
3. Add explicit simultaneous native fixed-detector producer nodes.
4. Resolve L-101220/L-101221 uniformity.
5. Correct the sparsity-times-energy AND classification.
6. Reconcile omission and computation genealogy with Reviewer C.

## Suitability for reconciliation

**PR #709 is suitable as evidence for Reviewer D after applying this delta. It is not suitable as the final canonical graph in its current form.**

RH remains unproved.
