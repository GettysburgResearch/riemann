# Reviewer A cross-review of Reviewer B

**Repository:** `gfreund123/riemann`  
**Frozen main:** `677203992eb0168920365ee45ae9db76bfa97dcf`  
**Primary review audited:** PR #708 at `eb1987502ef9043e782ac6ab1e19c47be9daef93`  
**Overlap context only:** PR #709 at `55fe0b6f23d9163e2b602608da84194ba243d7c4`  
**Scientific status:** **RH remains unproved.**

## Executive verdict

Reviewer B's review is scientifically strong and substantially reliable. Its hostile distinctions between analytic consumers and arithmetic producers, amplitude and curvature, positive and signed source types, uniform and fixed-center heat, finite and all-order criteria, and empirical and theorem evidence are generally correct.

The cross-review does **not** overturn Reviewer B's principal actual-`Xi` result. The determinant algebra, companion curvature, reciprocal-concavity sum closure, one-orbit reserve, global reciprocal-square budget and locally `C^2` assembly support **positive semidefiniteness of every actual-`Xi` safe Pick packet of size at most three**, subject to local replay, source-lock, multiplicity and strictness repairs. No high-zero computation was rerun.

The material corrections are:

1. `E-MELLIN-002` collapses an open negative-mass estimate into a verified consumer node and must become a true hyperedge.
2. The fixed `5:3` scalar is missing from Reviewer B's registry despite being part of the assigned overlap audit.
3. `SACF` is too strongly labelled RH-equivalent. PR #580 proves it is an exact FOCC gate and sufficient for RH; PR #606's **UOSACF** is the later criterion with the proved converse under RH.
4. The route graph is not referentially closed: many premise/result labels are not registered semantic nodes.
5. Numerous source paths are placeholders (`PR_BODY`, directories or wildcards) rather than exact proof objects.
6. The PR-body claim that registry schemas, counts and reachability were checked is not reproducible from the retained replay suite.
7. Several major Q4 and Q4-Hermite predecessor packets were omitted from the frozen census. They are recorded only as candidates for Reviewer C/D.

## 1. Mellin-Landau overlap

Reviewer B correctly separates the non-minor PR #641 moving-row gap from the verified fixed rows `2,3` in PR #652. The common-zero elimination is exact. Fixed logarithmic smoothing, specialized Landau, multiplicity handling, positive-real removability and functional-equation reflection survive with stated hypotheses.

The graph encoding is not yet canonical. A semantic node called `CONSUMER.MELLIN.SUBPOWER_NEGATIVE_MASS` contains the conditional theorem "if the estimate holds, then RH" and is marked `VERIFIED`; `E-MELLIN-002` then uses that node alone to reach RH. Its prose says the arithmetic estimate is unproved, but a mechanical graph engine cannot infer this hidden distinction. The corrected graph is:

```text
verified fixed-detector negative-mass API
AND
open source-faithful subpower negative-mass estimate
  -> RH.
```

Reviewer B also omitted the fixed `5:3` scalar. Its exact numerator

```text
-3(2^{-z}-1)(2^{-z}-2)
```

is zero-free in `Re(z)>0`; its arithmetic sign/negative-mass premise remains open.

The minimal-wavelet spectral abscissa remains correctly classified as an RH-equivalent boundary, not independent progress.

## 2. Actual-Xi Pick order three

### Determinant algebra

`L-92000` is exact. The three-node determinant is the positive prefactor times the second divided differences of `1/p` and `tp`.

**Cross verdict:** `AGREE_VERIFIED`.

### Companion `tp` curvature

The orbitwise derivative formula and sign are correct on `t>1/4`. Grouped differentiated convergence and multiplicity conventions should be deposited explicitly.

**Cross verdict:** `AGREE_VERIFIED_WITH_FIXES`.

### Reciprocal-concavity sum closure

The cross term is exactly a nonnegative square, so finite and locally `C^2` positive sums preserve reciprocal concavity.

**Cross verdict:** `AGREE_VERIFIED`.

### One-orbit reserve algebra

For one off-line orbit the negative reciprocal curvature and positive cross curvature with a critical orbit are exact. The proposed fraction

```text
epsilon = 2 m kappa / (1-kappa)
```

does absorb the defect under the stated high-orbit hypotheses.

**Cross verdict:** `AGREE_VERIFIED_WITH_FIXES`.

### Global budget and external input

The reciprocal-square Stieltjes tail is conservative and the total allocated fraction is far below one. One fixed critical orbit is divided once into disjoint fractions; no critical reserve is reused.

The canonical source lock must identify the exact Platt-Trudgian publication, distinguish "all zeros lie on the line" from "all zeros are simple", and state the imported zero-count bound. The heavy campaign was not rerun.

**Cross verdict:** `AGREE_VERIFIED_WITH_FIXES`.

### Infinite-sum assembly and multiplicity

The grouped zero sums and first two derivatives have locally uniform reciprocal-square tails. One local bookkeeping point must be repaired: if the selected critical orbit has multiplicity `m0>1`, the regrouping in `L-92103` must retain `(m0-1)R0`. Alternatively, simplicity of that selected verified orbit may be explicitly imported. This does not change the PSD theorem.

### Final theorem

The chain supports:

```text
every actual-Xi safe Pick matrix of size <=3 is PSD.
```

The source theorem's stronger sentence that all distinct-node matrices are positive definite requires an explicit strict reciprocal-concavity argument or should be omitted from the canonical statement. Reviewer B's registered theorem states PSD, so its main verdict survives.

**Cross verdict:** `AGREE_VERIFIED_WITH_FIXES`.

## 3. Operator and source-type interfaces

Sampled Suzuki, Hardy, Fisher, Brownian/theta, Weil and Fredholm rows generally respect the necessary typing:

- amplitude isometry is not promoted to curvature sign;
- source dilation is not promoted to target allocation;
- completed Fisher and ordinary-prime Poisson sources remain distinct;
- scalar commutators do not replace the full polarized Gram;
- resident, leakage, delay, orientation and bridge terms are retained;
- positive Schur complements do not rescue existing negative directions;
- fixed finite order is not presented as all-order progress.

Some accepted rows still use `PR_BODY` rather than exact proof paths, so they are not ready for object-level extraction.

## 4. Fractional heat and First-Hermite

Reviewer B correctly keeps four statements separate:

```text
uniform-center fractional heat       refuted;
fixed/pole-centered signed heat      open;
First-Hermite countable criterion    valid with fixes;
unconditional First-Hermite regions  independent local theorems.
```

The First-Hermite source explicitly says that only normalization is imported from Zeta23 and that the local terminal graph, criterion and finite-witness theorem inherit no Lean status. Reviewer B does not grant such inheritance.

The broad-kernel, fixed-resolution exterior and `(4-epsilon) log log` wedge verdicts survive with threshold and normalization fixes. PR #520's phase-locked Q4-Hermite all-sublinear-excess theorem is a significant omitted candidate requiring independent reconstruction.

## 5. Q4

The factor-1024 annularization, ten-band geometry and source-preserving Type-I/II normal forms survive. The five-million endpoint scan is correctly `EMPIRICAL_ONLY`.

The central correction is logical:

```text
PR #580:
  SACF <-> FOCC packet
  SACF -> RH

PR #606:
  RH <-> UOSACF <-> A(X)=X^{o(1)}.
```

Therefore `SACF` should be `OPEN_SUFFICIENT_FOR_RH`, not `OPEN_RH_EQUIVALENT`. `UOSACF` remains `AGREE_OPEN_RH_EQUIVALENT`.

Reviewer B's frozen Q4 census jumps from PR #580 to PR #606 and omits important intermediate and predecessor mathematics: passive scale state, finite odd core, one-sided moment compression, critical resonance, zero-moment/filter barriers, early carry/Fourier identities and the Q4-Hermite frontier. These are candidates for C/D, not cross-review verdicts.

## 6. Registry and graph audit

The primary registries have unique-looking headline semantic IDs, and numeric collisions are handled semantically. However the route graph is not closed over the claim registry. Many open premises and intermediate conclusions are prose labels absent from `CLAIMS.tsv`. The most serious is `E-MELLIN-002`, because its missing premise changes proved reachability.

Multi-premise rows are syntactically represented as hyperedges with `&`, and empirical/refuted rows are not advertised as proved. Those aspects are sound.

Several family aliases combine multiple PRs, SHAs and paths in one scalar row. They should be marked explicitly as family aliases and decomposed into exact child aliases before integration.

## 7. Computation audit

No forbidden heavy computation was rerun.

The six Reviewer B light replays are useful and honestly scoped. The historical PR #446 verifier is indeed corrupt. The clean replacement checks local algebra and the printed budget, not the external zero verification or full infinite assembly.

The packet contains no retained registry validator, even though the PR body says schema, count, uniqueness and reachability checks were performed. A deterministic validator must be added before Reviewer D trusts the machine-readable graph.

# Required closing inventory

## Overall quality of Reviewer B

**High quality, scientifically useful, and suitable as a major reconciliation input after targeted repairs.** The review is especially strong on mechanism firewalls, source typing, heat-family separation, and the actual-`Xi` order-three reconstruction.

## Claims accepted without change

- exact three-node determinant factorization;
- reciprocal-concavity closure under positive sums;
- fixed rows `2,3` common-zero exclusion;
- fixed logarithmic-box multiplier;
- Q4 factor-1024 annular filter;
- uniform-center fractional-heat refutation;
- binding Brownian Bohr and positive-Schur firewalls;
- finite-degree Fredholm high-carrier no-go.

## Claims accepted with local fixes

- specialized Mellin-Landau and fixed holomorphic defects;
- minimal-wavelet spectral abscissa;
- actual-`Xi` `tp` curvature, orbit reserve, global budget, infinite assembly and final order-three PSD theorem;
- Suzuki/Hardy/Fisher local operator theorems;
- First-Hermite criterion and unconditional positivity regions;
- Q4 ten-band and Type-I/II reductions;
- UOSACF equivalence/filter invariance;
- finite computational theorems retained without rerun.

## Claims weakened or overturned

- `CRITERION.Q4.SACF`: weaken from `OPEN_RH_EQUIVALENT` to `OPEN_SUFFICIENT_FOR_RH`.
- `CONSUMER.MELLIN.SUBPOWER_NEGATIVE_MASS`: retain the analytic implication, but split the unproved arithmetic estimate into its own open node.
- `CRITERION.BROWNIAN.NEW_PRODUCER`: move from theorem graph to methodology/frontier registry.
- No headline actual-`Xi` claim is overturned.

## Likely omissions

- fixed `5:3` Mellin scalar;
- PR #383 carry/Fourier/Haar/Jordan and finite-row renewal mathematics;
- PR #386 singular Fourier/Goldbach Q4 normal form;
- PR #520 all-sublinear phase-locked Q4-Hermite frontier;
- PR #540 scale-four divisor compiler/firewall;
- PR #563 passive scale state;
- PR #573 finite odd-core packet;
- PR #595 one-sided gate and moment compression;
- PR #597 critical resonance and exponent dictionary;
- PR #600 zero-moment tower and stable-filter barrier.

## Graph defects

- open Mellin premise collapsed into a verified node;
- multiple unregistered premise/result labels;
- missing fixed `5:3` node;
- incorrect SACF equivalence label;
- research category represented as a theorem edge;
- no reproducible registry/reachability validator;
- family aliases not distinguished from exact object aliases.

## Exact blockers to integration

1. repair graph referential closure and split all verified APIs from open premises;
2. correct SACF classification;
3. add the fixed `5:3` scalar;
4. pin exact source paths and internal IDs for every extracted row;
5. repair PR #446 replay/source lock and multiplicity/strictness wording;
6. add a retained registry validator;
7. reconcile the omitted Q4/Q4-Hermite candidates with Reviewer C.

## Suitability of PR #708 as reconciliation evidence

**Yes, with qualifications.** PR #708 is suitable as scientific evidence for Reviewer D, but its TSV graph should not yet be used as an authoritative machine-traversable registry. The prose, local replays and most verdicts are valuable; the identified graph, provenance, omission and Q4-classification deltas must accompany it.
