# Checkpoint 6 — fixed-window CRT jet selectors

Checkpoint base:
`75bf12b76014da6ea983ac1dc8083aaac79132ce`.

L/T-105105 closes the exact existence part of the local-jet/global-observable
gate left open by T-105104. Given the complete finite denominator-event
manifest on a regular window, two confluent CRT polynomials select only the
real noncommon odd-order turns and annihilate every other actual pole.

For \(P=F/F'\) and \(Q=F^2/(F'F'')\), the selectors satisfy

\[
\frac1{2\pi i}\int_{\partial\Omega}W_1P
=\sum\rho_c^{\rm jet},
\qquad
\frac1{2\pi i}\int_{\partial\Omega}W_2Q
=\sum(\rho_c^{\rm jet})^2.
\]

At an order-\(r\) target the required local terms are
\(w^{r-1}\) and \(r w^{2r-2}\). The factor \(r\) converts the leading
\(Q\)-coefficient \(\rho^2/r\) into the actual second moment.

The packet also gives a root-free square-free/CRT construction for
polynomials. That corollary encodes all odd critical embeddings; it is not
silently identified with a real-window projection.

Exact replay:

    PASS_T105105_FIXED_WINDOW_CRT_JET_SELECTORS
    16/16 focused tests in normal and optimized Python
    digest 3edde4fdb651ae0c3781e4f832884f0d7c81247edd3b5a151c6b2c54f051cf05

No heavy computation is used. The Xi event census, selector norm and edge
estimates, multiplicity-defect control, cofinal limit, RCMV104530, and RH
remain open.
