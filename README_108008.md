# T-108008 continuation checkpoint

This is the fifth checkpoint on
`research/gpt56-pro/108000-beta-chebyshev-perron-edge`, continuing draft
PR #775 from exact head
`bbfb9761c2b4bb37e35342e8d9279895f8d44be0`.

## New result

T-108004 exposed the full local cubic Perron–Fourier carrier. T-108006 then used beta-pair evenness to select one directed chiral branch inside the assembled source. T-108008 authenticates that front door, corrects its source-lag orientation sign (the stated z+2it face selects m>k), and now inserts the literal beta source into that carrier before any source norm.
For

\[
 F_z(t)=B_\beta(s-it)B_\beta(s+it),\qquad s=(1+z)/2,
\]

and

\[
 P_z(u)=\frac1{2\pi}\frac{z}{u^2+z^2/4},
\]

the exact transfer is

\[
 (\mathcal P_zF_z)(\xi)
 =\sum_{m,k\ge1}
 \frac{\beta(m)\beta(k)}{\sqrt{mk}\max(m,k)^z}
 e^{i\xi\log(m/k)}.
\]

For the Chebyshev jump polynomial \(Q_N\), the assembled Perron form becomes

\[
 \frac1{2\pi}\int_{\mathbb R}
 \frac{|Q_N(\xi)|^2}{\xi^2}(\mathcal P_zF_z)(\xi)\,d\xi.
\]

The diagonal is removed exactly:

\[
 D_\beta(z)=\sum_n\frac{\beta(n)^2}{n^{1+z}}
 =\frac{1+4x+x^2}{1+x}\frac{\zeta(1+z)}{\zeta(2+2z)},
 \qquad x=67^{-(1+z)},
\]

and contributes exactly \(D_\beta(z)\), with no \(N^{1/3}\) cusp
amplification. At \(z=4iN+\lambda N^{1/3}\), the remaining source splits
into the exact \(+2N\) and \(-2N\) Cauchy channels.

The packet also proves the exact signed multiplicative-cell identity and the
physical duplicate-67 relation before squaring.

## Files

- `research/l-families/atlas/function_field/BETA_CHIRAL_SOURCE_CAUCHY_DECOMPOSITION.md`
- `research/l-families/atlas/function_field/beta_chiral_source_cauchy_decomposition.py`
- `research/l-families/atlas/function_field/beta_chiral_source_cauchy_decomposition.json`
- `tests/test_beta_chiral_source_cauchy_decomposition.py`
- `integration/2026-08-31/t108008-source-lock.json`
- `T108008_CONTENT_SHA256SUMS`
- `PR_BODY_108008.md`

## Replay

```text
python -B research/l-families/atlas/function_field/beta_chiral_source_cauchy_decomposition.py --check
python -B -O research/l-families/atlas/function_field/beta_chiral_source_cauchy_decomposition.py --check
python -B -m unittest tests.test_beta_chiral_source_cauchy_decomposition
python -B -O -m unittest tests.test_beta_chiral_source_cauchy_decomposition
```

## Boundary

```text
T-108006 chiral front door authenticated       PROVED
primitive-pair orientation sign                  CORRECTED (m>k)
exact beta-source Cauchy transfer              PROVED
finite jump spectrum before source norm        PROVED
exact beta-square diagonal subtraction         PROVED
first-edge two Cauchy channels                  PROVED
signed multiplicative-cell currents            PROVED
signed off-diagonal cusp estimate               OPEN
outer-frequency tails                           OPEN
new zero-free half-plane                        NOT PROVED
RH                                              UNPROVED
```
