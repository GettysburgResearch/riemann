## Purpose

Step back across the complete post-#652 research graph, treat every later
firewall as binding, and carry the two strongest surviving routes to their
exact terminal arithmetic statements.

```text
base PR:      #685
base branch:  research/gpt56-pro/100300-two-route-endgame
base SHA:     4f69b7656f42dcb5ff250d13adc9f88e8d18f315
head branch:  research/gpt56-pro/100340-two-terminal-routes-hardening
```

**The Riemann Hypothesis remains unproved.**

## Route A — actual-prime quadratic activation

The route with genuine structural slack is the globally positive quadratic
SHARP family.  This packet proves that every product-threshold `3/2`
activation prefix lies strictly in `(0,1]`, and derives the exact global normal
form

```text
e(y)
 = 16 Delta_(3/2) - S_(3/2)(y)
   - 3 integral_1^y [4 sqrt(t) S_1(t)-3 S_(1/2)(t)] dt/t^2.
```

Thus the entire discontinuous activation ledger is closed: all remaining
arithmetic lies in one continuous critical drift.  The stronger pointwise gate
is `CDTG100320`; PR #685's exact one-sided form is `CATD100300`, already proved
RH-equivalent.  PR #684's adaptive finite squaring is retained as the strongest
local producer for this drift, not as a positivity-preserving desmoothing.

## Route B — minimal ordinary-Mobius wavelet

For the minimal ratio-eight kernel, Cauchy phase integration yields one fixed
multiplicative autocorrelation kernel `kappa_sigma(n/m)`.  GCD extraction then
removes every common Mobius sign:

```text
m=d a, n=d b, (a,b)=1
mu(m)mu(n)=mu(a)mu(b),
```

and the complete common-core sum is the positive Euler factor

```text
zeta(2 sigma-1)/zeta(4 sigma-2)
  * product_(p|ab)(1+p^(1-2 sigma))^(-1).
```

The only remaining sign is the primitive coprime ratio `1<b/a<=8`.
`PRBC100330` is this physical primitive-ratio convergence theorem.  By the
exact energy-abscissa theorem of PR #675 it is RH-equivalent.  PR #685's
zero-moment Vaughan decomposition gives a second exact coordinate,
`BVD100310`, for the same terminal ordinary-Mobius cancellation.

## Algebraic firewall

For any current

```text
C_N = 3 B_N - 4 sqrt(N) A_N,
A_N=sum_(n<=N) beta(n)/n,
B_N=sum_(n<=N) beta(n)/sqrt(n),
```

the exact activation coefficient in `C_(N+1)-C_N` is `-1`, not `+3`, because
the simultaneous update of `A_N` contributes `-4`.  The packet retains this as
a mutation firewall; it does not declare any current live PR refuted by that
standalone identity.

## Replay

```text
PASS_T100340_TWO_ROUTE_TERMINAL_REDUCTION
```

## Exact boundary

```text
activation prefix 0<S_(3/2)<=1             PROVED EXACT
discontinuous jump ledger                  CLOSED EXACTLY
continuous drift / CATD100300               OPEN / RH-EQUIVALENT
adaptive finite squaring local producer     RETAINED EXACT

minimal fixed ratio-eight kernel            PROVED EXACT
positive common-core factorization          PROVED EXACT
primitive ratio PRBC100330                   OPEN / RH-EQUIVALENT
Vaughan balanced deficit BVD100310           OPEN / RH-EQUIVALENT

Riemann Hypothesis                          UNPROVED
```
