# T100510 — Complex quadratic positivity and atom-free critical descent

Frozen parent:

```text
PR #673
9849df6a52bb4791ebf10cf0dd9f0c929d36bdd9
```

The packet extends the shifted quadratic positivity theorem from a real
interval to an exact complex disk, proves every mixed Bernstein carrier of
total degree at least two positive, derives the complete differential Harnack
chain, and gives an exact smooth countermodel showing that those positivity
constraints alone do not control critical weighted downward variation.

```bash
python3 experiments/X-100510-complex-quadratic/verify.py \
  --output /tmp/x100510.json
cmp /tmp/x100510.json \
  experiments/X-100510-complex-quadratic/results/verification.json
sha256sum -c T100510_CONTENT_SHA256SUMS
```

Expected:

```text
PASS_X_100510_COMPLEX_QUADRATIC_DESCENT
```

**AFCD100510 and RH remain unproved; the packet identifies them as equivalent.**
