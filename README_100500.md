# T100500 — Minimal ordinary-Möbius wavelet / Mertens frame

Frozen parent:

```text
PR #675
7b28224ba1b072d4ccd5b93ad37c0a64e7939740
```

The packet gives an exact compact Abel–Mertens representation of the minimal
ratio-eight wavelet, a direct quantitative Littlewood dictionary, and a
source-blind frame firewall.

## Collision-safe publication and overlap note

This is the Route A `MWOC100200` packet mechanically migrated to the fresh
`T100500` namespace because unrelated `T100200` work was already published.
The mathematical formulas and original verifier fixture stream are unchanged;
the exact identifier map is recorded in
`integration/2026-08-20/t100500-migration.json`.

The route deliberately retains PR #674's ratio-eight kernel and PR #675's
minimal-wavelet spectral contract. Its new material is the direct compact
Abel–Mertens frame and the critical inverse firewall. The terminal estimate
`MWOC100500` is RH-equivalent and remains open; this packet is not a proof of
RH.

```bash
python3 experiments/X-100500-wavelet-mertens/verify.py \
  --output /tmp/x100500.json
cmp /tmp/x100500.json \
  experiments/X-100500-wavelet-mertens/results/verification.json
sha256sum -c T100500_CONTENT_SHA256SUMS
```

Expected:

```text
PASS_X_100500_MINIMAL_WAVELET_MERTENS_FRAME
```

**MWOC100500 and RH remain unproved; the packet identifies them as equivalent.**
