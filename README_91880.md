# Live arithmetic coupling successor

Date: 2026-08-16  
Repository: `gfreund123/riemann`  
Frozen base: PR #509 at `e01daee9cdfea35d2a7d2591f1df6c8080084119`  
Branch: `research/gpt56-pro/91880-live-arithmetic-coupling`

## Purpose

Instantiate the abstract physical coupling of PR #500 and the native-fibre typing of PR #509 with the actual arithmetic sectors required by review #501.

The construction is deliberately asymmetric:

```text
Volterra bulk:
    exact native Möbius colours;
    rank-one source cancellation;
    terminal positive infinitesimal packet;
    no rough first owner and no causal infinitesimal difference.

anchored finite sector:
    exact native finite occurrences;
    exact P61 stopping-line path;
    unique least rough owner;
    explicit Target-Lorenz terminal leaf from PR #508;
    actual same-index placement;
    no synthetic leaf fixture.
```

The two sectors are assembled by one positive coupling `Gamma_X`, followed by one block-diagonal label-blind quantizer. The same arithmetic coefficients remain visible in target, declared score, every component row, ordinary `q`, ordinary `4q`, radix-four detail, and the final `Y_4` ledger.

The PR #503 witness `(p,y,s,j)=(67,15,1005,14)` is reproduced exactly and is negative for the forbidden infinitesimal causal difference. The compiler rejects that generator by type: Volterra fibres are terminal and rough-free; rough first owners occur only in the anchored finite stopping tree, where the replacement is the finite Target-Lorenz leaf.

## Controlling files

```text
R-91880  exact PR #503 witness and type firewall
L-91880  exact native two-sector source
L-91881  explicit anchored stopping paths and typed leaves
L-91882  actual Gamma_X formula
L-91883  one coefficient ledger through target/score/rows/q4/Y4
L-91884  all-column native slack and endpoint composition
T-91880  complete successor proposal
X-91880  live arithmetic replay
```

## Status

This is a candidate-complete unconditional endpoint proposal on explicitly frozen analytic inputs. It is not an independently accepted proof of RH.