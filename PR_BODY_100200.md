## Purpose

Step back across PRs #658--#675, discard routes now known to be merely
RH-equivalent or false, and push the two strongest genuinely nonredundant
mechanisms to exact terminal arithmetic statements.

```text
base PR #675:
e21383e7522962182491f88301b3cbd375d6d6d0

external phase/quadratic sibling PR #672:
2a351548eb7960ff8ae99f193c10e278984c5657
```

**RH remains unproved.**

## Route A — critical phase-Hasse residual

The quadratic-envelope source is exactly conjugate from `p^(-3/2)` to the
native critical `p^(-1)` geometry. Its kernel splits into a globally increasing
completion and one activation step; the step is exactly the reciprocal prefix.

The complete Cauchy energy of every Hasse edge boundary is proved. A binding
firewall shows that the cube-root residual has no `1-p^(i gamma)` factor and
must be cancelled across distinct squarefree cores.

Open theorem:

```text
PCRP100200
```

## Route B — zero-moment Vaughan dispersion

Apply one additional safe half-order notch to the minimal ratio-eight wavelet.
The resulting ratio-sixteen kernel has a true zero moment and preserves every
off-line reciprocal-zeta pole.

An exact Vaughan identity then proves:

```text
all Type-I ranges = O(X^(-1/6));
only one balanced compact trilinear remains.
```

Open theorem:

```text
BVD100210
```

## Replay

```text
PASS_T100200_CRITICAL_PHASE_AND_VAUGHAN_REDUCTION
0497689eb24ae83f359b3598120e6114b2658f761a6bcad2e94cf3b12c7b440f
```

The replay records both terminal estimates and RH as unproved.
