# T-103100 — Fractional Hankel/Haar assault on the half-completed Vaughan field

**Scientific status:** exact reduction and unconditional diagonal closure; one signed ratio-local off-diagonal estimate remains. RH is not proved.

This packet continues PR #696 at its exact head and attacks `HHFE102010` rather than introducing another detector.

Main advances:

1. an exact full-support Gram formula for the half-completed ratio-four field;
2. an explicit autocorrelation kernel supported on multiplicative ratio four;
3. an unconditional subpower bound for the complete coefficient diagonal;
4. equivalence of `HHFE102010` to one signed off-diagonal near-collision bound;
5. a positive, finite factorization of the fixed dyadic wavelet through arbitrarily narrow multiplicative Haar windows, with only polynomial cost in the refinement depth;
6. an exact gcd/unique-owner expansion leaving one Möbius sign and positive half-divisor weights.

Replay:

```bash
bash experiments/X-103100-fractional-hankel/replay.sh
```
