## Purpose

Pursue two genuinely distinct conclusion-facing routes from the live scalar
research graph, rather than renaming the Hardy/GCD gates that PR #671 proves
RH-equivalent.

```text
base PR #668:
15719b975115ecad1b23264c6a6303899b4c99dc

external sibling PR #670:
f5d37a5f1880749dd33b103d98e2d85bac60ae28
```

**RH remains unproved.**

## Route A — quadratic upper envelope

The globally positive quadratic SHARP transform is compared to its exact
Euler-product asymptote:

```text
E2(X)
 = 16(1-67^(-3/2))/zeta(3/2) * X - H2(X).
```

The packet proves:

- an exact absolutely convergent all-integer kernel;
- an exact future-tail identity involving the critical scalar;
- a Mellin transform analytic on every positive real point and retaining every
  off-line reciprocal-zeta pole;
- global one-prime and two-prime envelope positivity;
- arbitrary fully coactive block positivity;
- an exact finite activation reduction `FEAG99980`.

Thus `FEAG99980 -> RH`. The mixed activation theorem remains open.

## Route B — symmetric phase-Hasse transport

Averaging PR #670's priority flow over all label orders gives one exact
permutation-invariant edge kernel. Its Fourier transform is a closed Euler
product. Every physical potential boundary carries

```text
1-p^(i gamma),
```

so the source-blind neutral cube residual vanishes identically before physical
collapse.

The remaining theorem `PSCP99990` is a source-specific nonzero-phase packing
estimate for this explicit symbol. It is distinct from HTOC/DGOC, whose
root-containing positive forms are proved RH-equivalent in PR #671.

## Replay

```text
PASS_T99980_DUAL_CLOSURE_ASSAULT_ALGEBRA
42933705d7cdc0861b7651efcf6bf9bebe7e71e6a84e54ad7fc0e2d78380dda5
```

## Exact boundary

```text
quadratic envelope transform/pole audit       PROVED EXACT
one-/two-prime and fully active blocks         PROVED EXACT
FEAG99980 mixed activation                     OPEN / CONCLUSION-BEARING

symmetric random-order Hasse flow              PROVED EXACT
closed phase product symbol                    PROVED EXACT
neutral mode annihilation                      PROVED EXACT
PSCP99990 nonzero-phase packing                OPEN / CONCLUSION-BEARING

Riemann Hypothesis                             UNPROVED
```
