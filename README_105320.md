# T-105320 — First-chaos Wick cancellation for the low-order Xi Pick matrix

This checkpoint continues the actual low-order Levinson descent on draft PR
#726. It does not return to the already-closed high-derivative tail.

The new observation is that the reciprocal-log-derivative coefficients needed
by the Xi/Xi-prime Pick matrix are not a new source. For `N>1` they are exactly

```text
b_L(N) = -partial_L C(N;L)/log N,
```

where `C(N;L)` is the frozen xi-prime coefficient family already used by the
formal explicit formula.

The raw reciprocal has an unavoidable degree-one von Mangoldt packet. Its
normalized diagonal energy tends to `1/2` per safe-line side, so the direct
one-percent comparison in T-105310 cannot hold without a carrier removal.

Define the finite, zero-free Wick preconditioner

```text
W_(L,X)(s)=exp[-A_X(s)/(2L)],
A_X(s)=sum_(n<=X) Lambda(n)n^(-s).
```

Then, in the frozen reciprocal model,

```text
L W_(L,X)(s)^2/(L-A_X(s))
 = exp(-x)/(1-x)
 = 1 + sum_(m>=2) c_m x^m,
 x=A_X(s)/L,
 c_m=sum_(j=0)^m (-1)^j/j!.
```

The linear prime chaos vanishes exactly. `W_(L,X)` is entire and nonzero, so
multiplying the Pick observation family by it is a source-fixed congruence and
preserves inertia.

The normalized one-sided coefficient energy has the unconditional limit

```text
D_W=sum_(m>=2) c_m^2 m!/(2m)! < 7/320.
```

After Hermitian two-sided symmetrization, the model mean-square is below
`1+7/160`, so its normalized effective rank is above `160/167`.
A one-percent trace/HS transfer would still give effective rank

```text
(160/167)(99/101)^2 = 1568160/1703567 = 0.920515...,
```

and therefore the multiplicity-robust line proportion

```text
5765136493/8517835000 = 0.676831...,
```

above the published `0.672500703679...` constant.

The existing xi-prime re-expansion additionally proves that the entry-dependent reciprocal coefficient error is `O((log T)^-1/2)` in the critical H3 norm. The remaining theorem `WXFER105320` is therefore the actual contour/Gram transfer: construct the
source-owned Wick-preconditioned contour compression and prove that its trace,
Hilbert--Schmidt norm, boundary terms, entry-dependent freezing error and
canonical-product tail match the frozen model at the required scale.

No new zero proportion and no RH theorem is claimed.
