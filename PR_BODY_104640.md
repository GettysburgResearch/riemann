## T-104640 — binding source-to-inner-factor audit

This checkpoint records one direct attempt to prove the last model-space Gram
estimate in the self-contained \(>90\%\) programme.

**The attempt did not prove more than ninety percent. RH remains unproved.**

## Main correction

For the fractional current,

```text
e^(-delta) (C_sh - declared_tail)
  <= I_sh
  <= C_sh
  <= e^(delta) I_sh + declared_tail.
```

Thus the open fractional residual is quantitatively equivalent to the literal
shallow canonical-correlation charge. It is not a weaker source theorem.

For the projected Krylov family of `L-104633`,

```text
X_H = P_B M_H,
Z_(0,H) = P_A X_H.
```

The zeroth grade is already the exact unrestricted primal minimizer. Hence the
seven-grade least square is exactly the physical inner-factor defect; the
ridge functional is that defect plus a nonnegative regularization term. The
higher six grades create no source-to-inner leverage.

## Exact countermodel

For

```text
F_n(t)=c+cos(nt),  c>1,
```

the exact frozen source coordinate of `L-104637` gives

```text
d=(c w+w^2,1),
n=(1+c w,-w^2),
n=M(X)d
```

with zero source residual and no carrier error. Nevertheless

```text
U_n=-(z+c)/(z(cz+1))
   =-1/[z b_(-1/c)(z)]
```

has numerator inner degree zero, denominator inner degree two, and canonical
charge two per period. The nontrivial pole height is `log(c)/n -> 0`.

Therefore a universal promotion from exact degree-six source transport to a
small physical model-space Gram is false.

## Correct live theorem

The valid conditional \(>90\%\) ledger survives. The live estimate is now
stated without a misleading source-completion layer:

```text
XIMICRO104640:

For the literal Xi fifth-endpoint companion quotient, bound the forced
unmatched shallow endpoint index plus the matched degree-zero
phase/truncated-Toeplitz nonnormality charge below the exact shallow allowance,
retaining common outer factors, confluence, endpoints and the regular-window
ledger.
```

Thresholds:

```text
self-contained alpha_5 > 49/50:  7319/250000 N;
pinned alpha_5 > 0.997:           21701/1000000 N.
```

This is equivalent, up to the already-paid fractional tail, to the existing
`FRACTRANS` / `SHALLOWCORR` / small-regularization Pick formulations. It
remains open.

## Replay

```text
PASS_T104640_SOURCE_INNER_FACTOR_FIREWALL
afeb3d1091ce59a838821a7fd75112e7414a88e44f8819608989c6e5776842ff
checks=19
more_than_ninety_percent_established=false
rh_established=false
```

## Exact status

```text
self-contained >90% numerical ledger                 RETAINED
fractional residual as a weaker source theorem        WITHDRAWN
seven-grade projected Krylov source leverage          WITHDRAWN / GRADE-ZERO EXACT
frozen degree-six Bezout source transport             RETAINED
universal source-to-inner promotion                    REFUTED
XIMICRO104640                                          OPEN / >90%-BEARING
more than ninety percent                               UNPROVED
Riemann Hypothesis                                     UNPROVED
```
