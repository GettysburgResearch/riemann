# T-108310 — All-odd direct companion hierarchy and the endpoint-31 moat

This checkpoint continues T-108300 on the same durable branch.

Exact live inputs:

```text
PR #777 parent head:
34eb08d532de909d6c284b1586778e88b10f871a

PR #772 endpoint-31 input:
93726d7f4a4c17ee530d064c19aa3a4519fd5465
```

For every fixed positive odd `K`, the single entire companion

```text
Xi^(K) + i sigma epsilon Xi
```

counts the two signs of

```text
Xi(c) / Xi^(K+1)(c)
```

at the simple real zeros of `Xi^(K)`. The adverse interval count is at most
twice the minority orientation.

At `K=31`, the unconditional input

```text
alpha_31 > 999/1000
```

and the carrier-adapted source constant

```text
1/(2K) = 1/62
```

leave the exact physical-transfer moat

```text
99/2000 - 1/62 = 2069/62000.
```

The source constant is not promoted to a physical phase bound. The remaining
theorem is `XI31MINPHASE108310`.

Replay:

```bash
python -B experiments/X-108310-all-odd-direct-companion/verify.py
```

Expected:

```text
PASS_T108310_ALL_ODD_DIRECT_COMPANION
dce221b027bd77bb845ffd396b1df4f042f84d27920e0460423fecad0e9b4326
exact_checks=10819
```

```text
general odd-endpoint companion algebra   PROVED EXACT
general minority-transition theorem      PROVED EXACT
endpoint-31 numerical moat                PROVED EXACT
XI31MINPHASE108310                        OPEN
more than 90 percent                      UNPROVED
density one / RH                          UNPROVED
```
