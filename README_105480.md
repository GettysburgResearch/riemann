# T-105480 — F1 Hardy–Gram and signed near-collision frontier

This is the third checkpoint of the present ownership pass on durable draft
PR #730.

Frozen inputs:

```text
PR #730  74a4f6dcad0eabdc98c954ce0e7b46d39875cf32
PR #719  e56c981e2d0639a988f8f54aa362724eb00d9131
PR #751  98af0db6ec7f77d6333a77a3dac53c4698852f43
```

The requested checkpoint-four commit

```text
512ff17dd7880313694c20372ca2b84ad2a5c330
```

is present remotely and remains in the ancestry.

`T-105470` reduced the continuous reflection-Hodge current to one scalar
Hardy sequence.  This checkpoint identifies the strongest elementary square
route and proves exactly where it stops.

For

```text
delta_m = Delta_2 W(m) = H_K(m+)/4,
```

weighted Cauchy gives

```text
( sum |delta_m|/m )^2
  <= (log 2 + O(1/M)) sum |delta_m|^2/m.
```

The continuous logarithmic L2 energy is uniformly equivalent, modulo the
already-closed endpoint jump ledger, to this discrete square energy.

The discrete square has the exact positive-semidefinite Gram expansion

```text
sum_m |delta_m|^2/m
  = sum_(n,r) a_n conjugate(a_r) G_M(n,r),

G_M(n,r)
  = (1/16) sum_(M<=m<=2M) K_L(m/n)K_L(m/r)/m.
```

The Gram vanishes unless `1/8 < n/r < 8`.  Its diagonal is subpower from the
frozen coefficient energy.  Therefore the square gate is equivalent to the
positive part of one signed **distinct-product** near-collision sum.

A binding obstruction is proved.  Even with

```text
sum_n |a_n|^2 = 1
```

and bounded critical coefficients `a_n sqrt(n)`, an all-positive block has
Gram energy `>= c M`.  Thus no source-blind Schur, Bessel, diagonal-energy, or
F1-Hodge norm inequality can prove the square gate.  The signed Boolean
least-prime/physical-product structure is indispensable.

The full logarithmic square also has the exact Mellin–Plancherel form

```text
int_0^infinity |H_K(X)|^2 dX/X
  = (1/2pi) int |Khat_L(t)|^2
      |sum_n a_n n^(-it)|^2 dt.
```

This is the compact multiplicative Nyman/near-collision coordinate of the same
physical source.

The concurrent PR #719 spectral advance is also absorbed.  After critical
conjugation, the complete square route is

```text
M^(1/2) integral Omega_A(t) |Q_U(t)|^2 dt,

Omega_A(t)=|P(1/4+it)|^2 r_A(t)^4 > 0,
Q_U(t)=integral_0^1 (1-theta) S_(U,theta)(t)^2 dtheta.
```

Thus the kernel and detector contribute a strictly positive spectral weight;
the only remaining square amplitude is the literal normal-ordered analytic
source square.  One Beta-weighted fourth moment of the half-source is a valid
stronger sufficient condition, but remains open.

Scientific status:

```text
Hardy l1 <= constant times Gram l2                 PROVED EXACT
continuous/discrete L2 equivalence                 PROVED MODULO CLOSED JUMPS
positive-semidefinite near-collision Gram           PROVED EXACT
diagonal Gram                                      PROVED SUBPOWER
square gate <=> positive signed off-diagonal       PROVED EXACT
Mellin–Plancherel/Nyman coordinate                 PROVED EXACT
positive critical analytic-square weight           PROVED EXACT
half-source fourth moment -> F1 Gram                PROVED EXACT
source-blind Gram closure                          REFUTED WITH LINEAR LOSS
F1FOURTH105483 / F1ASQ2_105483                     OPEN / RH-BEARING
F1GRAM105480 / F1HCNC105481                        OPEN / RH-BEARING
F1HARDY105470                                      OPEN / RH-BEARING
Riemann Hypothesis                                 UNPROVED
```

## Replay

```text
PASS_T105480_F1_HARDY_GRAM
421243172d0f8069a7f4b8969265d99c01dab8df45ac16251357f57fa801e15d

finite checks:       185,819
hostile mutations:       16
```

The retained result object records the finalized checksum and check count.
The replay authenticates finite algebra, Gram positivity, cell-square
localization, the exact counterexample, and hostile firewalls.  It does not
prove `F1FOURTH105483`, `F1ASQ2_105483`, `F1GRAM105480`,
`F1HARDY105470`, `BCI102990`, or RH.
