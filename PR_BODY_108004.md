## Purpose

Dedicated continuation of the native beta–Chebyshev route from exact PR
#758 head `d79692ece0b7604ad309c459f565b24e9926f5c5`.

This PR now contains three source-locked checkpoints:

```text
T-108000  complete limiting-profile Perron boundary law
T-108002  exact finite first-edge cubic-cusp profile
T-108004  exact local Perron–Fourier cusp carrier
```

**RH remains unproved. No new zero-free region is claimed.**

## T-108000 — limiting boundary

The limiting Stieltjes profile has the complete physical boundary law,
including the odd square-root threshold ladder beginning at \(4i\), and no
zero on either physical boundary face.

## T-108002 — finite first edge

For \(n=r+1\) and

\[
 c_n(\lambda)=4i+\lambda n^{-2/3},
\]

T-108002 proves

\[
 n^{-1/3}\Phi_n(c_n(\lambda))\longrightarrow\mathcal A(\lambda),
\]

where

\[
 \mathcal A(\lambda)=
 \frac{36\sqrt2}{\pi^3}e^{-i\pi/4}
 \int_0^\infty u^{-1/2}
 e^{-\pi\lambda u/4+i\pi^3u^3/24}\,du.
\]

The edge has width \(n^{-2/3}\), height \(n^{1/3}\), and a cubic-cusp/Fresnel
rather than Airy local model.

## T-108004 — the missing Fourier coordinate

The assembled beta Perron formula contains the tilted Fourier kernel, not
only its value at \(t=0\). Define

\[
 \Psi_n(c,t)=\frac1{nc\kappa_r}
 \int_{\mathbb R}R_r(x)e^{-nc|x|/2}e^{-itx}\,dx.
\]

There is an exact finite identity

\[
 \boxed{
 \Psi_n(c,t)=\frac12\left[
 \frac{c+2it/n}{c}\Phi_n(c+2it/n)
 +\frac{c-2it/n}{c}\Phi_n(c-2it/n)
 \right].}
\]

At the first edge, the forced Fourier scale is \(t=\tau n^{1/3}\). For
\(\lambda>0\), locally uniformly on compact \((\lambda,\tau)\)-sets,

\[
 \boxed{
 n^{-1/3}\Psi_n(4i+\lambda n^{-2/3},\tau n^{1/3})
 \longrightarrow\mathcal C(\lambda,\tau),}
\]

with

\[
 \boxed{
 \mathcal C(\lambda,\tau)
 =\frac12\bigl(\mathcal A(\lambda+2i\tau)
 +\mathcal A(\lambda-2i\tau)\bigr).}
\]

Equivalently,

\[
 \mathcal C(\lambda,\tau)=
 \frac{36\sqrt2}{\pi^3}e^{-i\pi/4}
 \int_0^\infty u^{-1/2}
 e^{-\pi\lambda u/4+i\pi^3u^3/24}
 \cos(\pi\tau u/2)\,du.
\]

Its exact inverse-Fourier carrier is

\[
 \boxed{
 H_\lambda(v)=
 \frac{72}{\pi^{5/2}}e^{-i\pi/4}|v|^{-1/2}
 e^{-\lambda|v|/2+i|v|^3/3}.}
\]

Thus the local source coordinate is

```text
t = tau n^(1/3)
<->
v = n^(1/3) log(m/n),
```

and the arithmetic problem is now a signed beta correlation against the
literal cubic-phase carrier, with all four primitive local states assembled
before any absolute value.

## Next exact gate

```text
BETACUSPCORR108006

Prove a signed estimate for

  C(lambda,tau)
  B_beta(s_n-i tau n^(1/3))
  B_beta(s_n+i tau n^(1/3))

on the full local tau window and pay the outer-frequency tails. Equivalently,
work in primitive-pair coordinates against

  |v|^(-1/2) exp(-lambda |v|/2+i|v|^3/3).
```

## Replay and boundary

```text
PASS_T108000_BETA_CHEBYSHEV_PERRON_EDGE_BOUNDARY
PASS_T108002_BETA_CHEBYSHEV_CUBIC_CUSP_EDGE_PROFILE
PASS_T108004_BETA_CHEBYSHEV_PERRON_FOURIER_CUSP_CARRIER
```

The T-108004 producer authenticates six predecessor blobs, checks the exact
frequency split by an independent cell-pair integral, checks the dual
Fourier carrier, and replays the finite two-parameter edge at
\(n=32,64,128,256\) for \(\tau=0,1,2\).

```text
limiting physical boundary law           PROVED
finite scalar cubic-cusp edge             PROVED
full local Perron–Fourier cusp carrier    PROVED
exact cubic-phase log-ratio dual           PROVED
direct lambda=0, tau!=0 boundary theorem OPEN
signed beta-source correlation            OPEN
full contour / outer-frequency control    OPEN
new zero-free half-plane                   NOT PROVED
RH                                         UNPROVED
```
