# T-105114 anchored equal-disk minimum-modulus transfer

This checkpoint closes the finite analytic part of the simultaneous collar
handoff. After normalizing a holomorphic function at a nonzero anchor, it
proves

\[
\log\left|\frac{f(z)}{f(a)}\right|
\ge-
\left[
\frac{2r_1}{r_2-r_1}
+\frac{\log(2/\varepsilon)}{\log(r_3/r_2)}
\right]A_f
\]

outside one radius-\(\varepsilon r_2\) disk per zero in \(D(a,r_2)\).
Jensen gives the exact nominal radius budget

\[
S_f\le
\frac{\varepsilon r_2A_f}{\log(r_3/r_2)}.
\]

For the raw residue quotients, only zeros of \(F'\) and \(F''\) enter the
exceptional union; zeros of \(F\) are harmless. The sharp projection gate
\(2S<\min(\Delta_T,\Delta_\eta)\) then selects one common disk-safe
rectangle.

Exact replay:

    python -B experiments/X-105114-anchored-equal-disk-collar/tests/test_verify.py
    python -B -O experiments/X-105114-anchored-equal-disk-collar/tests/test_verify.py

Expected:

    PASS_T105114_ANCHORED_EQUAL_DISK_COLLAR
    16 / 16 in normal and optimized Python
    digest 15ff7ba8056a9beb5e942945c9f92dcce566f7d737bc04f8f2db6895109c3284

The proof object uses exact rational geometry and symbolic analytic
identities. It runs no Xi evaluation, root search, contour quadrature, or
heavy computation.

The Xi specialization remains conditional on off-center anchor values,
cofinal derivative growth, actual-pole manifests, and selector/margin
absorption. RCMV104530 and RH remain open.
