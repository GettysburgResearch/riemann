# T104600 — Persistence–Mellin reverse Rolle

This add-only continuation of PR #720 replaces the phase-drift producer by two
source-side routes.

## Exact new theorems

```text
G_y-W_y=N_y/2 at every regular amplitude threshold;
every wrong extremum pairs with a larger good extremum;
all positive signed amplitude moments are positive coarea integrals;
the normalized Mellin hierarchy is completely monotone and Hankel positive.
```

## Fixed-order routes

```text
EXCUR104600
  unsigned amplitude retention + excursion component complexity;

PVAR104600
  explicit p-variation + one positive marked critical moment.
```

Either route, with margin `eta>0`, gives

```text
alpha_2 >= eta alpha_3 > 0.9873 eta.
```

`UPHASE104590` remains an exact coordinate but is now classified as a detector:
its phase lower bound is the parent real-zero count up to bounded endpoints.

```text
EXCUR104600            OPEN
PVAR104600             OPEN
alpha_2 from alpha_3   NOT YET ESTABLISHED
RH                     UNPROVED
```
