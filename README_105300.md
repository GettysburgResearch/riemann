# T-105300 — Low-order Levinson–Hermite–Pick frontier

This continuation attacks the actual low-order descent rather than extending
the already-summable high-derivative tail.

The main object is the coefficient-computable trace form

```text
A_p = R[x]/(p')
q_p = -p/(p'') mod p'
B_p(u,v) = Tr_A(q_p u v)
H_p = <1> direct-sum B_p.
```

For a finite real squarefree polynomial with squarefree derivative,

```text
signature(H_p) = number of real roots of p.
```

The same construction localizes directly to a regular entire-function rectangle:

```text
B_ij(F;Omega) = -(2 pi i)^(-1) integral_(boundary Omega)
                 [F(z)/F'(z)] z^(i+j) dz.
```

Its signature is the number of good real extrema minus wrong real extrema;
nonreal critical pairs are exact hyperbolic blocks and cancel from the
signature. Source-owned analytic compressions obey an endpoint-aware
rank--trace real-zero lower bound.

At real critical points the trace form records the sign of `-p/p''`; every
nonreal critical pair contributes one positive and one negative square. Thus
wrong extrema and off-real derivative pairs are handled on one exact inertia
ledger.

A sign-blind polynomial `w` gives a congruent preconditioned form. Its
rank–trace statistic supplies

```text
real-root proportion >= 2 Theta(p,w)-1.
```

The threshold `Theta>0.83625` would beat the frozen public `0.67250` zeta record.
Using Anthropic's independent `0.86864` Xi-prime theorem, a `9/10` good-extrema
or coherence bound would yield `0.694912` on the line.

Neither asymptotic matrix estimate is proved. RH remains unproved.
