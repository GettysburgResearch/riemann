# T-105300 — Xi moderate saddle and residue spectrum

**Scientific status:** major proposed unconditional advance; independent
analytic review required; `CRDB105200` and RH remain unproved.

This is the dedicated multi-pass continuation branch requested after the
2026-08-22 integration. It is stacked on the live natural-scale Xi programme
and will be updated rather than replaced by a new PR on every pass.

## New results

```text
finite critical residues
 -> one quotient-algebra multiplication operator
 -> all residue moments and resultant characteristic polynomial;

positive Xi tilted measure
 -> cubic-corrected relative complex saddle
 -> common height (M/log M)^(2/3)
 -> all high derivatives real-rooted there
 -> critical residues = -w_m^-2(1+o(1))
 -> residue coherence = 1-o(1).
```

The derivative entry order for an original height `T` is reduced from the
parent natural-scale `T^2 log T` to

```text
T^(3/2) log T.
```

The improvement is proposed complete but requires hostile independent review
of the contour shift and uniform tails.

## Load-bearing files

1. `claims/lemmas/L-105300-critical-residue-quotient-algebra-and-resultant.md`
2. `claims/lemmas/L-105301-moderate-deviation-complex-saddle-for-xi-derivatives.md`
3. `claims/lemmas/L-105302-moderate-height-xi-tail-and-residue-coherence.md`
4. `claims/refutations/R-105300-gaussian-only-and-high-tail-descent-shortcuts.md`
5. `claims/theorems/T-105300-moderate-saddle-residue-spectrum-frontier.md`
6. `claims/methodology/M-105300-hostile-review-contract.md`
7. `experiments/X-105300-critical-residue-quotient-algebra/`

## Exact replay

```bash
python -B experiments/X-105300-critical-residue-quotient-algebra/verify.py \
  --output experiments/X-105300-critical-residue-quotient-algebra/results/verification.json
```

Expected:

```text
PASS_X_105300_CRITICAL_RESIDUE_QUOTIENT_ALGEBRA
3a61bc251360b0d165f30b405c3b6d2ec68bdccc1e133e69f9807ad188f7536c
RH_UNPROVEN
```

The replay authenticates exact finite algebra only. It does not certify the Xi
saddle analysis, `CRDB105200`, or RH.
