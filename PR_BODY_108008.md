## Purpose

Dedicated continuation of the native beta–Chebyshev route from exact PR
#758 head `d79692ece0b7604ad309c459f565b24e9926f5c5`.

This PR now contains five source-locked checkpoints:

```text
T-108000  complete limiting-profile Perron boundary law
T-108002  exact finite first-edge cubic-cusp profile
T-108004  exact local Perron–Fourier cusp carrier
T-108006  exact beta-even chiral source front door
T-108008  exact literal-source Cauchy and diagonal/off-diagonal decomposition
```

**RH remains unproved. No new zero-free region is claimed.**

## Earlier checkpoints

T-108000 proves the complete physical boundary law of the limiting
Chebyshev Stieltjes profile. T-108002 proves the finite first-edge chart with
width `N^(-2/3)`, height `N^(1/3)`, and cubic-cusp/Fresnel profile. T-108004
restores the full local Fourier coordinate

\[
 t=\tau N^{1/3}
\]

and identifies the exact dual lag carrier

\[
 H_\lambda(v)=\frac{72}{\pi^{5/2}}e^{-i\pi/4}|v|^{-1/2}
 e^{-\lambda|v|/2+i|v|^3/3}.
\]

T-108006 then proves, by beta-pair evenness, that the symmetric cusp integral
may be represented by one directed branch before absolute value. T-108008
source-locks that chiral front door, corrects its prose-level source-lag sign
(the stated z+2it face selects m>k), and gives it the complete literal
primitive-pair and diagonal/off-diagonal coordinates below.

## T-108008 — literal beta source before every norm

For

\[
 s={1+z\over2},\qquad
 F_z(t)=B_\beta(s-it)B_\beta(s+it),
\]

put

\[
 P_z(u)={1\over2\pi}{z\over u^2+z^2/4}.
\]

The new theorem proves the exact transfer

\[
 \boxed{
 (\mathcal P_zF_z)(\xi)
 =\sum_{m,k\ge1}
 {\beta(m)\beta(k)\over\sqrt{mk}\max(m,k)^z}
 e^{i\xi\log(m/k)}.}
\]

For the finite Chebyshev jump polynomial \(Q_N\), the assembled Perron form is
therefore

\[
 \boxed{
 {1\over2\pi}\int_{\mathbb R}
 {|Q_N(\xi)|^2\over\xi^2}
 (\mathcal P_zF_z)(\xi)\,d\xi.}
\]

The literal primitive-pair source is now inserted before any source norm or
channelwise absolute value.

### Exact diagonal subtraction

The diagonal is

\[
 D_\beta(z)=\sum_n{\beta(n)^2\over n^{1+z}}
 ={1+4x+x^2\over1+x}{\zeta(1+z)\over\zeta(2+2z)},
 \qquad x=67^{-(1+z)}.
\]

It contributes exactly \(D_\beta(z)\), independent of the finite edge order.
Thus the `N^(1/3)` cubic cusp does not amplify the diagonal; the remaining
RH-bearing term is entirely off-diagonal.

### Exact two first-edge channels

At

\[
 z_N=4iN+\sigma,\qquad\sigma=\lambda N^{1/3},
\]

\[
 P_{z_N}(t-\xi)
 ={1\over2\pi i}
 \left[
 {1\over t-\xi+2N-i\sigma/2}
 -{1\over t-\xi-2N+i\sigma/2}
 \right].
\]

The `+2N` and `-2N` finite spectral packets are therefore transported to the
same local source window and must be assembled before absolute value.

### Exact physical source coordinate

The sharp beta field also has the exact signed-cell forms

\[
 G_{N,X}(u)=\sum_jq_jA_X(u-y_j)
 =\sum_{j=0}^{N-1}(-1)^j
 \sum_{e^{u-y_{j+1}}<m\le e^{u-y_j}}{\beta(m)\over\sqrt m},
\]

and

\[
 G^\beta_{N,X}(u)
 =G^\mu_{N,X}(u)
 -67^{-1/2}G^\mu_{N,\lfloor X/67\rfloor}(u-\log67).
\]

## Next exact gate

```text
BETACHIRALOFFDIAG108010

Subtract D_beta(z) exactly. On t=tau*N^(1/3), prove a signed bound for the
complete off-diagonal source after the +2N and -2N Cauchy channels and all
exceptional-prime local states have been assembled. Equivalently estimate
m != k against the exact cubic lag carrier, then pay the outer-tau ranges.
```

## Replay and boundary

```text
PASS_T108000_BETA_CHEBYSHEV_PERRON_EDGE_BOUNDARY
PASS_T108002_BETA_CHEBYSHEV_CUBIC_CUSP_EDGE_PROFILE
PASS_T108004_BETA_CHEBYSHEV_PERRON_FOURIER_CUSP_CARRIER
T-108006 CHIRAL SOURCE FRONT DOOR (authenticated exact source lock)
PASS_T108008_BETA_CHIRAL_SOURCE_CAUCHY_DECOMPOSITION
```

The T-108008 replay authenticates eight exact predecessor blobs, executes 70
finite checks, and includes exact rational cell-current and exceptional Euler
factor fixtures.

```text
limiting physical boundary law             PROVED
finite scalar cubic-cusp edge               PROVED
full local Perron–Fourier cusp carrier      PROVED
beta-even chiral source reduction           PROVED
literal beta-source Cauchy transfer         PROVED
exact diagonal subtraction                  PROVED
first-edge two signed channels              PROVED
signed off-diagonal cusp estimate           OPEN
full contour / outer-frequency control      OPEN
new zero-free half-plane                    NOT PROVED
RH                                           UNPROVED
```
