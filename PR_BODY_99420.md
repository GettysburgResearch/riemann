## Purpose

Stack on PR #641 and close its remaining accumulated signed-ledger interface.

```text
base PR:      #641
base SHA:     19cd3939a54ccea73b055b3952b5dd7ed638c4fb
head branch:  research/gpt56-pro/99420-calibration-coboundary-closure
```

**RH remains unproved pending hostile independent reconstruction.**

## Single new idea

The endpoint calibration is a primitive potential, not a generationwise debt.
If the exact frame and positive frame are

```text
E=P+A,
P=J+PT,
```

then

```text
E=J+ET+(A-AT),
E=J(I-T)^(-1)+A.
```

All descendant calibration terms telescope exactly.  Only the bounded compact
root calibration remains.

## Exact consequences

- reconstructs PR #641's signed local identity canonically;
- removes the geometric `1/(1-kappa)` calibration accumulation;
- derives `d(Vf)=x d[x^(-1/2)(D-1)f]`, so all knot atoms and two boundary modes
  are one first-order finite-variation potential;
- respects the PR #561 accumulated-parity firewall;
- never promotes the PR #512/#514 negative oriented child;
- leaves one nonnegative fixed row modulo an `O_j(1)` holomorphic defect;
- preserves every reciprocal-zeta pole for the fixed-row Landau consumer.

## Replay

```text
PASS_T99420_CALIBRATION_COBOUNDARY_CLOSURE_ALGEBRA
```

The replay checks exact algebra and mutations. It does not replay the frozen
compact Hall or endpoint-frame directed campaigns and does not establish RH.

## Status

```text
calibration coboundary                 proved exact
distributional first-order potential   proved exact
descendant calibration cancellation   proved exact
root fixed-row boundedness             proved on frozen compact inputs
candidate composition                  complete on frozen inputs
accepted proof of RH                   no
Riemann Hypothesis                     unproved pending review
```
