## Purpose

Hostilely attempt the requested proofs of the two minimal implication-matrix
classes:

```text
CV  via the literal adaptively squared-core collar;
XD  via the largest-prime / short-long hybrid.
```

Frozen base:

```text
PR #694
e6d923c1069f5a481abdf0769ca6d870a8b5ae4b
```

**RH remains unproved.**

## Exact CV disposition

The adaptively completed small-prime core plus depth-one unsquared collar is
pointwise nonnegative. Its one-sided negative variation is exactly zero.

That observable is not the fixed CV scalar. Finite desmoothing is signed, and
the cutoff depends on `X`; a two-state exact counterexample and the fixed-Mellin
firewall reject the missing implication.

## Exact XD disposition

The long sector is eventually positive. The short-sector energy is false
because it contains the negative singleton-prime carrier

```text
-kappa_0 sqrt(X)/log X.
```

The opposite carrier lives in the long sector. Orthogonal projection onto the
physical sum channel cancels both and returns exactly `G_mu`.

## New common bridge

```text
D_dyadic h_beta
  = (I-67^(-1/2) S_67) D_log G_mu.
```

Thus CV and XD meet at one signed carrier-preserving desmoothing/cross-core
interface.

## Replay

```text
PASS_T101100_CV_XD_CARRIER_HARDENING
3ea506591d0e96c573700862e699c80b24948592ad7c0ee7cc0ea5162a236585
```

## Boundary

```text
completed squared-core collar variation     PROVED ZERO
long XD sector negative variation            PROVED ZERO EVENTUALLY
desmoothing to fixed CV                       OPEN / RH-EQUIVALENT
carrier-preserving XD covariance              OPEN / RH-EQUIVALENT
Riemann Hypothesis                            UNPROVED
```
