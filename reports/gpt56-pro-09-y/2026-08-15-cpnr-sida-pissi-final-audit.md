# CPNR / SIDA / PSSI closure attack

## Verdict

The three requested statements do not all survive.

```text
CPNR_67   candidate-complete on the repaired frozen factor-67 stack;
SIDA_d    exact corollary after the Route-A endpoint conclusion;
PSSI      false as a positive parallel Stieltjes identity.
RH        not treated as accepted before independent reconstruction of Route A.
```

## Main new estimate

An elementary Chebyshev argument proves

```text
psi(x)<4 log(2) x,
J_Lambda(X)<16 log(2) sqrt(X).
```

Therefore the stronger square-root thinning of PR #479 has exact native cost below `12012`, removing the final apparent `O(log^2 X)` payment.

Combined with all-column reserve, activation-knot collars, mass-normalized children and the one uncolored common port, this gives one literal residual

```text
r_X=Omega_X-Xi(current)-sum alpha U Omega_child >=0
```

with bounded `Y4` pairing on the frozen stack. The exact native slack cocycle then gives `Delta_X=O(1)`.

## Route B

If the frozen endpoint consumer accepts that Route-A packet, RH follows and the positive-depth hyperbolic measure is zero. The native radial defect is Lipschitz, so every shrinking interval carries `O(|I|)` source mass. This gives the singular SIDA allocation exactly, though not an independent pre-RH operator domination theorem.

## Route C firewall

The proposed PSSI cannot hold. PSSI would make the Xi admittance Stieltjes, hence RH, hence a purely atomic zero measure. Its positive native-defect branch has a nonzero absolutely continuous measure. Uniqueness of Stieltjes measures forbids such a positive parallel decomposition.

The passive fallback must be a transfer/Schur realization, not a parallel sum.

## Frozen heads

```text
PR #473 71d6a859ea741fe035de709e8d10ed37301b778e
PR #476 9f16ce483954d4233b68ee09cb6bec47400aa3cc
PR #477 5acd9007b4f4bb1792466f5013c39bf4eac33f9e
PR #479 518b6a5ec2b49b7decbd4c2e349d0ee5b26bfc9b
```
