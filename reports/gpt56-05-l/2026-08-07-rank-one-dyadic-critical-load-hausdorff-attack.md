# Full-problem global attack — rank-one Weil ray, dyadic Chebyshev energy, critical-load transport, and Hausdorff parabolic cone

Date: 2026-08-07  
Agent: `gpt56-05-l`  
Intended branch: `agent/gpt56-05-l/154-nonlocal-barta-floor` / PR #165  
Status: **new claims are PROPOSED pending independent review; RH is not claimed proved or disproved**

## Executive result

This pass deliberately abandoned the progressively narrowing finite-packet queue and re-read the strongest repository routes as one global programme.

The repo now contains four mature coordinates of the same RH-sensitive object:

```text
square screw / D-0001 constant coordinate,
terminal-prime Hardy energy and finite prime-pair Gram,
central Hausdorff/Stieltjes rows,
original K0 / complete Loewner kernel.
```

The new work supplies four global advances.

1. **Rank-one Weil compression (`T-15411`).** The square-screw scalar is exactly the complete Weil energy of one centered interval indicator, normalized by its `L2` mass, and exactly the constant D-0001 coordinate.
2. **Raw dyadic Chebyshev square function (`T-15412`).** One elementary raw-prime signal
   
   \[
   Q_\diamond(\log Y)=\frac{\psi(Y)-4\psi(Y/4)}{\sqrt Y}
   \]
   
   has weighted-Hardy and cumulative-energy exponent exactly `Theta_zeta`. Hence
   
   \[
   \mathrm{RH}
   \iff
   \int_1^Y\left|\frac{\psi(t)}t-\frac{\psi(t/4)}{t/4}\right|^2dt
   =Y^{o(1)}.
   \]
3. **Critical-load deposition transport (`L-15439`).** The square-screw second derivative is one explicit positive archimedean density minus the complete prime-power deposition measure. In the square-root coordinate the positive service density is
   
   \[
   2\left(1-\frac1{x^2(x^4-1)}\right)dx,
   \]
   
   while the average prime demand is asymptotically exactly `2 dx`: the problem is a critical-load stop-loss transport, not a loose PNT domination.
4. **Uniform Hausdorff parabolic-cone proof candidate (`L-21702`).** A uniform saddle argument extends the fixed-row result proposed on PR #217:
   
   \[
   \frac m{\sqrt k}\to0
   \quad\Longrightarrow\quad
   \mathcal H_{m,k}(R)=I_{m,k}(R)(1+o(1))>0.
   \]
   
   In particular there are constants `C_R,K_R` such that every row with
   
   \[
   k\ge C_Rm^2,
   \qquad k+m\ge K_R
   \]
   
   is predicted to be unconditionally positive; the uniform tail estimates remain explicitly pending independent verification.

These are not a proof of RH. They do, however, replace the vague phrase “the final sign remains” by two direct global arithmetic targets and one sharply isolated moment transition strip.

## Parallel-branch reconciliation

During this pass PR #165 advanced from `c3c6c98a580277e22a04790222989bf3816f2ef2` through the endpoint-collapse head and then to the re-queried live head `de31a1df1f6011f306eb3f00ff6ba212a1421d09`. The live branch now contains:

```text
L-15433--L-15435  endpoint collapse and completed-xi ratio defect,
L-15436--L-15438  fixed-shift and small-shift boundary-layer asymptotics.
```

The present continuation is additive rather than substitutive. The critical-load claim is allocated `L-15439`, the two new theorems are allocated `T-15411` and `T-15412`, and the exact regression is allocated `X-15414`; this report uses a separate filename so none of the parallel operator/total-positivity work is overwritten. The combined global picture is:

```text
operator/Mellin side:
    endpoint channel collapses to one positive rank-one trace;
    regular tail becomes a completed-xi complete-monotonicity problem;

prime/Weil side:
    the same obstruction appears on one interval ray;
    one dyadic Chebyshev square function measures its exact exponent;
    the square-screw curvature is a critical-load prime deposition queue;

moment side:
    the proposed parabolic saddle cone removes an exterior region of the
    all-order Hausdorff hierarchy.
```

# 1. Live repository synthesis

## 1.1 Square-screw / D-0001 route

PR #202 supplies the square-sampling Landau transfer and the exact rightmost-zero exponent

\[
\Theta_\zeta
=
\limsup_{M\to\infty}
\frac{\log(1+[-\Psi(2\log M)]_+)}{2\log M}.
\]

PR #208 proves the term-by-term identity

\[
\Psi(2\log M)
=
\log M\,e_0^TA_{N,M^2}e_0.
\]

Thus the scalar square-screw and the matrix route share one immutable principal coordinate.

## 1.2 Complete finite-kernel route

The independently reviewed chain

```text
#192 canonical deficit augmentation
-> #199 off-line Xi-cardinal survives Schur elimination
-> #204 exact local Möbius radical extension
-> #206 conditional line frames and joint residual
```

removes qualitative complement, capture, and synthesis-existence ambiguity. Its surviving theorem is a cofinal form/metric sign estimate on the complete corrected kernel. The new `T-15411` shows that false RH must also generate polynomially deep negative excursions in the much simpler interval ray; the negative defect cannot remain hidden only in a sophisticated packet.

## 1.3 Global prime-energy route

PR #216 constructs a fixed compact safe window and proves

\[
\Theta_\zeta
=
\limsup_{X\to\infty}
\frac{\log(1+\int^X|Q_G(x)|^2dx)}{2X}.
\]

It reduces the energy to one exact finite bounded-ratio prime-pair Gram and identifies the positive off-diagonal coherence as the full exponent carrier. `T-15412` supplies a second, even more elementary prime coordinate: one dyadic difference of the ordinary Chebyshev function.

## 1.4 Spectral-charge / Hausdorff route

PR #217 proves the exact screw–Stieltjes transform and proposes a uniform parabolic saddle sector in the all-order Hausdorff hierarchy. `L-21702` supplies a complete proposed argument for that uniform sector, pending adversarial review of its two tail estimates. The remaining moment difficulty is no longer the whole quadrant, but a moving diagonal transition strip where the saddle lies above the verified line-zero block and is too narrow for coarse zero-density smoothing.

## 1.5 Original `K0` / Loewner route

The operator branch identifies

\[
K_0\succeq0
\iff
L_\Xi\succeq0
\iff
\mathrm{RH},
\]

and reduces the internal normalized proof to a positive augmented Mellin/Green defect. This remains a legitimate one-stroke endpoint, but the raw Gram shortcut and endpoint-repair shortcuts have been refuted. Any future factorization must reproduce the exact rank-one interval scalar of `T-15411`.

# 2. Eureka I — one interval ray already carries the full obstruction

For

\[
v_t=\mathbf1_{[0,t]},
\qquad
w_t=\mathbf1_{[-t/2,t/2]},
\]

and the centered Weil form,

\[
Q_W(v_s,v_t)
=
\Psi(s)+\Psi(t)-\Psi(s-t).
\]

Hence

\[
Q_W(w_t,w_t)=2\Psi(t),
\qquad
\|w_t\|_2^2=t.
\]

At the square support `t=2 log M`,

\[
\boxed{
\frac{Q_W(w_{2\log M},w_{2\log M})}
     {\|w_{2\log M}\|_2^2}
=
\frac{\Psi(2\log M)}{\log M}
=
e_0^TA_{N,M^2}e_0.}
\]

Therefore

\[
\mathrm{RH}
\iff
Q_W(w_{2\log M},w_{2\log M})\ge0
\quad\text{eventually}.
\]

If RH is false and `theta<Theta_zeta`, then

\[
\frac{[-Q_W(w_{2\log M},w_{2\log M})]_+}{M^{2\theta}}
\]

is unbounded on every integer tail.

This is a genuine global witness-extraction theorem: a hypothetical off-line zero must force polynomially deep negativity on one explicit one-dimensional family.

# 3. Eureka II — the dyadic Chebyshev square function

Choose

\[
g(u)=e^{-u/2}\mathbf1_{u\ge0},
\qquad
G_\diamond(u)=g(u)-2g(u-\log4).
\]

Then

\[
\widehat G_\diamond(z)
=
\frac{1-2\,4^{-z}}{z+1/2}.
\]

Its added zeros lie only on `Re z=1/2`; it cancels the zeta pole and no point in the open counterexample strip.

The raw prime signal is exactly

\[
\boxed{
Q_\diamond(\log Y)
=
\frac{\psi(Y)-4\psi(Y/4)}{\sqrt Y}.}
\]

Writing `C(Y)=psi(Y)/Y`, one gets the exact square identity

\[
\boxed{
\int_0^{\log Y}|Q_\diamond(x)|^2dx
=
\int_1^Y|C(t)-C(t/4)|^2dt.}
\]

Hardy half-plane theory gives

\[
\boxed{
\Theta_\zeta
=
\limsup_{Y\to\infty}
\frac{
 \log\left(1+
 \int_1^Y|C(t)-C(t/4)|^2dt
 \right)}{2\log Y}.}
\]

Therefore the exact remaining arithmetic theorem can be written with no zero data and no special functions:

\[
\boxed{
\int_1^Y
\left|
\frac{\psi(t)}t-
\frac{\psi(t/4)}{t/4}
\right|^2dt
=Y^{o(1)}.}
\]

The same exponent equals the classical Chebyshev mean-square exponent

\[
\int_1^Y\frac{|\psi(t)-t|^2}{t^2}dt,
\]

because the dyadic delay filter is boundedly invertible on every subcritical exponentially weighted `L2` space.

## 3.1 Exact renewal

For the centered prefix error

\[
F(x)=e^{-x/2}\psi(e^x)-e^{x/2},
\]

one has, beyond a fixed initial interval,

\[
Q_\diamond(x)=F(x)-2F(x-\log4),
\]

and the PNT gives the exact anti-causal renewal

\[
\boxed{
F(x)
=-\sum_{j\ge1}2^{-j}
Q_\diamond(x+j\log4).}
\]

## 3.2 Simple bounded-ratio kernel

The full autocorrelation of the exponential filter is

\[
C_\diamond(r)
=
\begin{cases}
4e^{-|r|/2}-e^{|r|/2},&|r|\le\log4,\\
0,&|r|\ge\log4.
\end{cases}
\]

After the prime weights are inserted, a pair `n<=m<=4n` carries the exact coefficient

\[
\boxed{
\Lambda(m)\Lambda(n)
\left(\frac4m-\frac1n\right),}
\]

and pairs outside ratio four do not interact in the stationary kernel. This is a particularly clean interface for a twisted Selberg/dispersion attack. Boundary truncations must be retained; the stationary kernel alone is not a cumulative upper bound.

# 4. Eureka III — critical-load prime deposition

The exact screw formula can be written

\[
\Psi(t)
=
\mathcal A(t)
-
\sum_{\log q\le t}
\frac{\Lambda(q)}{\sqrt q}(t-\log q),
\]

with

\[
\mathcal A''(t)
=
e^{t/2}
-
\frac{e^{-5t/2}}{1-e^{-2t}}.
\]

The curvature is positive for `t>=log 2`. In the square-root coordinate `x=e^(t/2)`,

\[
\boxed{
\mathcal A''(t)dt
=
2\left(1-\frac1{x^2(x^4-1)}\right)dx.}
\]

A prime power `q=p^k` deposits mass

\[
\frac{\log p}{\sqrt q}
\]

at `x=sqrt(q)`. The mean arrival demand of ordinary primes is asymptotically exactly the service density `2 dx`. The scalar problem is therefore a **critical-load queue**.

From any base `X0`, define the exact signed queue `J(X)` by cumulative service minus cumulative prime demand. Then

\[
\boxed{
\Psi(2\log X)
=
\Psi(2\log X_0)
+2\int_{X_0}^X\frac{\mathcal J(y)}y\,dy.}
\]

The queue itself may be negative. The exact condition is nonnegative logarithmically weighted reserve; a fixed negative base value contributes only one explicit additive constant.

## 4.1 Convex allocation theorem

If each prime atom is assigned a disjoint positive curvature submeasure with at least its mass and with barycenter no later than the prime deposition time, Jensen's inequality for `(t-u)_+` proves the complete tail sign and hence RH.

## 4.2 One-sided queue-energy route

Let `J_-` be the negative queue part. Then

\[
[-\Psi(2\log X)]_+
\le
2\sqrt{\log(X/X_0)}
\left(
\int_{X_0}^X\mathcal J_-(y)^2\frac{dy}{y}
\right)^{1/2}.
\]

Thus

\[
\boxed{
\int_{X_0}^X\mathcal J_-(y)^2\frac{dy}{y}
=X^{o(1)}
\Longrightarrow
\mathrm{RH}.}
\]

This is weaker than controlling the full queue energy because positive excursions are free.

# 5. Breakthrough on the Hausdorff route

For

\[
x_\rho=(\gamma-ia)^{-2},
\qquad
\mathcal H_{m,k}(R)
=
\sum_{\Im\rho>0}m_\rho x_\rho^m(R-x_\rho)^k,
\]

let

\[
M_{m,k}(R)
=
\sum m_\rho\gamma^{-2m}(R-\gamma^{-2})^k.
\]

The beta saddle is

\[
t_*^2=\frac{k+m}{Rm},
\]

with zero-ordinate width

\[
\Delta t_*\asymp\frac{\sqrt k}{m}.
\]

Consequently the Riemann–von Mangoldt remainder is lower order precisely when

\[
\frac m{\sqrt k}\to0.
\]

The complex displacement ratio has a first-order phase

\[
2q\left(m-\frac{ky}{1-y}\right),
\]

which vanishes exactly at the beta saddle. Uniform beta concentration then gives

\[
\boxed{
\mathcal H_{m,k}(R)
=
I_{m,k}(R)(1+o(1))>0
\quad
(m/\sqrt k\to0).}
\]

The uniform estimates, if independently confirmed, extract an unconditional parabolic cone

\[
\boxed{k\ge C_Rm^2}
\]

outside a finite initial region.

## 5.1 A stronger transition clue

At the saddle, the logarithm of the complex displacement ratio has positive quadratic modulus term

\[
\frac{2m(k+m)}kq^2.
\]

The variance of the first-order phase across the beta saddle produces the Gaussian loss

\[
\frac12
\left(2q\frac{(m+k)^2}{k}\right)^2
\operatorname{Var}(y)
\sim
\frac{2m(k+m)}kq^2.
\]

These cancel to leading order. This is a major clue for the remaining diagonal strip: the full complex saddle should be evaluated before modulus and phase are charged separately.

# 6. Unified rightmost-zero invariant

Subject to independent verification of the named transfer claims, the repository now has

\[
\boxed{
\begin{aligned}
\Theta_\zeta
&=
\limsup_M
\frac{\log(1+[-\Psi(2\log M)]_+)}{2\log M}\\
&=
\limsup_M
\frac{\log(1+\log M[-e_0^TA_{N,M^2}e_0]_+)}{2\log M}\\
&=
\limsup_Y
\frac{\log(1+\mathcal E_\diamond(Y))}{2\log Y}\\
&=
\limsup_X
\frac{\log(1+\int^X|Q_G(x)|^2dx)}{2X}\\
&=
\text{the unit-block exponent of the centered infinite-notch residual.}
\end{aligned}}
\]

The scalar, matrix, compact-prime-energy, dyadic-prime-energy, and infinite-notch routes are different coordinate systems for one spectral displacement.

# 7. SERIOUS RESOLUTION PATH

A serious full-problem path is present. It is not yet a proof.

## Route A — dyadic Selberg energy

Prove

\[
\int_1^Y
\left|\frac{\psi(t)}t-\frac{\psi(t/4)}{t/4}\right|^2dt
=Y^{o(1)}.
\]

The exact attack should retain all of:

1. the raw square, rather than entrywise absolute values;
2. the bounded-ratio coefficient `4/m-1/n`;
3. the upper-boundary truncation terms;
4. Selberg's exact nonlinear convolution identity;
5. a unit-log or dyadic recursive energy inequality.

A proof closes RH immediately through `T-15412`.

## Route B — critical-load one-sided queue

Construct a complete stop-loss transport, or prove

\[
\int\mathcal J_-^2\,d\log y=X^{o(1)}.
\]

This is a one-sided target and may be strictly more adaptable than controlling a symmetric prime error norm.

## Route C — Hausdorff diagonal transition strip

After independent verification, use the parabolic cone together with the complete verified-height saddle sector. The sole remaining moment region is where the saddle lies above the verified line-zero block while its width is `O(1)`. The exact second-order modulus/phase cancellation should be built into a complex saddle or local trace formula. Proving positivity there completes every Hausdorff row and hence RH.

## Route D — original `K0` positive factorization

Construct the missing positive Mellin-prefix/augmented Green Gram so that

\[
D_{\rm aug}=C-\Gamma^*\Gamma\succeq0.
\]

The factorization must survive the known one-mode and Green-stationarity counterexamples and reproduce `T-15411` on interval rays.

# 8. Recommended parallel work allocation

```text
Agent 1:
  independently audit T-15412 Hardy abscissa and the dyadic delay inverse;

Agent 2:
  derive the finite cumulative prime-pair kernel and attempt a Selberg
  completion without replacing it by the stationary kernel;

Agent 3:
  independently audit L-21702, make C_R effective, and implement a
  directed saddle certificate;

Agent 4:
  attack the diagonal Hausdorff strip with the exact second-order complex
  saddle cancellation;

Agent 5:
  compare the one-sided queue energy with PR #216's compact-window H2
  energy and seek a coercive transfer between them;

Agent 6:
  continue the original K0 augmented-Mellin Gram factorization, using the
  interval ray as a mandatory regression.
```

# 9. Exact finite regression

`X-15414-global-rank-one-attack` is a standard-library `Fraction` checker. On a synthetic rational atom sequence it verifies:

- the raw dyadic point identity;
- the exact piecewise square-function integral;
- the compact-ratio stationary kernel;
- strict exact LDL positivity of the retained synthetic kernel;
- five central/adversarial tests.

Retained proof-object SHA-256:

```text
ad39be6fa29dd5ceea912164303e8e68e9d6787c57966a3dc7e003fc663bc232
```

This is finite algebra only, not a prime or RH certificate.

# 10. Honest frontier

This pass does **not** prove RH. It does not certify a negative counterexample either.

What changed is the scale and precision of the attack:

```text
before:
  several cofinal matrix/tail/Schur signs with overlapping bookkeeping;

after:
  one rank-one interval witness,
  one raw dyadic Chebyshev square function,
  one critical-load one-sided queue,
  one proposed Hausdorff exterior-cone proof,
  and one sharply isolated diagonal transition strip.
```

The next decisive result must be one of:

\[
\mathcal E_\diamond(Y)=Y^{o(1)},
\qquad
\mathcal E_{\mathcal J}^-(X)=X^{o(1)},
\qquad
\mathcal H_{m,k}(R)\ge0\text{ in the transition strip},
\qquad
D_{\rm aug}\succeq0.
\]

Any one, with the stated exact bridges independently verified, resolves the full Riemann Hypothesis.
