# Full proposed RH proof after repository-wide consolidation

Author: `gpt56-02-p`  
Date: 2026-08-07  
Branch: `agent/gpt56-02-p/215-prime-polygon-rh-attack`  
Status: **FULL PROOF PROPOSAL PENDING INDEPENDENT REVIEW; RH NOT CLAIMED PROVED**

## Executive result

The repository has accumulated many exact finite identities, normalization
adapters, negative semidecision procedures, source frames, Schur reductions,
and global RH-equivalent observables.  Their common obstruction is not another
finite matrix identity.  It is a cofinal one-sided arithmetic estimate.

This continuation selects one architecture and carries every nonarithmetic
piece to an exact endpoint:

```text
complete Gevrey-dense finite Fourier spaces
-> smooth all-grid arithmetic source frame
-> exact alias/fold corrected tail
-> uniform ordinary-tail floor by half-grid antiperiodization
-> centered Selberg equation
-> explicit positive-Hankel exponential adjoints
-> one operator-valued Selberg-Hankel certificate SH(L)
-> whole finite Weil matrix >= -rho_L G, rho_L -> 0
-> global Weil positivity
-> RH.
```

The implication from `SH(L)` to RH is complete.  The existence of `SH(L)` with
the required cofinal rate is the sole new load-bearing statement still pending
review.  It is finite and proof-producing at every level, but a finite positive
ladder would not establish its unbounded quantifier.

## 1. What the repository has genuinely accomplished

### 1.1 Finite negative witnesses

The direct-xi, Pick, Loewner, notch, prime-window, zero-count, and carrier
programmes have made the negative direction unusually mature.

A strict directed negative value on an admitted test vector is a genuine global
negative Weil witness.  Many apparently negative candidates were correctly
resolved as conditioning ghosts at higher precision.  The repository now has:

- fixed-vector exact contractions instead of interval eigenvectors;
- complete prime-power manifests and outward scalar accumulation;
- exact zero-count discrepancy predicates;
- zero-side and prime-side independent replay interfaces;
- fail-closed candidate allocation rules.

No unconditional negative witness has survived.

### 1.2 The square-screw scalar

PR #202 proves that one square-cutoff screw value is a finite prime-power
quantity and that eventual nonnegativity is RH-equivalent.  The positive-kernel
Chebyshev--Riesz form and the beta-smoothed square-cell form remove irrelevant
pointwise roughness while preserving the exact rightmost-zero exponent.

PR #208 embeds this scalar as the constant coordinate of the complete D-0001
matrix.  Therefore no cofinal matrix theorem can evade the scalar arithmetic
sign.  Matrix conditioning may organize the proof, but it cannot replace the
prime cancellation.

### 1.3 Prime-polygon convex duality

The first part of PR #219 rewrites the complete screw function as

\[
 \Psi=F-G,
\]

where `F` is an explicit strictly convex archimedean function and `G` is the
prime-power ramp.  Fenchel duality makes the conjugate of `G` the polygon through

\[
 (A_j,B_j),
 \qquad
 A_j=\sum_{q\le q_j}{\Lambda(q)\over\sqrt q},
 \quad
 B_j=\sum_{q\le q_j}{\Lambda(q)\log q\over\sqrt q}.
\]

The complete negative depth is

\[
 \sup_j[F^*(A_j)-B_j].
\]

This gives a complete finite negative semidecision and an exact mass-transport
recurrence.  It also proves that the remaining positive inequality is the full
RH sign itself, not a soft consequence of the prime number theorem.

### 1.4 Prime-energy and Selberg routes

PRs #216, #222, #224, and #158 identify positive energies whose growth exponent
is the horizontal displacement of the rightmost zero.  They reduce higher prime
powers to ordinary-prime or balanced semiprime forcing, expose exact Selberg
Riccati and Volterra equations, and rule out entrywise absolute-value and
resolution shortcuts.

The important lesson is structural: the quadratic von Mangoldt channel must be
kept as a positive or signed square until the final contraction.  Any proof
that replaces it by its absolute mass loses a full exponential factor.

### 1.5 The prolate/CCM positive route

PR #164 and its dependencies contribute:

- an exact repaired Xi/prolate target satisfying the two source constraints;
- the fixed `d_4/d_8` hierarchy;
- exact radial leakage normalization;
- useful real-axis Dunster/Bessel comparisons;
- endpoint-jet and Poisson ledgers;
- abstract operator local-Weyl and support large-sieve theorems;
- finite source-bound calibrations.

The proposed PR #202 composition is correct conditionally, but the complete
source-specific complex-strip profile LMI is not proved.  The earlier
quarter-power repair also omitted the scaled support derivative; `L-21503`
repairs the phase algebra but leaves the source profile estimates open.

### 1.6 Exact source completion

The source-frame work eventually found the correct finite algebra.

A smooth differential cardinal is exact on the complete Fourier lattice and
satisfies both arithmetic-source constraints.  Its arithmetic image is
logarithmically Schwartz.  Its periodization, sharp finite projection, and
alias-corrected tail all lie in one absolutely convergent zero-side form domain.
Zeta inversion may be delayed until a safe support is selected and then applied
by exact congruence.

This removes the former ambiguity between algebraic source density and a
production source frame.

### 1.7 Canonical packets and direct Schur shorts

PRs #169, #191, and #192 establish the correct finite algebra:

- eliminate the ambient block before splitting the finite packet;
- retain the complete arithmetic matrix before taking norms;
- use exact residual shorting for approximate harmonic solves;
- define canonical high-deficit augmentations by spectral projectors rather
  than same-rank source surrogates;
- never infer ambient positivity from one positive finite compression.

These are permanent proof-engineering gains, even though they do not by
themselves settle the cofinal sign.

### 1.8 Annihilator, Brownian, and totient coordinates

PR #217 compresses the off-line obstruction to a positive variance defect and
connects it to the Brownian-bridge range and an explicit gamma convolution.
PR #226 identifies an RH-equivalent positive second moment of the analytic
summatory-totient error.  These are valuable independent coordinates, but their
reverse variance and local-to-Bohr estimates remain RH-strength.

## 2. Why the selected route is preferable

A proof through the bare prime polygon would have to establish

\[
 B_j\ge F^*(A_j)
\]

cofinally.  That is a clean statement but leaves no analytic reserve.

A proof through the original prolate frame would need a complete complex-strip,
all-alias, growing-packet local-Weyl theorem in a superexponentially delicate
metric.

The selected route takes the strongest part of each:

1. use a complete smooth cardinal source frame, not a fitted prolate frame, for
   the growing finite space;
2. use an exact source metric and no omitted projection tail;
3. use the centered Selberg identity, not a phase-blind zero or prime estimate,
   for the arithmetic sign;
4. pass to RH through the whole-matrix diagonal theorem, avoiding ground-state
   and complete-complement bookkeeping.

## 3. New theorem: dyadic antiperiodic metric floor

`L-21904` doubles the support of the smooth box cardinal while retaining period
`L`.  The resulting transform vanishes at both

\[
 {2\pi k\over L}
 \quad\text{and}\quad
 {(2k+1)\pi\over L}.
\]

For the corrected tail `W_c`, half-grid Parseval gives the exact identity

\[
 \mathcal A_LW_c=-y_c,
\]

where `mathcal A_L` is alternating periodization and `y_c` is the sharp finite
Fourier vector.

Only two positive translated cells can be nonzero; all negative cells have an
exponentially small Poisson tail on a zeta-safe support.  Consequently

\[
 \|W_c\|_2^2
 \ge[1/2-o(1)]\|y_c\|_2^2.
\]

After a finite orthogonal enlargement, the same argument survives Schur
shorting by any fixed low packet.  Thus the complete cardinal quotient has a
uniform ordinary-tail moat.

This is a substantial simplification: no weighted-deficit trace tail, soft
profile sector, source-frame singular value, or radial alias floor remains in
the ordinary metric.

## 4. New theorem: explicit positive Selberg adjoints

`L-21905` solves the centered Selberg adjoint equation for every exponential.
For `s>1/2`, put

\[
 m_s(u)=\left({s-1/2\over u-1/2}\right)^2{f1}_{u\ge s}
\]

and

\[
 f_s(y)=\int_s^\infty m_s(u)e^{-uy}du.
\]

Then

\[
 \mathscr L^*f_s=e^{-sy}.
\]

Moreover

\[
 f_s(x+y)=\int_s^\infty
 [m_s(u)^{1/2}e^{-ux}]
 [m_s(u)^{1/2}e^{-uy}]du
\]

is a positive Hankel kernel.  Pairing with Selberg's exact equation gives

\[
 H(s)+\int_s^\infty m_s(u)H(u)^2du
 =\int_s^\infty m_s(u)\mathcal R(u)du.
\]

Thus an entire positive cone of prime tests has a closed adjoint and a literal
nonnegative quadratic channel.  This is the correct replacement for taking
absolute values in the prime-pair term.

## 5. The full proposed proof

`T-21901` defines the finite matrix kernel `mathcal K_L` appearing in the
centered prime-side explicit formula.  It asks for one finite
operator-valued certificate `SH(L)`:

\[
 -\mathcal K_L
 =\mathscr L^*F_L+e_L,
\]

where `F_L` is a positive matrix-valued mixture of the explicit exponential
adjoints plus a positive-Hankel curvature term.

The directed residual conditions are

\[
 \mathcal B_L-\langle R,F_L\rangle
 \succeq-\alpha_LG_L,
\]

\[
 -\beta_LG_L
 \preceq\langle\nu,e_L\rangle
 \preceq\beta_LG_L,
\]

and

\[
 \alpha_L+\beta_L\to0.
\]

Pairing the exact Selberg equation with `F_L` keeps the quadratic prime term as
a nonnegative matrix square and yields

\[
 A_L\succeq-(\alpha_L+\beta_L)G_L.
\]

The finite spaces have bandwidth `exp(L^theta)`, `theta<1/2`, and form a
Gevrey-dense diagonal.  Their approximation error beats the exponential
support constants in the explicit formula.  Letting `L` tend to infinity gives
global Weil positivity, hence RH.

## 6. Why this is not circular

The certificate is not the statement `A_L>=-o(1)G_L` rewritten with another
letter.  It is a constrained factorization:

1. its adjoint map is the explicit Selberg operator;
2. its Hankel kernel must be positive semidefinite before arithmetic
   specialization;
3. its exponential part uses the closed density `m_s`;
4. its residual is checked against the explicit Selberg forcing;
5. its constant coordinate reproduces the prime-polygon margin;
6. all spline cells, prime manifests, and final pivots are finite and directed.

A false certificate can be rejected by one finite algebra or sign failure.  A
valid cofinal construction would be a new arithmetic positivity theorem and
would prove RH.

## 7. Exact review hinge

The only unproved line in the proposed completion is:

> **Selberg–Hankel synthesis.** On every sufficiently large safe support, the
> complete finite centered Weil kernel admits the factorization and residual
> bounds `SH(L)` with `alpha_L+beta_L -> 0`.

The intended construction combines:

- the prime-polygon transport reserve;
- the curvature square from the Haar/dilation route;
- the positive exponential resolvents of `L-21905`;
- exact prime-spline cell translation;
- a finite endpoint residual.

An independent reviewer should build the factor maps cell by cell, verify the
Selberg coefficient multiplicities, and prove or reject the residual rate.
No entrywise absolute value may be introduced before the positive Hankel square
is assembled.

## 8. Review order

1. `claims/lemmas/L-21905-positive-hankel-exponential-selberg-adjoints.md`
2. `claims/lemmas/L-21904-dyadic-oversupport-antiperiodic-tail-floor.md`
3. `claims/theorems/T-21901-full-rh-dyadic-cardinal-selberg-proposal.md`
4. `claims/lemmas/L-21902-exact-cardinal-corrected-tail-symbol.md`
5. `claims/lemmas/L-21901-all-grid-cardinal-eliminates-projection-tail.md`
6. `claims/lemmas/L-21504-smooth-source-poisson-schwartz-periodization.md`
7. `claims/lemmas/L-21505-bv-zero-side-radical-tail-congruence.md`
8. `claims/lemmas/L-21506-raw-cardinal-average-zeta-safe-selection.md`
9. `claims/lemmas/L-21501-prime-power-convex-duality.md`
10. `claims/lemmas/L-21502-prime-quantile-transport-recurrence.md`
11. PR #218's curvature/Bregman and Selberg-adjoint files
12. the accepted whole-matrix finite-to-global theorem and Weil normalization

## 9. Final status

This is a full **proposed** proof in the repository's established sense: every
arrow except one named finite operator factorization is supplied, and the
remaining factorization is explicit enough for independent reconstruction.

It is not an independently verified proof of RH.  The repository and public
summary must continue to state:

```text
RH NOT PROVED.
SH(L) COFINAL SELBERG-HANKEL SYNTHESIS PENDING REVIEW.
```

If `SH(L)` is verified, the deduction in `T-21901` proves RH.  If it fails, the
first failed cell or endpoint sign identifies the exact arithmetic obstruction
without invalidating the source, metric, or finite-to-global reductions.
