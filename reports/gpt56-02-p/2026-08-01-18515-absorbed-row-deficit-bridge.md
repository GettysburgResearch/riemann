# 2026-08-01 — Absorbed-row obstruction and exact deficit bridge

## Published production package

`X-18507` is now on PR #191 with its arbitrary-`N` MPFR producer, exact
consumer, source reconstruction hash, retained nine-level summary, fixture,
tests, and CI replay. All retained finite levels have directed negative part
zero. No finite result is promoted to a cofinal theorem.

## Absorbed row

The front flag contains

```text
k_1=-2b_0+b_1
```

at every support. Its exact complex D-0001 evaluation is

```text
V_(L,z)(k_j)
 =-4 j^2 sin(Lz/2)
   /[z sqrt(L)((Lz/2pi)^2-j^2)].
```

A zero `rho=1/2+delta+i gamma`, `delta>0`, gives

```text
|V_(L,z_rho)(k_1)|
 >= c_rho exp(delta L/2)/L^(5/2).
```

Every exact global-radical lift has a tail whose value at that zero is the
negative of this quantity. Therefore no Hardy/Schwartz/form topology controlling
zero evaluation can make the front packet's exterior tail vanish under false
RH. The requested `e_N,kappa_N -> 0` theorem is itself RH-bearing.

Under RH, a direct zero-sum estimate gives the conditional soft bound

```text
||B_R||_(G_R) <= C_eta (N/L)^5
```

when the band remains below a fixed fraction of the first zero resonance. Thus
`N=o(L)` is sufficient for soft radicality, while complete deficit capture may
require linear support rank. That rank/support conflict is now explicit.

## Deficit bridge

`L-18516/X-18508` give the exact finite certificate for identifying a source
flag with the global high-deficit spectral augmentation. The producer must emit

```text
G_lambda,D_lambda,g_lambda,Gamma_lambda,
Q_0,Y_D,Y_C,source Y_S.
```

The exact consumer requires

```text
Y_C*G_0Y_D=0,
Y_C*D_0Y_D=0,
Y_D*(D_0-theta G_0)Y_D>0,
Y_C*(theta G_0-D_0)Y_C>0.
```

Only then is `Q_D=Q_0Y_D` the deficit-canonical augmentation; exact span equality
with `Y_S` is checked separately. Same rank is insufficient.

## Smallest remaining production object

The repository still lacks one actual complete Suzuki symbol-deficit matrix
`D_lambda` and its directed spectral split at an X-18507 support. This—not
another finite D-0001 sign—is now the smallest bridge artifact. The assembly
radius can be diagonalized to zero once the analytic entries are emitted; the
RH-bearing work is the global radical tail and the actual deficit projector.
