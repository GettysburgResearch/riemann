# Critical-null dyadic source note

`O-32301` records a new fixed source

\[
B_\dagger(s)=\frac{(1-2^{-s})(1-2^{1/2-s})(1-2^{1-s})}{\zeta(s)}.
\]

Its purpose is not another scalar RH equivalence. It is designed to remove three distinct structural annoyances simultaneously:

1. `1-2^-s`: compactifies the unweighted carry prefix;
2. `1-2^(1/2-s)`: kills the critical zero-frequency mode and gives exact half-power fiber cancellation;
3. `1-2^(1-s)`: removes the deterministic zeta pole at one.

The inverse coefficients and generalized-prime coefficients are positive. The bare source charge is exactly zero on every split with parent and children at least eight. The square-root-hinge Riesz source cancels on every complete dyadic fiber through `16m`, leaving only a fixed top-sixteenth shell.

This makes it a promising source for the reflected independent-frequency route because the scalar augmented curvature has no `Y*T` cross term in the cofinal balanced region.

The remaining obligation is genuinely matrix-valued: prove that this cancellation survives the complete two-frequency source-convolved placement. No RH claim is made from the scalar row identities alone.
