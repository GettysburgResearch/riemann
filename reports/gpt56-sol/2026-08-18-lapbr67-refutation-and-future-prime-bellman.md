# 2026-08-18 — LAPBR67 refutation and future-prime Bellman reconstruction

## Executive result

This closure pass changes the live factor-67 frontier in two material ways.

First, the large-prime adaptive sign target `LAPBR67` from PR #578 is false as
quantified.  At the admissible moving state whose least rough prime lies just
above `Z_X=(log X)^(1/4)`, the small-prime mass is empty and the prescribed depth
is exactly eight.  The seventh reciprocal almost-prime layer then dominates the
current negatively.  This is proved asymptotically in `R-97700`.

Second, the completed-parity Lorenz dual admits an exact source-faithful
future-prime Bellman equation.  If `D^+` is the oriented hinge deficit consumed
by `CPSL67`, parity covariance forces a companion `D^-`, and the exact least-prime
owner recursion gives

```text
D+ = d+ + R D-
D- = d- + R D+
```

hence

```text
(I-R^2)D+ = d+ + R d-.
```

The parity-restored transition is therefore the literal two-prime operator
`R^2`.  This supplies the precise Type-I/Type-II interface missing from the
previous reserve formulations.

## New unconditional theorem

The repaired `P_61` bias also gives a native safe edge.  For every rough prime
`p>=67` and `Y>=67p`,

```text
F61(Y)-p^(-1/2)F61(Y/p)
 >= (1/42-1/(8sqrt(p))) M61(Y) > 0.
```

This is useful local capital for a Bellman barrier, but it does not compose by
itself through arbitrary rough depth.

## Canonical target

The branch defines `GPC67` directly on the scalar observed by the Mellin
consumer:

```text
R_X = sum_(m rough squarefree) mu(m)/sqrt(m) F61(X/m) >= 0 eventually.
```

No recombined current is allowed to substitute for this native scalar.
The retained Mellin-Landau theorem gives `GPC67 -> RH`.

## Exact remaining theorem

The corrected conclusion-producing theorem is construction of an explicit
source-computable nonnegative barrier `B_v(lambda)` satisfying

```text
B_v(lambda) <= g_v^+(lambda) + (R^2 B)_v(lambda)
```

at every owner state and every Lorenz hinge breakpoint.

Abstract existence is equivalent to `CPSL67` because `B=D+` satisfies equality;
the hard work is an explicit lower-dimensional barrier that does not use the
unknown completed deficit.

The two-prime term expands bilinearly over ordered rough prime pairs.  This is
where large-prime sparsity, exact future-prime quotients, and the separated
coprime Type-II machinery of the Q4 lane can legitimately enter.

## Scientific boundary

```text
repaired P61 scalar bias                 PROVED
native P61 one-prime safe edge           PROVED
small-prime critical cube                PROVED
LAPBR67                                  REFUTED
finite scalar Lorenz dual                PROVED
Lorenz hinge Bellman equations           PROVED
explicit future-prime barrier            OPEN / RH-BEARING
GPC67                                    OPEN / RH-BEARING
Riemann Hypothesis                       UNPROVED
```

This branch is therefore a genuine correction and reduction advance, not a
completed proof of RH.
