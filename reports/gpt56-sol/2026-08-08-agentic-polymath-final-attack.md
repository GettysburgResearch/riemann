# Final attack pass: no false closure, unified RH frontier

## Status

This pass intentionally does not claim an unconditional proof of RH. The purpose is to produce the strongest honest final proposal object after the full repository synthesis.

The project has eliminated many false closures. The remaining object is not another positivity surrogate.

## Unified theorem target

All surviving routes reduce to a single statement:

> The complete signed arithmetic source, after all mandatory recombination operations, admits a finite positive certificate whose cost is subpolynomial.

Equivalent interfaces:

1. Transition-band recombination.
2. Reciprocal-eta sparse boundary control.
3. Cycle/collector/parity transport.
4. Source-specific Möbius contraction.

## What is closed

- Fixed Abel smoothing routes are closed negatively: fixed-order cumulative kernels are insufficient.
- Generic Hankel/operator positivity routes are closed negatively.
- Monotone positive-part cover routes are closed negatively.
- Atomic endpoint source norms are closed negatively.
- First-entrance localization is exact.
- Size-biased fragmentation Green identities are exact.
- Reciprocal/Mersenne boundary seminorm control is exact for the isolated endpoint bank.

## Final proposed proof architecture

Möbius source
-> exact first-entrance fragmentation
-> transition band
-> explicit arithmetic transport decomposition
-> reciprocal-eta boundary preservation
-> positive balanced certificate
-> sharp prime-power ramp
-> square-screw/Landau
-> RH.

## Missing theorem

The only unresolved theorem is the constructive transition certificate:

Given the exact transition-band source, emit explicit cycle coordinates, collector coordinates, parity boundary coordinates, and a nonnegative remainder with certified cost.

This document records that as OPEN rather than pretending it is proved.
