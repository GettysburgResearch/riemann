# Negative-mass, Euler–Hurwitz, and vanishing-hinge attack

## Executive result

The live factor-67 graph can be weakened in two conclusion-producing ways.
First, RH does not require eventual pointwise scalar positivity: subpower
logarithmic mass of the negative part suffices. Second, the difficult zero hinge
may be replaced by one positive hinge

\[
\lambda_X=L(X)/\sqrt X
\]

with an arbitrary subpower `L`: its slack is the native scalar plus the explicit
positive squarefree-shell shift

\[
0.3085927362743162\ldots L(X)+o(L(X)),
\]

and its two-orientation Bellman cushion is twice that size.

## What was killed

The raw fixed-cutoff reciprocal-Möbius prefix cannot be eventually one-signed;
Landau and a known critical-line zero force infinitely many sign changes. The
long positive transient at cutoff `67` is therefore not a closure mechanism.
A separate exact endpoint argument rules out every finite dyadic target filter
that cancels both asymptotic modes while remaining nonnegative.

## Positive structure retained

The target Euler discrepancy at integer knots is a strict Hausdorff moment
sequence. Its Hurwitz interpolation is completely monotone, and the difference
from the actual sawtooth is one explicit positive fractional-cell reset.

The reciprocal-Julia state admits fractional roots with coefficientwise PSD
channels for every exponent; however any scalar positive channel retains the
real zeta pole, so trace-free extraction remains necessary.

## Exact remaining arithmetic

The scalar negative mass is bounded by the exceedance of

\[
\mathcal D(t)=6B_{1/2}(t)-{9\over\sqrt2}B_{1/2}(t/2)
+{3\over2}B_{1/2}(t/4)
\]

above `6`. Its quadratic energy has an exact max-kernel and a polylogarithmic
diagonal. The only remaining quadratic object is the source-faithful Möbius
cross term.

The new vanishing-hinge route offers a parallel finite-block target: spend the
subpower squarefree-shell cushion to control the exact Bellman residual, then
invoke the negative-mass Mellin theorem rather than demand a zero-debt sign.

RH remains unproved.
