# Transition-band proof attempt

This pass changes the goal from proving raw first-entrance positivity to proving an explicit arithmetic normal form.

The reason is that all previous local positivity attempts failed while preserving only partial ancestry information.

## Claim under attack

For each transition source Sigma_{X,n} in [n,2n), construct

Sigma=C+S+P+R

with C Pascal-cycle, S squarefree collector, P parity/factor-five, and R>=0.

## Current progress

Exact:
- first entrance representation;
- divergence-free ambiguity equals cycle space;
- transition band is the only new scale after recombination.

Open:
- constructive collector decomposition;
- parity boundary closure;
- proof that every transition source enters the generated cone.

The remaining theorem is now finite-scale transport rather than an infinite ancestry estimate.

RH remains unproved.
