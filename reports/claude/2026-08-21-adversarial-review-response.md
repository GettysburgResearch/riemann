# Adversarial review response — packet 105000 hardening

Before deposit, the packet was subjected to three independent hostile
reviews (algebra re-derivation; statement-to-use citation audit against all
source branches; overclaim/scope/consistency). Verdicts: **no fatal error in
any lane**; all three returned NEEDS FIXES. Every finding and its
resolution:

## Serious findings → fixes (all applied)

1. "Trichotomy" overstated (typology, not proved classification; cell (A)
   contradicted the quantifier) → restated as **proved dichotomy**
   [fail conservation with recorded deficits | carry CHILD exposure of A.3
   size and Mellin type] **plus typology** over the executed corpus;
   Status line updated (`T-105000 §4`).
2. Headline crossing `Y*` was proved only for a proxy coordinate →
   **strengthened**: new Theorem A.1′ derives the `T`-form budget
   `Σ d_p T(Y/p) ≤ T(Y)` from the same pointwise positivity at `t = 1`,
   making `Y* ∈ (578906, 584375)` a genuine earliest horizon; the
   row-coordinate horizon is bracketed separately
   (`Π_{Q(2)}: (5·10⁶, 7·10⁶)`, S9b).
3. DEFECT exclusion under-argued ("by their own definition"; the and/or and
   partial absorption open) → closed: bounded ⇒ transform holomorphic in
   `Re s > 0` by absolute convergence; finite DEFECT aggregates are
   holomorphic; `E` minus any aggregate retains the `s = 1/2` singularity
   (`T-105000` A.3).
4. Landau step hypotheses (nonnegativity, measurability) → `d_p ≤ r_p` per
   prime from the sign inventory (every first-order carrier observes
   negatively within the axioms), so `E ≥ 0` for all `Y`; sandwich gives
   abscissa exactly `1/2`; measurable-realization phrasing added.
5. `Π_{Q(j)}` divergence lacked a weight lower bound → added the
   `R-99440.4`-based bound `w_p^{Q(j)} ≥ 1 − K'_j Y^{-1/4}\log Y` for
   `p ≤ √Y` (`T-105000` A.2(ii)); the `(1−¾Y^{-1/4})` constant restricted
   to `G = T` where it is proved.
6. Nonlinear free blocks (e.g. `(1−cR)² dM`) escape the linear ledger →
   remark added: their excess rides on a reweighted-rough-lift residue that
   fails all three typings (FREE-canonical/CHILD/DEFECT), so nonlinear use
   of the positivity buys no delivery (`T-105000 §3`).
7. Two practiced boundary mechanisms (two-prime `R²` of `T-97700.8`;
   annular bias edge `L-97700`) outside the axiomatized class → FREE
   currents generalized to composite couplings (even-order slots are
   sign-free; A.1 unchanged); both mechanisms named and their budget
   status stated (`T-105000 §2, §4`).
8. "Interval-guarded" labels on plain-float checks → S8 reimplemented with
   `mpmath.iv` (true directed enclosures at both bracket endpoints); S3's
   directed rounding chain corrected per operation monotonicity; remaining
   float checks labeled as such.
9. `R-101000` four-coordinate citation firewall (bare IDs, several collided
   across branches) → `M-105000 §1` rebuilt with branch coordinates and
   per-anchor source statuses; collided IDs disambiguated (two `R-97500`s,
   three `R-99700`s, two `L-99704`s, etc.).
10. `d_p` definition vs usage divergence → definition now includes
    reserve-injected dominated children.

## Minor findings → fixes (all applied)

Lemma 0 downgraded to "sketched and fixture-verified"; "no third road"
qualified "inside the axioms"; "size and Mellin type of the FCHD67/IHR67
class"; corrupted sentence in `L-105002 §1` and the dangling general-kernel
parenthetical fixed; margin->17 witness re-attributed `R-96650`/`R-97301`
(not `R-96500`); sign-character tag `L-96501.1`; `R-99260` parent
PR #647/#649; internal pointers (`M-105000 §1`, `L-105001 §4`); anchor-
status inflation ("all previously proved" → statuses annotated);
`§0` corpse-shape wording covers all five shapes; S7 relabeled a
consistency fixture; S9 child endpoints un-floored (real endpoints; the
floored values understated `Π_Q` by ~1.5%); S9b and S11 added; results
path fixed; counting rule for survey figures stated; "least prime above
64"; provenance of the "exact but degenerate" phrase.

## Independent confirmations obtained during review

The algebra reviewer re-derived every displayed identity from scratch,
independently interval-certified the `Y*` bracket, located the
row-coordinate crossing near `6.2·10⁶` (our S9b bracket agrees), and
confirmed: the Hall no-rough-slot claim, the endpoint-difference blocks
being exactly the cap-extremal current, and the `R-99440.7` pole
computation. The citation reviewer verified every quoted source verbatim
(25+ anchors), confirmed FCHD67's definition, the three-option repair menu
of `R-99260 §2`, the `L-97501 §4` minimality lemma, and the unit-partition
faithfulness of the corpses. Real-endpoint `Π_{Q(2)}(10⁵) = 0.6028` now
matches the reviewer's independent computation to displayed precision.

RH status: unproved, not addressed by any of this.
