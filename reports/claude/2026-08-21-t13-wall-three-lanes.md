# Three lanes, one wall: t = 13 — circulation prices it, the cap certifies it, the prime zeta explains the cut

Agent: claude (external reviewer lane) · Date: 2026-08-21 · Packet:
`L-105021`, `L-105022`, `O-105023`, `R-105024`, `L-105025`, `M-105026`,
`experiments/X-105020-t13-wall`. Companion to the hazard-budget packet
(`T-105000` family) and the moving-cut proposal (`O-105010`).

## What was attempted

Three deliberately different ideas, executed in parallel with the explicit
mandate to record failures: (H) import Hoffman circulation theory — the
signed/reversible Hall — absent from all ~670 branches; (P) build the
Mellin consumer for depth-truncated rough Euler products via the prime
zeta function's branch calculus — also absent from the repo; (S) measure
whether the compact Hall technology survives the moving-cut architecture's
growing smooth block.

## What happened: the three lanes converged on one finite object

**The quarantine lemma (`L-105022`, proved by complete enumeration).** Over
the entire `2^18` divisor lattice of `P_61`, the smooth Möbius mean `A_t`
is negative on exactly 19 threshold points, all `≤ 61`, with global
minimum `A_13 = −2323/30030`. The 61/67 smooth/rough boundary exactly
quarantines every negative excursion inside the compact block — the
structural reason compact-fibre Hall works, and the third independent
extremality of 67 (after the `p > 64` defect threshold and the budget
horizon).

**Lane S (`L-105021`, proved/measured).** The 7/20 moat dies at cut
`67.494` (67 is the largest viable prime — the certificate had *zero*
headroom); true LP feasibility outlives it by only 29% and dies at
`C* = (45045|B_13|/4646)^2 = 87.359` (largest feasible prime cut 83,
first failing fibre `x = 88`, killer `t = 13`, fixed violator
`{2,3,5,7,11,13}` vs `{1,6,10}`). The moving-cut smooth block cannot keep
the `L-99020` Hall order; the difficulty is the system, not the
certificate.

**Lane H (`O-105023`, computed + conjecture CIRC-13).** In the Hoffman
circulation formulation of the odd-history wall, the min-cut at every
witness on a ten-point ladder (X from 5.2e4 to 1.2e6) is the *same* `t=13`
prefix plus history head, with the exact deficit law
`(371342/1006005)√X − 3.2586`. The famous 17-margin under-reports the wall
(worst per-threshold cut: 117). Conjecture CIRC-13: any row-monotone
global certificate must carry `≥ 0.30942√X − 2.9` of target mass across
the `d = 13` boundary by anti-monotone/score-debt mechanisms — the
non-Hall component of any CPSL67-type theorem is lower-bounded by a
universal `√X` law with a fixed combinatorial address.

**Lane P (`R-105024` + `L-105025`, proved).** The recorded death: fixed-cut
depth-truncated gates are analytically empty — the truncation installs its
own `(s−1/2)^{-1}log²` singularity at `s = 1/2` (the Mellin shadow of the
budget divergence `Σ 1/p`), Landau saturates on the artifact, and the
truncated positivity (true! empirically to `10^6` and at main-term level)
implies nothing; even depths uninformative, odd depths false. What
survives is better than the idea: the depth-split Dirichlet calculus, the
prime-zeta branch inventory, and the `m²ℓ²/2` no-cancellation lemma at
off-line zeros — the exact singular germ any moving-cut Type-II energy
bound must feel. The moving cut (depth-2 exact, nothing discarded) is not
subject to the see-saw.

## The composite picture

The fixed cut at 67 is now dead from four independent directions: budget
(`T-105000`), Hall cap (`L-105021`), Mellin log² wall (`R-105024`), and
the α-defect corpses it retro-explains. Every repair points the same way:
(i) the moving cut with exact depth 2 (`O-105010`), whose smooth block
must abandon nested Hall for circulation-type transport — priced exactly
by CIRC-13 — or x-dependent renormalization; (ii) the Type-II object for
the 102000 Vaughan lane, whose singular target is the `m²ℓ²/2` germ.
What used to be "global compensation is needed" (prose) is now a priced
demand with a stable combinatorial address (`d ≤ 13`) and a √X law.

## Reviews landed today (recon digest)

Reviewer A's arithmetic freeze (72 PR heads) and Reviewer B's operator-heat
audit both replay clean, reference none of this session's packets, and
independently re-certify the packet's inputs (α-child promotion FALSE,
normalization firewall, compact moat, FCHD67 OPEN_SUFFICIENT_FOR_RH; no
prime-zeta or circulation methods anywhere). No contradictions found in
either direction; two shared frontiers (Dickman corridor GAP_BLOCKED edge;
Vaughan near-collision OPEN) sit exactly where this packet's forward
pointers aim.

## Honesty

RH is unproved and untouched by all of the above. One idea died and is
recorded with its autopsy (`R-105024`); one idea produced a hard cap where
a green light was hoped for (`L-105021`); one idea produced a stable
priced conjecture rather than a theorem (`O-105023`). The proved products
are finite/structural: the quarantine lemma, the cap constants, the
depth-split calculus. The failed-ideas ledger is `M-105026`.
