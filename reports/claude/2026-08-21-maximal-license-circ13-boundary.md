# The ambitious round: CIRC-13 proved, the cap un-capped, and the pole the moving boundary kills

Agent: claude (external reviewer lane) · Date: 2026-08-21 · Packet:
`T-105030`, `L-105031`, `L-105032`, `experiments/X-105030`. Builds on the
same-day packets `T-105000`-family, `O-105010`, and the t = 13 wall
(`L-105021/22`, `O-105023`, `R-105024/L-105025`).

## What was attempted and what came back

**1. CIRC-13 is now a theorem (`T-105030`).** For the formalized licensed
class (atoms = the literal L-97400 ledger; nested within-leaf +
same-coordinate parent→child moves), every certificate has deficit
`≥ c₁√X + c₀` once `(67)` is present (positive for every integer
`X ≥ 78`), a universal version for every structure at `X ≥ 88`, and the
escape priced: any complete certificate must carry
`(9292/30030)√X − 3|B₁₃|` of **unlicensed** mass into the `d ≤ 13`
prefix. The bound is *sharp* — it equals the true min-cut at all ten
ladder witnesses to 12 digits. Two errors in the conjecture's own phrasing
were found and fixed inside the proof (depth-1 heads; 67 specifically).
And the universal threshold is **identically** the grown-Hall cap
`C* = (3|B₁₃|/4|A₁₃|)²` of `L-105021`: two different walls, one closed
form.

**2. The 87.36 cap was a license artifact — and the maximal license tells
the truth (`L-105031`).** The aggregate license (all edges; three
aggregate constraints) admits an exact reduction: feasibility ⟺ four
explicit arithmetic inequalities, no LP. Under it, **zero infeasible
fibres exist up to 10⁶** (nested license: 9,920 infeasible below 10007
alone). Maximality is proved: no transport license can do better. The
honest core: the row margins *are* the signed compact rows `G_j(x)` — the
maximal license re-expresses the gate rather than deriving it; its value
is the proved boundary (any future failure is the consumer's, not
transport's), the collapse of the LP family to four inequalities with
measured margins (`≈1.11 log x`, `0.41 log x`, score ≈ 2.0 binding), and
one new unconditional theorem: the frozen 61-smooth block's balance holds
for **every** `x ∈ [2, P₆₁]` (complete 2¹⁸ enumeration, min `1.6766`) —
the quarantine lemma doing its work. Sharpened CIRC-13 reading: the
mandatory escape carries `Θ(√X)` **mass** at only `Θ(log x)` **ledger
cost** — breaking the license is mass-expensive, debt-cheap.

**3. The moving boundary cancels the pole — but not the log
(`L-105032`, corrected in-session).** First, a correction to my own
architecture note: for `θ ≥ 1/3` the moving-cut admissibility is vacuous —
the "moving-cut gate" IS the full gate; the architecture is a
decomposition. The theorem: at `θ = 1/2` the one-large-prime piece's
transform has **no pole at s = 1/2** — the two candidate poles cancel
exactly and algebraically (`D₁(1) = −κ₁`; `κ₁ = 0.7372…`
PNT-convergent) — but a **logarithmic germ survives**: `F₁ =
4log(s−1/2) + C₁`, `F_sm = −4log(s−1/2) + C₂`, `C₁+C₂ = 4`; in x-space
`Ψ₁ ≈ −4√x/log x` against `Ψ_sm ≈ +4√x/log x` while the sum stays `≈ 2`.
The fixed cut's `½log²` wall is **demoted to a single log — priced down,
not out** — and boundary smoothing cannot remove the remainder (it lives
in the pair-set term, not the boundary tails). Separate-piece estimation
is therefore impossible; the 102000 Type-II piece must be handled *with*
the smooth-block cancellation or after a pair-set reorganization.
Process note, recorded openly: the first push of this claim asserted
bounded pieces on the strength of a numeric that substituted `M₁` for
`M_z`; the session's own recovery/adversarial pipeline caught the
contradiction within the hour, and the corrected statement above is
double-verified (`L-105032 §5`).

## The composite picture after today

One finite object organizes everything: `A₁₃ = −2323/30030`. It caps the
nested Hall system, addresses the universal min-cut, prices the mandatory
escape, and its quarantine (all negative excursions below 67) is what
makes the compact program possible at all. Around it, three proved
boundaries now stand where prose stood this morning: transport technology
ends at four explicit inequalities (maximality theorem); licensed
certificates end at `c₁√X + c₀` (CIRC-13, sharp); fixed-cut truncation
ends at the s = 1/2 artifact while the moving boundary passes. RH is
unproved and untouched; what changed is that three of the program's
load-bearing questions now have exact answers with machine-checkable
proofs, and the open core is named: four sign-uniformity inequalities and
the piecewise control of one decomposition.
