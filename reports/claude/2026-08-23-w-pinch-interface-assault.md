# The interface assault: the gate refutation narrows to one inequality

Date: 2026-08-23
Agent: claude (external reviewer lane), orchestrating the 4-lane W-pinch assault
(T-105070), a far-field lane, a 4-stage weld pipeline, and three deposit-level
hostile reviews.
Deposit: `T-105070` (+ hardening), `L-105071` (+ hardening), `L-105072`
(+ hardening); replay under `experiments/X-1050{70,71,72}-*`.
RH status: **unproved, not addressed.** Nothing here proves or claims RH; the
target of the refutation is the gate `HHFE102010` (Ht reading), one of the
equivalent sufficient conditions in the T-105050 lattice.

## 1. What was deposited

- **T-105070** (the assembly): GATE_{o(1)} is FALSE — Theta_gate >= 1/3, and
  = 1/3 exactly under RH — modulo two named interfaces. Tier A (tent reduction,
  transfer architecture, coefficient |c_B| = 0.04782893516094 certified,
  bookkeeping exact) is proved, RH-free. Its hostile review (no FATAL) forced
  two mechanical repairs: the missing sieve module (replay integrity) and a
  false concrete constant ((L+1) -> (L+2) near octave tops, asymptotics
  untouched) — and contributed the exact bridge identity |rho_1 - 1| = 2|z*|,
  closing the P2/P3/P4 constant triangle analytically.
- **L-105071** ([P2-FAR] reduced): the far-field wall — 1/zeta at unbounded
  heights in the strip — is ELIMINATED from the interface. The reduction
  theorem ((FAR-WIN) => [P2-FAR] form (b), delivering eps_1' = eps_1/sqrt(2))
  is proved from four operator lemmas: the reductio's Plancherel log-budget,
  the exact identity "D_half IS the sqrt(x)-multiplier", and a bounded window
  commutator. Its hostile review reconstructed the correct chain and caught
  three invalid steps in the first proof (false kernel-support premise,
  divergent full-line display, multiplier off its domain) — all repaired with
  the reviewer's own arguments; the ledger's F-6 entry had itself misdiagnosed
  a failure and is annotated, not deleted. What remains open, (FAR-WIN), is one
  scalar inequality about the arithmetic field at the single frequency
  gamma_1/3: measured ratio 0.007-0.013 vs required < 0.1061 (implied
  eps_1 = 0.25-0.35), against a proved self-consistency ceiling of 27.5x.
- **L-105072** ([P1-LOC-WELD] discharged, with a finding): the weld pipeline
  produced the explicit compact cut-integral formula for Z_loc via a homotopy
  crossing NO singularities (machine-arbitered to 4.3e-28), pinned the branch
  phase omega = +1, and confirmed c_B in modulus AND phase to 1.03e-6 by
  genuine deformed-contour numerics. In doing so it DISPROVED T-105070's
  literal interface clause: R_loc carries a genuine half-power
  kappa (s-s_0)^{1/2}, |kappa| = 0.0140799 (zeta''-terms cancel exactly;
  verified four independent ways, best agreement 3.7e-40), so the derivative
  bound fails at the pinch corner for every r_0, M_loc. The amended interface
  (|R_loc| <= 5.71; |R_loc'| <= 640(1 + |s-s_0|^{-1/2})) is proved
  unconditionally from bounded-height inputs (max zero-height consumed 17.33),
  and the consumption point tolerates it at cost log^2(1/h) — still o(1/h),
  every downstream constant unchanged. Its hostile review re-derived the
  repair independently and caught the one cross-claim debt: L-105071 had cited
  the literal bound; both citations repaired with no quantitative change.

## 2. Net standing

The chain GATE_{o(1)} FALSE (unconditional) now rests on exactly ONE open
statement: **(FAR-WIN)**, with the amended [P1-LOC] reading recorded in
T-105070 §2. Under the (FAR-WIN) route the refutation constant is
(1 - eps_1/sqrt(2))^2 c_0'. The wall is real — the residual cancellation is a
fixed-height Mobius/Hankel statement of the same class as L-105058.4's (P-ii),
proved there only modulo a zero-free half-plane; Montgomery-Vaughan does not
apply (fixed-length window), PNT-strength cancellation is one power-scale
short, and the reductio is frequency-blind (proved). Route markers: the
corner/smooth split with Delta^2 budgets (coefficient size never the
obstruction — 1000x headroom; only cancellation), and conditional Prop G
(two-zero anti-conspiracy closes the smooth far field).

## 3. Review discipline

Three deposit-level hostile reviews (FIX_FIRST / FIX_FIRST / FIX_FIRST, zero
FATAL): every finding applied in a same-day hardening commit; two reviews
supplied their own reconstructions of the correct arguments, which were
incorporated with attribution in the deposited texts. Failure ledgers grew by
nine entries, including one annotating its own earlier misdiagnosis. One lane
died on the output cap and was relaunched as a 4-stage pipeline whose
adversarial audit stage caught five defects before deposit.

## 4. The frontier

(FAR-WIN) is the single frontier object of Program A's refutation arc. It is
finitely checkable at any truncation, has an order of magnitude of measured
headroom, and both R1 (arithmetic) and R2 (reductio-side) terminate at it —
new input is required, of Selberg-Delange/Hankel type at one fixed height.
Program B's frontier (near-line pair statistic, R-C1', the quantitative Conrey
rate) is unchanged this wave. RH is unproved and unaddressed.
