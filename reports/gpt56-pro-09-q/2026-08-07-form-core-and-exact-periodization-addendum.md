# Addendum — source form core and exact periodization

Agent: `gpt56-pro-09-q`  
Date: 2026-08-07  
Parent report: `2026-08-07-projection-review-adversarial-recovery.md`

Two later deductions strengthen the parent report.

## 1. Continuous source density is closed

`L-19860` applies finite Möbius inversion to any

\[
 h\in C_c^\infty((\lambda^{-1},\lambda))
\]

and constructs an admissible compact-BV source `f_h` satisfying

\[
 E(f_h)(u)=h(u)
 \qquad
 (\lambda^{-1}\le u\le\lambda).
\]

Therefore the exact arithmetic-radical restriction range contains the defining smooth form core of the localized Weil form. Its graph closure is the complete localized form domain.

The parent report's final item asking for graph-density should be replaced by the narrower requirement:

```text
verify the complete signed quotient d4/d6 hierarchy and relative local-Weyl
estimate on that graph-closed core.
```

## 2. Finite periodization is exact

`L-19858` upgrades generic source density on the circle to exact containment of every Fourier character by using the rotation-invariant source action and character projection. Thus the finite periodized source vector can be chosen with no high Fourier residual.

The exact residual is not the ordinary tail. It is

\[
 W=t-\iota_L\mathfrak F_Lt.
\]

`L-19859/T-19812` retain that fold exactly.

## 3. Revised irreducible frontier

The common analytic heart of both recovered proposals is now:

1. prove that the complete canonical tail relation has only one direction below `R^{-o(1)}d_6` and contains the Xi target at `R^{o(1)}d_4`;
2. prove the corrected Bessel/stationary-alias/Airy/endpoint estimate on that complete tail range;
3. use it to derive relative local-Weyl scalarization;
4. prove moving-Hardy target convergence.

The assembly and source-density defects have been removed from this list.

Status remains:

```text
full proposals:          PROPOSED
accepted proof of RH:    NO
```
