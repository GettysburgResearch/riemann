# Agent report — cofinal profile-soft signature LMI

Agent: `gpt56-08`  
Date: 2026-08-01  
Branch: `agent/gpt56-pro-09-d/156-capacity-saturation`  
Primary results: `L-15632`, `L-15633`, `T-15606`, `M-15605`, `X-15613`  
Classification: exact finite theorem and conditional cofinal composition; no production packet and no RH claim

## Executive result

The profile-soft sector does not need a separate terminal-prime norm or an
absolute harmonic-cross estimate.

Before the soft projector is formed, compare the complete actual
prime/polar/archimedean/harmonic block with the artificial block obtained by
moving every zero horizontally to the critical line. The line-centered block is
positive semidefinite. The complete-frame support large sieve makes the full
horizontal-displacement block `o(log R)` in the regularized profile metric on a
support selected from every dyadic block.

After that support is selected, export the actual spectral soft packet. Its
regularized profile Gram is `O(tau_R G)`. The exact PR #191 residual-short
identity then gives a soft Schur negative part of order

\[
 O(a_R^{3/4}),
 \qquad
 a_R={M_R^2(\log R)^2\over R}.
\]

Thus the soft signature tends to zero whenever the complete source/harmonic
support envelope satisfies the already-isolated sub-square-root condition.

## Exact finite theorem

For a positive line-centered block

\[
 H^0=\begin{pmatrix}B^0&(Z^0)^*\\Z^0&C^0\end{pmatrix}\succeq0,
\]

with

\[
 C^0\succeq hM,
 \qquad
 B^0\preceq\ell D,
\]

and a complete relative perturbation of size `epsilon`, the actual Schur block
obeys

\[
 B-Z^*C^{-1}Z\succeq-\eta D,
\]

where

\[
 \eta
 =\epsilon(1+\ell/h)
 +{\epsilon^2\over h-\epsilon}
  (1+\sqrt{\ell/h})^2.
\]

The proof uses the line-centered harmonic minimizer as the trial solve in
`L-18512`.

A graph-only version needs only the perturbation trial energy and the actual
solve residual. The line-centered minimizer has norm

\[
 X_0^*MX_0\preceq(\ell/h)D
\]

and its covariant support derivative is bounded by differentiating
`C^0 X_0=Z^0`. Therefore it introduces no new conditioning scale.

## Cofinal balance

Let `M_T` be the complete unwhitened amplitude/support-derivative envelope and
put

\[
 a_T=M_T^2(\log T)^2/T,
 \qquad
 \tau_T=M_T/\sqrt T.
\]

Then

\[
 {M_T/\sqrt{\tau_T}\over\sqrt{T/\log T}}=a_T^{1/4},
 \qquad
 \tau_T\log T=a_T^{1/2}.
\]

The support large sieve selects a point with relative full-block perturbation

\[
 \epsilon_T=O(a_T^{1/4}\log T).
\]

On the soft packet,

\[
 \widehat D_T\preceq2\tau_TG_T,
\]

and hence

\[
 \rho_T
 \le2\tau_T\eta_T
 =O(a_T^{3/4})\to0.
\]

The soft projector is formed only after support selection, so no derivative of
a moving spectral subspace occurs.

## Production object

`M-15605` specifies the exact exporter:

```text
support-average certificate
-> selected support
-> directed generalized soft/hard split
-> complete prime/polar/archimedean low matrix
-> complete harmonic block and cross
-> line-centered comparator ledger
-> PR #191 trial solve and residual
-> directed LDL for the final soft matrix.
```

Every transition-band eigenvalue is assigned to the soft packet. Every prime
power is required. Terminal/polar cancellation occurs before interval
contraction.

## Exact regression

`X-15613` retains

\[
 H^0=\begin{pmatrix}1/100&1/10\\1/10&1\end{pmatrix},
\]

one relative perturbation of size `1/10`, and exact actual Schur value

\[
 -1/1000.
\]

The theorem proves the lower bound

\[
 -11/4500,
\]

with strict normalized slack `13/9000`. Six mutation tests pass.

## Honest frontier

No actual Suzuki `P_R^soft` packet has been exported because the upstream
production artifacts do not yet provide, on one common support block:

1. the directed complete source-frame graph LMI;
2. the complete low-plus-harmonic support-phase ledger;
3. the line-centered local-Weyl upper and ambient-floor constants;
4. the complete prime/polar/archimedean matrices and harmonic residual.

The symbolic soft-signature implication is now complete. The remaining work is
one production ledger, not another asymptotic matrix identity.
