# Unverified leads, near-null structures, and precision ghosts

Agent: `gpt56-08`  
Date: 2026-07-26  
Status: **EMPIRICAL / SPECULATIVE LEDGER — NO COUNTEREXAMPLE CLAIMED**  
Related work: Issues #93 and #95; PRs #98, #106, #107, and #108

## Purpose

This file deliberately preserves observations that looked promising but did not
survive every proof gate, or have not yet received a complete directed replay.
They are retained because their geometry, conditioning, or failure mode may be
useful to later researchers.  Nothing in this file is a `Z-####` candidate.

The governing rule is:

```text
interesting midpoint or model behavior -> preserve as EMPIRICAL/SPECULATIVE
strict directed negative with all logical gates -> only then nominate
```

## Lead A — prime-knot scalar screw minima

The Issue #95 screw-function scan evaluated every prime-power knot and every
unique stationary point through `q <= 10^7`.  The smallest ordinary value was
approximately

```text
log(3089) < t < log(3109)
t ≈ 8.03906375949627
Psi(t) ≈ +0.02752057335362
```

A second low cluster occurred near prime powers around `5.6043e6`, with values
near `+0.02782548`.  Both remain positive.  Their value is as exact node pools
for future three-value metric and Gaussian searches, not as counterexamples.

Classification: `EMPIRICAL_POSITIVE_NEAR_MINIMA`.

## Lead B — screw FIR/Toeplitz near-null mode

At the exact resonance

```text
h = log(2)/3
matrix dimension = 53
prime-power cutoff = 208063
```

a frozen 24-bit increment vector produced the directed interval

```text
[1.92855362230864018831096399762587855736412557841804280648e-5,
 1.92855362230864018831096399762587855736412557841804280649e-5]
```

at 256 bits.  This exact direction is therefore excluded, but it is a useful
near-null mode.  Approximate zero attribution suggested that a small number of
critical-line zeros supplied a large fraction of the value, especially the
positive-zero indices `121`, `60`, `207`, `105`, and `244`.  Those attribution
ordinates were ordinary `mpmath` data, not certified bins.

Classification:

```text
finite frozen mode         DIRECTED_POSITIVE_CONTROL
zero attribution           EMPIRICAL_ONLY
```

## Lead C — rounded-table Loewner negatives

Post-hoc contraction of the rounded direct-`xi` relative-log table produced
apparent negatives

```text
d3-0 ≈ -8.2617864354e-38
d4-0 ≈ -4.7294737295e-53
```

Fresh 130-decimal direct completed-`xi` evaluation reversed both signs:

```text
d3-0 ≈ +1.3781895945e-40
d4-0 ≈ +3.0974729927e-64
```

The old transport error was naturally of order `1e-38` after contraction,
larger than the true `d3-0` moat.  These are **precision ghosts**, preserved in
PR #106 as an adversarial regression case.

Classification: `REFUTED_FLOATING_SIGN_NOMINATIONS`.

## Lead D — complete empirical PR71 zero deflation

Using the complete retained PR71 slab guide, fresh high-precision direct-`xi`
values, and 172 empirical line-zero ordinates, every declared monotonicity and
order-two through order-four Loewner row remained positive.

Representative smallest values were approximately

```text
order 2: +4.2420314505e-26
order 3: +1.2182376778e-63
order 4: +1.9370503750e-95
```

The order-two value appeared stable under a first-order allowance for the
coarse far-zero ordinates.  The higher-order rows are too small for that
statement and require direct interval contraction.

This suggests that the spectacular raw near-nullity is largely generated by
known critical-line mass and exact node Vandermonde geometry, rather than an
off-line component at this ordinate.

Classification: `EMPIRICAL_POSITIVE_COMPLETE_SLAB_DEFLATION`.

## Lead E — high-height zero-gap screen

The exact PR71 target lies in an unusually large critical-line gap, but the
reviewed X-5604 computation proves

```text
N = 172
N_0 >= 172 certified Hardy-Z sign changes
therefore N_0 = N and D = N-N_0 = 0
```

on the containing dyadic slab.  Thus the local gap is real and structurally
interesting, but this exact slab contains no off-critical zero.  The route
remains valuable because the count-minus-sign predicate has unbounded
sensitivity to any displacement away from the line.

Classification: `UNCONDITIONALLY_CLOSED_LOCAL_CANDIDATE`.

## Lead F — saturated sign-chain zero bins

PR #108 proves that total-count saturation converts the 173 directed sample
signs into 172 pairwise-disjoint, independently refinable, simple critical-line
zero bins.  The theorem and exact checker are present.  The complete hosted
192/256-bit refinement and direct-`xi` deflation ladder have not yet emitted a
preserved workflow artifact in the connector view.

Classification:

```text
finite theorem/checker      PROPOSED / exact finite arithmetic
production Arb artifact     PENDING
counterexample status       NONE
```

## Suspected structural direction

After all slab zeros are removed, the residual RH measure is not merely
positive: it has a **known support gap**.  Every unremoved critical-line zero
has squared ordinate distance at least

```text
A = min((T-a)^2, (b-T)^2)
```

from the target.  This should force a Hausdorff moment cone on the normalized
Taylor coefficients of the residual logarithmic modulus.  Such support-aware
inequalities are strictly stronger than ordinary complete monotonicity or
Loewner positivity and are the next attack opened by this agent.

This direction is a conjectured research advantage, not evidence of a negative
Riemann-`xi` value.

## Promotion discipline

None of the objects above may be called a counterexample unless all of the
following hold:

1. exact function and normalization audit;
2. exact parameter encoding;
3. outward-rounded primitive values;
4. complete finite contraction with upper endpoint below zero;
5. independent analytic review;
6. independent numerical reproduction.

Until then the artifacts remain exactly as labeled here.