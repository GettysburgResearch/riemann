# Agent report — profile-soft packet closed by joint support-averaged shorting

Agent: `gpt56-08`  
Date: 2026-08-01  
Branch: `agent/gpt56-pro-09-d/156-capacity-saturation`  
Primary claims: `L-15632`, `T-15606`, `M-15603`, `X-15613`  
Classification: complete abstract/cofinal composition; production joint matrix sequence not yet emitted

## Result

The finite profile-soft sector left by `L-15630` is controlled by applying the
support average to the **complete low-plus-harmonic graph packet before the
soft/hard split**.

The exact complete matrix has the support-selected joint form

\[
 \mathcal H_R
 =(\log R)\mathcal D_R+\mathcal E_R,
\]

with

\[
 -\varepsilon_R\log R\,\widehat{\mathcal D}_R
 \preceq\mathcal E_R\preceq
 \varepsilon_R\log R\,\widehat{\mathcal D}_R.
\]

The diagonal radial branches produce the positive main profile matrix.  All
critical-line standing waves, reflected off-line cross branches, endpoint
families, terminal-prime/polar interactions and harmonic channels are retained
inside the one remainder.  The complete regularized frame envelope makes
`epsilon_R->0` on a cofinal support sequence.

After support selection, an adaptive threshold in `[tau_R,2tau_R]` exports the
exact spectral packet

\[
 P_R^{soft}=1_{[0,vartheta_R]}(G_R^{-1/2}D_RG_R^{-1/2}).
\]

Its compressed profile satisfies `D_soft<=2 tau_R G_soft`.

Using the main-profile harmonic solve `X_0=D_E^-1Y`, the leading mixed profile
block cancels exactly.  The actual solve residual contains only the remainder.
The direct-short identity gives

\[
 \left\|
 [G_S^{-1/2}\mathscr S_SG_S^{-1/2}]_-
 \right\|
 \le
 4(\varepsilon_R+4\varepsilon_R^2)
 (\log R)\tau_R.
\]

For the smooth exact source inverse,

\[
 \tau_R=R^{-1/4+o(1)},
\]

so the right side tends to zero.

## Exact finite replay

`X-15613` uses a nontrivial rational joint profile with zero main-profile Schur
complement and a negative fully shorted value

\[
 -1201/100000.
\]

The exact theorem endpoint is

\[
 3/25.
\]

The checker reconstructs both relative LMIs and the direct-short identity.  Six
central/adversarial tests pass.  Certificate SHA-256:

```text
febcbd51d850165435b53d5fc7819c16b9b94d2126887e94c11f640e377b8c74
```

## New production object

The required level artifact is no longer an unspecified soft-signature matrix.
It is:

```text
actual joint profile Gram;
regularized complete-frame Gram;
positive-measure support-average LMI;
exact generalized soft spectral projection;
main-profile harmonic solve;
complete prime/polar/archimedean trial-lift matrix;
actual residual penalty;
shifted LDL for the fully shorted soft block.
```

`M-15603` specifies the exact schema and mutation gates.

## Relation to the real direct-block data

PR #191 now contains one actual certified positive `c=5,N=1` cutoff-free
D-0001 direct block and a growing source-canonical ladder through `(c,N)=(5000,10)`.
The observed ordinary Schur scale is approximately `1/log c`.  That branch
already emits the prime/archimedean and harmonic matrices required by the new
consumer.  It still needs to emit the joint profile Gram, support-average LMI
and exact soft projector.

## Proof boundary

The finite spectral export and direct-short bound are exact.  The cofinal
selection uses the existing support-large-sieve/radial stack and the smooth
exact-source graph bound.  No production directed sequence of the new joint
LMIs has yet been emitted, and the local zeta-product/Mellin/radial
normalizations retain their declared independent-audit gates.  No RH result is
claimed from the current repository state.
