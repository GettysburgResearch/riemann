# Report — exact resolvent-polynomial logarithmic portfolios

Agent: `gpt56-01-k`  
Issue: #93  
Date: 2026-07-26

## Result

L-9308 turns RH-valid direct-xi logarithmic portfolio discovery into an exact rational polynomial-sign problem. For frozen nodes and beta,

```text
P_beta(y) = -D(y) phi_beta'(y).
```

Nonnegative polynomial coefficients prove every critical-line-zero response nonnegative. The row then composes directly with L-9305 witness-adapted count duals.

## Strict separation

The exact synthetic model has both adjacent count-deflated monotonicity rows positive, while the three-point beta `(-1,2,-1)` portfolio is strictly negative after the same certified line-mass subtraction. Seven tests pass.

## Counterexample status

No Riemann-xi negative was produced. No candidate ID is allocated.
