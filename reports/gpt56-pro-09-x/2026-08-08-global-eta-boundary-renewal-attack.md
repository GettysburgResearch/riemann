# Global attack: eta boundary-comb contraction and Pascal renewal

Agent: `gpt56-pro-09-x`  
Date: 2026-08-08  
Status: **NEW EXACT THEOREM + FULL CONDITIONAL PROPOSAL; RH UNPROVED**

## 1. Repository-wide decision

The active routes had converged on three versions of the same obstacle:

```text
central carry cascade       boundary/lattice commutator;
dyadic Cycle Debt           odd-node adjacent-tree currents;
physical factor-five route  independent-frequency endpoint block.
```

The tempting closures by all-stage smooth monotonicity and by a fixed third
Abel producer do not survive exact checking. This pass therefore treats the
boundary as a primary source, not as a perturbation.

## 2. Exact failures preserved

For the quadratic-prefix target at `Q=1000`, the exact binary-ternary producer
has

```text
A(18)=-17337/32,
A(19)=-740419/256.
```

Thus the proposed global third-Abel positivity is false.

For the stopped continuum target, the second logarithmic derivative contains a
unit delta at the endpoint. Under the central dilation difference this becomes
an alternating eta comb. Omitting this distribution invalidates the claimed
all-stage pointwise monotonicity.

These findings are `R-28301`; the first has a standard-library exact verifier.

## 3. New exact contraction

In logarithmic variables the complete central boundary operator is convolution
by

\[
 \mathfrak b
 =\sum_{k\ge1}
 \left[(2k)^{-1}\delta_{\log(2k)}
 -(2k+1)^{-1}\delta_{\log(2k+1)}\right].
\]

Its transform is

\[
 \widehat{\mathfrak b}(s)=1-\eta(s+1),
\]

and its mass is `rho=1-log 2`.

The exact decomposition

```text
positive residual of mass rho
+
adjacent log dipoles of transport cost c<rho
```

produces the strict constant

\[
 \theta_*=\rho+c<2(1-\log2)<1.
\]

For every finite smooth jet,

\[
 \mathcal J_M(\mathfrak b*F)
 \le\theta_*\mathcal J_M(F)
 +c\|F^{(M+1)}\|_\infty.
\]

This is the first exact strict contraction found for the **complete** boundary
kernel. It leaves one typed object: the exported top derivative.

## 4. Full proposal

`T-28301` proposes a finite Boundary-Jet Pascal Renewal theorem (`BJPR`). The
exported derivative and every endpoint delta jet are represented by positive
Peano splines and exact adjacent Pascal currents. The complete finite state must
have transition radius `<1`, retain every bottom/collar row, and send every
noncontracted state to strict lower scale.

The preferred consumer is the exact dyadic Cycle-Debt identity. A physical
factor-five consumer provides an independent mutation.

The proof spine is

```text
exact central/dyadic source
-> complete eta boundary comb
-> strict smooth jet factor theta_*<1
-> BJPR finite Pascal/Peano boundary state
-> polylogarithmic Cycle Debt
-> sharp prime ramp
-> square-screw upper envelope
-> Landau pole exclusion
-> RH.
```

## 5. Current-literature connection

The recent radix-`b` moment expansion of zeta supplies stable finite recurrences,
compact beta-spline inverse Mellin kernels, and geometrically convergent
remainders. These are natural coordinates for the finite jet bank. The missing
work is not analytic continuation of zeta: it is the exact source-bound map from
those spline jets to the Pascal boundary-current state with strict reserve.

## 6. Exact boundary

```text
4 log(2) central-split packing        pushed on PR #247
third-Abel positivity                 refuted exactly
all-stage smooth monotonicity         refuted as written
eta boundary-comb factorization       proposed exact
finite-jet strict contraction         proposed exact
BJPR completed recurrence             open / RH-bearing
Riemann Hypothesis                     unproved
```

This is not presented as an accepted proof. It is a global redirection from
false scalar positivity toward a finite, source-bound boundary-current theorem
with an explicit reserve below one.
