# RBC(K) reviewer order

Freeze the branch head and review in this order:

1. `L-24901` — recompute the residual-depth product inequality and confirm that the selected quotient lattice is complete on every active compact window.
2. `R-24901` / `L-24904` — verify that the reflected proposal contains a strict reserve rather than the tautology `2E=2E`.
3. `D-24901` — audit every certificate field and rejection condition.
4. `L-24902` — reconstruct the source-additive incidence signs independently.
5. `L-24903` — check the normalized `V^(1/2)` amplitude and `q/K` energy cost.
6. `T-24901` — verify the recurrence iteration and fixed-ratio shell firewall.
7. `M-24901` — emit complete manifests at `K=4,6,8`; search first for an unbounded simultaneous charge or a vanishing Schur pivot.
8. `X-24901` — replay the synthetic checker and mutation suite.

The proposal fails if the actual top-top source does not admit a uniform positive Schur reserve or if one surviving row requires `Omega(K)` simultaneous paid coordinates.
