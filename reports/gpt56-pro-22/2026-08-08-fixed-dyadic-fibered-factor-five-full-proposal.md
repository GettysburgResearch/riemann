# Full continuation — fixed-dyadic fibered factor-five contraction

Agent: `gpt56-pro-22`  
Date: 2026-08-08  
Issue: #281  
Parent: PR #266 at `bc19c0bf007fb6278f0caf2a853f4a4a7026aad2`  
Status: **FULL CONDITIONAL PROPOSAL; ONE FIXED-SOURCE PHYSICAL BOUNDARY THEOREM OPEN; RH UNPROVED**

## Executive conclusion

The continuation of Prime-Anchored Digit Transport produced one exact rejection
and one cleaner full proposal.

The exact rejection is:

\[
\boxed{
Q=e^{\delta J}
\text{ is eventually too large to shift the top source inside }V^K.
}
\]

The fixed-fraction PADT dipole is therefore the original top source, not a
current/lower-scale pair.

The replacement is a fixed dyadic two-stage filter:

\[
T_{K,V}
\longmapsto
D_{2;K,V}
\longmapsto
S_{K,V},
\]

with

\[
D_{2;K,V}=b_2*H_{K,V},
\qquad
S_{K,V}=\omega_2*H_{K,V}.
\]

Both filters are zero-safe and stably invertible. The packet-order dependence
has been isolated in one reciprocal-free common fiber `H_(K,V)`, while all
RH-bearing source geometry is carried by the fixed opposite-parity source
`omega_2`.

The remaining theorem is not a new `K`-dependent transport estimate. It is one
fixed-source independent-frequency boundary-commutator factor-five certificate,
exported in a form stable under common-fiber congruence.

## 1. Why the former radix cannot work

The top source is

\[
T_{K,V}=\Lambda*r_V^{*(K-1)}.
\]

Its support begins no earlier than

\[
2(V+1)^{K-1}.
\]

Therefore

\[
\delta_Q*T_{K,V}=0
\quad(n\le V^K)
\]

whenever

\[
2Q(V+1)^{K-1}>V^K.
\]

In particular, every `Q>=V/2` is vacuous.

The former choice

\[
Q=V^{\lfloor\delta_0K\rfloor}
\]

is at least `V` once `floor(delta_0 K)>=1`. The later choice
`Q=exp(delta_0 J+O(1))` is even larger relative to `V=exp(J/K+O_K(1))` along
an unbounded order sequence.

Thus

\[
(\varepsilon-\delta_Q)T_{K,V}=T_{K,V}
\]

through the complete coefficient endpoint.

The reciprocal-free algebra remains true, but it has not created a scale
dipole.

## 2. Fixed dyadic replacement

Define

\[
H_{K,V}
=
\ell*\mathbf1^{*(K-1)}*\mu_{>V}^{*(K-1)}.
\]

Then

\[
T_{K,V}=\mu*H_{K,V}
\]

and

\[
H_{K,V}(s)=-\zeta'(s)R_V(s)^{K-1}.
\]

The fixed dyadic difference gives

\[
D_{2;K,V}
=
(\varepsilon-\delta_2)T_{K,V}
=
b_2*H_{K,V},
\]

where

\[
b_2=(\varepsilon-\delta_2)\mu.
\]

Applying one further stable filter gives

\[
S_{K,V}
=
(\varepsilon-\tfrac12\delta_2)D_{2;K,V}
=
\omega_2*H_{K,V},
\]

where

\[
\omega_2
=
\mu-\frac32\delta_2*\mu+\frac12\delta_4*\mu.
\]

This is the fixed opposite-parity source of the factor-five programme.

## 3. Stable exponent equivalence

In normalized logarithmic coordinates,

\[
D_2=(I-2^{-1/2}\tau_{\log2})T
\]

and

\[
S=(I-2^{-3/2}\tau_{\log2})D_2.
\]

The two inverse series have `ell^1` norms

\[
(1-2^{-1/2})^{-1}
\]

and

\[
(1-2^{-3/2})^{-1}.
\]

Therefore `T`, `D_2`, and `S` have the same Hardy abscissa and the same
cumulative/local block-energy exponential exponent.

Unlike the fixed-fraction construction, the dyadic shift is genuinely present
inside the top coefficient range for large controls.

## 4. Why the factor-five package is the natural fixed source

The fixed source `omega_2` has:

- a zero-safe reciprocal-zeta transform;
- positive inverse coefficients;
- nonnegative generalized-prime coefficients;
- a compact pointwise carry wavelet;
- exact localization of every negative Kummer row to `2m<=n<5m`;
- a uniform scalar carry Schur reserve;
- a zero/first/second Selberg-carry moment tower;
- a finite parity-paired physical reconstruction;
- exact bottom-charge and fixed-ratio Mertens firewalls.

These are all independent of packet order.

The top residual tensor appears only in the common fiber `H_(K,V)`.

## 5. Common-fiber congruence

The reviewed physical block uses independent frequencies. If a base source
matrix is `A_J(t,s)`, the fibered source replaces it by

\[
\overline{H(t)}A_J(t,s)H(s).
\]

Thus every full PSD inequality lifts by congruence.

In finite coordinates,

\[
A-\kappa G\succeq0
\]

implies

\[
M_H^*(A-\kappa G)M_H\succeq0.
\]

The reserve constant is unchanged, even if the fiber map has a kernel.

This observation is powerful but scope-sensitive. It applies only when:

- the source map is linear over the common fiber;
- every product collision is retained;
- every frequency cross term is retained;
- block-cutoff commutators are exported;
- total-product carry operations are bound to an exact marked coordinate.

## 6. The surviving boundary

Pure carry windows contain a factor of `zeta(s)` and cancel every off-line pole.
They can control only the transverse transition sector.

For the fixed source, the positive first and second logarithmic carry moments
omit the unit coordinate. After fiber convolution that boundary becomes

\[
H_{K,V}
\]

itself.

It is an unbounded, RH-bearing source. It may not be described as a finite
endpoint.

The correct decomposition is

\[
I=P^{\rm trans}_{K,V}+B^{\rm fib}_{K,V}.
\]

The first term enters the factor-five carry reserve. The second retains the
physical unit-source fiber and every boundary commutator before the common zeta
factor cancels.

## 7. The fixed theorem

`FBCF5TC` asks for:

1. a complete fixed-source `omega_2` physical manifest;
2. an exact transverse/boundary decomposition;
3. a transverse physical/carry factor-five LMI;
4. a physical boundary Schur reserve before zeta cancellation;
5. an exact additive or multiplicative lower-scale recurrence;
6. a proof-object form stable under common-fiber congruence.

If the fixed-source certificate is exported as exact matrices and source maps,
the top-fiber lift is automatic. No `eta_K`, endpoint count, or unbounded order
limit is needed.

One fixed packet order is enough.

## 8. Conditional completion

The boundary recurrence gives

\[
E_B(J)=e^{o(J)}.
\]

The transverse factor-five estimate gives

\[
E_S(J)=e^{o(J)}.
\]

Stable dyadic inversion gives

\[
E_T(J)=e^{o(J)}.
\]

The non-top finite-resolvent rows are Euler-small. Hence the complete safe
prime/Möbius energy is subexponential, the rightmost-zero displacement is zero,
and RH follows.

The conditional implication is complete. `FBCF5TC` remains open.

## 9. Exact regression

The standard-library exact checker verifies:

```text
top normal form;
support floor;
vacuity of Q=V;
nonvacuity of Q=2 on a finite control;
D_2=b_2*H;
S=omega_2*H;
both geometric fixed-filter recoveries;
finite PSD common-fiber congruence.
```

Retained output:

```text
PASS_EXACT_FIXED_DYADIC_TOP_SOURCE_AND_FIBER_CONGRUENCE
cases 5
mutation tests 7/7
proof-object SHA-256
b8980012fe9f4b116b77e1e4c6c2bd70dc323cb5e74113c5aa3070c4c12b3fc
```

This is finite algebra only.

## 10. Exact status

```text
fixed-fraction PADT radix                   REFUTED AT CLAIMED SCOPE
top source / reciprocal-free fiber          PROPOSED COMPLETE EXACT
fixed dyadic b_2 factorization              PROPOSED COMPLETE EXACT
opposite-parity omega_2 factorization       PROPOSED COMPLETE EXACT
stable filter exponent equivalence          PROPOSED COMPLETE EXACT
two-frequency common-fiber congruence       PROPOSED COMPLETE EXACT
fibered factor-five source normal form      PROPOSED COMPLETE
fixed-source FBCF5TC                        OPEN / RH-BEARING
FBCF5TC -> RH                               COMPLETE CONDITIONAL PROPOSAL
Riemann Hypothesis                          UNPROVED
```

## 11. Review order

1. `R-28101`.
2. `X-28101`.
3. `L-28101`.
4. `L-28102`.
5. `L-28103`.
6. PR #269 `R-26902` and `T-26903`.
7. PR #263 parity-paired source files.
8. `T-28101`.
9. `M-28101`.
10. Complete production boundary matrices.

The proposal fails if the fixed-source matrix theorem cannot be made
module-linear, if the unit-source fiber is lost, or if the boundary recurrence
retains a current-scale coefficient of total weight greater than one.
