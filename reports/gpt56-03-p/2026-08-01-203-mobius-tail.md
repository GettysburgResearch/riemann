# Report — the complete kernel has an exact local Möbius radical extension

Agent: `gpt56-03-p`  
Issue: #203  
Stack: PR #199

## Result

Every finite localized kernel vector `h` supported in `[a,b]` admits an explicit
exact global arithmetic-radical extension.

With `g(u)=u^(-1/2)h(u)` and any integer `N` satisfying `Na>b`, define

```text
f_N(x)=sum_(n<=N) mu(n) g(nx)
       - [sum_(n<=N)mu(n)/n] [integral g] psi(x),
```

where `psi` has integral one and support strictly below `a`. Then

```text
f_N(0)=0,
integral f_N=0,
E(f_N)=h on [a,b].
```

The error is supported only below `a` and is exactly

```text
u = u,

t_N(u)
 = u^(1/2) sum_(k>N) A_N(k) g(ku)
 - u^(1/2) m_N (integral g) sum_m psi(mu),

A_N(k)=sum_(d|k,d<=N) mu(d).
```

In Mellin coordinates it is the packet multiplier

```text
[zeta(s) P_N(s)-1] hhat
```

plus the explicit pole-canceling source correction.

## Complete-kernel consequence

A finite simple-critical-line frame of the actual complete complement makes the
selected-real-zero kernel an exact graph over the old repaired radical packet.
It therefore has the old radical rank, and the local Möbius formula applies to
all of it—not only to the original radical rows.

The remaining RH-bearing estimate is now one explicit operator LMI for the
Möbius lower tail plus the Schur cross.

## Exact validation

The retained Fraction-only model verifies:

```text
target support             [2,5]
cutoff                      N=3
source integral             0
local reconstruction cells  4
graph kernel                (1,-1/6)
graph norm                  37/36
formal zero target/tail      7/5, -7/5
```

Three nonzero lower-tail samples and nine adversarial mutations pass. The
proof-object SHA-256 is

```text
5a682844bcf83a618b76f796a988764902d7ade58b9db43a695c399a103c0bd4
```

## Literature match

The truncated Möbius multiplier is structurally aligned with the arithmetical
Nyman–Beurling approximants of Báez-Duarte and Burnol. Those works also warn why
pointwise/local reconstruction cannot be promoted silently to the required
`L2` or form convergence.

## Status

Qualitative radical-synthesis existence is closed. The uniform Weil-form bound
for the explicit divisor tail is not proved, and RH is not claimed.
