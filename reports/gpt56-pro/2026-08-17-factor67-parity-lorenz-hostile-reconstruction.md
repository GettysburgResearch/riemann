# Factor-67 parity-Lorenz hostile reconstruction

The manuscript pass found that the certified local objects in the lineage are
not the issue.  The failure is at the exact composition interface.

The `alpha=r lambda` causal identity is an unsigned source identity.  After a
recursive parity swap, one rough prime contributes `2r^2`, while the native
coefficient is `r`.  The missing `r-2r^2` source must be explicitly owned and
paid.  PR #566's `L-96651` names such a reserve but does not construct it.

The odd-history witness independently gives target gap

```text
17.0050865382190525906984557536069...
```

at `X=61841`.  Hence local canonical Hall cannot pay the missing source after a
single swap.  PR #574 correctly shows that TP2 and Cauchy-Binet do not imply the
global capacity needed for compensation.  PR #575 correctly shows that a
scalar-exact coupling cannot be promoted to simultaneous row positivity.

The exact remaining producer is the completed-parity scalar Lorenz inequality
`CPSL67`.  Its finite primal/dual is explicit, and a dual threshold is an exact
separator if it fails.  Uniform `CPSL67` would give eventual nonnegativity of
`R_X=5c_X(2)+3c_X(3)`, whose reciprocal-zeta Mellin numerator is zero-free in
`Re z>0`; Landau then yields RH.  Thus `CPSL67` is precisely the theorem carrying
the zero-free-strip strength.

The new paper proves all downstream analytic interfaces and all finite algebra,
but it does not conceal `CPSL67` as a reserve lemma.  RH remains unproved.
