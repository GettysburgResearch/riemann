# Confluent cluster renormalization after Zeta23 finite isolation

## Verdict

**RH remains unproved.** The finite-isolation route has nevertheless moved one step forward: raw evaluation-Gram collapse caused by any fixed finite coalescing packet is a coordinate singularity, not an invariant loss of interpolation geometry.

The canonical Newton divided-difference basis converges to a strictly positive Hermite jet Gram. For a reflected pair of depth `y`, the unit cardinal cost grows like `y^-2`, but the depth-normalized target has finite cost and negative Weil value exactly `-2m y^2`.

## New exact reduction

The previous alternative was

```text
persistent negative corrected kernel
or
collapse of the local zero-evaluation Gram.
```

It can now be sharpened to

```text
persistent negative corrected kernel
or
unbounded active jet/cluster complexity
(or loss of tail control in that invariant basis).
```

Finite multiplicity, finite coalescence, and any fixed finite collection of clusters do not survive as separate failure mechanisms.

## Why it matters

The Zeta23 proportion theorem controls average first/two-moment information and cannot see a single off-line pair. The repository's kernel programme can see one pair, but its quantitative capture cost was vulnerable to ill-conditioned point coordinates. Confluent renormalization makes the correct invariant cost explicit and identifies the true scale: the combined tail and corrected-floor radius must beat the square of the pair's horizontal depth.

For the uniform control window the limiting derivative capture cost is exactly `12`, matching the expansion

```text
2/[sinh(y)/y-1] ~ 12/y^2.
```

## Remaining theorem

The unresolved quantitative problem is no longer finite-cluster interpolation. It is to control a packet whose active order may grow like the local zero count `O(log T)` while simultaneously retaining a tail/floor radius `o(y^2)`.

Candidate next attacks:

1. an explicit lower bound for the order-`r` weighted moment Gram with `r=O(log T)`;
2. a cluster partition whose high-order tail is paid by the Zeta23 first/two-moment budget;
3. a complete Xi-cardinal construction in a native confluent metric;
4. a source-complete arithmetic floor stable under the divided-difference basis.

## Replay

```text
PASS_ZETA23_CONFLUENT_CLUSTER_RENORMALIZATION
```

The retained verifier uses only the Python standard library.
