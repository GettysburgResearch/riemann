# MESOTRANS106630 hostile audit

The exact Cauchy least-squares formulation is mathematically sound, but its normal-equation optimizer does not supply the missing estimate. Every primal residual decomposes into:

```text
endpoint index
+ favorable model-space escape
+ numerator-subfactor omission
+ normal-equation error.
```

Only the last term is removed by solving the transport equations.

The positive-source family `c+cos(nt)` shows that all presently advertised structural inputs can coexist with transport residual density one. Therefore the remaining theorem must be genuinely Xi-arithmetic.

For a conclusion-only attack, the direct sign-transition statistic at the simple real zeros of `Xi^(5)` is quantitatively weaker: the pinned `0.9863` simple-real proportion leaves an `0.0863` transition allowance for a `90%` conclusion, whereas the positive shallow transport packet asks for `0.022`. The transport route remains useful because it is compatible with Cauchy interpolation and mollified normal equations, but it is not formally closed.
