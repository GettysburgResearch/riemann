# Three-interface hostile audit of the live SHARP/common-parent RH candidate

## Executive verdict

The recent repository work has genuinely closed the component-row kernel,
endpoint Radon--Nikodym map, and most abstract composition algebra. The live
chain nevertheless contains one source-level gap that none of those theorems
repairs:

```text
contracted alpha child != native rough Euler occurrence.
```

The issue is visible before asymptotics and before any Hall solver. In one
prime, the alpha identity has zero net shifted coefficient, while the native
Möbius factor has coefficient `-p^(-1/2)`.

The result is not that the SHARP programme fails. It is that its common-parent
closure must use a coefficient-exact first-owner Euler source, or bypass the
source recursion through the scalar SHARP Harnack route.

## Vulnerability ranking

### 1. Native rough source and same-operator calibration

Severity: **binding**.

The RN child map proves domination and functoriality of endpoint measures. It
does not prove the native rough coefficient. The calibration coboundary is
correct algebra, but it telescopes whichever operator it is given. It cannot
turn `r^2` into `r`.

Repair: `L-99601`.

Remaining theorem: `FCHD67`.

### 2. Compact Hall and endpoint restriction

Severity before audit: **high but finite**.

The compact Hall prefix inequalities were repeatedly imported from directed
campaigns. They are small enough to reconstruct independently. The new replay
checks all thresholds, identifies the exact worst fixture, and preserves a
strict moat.

The child map also required a composition theorem. The exact RN cocycle now
gives it.

Disposition: **closed at scalar/fixed-row scope**.

### 3. Noncancellation and Landau

Severity before audit: **moderate**.

The large-row asymptotic introduces a uniformity and statement-to-use surface
that the proof does not need. Rows 2 and 3 already have an exact algebraic
noncancellation theorem.

Disposition: **closed exactly**.

## Reinvented minimal proof graph

```text
coefficient-exact first-owner rough source
 -> FCHD67, or HNM67 negative-mass bypass
 -> rows 2 and 3 nonnegative / positive witnesses
 -> exact two-row numerator noncancellation
 -> Landau
 -> RH.
```

The remaining producer is explicit and RH-bearing. RH is not established by
this audit.
