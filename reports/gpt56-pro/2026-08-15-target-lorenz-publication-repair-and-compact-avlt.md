# Target-Lorenz publication repair and compact AVLT theorem

Date: 2026-08-15  
Branch: `research/gpt56-pro/91381-euler-shell-recovery`  
Parent before this packet: `8085903e190589e3117a0c7494fb1150c2222401`  
RH status: **unproved**

## Executive result

The reviewer correctly identified a publication mismatch: the advertised complete AVLT/tail/native-root theorem objects were not on the branch. This packet repairs that mismatch and publishes only reconstructible claims.

The new arithmetic result is substantial but deliberately narrower than the withdrawn summary:

\[
 \Theta_j(p,y)>0
 \qquad(p\ge67,\ 1\le y<67,\ py<166000,\ 2\le j\le66).
\]

Together with the exact common-source optimizer, this closes every compact Target-Lorenz row margin. The proof is a complete real activation-cell census, not a prime grid or random scan.

The same packet proves the exact tail parity reserve

\[
 A_eB_o-A_oB_e>7/4\qquad(z\ge166000),
\]

but does not promote it to the full component-row determinant. The analytic tail and live native source/channel allocation remain explicit gates.

## Compact proof object

```text
702,511,095 row-cell inequalities;
702,511,091 direct rectangle certificates;
4 correlated-cell repairs;
raw direct minimum 0.0012010574018749666 at (125,1,62);
certified direct margin after audit >0.001;
correlated-cell lower bounds all >0.315.
```

The four coarse exceptions are exactly `(2j+1,1,j)` for `j=63,...,66`. Their child rows vanish; monotonicity in `y` reduces each to `y=1`, after which the exact relation `q=1/sqrt(x)` closes the interval.

## Tail progress

The exact prefix census checks `259133` tail states and proves a minimum lower bound `1.7529746231757814...` at prefix `168113`. This is the leading positive coefficient expected in the Euler--Green tail, but the boundary row and causal child correction still require one uniform analytic inequality.

## Native composition hardening

`L-91783` makes the source-Fubini rule explicit: one coefficient vector is carried through target, literal score, rows, ordinary responses, boundaries and the shared port; radix-four detail is formed only after the common ordinary sum. It proves formal nonduplication but does not invent the live allocation.

`L-91784` records the correct recursive currency: positive native `Y_4`-weighted slack. It explicitly forbids substituting a continuum equality score.

## Exact frontier

```text
publication mismatch correction                  CLOSED
common-source optimizer                           CLOSED / L-91720
proportional determinant reduction                CLOSED / L-91780
compact AVLT py<166000                            CLOSED / L-91781
tail parity reserve                               CLOSED / L-91782
analytic Tail-AVLT                                OPEN
formal source-Fubini                              CLOSED / L-91783
live source-owned native allocation               OPEN
native slack consumer                             CLOSED CONDITIONAL / L-91784
Riemann Hypothesis                                UNPROVEN
```
