# Adaptive Jensen index and convex-order final attack

This pass reconstructed the common physical interface of PR #729's
current/Turan collar programme and PR #731's signed hard-band and oriented
phase-angle programme.

## Main exact result

For a finite reduced inner quotient `U=A/B`, the scalar

\[
\mathfrak J_y(U)
={1\over4\pi y}\int_{\mathbb R}\log|U(x+iy)|^2dx
\]

is exactly

\[
\sum_{b\in B}\min(1,\operatorname{Im}b/y)
-
\sum_{a\in A}\min(1,\operatorname{Im}a/y).
\]

It converges to the signed all-pass index as `y` tends to zero, and its scale
coarea recovers the complete signed vertical divisor.

The soft depth charge `2 delta/(H+2 delta)` is a universal random-scale average
of these exact clipped indices under the probability density

\[
2s(1+s)^{-3}ds.
\]

Every monotone source profile is independently a positive Stieltjes mixture of
the exact hard-band visible/complementary first-crossing charges. Hence the
current owner law, adaptive source band and endpoint index are one positive
scale resolution.

## Convex-order strengthening

Root compression gives a doubly stochastic matrix sending the parent roots to
the critical points plus the centroid. Consequently every convex function on
the complex plane decreases under differentiation after the centroid atom is
retained.

For real polynomials, the horizontal Jessen excess

\[
\sum_{p(\rho)=0}|\operatorname{Im}\rho-H|
-
\sum_{p'(c)=0}|\operatorname{Im}c-H|-H
\]

is nonnegative for every `H>=0`. Along a derivative ladder ending in a
real-rooted packet, the complete parent penetration is exactly the sum of these
nonnegative adjacent-rung excesses.

Thus the horizontally averaged balanced Turan flow is already favorable.

## Honest surviving defect

The oriented Hankel charge decomposes exactly as

\[
\|H_U\|_{\mathcal S_2}^2
=
\lim_{y\downarrow0}\mathfrak J_y(U)
+
\|H_{U^{-1}}\|_{\mathcal S_2}^2.
\]

The signed integer is fully represented. The remaining term is degree-zero
reverse phase energy, equivalently the canonical-correlation or phase-angle
defect. An equal-degree one-pair quotient has zero signed index but strictly
positive reverse phase energy, so this defect cannot be discarded.

The exact remaining target is `D0PHASE105650`: absorb that degree-zero physical
overlap using the actual positive Xi current/Turan hierarchy, with pointwise
localization and the cofinal endpoint ledger retained.

RH remains unproved.
