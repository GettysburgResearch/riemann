# 2026-08-14 — Hall-free raw-residual stopped-leaf attack

**Repository:** `gfreund123/riemann`
**Proposed base:** PR `#468`, head `a41f81466f85d52597c97b41505756a8860698d0`
**Proposed successor branch:** `research/gpt56-pro/91685-hall-free-raw-residual`
**RH status:** unproved

## Earlier deposit verification

The earlier hostile-leaf repair is present on the live PR `#436` branch. Its retained replay has 151 flow pieces, maximum upward displacement four, target gain above `1.939528135...`, and minimum row gain at row 66 above `0.008381548...`.

The direct raw replay below obtains the same row-66 value without a transport.

## Newest live inputs

PR `#462` gives an exact target-proportional source producer. PR `#466` proves that literal `P_61` entropy absorbs its declared-score debt after row assembly. PR `#467` closes both causal-profile corridors, supplies score/target Lorenz cutoffs below `2000`, bounds aggregate debt, and leaves one explicit target-Lorenz component-row family. PR `#464` moves Hall to the certified root window and proves formal one-global-quantizer commutation. PR `#468` corrects the native normalization: the canonical finite-Euler row equals the native datum plus an exact positive rough reservoir, and its Native-Root Capacity Theorem is the controlling remaining producer.

## Radical local replacement

For `p>=67` prime and `1<=y<67`, define

\[
G_{p,y}=D_{P_{61},py}-p^{-1/2}D_{P_{61},y},
\qquad
C_{p,y}=p^{-1/2}D_{P_{61},y}.
\]

The inherited rows are positive by `L-91347`. On frontier rows the child vanishes and the full global `L-91364` gives positivity. Exact response kernels show ordinary and radix-four response positivity. The target and declared score are strictly positive, and literal entropy exceeds declared score by more than `893/100`. Since `y<67`, the child is exactly `p^{-1/2}c_y` with its native capacities.

Thus the preferred one-prime current candidate of PR `#468/O-91377` is proved row-positive on the complete terminal stopped-leaf domain. Stopped-leaf Hall and PR `#467`'s remaining target-Lorenz row-family producer are unnecessary for the aggregate leaf row/score signs.

## Native-capacity correction

The pair `G+C` equals the canonical finite-Euler row, not the native root row. By `L-91379`, its ordinary/detail responses equal the native capacities plus the exact positive rough reservoir. Therefore this packet does not count `G+C` as a native packing and does not claim NRCT.

The remaining theorem is the explicit `NRSLI` allocation:

```text
native root + exact rough reservoir
  -> source-disjoint least-prime stopping-line fibers
  -> terminal raw generator + exact terminal child
  -> one owner for every reservoir atom and root correction
  -> ordinary/detail/native-port inequalities inside one parent copy
  -> total recursive coefficient <1/8 and bounded Y4 slack.
```

This is a narrower and more accurate frontier than the first draft's generic root bridge.

## Replay

The companion standard-library checker verifies:

```text
17,622 inherited/frontier sector cases;
5,358 terminal nonzero-Mobius support identities;
formal current+child and monotonicity algebra;
all 870 rows of the hostile leaf at 80-digit precision;
local content and dependency-lock hashes.
```

It explicitly does not replay the imported global `L-91364` certificate, the inherited-row/entropy certificates, the exact PR `#468` rough-reservoir theorem, `NRSLI/NRCT`, the root analytic correction packet, or the endpoint-to-RH chain.
