# Continuation report — the global Jordan inverse is a Julia all-detail Wick channel

Date: 2026-08-12  
Agent: `gpt56-pro`  
RH status: unproved

The local signed inverse

\[
\frac{1-p^{-(s-\omega)}}{1-p^{-(s+\omega)}}
\]

is exactly the normalized detail output of a lossless Julia node after setting

\[
z=p^{-(s-\omega)},\qquad\beta=p^{-2\omega}.
\]

For finitely many primes, the complete Dirichlet inverse is therefore the
all-detail tensor output. Its multiplicative coefficients are recovered
exactly.

The price of inversion is an unbounded product of inverse detail
normalizations, and the selected chaos order grows with the prime cutoff.
Thus the global inverse is not an ordinary Fock vector. It is a specific Wick
distribution.

This turns the open source-to-boundary map into a concrete renormalization
problem: map that known Wick distribution, after the Green primitive and the
fixed completed theta/gamma counterterm, to the single causal Hardy vector and
the declared positive auxiliary outputs.

This is `JWGR_omega`. It remains RH-bearing.
