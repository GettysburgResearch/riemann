# Prime Clark–Redheffer continuation of the parity-domain-wall route

## Freeze

```text
main:        b837c12199dd407116f604ce6c938039d1a76da4
parent PR:   #412
parent head: f848f3cd2fb7ca6e46dbab5a053a8e51fc97729a
branch:      research/gpt56-pro/91510-prime-clark-redheffer
```

## Result

The prime part of the parity-domain-wall problem is now a canonical passive
impedance rather than an unresolved atomic sampling channel.

For every safe local Euler factor, the Julia column `(m,d)` gives the Cayley
impedance

```text
h=(1-m)/(1+m),
```

whose real part is positive and whose dissipation is exactly the local Julia
detail. Ordered products compose by

```text
h_(12)=(h_1+h_2)/(1+h_1 h_2).
```

At the kernel level the complete finite-prime defect telescopes with all cross
terms. On a safe line the infinite product converges to one explicit Herglotz
kernel.

This repairs the prime-atom-isolation obstruction in the correct way. At a
prime resonance the detail vanishes, but the coefficient-one returned Euler
state carries the norm forward. The earlier failed continuous-to-atomic
comparison omitted precisely that state.

## New final joint

The prime source map is no longer open. The remaining theorem is an
archimedean Redheffer interconnection:

```text
safe prime Clark impedance + returned Euler state
+ gamma ladder - pole/domain-wall channel
-> completed six-safe-jet connection
 + favourable parity production
```

with all delay, reflection, and bridge coordinates retained.

This is `CRDWC_a` in `T-91510`. It implies the parent `PDWT_a`, then CPPD and
RH. It remains open.

## Hostile boundary

Prime passivity cannot be promoted by itself. The completed factor contains an
active pole channel, and the three-scale Cauchy observation moves the middle
positive ladder to the adverse side. The interconnection must occur before
applying the signed recurrence.

## Exact state

```text
local prime Julia node                         EXACT
prime Cayley/Herglotz connection               EXACT
finite/infinite safe Redheffer cascade          EXACT
prime atom-isolation geometry                  CLOSED
completed gamma/pole Redheffer completion      OPEN / RH-BEARING
Riemann Hypothesis                             UNPROVED
```
