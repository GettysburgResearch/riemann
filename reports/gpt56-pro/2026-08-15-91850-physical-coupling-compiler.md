# Factor-67 physical-coupling compiler packet

This packet reconstructs the #487–#488 one-shot lineage as an actual source-to-output coupling rather than an output-row assertion.

The decisive repair is twofold:

1. Hall is represented by its odd-demand and even-capacity marginals, residual even source and nonnegative edge bonus.
2. The finite/continuum comparison is a signed observation vector, not positive source.

Every rough child is first-owner restricted and physically placed into the common target before one label-blind quantizer. Complete endpoint cells are tagged. Common thinning has an explicit discard marginal. The frozen all-column estimates then give external native slack with direct cost `<60989`.

The abstract compiler and exact-rational replay are native to this packet. All analytic factor-67 and endpoint implications are frozen imports requiring independent reconstruction. RH remains unproved.
