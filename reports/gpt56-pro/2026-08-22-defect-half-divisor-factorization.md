# Defect current meets the half-divisor occupancy route

The completion-defect source is the difference of two convolution squares.
Taking the literal arithmetic square root at both endpoints gives one root-free
field and one root-bearing companion.

The common mother admits the matching ratio-four factorization. This makes the
defect current one exact two-field packet rather than an abstract
near-collision condition.

The diagonal estimates prove that no source-energy or same-product
multiplicity remains open. A corrected largest-owner argument removes the
previously advertised optional unsquared cofactor: every cofactor prime lies
below `sqrt(X)`, so the completed products are exactly `p a^2`. The map
`(p,a) -> p a^2` is injective, and all distinct square cores under one owner
have only polylogarithmic overlap.

The conclusion-bearing object is therefore exclusively the cross-owner,
off-diagonal correlation of distinct integer products inside the fixed compact
multiplicative support window.

This identifies the stress-tensor, completion-defect, half-divisor and balanced
phase routes as four coordinate systems on one final physical operator.

```text
HDNC102703   OPEN / RH-BEARING
RH            UNPROVED
```
