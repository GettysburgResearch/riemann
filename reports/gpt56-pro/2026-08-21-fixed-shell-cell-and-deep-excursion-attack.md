# Fixed-shell cell calculus and deep-excursion attack

The audited T101200 implication matrix initially proposed raw negative-cell sparsity. Exact evaluation shows that shallow negativity can occupy a large fraction of a dyadic block, so that target is replaced.

The fixed ratio-536 kernel has seven elementary bands. On every open unit cell the conclusion-facing scalar is exactly

```text
A_N sqrt(X) + B_N log(X) + C_N.
```

Its unit-cell oscillation is `O(N^-1/2)`. Consequently continuous deep excursions are detected at the midpoint `N+1/2` with only a vanishing threshold adjustment.

The corrected closure target is:

```text
there exists tau_L=2^o(L) such that
#{N in [2^L,2^(L+1)): G(N+1/2)<-tau_L}=2^o(L).
```

Together with the elementary fixed-shell amplitude bound, this implies subpower negative mass and RH. The sampled tail estimate remains unproved.
