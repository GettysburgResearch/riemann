# Falsification and repair of the frozen PR #488/#489 factor-67 closures

Date: 2026-08-15
Frozen proposals:

```text
PR #488  9acd381fa168db02a03646ab16851daebbf4d0fd
PR #489  0bb487c8a0782f601be0a3041743b357ad93726a
```

Both heads were treated as immutable.

## Executive result

The shared producer-to-consumer chain contains one genuine composition type
error. The finite/continuum comparison is a signed quadrature defect, but both
frozen proposals place it inside an all-positive unused-source telescope.

This affects the constructions differently:

```text
PR #488:
    the numerical all-column feasibility may survive;
    the claim that all fourteen stages are positive source splits does not.

PR #489:
    the logarithmic root-cost proof fails because it applies a
    positive-packet target-mass theorem to a signed comparison datum.
```

The endpoint consumer itself survived this audit.

The repair is a third assembly implementation:

```text
positive source ledger;
signed observation ledger;
explicit native reserve;
export first-generation children;
insert one canonical feasible row for every child;
stop.
```

On the frozen analytic estimates it gives the candidate bound

\[
0\le J_\Lambda(X)-\mathcal H(d_X)<61744
\qquad(X\ge10^{12}).
\]

RH remains unproved pending independent reconstruction.

## I. Shared source-versus-observation flaw

`L-91840` defines the retained finite/continuum error from a signed measure
\(\eta_X\) and controls its total variation. `L-91733` similarly uses absolute
ordinary and detail bounds.

Nevertheless, `L-91843` requires its mismatch/collar/taper/base stage to be a
positive source split, and `L-92883` includes the retained-cell comparison in a
positive unused-source packet whose cost is then bounded by target mass.

A signed correction may have one current owner, but ownership does not make it
positive source. `R-92900` records the exact type failure.

## II. Correct two-ledger composition

`L-92900` separates:

1. a positive source identity
   \[
   P=P^{cur}+\sum_b\beta_bU_bP_b+U^{src};
   \]
2. a signed finite observation error \(e_X\).

If \(u_X=\Xi(U^{src})\) and the all-column estimates give
\(e_X\le u_X\), then

\[
r_X=u_X-e_X\ge0
\]

and

\[
\Omega_X
=
\Xi(d_X^{cur})+r_X+
\sum_b\beta_bU_b\Omega(P_b).
\]

This proves equality and positivity without pretending that \(e_X\) is a
positive packet.

## III. Root cost

The imported whole-cell, all-column one-shot estimates are used only at their
valid response scope:

```text
common square-root thinning                  <12012
nonterminal signed comparison                <4
terminal signed comparison                   <48972
positive omissions                           <1
port and large-X base                         0
-------------------------------------------------
root native cost                             <60989
```

This is `L-92901`. No estimate of
\(J_\Lambda(X)-4\sqrt X\) appears.

## IV. Terminal children

Actual target-mass normalization gives

\[
m(\widetilde P_b)=M_X,\qquad
\sum_b\beta_b<\frac18,\qquad
M_X<\frac{6039}{2}.
\]

Every positive child has a canonical feasible row with deficit at most twice
its target mass. Hence

\[
\sum_b\beta_b\Delta(\widetilde P_b)
<
\frac{M_X}{4}
<
\frac{6039}{8}.
\]

No descendant receives another root Hall, quantizer, finite comparison,
omission, correction or port.

## V. Final composition

For

\[
d_X=d_X^{cur}+\sum_b\beta_bU_bk_b
\]

one has

\[
C_{d_X}\le w_X,\qquad
\Xi(d_X)\le\Omega_X,\qquad
d_X\ge0,
\]

and

\[
\begin{aligned}
J_\Lambda(X)-\mathcal H(d_X)
&=\delta_X^{root}
+\sum_b\beta_b\Delta(\widetilde P_b)\\
&<
60989+\frac{6039}{8}\\
&=
\frac{493951}{8}
<61744.
\end{aligned}
\]

The frozen one-sided endpoint consumer then gives the candidate RH conclusion.

## VI. Exact review boundary

The new packet does not make the imported analytic statements self-verifying.
Review must still reconstruct:

```text
the P61 stopping-line source identity;
factor-67 root Hall and normalized rows;
whole-cell endpoint measure interpretation;
all-column and terminal response bounds;
positive packet canonical-row theorem;
root target mass <6039/2;
native Y4 dual;
prime-square and Mellin--Landau endpoint consumer.
```

The new result is a composition repair and a new terminal-child implementation,
not an accepted proof of RH.
