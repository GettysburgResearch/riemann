# Low-order Xi descent: critical-residue Pick rank–trace

## Finding

The high-order coherence tail on PR #726 does not touch the first Levinson
step. The current PR already contains the simple-window Hermite--Pick trace form.
This checkpoint extends it to confluent/multiplicity-robust analytic
subcompressions, freezes the exact current record constants, and supplies a
concrete Cauchy-power carrier with quantitative trace/HS reserve.

## Exact finite theorem

The centered divided difference of `p/p'` is a sum of signed residue atoms.
Good real critical points are PSD rank-one atoms; wrong real critical points are
NSD; nonreal/confluent blocks have fixed signature budgets. Therefore

```text
good extrema >= effective rank - nuisance positive index.
```

The published xi-prime simple/distinct constants imply nuisance density at
most `821/10000`.

## Quantitative target

```text
eta >= 919/1000
  -> line proportion >= 3369/5000 = 0.6738.
```

The archimedean Cauchy-power model has `eta>=49/51`; a 1% source perturbation still
has `eta>=160083/173417=0.923110...` and would yield `0.682020...`.

## Remaining arithmetic

The open `LPRT105310` theorem is to evaluate a source-fixed growing Xi/Xi-prime
Pick compression by explicit formula, keeping the trace within 1%, the HS norm
within 1%, and the canonical-product/tail positive index `o(N)`. This is a
matrix prime-side estimate, not an infinite reverse-Rolle cascade.

The current packet proves no new global zero proportion and no RH result.
