# All-order Cauchy storage and explicit generalized-Jordan Stinespring

## Status

```text
branch: research/gpt56-pro/91008-cauchy-square-clark-jordan
parent: PR #396
RH:     UNPROVED
```

This continuation attacks the sole source-to-boundary theorem on the Cauchy/Jordan spine. It closes the scale algebra and the positive arithmetic source dilation, but it does not close their completed critical-boundary coupling.

## Main results

### 1. All-order storage

For the scale-free dyadic Cauchy gate

\[
F_0(y)=\frac{16}{(y+4)^2}-\frac1{(y+1)^2},
\]

define

\[
F_{m+1}(y)=F_m(y)-4^{-(m+2)}F_m(y/4).
\]

Every residual is positive. More precisely,

\[
F_m(y)=(m+2)!y\int_0^\infty\frac{W_m(r)}{(y+r)^{m+3}}\,dr,
\]

where

\[
W_0(r)=r\mathbf1_{[1,4]}(r),
\qquad
W_{m+1}(r)=\int_{r/4}^rW_m(v)\,dv.
\]

The storage coefficient `4^(-(m+2))` is sharp at every order. The recurrence suppresses the lower geometric endpoint factorially:

\[
W_m(r)=\frac{(r-1)^m}{m!}+\frac{(r-1)^{m+1}}{(m+1)!}
\qquad(1\le r\le4).
\]

After normalization by `a^(-2m-4)`, the inherited state returns with coefficient exactly one.

### 2. First minimum-phase innovation

The first residual is

\[
\frac{27y(14y^2+163y+224)}
{(y+1)^2(y+4)^2(y+16)^2}.
\]

It is the boundary modulus square of the stable minimum-phase rational function

\[
\Psi_a(s)=\sqrt{378}a^3
\frac{s(s+\sqrt\alpha a)(s+\sqrt\beta a)}
{(s+a)^2(s+2a)^2(s+4a)^2},
\]

where

\[
\alpha,\beta=\frac{163\mp5\sqrt{561}}{28}.
\]

The factor further splits into a stable three-pole Green factor and three scalar right-half-plane Schur factors.

### 3. Explicit divisor Stinespring

The normalized sieve coefficients

\[
q_a(n)=\prod_{p\mid n}(1-p^{-2a})
\]

obey

\[
q_{a+b}(n)=\sum_{de=n}q_a(d)q_b(e)e^{-2a\log e}.
\]

Normalizing these positive summands gives an explicit isometry

\[
V_{a,b}e_n
=\sum_{de=n}
\sqrt{\frac{q_a(d)q_b(e)e^{-2a\log e}}{q_{a+b}(n)}}
e_d\otimes e_e.
\]

It factorizes coherent vectors exactly,

\[
V_{a,b}k_{a+b,s}=k_{a,s}\otimes k_{b,s+a},
\]

and intertwines vertical phases and the logarithmic generator:

\[
V_{a,b}D_x=(D_x\otimes D_x)V_{a,b},
\]

\[
V_{a,b}L=(L\otimes I+I\otimes L)V_{a,b}.
\]

The isometries are coassociative.

### 4. Coefficient-one RH criterion

Put

\[
\mathcal E_x(a)=a^{-4}[\mathcal N_x(2a)-\mathcal N_x(a)].
\]

Subject to review,

\[
\mathrm{RH}
\iff
\mathcal E_x(a)\ge\mathcal E_x(2a)
\quad(x\in\mathbb R,a>0).
\]

Under RH the innovation is the zero-side Gram generated by `Psi_a`. A matched maximal-depth off-line zero forces strict failure near its horizontal depth.

## What remains

The source isometry and the Cauchy storage factor live on opposite sides of the explicit formula. The final theorem must couple them through the exact completed gamma and pole channels.

The required map must:

1. return the inherited state with coefficient one;
2. preserve every logarithmic coproduct cross term;
3. emit `Psi_a` as a genuine output port;
4. include the completed gamma and rational pole factors;
5. leave no hidden signed same-scale port.

PR #398 supplies an important firewall: positive source kernels, one-Green complete monotonicity, boundary unitarity and a cocycle do not imply the target Pick or Weil positivity abstractly. Therefore the coupling must use the exact completed xi factorization.

## Verification

```text
PASS_ALL_ORDER_CAUCHY_STORAGE_AND_DIVISOR_STINESPRING
checks: 15745
exact sieve cocycle/probability checks: 1800
formal logarithmic coproduct checks: 1223
max spectral-factor error: 2.11e-81
```

The replay establishes finite algebra and synthetic high-precision controls only.

## Honest boundary

```text
all-order dyadic storage hierarchy       EXACT
sharp storage coefficient at each order  EXACT
minimum-phase first residual              EXACT
explicit divisor Stinespring              EXACT
coassociativity/logarithmic coproduct      EXACT
coefficient-one RH criterion               PROPOSED COMPLETE
completed source-to-Weil intertwiner       OPEN / RH-EQUIVALENT
Riemann Hypothesis                         UNPROVED
```
