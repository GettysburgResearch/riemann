# SHARP owner-martingale hardening

The latest repository graph reduces RH to the global tail of one factor-67
SHARP Harnack scalar.  The SHARP kernel, exact RN child thinning, direct Mellin
consumer, and directed range through `10^8` survive reconstruction.

The broader positive-row tree still used an alpha-child split that algebraically
returns its positive parent and deletes the native `-p^(-1/2)` rough
coefficient.  This packet records that separator and removes that tree from the
conclusion-facing graph.

The replacement is source-specific.  The local square
`(1-67^(-z))^2/zeta(z)` has a positive reciprocal, an exact positive
logarithmic owner, a bounded parity martingale, logarithmic coefficient energy,
and a strict source-level owner half-contraction.  Pure coefficient variation
is insufficient on squarefree histories, so the SHARP scale is retained.  The
augmented scale-sensitive owner energy is also logarithmic.

The sole open theorem is `SOCE99610`, an incoming multiplicative Carleson
embedding on the explicit owner DAG.  Polylogarithmic logarithmic negative mass
from that embedding implies RH by the direct SHARP Mellin transform.

RH remains unproved.


## Supercritical theorem and the critical wall

A further attack on incoming owner congestion succeeds for every real boundary
power `m>=2`.  The threshold argument is already quadratic: the complete
labelled-prime mass `sum_p p^(-3/2)+67^(-3/2)` is below one, so adjacent Euler
levels pair positively at every scale.  The cubic estimate
`log(zeta(2)/(1-67^-2))<1/2` is retained as an independent stronger incoming
column check.  Returning
to square-root growth removes exactly this extra decay, and the column sum
becomes the divergent prime-harmonic mass.  This proves that `SOCE99610` is the
critical homogeneity wall rather than a byproduct of a loose estimate.
