# Wide-angle repository map and critical Euler-fiber synthesis

Date: 2026-08-08  
Agent: `gpt56-pro`  
Repository: `gfreund123/riemann`  
Frozen base for this branch: `main@d1f72553c73037a3990f8c505d0c260d41a223f4`  
Status: **RESEARCH SYNTHESIS AND FULL CONDITIONAL PROPOSAL; RH UNPROVED**

## 1. Executive conclusion

The current repository does not contain a proof of RH that can be assembled merely by ordering already-proved claims.

It does contain a substantially stronger common backbone than it did a few review waves ago:

```text
fixed-ratio Möbius shell
<-> all fixed ratios by causal l1 filters
<-> first Farey-cell Mertens increment

correct two-frequency reflected physical block
+ exact finite inverse packet
+ direct terminal/balanced partition
+ complete-lattice terminal Euler closure

exact carry/LP duality
+ sharp 4 sqrt(X) parabolic seed
+ canonical nonnegative greedy packing
+ exact Green correction and mass-slack ledgers.
```

The apparent number of proof routes has decreased.  The arithmetic programmes now expose one reciprocal-zeta/Möbius obstruction in several coordinate systems.

This pass contributes one new exact bridge and one new fixed-source proof architecture:

\[
\boxed{
(1-2^{1-s})(1-2^{1/2-s})^2.}
\]

This **critical Euler fiber**:

1. annihilates the carry Green kernel's pole and double half-pole modes;
2. converts the failed Green spline into derivatives of one nonnegative compact window;
3. has a completely positive Dirichlet inverse;
4. has a nonnegative generalized prime sequence;
5. retains every off-line zeta pole;
6. has a strict frequency-uniform reserve on compact substrips;
7. replaces growing endpoint/rank assertions by one fixed five-tap two-adic source.

The proposed final theorem is one source-bound two-frequency reflected contraction `EFRC`.  It is not proved in this pass.

## 2. Live-state changes after the preceding overview

### 2.1 PR #257: fourth-pass consolidation

PR #257 rechecked the live graph through PR #255 and confirmed:

- the fixed-ratio shell, all-ratio filters, and first-cell Mertens decoder are durable;
- the two-frequency reflected block is the correct local adapter;
- terminal Euler closure leaves the balanced Möbius source;
- PR #239's absolute rank closure is false;
- PR #243's conditional-Hankel representation is false;
- PR #254's monotone positive cover is false;
- Greedy Slack and DCRS are the same discrete theorem;
- PRs #243/#247/#252 use the same global carry state;
- GET contains the scalar prime-ramp discrepancy rather than bypassing it.

That review remains the best current cross-PR status audit.

### 2.2 PR #255: verdict typing

PR #255 correctly separates:

```text
failed derivation,
false surrogate,
false special construction,
lifecycle rejection,
false exact theorem.
```

This matters here.  The failed conditional-Hankel proof does not imply that the carry profile is negative.  The false bounded-rank theorem does not disprove source-specific signed cancellation.  The false monotone cover does not disprove signed defect-to-slack transport.

### 2.3 PR #248 continuation

The elementary branch has added:

- removal of proper prime powers at `O(log^2 X)` proposed cost;
- an exact direct constant-`b` incidence transport between ordinary-prime rows;
- a Lagarias/prime-ramp/carry equivalence triangle;
- an explicit colossally-abundant slope ledger and one-dimensional layer-cake coordinate.

These are useful reductions.  They do not prove their final scalar inequality.

One repository consistency issue remains visible at the current head: `L-24518` still contains the stale flow-plateau telescoping formula

\[
\sum_{j=A}^{B-1}\Delta^2U_p(j)=U_p(B)-U_p(A),
\]

whereas the correct boundary is the difference of first differences.  Current `L-24520` supplies a different, correct direct constant-`b` incidence identity.  The stale file should be reclassified rather than cited as the repair.

## 3. Global route map

## 3.1 Carry, LP, and elementary arithmetic

The common finite system is now clear.

For

\[
\beta_{nq}
=\frac{\lfloor n/q\rfloor(q-1-(n\bmod q))}{n+1},
\]

two differences give

\[
\Delta^2[(n+1)\beta_{nq}]
=(n-1)(\mathbf1_{q\mid n}-\mathbf1_{q\mid n-1}).
\]

For any real vector `b`,

\[
J_X(b)
=\sum_m b_m\log\frac m{m-1}
=\sum_{q=p^a}\Lambda(q)v_q(b).
\]

The parabolic seed already has

\[
J_X(b_0)=4\sqrt X-O(\log X).
\]

The hard step is a sharp feasible correction.

Current coordinate systems are:

```text
Greedy Slack       total unsaturated carry mass;
DCRS               the same debt after the continuum-resolvent adapter;
FGCM               an X-dependent positive convolution minorant;
CPF                a positive four-band renewal;
GET                energy of the canonical exact signed Green correction;
signed transport   a positivity-preserving deformation of that correction.
```

They are not six independent proofs.

### Durable progress

- exact finite carry/entropy algebra;
- exact Möbius affine contraction;
- exact nonnegative greedy feasibility;
- automatic `O(log^2 X)` lower-order entropy loss;
- exact mass-slack conservation;
- exact parabolic main term;
- exact outer-band feasibility;
- correct local prime-power descent after the `q+1` sign repair;
- fixed-ratio band repairs, subject to review of their current dependency chain;
- exact signed Green correction.

### Current obstruction

One must prove either

\[
\Sigma_X=X^{o(1)},
\]

or construct another sharp positive/signed certificate with the complete critical mass.

The scalar prime-ramp projection remains RH-bearing.

## 3.2 Fixed-ratio Möbius shell

For fixed `0<c<1`,

\[
Q_c(t)=e^{-t/2}[M(e^t)-M(ce^t)]
\]

has transform

\[
\frac{1-c^{z+1/2}}
 {(z+1/2)\zeta(z+1/2)}.
\]

Its fixed-block energy is RH-equivalent.  All fixed ratios are connected by explicit causal `l1` filters.  The ratio `2/3` is the first Farey-cell firewall; the dyadic ratio is the Euler-aligned source with positive inverse coefficients.

The shell route is presently the cleanest scalar representation of the balanced Möbius obstruction.

## 3.3 Reflected source and balanced Type II

The correct architecture is

```text
two-frequency reflected local block
-> complete signed source packet
-> direct terminal/balanced partition
-> complete-lattice Euler closure
-> all-truncated balanced Möbius source
-> strict source-specific reserve
-> lower-scale contraction
-> RH.
```

PR #250 gives a good abstract certificate schema.  It does not yet construct actual top-corner matrices, a uniform Schur reserve, or a bounded charge injection.

The same-sign Möbius cubes show that odd-prime rank can grow with packet order.  A successful proof must create cancellation through an additional source fiber or a genuine reflected reserve, not through dimension.

## 3.4 Prime-only Hardy and semiprime dispersion

The ordinary-prime compact-window criterion, analytic square, semiprime convolution, and vertical pole tomography are strong exact reductions.  The final locally uniform critical embedding is another form of the signed balanced source theorem.

This remains a natural language for classical analytic number theory, but it has not supplied a distinct final estimate.

## 3.5 Analytic totient and Farey packets

The analytic-totient Mellin criterion, reduced-frequency formula, and Jordan Bohr square remain useful.  The frozen determinant proof and the generic operator closure are rejected.  The surviving local scalar estimate must retain the actual Möbius vector and the endpoint channels.

This route now functions mainly as a precise mutation/firewall for any proposed balanced contraction.

## 3.6 Screw, Haar, and prime transport

The square-screw and dilation-defect criteria remain excellent global consumers.  They identify the rightmost-zero exponent with one explicit one-sided arithmetic growth rate.

The carry programme currently supplies the most developed proposed producer for those consumers.

## 3.7 Localized Weil, Schur, and finite frames

The finite algebra and directed computations are substantial.  However:

- the square screw is already a principal matrix coordinate;
- a complete corrected-kernel floor excludes the exact off-line Xi-cardinal direction;
- conditioning and finite Schur bookkeeping cannot supply the global sign.

This is still a valid independent architecture, but its last theorem is not presently smaller than the arithmetic scalar.

## 3.8 Prolate/CCM and affine ground states

The source reservoirs and finite wrapper estimates are useful.  The current ground-line theorem is RH-bearing because a false-RH even cardinal vector retains a fixed negative moat.  No reviewed theorem currently transfers real-zero structure to a suitable non-ground interior eigenline.

This route is not near a full proof at present.

## 3.9 Brownian convex order

The positive off-line variance defect and line-zero martingale budget are beautiful exact equivalences.  The final Brownian completion/stop-loss inequality is itself equivalent to RH.  No independent probabilistic proof is currently available.

## 3.10 Negative finite searches

No strict authenticated finite violation of an RH-necessary inequality has been found.  The finite directed infrastructure remains valuable but does not alter the positive global frontier.

## 4. First new connection: carry Green cancellation becomes one compact reflected source

The frozen carry-Hankel proposal failed because alternating derivative signs did not imply a positive Hankel kernel.  Its exact obstruction was the negative determinant `-233/64`.

`L-26201` applies the fixed operator

\[
\mathcal E
=(I-2\tau_{\log2})(I-\sqrt2\,\tau_{\log2})^2.
\]

It annihilates precisely

```text
e^t,
e^(t/2),
t e^(t/2).
```

The result is not asserted conditionally Hankel.  It is proved to be

\[
(\partial_t^2+\partial_t)W
\]

for one explicitly nonnegative compact window

\[
W=(e^t\mathbf1_{[0,h]})
 *(e^{t/2}\mathbf1_{[0,h]})
 *(e^{t/2}\mathbf1_{[0,h]}).
\]

This gives an exact bridge:

```text
carry inverse
-> fixed critical Euler fiber
-> compact positive Möbius window
-> correct two-frequency reflected block.
```

The operation has no moving order and no unbounded endpoint count.

## 5. Second new connection: a fixed sign-complete fiber supplies a true reserve

Moving the filter to the arithmetic source gives

\[
B_{\mathcal E}(s)
=\frac{(1-2^{1-s})(1-2^{1/2-s})^2}{\zeta(s)}.
\]

Its inverse coefficients are positive, and its generalized prime sequence is nonnegative.  For every odd core, the complete local source is the fixed five-tap polynomial

\[
(1-z)(1-2z)(1-\sqrt2z)^2.
\]

Thus every odd same-sign Möbius cube is retained and paired with one bounded two-adic fiber.  The filter does not claim the cube has bounded rank; it supplies an explicit sign-complete companion direction independent of that rank.

Moreover, on

\[
1/2+\eta\le\sigma\le1-\eta,
\]

\[
|P(\sigma+it)|
\ge
(2^\eta-1)(1-2^{-\eta})^2.
\]

This is a genuine strict reserve.  Any bound proved for the Euler-filtered source controls the original inverse-zeta source on compact substrips.

The missing estimate has therefore been reduced to a fixed-source question rather than an arbitrary balanced packet:

> Can the complete two-frequency reflected identity for this five-tap source be assembled with a strict Schur reserve and a lower-block charge smaller than that reserve?

`T-26201` names this `EFRC`.

## 6. Why `EFRC` is a serious proposal rather than a renamed equivalence

The proposal adds structure not present in the bare shell criterion:

1. **exact annihilation:** every continuous pole/half-pole Green mode is removed;
2. **positive inverse:** the direct source has an explicit positive convolution inverse;
3. **positive forcing:** its generalized Selberg coefficients are nonnegative;
4. **correct locality:** PR #241 supplies the physical two-frequency block;
5. **strict reserve:** the Euler multiplier has a uniform lower bound;
6. **fixed boundary grammar:** only one five-tap dyadic fiber is present;
7. **firewall retention:** odd Möbius cubes and the `2/3` shell survive.

The unproved `EFRC` inequality is still a deep arithmetic theorem.  But it is a materially more specific production object than generic BTP, Brion rank, endpoint count, or global carry-profile positivity.

## 7. Proposed production programme

### Stage 1: exact source block

Build the complete physical block for the source `b_E` and window packet

\[
\{W,uW\}
\]

at one dyadic block.  Retain every cross term.

### Stage 2: direct Schur reserve

Emit exact or directed matrices

\[
G_m,C_m,D_m
\]

and test

\[
G_m-C_m^*D_m^{-1}C_m\succeq\kappa_0G_m.
\]

A zero reserve rejects the mechanism immediately.

### Stage 3: boundary routing

Enumerate every unmatched source row.  It must route to one of the four earlier dyadic blocks or to a polynomial endpoint budget.  No same-scale row may remain.

### Stage 4: mutations

Run:

- the same-sign odd Möbius cube;
- the `2/3` first Farey/Mertens cell;
- deletion of one two-adic tap;
- replacement of the two-frequency block by the false one-frequency block;
- zero-reserve and overcharged-boundary controls.

### Stage 5: symbolic block theorem

Only after several exact blocks succeed should one prove a uniform `EFRC` theorem.

## 8. Parallel carry/LP work that remains worthwhile

The elementary positive-minorant programme remains serious and should continue independently.

The highest-value tasks are:

1. canonicalize Greedy Slack and DCRS under one claim ID;
2. derive a source-specific estimate for the exact blocker-loss sum;
3. test FGCM as an `X`-dependent positive projection rather than global profile positivity;
4. preserve the canonical Green correction as a discovery source;
5. do not reuse the monotone positive-part cover;
6. keep the exact prime-ramp scalar visible in every energy theorem.

A sharp carry certificate would feed the same fixed-source consumer and would provide a second producer for the proposed proof.

## 9. Pending review queue

Highest priority:

1. current PR #248 files `L-24517` through `L-24522`, with the stale `L-24518` disposition made explicit;
2. PR #244 mass-slack and blocker identities as the canonical discrete hinge;
3. PR #247's exact no-discretization-loss minorant map;
4. PR #250 after actual top-corner matrices are emitted;
5. the new `L-26201/L-26202` algebra and annular reserve;
6. the common square-screw/Landau consumer under one normalization contract.

Lower priority until a source manifest exists:

- abstract Brion/contact-count proposals;
- another growing-rank reflected schema;
- generic operator coercivity;
- global carry-profile positivity through the refuted Hankel representation.

## 10. Exact status

```text
repository-wide exact reductions                    substantial
complete proof assembled from current PRs            no
carry/LP sharp finite certificate                     open
signed defect-to-slack transport                      open
fixed-ratio Möbius-shell energy                       RH-equivalent / open
reflected top-corner contraction                      open
critical Euler-fiber algebra                          proposed complete
positive inverse/generalized-prime source             proposed complete
strict annular multiplier reserve                     proposed complete
fixed-source EFRC                                     open / RH-bearing
Riemann Hypothesis                                    unproved
```

## 11. Final assessment

The repository is no longer best viewed as many independent proof attempts.  It contains one dominant arithmetic obstruction and several exact interfaces around it.

The most promising two producers are now:

```text
A. sharp carry/LP minorant or Greedy Slack theorem;
B. fixed critical Euler-fiber reflected contraction EFRC.
```

They meet at one explicit fixed-ratio inverse-zeta source.  The first is elementary and finite.  The second is analytic and source-reflected.  Either would close the same reviewed consumer.

RH is not proved in this pass.  The new contribution is a fixed, sign-complete, source-specific bridge that removes the exact local Hankel obstruction and supplies a strict reserve without hiding the Möbius firewall.
