# Proposed centered-Q4 heat-resolvent prime-coherence bridge

This successor is research-only and is not part of either frozen-head review.

It deposits:

```text
L-93850  exact heat representation, weighted Cauchy bridge, safe diagonal
T-93851  open HRPBD estimate and conditional CPBD/RH implication
X-93850  finite Fourier/constant regression
```

The open theorem is

\[
\left|
\sum_{p<r}\int_0^\infty e^th_{p,N}(t)h_{r,N}(t)dt
\right|
\ll N\log^B N.
\]

No claim is made that this estimate is easier than RH. It is a genuinely new
heat-domain formulation with an unconditional same-prime diagonal and an
explicit resolvent kernel.
