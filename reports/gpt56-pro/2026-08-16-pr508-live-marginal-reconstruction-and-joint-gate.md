# PR #508 live-marginal reconstruction and the joint native Target–Lorenz gate

Date: 2026-08-16
Repository: `gfreund123/riemann`
Frozen base PR: `#508`
Frozen base head: `4ae97dffd1f76ed3244b8f3028560ffa80663caf`
Intended successor: `research/gpt56-pro/94020-target-lorenz-live-marginal`
RH status: **unproved**

## Executive verdict

PR #508 remains the controlling directed arithmetic packet.  Its complete
51,118,080-event tail campaign is not rerun here.  Review #504's second and
harder objection is resolved at source-registry scope but not at complete
positive-producer scope.

The new packet now gives explicitly:

```text
the continuum endpoint root x=X/s;
every Möbius colour and its exact E/T/S coefficient;
every literal finite endpoint coefficient a_(X;m,k)^fin;
the unique P61 divisor / rough history / first owner / parity label;
the exact paired stopping-tree coefficient and parity swaps;
the complete 229-atom hostile leaf (p,y)=(67,13);
all 132 finite-forcing beta/gamma terminal-prime weights at x=871;
a common q/4q coordinate contract;
a directed positive outer-fibre theorem for 1<=x<67.
```

The same reconstruction also proves a decisive obstruction:

```text
the actual oriented rough-child aggregate has a strictly negative q=2
ordinary and detail coordinate, so it cannot be realized branchwise by
separately nonnegative physical rows.
```

Thus the missing proof is not another owner label or another synthetic leaf.
It is one **joint native Target–Lorenz coupling** which cancels the finite
forcing's rough overcapacity against the actual oriented children *before*
entry into the positive physical-row cone.

The result is an unconditional, fail-closed frontier theorem.  It is not a
complete RH proposal and does not promote the synthetic PR #508 fixture into a
live common parent.

---

# I. Frozen genealogy and disposition

## PR #508

```text
head: 4ae97dffd1f76ed3244b8f3028560ffa80663caf
```

Retained without modification:

```text
directed tail and row-66 certificate extremality;
exact compact/tail join;
complete arithmetic AVLT candidate;
typed terminal-leaf algebra (nu,B,sigma);
two-ledger native deficit and endpoint interfaces.
```

Not accepted as established by the old synthetic replay:

```text
the actual endpoint source marginal;
the map from endpoint occurrences to the stopped tree;
the complete live leaf/path-weight family;
the resulting common parent.
```

## Review #504

```text
head: fe1a3c94379c3d159efa05ea216d12643e63be16
```

The numerical-tail objection is answered by PR #508.  The live-marginal
objection controls this packet.

## PR #507 and review #512 / PR #514

```text
PR #507: dfaa70cd2eefcabbf6717e3da060792904c7f357
review #512: 625a9273696a1f017f32d37bf9052423ee8590c7
PR #514: 944dabba065271f08f3cade0215292dcfbab590f
```

Retained:

```text
native source-tree versus P61 rough-lift distinction;
unique first rough ownership;
paired parity/orientation bookkeeping;
necessity of a joint cancellation interface.
```

Rejected:

```text
branchwise positive realization of actual oriented children;
one-shot or terminal-child implementations relying on such rows.
```

## PR #509, #510 and #511

```text
PR #509: e01daee9cdfea35d2a7d2591f1df6c8080084119
PR #510: 3d1b1ff88bb96719cd63221bc37fa01e80aab563
PR #511: 6ece82279cb03474ebc79914db572f6ff095d238
```

Retained:

```text
finite anchored occurrence versus continuum Volterra marginal;
exact E/R two-channel identities;
owner-labelled hybrid-source architecture;
q and 4q before detail;
signed comparison outside positive source.
```

Not accepted as complete evidence:

```text
schema fixtures as the live anchored marginal;
one hostile leaf as the complete root generator;
positive placement of actual oriented children.
```

## PR #513

```text
head: 4275fe97aa5ba885210abb6296c7048386e5909d
```

The direct positive outer Volterra row is compatible and is recovered here in
source coordinates.  Its anchored equality remains dependent on the open
joint native coupling.

---

# II. The actual endpoint fibre

For a global endpoint `X` and endpoint coordinate `s`, the root parameter is
not chosen by optimization:

\[
 x=\frac Xs.
\]

The exact continuum inverse gives the signed equality fibre

\[
 \lambda^\star(x)
 =\sum_{k\le x}\frac{\mu(k)}{\sqrt k}
   \left(2\sqrt{x/k}-1\right).
\]

Each live colour therefore has:

```text
occurrence coefficient: |mu(k)|/sqrt(k);
root parameter:          x=X/s;
parity:                  sign(mu(k));
equality profile:        E(z)=2sqrt(z)-1;
reserve profile:         R(z)=sqrt(z)-1;
SHARP target:            T=E+2R;
declared score:          S=2E+R.
```

The same occurrence coefficient and owner are used in every channel.  The
reserve channel is row-zero; the equality channel carries the component row
and all ordinary observations.

On a literal integer endpoint cell `m`, however, the actual finite-divergence
coefficient is

\[
 a_{X;m,k}^{\rm fin}
 =\frac{\mu(k)}{\sqrt{mk}}\log\frac X{mk}.
\]

These two marginals are not atomwise identical.  At

\[
 (X,m,k)=(536,8,67),
\]

the finite coefficient is zero while the paired equality and SHARP target
coefficients both equal `1/sqrt(67)`.  The finite/continuum relation is a signed
comparison ledger, not an identity of source atoms.

The generated certificate includes every one of the 2,473 finite endpoint
occurrences at `X=536`, rather than a preview or toy fixture.

---

# III. Exact P61/rough owner registry

Every squarefree source colour factors uniquely as

\[
 k=d p_1\cdots p_t,
 \qquad d\mid P_{61},
 \qquad 67\le p_1<\cdots<p_t.
\]

The complete owner label is

```text
endpoint cell m;
root parameter X/m;
small divisor d;
ordered rough history (p1,...,pt);
first rough owner p1, or root when t=0;
Möbius parity;
causal path.
```

The path coefficient before observation is exactly

\[
 (d p_1\cdots p_t)^{-1/2},
\]

with one parity swap at each least-prime edge.  Applying any linear row,
ordinary, boundary or literal-score map to this same registry preserves the
source-tree identity.  Detail is formed only after the common ordinary sums at
`q` and `4q`.

At `X=536` the deterministic source certificate contains:

```text
327 squarefree source atoms;
167 P61 finite-forcing atoms;
160 actual rough-child atoms;
one owner for every atom.
```

---

# IV. Real terminal leaf and nonduplicating parent weights

The packet emits all 229 active `P_61` divisors of the hostile terminal leaf

\[
 (p,y)=(67,13),
 \qquad x=871.
\]

Nine of those atoms have an active child term.  Every record contains one
common causal scalar and the formulas for:

```text
component row;
SHARP target;
declared score;
ordinary q;
ordinary 4q;
radix-four detail after the common sum;
first owner.
```

For the finite-forcing parent, list the rough primes `p>x/67` and put

\[
 r_p=p^{-1/2},
 \quad
 s=\prod_p(1-r_p),
 \quad
 \lambda_p=r_p\prod_{q<p}(1-r_q),
 \quad
 \alpha_p=r_p\lambda_p.
\]

The actual nonduplicating leaf and child coefficients are

\[
 \beta_p=\frac{\lambda_p}{1-s},
 \qquad
 \gamma_p=r_p\beta_p,
\]

with

\[
 \sum_p\beta_p=1,
 \qquad
 \sum_p\gamma_p<67^{-1/2}<\frac18.
\]

At endpoint 871, all 132 such records are deposited.  These weights allocate
the `P_61` finite-forcing parent exactly.  They do not cancel the native paired
source's oriented rough-child marginal.

---

# V. Outer fibre closes unconditionally

For `1<=x<67`, rough primes are absent.  Define

\[
 L_E(x)=\sum_{k\le x}\frac{\mu(k)}{\sqrt k}
   (2\sqrt{x/k}-1),
\]

\[
 L_R(x)=\sum_{k\le x}\frac{\mu(k)}{\sqrt k}
   (\sqrt{x/k}-1).
\]

A directed, rational square-root enclosure on all 66 quotient cells proves

\[
 L_E(x)>0.318,
 \qquad
 L_R(x)\ge0.
\]

Therefore the complete outer endpoint fibre has a positive rank-one
realization in the equality and reserve channels.  No causal difference of
Volterra derivative fibres, rough lift, exported child or Schur port occurs.

---

# VI. Exact branchwise-child obstruction

The paired stopping line has the signed identities

\[
 \Sigma F_{61,X}=N_X+\mathcal R_X,
 \qquad
 \Sigma P_X^{\rm child}=-\mathcal R_X.
\]

At ordinary physical column `q=2`, the single rough term `m=67` gives, for
`X>134`,

\[
 C_{\mathcal R_X}(2)
 \ge\frac1{\sqrt{134}}\log\frac X{134}>0.
\]

Thus the actual child aggregate has a strictly negative ordinary coordinate.
At `X=536`, both the ordinary and detail child coordinates are at most

\[
 -\frac{\log4}{\sqrt{134}}<-\frac19.
\]

A separately nonnegative physical row has nonnegative ordinary response.
Therefore no observation-preserving branchwise positive child realization can
exist.

This is not a failure of owner bookkeeping.  It is a sign obstruction in a
physical coordinate.  Finite forcing and oriented children must be coupled
jointly before any positive physical-row split.

---

# VII. The exact remaining producer: JNTLC

For fixed `X`, generate every actual anchored occurrence and every admissible
joint template in one common typed coordinate order:

```text
source incidences;
target;
declared score;
component rows;
ordinary q and ordinary 4q;
boundary/terminal data;
one owner coordinate per occurrence.
```

A valid coefficient vector solves

\[
 A_Xz=b_X,
 \qquad
 G_Xz\ge0,
 \qquad
 z\ge0.
\]

The same scalar multiplies every coordinate of a template.  Oriented child
terms may cancel finite-forcing terms inside a joint template, but may not be
exposed as a separately nonnegative row.

The fail-closed alternative is:

```text
positive primal certificate;
or exact symbolic/directed Farkas separator for the declared cone.
```

A separator for an incomplete template list is not a refutation of every
native realization.  Conversely, a numerical feasible point without symbolic
equalities and directed strict margins is not a proof.

JNTLC is the sole remaining producer isolated by this packet.

---

# VIII. q/4q, Y4 and endpoint consequence

If JNTLC returns one cofinal joint certificate, combine it with the proved
outer fibre.  Evaluate ordinary `q` and `4q` on the same total row and only then
form

\[
 \Xi(q)=C(q)-2C(4q).
\]

The frozen PR #508 all-column and terminal estimates then yield native
feasibility.  Define the numerical slack only afterward:

\[
 r_X=\Omega_X-\Xi(d_X)\ge0.
\]

The unchanged direct ledger is

```text
common thinning               <12012
nonterminal signed comparison <4
terminal signed comparison    <48972
literal positive omissions    <1
port / large-X base           0
-----------------------------------
Y4-weighted native deficit    <60989<61000.
```

The finite dual has the one-sided orientation

\[
 F_\Lambda(X)
 \le J_\Lambda(X)-\mathcal H(d_X).
\]

Thus JNTLC plus the frozen prime-square and Mellin--Landau inputs would produce
the endpoint implication.  Since JNTLC is open, that endpoint is not reached
here.

---

# IX. Replay and proof boundary

Retained verdict:

```text
PASS_LIVE_ENDPOINT_SOURCE_TREE_AND_JOINT_CANCELLATION_GATE
```

Proof objects:

```text
generator payload:
38a5376b596fe7f638d803457836278a26b44f2fec8028d3adadd27ba2032463

verification payload:
d2ce818cce6616fb566a402ffef72773d7fcbac717cfda9ff8d27127fc940654
```

Exact boundary:

```text
PR #508 directed tail                          FROZEN / RETAINED
continuum endpoint fibre and root              EXACT
finite endpoint occurrence registry           EXACT / GENERATED
P61/rough owner and path registry              EXACT / GENERATED
outer x<67 source/physical fibre               PROVED DIRECTED
real hostile P61 leaf                          GENERATED / 229 ATOMS
finite-forcing beta/gamma weights              EXACT / 132 RECORDS
branchwise actual-child positivity             FALSE / EXACT SEPARATOR
joint native Target-Lorenz coupling            OPEN / RH-BEARING
q/4q, Y4 and endpoint composition              COMPLETE CONDITIONAL
Riemann Hypothesis                              UNPROVEN
```
