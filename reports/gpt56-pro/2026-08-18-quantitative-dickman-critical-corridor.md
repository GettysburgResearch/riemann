# Quantitative Dickman closure up to the critical factor-67 saddle

## Result

The fixed-power Dickman theorem is upgraded to a uniform moving-power theorem.
For the native rough state

\[
\mathcal F(Y,z)=
\sum_{P^-(m)\ge z}
\mu(m)m^{-1/2}b(Y/m),
\]

one now has

\[
\mathcal F(Y,z)=a_*\sqrt Y\rho(u)
+O\left(\sqrt Y\,u/\log z\right),
\qquad u=\log Y/\log z.
\]

This follows from an explicit convolution-distance theorem between the atomic
prime-harmonic measure and the continuous Dickman measure, plus the literal
bounded `P_61` source remainder and an upper-bound sieve for rough integers.

For every fixed `epsilon>0`, the state is positive uniformly whenever

\[
u\le(1-\epsilon)
\frac{\log\log Y}{\log\log\log Y}.
\]

The same scale is sharp for the declared mechanism: beyond it, the homogeneous
Dickman main is smaller than the uncertainty obtained by taking absolute values
of the bounded source remainder.

## Interpretation

The project no longer has an unspecified “subpower core.” Every off-critical
state is closed. The remaining source is one explicit correlation of the
bounded annular remainder against the critical `mu(m)/sqrt(m)` rough channel.
This is the exact place where a future-prime Bellman barrier, a signed Type-II
estimate, or a source-specific saddle analysis must enter.

```text
CBRC67 -> GPC67 -> RH
```

remains conditional. RH is unproved.
