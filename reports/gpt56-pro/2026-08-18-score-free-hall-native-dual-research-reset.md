# Score-free Hall and native-dual factor-67 research reset

## Executive result

This packet reconstructs a proposed full factor-67 closure after accepting the
exact score-sign obstruction which invalidated the complete target-null Hall
packet.  The replacement is a two-sort common parent: positive recursive source
plus positive current-only row bonus.

The candidate is stronger than the vanished description in four ways:

1. the target-null bonus is explicitly **score-free**, not merely said to have
   favorable score;
2. actual child target masses are normalized only after integration;
3. omissions are performed before the single quantizer and partial retained
   cells are kept outside the positive-source telescope;
4. ten hostile mutations are specified and checked.

## Core algebra

For a no-upward Hall flow `t(o,e)`,

\[
 \sum_eT_e\rho_e-\sum_oT_o\rho_o
 =\sum_eu_e\rho_e+
  \sum_{o,e}t(o,e)(\rho_e-\rho_o).
\]

The first term is recursive source.  The second is a nonnegative physical row
bonus.  The declared-score analogue of the second term is negative at `x=2`, so
it is not part of the source packet.

## Physical compiler

The endpoint frame integrates both positive sorts.  One label-blind B-spline
kernel acts on them together.  Hall bonuses, omissions, signed mismatch,
collars, and ports are current-owned.  Only target-bearing residual source can
be exported as a child.

The ideal root identity reserves full child capacities.  Retained-cell error,
collar, interpolation, and terminal comparisons are paid after observation.
The square-root thinning leaves strict detail slack in every physical column,
including `q<K`.

## Native objective

The exact endpoint deficit is

\[
 J_\Lambda(X)-\mathcal H(d_X)
 =\langle Y_4,\Omega_X-\Xi(d_X)\rangle.
\]

No comparison with `4sqrt(X)` is used.  The conservative local ledger is below
`60989`, actual child mass is below one eighth, and the resulting unit-mass
envelope is uniformly bounded.

## Stress-test disposition

The replay checks:

```text
negative x=2 score edge;
exact Hall residual+bonus identity;
bonus row positivity;
bonus cannot be scored or recursed;
causal coefficient identity;
actual-mass contraction;
label-blind quantizer commutation;
omission-before-quantizer ownership;
q<K constant arithmetic;
positive radix-four inverse;
native slack cocycle;
60989 ledger and geometric envelope;
forbidden RH-strength inputs.
```

It does not authenticate the inherited large finite/directed campaigns.  Those
are frozen by path/blob where known and by exact PR head otherwise.

## Honest boundary

This is a historical proposed-candidate packet, not a complete theorem or a
community-accepted proof. PR #521's later live-marginal analysis leaves the
required joint native coupling open. The first review target is `L-98802`:
whether the endpoint-frame source
and row-bonus sorts are transported by exactly the same common kernel into the
row consumed by the all-column theorem.

RH remains unproved pending independent reconstruction.
