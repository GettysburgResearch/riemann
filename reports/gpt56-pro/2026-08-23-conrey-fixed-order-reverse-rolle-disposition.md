# Conrey fixed-order proportions and the Xi''' -> Xi'' reverse-Rolle question

## Bottom line

The requested numerical statement already exists unconditionally:

```text
xi''   critical-line proportion > 95.84%
xi'''  critical-line proportion > 98.73%
```

Both are independent rows in Conrey's 1983 fixed-order theorem.

There is no theorem in the current packet—or in the ambient Cartwright class—
which derives the first number from the second number alone.  The exact
counterexample `2+cos z` shows that even a derivative proportion of 100% can
coexist with a parent proportion of 0%.

## What the reverse-Rolle programme has added

The programme now supplies:

1. the exact factor-two wrong-extremum conservation law;
2. the exact Riccati/Pick and Hermite–Biehler dictionaries;
3. a conditional residue-coherence proportion transfer;
4. an exact first-moment identity for critical residues;
5. the new two-level consequence that at least 97.8932% of the known real
   `xi'''` zeros are correctly oriented extrema of `xi''`.

The final item uses both Conrey rows and is not circularly promoted as a proof
of the lower row.

## Research boundary

A genuinely new historical reverse implication still requires an independent
Xi-specific estimate at the real zeros of `xi'''`.  The most concrete target is

\[
\left(-\sum_{\Xi'''(c)=0}{\Xi''(c)\over\Xi''''(c)}\right)^2
>
\left({1\over2}+\delta\right)
R_3(T)
\sum_{\Xi'''(c)=0}
\left|{\Xi''(c)\over\Xi''''(c)}\right|^2.
\]

Nothing in the 98.73% input alone proves this signed inverse-curvature moment.

RH remains unproved.
