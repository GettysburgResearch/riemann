# Source-to-inner-factor completion attempt — 2026-08-30

## Question

Can the remaining source-to-inner-factor/model-space Gram estimate in
`T-104636` be proved from the exact seven-grade source module, the fractional
current, and the source-negligible carrier variation?

## Answer

Not by the proposed mechanism.

The attempted proof exposed a structural collapse rather than a missing
estimate.

## 1. Fractional resolution does not weaken the physical charge

The fifth-current weight is pointwise comparable in both directions with the
Calderón exponential weight.  After the declared gamma tail is removed,

\[
e^{-\delta_0}(C-m\varepsilon)\le I\le C.
\]

Therefore the open fractional residual \(I\) and the shallow canonical charge
\(C\) are quantitatively equivalent.  The unmatched topological dimension is
already bounded by \(C\), so an \(I\)-bound already proves the new zero-count
theorem.

## 2. The seven-dimensional certificate has the solution at grade zero

The proposed family is

\[
Z_r=P_{K_A}D^rP_{K_B}M_H.
\]

At \(r=0\),

\[
Z_0=P_{K_A}P_{K_B}M_H
\]

is exactly the unrestricted projection minimizer.  Thus the seven-dimensional
least square equals the physical model-space defect before the degree-one
through degree-six grades are used.  The regularized matrix is merely that
defect plus a positive ridge term.

The finite source module has not been promoted through inner factorization;
the unknown inner projection has been included in the definition.

## 3. Exact zero-residual source transport is insufficient

For \(F_n(t)=c+\cos(nt)\), \(c>1\), one can place the function exactly in the
frozen source coordinate of `L-104637`.  The degree-six Bézout identity holds
with zero residual and no carrier error.  Nevertheless the endpoint quotient
is

\[
U_n=-{1\over z\,b_{-1/c}(z)}
\]

and has model-space charge two per period.  The nontrivial pole height is
\((\log c)/n\to0\).

This makes the failure directly source typed: it is not enough to know a
finite differential relation between complete numerator and denominator
sources.

## Correct frontier

The valid \(>90\%\) ledger remains.  The live theorem is now stated without a
misleading source-completion layer:

```text
XIMICRO104640:
  bound the forced unmatched endpoint index plus the matched
  phase/truncated-Toeplitz nonnormality charge for the literal Xi fifth
  companion packet below the exact shallow allowance.
```

The `1/10` carrier-adapted Xi Fourier law may help the matched phase term.
It does not pay the unmatched topological index or commute through the common
outer factor.

## Scientific status

```text
concrete completion found                         NO
proposed seven-grade bridge corrected             YES
universal source-to-inner promotion refuted       YES
valid conditional >90% ledger retained            YES
more than ninety percent proved                   NO
RH proved                                         NO
```
