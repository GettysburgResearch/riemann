# Three-route closure attack

RH remains unproved.

The durable results are `L-91410` through `L-91414` on this branch. They establish the secant barrier, cutoff-determinant reduction, finite-prefix reduction, eventual large-`p` positivity, and shift-eight debt localization.

The three active closures are: (1) a directed compact determinant certificate; (2) a finite Lorenz/Monge certificate below cutoff `2000`; and (3) adjacent endpoint-butterfly correction of the shift-eight flow.
