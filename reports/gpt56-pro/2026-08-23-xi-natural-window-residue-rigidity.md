# Xi natural-window residue rigidity — research report

## Selection after the 2026-08-22 integration

The integration at main `852d8aa...` leaves no reviewed path to RH. The two
new post-release programmes are:

1. the compact common-mother scale–phase transport;
2. the Xi reverse–Rolle/Riccati–Pick cascade.

The Xi programme has a clean opportunity for unconditional progress because
its new residue moment criteria can be tested against a positive Fourier
saddle already proved in the high derivative regime.

## Advance

The old saddle approximation controlled `T_m a_m ->0`. A second-order Laplace
normal form retains the Gaussian factor and extends the approximation to every
fixed `T_m a_m<=C`. This is the natural central-limit width
`sqrt(m/log m)` in the original Xi variable.

The simultaneous adjacent-order expansion then shows that all derivative-ratio
residues are asymptotically equal and negative. Their common scale is the
native moment ratio `M_(m-1)/M_(m+1)`, asymptotic to `w_m^-2`.

This proves the residue-coherence mean-value condition on the whole high band,
not merely the separate real-zero count. It supplies a genuine load-bearing
base case for the residue cascade.

## Remaining frontier

The obstruction has moved below the Gaussian band. The next passes on this
same PR should attack one of:

- a moderate-deviation extension `C=C_m->infinity`;
- a summable coherence-loss theorem under one-step descent;
- a localized canonical-product version of the PR #723 second-residue ledger;
- a bound for the off-real and second-derivative debts during the transition
  out of the Gaussian regime.

No claim of RH is made.
