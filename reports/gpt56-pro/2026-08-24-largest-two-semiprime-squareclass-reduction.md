# Largest-two semiprime-squareclass reduction

## Result

The equal unordered-pair Wick owner is ideal for free Fock energy but hides the
arithmetic order needed by the physical restriction. The same source may be
reallocated to the two largest selected prime labels with only a
`O(log^2 Y)` labelled-energy change and no change whatsoever after physical
realization.

After completing every smaller cofactor prime, the hard products are exactly

```text
p q a^2,

p>q>P^+(a).
```

This representation is injective. Same-pair square-core overlap is
polylogarithmic. Distinct pairs are exactly distinct squareclasses.

Independent prime signs orthogonalize the squareclasses, but the physical
field is the exceptional identity point. The source-blind Walsh fixture shows
that average phase energy alone cannot control it.

The exact new arithmetic owner of every cross term is the largest prime in the
symmetric difference of the two semiprime kernels. This gives one
prime-indexed dispersion ledger and distinguishes shared-owner from disjoint-
owner sectors without multiplicity.

## Recommended next attack

For each discrepancy prime `ell`, split the non-`ell` square-core variable by
residue classes modulo `ell`.

```text
long core:
  use multiplicative/additive large-sieve energy, with the owner weight 1/ell;

short core:
  use direct residue sparsity and the injective p q a^2 representation;

shared owner:
  factor the common prime and preserve the exact first-chaos quotient;

disjoint owners:
  use the four-label parity difference and quadratic-character dispersion.
```

The zero-frequency/carrier term must be moved by the existing exact carrier
quotient before any character average or Cauchy inequality. A character mean
or free Walsh norm without that quotient is insufficient.

## Boundary

```text
L2SC102833 / LDPC102834   OPEN / RH-BEARING
RH                        UNPROVED
```