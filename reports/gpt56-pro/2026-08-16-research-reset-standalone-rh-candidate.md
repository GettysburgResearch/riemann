# Research reset: standalone anchored/affine-Volterra RH candidate

This packet treats the repository as a theorem library and starts a new branch from `main`. It does not extend the failed derivative-fibre causal lineage.

The strongest synthesis found is:

```text
exact native endpoint / positive Y4 dual
+ directed finite Target-Lorenz leaves on the anchored sector
+ verified Volterra antiderivative on complete outer cells
+ exact affine-cell compression to actual finite rows
+ one signed retained-cell comparison
+ one common thinning
+ complete prime-square/Mellin-Landau endpoint consumer.
```

The exact cell compression is new. It removes the continuum-to-finite realization ambiguity without adding a B-spline collar or a mesh limit.

Scientific status:

```text
full unconditional candidate       yes, pending hostile review
accepted proof of RH                no
known forbidden witness             embedded and fail-closed
large directed certificate          PR #508, exact frozen head
endpoint analytic consumer          PRs #352/#353, exact frozen heads
```
