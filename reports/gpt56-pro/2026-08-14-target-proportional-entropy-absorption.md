# Target-proportional score debt is absorbed by the literal `P_61` row

Date: 2026-08-14  
Parent: PR #462 at `8642e062b6c6f5a4c7d443a1ee5a6e9ecf3e4706`  
Status: exact cross-branch advance; RH unproved

## Result

PR #462 exposes an exact declared-score debt for its target-proportional source ray. The direct-Euler lineage had already established that the mathematically correct score is the literal entropy of the assembled component row, not the affine score attached to an intermediate source template.

`L-91682` combines those two facts. Under the full row determinants of `L-91680`, the target-proportional source and its nonnegative bonuses sum to the complete signed causal row. Their literal entropy is therefore exactly the signed arithmetic-row entropy. A new `P_61` moat proves uniformly

\[
 \operatorname{Ent}(C_{p,y})-\mathfrak S_{p,y}>\frac12
 \qquad(p\ge67,\ 1\le y<67).
\]

The proof retains the positive prime-power subpacket, starts at a directed nine-term base at `67`, and uses the explicit Chebyshev lower bound to propagate the margin. The declared score remains below `4sqrt(py)/3` by the complete `30` Euler block.

## Consequence

The aggregate-debt Gate C of `O-91680` is redundant after the row determinants are established. The exact remaining factor-67 gates are:

```text
A  finite causal profile corridor;
B  complete physical target-normalized determinants;
D  one-use root equality realization.
```

The result does not restore the false Hall theorem, does not count the canonical row as current-only, and does not solve A, B, D, or RH.
