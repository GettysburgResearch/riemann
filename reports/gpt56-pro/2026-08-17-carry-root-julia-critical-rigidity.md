# Carry research reset successor: critical-exponent rigidity and root Julia channels

## Executive result

This packet makes two substantive unconditional advances beyond the signed-span/Farkas reset:

1. every strict subcritical fractional-hinge route `0<alpha<1/2` is impossible, because known critical-line zeros force sign oscillation in one of the first two inverse rows;
2. the irreducible dyadic root source has a positive, coefficient-one, two-channel Julia compiler with logarithmic source mass and logarithmic Hilbert energy.

The packet does **not** claim RH. Its remaining theorem is a trace-free matrix-to-scalar extraction which must preserve the off-diagonal root current without charging the full positive trace.

## Why the exponent result matters

The repository's positive alpha-hinge decomposition allowed any `alpha<=1/2` in principle. This created a large search space. `R-95170` closes every strict subcritical exponent at once. The square-root exponent is forced by the exact placement of known critical-line zeros: for `alpha<1/2`, their poles move into the positive Mellin half-plane and contradict eventual one-sign; at `alpha=1/2`, they lie exactly on the boundary.

## Why the Julia compiler matters

The earlier scalar reset proved:

```text
root completion -> signed interior flow          true;
root completion -> positive scalar flow          false generically.
```

The new object is matrix-valued. The positive reciprocal `g_2` and signed root state `b_2` form

\[
\Sigma_2(n)=\begin{pmatrix}g_2(n)&b_2(n)\\b_2(n)&g_2(n)\end{pmatrix}\succeq0.
\]

The generalized-prime transition flips the two positive eigenchannels exactly. Every carry current remains PSD. The signed ratio is an alternating martingale and its weighted square energy is at most `(25/12)(1+log X)`.

## Exact frontier

```text
alpha<1/2 fractional SHARP                   FALSE
alpha=1/2 uniquely viable in hinge family    PROVED
signed root span                              PROVED / parent
positive scalar root cone                     FALSE GENERICALLY / parent
positive two-channel root compiler            PROVED HERE
logarithmic root Hilbert energy                PROVED HERE
trace-free Schur extraction                    OPEN / RH-BEARING
RH                                             UNPROVED
```
