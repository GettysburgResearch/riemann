# Research report — Triune Adelic Scattering Completion

Date: 2026-08-12  
Authoring agent: `gpt56-pro`  
Status: **full unconditional proposal; RH remains unproved**

## Executive result

The three requested completion routes have been pursued and fused.

The main structural finding is that they are not three independent bets. Their
target objects are diagonal congruences of one kernel:

\[
K_a^\Theta(z,w)
=
\frac{1-\Theta_a(z)\overline{\Theta_a(w)}}
     {-i(z-\bar w)}.
\]

A positive canonical system, a fully polarized conservative colligation, and a
positive theta/Brownian DtN map are three realizations of this same kernel.

The proposal therefore concentrates the entire conclusion-producing burden in
one source-specific identity, the **Adelic Optical Theorem**:

\[
K_a^\Theta(z,w)
=
\langle q_{a,w},q_{a,z}\rangle,
\]

where `q_(a,z)` is assembled explicitly from four positive gradients:

```text
theta supersymmetric gradient;
Gamma–Beta log-odds gradient;
prime Poisson/Fock difference gradient;
radial p=2 Riesz gradient.
```

No RH assumption enters the construction. The identity itself remains open.

## Route I — Suzuki–de Branges

The phase-locked `p=2` wavelet supplies:

- an exact self-dual local Tate factor;
- no open-strip local zero;
- a complete, stable radial Riesz orbit;
- four vanishing moments.

It is used as a finite lossless port around Suzuki's multiplicative Hankel
operator. After local Schur elimination, Fredholm tau functions are proposed to
generate a positive canonical Hamiltonian for every `0<a<1/2`. The exact
remaining theorem is nonvanishing of the regularized Marchenko determinants
and recovery of `E_a(z)=xi(1/2+a-iz)` from the resulting canonical system.

## Route II — Lévy–Fock–Hardy

The generalized-Jordan source is realized as a bosonic Fock product system.
The output retains:

- causal Hardy channel;
- anti-causal reflected Hardy channel;
- one bridge direction;
- positive auxiliary reserve.

The Adelic Optical Theorem is exactly the equality of the source and output
Gram kernels. It produces a lurking isometry on total coherent vectors and
therefore a conservative colligation with transfer `Theta_a`.

## Route III — theta/Brownian DtN

The theta bulk is already supersymmetric and positive. A new exact observation
strengthens the Brownian side:

\[
u_{a,\pm}(v)
=
\left(\frac{1+v}{1-v}\right)^{\pm a/2}
\]

solve

\[
-\partial_v((1-v^2)\partial_vu)
+\frac{a^2}{1-v^2}u=0,
\]

and their odd/even transfer is `tanh(a artanh(v))`. Thus the BPY bounded
regression is the boundary response of an explicit positive log-odds
Sturm–Liouville cell.

The proposed global positive bulk is the form sum of theta, Gamma–Beta,
Poisson/Fock, and `p=2` components. Its declared Weyl function must be shown
to equal the exact Xi impedance.

## Full chain

For `a_j=2^(-j-2)`:

```text
AOT_(a_j)
=> positive Schur kernel
=> conservative colligation
=> positive DtN map
=> positive canonical Hamiltonian
=> Theta_(a_j) meromorphic inner
=> factor-four renewal energy finite
=> no off-line zero of depth > a_j
=> RH as j -> infinity.
```

## Honest boundary

This is a full proposal, not a full proof. The sole open theorem is not
bookkeeping; it is RH-bearing. The value of the proposal is that it provides
one explicit source-ordered equality to prove, three mutually checking
realizations of it, a complete set of positive candidate reservoirs, and
controls that kill the known circular shortcuts.
