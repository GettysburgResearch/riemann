# `P_61` literal one-prime entropy surplus

Date: 2026-08-13  
Frozen parent: PR #432 at `5831eae7081ad71a4cc44f45a911426c3d4fcbf6`  
Verdict: **new exact literal-score theorem; physical current-row realization remains open; RH unproved**

## Result

For the preferred finite Euler block

\[
P_{61}=\prod_{q\le61}q,
\]
let `E_P(X)` denote the literal average-binomial entropy of the signed finite-Euler component row and let `S_P(X)` denote its declared endpoint score. For every prime `p>=67` and every `1<=y<67`,

\[
\boxed{
 [E_P(py)-p^{-1/2}E_P(y)]
 -[S_P(py)-p^{-1/2}S_P(y)]
 >\frac{893}{100}.
}
\]

Thus the exact `P_61` one-prime row has at least `8.93` units of **literal finite-row entropy surplus** over the score ledger consumed by the factor-54 route.

This is stronger than a bounded debt statement and avoids the false source-score equality isolated by `R-91552`.

## New positive arithmetic coordinate

The decisive identity is

\[
E_P(X)
 =\sum_{n\le X}\frac{\lambda_P(n)}{\sqrt n}\log\frac Xn,
\]
where

\[
\lambda_P(n)=\sum_{d\mid(n,P)}\mu(d)\log\frac nd.
\]

The sieve coefficient is nonnegative and has the exact three-case form:

```text
log n     if n has no P61 prime divisor;
log q     if n has exactly one distinct P61 prime divisor q;
0         if n has at least two distinct P61 prime divisors.
```

Therefore the signed finite-Euler entropy itself is generated by a positive arithmetic density. This coordinate is substantially more rigid than the signed Möbius row representation.

## Directed and analytic gates

The retained replay certifies:

```text
all divisor states                         262,144
all derivative-gap cells                  14,933
all terminal score cells                      66
minimum derivative gap                    >13.20684
E_P(67)-S_P(67)                            >15.35708
E_P(67)                                    <17.69244
terminal score                             >1.31622
```

The analytic tail gives a derivative-gap margin `334/25`, while the final exact one-prime comparison gives `893/100`.

## Combined `P_61` state

Together with `L-91345` and `L-91347`, the preferred one-prime packet now has:

```text
SHARP target positivity                     exact;
declared endpoint score positivity           exact;
literal row entropy > declared score +8.93  exact;
every inherited component row positive       exact.
```

No scalar target, scalar score, inherited-row, or literal-entropy separator remains.

## Remaining theorem

The surviving gate is geometric rather than scalar:

> Construct the complete current-generation physical row as a nonnegative ordinary/radix-four feasible object, with target charged once, and identify its entropy with the positive sieved density before the common collar/port correction.

The nonnegative `lambda_P` density is the new attack surface. A successful positive disintegration from `lambda_P` into endpoint/detail atoms would bypass the signed Möbius Hall source altogether.

## Replay

```bash
cd experiments/X-91127-p61-literal-entropy-surplus
python3 verify.py
```

Expected:

```text
PASS_P61_LITERAL_ENTROPY_SURPLUS
```

## Boundary

```text
positive sieved entropy density                EXACT
uniform P61 literal entropy surplus            EXACT
inherited-row positivity                       EXACT / PR #432
complete current-row positivity                OPEN
ordinary/radix-four one-use physical assembly  OPEN / RH-BEARING
Riemann Hypothesis                             UNPROVEN
```
