# Native priority-Hasse surface recovery

## Publication correction

The previous response named a PR and branch that did not land, and its ZIP link was stale.  This packet is a new reconstruction on PR #667's corrected normalized-box source.  It deliberately uses a fresh T-99920 namespace.

## Mathematical advance

The weighted Boolean Euler cube has a canonical odd-to-even Hasse flow with edge mass

\[
J_{i,A}=a_i\prod_{h<i}(1-a_h)\prod_{j\in A}a_j,
\qquad A\subseteq\{i+1,\ldots,k\}.
\]

At the normalized box, `a_p=1/p` and 67 occurs twice.  The flow exactly matches every odd vertex to every nonempty even vertex, leaving only the positive empty-set survival mass.

Truncating by the physical activation potential remains feasible.  Its unmatched mass is exactly the sum of upward first-owner potential drops, which bounds every Hasse min-cut and the negative part of the normalized box scalar.

A Stieltjes coarea rewrites that loss as one product-boundary crossing profile.  Independently, the Cauchy-Poisson square is exactly the weighted square of coefficient suffixes.  These show that the network and Hardy formulations share one multiplicative boundary geometry.

## Boundary

The surface profile still needs an arithmetic estimate:

\[
\int_2^Y\sqrt X\,\mathcal U_X\,dX/X=Y^{o(1)}.
\]

This is `UPBF67`.  It would imply RH but is not proved in this packet.
