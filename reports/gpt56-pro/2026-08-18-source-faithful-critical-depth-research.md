# Research digest: source-faithful ownership closure and the critical-depth parity boundary

## Choice

The highest-leverage task was the reviewer’s exact pair of objections:
source faithfulness and the nonlocal sign inequality.

## New results

1. The literal native rough source is reconstructed as one finite positive
   paired-source DAG with unique first ownership, exact activation, exact raw
   coefficients, disjoint current/children, and root scalar `A_X`.
2. The cutoff-239 local contraction is not a source-complete proof.
3. Every even stopping depth `L=O(log log log X)` has an eventually negative
   natural current.
4. In particular, PR #578's `LAPBR67` large-prime complement is eventually
   negative.
5. The remaining producer is forced into the critical product-boundary regime
   `omega(m) asymp log log X`, equivalently NCBI67/CPSL67.

## Portfolio effect

The source gate is no longer an ambiguity. The repeated local, thinned,
fixed-cutoff and subcritical-depth repairs are exhausted. Future work should
attack the exact Lorenz/Bellman dual or switch architectures.

## RH status

```text
Riemann Hypothesis: UNPROVED
```
