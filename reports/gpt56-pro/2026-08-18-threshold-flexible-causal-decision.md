# Research digest: threshold-flexible causal method decided

## Choice

The highest-leverage task was the exact source identity behind the factor-67
threshold method. Recent work had repaired the finite `P_61` scalar but still
used contracted recursion as though it reduced the negative rough exposure.
The pass therefore reconstructed every raw occurrence and then attacked the
actual signed current.

## New exact results

- complete first-owner ledger for every native rough occurrence;
- exact root identity with the native annular scalar;
- positive paired-source residual completion for every threshold;
- exact absorption of `r-2r^2` and the general `(R-A)F` term;
- raw-exposure conservation;
- asymptotic theorem that every subcritical even threshold depth is eventually
  negative;
- refutation of `LAPBR67`;
- unconditional positivity of the sector `p_0>=Y^theta`, `theta>e^-1`.

## Portfolio consequence

The finite scalar bias is not the remaining problem. Nor is source ownership.
The proof strength is concentrated in a critical-depth, source-complete Hall
transport near `omega(m)~log log X`. This is named `CSHT67`.

## RH status

The Riemann Hypothesis remains unproved.
