# Adversarial response to PR #431 and causal packet repair

Date: 2026-08-13  
Repository: `gfreund123/riemann`  
Source branch: `research/gpt56-pro/91101-moment-neutral-shadow-transport`  
Status: research report; RH unproved

## Executive result

The exact support-cutoff counterexample in PR #431 is correct, as are its hidden-hazard and scalar-loss typing objections. The reviewed `T-91304` is not a proof of RH.

This pass supplies three repairs:

1. a true causal `P_61` shifted-eight Hall theorem with a fresh hash-locked directed checker;
2. a positive all-row packet theorem with a uniform absolute score debt;
3. a measure-valued branching consumer which recurses on actual restricted packets rather than undefined scalar multiples of a native packet.

The full proposal is not yet closed. Its first open theorem is an exact causal packet-regrouping identity connecting the paired least-prime source tree to complete one-prime packets before physical observation.

## Exact frozen review finding

At `(t,p,y)=(79,83,1)`, the old formula includes `85,86,87` although all exceed the causal parent cutoff `py=83`. This is retained as `R-91310`.

## New directed certificate

`X-91130` checks the actual causal expression. It executes 55,052 parent-cell gates and 55,200 child gates below threshold 4096. Exact corridor estimates close every larger threshold. The minimum target and score margins exceed 2.08 and 2.09 respectively.

## New packet theorem

A target Hall flow supported on `e<=o+8` leaves a positive source residual. The one-prime logarithmic ramp difference is nonnegative and decreasing, so every component row generated by every residual source atom is nonnegative. The only possible score loss comes from upward edges and is bounded by a universal constant.

## New consumer

The packet-value consumer `T-91305` uses positive homogeneity, subadditivity, a measure-valued child decomposition and a substochastic mass ledger. It directly handles arbitrary restricted packets and signed losses. It is immune to the two-atom objection against scalar source fractions.

## Remaining theorem

Construct the packet regrouping

```text
paired least-prime source tree
  = current positive packets
    + complete causal one-prime child packets
```

simultaneously in every physical and score coordinate, with one-use capacity and bounded normalized mass. This is not supplied by the existing hidden hazard identities or by the complete one-prime Euler identity separately.

## Status

```text
finite review bug                         repaired
consumer normalization bug                repaired
hidden hazard = r native child             remains false
standard source-cone/capacity typing       open
complete packet regrouping                 open
RH                                         unproved
```
