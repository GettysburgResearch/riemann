# Consolidated parity-paired factor-five RH proposal — review packet

Date: 2026-08-08  
Branch: `agent/gpt56-pro-262-dyadic-two-contact`  
PR: #269  
Status: **full conditional proposal; physical transference open; RH unproved**

## Executive conclusion

The live reflected/carry programme has now been consolidated into one source-specific candidate.

The source-side questions that repeatedly caused earlier proposals to fail are no longer left as prose:

```text
correct physical block                 independent-frequency, exact
RH-bearing source                      fixed and zero-safe
parity siblings                        explicit
carry image                            pointwise and compact
negative quotient tail                 exactly localized to factor five
positive inverse                       explicit
positive generalized primes            explicit
carry Schur reserve                    uniform and elementary
Selberg moment structure               exact through order two
bottom boundary                        isolated explicitly
first-cell Mertens mutation             retained
```

The remaining theorem is not “some Type-II cancellation.” It is one exact finite-ratio physical source-image theorem:

```text
quotient cells 2/3/4 in the independent-frequency normal block
-> bounded map to the generalized-prime carry Gram
-> preservation of the strict carry reserve and m=1 boundary
-> explicit lower-scale recurrence.
```

This object is named `F5TC`.

No unconditional proof of RH is claimed before `F5TC` is emitted and independently verified.

## Consolidated review front doors

```text
T-26902  complete conditional theorem and frozen dependency graph
M-26902  fail-closed review and mutation protocol
M-26901  production matrix contract for F5TC
status TSV exact component ledger
```

## Main exact advance in this consolidation

`L-26904` adds the complete Selberg–carry moment tower. For every row `n`, split `j`, and source wavelet `Z_(n,m)`:

```text
sum_m a_omega(m) Z_(n,m)(j) = 0

sum_m a_omega(m) log(m) Z_(n,m)(j)
 = generalized-prime carry profile >=0

sum_m a_omega(m) log(m)^2 Z_(n,m)(j)
 = generalized Selberg carry profile >=0.
```

The first line comes from `omega_2*a_omega=epsilon`. The second is the generalized von Mangoldt identity. The third is the exact Selberg identity

```text
omega_2*(a_omega log^2)
 =Lambda_omega log+Lambda_omega*Lambda_omega.
```

The new exact firewall is equally important: `log(1)=0`, so the unit-source wavelet is absent from both positive logarithmic moments. This is precisely the bottom source coordinate which survives in

```text
5c_X(2)+3c_X(3)=-6 R_omega(X).
```

Consequently generalized-prime coefficient positivity, the carry Schur reserve, or parity-frame reconstruction cannot silently sign the RH-bearing boundary. A valid physical certificate must retain that coordinate explicitly.

## Why the parity-paired work is imported but not promoted beyond scope

PR #263 supplies:

```text
|p(z)|^2+|p(-z)|^2 >=45/4
U(z)p(z)+U(-z)p(-z)=1 with U coefficientwise positive
omega_2 =V_+B_+ +V_-B_-
complete odd-core half-pole-null fibers.
```

These are real exact advances. They close source reconstruction and prevent a common-zero loss.

They do not supply a matrix inequality comparing the physical normal Gram with the carry Gram. The new master packet records this as a dependency, not as the missing transference theorem.

## Current mathematical chain

```text
PR #241 corrected physical block
-> fixed-q0=2 Mobius source
-> omega_2 zero-safe opposite-parity source
-> exact pointwise carry wavelet
-> negative Kummer rows only in 2m<=n<5m
-> positive inverse and generalized-prime synthesis
-> uniform carry-space Schur reserve
-> exact zero/first/second moment tower
-> F5TC physical transference
-> DSS or shell recurrence
-> Mellin/Landau pole exclusion
-> RH.
```

Every arrow except `F5TC` and the recurrence emitted by it is represented by a named file and exact review boundary.

## Exact replay added

The new moment-tower checker verifies:

```text
convolution rows                         72
pointwise carry cells                  2698
nonzero unit-boundary cells             411
source coefficient mutation rejected     1
```

Retained digest:

```text
eaaed5595e09e6b7d0f9fd990f42bea3e99d4c4e180fd04d78b7d2fa8434c2af
```

It is finite algebra only.

## What a reviewer should attempt to break

Before reviewing the open theorem, attack:

1. the scaled box identity for `Z_(n,m)`;
2. the factor-five far-tail product inequality;
3. the uniform constant-run Schur proof;
4. the positive inverse formula;
5. the generalized-prime correction and its norm bound;
6. the zero/first/second moment tower;
7. the exclusion of the `m=1` boundary from positive logarithmic moments;
8. the parity-paired Bezout identity and finite source synthesis;
9. the bottom-charge and first-cell Mellin consumers.

Then require the actual `F5TC` matrices. Do not accept a prose identification of the two feature spaces.

## Exact status

```text
consolidated source/carry theorem package   PROPOSED COMPLETE
new moment tower and boundary firewall      PROPOSED COMPLETE
review and mutation contract                COMPLETE METHODOLOGY
physical F5TC source map                    OPEN
physical reserve and recurrence             OPEN
conditional F5TC -> RH                      PROPOSED COMPLETE
Riemann Hypothesis                          UNPROVED
```

This packet is ready for adversarial review at those exact scopes. It is not ready for an “RH proved” verdict.
