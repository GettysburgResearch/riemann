# Two strongest routes after the full repository sweep

## Executive disposition

The latest audits show that the Hardy/GCD packings, critical variation, and
several scalar sign gates are exact RH-equivalent normal forms.  They are not
independent producers.  Two mechanisms remain genuinely upstream.

## Route A — critical centered carrier

PR #676's cubic critical remainder is a positive-definite smoothing of the
quadratic envelope and has a fixed reciprocal-zeta Mellin consumer.  The new
`L-100100` theorem proves a hereditary future-prime corridor

```text
q <= Y <= q^A,  A<1+sqrt(2),  q sufficiently large.
```

The infinite tail is therefore unconditional.  The sole producer is the exact
low-prime source-orbit maximum principle `DCE100100`.  If it propagates the
cubic Harnack ratio through the finite block, eventual cubic positivity and RH
follow.

Why this route is promising:

- fixed source and fixed Mellin multiplier;
- positive, self-reciprocal, positive-definite cubic kernel;
- unconditional hereditary terminal boundary;
- finite low-prime quotient orbit is the only open interface.

## Route B — finite Euler completion and pole exposure

PR #677's order-two completion is generalized in `L-100101` to arbitrary
finite order.  The optimal level-pairing exponent is

\[
A_k^*=\exp(\operatorname{arsinh}1-P(k)-67^{-k}),
\qquad A_k^*\to1+\sqrt2.
\]

Every finite multiplier is zero-free in the open strip.  `L-100102` proves
that at an off-line zero its residue is alternately amplified and suppressed
like

\[
\exp\!\left(\pm c Z^{1-\beta}/\log Z\right).
\]

The sole producer `QPET100101` is a uniform sign-change-location theorem: on
one amplified cutoff subsequence, the completed cubic scalar must turn
negative before the unconditional corridor ends.

Why this route is promising:

- unconditional positivity on growing intervals;
- exact finite detector with no zero cancellation;
- quantitatively improving corridors as completion order grows;
- superpolynomial residue amplification on explicit subsequences;
- no Hall, source-ownership, score, or capacity interface.

## Common critical geometry

Both routes bound Euler levels by

\[
M_r\le K\Sigma^r/r!
\]

and therefore by

\[
\text{scalar}\ge K[1-\sinh\Sigma].
\]

Mertens converts a prime-ratio interval into `Sigma=log A+o(1)`, producing the
common ceiling

\[
A<\exp(\operatorname{arsinh}1)=1+\sqrt2.
\]

This is the exact shared geometry, not a numerical coincidence.

## Scientific boundary

```text
arsinh-one future-prime corridor       proved
arbitrary-order completion corridor    proved
finite pole preservation               proved
residue amplification                  proved
DCE100100                              open
QPET100101                             open
Riemann Hypothesis                     unproved
```

The branch takes both mechanisms to their first genuinely new producer theorem
and records every previous/next interface needed for hostile reconstruction.
