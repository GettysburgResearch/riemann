# First-principles zero-safe source proposal toward RH

Status: PROPOSED COMPLETE FRAME / OPEN LOAD-BEARING THEOREM

## Objective

Attack the remaining RH hinge by abandoning absolute boundary atomization and preserving the exact signed source through every transformation.

## Main theorem sought

**Zero-safe signed source transference theorem (ZSST).**

For the explicit dyadic parity source

\[
B_\alpha(s)=\frac{1-\alpha2^{-s}}{\zeta(s)},\qquad 0<\alpha<1,
\]

construct a positive-capacity carry realization whose negative debt is bounded by a subpower quantity.

## Why this source

The failed terminal approach measured

\[
\sum_m \sqrt m |\sigma_m|,
\]

which destroys cancellation. The surviving object must instead preserve:

1. the zero-safe numerator;
2. the positive inverse coefficients;
3. the signed carry profile;
4. the Selberg hierarchy.

## Exact source facts

The inverse coefficients are

\[
a_\alpha(n)=\sum_{0\le k\le v_2(n)}\alpha^k>0.
\]

The generalized prime sequence is

\[
\Lambda_\alpha(n)=\Lambda(n)+\log(2)\sum_k\alpha^k1_{n=2^k}\ge0.
\]

The carry profile is explicitly negative in every interior split:

\[
Y_{\alpha,n}(j)\le0.
\]

## Single remaining theorem

Prove a finite positive dual certificate showing that the negative carry profile implies a lower bound on the RH-sensitive Riesz coordinate rather than merely a formal sign pattern.

The certificate must emit:

- exact finite source manifest;
- all carry columns;
- dual obstruction variables;
- lower-scale recurrence;
- Mellin continuation.

## Rejection conditions

This proposal is rejected if it:

- replaces signed sources by absolute norms;
- hides a boundary term in a reviewer-supplied correction;
- uses pointwise positivity without physical carry realization;
- claims RH from finite computation.

## Current status

VERIFIED:

- terminal absolute atomic closure fails;
- dyadic source algebra;
- positive inverse;
- generalized-prime positivity;
- Selberg hierarchy identities.

OPEN:

- ZSST.

RH remains UNPROVED.
