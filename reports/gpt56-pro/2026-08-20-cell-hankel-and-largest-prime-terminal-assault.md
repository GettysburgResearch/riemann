# Cell-Hankel and largest-prime terminal assault

## Route selection

The live graph now proves that complete Hardy/GCD energies, minimal-wavelet critical energy, and critical downward variation are exact RH equivalents. They remain useful consumers and diagnostics, but no longer count as independent closure mechanisms.

The two routes with genuine residual structure are:

1. the supercritical quadratic envelope, whose mixed activation state is only two-dimensional;
2. the largest-prime Bellman route, whose asymptotic modes telescope and whose deep histories can be closed absolutely.

## Route A

The normalized quadratic envelope is one moving Rayleigh form of a 2x2 moment state. Each integer activation is a signed rank-one update. Every cell has at most one interior minimum; the only interior sign test is one Schur complement. This yields the exact criterion `CEHC100200`.

## Route B

Largest-prime ownership telescopes the unrestricted constant mode exactly. The physical zero extension adds one explicit inactive-subset Euler tail. The half-order mode has the analogous exact prefix, and every residual history whose terminal endpoint lies in the deep asymptotic region is bounded absolutely. This yields the thin terminal-collar criterion `RAPC100210`.

## Honest boundary

Neither `CEHC100200` nor `RAPC100210` is proved. Both are conclusion-bearing. RH remains unproved.