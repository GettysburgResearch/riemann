# Filtered SHARP rays: first exact passage through the signed compact filter

## Result

The general compact filter is not cone preserving.  Nevertheless two fixed
activation-zero shifted quadratics have positive filtered kernels and globally
subcritical labelled prime budgets.

```text
z=-1      global budget <0.874777;
z=-1/2    global budget <0.941359.
```

Adjacent-level greatest-owner pairing therefore proves native-source
positivity after the complete signed filter.  Because the completion tangent is
a positive combination of shifts of the native source, both inequalities lift
to every homotopy time.

## New exact inequalities

The quadratic boundary bridge converts the two positive filtered rays into

```text
P2 Q_tau - 5 G_tau >= 0;
P2 Q_tau - 4 G_tau - 3 P2 a_tau / 4 >= 0.
```

These are the first source-faithful filtered inequalities in the
completion-defect programme.

## Boundary

The two upper rays do not determine the lower Lorentz direction.  An explicit
three-variable fixture satisfies both inequalities while `5 P2 a-G<0`.

The remaining task is therefore no longer generic cone preservation.  It is one
carrier-recombined lower control compatible with:

- the two proved filtered upper rays;
- the single unfiltered S-lemma slack;
- the polarized cross-owner current;
- the centered completion envelope.

```text
FLC102740   OPEN / RH-BEARING
RH           UNPROVED
```
