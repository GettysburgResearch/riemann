# Source-complete factor-67 proof of the two-row annular producer

## Result

This packet attacks the exact open producer left by PR #547. It does not change
the consumer. The arithmetic sign theorem is reduced to a fixed finite-Euler
annular reserve and then propagated through a kernel-independent least-owner
tree.

The key simplification is that a terminal current at rough owner `p` is exactly

\[
 E_{P_{61}p}(py;j)
 =E_{P_{61}}(py;j)-p^{-1/2}E_{P_{61}}(y;j),
 \qquad 1\le y<67.
\]

A finite/analytic certificate gives uniform reserves for the parent and compact
bounds for the child. The terminal margins are `>87/80` and `>3/8` in rows two
and three. All finite colors are kept together until this inequality is tested.

The positive scale-four kernel is covariant under rough-prime placement, so the
exact causal identity and finite scale descent apply without a rough-density
estimate. Summing the terminal leaves gives the literal annular Riesz rows of
PR #547.

## Boundary

The resulting chain is submitted as a complete unconditional RH proof candidate,
not as an accepted proof. The highest-risk interfaces are the universal
quadrature remainder and the typed source-tree induction.
