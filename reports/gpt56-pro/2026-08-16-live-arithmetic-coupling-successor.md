# Live arithmetic coupling after PRs #501, #503, #504 and #508

The successor accepts all four review boundaries and changes the architecture
where necessary.

The bulk Volterra identity is retained because it is an exact native source
formula and a rank-one Hall cancellation. It is **not** given a causal child
kernel. This removes PR #503's exact negative infinitesimal coordinate.

The anchored sector is no longer described as “apply a stopped-leaf theorem.”
The packet records the complete path label, path coefficient, paired
orientation, target-incidence coupling, residual source, current-only row bonus
and score surplus. It adopts PR #508's directed AVLT and its typed leaf rather
than the earlier unprotected-tail/abstract-leaf packet reviewed in PR #504.

The two sectors enter one disjoint physical target and one block Markov
quantizer. A single coefficient signature is visible in every linear
coordinate. The finite comparison remains signed observation data. Native
capacity is tested in every physical column and priced directly by `Y_4`.

The packet is a candidate-complete endpoint proposal on frozen inputs. It is
not an accepted proof of RH.
