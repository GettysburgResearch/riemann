# Final addendum — Complete Endpoint Stability is the canonical hinge

Date: 2026-08-08  
Agent: `gpt56-pro`  
PR: #291  
Status: **one complete-source rate theorem open; RH unproved**

## Final narrowing

The route began with WSTS, strengthened its continuum shell to one crossing, then proved/proposed cofinal finite one-crossing. That removed every tail cut.

The remaining ordinary-prime endpoint sign has now been decomposed exactly:

```text
A_P(X)=A_Lambda(X)-R_pp(X),
R_pp(X)>=c_pp log X-O(1).
```

The deterministic reserve is supplied principally by prime squares.

The complete source is

```text
A_Lambda(X)
 =sum_(m=2)^X 2sqrt(m)(1-sqrt(m/X))log(m/(m-1))
  -sum_(n<=X)Lambda(n)/sqrt(n).
```

The canonical theorem is

```text
CEP: A_Lambda(X)=o(log X).
```

## Two exact consumers

### Direct Mellin consumer

```text
Mellin[A_Lambda](z)
 =Phi(z)/[z(z+1/2)]
  +(zeta'/zeta)(z+1/2)/z.
```

Every zero `rho` to the right of the critical line gives residue

```text
m_rho/(rho-1/2).
```

CEP gives holomorphy on `Re z>0`, hence RH.

### Elementary shell consumer

CEP and the prime-square reserve give

```text
A_P(X)<0
```

cofinally. Therefore the prime discrepancy `Delta_X` is nonincreasing. Cofinal one-crossing gives

```text
B_X=[Delta_X-Delta_floor(X/2)]_+=0.
```

The frozen WSTS chain again gives RH.

## Preferred producer

The preferred analytic object is `ERSC`, an Endpoint Reflected Selberg Certificate on the complete von Mangoldt source.

It must retain:

```text
independent frequencies;
all prime powers, especially squares;
all reflected cross terms;
the endpoint benchmark;
all support commutators;
the m=1 and 2/3 Mertens firewalls.
```

Its output is a directed sublogarithmic estimate for `A_Lambda`, not an ambient energy theorem.

The Endpoint Squarefree Collector remains a valid elementary alternative for the ordinary-prime consumer, but it is no longer the canonical sole hinge.

## Exact final boundary

```text
continuum dyadic one-crossing               proposed complete
cofinal finite shell-crossing                proposed complete
WSTS tail collapse                           proposed complete
proper-prime-power logarithmic reserve       proposed complete
complete endpoint Mellin firewall            proposed complete
CEP                                           open / RH-bearing
CEP -> RH                                    proposed complete twice
RH                                            unproved
```
