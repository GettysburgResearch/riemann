# Fractional Julia–Hermite hardening: exact source reconstruction and Bohr no-go

## Executive result

The submitted uniform estimate `L-98703.1` is false. A completely
multiplicative Bohr twist turns the reciprocal Euler product into

\[
\zeta(s)^\theta
\left[(1+2^{-s})(1+2^{-s-1})/\zeta(2s)\right]^\theta,
\]

so its Gaussian heat packet has amplitude `e^(T/4+o(T))` and localized energy
`e^(T/2+o(T))`. Vertical translates approximate this twist. At
`theta=1/384`, the claimed upper exponent is only `T/4+o(T)`.

The failure is not a minor constant defect. It proves that no
phase-independent positive Gram or trace estimate can supply the tunable rate
uniformly in the center.

## Exact positive replacement

Fractional coefficients admit a canonical Sibuya-decorated squarefree-support
source. On this source the actual Gaussian window defines one prescribed
common finite Gram, all cross-blocks are simultaneously Schur controlled, and
cutoff exhaustion is ordinary dominated convergence.

This closes three of the architectural interfaces requested for `L-98703`, but
it exposes the decisive obstruction: the resulting phase-blind diagonal has
Bohr rate `e^(T/2+o(T))`, not `e^(96 theta T+o(T))`.

## Interface-by-interface disposition

```text
finite Tao atoms -> prescribed cross-state Gram     FALSE AS USED
simultaneous reflected-history Schur domination     PROVED after Sibuya/Gaussian rebuild
coefficient 96 theta T                              FALSE uniformly in center
T^(3/4) log^2 T remainder                           not reached / no derivation needed
cutoff exhaustion                                   PROVED by dominated convergence
```

The local Tao semigroup theorem remains valid. Its integer `mu(n)` trace-free
atoms do not represent the fractional `p^2` coefficient and its state labels do
not define the asserted isometric directed system.

## Correct frontier

The only surviving version is a fixed-center, phase-sensitive estimate PLFHE.
Its constants may depend on the fixed center. It remains RH-bearing. Its
first variation is the existing phase-locked First-Hermite prime carrier, while
higher variations are explicit Sibuya support cumulants.

```text
L-98700 fractional chaos algebra              retained
L-98701 local Tao atom identity               retained locally
L-98703 uniform heat estimate                 false
T-98700 complete candidate                    withdrawn/superseded
L-98710 Sibuya source                         proved
L-98711 exact common Gaussian Gram            proved
R-98710 Bohr no-go                            proved
PLFHE                                         open / RH-bearing
Riemann Hypothesis                            unproved
```

## Exact First-Hermite tangent

`L-98712` proves that the log-number derivative of the `theta=0` variation is
exactly the generalized-prime phase carrier. Therefore the corrected route is
not free of the earlier phase-locked arithmetic frontier: it is a nonlinear
Sibuya-cumulant extension of it. This gives a precise hostile test for every
future completion rather than an informal analogy.

## Exact phase-blind rate and main-mode audit

The absolute-coefficient Euler product is `zeta(s)^theta` times a holomorphic
nonzero factor. Gaussian Selberg--Delange and the Bohr lower bound therefore
give

```text
lim (1/T) log sup_tau0 E_(theta,T)(tau0) = 1/2.
```

The genuine heated one-particle trace is only
`(theta/2)log T+O(theta)`. It cannot be the source of the asserted
`96 theta T` exponential. Finally, Weyl displacement obeys
`W(f)^* P W(f)=W(-2f)P`; it changes the parity coefficient. These facts locate
the failed step in the coherent cross-history collapse, not in the local Tao
trace or the cutoff limit.
