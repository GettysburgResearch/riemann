# Critical phase-circle dual closure attack

## Decision after the live-graph reset

The route selection was made after incorporating the newest firewalls:

- root-containing Hardy and divisor-GCD squares are RH-equivalent;
- unrestricted positive priority flux is power-sized;
- the minimal wavelet cumulative energy is RH-equivalent;
- source-blind local cube matching leaves a logarithmic parity residual;
- the quadratic envelope is critical after its native conjugation.

The two surviving mechanisms are therefore:

1. remove the neutral mode **before physical collapse** by the symmetric
   phase-Hasse boundary;
2. remove the root term **after physical collapse** by differentiating the
   minimal wavelet in phase and centering its analytic circle.

They are not two names for the same positive square.

## Route A result

The exact phase-Hasse symbol of PR #672 obeys

\[
\int|\mathscr S_B(\gamma)|^2P_\tau(\gamma)d\gamma
\le2P_B^2A_B^2.
\]

At native activity `1/p`, this is polylogarithmic.  Thus no large local Euler
block, duplicate-67 factor, or neutral cube mode remains.  The complete open
term is the sum over distinct outside squarefree cores that occupy the same
physical compact shell.

The theorem `PHCC100210` asks for the corresponding blockwise cross-core
Carleson estimate.

## Route B result

The lower activation of the minimal wavelet is first order, so

\[
K_0(y)=\log y\,J_0(y)
\]

with bounded compact `J_0`.  Consequently

\[
G_\mu(X)=-\mathscr A_X'(0).
\]

Centering the phase circle gives

\[
\frac1{2\pi}
\int|\mathscr A_X(re^{i\theta})-\mathscr A_X(0)|^2d\theta
=
\sum_{k\ge1}\frac{r^{2k}}{(k!)^2}|M_k(X)|^2.
\]

The root term is absent, and factorial decay leaves only

\[
K_X\asymp\log X/\log\log X
\]

grades.  The open theorem `GMPC100212` is a subpower Carleson estimate for
this explicit finite moment tower.

## Why the two routes should be combined

The `k`-th phase-circle moment is a compact logarithmic polynomial of the same
native squarefree source carried by the phase-Hasse graph.  The exact local
phase bound is uniform under multiplication by a fixed logarithmic grade.  A
proof should therefore:

1. apply the symmetric Hasse owner flow before same-product collapse;
2. project the resulting root-free phase boundary into the first `K_X`
   logarithmic grades;
3. use first-owner Jensen to remove cross-owner interactions;
4. organize the remaining terms by multiplicative near-collision clusters;
5. prove one cluster packing estimate with total `X^o(1)` congestion.

The diagonal, far-product interactions, local Euler blocks, neutral phase,
and high-grade tail are already controlled in the cited exact packets.

## Exact boundary

```text
local labelled phase geometry                 closed
neutral mode                                  closed
minimal-wavelet root term                     closed
high logarithmic grades                       closed
same-product cross-owner duplication           closed by first-owner Jensen input
near-collision distinct-core packing           open / RH-bearing
Riemann Hypothesis                             unproved
```

The branch makes no claim that either final Carleson theorem has been proved.
It is a two-route conclusion packet and an exact narrowing of the remaining
arithmetic, not an announcement of RH.
