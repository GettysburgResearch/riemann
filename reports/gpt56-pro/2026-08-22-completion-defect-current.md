# Completion-defect continuation of the common-mother programme

## Result

The scale-transfer and owner-occupancy rows of PR #715 share one exact critical
atom.  When source prefixes are aligned, those atoms cancel before physical
collapse.  The simultaneous update is a squared shift of activity `1/p`.

This does not finish the proof.  Positive completion changes the Euler source
from

```text
(1-67^-z)/zeta(z)
```

to

```text
(1-67^(-2z))/zeta(2z).
```

The completed term is harmless in the positive Landau half-plane; the
native-minus-completed defect retains every hypothetical off-line pole.

The programme has therefore progressed from two arithmetic rows to one exact
Duhamel current.  Squared-core and higher-prime-power terms are polylogarithmic.
The remaining current is source-faithful, fixed-detector and conclusion-bearing.

## Next attack

The natural next step is to apply the least/greatest-owner and phase-Hasse
coordinates directly to the Duhamel integrand

```text
sum_p p^-1/2 U_p A_(t,not p) E
```

while retaining the completion parameter `t`.  Any regional norm must follow
exact deterministic carrier recombination.  A proof of subpower one-sided
mass for that current would close the common-mother route.

## Equal-owner reduction

The Duhamel parameter is also a canonical random-order owner. A `k`-prime squarefree occurrence is split into `k` equal pieces. Same-product owner collapse therefore costs at most `k<=log_2(16Y)`. The remaining physical obstruction is exclusively overlap of distinct products under the compact mother.
