# RJTE completion-port attack and finite separators

## Executive result

PR #570 is frozen at

```text
992404909d6c2940ae8ae849633fe5530ed79db0
```

and its reciprocal-Julia algebra reconstructs. The route does **not** close with the existing local state. A finite Euler system satisfies every proved local input but has normalized boundary strictly above one. This proves that the missing boundary functional is not a consequence of local PSD, parity covariance, the alternating martingale or logarithmic energy.

The pass reconstructs the unpublished mathematics summarized by PR #564, corrects its relation to RJTE, and identifies the smallest exact dynamic repair: the future-prime quotient profile.

## Exact finite overshoot

For `P={3,5,7,11,13}` and `N=26`, a 200-bit radical enclosure gives

```text
B_P(26) > 1.1306169339746402.
```

The finite reciprocal is positive, the Julia channels are nonnegative, the channel swap and martingale laws hold, and all moment matrices are positive. Yet the unit-normalized Schur matrix has negative determinant.

This is the requested finite overshoot showing that the current state space is insufficient. It does not refute the all-prime RJTE inequality.

## Reconstructed TFPE/ACBI

For the finite Euler boundary defect `M_P(Y)=6[1-B_P(Y)]`,

\[
A_{P,X}=\int_{X/4}^{X}M_P(y)\,dy/y.
\]

Thus TFPE is a factor-four moving-average replacement for pointwise RJTE. The exact prime recurrence is

\[
A_{Pp,X}=A_{P,X}-p^{-1/2}A_{P,X/p}.
\]

PR #564 published only a route-disposition observation; these formulas are reconstructed here rather than imported as theorems.

The obvious trace shortcut is false. At `X=500`, with every odd prime at most 500 installed,

```text
Julia trace      > 40.75888153133406722024
six times bulk   < 26.86217458425043977536
```

while the signed parity defect is negative and the actual TFPE margin is positive. Hence parity cancellation, not trace mass, is the missing resource.

## Completion port

A prime update uses both `B_P(N)` and `B_P(floor(N/p))`. The scalar boundary alone is not Markovian. The exact finite closure is the quotient profile

\[
(B_P(\lfloor N/m\rfloor))_m.
\]

Processing missing primes in descending order leads to FCBI, an explicit Bellman cone. A 220-bit directed replay verifies all stages through `N=8192`; this is finite evidence only.

## Five languages

The packet supplies exact truncated-moment, semidefinite, extremal-martingale, unit-Schur and Bellman formulations. The overshoot simultaneously separates the first four local formulations from the required unit boundary.

## Scientific boundary

No all-scale producer was proved. The strongest honest successors are:

1. RJTE itself;
2. TFPE/ACBI, which directly feeds the annular Mellin--Landau consumer;
3. FCBI on the future-complete quotient profile.

RH remains unproved.
