# Holomorphic-defect fixed-row closure attack

## Executive result

The newest direct component-row route does not need a positive two-anchor
equality frame of the kind reduced in PR #638. Its finite/continuum mismatch, derivative
jumps and two Volterra boundary modes may remain signed.

For each fixed component row the endpoint fibre is uniformly bounded, the
retained-cell error is \(O_j(Y^{-1/2})\), and the complete factor-67 child tree
has total source mass bounded by a geometric series. Hence the fully resolved
calibration is \(O_j(1)\).

Subtracting that bounded calibration from the full Möbius row leaves an
eventually nonnegative surrogate. The bounded correction has Mellin transform
holomorphic in \(\Re s>0\), so it cannot cancel a reciprocal-zeta pole.
Landau therefore closes the same fixed-row consumer.

## Main strategic gain

The first external interface in PR #636 is weakened from

```text
positive compact frame + positive anchors = c_X exactly
```

to

```text
positive common-parent row = c_X + bounded signed fixed-row calibration.
```

The latter is compatible with the exact boundary audit of PR #638 and the
same-row defect resolvent of PR #635.

## Status

This is a proposed complete proof candidate, not an accepted proof. RH remains
unproved pending independent reconstruction of the local common-parent
arithmetic and calibration ledger.
