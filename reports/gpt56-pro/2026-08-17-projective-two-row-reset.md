# Projective two-row research reset

## Executive result

The endpoint support request was separated from the arithmetic normalization.
`B_X` controls only physical packing slack.  It does not control the separate
arithmetic term `F_Lambda`, so forcing NEDB into the old endpoint consumer
cannot complete RH.

The successor instead uses the native component rows themselves.  It rewrites
the rough-prime source compiler as a nested interval transport.  A recursive
demand interval is never counted as a physical output; only stabilized terminal
incidences are observed.  The terminal theorem is reduced to rows 2 and 3.
Their Mellin numerators have no common zero in the open critical strip.

## Strongest claim

`T-96400` is a candidate complete proposal.  It awaits hostile reconstruction
of the exact source-incidence equality and projective restriction property.
The replay checks finite source algebra and mandatory counterexamples, not the
large analytic Hall inputs or RH.
