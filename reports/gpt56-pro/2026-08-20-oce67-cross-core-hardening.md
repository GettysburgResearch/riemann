# OCE67 hardening after the native first-owner recovery

## Result

The first-owner/Littlewood--Paley packet is now genuinely published on PR #660.
This continuation tested the missing physical embedding rather than assuming
that a labelled Hilbert estimate survives scalar collapse.

Two parts close exactly:

1. the scalar one-prime convexity inequality removes every cross-owner term;
2. the ratio-eight zero-safe packet gives an absolute owner-diagonal bound
   below `60`.

The universal branchwise interpretation of OCE67 is false. A genuine
prime-interval future profile supported on the primes `X/2<p<=X` has labelled
norm `1+o(1)` but compact physical observation of size

\[
\gg \sqrt X/\log X.
\]

At `X=200`, the 21-prime fixture already gives squared collapse ratio larger
than `147/80`.

The corrected source-orbit theorem retains the complete small-prime base and
all incomparable future-product covariance. Its only unproved term is the
signed near-collision Gram

\[
\sum_{A\ne B}(-1)^{|A|+|B|}r_Ar_Bd_A(X)d_B(X).
\]

This is the same boundary represented as the prime-exchange min-cut on PR #658
and the signed Poisson off-diagonal packing on PR #659.

## Scientific status

```text
native first-owner algebra              proved
native Littlewood--Paley identity        proved
convex cross-owner decoupling            proved
compact physical diagonal                proved bounded
universal branchwise OCE67               refuted
source-orbit SOCE67                       open / RH-bearing
Riemann Hypothesis                        unproved
```

The continuation therefore repairs the scope of OCE67 and removes every local
or diagonal payment, but does not claim the remaining cross-core cancellation.
