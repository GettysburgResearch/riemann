# Common annular embedding breakthrough and parity-transition scope correction

Date: 2026-08-08  
Branch: `agent/gpt56-pro/262-critical-euler-fiber-bridge`  
Status: **exact source-map progress; final quantitative recurrence open; RH unproved**

## Main results

### 1. The naive finite parity-Gram contraction is false

For a rank-one parity Gram `G=vv*` and an unequal diagonal transition `T=diag(a,b)`, one has exactly

```text
det(G-T*GT)=-|v_+ v_-|^2 |a-b|^2.
```

Thus the proposed shortcut is indefinite whenever both parity coordinates are present and the channel multipliers differ.

The quotient ratios `2,3,4` also cannot be encoded as `z^2,z^3,z^4` with `z=2^-s`; the ratio-three multiplier is `3^-s`.

This refutes only the shortcut, not the complete independent-frequency source ledger.

### 2. The common annular bank embedding is exact

For every finite arithmetic source `x` and every finite multiplicative prefix `a`,

```text
A_a Q_x=Q_(a*x),
F_(a*x)(r)=sum_d a(d) F_x(floor(r/d)).
```

All observations live in the fixed weighted space

```text
ell^2(N,1/[r(r+1)]).
```

If `x` lies in `[M,2M)` and `a` is supported below `R`, every observation has the exact carry representation on the one common row

```text
N=16MR-1.
```

No metric identification remains open.

### 3. Every prefix observation has one finite current source and one strict-delay tail

For

```text
c_2*omega_2=epsilon-(5/2)delta_2+delta_4=:h_2,
```

and a strict prefix `c_<Y`,

```text
omega_2*(c_<Y*x)
 =h_2*x-(omega_2*c_>=Y)*x.
```

For the annular generalized-prime source, the current term is the exact three-scale feature

```text
P_M-(5/2)P_(2M)+P_(4M),
```

while every monomial in the remaining source has multiplicative delay at least `Y`.

Thus source-map existence, common metric, and strict-delay support are closed. The surviving theorem is a quantitative signed estimate for the recombined tail and pole-preserving boundary commutator against the finite current carry reserve.

## Exact replay

```text
PASS_EXACT_COMMON_WEIGHTED_ANNULAR_EMBEDDING_AND_PARITY_NO_GO
proof-object SHA-256
a965f12f7bb2dad4e9186f8a83e5c0226b8d5ee14c60a8d448f1f566aaf51cad
```

The replay checks nine signed source/prefix cases, twelve exact dilation scalings, and 400 rank-one determinant specializations.

## Updated frontier

```text
parity rank-one transition shortcut       REFUTED
common annular bank metric                PROPOSED COMPLETE
exact current/strict-delay source split   PROPOSED COMPLETE
factor-five current carry reserve         IMPORTED PROPOSED COMPLETE
signed recombined tail norm bound         OPEN / RH-BEARING
pole-preserving commutator boundary       EXPLICIT, estimate open
Riemann Hypothesis                        UNPROVED
```
