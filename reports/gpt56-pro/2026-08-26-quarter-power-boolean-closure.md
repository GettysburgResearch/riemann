# Quarter-power Boolean closure

## Executive finding

The Boolean/Vaughan cutoff was previously fixed at the sixth-root scale to
obtain a power-saving Type-I estimate. That is stronger than the Mellin
consumer needs.

At the quarter-power endpoint:

```text
Type I:   loses the power saving but remains subpower;
balanced: becomes physically support-empty.
```

The old balanced row is exactly the difference between the new and old Type-I
rows. This proves the frozen BCI criterion on the submitted dependency stack.

## Why the support vanishes

On one owner-product block, a nonzero balanced core contains two disjoint
factors larger than `V`, so `a^2>(V+1)^4>2Y/A`. Multiplication by `P>=A`
places the physical atom above `2Y`, outside the complete dyadic observation.

## Why Type I remains sufficient

The squarefree zero-moment lattice gives `Z^(-1/4)`. Summing the two truncated
divisor variables costs `V`. At `V~Z^(1/4)` the product is `O(1)` up to the
existing subpower source ledger.

## Scientific boundary

The new argument is short because it uses a large body of frozen exact work:
Hodge identification, completion functoriality, equal-pair normalization,
squarefree lattice transfer, compact support, and the fixed Landau consumer.

Every one of those interfaces requires independent reconstruction. RH should
not be announced as proved before that review.

## Critical-exponent interpretation

For a cutoff `U=W^alpha`, Type I costs `W^(alpha-1/4+o(1))`, while balanced support begins beyond `W^(4alpha)`. The unique meeting point is `alpha=1/4`.

## First hostile review target

Reconstruct `L-103070` from the exact operator-valued statement of `L-102953`.
In particular, verify that the complete owner/equal-pair/source Hilbert
transport is uniform when the cutoff is increased to the local quarter-power
endpoint and that no labelled owner fibre is replaced by an orthogonal proxy.
This is the first place the proposal should be broken if it is wrong.
