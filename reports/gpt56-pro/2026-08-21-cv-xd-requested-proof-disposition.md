# CV/XD requested proof attack — exact surviving result

## Executive result

The literal squared-core collar requested for CV is easier than expected: it
is pointwise nonnegative, so its one-sided negative variation is zero.

The hoped-for implication to CV fails at the next arrow. The collar belongs to
an adaptively completed source. Returning to the fixed critical scalar requires
an alternating, source-labelled desmoothing correction.

On XD, the long largest-prime sector is eventually positive. The short sector
contains the exact negative prime carrier of equal magnitude, so its proposed
energy estimate is false. The only valid hybrid is the carrier-preserving
sum-channel projection, which is exactly the original minimal wavelet.

## New bridge

The kernel calculation

\[
\mathscr DT=DK_0
\]

gives

\[
\mathscr Dh_\beta
=(I-67^{-1/2}S_{67})DG_\mu.
\]

This identifies one common signed interface rather than two independent
missing estimates.

## Research consequence

The programme should no longer dispatch separate agents to prove:

```text
completed-collar positivity;
long-sector positivity.
```

Both are closed.

The remaining work is the common source-faithful covariance:

```text
signed desmoothing correction
  + carrier-annihilating dyadic observation
  + physical short/long recombination.
```

That estimate is still RH-scale. It was not proved in this pass.
