# Common mother and covariant stress tensor — research report

## Main discovery

The activation-free critical channel and corrected same-`K1` wavelet are not
merely compatible. After one mandatory fixed zero-safe dyadic filter they are
first-order images of one compact mother:

\[
K_{\rm CV}=D\Phi_*,
\qquad
K_{\rm XD}=(D+3/2)\Phi_*/2.
\]

The constant Bezout identity reconstructs the mother with no inverse. This
removes the former `K0/K1` mismatch and gives one source normalization for the
whole programme.

## Stress redesign

The naive `(u,theta)` tensor fails because the absolute phase derivative
carries `(log n)^2`. The correct coordinate is

\[
Q=u-i\sum_\ell\log p_\ell\,\partial_{\vartheta_\ell}.
\]

It measures relative position inside the compact mother packet and obeys a
Heisenberg commutator with scale translation. The resulting determinant reserve
is uniform.

## Strongest exact quantitative statement

For every one-octave source shell after exact carrier recombination,

\[
\|K_{\rm XD}\|^2\le0.934564\,\|K_{\rm CV}\|^2,
\]

and the channel Gram has a strictly positive determinant unless the packet
vanishes.

This is the strict local Perron reserve sought in the programme. It does not
estimate the native arithmetic coefficients.

## Remaining frontier

The programme is now blocked only at two arithmetic rows:

1. the carrier-recombined scale/collar estimate;
2. physical occupancy of the residual balanced same-`K1` packet.

The finite-squaring, Dickman, Vaughan, Volterra and finite-certificate tools now
act functorially on both channels, so future work cannot hide a source
normalization mismatch inside their composition.

RH remains unproved.
