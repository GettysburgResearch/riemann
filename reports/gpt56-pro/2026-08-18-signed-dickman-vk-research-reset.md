# Research digest: signed Dickman transfer and the Vinogradov–Korobov terminal sector

## Choice

The latest Dickman packet proved positivity only while the positive Dickman
main term dominated an **absolute** payment for the bounded annular remainder.
That payment becomes sharp at the critical corridor. The highest-leverage task
was therefore to transport the remainder through the same rough parity source
rather than discard every sign.

## New theorem

For a general annular base,
\[
\mathcal F_b(Y,z)/\sqrt Y
\]
is the convolution of the boundary profile
\[
\beta(w)=e^{-w/2}b(e^w)
\]
with the discrete squarefree prime convolution exponential.

Replacing the prime reciprocal measure by \(dt/t\) gives exactly the signed
Dickman measure \(d\rho\), not merely the main scalar \(\rho\). The complete
continuum boundary term is
\[
\eta(Lu)+\frac1L\int_0^{Lu}\eta(w)\rho'(u-w/L)\,dw.
\]

The discrete error is controlled by the prime-harmonic interval discrepancy
and repeated-prime mass. The Vinogradov–Korobov PNT then gives positivity in
the moving sector
\[
\log z\ge
C_0(\log Y)^{5/8}(\log\log Y)^{3/4}.
\]

## Exact architectural diagnostic

The first boundary moment of the \(P_B\) base is
\[
\kappa_B/a_B=
\sum_{p\le B}\frac{\log p}{p-1}
+\gamma+\log4-\frac{35}{8}.
\]
It changes sign between \(B=11\) and \(B=13\). The \(P_{61}\) correction is
adverse, but only relative \(O(\log u/\log z)\); the previous unsigned
\(1/\log z\) payment was not intrinsic.

## Portfolio effect

The new theorem removes a much larger canonical terminal region. It does not
resolve odd-history orientation or the low-threshold critical Type-II/Lorenz
core. Those remain the exact RH-bearing interface.

```text
new mathematics             unconditional
new terminal sector         unconditional
full critical transport     open
Riemann Hypothesis           UNPROVED
```
