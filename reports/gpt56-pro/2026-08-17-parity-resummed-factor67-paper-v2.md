# Parity-resummed factor-67 paper v2

The first paper's leafwise terminalization is false by the exact odd-history witness.  The v2 replaces it with a complete-source construction:

- keep all P61 colors grouped;
- carve exact future-child reserves before Hall observation;
- use the positive 5:3 scalar;
- resum parity with `(I+T)^-1=(I-T)(I-T^2)^-1`;
- finish directly by Mellin-Landau.

The reserve-preserving Hall injection is the new load-bearing theorem.  It is written atomwise and uses only activated source occurrences.  RH remains unproved pending hostile reconstruction.
