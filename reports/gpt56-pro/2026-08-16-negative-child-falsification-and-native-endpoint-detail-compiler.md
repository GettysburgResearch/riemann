# Negative-child falsification and native endpoint-detail compiler

## Frozen target and scope

```text
PR #511:
6ece82279cb03474ebc79914db572f6ff095d238

review #512:
625a9273696a1f017f32d37bf9052423ee8590c7

repair/audit #514:
944dabba065271f08f3cade0215292dcfbab590f

Target-Lorenz review #516:
2492bd48f8e2bafacfc04fc9237c06219b8c9bf8

live marginal PR #521:
b4a60a54b1d730a73ab1eb6bb571485dd40438d0
```

PR #511 is treated as a reconstruction target and is not amended.

## 1. Binding failures in the factor-67 source compiler

The paired stopping-line identity is a valid signed normalization, but its
actual oriented rough-child aggregate has strictly negative ordinary response
at `q=2`. It cannot be represented by a sum of separately nonnegative physical
rows. Cancellation with the finite forcing must occur before the unpaired
positive-row cone.

That necessary joint cancellation does not by itself solve the producer. If a
compiler is also required to preserve every native component-row coordinate
exactly, its output must equal the native Möbius row `c_X`. Positivity of the
compiler is therefore equivalent to coefficientwise positivity of `c_X`. A
negative component coordinate is an exact coordinate Farkas separator.

Review #516 independently removes the only claimed exact all-parameter
Target-Lorenz tail certificate: the retained transcendental interval backend
does not provide directed inclusion. Later reviews also expose source
activation knots inside nominal integer cells and unresolved anchored path
normalizations. These failures make another source-tree wrapper unattractive.

## 2. Radical replacement

The replacement abandons branchwise Möbius realization. It uses the explicit
positive parabolic endpoint atoms

\[
 a_T=d_T-d_{T-1}\ge0.
\]

Each endpoint `T` is one immutable owner. Its complete physical record is

\[
 (a_T,\Gamma_T,\Delta_T,H_T),
\]

where

\[
 \Gamma_T(q)=C_{a_T}(q),
 \qquad
 \Delta_T(q)=\Gamma_T(q)-2\Gamma_T(4q),
\]

and

\[
 H_T=\sum_qY_4(q)\Delta_T(q).
\]

The new theorem `L-94100` proves

\[
 \Delta_T(q)>0\quad(2\le q<T),
 \qquad
 \Delta_T(q)=0\quad(q\ge T).
\]

This is the missing native-detail property not present in the historical
ordinary-column endpoint-scale route.

## 3. One exact coefficient system

For fixed endpoint `X`, the backward native-detail greedy chooses

\[
 \lambda_T
 =\min_{2\le q<T}
  \frac{\rho^{(T+1)}(q)}{\Delta_T(q)}.
\]

The same `lambda_T` multiplies:

```text
source owner T;
every component-row coefficient a_T(n);
ordinary q;
ordinary 4q;
radix-four detail;
literal score H_T;
Y4 pairing;
endpoint deficit.
```

The output

\[
 d_X^{\rm ned}=\sum_{T=3}^X\lambda_Ta_T
\]

is nonnegative and satisfies

\[
 \Xi_{d_X^{\rm ned}}\le\Omega_X,
 \qquad
 C_{d_X^{\rm ned}}\le w_X
\]

at every physical column. This finite theorem is unconditional and has no
factor-67 window, child, activation-cell quantizer, finite/continuum source
comparison, terminal patch, or port.

## 4. Exact blocker scalar

Let

\[
 s_X(q)=\Omega_X(q)-\Xi_{d_X^{\rm ned}}(q).
\]

At stage `T`, the diagonal loss is

\[
 \ell_T=\widehat\lambda_T-\lambda_T,
\]

and exactly

\[
 s_X(T-1)=\Delta_T(T-1)\ell_T.
\]

Hence

\[
 J_\Lambda(X)-\mathcal H(d_X^{\rm ned})
 =\sum_TY_4(T-1)\Delta_T(T-1)\ell_T.
\]

If `B_X` is the largest unsaturated detail column, elementary Chebyshev
summation gives

\[
 B_X\le B\le X/4
 \Longrightarrow
 J_\Lambda(X)-\mathcal H(d_X^{\rm ned})
 <32(\log2)^2\sqrt B.
\]

Thus the explicit finite statement

\[
 B_X=o(\log^4X)
\]

would imply the required `o(log^2 X)` native deficit and feed the frozen
one-sided endpoint consumer. This blocker-localization theorem is **open** and
is not inferred from finite reconnaissance.

## 5. Actual generator and replay

`X-94100` evaluates the actual endpoint/carry formulas, not synthetic vectors.
It emits a complete `X=256` certificate containing:

```text
254 endpoint owners and coefficients;
254 physical row coefficients;
255 ordinary columns;
255 radix-four columns formed after the common q and 4q sums;
255 native targets, slacks and Y4 weights;
exact score/benchmark/deficit ledger at 70 decimal digits.
```

The retained replay checks:

```text
20,000 square-root harmonic inequalities;
8,001 endpoint-row entries;
32,385 positive endpoint-detail entries;
754 row/seed response cross-checks;
complete greeds at X=64,128,256;
11 hostile interface mutations.
```

Retained verdict:

```text
PASS_NATIVE_ENDPOINT_DETAIL_COMPILER_94100
93a37cab05f5f3da2317b62124c67192fb810f11bb1db44a9a141b4575acb20e
```

The emitted `X=256` certificate has proof-object digest

```text
980c7a85fd79a22f25a9b138e5700188cb59c5c2ae9e00bae55dd40d11945711
```

The replay is not evidence for blocker localization or RH.

## 6. Exact boundary

```text
branchwise oriented-child realization          REFUTED
Target-Lorenz directed tail at PR #508          UNCERTIFIED
exact all-native-row positive compiler          EQUIVALENT TO c_X>=0
all-scale positive detail atoms                 PROVED
native-feasible row at every finite X           PROVED
one coefficient across all coordinates          PROVED
native deficit                                  EXACT BLOCKER SUM
blocker localization B_X=o(log^4 X)             OPEN / RH-BEARING
accepted proof of RH                            NO
Riemann Hypothesis                              UNPROVED
```
