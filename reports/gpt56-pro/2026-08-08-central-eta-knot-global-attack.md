# Central cascade scope audit and parity-paired knot-resolvent attack

## Frozen scope

```text
PR #280  6a2195f4c173dfc3db1ac42596dce58706062b28
PR #279  d5fb54844711a0ecad6805298f749d07e9c2fcf4
```

The purpose of this pass was not to polish another scalar equivalent of RH. It was to test the newest proposed positive producer at its load-bearing continuum and Abel interfaces, identify its exact global source, and preserve only the path that still retains the reciprocal-zeta mode correctly.

## Exact findings

### 1. The claimed all-stage continuum positivity is false

For the stopped critical source

\[
W(x)=x^{-1/2}\log(1/x)\mathbf1_{x\le1}
\]

and the central residual operator

\[
(\mathcal Tf)(x)=\sum_{k\ge1}[f(2kx)-f((2k+1)x)],
\]

one has exact directed inequalities

\[
(\mathcal T^2W)(1/7)-(\mathcal T^2W)(1/8)>9/100,
\]

and

\[
(\mathcal T^3W)(1/16)<-1/100.
\]

Thus the second iterate is not decreasing and the third iterate is not nonnegative. The missing terms are reciprocal-knot distributions created when logarithmic derivatives are commuted through the compact stop at `x=1`.

### 2. The proposed third-Abel ambient repair is also false

For the exact binary--ternary producer and

\[
w_{520}^{(3)}(q)=\binom{522-q}{2}\mathbf1_{q\le520},
\]

the row `n=15` is

\[
-91/256.
\]

The third cumulative kernel therefore fails at a finite rational mutation. This does not decide the sign of the actual critical producer.

### 3. The full central cascade is an eta resolvent

The dilation coefficient has Dirichlet symbol

\[
C(s)=1-\eta(s),
\qquad
\eta(s)=(1-2^{1-s})\zeta(s).
\]

Hence

\[
(I-\mathcal T)^{-1}
\quad\text{has multiplier}\quad
1/\eta(s).
\]

Its coefficient at `n=2^v m`, `m` odd, is

\[
a_\eta(n)=
\begin{cases}
\mu(m),&v=0,\\
2^{v-1}\mu(m),&v\ge1.
\end{cases}
\]

The central cascade is therefore another exact gauge for the dyadic Möbius core. The scalar mass contraction `1-log 2` never removed the arithmetic pole; it moved it into the reciprocal-knot boundary source.

## Corrected global proposal

The surviving attack is not a single smooth cascade. It is a parity-paired decomposition

```text
smooth eta-contracted bulk
+
complete reciprocal-knot dyadic source.
```

The smooth bulk retains the useful central contraction. The boundary source is treated before zeta cancellation through:

```text
PR #263 parity frame and positive finite Bezout reconstruction;
PR #241 independent-frequency reflected physical block;
PR #269 factor-five localization and carry reserve;
PR #272 Pascal-cycle debt language.
```

The resulting review hinge is `PKRC`, an explicit strict recurrence for the reflected physical knot energy. All potentially negative carry interactions are confined to quotient cells `2,3,4` and a finite small-row table, but the physical pole channel, bottom charge, and first-cell Mertens mutation must remain present.

## Status

```text
central exact residual / two-pass packing       retained
all-stage continuum positivity                  refuted
third-Abel ambient positivity                   refuted
eta-resolvent / odd-Mobius decoder               proposed exact
parity-paired knot-resolvent architecture        full conditional proposal
PKRC                                             open / RH-bearing
Riemann Hypothesis                               unproved
```

The branch should be reviewed first as a scope correction. No unconditional RH claim is made.