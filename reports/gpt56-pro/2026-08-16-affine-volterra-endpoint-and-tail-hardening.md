# Affine–Volterra endpoint and imported-tail hardening

## Executive result

The affine geometry is sound, but the former closure was normalized against the wrong endpoint quantity.

```text
physical Y4 slack            = P_Lambda-H
required complete loss       = J_Lambda-H
difference                   = F_Lambda=J_Lambda-P_Lambda
```

The exact affine compression, same-row column algebra, and physical `<3457` estimate survive. The RH conclusion does not.

The imported Target–Lorenz tail is also not proof-grade because its transcendental interval wrapper does not enclose irrational values. A true MPFR producer is supplied, but the full artifact and actual anchored source marginal remain mandatory.

The strongest separate bypass is the fixed-row Mellin–Landau route. Its analytic consumer survives, while the published finite prime-sieve transport still lacks a global one-use allocation.

## Scientific status

\[
\boxed{\text{RH remains unproved.}}
\]

This packet is a hardening and repair boundary, not an unconditional proof.
