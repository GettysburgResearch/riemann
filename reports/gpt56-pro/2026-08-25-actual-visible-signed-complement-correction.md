# Actual-visible × signed-complement correction

The OUT/HOLE decomposition of `L/T-106440` is exact. The inherited visible
payment `<1/600 N` is not: it is a source-density ratio before the physical
quotient, whereas the conclusion uses `||H_U P||_HS^2`.

The exact missing map is

```text
H_(N/D) P = H_(1/D) T_(N-D) P.
```

A simple `U=z^-m` fixture has zero denominator-cancelled analytic Hankel
energy but actual hard-band energy `min(m,d)`. The source-blind promotion is
therefore false.

The corrected conclusion ledger is

```text
q = actual visible all-pass energy / N
a = positive outer spectral asymmetry / N
b = positive signed in-band hole / N

q + a + b < 73/1250  ->  more than 90%.
```

At `q=1/600`, the old residual allowance `a+b<851/15000` is recovered
conditionally. Rational companion visible and tail terms are now explicit
Cauchy--exponential quadratic forms in the actual poles, zeros and residues.

PR #729 supplies a sharp causal commutator route for the literal vertical-
shift inner phase. `L-106441` correctly prevents transferring it to the
finite-alpha derivative companion without a factorization, product cocycle,
or zero-crossing-free homotopy.
