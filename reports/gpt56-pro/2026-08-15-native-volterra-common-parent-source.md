# Native Volterra common-parent source attack

## Purpose

Respond to the independent review of the one-shot factor-67 packet, whose first open inherited arrow was the absence of an exact derivation of the positive common-parent endpoint measure from the original signed native arithmetic datum.

## Result

The missing measure is the exact Volterra image of the Möbius equality seed.  The rough lift commutes with that image, producing an explicit positive common-parent fibre (`L-91762`).
For

\[
 g_s(m)=\left(\sqrt m-m/\sqrt s\right)\mathbf1_{m\le s}
\]

and

\[
 L(x)=\sum_{k\le x}\mu(k)k^{-1/2}(2\sqrt{x/k}-1),
\]

one has exactly

\[
 \overline b_X^\star
 =\int_1^X\frac{2L(X/s)}s g_s\,ds,
\qquad
 \overline c_X
 =\int_1^X\frac{2L(X/s)}s\mathcal Rg_s\,ds.
\]

The finite native row satisfies

\[
 c_X=\overline c_X+\mathcal RE_X,
\]

so its ordinary and radix-four targets are exactly the continuum observations plus the genuine finite mismatch.

At fixed `s`, every Möbius colour multiplies the same endpoint feature.  An explicit complete-graph coupling exhausts every negative colour and leaves a positive residual of total mass `L(X/s)`.  On the retained factor-67 interval, `L>159/500`, all root colours are below `67`, and the source coupling is disjoint from rough first ownership.

## Composition

Substituting this explicit source into the frozen one-shot realization removes the inherited common-parent assumption.  The already-deposited closing layer then gives, on its frozen analytic inputs, one finite nonnegative native-feasible row with exact weighted slack below `61000`.

The resulting composition is deposited as a candidate-complete packet for hostile review.  RH is not treated as accepted.

## Exact review frontier

```text
one-colour integral formula                   reconstruct directly;
infinitesimal endpoint-row positivity         retained L-91112 sublemma;
rank-one source coupling                      exact finite algebra;
finite/native mismatch identity               exact;
factor-67 L positivity                        directed frozen input;
all-column/terminal reserve                    frozen PR #493 stack;
one-sided endpoint consumer                    frozen, reconstruct;
Riemann Hypothesis                             unproved pending review.
```
