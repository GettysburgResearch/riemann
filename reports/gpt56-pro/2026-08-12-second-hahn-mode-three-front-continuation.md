# Second-Hahn-mode continuation of the three-front attack

The central new observation is that the three leading routes share not only a martingale-shadow vocabulary but the same first nontrivial spectral sector.

- The factor-54 butterflies are two-moment-neutral and therefore live beyond the constant and linear Hahn modes.
- The Cauchy/Jordan Green-removal numerator removes the corresponding low-order storage data.
- The Beta(2,2) Stein variability in the Brownian/theta route is exactly one quadratic Jacobi mode.

`L-91405` proves the exact spectrum and the uniform `2/5` inverse on the two-moment complement. `L-91406` proves the rank-two finite Stein decomposition and the rank-one Jacobi limit. `L-91407` repairs the factor-54 projection target: signed radix-four capacity requires a cone-valued joint output certificate, not merely ordinary positive color erasure.

## Route A

The final finite certificate must include the signed detail order

```text
(R_q, 2 R_(4q)) in {(x,y): x>=y>=0}
```

for every active column. A positive ordinary-column partition does not imply this. The robust grid theorem remains applicable once the complete cone coordinates are included.

## Route B

The exact knot reduction remains valid, but one-sided FKG is not the missing mechanism. The natural next object is a degree-two Hahn edge square coupling the contact term and the first two Green orders before any scalar lower bound is taken.

## Route C

The finite Stein fluctuation is exactly

```text
-(1/2) H_1 -(1/4) H_2,
```

and the continuum Beta(2,2) fluctuation is exactly

```text
-(1/4)(v^2-1/5).
```

No Jacobi mode of degree at least three appears. The remaining Brownian/theta problem is therefore a finite low-rank Schur completion, not an uncontrolled infinite-dimensional Stein defect.

RH remains unproved. The three exact next proof objects are listed in `T-91402`.
