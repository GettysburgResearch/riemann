# Carry successor: source-specific terminal control beyond the span/Farkas obstruction

## Result

The root-completed source is no longer sent through one hoped-for positive
policy. Every descending policy already closes all nodes `n>=4`; its complete
failure is the one terminal vector

\[
(-3\tau,2\tau).
\]

A finite cutoff controller changes this scalar monotonically. The local drop at
parent `n` is its nonnegative occupation times an explicit mod-12 coefficient:

```text
n mod 12:  0   2     4      6   8   10
coefficient 0  1/16  31/16  1/16 0   2.
```

Two positive occupations on opposite sides of zero glue to an exact positive
CRCTP flow. Directed finite certificates exist for every tested endpoint
`40..128,160,192,256,384,512,768,1024`.

## Advance over the reset packet

PR #538 proved the signed span and a positive two-leaf subcone. This packet
constructs a source-specific positive control mechanism outside a single fixed
tree cone. It explains why the preliminary mixed renewal looked positive while
still missing the terminal ratio, and repairs that ratio without negative flow.

## Honest boundary

The unbounded statement is not established. Fixed finite branching laws remain
subject to the repository's resonance firewall, and the dyadic root scalar is
independently RH-bearing. The exact remaining theorems are FBTC and the
one-sided root estimate; RH remains unproved.
