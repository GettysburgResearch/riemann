# Two-route terminal assault after the T-100300 integrator

## Verdict

The repository no longer contains many independent conclusion paths.  After
all native-source, normalization, positive-flux, detector-cancellation and
root-term firewalls, two routes remain genuinely distinct.

### A. Actual-prime activation

The globally positive quadratic family supplies real slack.  The new
activation-prefix theorem proves `0<S_(3/2)<=1` on every product-threshold
ideal and sums the complete jump ledger.  The only remaining arithmetic is the
continuous critical drift.  PR #685's exact negative-area coordinate is
`CATD100300`; `CDTG100320` is a stronger pointwise producer.  PR #684 supplies
the strongest local decomposition: squared small primes plus one short
large-prime collar.

### B. Primitive ratio-eight wavelet

The minimal wavelet is the exact signed terminal map of the Hasse flow.  Cauchy
phase integration followed by GCD extraction leaves a primitive coprime-pair
bilinear; all common-core signs disappear into a positive Euler factor.  PR
#685's Vaughan identity supplies the complementary balanced-trilinear terminal
`BVD100310`.

## New exact contributions

```text
product-threshold 3/2 prefix in (0,1]       proved
complete activation-jump sum                 proved
continuous-drift normal form                 proved
fixed ratio-eight Poisson kernel              proved
positive common-core Euler factor             proved
primitive coprime-pair terminal               proved
rank-one recurrence mutation firewall         proved
```

## Scientific boundary

`CATD100300`, `CDTG100320`, `BVD100310` and `PRBC100330` are not proved by
this packet.  RH remains unproved.
