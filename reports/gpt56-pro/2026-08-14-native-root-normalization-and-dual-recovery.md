# Native-root normalization and dual recovery after the PR #444 review

Date: 2026-08-14  
Status: **research packet; RH unproved**

## Executive correction

The PR #444 reviewer was right that the decisive endpoint datum had not been
assembled. A more serious normalization issue was then isolated:

\[
C_{P_{61},X}(q)
=w_X(q)+\text{positive rough reservoir}.
\]

The strict witness at `(X,q)=(136,2)` is

\[
C_{P_{61},136}(2)-w_{136}(2)
=\frac{1}{\sqrt{134}}\log\frac{68}{67}>0.
\]

Therefore the canonical finite-Euler row cannot be inserted as the native root
row while its rough children are also retained. Historical language treating
it as a complete native endpoint datum is withdrawn.

## Exact results now resident in this packet

### 1. Native normalization

The full Möbius row

\[
c_X(j)=\sum_k\mu(k)k^{-1/2}Q_{X/k}(j)
\]

has exactly:

\[
C_{c_X}=w_X,
\qquad
\Xi_{c_X}=\Omega_X,
\qquad
\mathcal H(c_X)=J_\Lambda(X).
\]

This is the correct root dictionary.

### 2. Positive radix-four dual

Define

\[
Y_4(q)=\sum_{k=0}^{v_4(q)}2^k\Lambda(q/4^k).
\]

Then

\[
Y_4(q)-2\mathbf1_{4\mid q}Y_4(q/4)=\Lambda(q),
\]

and every finite row satisfies

\[
J_\Lambda(X)-\mathcal H(d)
=\sum_qY_4(q)[\Omega_X(q)-\Xi_d(q)].
\]

The endpoint objective is exactly positive weighted detail slack.

### 3. Exact rough reservoir

With `R_67` the monoid generated by primes at least `67`,

\[
D_{P_{61},X}
=\sum_{m\in R_{67}}m^{-1/2}c_{X/m}
\]

simultaneously in rows, ordinary columns, radix-four columns, and literal
entropy. The `m=1` term is the native packet; all other terms are the rough
reservoir. Least-prime labels make the reservoir source-disjoint.

### 4. Positive typed causal generator

After an arithmetic packet has entered the positive typed component cone, its
causal residual has nonnegative rows and exact residual capacities. Its local
physical deficit is at most twice its exact SHARP target mass.

Combined with a child-mass ledger below `1/8`, the abstract recurrence gives a
uniformly bounded deficit. The remaining theorem is the hereditary native
entry, not the consumer.

## Ambitious current attack

The strongest current generator candidate is

\[
D_{P_{61}\cup\{p\},X}(j)
=D_{P_{61},X}(j)-p^{-1/2}D_{P_{61},X/p}(j).
\]

Deterministic scans over nearly sixteen million structured cells for
`p=67,71,83,127`, plus one million randomized cells through prime `5003`, found
no negative value. This does not prove the infinite theorem, but it strongly
supports a Green/Kantorovich proof with worst prime `67`.

The exact global target is now the Native-Root Capacity Theorem (`T-91314`):
construct one nonnegative current row and source-disjoint children of total
mass below `1/8`, prove componentwise one-use native ordinary/detail capacity,
and bound the `Y_4`-weighted current slack.

## Why exact zero slack is not an easier target

Finite linear-program reconnaissance showed that, when the canonical row is
thinned under the native capacities and the `Y_4`-weighted slack is forced to
zero, the optimizer coincides with the full Möbius row `c_X` throughout the
tested range. This is expected: exact native capacity and exact native entropy
recover the conclusion-producing object.

The reset architecture must therefore prove a bounded positive slack, not hide
an exact global saturation claim.

## Exact frontier

```text
compact canonical P61 row census               DIRECTED EXACT
canonical P61 native admissibility              FALSE / R-91312
full Möbius native dictionary                   EXACT / L-91377
positive radix-four dual                        EXACT / L-91378
rough reservoir decomposition                   EXACT / L-91379
positive typed causal debt <=2 target           EXACT / L-91375
subcritical target-mass consumer                EXACT CONDITIONAL / T-91312
one-sided endpoint-to-RH consumer               EXACT CONDITIONAL / T-91313
one-prime current-row positivity                STRONG FINITE EVIDENCE / OPEN
Native-Root Capacity Theorem                    OPEN / RH-BEARING
CFFP / hereditary typed reset                   OPEN
Riemann Hypothesis                              UNPROVEN
```
