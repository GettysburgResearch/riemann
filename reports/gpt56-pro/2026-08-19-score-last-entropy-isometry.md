# Score-last entropy isometry: removing the final artificial score debt

## Executive result

The factor-67 candidate has been carrying a local declared-score ledger through
its rough-prime recursion. That ledger is useful for Hall orientation, but it
is not necessary for the conclusion. Every conclusion-producing operation
before root safety is an exact identity of physical rows.

The decisive reordering is:

```text
old:
  score each packet, estimate a local defect, recurse;

new:
  expand the complete finite positive row tree, then score once.
```

The actual rough-prime tree terminates because every child satisfies
`Y'<=Y/67+1`. Literal entropy is linear, so the complete arithmetic recursion
preserves score exactly. No terminal debt remains.

## Explicit consequence

The only score losses are current-owned:

```text
fixed top omission  <60;
one scalar thinning <792.
```

Thus the final feasible row obeys

\[
 \mathcal H(d_X)>4\sqrt X-852
\]

and

\[
 J_\Lambda(X)-\mathcal H(d_X)<4\log X+852.
\]

## Why this matters

This removes four recurring proof risks at once:

- source-declared score versus literal entropy;
- artificial fixed-67 descendant paths;
- terminal score debt multiplied by generations or leaves;
- mass-normalized proxies for signed loss.

The remaining candidate is now concentrated on physical-row identities and the
endpoint consumer.

## Status

The new finite-tree and root-cost arguments are exact. Their inherited
endpoint-frame and all-column inputs remain subject to hostile reconstruction.
RH remains unproved pending that review.
