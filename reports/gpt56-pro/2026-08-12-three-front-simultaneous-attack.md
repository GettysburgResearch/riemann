# Three-front simultaneous attack on the remaining RH proof obligations

Date: 2026-08-12  
Branch: `research/gpt56-pro/91410-three-front-final-attack`  
Base: `b837c12199dd407116f604ce6c938039d1a76da4`  
Status: **research report; RH unproved**

## Executive result

The pass compared the live arithmetic reset, Brownian/theta, and Cauchy–Jordan
stacks rather than adding another equivalent scalar. It produced one new exact
or proposed-complete theorem on each front:

1. `L-91410`: factor-54 rough recursion is a **one-prime first-entrance reset**;
   same-generation multiprime tensorization is unnecessary;
2. `L-91411`: Xi Pick positivity is the integral of **infinitesimal Stieltjes
   kernels** with an explicit two-channel feature map;
3. `L-91412`: a redesigned **two-Green Jordan source** is unconditionally a
   positive Hardy Gram at every horizontal scale.

None of the three conclusion-producing terminal theorems was proved. The pass
substantially changes where effort should be placed.

## Route I — arithmetic first entrance

The support inequality

\[
1/67<c_0
\]

means that one source atom cannot traverse two new rough primes in one reset
generation. Its least rough prime sends it directly below the contracted
endpoint. Thus the distinct-prime scalar tensorization counterexamples on PR
#399 do not obstruct the coefficient-one recursion.

The target capacity must be partitioned by the same least-prime source
partition. A positive scalar or matrix-valued Markov kernel preserves that
partition exactly. This eliminates branchwise target duplication and all
unmatched-column leakage once continuous affine covariance is used.

The remaining hostile joint is precise: the positive completion `N_p` and the
strict endpoint Schur reserve must be shown to form a **matrix-valued source
partition** before target transport. A lower eigenvalue bound alone is not that
identity. The radix-four detail must also be carried by a positive detail
transport, not inferred by subtracting ordinary inequalities.

If those two joints pass, the factor-54 recurrence follows formally and is the
closest route to a conventional finite proof.

## Route II — Brownian/theta Stieltjes gate

Differentiating the finite Xi transport in the horizontal scale gives

\[
\partial_a(C-D_aCD_a)=D_a(H_aC+CH_a)D_a.
\]

This is useful because the kernel generated by

\[
h(r)=r\int_0^\infty(r^2+t)^{-1}d\mu(t)
\]

has the explicit feature map

\[
r\mapsto
\left(
 \frac r{r^2+t},
 \frac{\sqrt t}{r^2+t}
\right).
\]

The Brownian/theta route no longer has to identify the full nonlinear
impedance at once. It may construct one positive spectral measure for each
symmetrized Fisher state

\[
h_a(r)=E_{r-a}Z+E_{r+a}Z.
\]

The exact two-point test reduces further to monotonicity of

\[
\ell_a(r)/r.
\]

This is a useful low-order audit but not an all-size proof. The all-size target
is the Stieltjes measure itself, plausibly obtainable as the spectral measure of
an explicit theta DtN boundary problem or from the Gamma–Beta Jacobi bulk.

## Route III — universal positive Jordan source

The previous sharp three-Green numerator left a compact density sign problem
for `0<a<1/2`. The new port changes the source design.

Weighted Harris–FKG gives the uniform bound

\[
E_{s,0}(t)\ge c_s(1-\log2).
\]

Consequently the two-Green channel with one fixed pole at four has the positive
measure

\[
1+
\sum_{n\ge2}\frac{F_s(n)}n\delta_{\log n}
+igl[-c_s+4E_{s,0}(t)\bigr]dt,
\]

and the continuous density is bounded below by

\[
c_s(3-4\log2)>0.
\]

This supplies a fully phase-resolved source Hilbert space for every scale. The
remaining problem is no longer arithmetic source positivity but an exact
completed gamma/pole source-to-Hardy colligation. The new source is not silently
identified with the old sharp residual.

## Ranking after the pass

1. **Factor-54 first entrance** — closest to a finite end-to-end proof; two
   sharply stated audit joints remain.
2. **Brownian/theta Stieltjes gate** — strongest independent conceptual route;
   one positive spectral measure would close every matrix at once.
3. **Two-Green Jordan–Hardy** — strongest unconditional safe-side source; one
   completed colligation remains.

## Immediate experiments and proof tasks

### Route I

- reconstruct the one-prime completion as a matrix-valued measure identity;
- check the exact source mass before and after the `N_p` correction;
- replay the detail kernel directly on every fractional column;
- reject any proof which uses ordinary-column subtraction.

### Route II

- compute `ell_a(r)/r` and its derivative with certified arithmetic on a safe
  real grid as reconnaissance;
- derive the theta Sturm–Liouville candidate spectral measure;
- test whether the measure is positive before invoking any Pick conclusion;
- compare its first moments with the Stein-kernel variability formula.

### Route III

- construct the Stinespring space represented in `L-91412.7` explicitly;
- factor the fixed-pole source through the causal Hardy target;
- retain the gamma and pole channels from the first line;
- verify coefficient-one return at scales `a` and `2a`.

## Exact status

```text
three-front mathematical synthesis                    COMPLETE
one-prime reset support/composition theorem           PROPOSED COMPLETE
infinitesimal Stieltjes feature theorem                EXACT
universal two-Green positive source theorem            PROPOSED COMPLETE
route-I matrix port identity                           OPEN
route-II theta spectral measure                        OPEN
route-III completed colligation                        OPEN
Riemann Hypothesis                                     UNPROVED
```
