# Boolean incidence hardening and equal-pair bypass

## Research inputs

This pass started from:

```text
PR #719  independent T-106080 disposition / Hodge-Boolean incidence frontier;
PR #751  corrected minimum-owner Boolean Vaughan theorem;
PR #730  radial-owner and Pluecker Hodge identities;
PR #715  common mother, carrier and fixed Mellin consumer.
```

The original full proposal on PR #751 had already been retracted at the physical owner-occupancy interface. The purpose of this continuation was to determine which parts of that interface are genuine.

## Exact new findings

### 1. The exceptional sector is empty

Boolean balance gives two disjoint factors above `U=floor(Y^(1/6))`, so the completed core is larger than `Y^(1/3)`. An exceptional co-owner above `4 sqrt(Y)` would force `lambda a^2<4 sqrt(Y)`, contradicting `lambda>=2` for every `Y>64`.

Thus the no-exception inequalities

```text
Lambda^2 <= a;
P=lambda Lambda <= a
```

hold cofinally for every balanced atom.

### 2. The complete opposite owner products are paid

For an interacting ratio-eight pair

```text
N=P a^2;
M=Q b^2,
```

one has

```text
Q<=2a;
P<=2b.
```

Hence every fixed clean four-owner packet is already in the full product-modulus long-core regime.

### 3. Incidence masks do not damage the centered kernel

The exact one- and two-family centered phase estimates survive arbitrary modulus-dependent masks of modulus at most one. Owner identity, Boolean support, gcd strata, discrepancy labels, marked primes and shell data can therefore remain in the physical integer sequence without changing the divisor-count theorem.

The unresolved issue is source coefficient size and coherent aggregation, not mask insertion.

### 4. The selector Gram is a signed transpose form

The corrected PR #751 Gram has the exact normal form

```text
P = ||F||^2;
G = <F,T F>;
G = ||F_+||^2-||F_-||^2.
```

The total positive moment is an overstrong sufficient target. Only the antisymmetric owner current can contribute adversely to the clean Gram.

### 5. Minimum-owner transfer is a star plus a Pluecker cycle

For one depth-`k` occurrence, the difference between equal-pair and minimum-pair ownership splits orthogonally into:

```text
star energy   2/k;
cycle energy  (k-3)/(k-1).
```

The star is the radial actual-owner endpoint current. The cycle is exactly the row-zero Pluecker local-system current of PR #730. The cycle is not combinatorially small.

### 6. Minimum-owner ownership is optional

The exceptional-sector proof also shows that every label in a cofinal Boolean balanced occurrence is at most `4 sqrt(Y)`. Therefore every unordered pair is horizon-safe, and the canonical equal-pair Duhamel gauge can be retained.

Phase labels may be endpoint-placed independently through the primewise flat gauge. The owner transpose curl is therefore a coordinate of the concentrated minimum-owner proof, not an intrinsic final obstruction.

## Correct final interfaces

Two compatible routes remain:

```text
concentrated-owner route:
  COCURL102980
  = arithmetic orientation of the star/Pluecker owner current;

symmetric-owner route:
  BCI102990
  = physical orientation of the coprime two-sided Boolean core in the
    canonical equal-pair gauge.
```

The second is the cleaner normal form. Both feed the already frozen chain

```text
ICPR102970
 -> OICP102960
 -> HMO102940
 -> fixed Mellin-Landau consumer
 -> RH.
```

Neither open arithmetic estimate is proved in this pass.

## Status

```text
exceptional balanced owner sector             removed cofinally;
full owner/core range mismatch                 removed;
incidence-mask analytic transport              proved;
minimum-owner Gram signature                   proved;
minimum/equal owner Hodge change               proved;
canonical equal-pair bypass                    proved;
BCI102990 / COCURL102980                       open / RH-bearing;
Riemann Hypothesis                             unproved.
```