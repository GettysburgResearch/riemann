# Audited cubic hinge and compensated rough-prefix frontier

## Why this successor exists

PR #694 proved the short/long minimal-wavelet split was source-exact but
norm-incompatible: the two regions carry equal and opposite prime main terms.
The live implication matrix on PR #691 then advanced and withdrew its original
positive-renewal desquaring claims. PR #695 replaced them with an audited
mixed-coboundary and completed-minus-transition homotopy.

This packet therefore:

1. keeps the carrier until it is removed with its exact sign;
2. opens every remaining cubic collar into a literal arithmetic prefix;
3. restacks on PR #695;
4. does not use the withdrawn renewal shortcut.

## Exact kernel opening

In square-root coordinate,

```text
Psi(t^2)=192t-64+64(1-t)_+^3.
```

Every double-owner interval equals an explicit nonnegative linear carrier plus
one compact cubic spline.  Its derivative is a positive coarea of a four-term
compensated reciprocal-Mobius prefix on the finite interior prime semigroup.

This is the exact source-level form of the former endpoint collar.

## Uniform arithmetic control

Tao's elementary semigroup theorem gives `|A_P(x)|<=1`. Therefore the full
compensated-prefix derivative is uniformly bounded independently of the number
of interior primes.

The centered collar's third derivative has coefficients `d^-2` and endpoint
weights `p^-3/2,q^-3/2`; its total variation is controlled by the convergent
prime-`3/2` Euler product. Finite prime cutoffs converge in total variation,
then locally uniformly after restoring the exact boundary data.

These facts remove history-count and undefined-cross-Gram losses, but do not by
themselves prove critical one-sided mass.

## Exponent-e positive region

Reciprocal Euler-level pairing gives

```text
1-S_(p,q) <= A_P(x) <= 1,
S_(p,q)=sum_(p<ell<q) 1/ell,
```

whenever `S_(p,q)<1`. Hence the compensated prefix is positive whenever

```text
S_(p,q)+p^(-1/2)+q^(-1/2)<1.
```

By Mertens' theorem, for every fixed `A<e`, every sufficiently large prime
interval `q<=p^A` is pointwise nonnegative at every physical scale. The live
negative matrix is confined to supercritical endpoint separation

```text
log q >= (e-o(1)) log p.
```

## Adaptive two-certificate gate

Taylor expansion from the activation origin and the completed deep end gives
positive certificates `L,R` with

```text
H_- <= min(L,R).
```

The first draft's unrestricted integral of `L` was false: after the collar
vanishes, `L` grows quadratically. The corrected packet makes the deterministic
partition

```text
activation side  L<=R: spend L;
deep side        R<L:  spend R.
```

A joint-min--max average of the two pieces gives the exact implication

```text
APCC100723 AND DPCC100723 -> RH.
```

`APCC` is assigned to finite reciprocal-prefix / activation arithmetic.
`DPCC` is assigned to PR #695's balanced completed-minus-transition homotopy.

## Exact boundary

```text
cubic hinge / carrier / collar                    proved
arithmetic derivative coarea                      proved
uniform semigroup-prefix control                  proved
summable third variation                          proved
positive interval region A<e                      proved asymptotically
adaptive Taylor gate                              proved
regularity-only closure                           refuted
unrestricted left-certificate integral            refuted
APCC100723                                        open
DPCC100723                                        open
Riemann Hypothesis                                unproved
```
