# Fixed-`67` row audit and the current full-proof frontier

Date: 2026-08-13  
Frozen candidate base: PR #424 at `7c0927e9364e191c720d5a618928d99847ac8edd`  
Branch: `agent/91552-fixed67-row-audit`  
RH status: **unproved**

## Executive result

The live factor-54 route has replaced the withdrawn parallel-source telescope
by a typed positive-source recurrence and one fixed scale-`67` geometric split.
This branch independently checks the first arithmetic component-row interface
of that geometry.

For the `P_61` packet, every real child endpoint `1<=y<67`, and every inherited
row `2<=j<=y`, the raw `P_67` row is exactly

```text
positive effective/survival row
+ positive dilation-normalization bonus.
```

Directed-exact uniform margins are

```text
parent effective row       > 1/100;
dilation bonus             > 1/100;
raw P_67 row               > 3/250.
```

The replay covers `143,715` activation cells and `862,290` endpoint quantity
checks using outward integer intervals at scale `10^35`.

## Exact identity

For

```text
A_j(x)=sum_(d|P_61) mu(d)/sqrt(d) Q_(x/d)(j),
x=67y,
r=67^(-1/2),
```

the raw row satisfies

```text
A_j(x)-r A_j(y)
 =(1-r)A_j(x)+r[A_j(x)-A_j(y)].
```

The theorem proves both pieces on the right strictly positive throughout the
complete fixed-scale window.

## What this removes

The following finite objections do not occur on this slice:

```text
negative inherited physical row;
negative raw-to-effective normalization correction;
target or score scalar separation;
need to spend the common endpoint port on this row correction.
```

This is useful because the current candidate uses the same fixed contraction
prime after positive source entry.  The theorem gives an independent raw
arithmetic compatibility check rather than relying solely on abstract type
heredity.

## What remains before a proof claim

The candidate composition must still be reconstructed independently at the
following joints:

1. **Finite producer.** Recheck the directed Hall margins and normalized
   component-row monotonicity from retained certificates and source code.
2. **Typed source identity.** Verify that the positive residual measure used by
   the fixed-`67` split is exactly the source carried by the physical row and
   ordinary/radix-four capacities, not merely a scalar ledger.
3. **One-use port ledger.** Confirm that endpoint correction, finite mismatch,
   collar and terminal annulus are each charged once after colors are summed.
4. **Native loss.** Derive the recursive inequality for the original signed
   entropy-score loss, including the allowed weights above one, rather than a
   renamed target-normalized quantity.
5. **Base and iteration.** Supply a finite base and rederive the contraction
   depth and accumulated debt without importing the withdrawn `T-91424` source
   partition.
6. **Dependency audit.** Exclude every path through `R-91303`--`R-91307` and the
   withdrawn parallel-mass recurrence.

Until those joints pass, the full proposal remains under review and RH remains
unproved.

## Independent source/colligation warning

The source/colligation route cannot currently serve as an independent backup.
The atomic Radon--Nikodym multiplier acts as `m_a(u)` after triangular lifting,
not as `m_a(u-t)` on the incoming tail-Hankel variable.  Moreover a Julia column
identity controls `I-M^*M`, while a de Branges model kernel is an output defect
`I-MM^*`.  These are separate operator-order corrections and must be repaired
before that route can be composed with the present factor-54 proof.

## Current status

```text
fixed-67 raw arithmetic component row              DIRECTED EXACT POSITIVE
fixed-67 raw/effective normalization bonus          DIRECTED EXACT POSITIVE
fixed-67 scalar target and score                    RESIDENT EXACT
recursive positive source type                      RESIDENT PROPOSED COMPLETE
full physical proof-DAG reconstruction              OPEN REVIEW
Riemann Hypothesis                                  UNPROVEN
```
