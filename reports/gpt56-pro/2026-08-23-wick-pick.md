# First-chaos Wick cancellation in the low-order Xi Pick matrix

## Correction

The T-105310 archimedean Cauchy model omitted an unavoidable degree-one prime
packet. After the natural normalization, its diagonal energy tends to `1/2`
per one-sided source. A direct one-percent comparison is therefore false.

## Repair

The reciprocal coefficients are `-partial_L C(N;L)/log N`, where `C` is the
frozen xi-prime coefficient family. Multiplying the observation family by

```text
W_(L,X)=exp[-A_X/(2L)]
```

is a zero-free source congruence. It changes the frozen reciprocal to

```text
exp(-x)/(1-x)=1+sum_(m>=2)c_m x^m
```

and removes the entire first von Mangoldt chaos.

PNT gives the one-sided coefficient energy

```text
D_W=sum c_m^2 m!/(2m)! <7/320.
```

The Hermitian frozen model therefore has effective rank above `160/167`; a
one-percent actual-Xi transfer would imply the multiplicity-counted proportion
`0.676831...`.

## Remaining problem

The open theorem is no longer an unexplained 1% model comparison. It is the
explicit three-row transfer `FREEZE + BOUND + TAIL`: derivative of the pinned
xi-prime Gram formula, horizontal-edge control for the finite Wick factor, and
an `o(N)` positive-index tail.

No new record or RH theorem is claimed.
