# Corrected provenance-causal factor-54 closure

**Date:** 2026-08-13  
**Status:** proposed complete proof pending independent reconstruction  
**Branch:** `research/gpt56-pro/91661-corrected-provenance-closure`

## Executive result

The corrected composition is `T-91652`.  It produces an absolute constant `K`
such that

\[
F_\Lambda(X)\le K
\]

for all sufficiently large integer endpoints.  The one-sided threshold in
`T-90011.14` then gives RH.

The proof does not assert `|F_Lambda|=O(1)` and does not require a lower bound.
The load-bearing independent-review target is the complete root current ledger
of `L-91659`.

## 1. Exact generator reset

For every base datum `P_X` and all active rough primes, `L-91650` gives

\[
P_X=s_kP_X+
\sum_i\lambda_i(P_X-r_iU_iP_{X/p_i})+
\sum_i\alpha_iU_iP_{X/p_i},
\]

where

\[
s_k+\sum_i\lambda_i=1,
\qquad
\rho:=\sum_i\alpha_i<1/8.
\]

The first two terms are current and have total certificate coefficient one.  The
last terms are actual same-index children with total coefficient below `1/8`.

`L-91654` proves every causal difference is nonnegative in target, component
rows, ordinary capacities and radix-four capacities.  `L-91657` gives a uniform
literal-score debt on the compact child window.  Thus every unit current
certificate has positive deficit at most one absolute `C_*`.

## 2. Uniform packet envelope

Positive homogeneity and subadditivity give

\[
\Lambda(X)\le C_*+\rho\Lambda(X/67),
\qquad \rho<1/8.
\]

Therefore

\[
\Lambda(X)<8C_*/7.
\]

This is a coefficient-space contraction before realization; it does not identify
a restricted packet with a scalar native packet and does not multiply signed
loss by source-mass fractions.

## 3. Root ledger

The native datum is decomposed exactly as

\[
\mathcal N_X=\mathcal C_X+R_X\widehat Z_X,
\qquad
\widehat m(\widehat Z_X)\le54.
\]

The current row contains matched Hall interval/butterfly rows, one globally
summed outer B-spline producer, one interior safety factor, one positive
width-three collar, one finite/continuum mismatch correction, one top omission,
and one common endpoint Schur port.

Every correction is current and charged once after current terms are summed.
The finite small-prime block is not reintroduced on a child.  The frozen imports
give one absolute current debt `C_root`, whence

\[
\Delta_X(\mathcal N_X)
< C_{root}+432C_*/7.
\]

The exact path, commit and blob for every import are frozen in
`t91652-full-ledger-lock.json`.

## 4. Endpoint conclusion

Ordinary feasibility and the exact finite von-Mangoldt identity give

\[
F_\Lambda(X)\le\Delta_X(\mathcal N_X).
\]

Thus `F_Lambda` has a uniform upper bound.  Since

\[
\frac{-1-\zeta(1/2)}4>0,
\]

the one-sided threshold theorem gives eventual negativity of the prime-only
endpoint and hence RH.

## 5. New normalization firewall

The tempting canonical finite-Euler route cannot be inserted unchanged into the
native ordinary cone.  Its response is

\[
C_{P,X}(q)=q^{-1/2}H_P(X/q),
\]

whereas the native budget is `q^-1/2 log(X/q)`.  Their exact difference is the
positive rough-number tail.  It is strict once `X/q>67`.

`L-91661` identifies that overdraw exactly as the total least-prime rough-child
reservoir.  The corrected proof subtracts each child inside a causal difference
before adding one contracted recursive copy.  Therefore no rough capacity is
spent twice.

## 6. Replay boundary

`X-91651` checks, with exact rational arithmetic, the reset identity, child
cancellation, recursive mass bound, geometric envelope, root mass-54 arithmetic,
one-sided threshold, least-prime partition, and frozen identifier shape.  It
deliberately does not certify the expensive root imports.

Expected verdict:

```text
PASS_CORRECTED_PROVENANCE_ENVELOPE_ALGEBRA
```

## 7. Independent-review order

```text
R-91654 and L-91661 normalization firewall;
L-91650/L-91652/L-91653/L-91654;
L-91657/L-91658;
L-91406 and T-91650 envelope;
L-91659 root current ledger and every frozen import;
L-91660/L-91662 endpoint conclusion;
T-91652 complete composition;
X-91651 exact algebra replay.
```

A failure in the root current ledger retracts the full proposal while leaving the
reset, envelope, normalization firewall and endpoint conclusion intact.
