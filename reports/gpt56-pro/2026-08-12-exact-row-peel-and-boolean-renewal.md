# Exact row peel and the full Boolean-shadow renewal

Date: 2026-08-12  
Branch: `research/gpt56-pro/91101-moment-neutral-shadow-transport`  
Base: PR #352 at `906b5a477a1ed7c88a40db7569924f15f3d54b72`  
Status: research continuation; **RH remains unproved**

## 1. Question attacked

The requested gate was to recombine:

1. the finite target/continuum mismatch;
2. the terminal quotient annulus;
3. the width-three positive quantization collar;
4. the 33-state parity shadow;

while preserving radix-four capacity, nonnegative endpoint weights, and bounded transfer to the contracted `(L,R)` state.

## 2. Main correction: the three collars are optional

The previous architecture first represented the equality correction by a continuum endpoint density, quantized that density into integer endpoint atoms, and then attempted to repair the resulting local errors.

That is not the sharp coordinate.

The exact equality inverse rows themselves have the representation

\[
 c_X(n)=\int_n^X L(X/s)\dot d_s(n)\,ds,
\]

where every infinitesimal endpoint row satisfies `dot d_s(n)>=0`. Since the equality density `L` is strictly positive for `1<=X/s<=c_0^{-1}`, every exact row with `n>=c_0X` is nonnegative.

Truncating the exact row vector at `K=ceil(c_0X)` then gives:

```text
ordinary columns q>=K                 saturated exactly;
radix-four detail columns q>=K        saturated exactly;
terminal annulus q>X/4                saturated exactly;
endpoint weights                      nonnegative;
target/continuum mismatch              absent;
B-spline quantization collar           absent.
```

All three former errors are the single exact inner residual

\[
 \mathcal R_{X,K}(q)=\sum_{q\le n<K}c_X(n)\beta_{nq}.
\]

The score also splits exactly; no outer score debt is introduced.

This is `L-91112`.

## 3. Source-faithful row packets

The equality row has the exact component decomposition

\[
 c_X(n)=\sum_{k\le X/n}\mu(k)k^{-1/2}Q_{X/k}(n),
\]

where every `Q_Y(n)` is manifestly nonnegative. Thus the squarefree parity labels of `L-91109` survive all the way to exact rows, ordinary carries, radix-four details, and entropy scores.

The remaining inner residual is therefore a genuine positive-packet parity problem, not a continuum discretization error.

## 4. Full sixteen-prime Boolean closure

The first parity certificate used the 33 squarefree integers active below `54.219...`. The complete finite Euler block contains all products of the sixteen primes through 53, hence 65,536 Boolean states.

For `a=1,2`, define

\[
 F_a^{(53)}(x)
 =\sum_{d\mid P_{53},\,d\le x}
 \mu(d)\left(\frac{a\sqrt x}{d}-\frac1{\sqrt d}\right).
\]

An exact directed certificate proves globally:

\[
 F_1^{(53)}(1)=0,
 \qquad
 F_1^{(53)}(x)>\sqrt2-1\quad(x\ge2),
\]

and

\[
 F_2^{(53)}(x)>0.3186\quad(x\ge1).
\]

The equality minimum occurs at the cell beginning at `x=33`.

Thus no product of the sixteen small primes creates a later sign obstruction. The 33-state result was the visible first slice of a globally positive finite Boolean forcing.

This is `L-91113` and replay `X-91104`.

## 5. Exact remaining renewal

Let `M_59` be the multiplicative monoid generated by primes at least 59. For the reserve/equality vector `U=(L,R)`,

\[
 \sum_{m\in M_{59},\,m\le x}m^{-1/2}\mathbf U(x/m)
 =\mathbf F^{(53)}(x)\ge0.
\]

Every nontrivial term has scale at most `x/59`, beyond one complete factor-54 reset.

This is the exact all-scale generalization of the finite parity automaton:

```text
all primes <=53:
    globally positive finite Boolean forcing;

all primes >=59:
    one delayed rough-prime renewal.
```

## 6. Hostile-review consequence

The positive forcing identity is not yet a positive inverse. Abstractly,

\[
 F=(I+K)U,
 \qquad K\ge0,
\]

but `(I+K)^(-1)` alternates. Therefore a finite Hall certificate plus a long delay does not automatically produce a cone-preserving reset.

The earlier phrase “only a finite-width collar remains” was too optimistic. The correct remaining theorem is:

> Allocate the positive Boolean forcing between the current exact inner row residual and the delayed contracted `(L,R)` copies, in the actual carry/detail cone, with coefficient-one score transfer and bounded additive debt.

This correction is recorded in `R-91101`.

## 7. Radical continuation

The delayed renewal should be attacked as a **martingale Schur allocation**, not by scalar inversion:

1. work with the positive component rows `Q_Y(n)`, not with the signed scalar `L`;
2. use the complete Boolean forcing as the positive parent reservoir;
3. allocate delayed rough-prime packets by equal divisor destination before taking any positive part;
4. use reverse-TP2 endpoint butterflies as the local martingale basis;
5. require the score Schur complement, rather than the scalar reciprocal-zeta inverse, to be nonnegative.

The desired finite-state object is a source-indexed block kernel

\[
 \begin{pmatrix}
 F^{(53)} & Z^*\\
 Z & C
 \end{pmatrix}\succeq0,
\]

whose Schur complement is the current exact residual and whose lower block consists of delayed endpoints `X/m`, `m in M_59`, with first delay 59. A coefficient-one trace/score identity must accompany the block positivity.

This is narrower than global equality-weight positivity and uses information lost by the scalar renewal.

## 8. Honest frontier

```text
exact nonnegative outer equality rows             PROPOSED COMPLETE
outer ordinary/detail saturation                  EXACT
terminal annulus                                  CLOSED EXACTLY
finite target/continuum mismatch                  REMOVED
quantization collar                               REMOVED
all 65,536 small-prime Boolean states             DIRECTED POSITIVE
rough-prime delayed renewal identity              EXACT
capacity-faithful positive Schur allocation       OPEN / RH-BEARING
bounded coefficient-one score transfer            OPEN / RH-BEARING
Riemann Hypothesis                                UNPROVEN
```
