# Publication recovery: two surviving routes

The earlier response described a two-route packet but neither its branch nor
its fallback ZIP was actually durable.  This recovery reconstructs the work
under the fresh `T-100400` namespace.

The quadratic route is now an exact atom-plus-collar identity.  Its main
benefit is summable `p^-3/2` arithmetic, but the mixed activation boundary is
not automatically positive.

The minimal-wavelet route is now an exact largest-prime bilinear form.  Smooth
histories contribute `X^-1/6+o(1)`; all RH-scale arithmetic is concentrated in
the rough ordered form.

The phase-Hasse audit shows that zero phase is removed but half of the original
Euler root remains at nonzero phase.  The route is therefore not counted as an
independent escape.

Neither `QACG100400` nor `LPMW100410` is proved.  RH remains unproved.
