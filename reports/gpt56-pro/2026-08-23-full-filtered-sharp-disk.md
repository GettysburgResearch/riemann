# Full filtered SHARP disk and centered radial-slack reduction

## Result

The three filtered rays in `T-102750` extend to the entire complex disk

\[
 z=-1+w,
 \qquad |w|\le1/2.
\]

The proof has two independent parts:

1. exact cellwise minimization of the filtered carrier;
2. a fail-closed native prime-removal budget below one, including the second
   labelled `67`.

Consequently the signed dyadic filter transports one complete Hermitian test
family on the literal native source and on every completion tangent.

## Matrix form

The integrated filtered family is

\[
 P(w)=P_0+2B\Re w+A|w|^2,
\]

with

\[
 A=P_2a_\tau,
 \qquad B=G_\tau-A.
\]

Full disk positivity gives one post-filter S-lemma matrix and one reserve.
The remaining Lorentz current is the fixed support functional `4A-B`.

## Carrier correction

The exact common carrier is

\[
 cX\sum_n\Sigma_\tau(n)n^{-3/2},
 \qquad c=2(2-\sqrt2),
\]

not the earlier `(X-1)` display.  The barycentric target is unchanged because
its coefficients sum to zero.

After subtracting this exact carrier, every ray kernel is supported on
`[1,8]`.  Thus the remaining matrix/occupancy problem is a fixed ratio-eight
physical shell.

## Boundary

A disk-positive quadratic can have negative Lorentz functional.  Therefore the
new matrix does not establish RH automatically.

The corrected final scalar is the minimal centered radial cost
`RSC102760`: the one-time carrier subtraction plus the single S-lemma reserve,
transported through exact source/gauge/owner recombination.

```text
full filtered disk                     PROVED
uniform disk prime budget <1           PROVED
post-filter S-lemma matrix              PROVED
exact common carrier                    CORRECTED
centered support [1,8]                  PROVED
RSC102760                               OPEN / RH-BEARING
Riemann Hypothesis                      UNPROVED
```
