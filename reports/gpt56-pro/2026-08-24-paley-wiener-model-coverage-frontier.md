# One-matrix Paley--Wiener coverage frontier

The endpoint-companion programme now has an exact separation between source
analysis and topology.

## Closed rows

```text
two-rung endpoint all-pass telescope;
actual Xi exterior-square Turan source;
finite two-boundary Toeplitz--Hankel adapter;
complete four-channel source cost < 1/600;
superexponential Xi Fourier-tail truncation;
common-factor and confluent finite ledger;
model-space sampling matrix normal form;
coverage-to-complete-Hankel trace inequality.
```

## Single open row

For the reduced endpoint all-pass factorization

```text
U_T = omega_T B_(+,T)/B_(-,T),
```

let `C_T` be the normalized confluent Paley--Wiener sampling matrix on
`K_(B_(-,T))`.  The remaining theorem is

```text
PWSAMP106420:
  tr(I-C_T)_+ = o(N(T,2T)).
```

The matrix entries are explicit normalized exponential/Cauchy kernel values
and jets at the bad companion zeros.  Near collisions are retained through
confluent kernels.

Under this one estimate, the exact source cost and the fixed-order
`R_2/N>599/625-o(1)` input give

```text
N0/N > 95507/100000 = 0.95507.
```

The monomial family `U=z^(-m)` proves that the matrix row cannot be omitted:
denominator-cancelled numerator energy can be zero while the all-pass index is
arbitrarily large.

`PWSAMP106420`, ninety percent, density one and RH remain unproved.
