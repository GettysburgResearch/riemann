# Consolidated parity/factor-five review spine

Date: 2026-08-08  
Branch: `agent/gpt56-pro/262-critical-euler-fiber-bridge`  
Status: **FULL CONDITIONAL PROPOSAL — RH UNPROVED**

## Executive statement

The repository does not yet contain an unconditional proof of RH. The strongest exact pieces can, however, be consolidated into one proof architecture with one declared production theorem.

```text
fixed inverse-zeta source
-> critical Euler filtering
-> positive compact Green potential
-> parity analysis frame
-> positive finite Bezout reconstruction
-> finite synthesis to omega_2
-> exact two-frequency physical block
-> factor-five carry localization
-> strict carry-feature reserve
-> one physical source-image recurrence (SIFD)
-> fixed-ratio shell energy
-> Mellin/Landau pole exclusion
-> RH.
```

Every arrow except `SIFD` is represented by an exact claim or a frozen imported theorem. `SIFD` is open and RH-bearing.

## 1. What is no longer open separately

The following are one source in different coordinates:

- normalized dyadic Mertens shell;
- oversupport half-pole collar jet;
- dyadic Riesz mean;
- opposite-parity Riesz mean;
- bottom carry charge.

`L-26209` gives the exact maps. These objects must not be advertised as independent routes.

Likewise:

- the parity pair and the original inverse-zeta source are related by an exact finite positive Bézout reconstruction;
- `omega_2` is a finite degree-six synthesis of the parity pair;
- every negative `omega_2` Kummer row lies in quotient cells `2,3,4`;
- every complete five-tap odd-core fiber has zero half-pole-value jet.

Thus the broad balanced packet has been reduced to one finite source grammar.

## 2. Exact source and analysis frame

The local fiber is

\[
p(z)=(1-z)(1-2z)(1-\sqrt2z)^2.
\]

The analysis pair is `p(z),p(-z)`. On the critical annulus,

\[
|p(z)|^2+|p(-z)|^2\ge45/4.
\]

The exact synthesis identity is

\[
U(z)p(z)+U(-z)p(-z)=1
\]

with four positive coefficients. This eliminates infinite-filter and boundary-strip ambiguities.

## 3. Exact physical source

The physical block is the independent-frequency normal Gram of PR #241. Every translate cross term remains. A one-frequency vertical integral is not used.

The complete odd-core fibers are formed before quotient partition. Their unrestricted half-pole indefinite charge vanishes exactly. The remaining source collar is the fixed-ratio Mertens firewall and must be routed, not discarded.

## 4. Factor-five transition

The finite synthesis source

\[
\Omega_2(s)=
\frac{(1-2^{-s})(1-2^{-s-1})}{\zeta(s)}
\]

has an exact compact pointwise carry wavelet. PR #269 proves:

```text
negative Kummer row => 2m<=n<5m;
```

and a uniform strict carry Schur reserve on that sector. The actual generalized-prime profile differs from the ordinary logarithmic profile only by a controlled nonnegative dyadic correction.

## 5. Sole theorem for review

`SIFD` must construct the actual source map from the physical transition block to the carry/boundary feature block and prove

\[
\kappa_0m^2E_m+Q_m
\le C(1+m)^A+
\sum_r\theta_r(m-r)^2E_{m-r},
\]

with

\[
Q_m\ge0,
\qquad
\sum_r\theta_r<\kappa_0.
\]

The source map, matrix congruence, reserve, boundary destinations, and constants must all come from one generated arithmetic ledger.

This is not supplied by the present branch. The proposal is therefore conditional.

## 6. Why SIFD would finish RH

The strict recurrence gives polynomial block energy. Finite synthesis and the all-ratio adapter give subexponential dyadic-shell energy. `L-26209` gives a subpower Riesz estimate. Its Mellin transform contains an uncancelled reciprocal-zeta pole at every hypothetical zero to the right of the critical line. Landau continuation excludes such zeros, and functional-equation symmetry proves RH.

## 7. Nearest false versions already excluded

The consolidated proposal does not use:

- the false one-frequency block identification;
- the false bounded endpoint count;
- the false absolute Bohr-rank theorem;
- the false conditional-Hankel representation;
- the refuted nonnegative monotone carry cover;
- a generic operator norm on arbitrary coefficient vectors;
- a finite positive ladder in place of a cofinal theorem.

## 8. Reviewer decision tree

1. Reconstruct the filter, parity frame, and Bézout identity.
2. Verify the finite synthesis to `omega_2`.
3. Verify complete-fiber half-pole nullity.
4. Independently replay PR #269 factor-five localization and reserve.
5. Verify `L-26209`.
6. Inspect the production `SIFD` ledger.

If Step 6 is absent, the verdict is `UNPROVEN`. If its source congruence or strict reserve fails, reject `SIFD` while retaining the preceding exact claims. If it succeeds uniformly, the remaining deduction to RH is short.

## 9. Status table

```text
critical Euler filter                       proposed complete
positive compact Green factor               proposed complete
positive inverse/generalized primes         proposed complete
parity frame                                proposed complete
finite positive Bezout reconstruction       proposed complete
finite synthesis to omega_2                 proposed complete
correct two-frequency block                 imported/reviewed
factor-five carry localization              proposed complete
uniform carry reserve                       proposed complete
shell/Riesz/collar/bottom-charge triangle   proposed complete
SIFD production theorem                     open / RH-bearing
RH                                           unproved
```

## 10. Recommendation

Pass `T-26203` and `M-26202` for review as the canonical consolidated proposal. Do not present it as an unconditional proof until the production `SIFD` object exists and passes the fail-closed protocol.
