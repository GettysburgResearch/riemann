# Research report — hostile corrections and the one-node RH endpoint

Date: 2026-08-12  
Status: **corrected full unconditional proposal; RH remains unproved**

## Executive verdict

The first triune proposal was pushed and then attacked rather than defended.
Two attractive constructive shortcuts failed exactly:

1. the fixed phase-locked `p=2` packet does not regularize Suzuki's hard-range
   integer singularities;
2. the Brownian log-odds Sturm--Liouville cell realizes the two-copy difference
   coordinate, not the one-copy Xi impedance.

The failure analysis produced three replacements:

1. an exact all-prime Jordan deconvolution reducing every integer singularity
   to one archimedean endpoint singularity;
2. one Green primitive which regularizes that endpoint locally for every
   positive scale;
3. a one-interior-node exhaustion criterion which detects the complete
   crossed-zero Blaschke port.

The current proposal is therefore strictly smaller and more honest than the
first version.

## 1. Fixed local filtering fails

Near every integer `N`, Suzuki's kernel has leading term

\[
 h_\omega(x)
 =C_{\omega,N}(x-N)^{\omega-1}+\cdots.
\]

At every odd `N`, the phase-locked filter leaves the leading coefficient

\[
 \kappa_\omega
 =2t^2-15t+15/t+33,
 \qquad t=2^\omega.
\]

For `0<omega<=1/2`,

\[
 \kappa_\omega\ge37-\frac{15}{2}\sqrt2>\frac{103}{4}.
\]

The local `p=2` packet remains zero-safe and stably invertible, but it does not
change the local `L2` threshold.

## 2. The Brownian cell was assigned the wrong coordinate

For paired BPY copies,

\[
 \Delta=\operatorname{artanh}(D/A),
 \qquad
 S=\frac12\log(A^2-D^2)+2C.
\]

The exact local cell realizes `exp(+/-a Delta)` and `tanh(a Delta)`. The full
reflection form depends simultaneously on `S`, `Delta`, and the half-size tilt
`exp(S/2-C)`. Any route-III proof must construct a coupled sum--difference
boundary energy.

## 3. Global all-prime deconvolution

The positive coefficients

\[
 c_\omega(n)=J_{2\omega}(n)n^{-\omega}
\]

have Dirichlet series `zeta(s-omega)/zeta(s+omega)`. Their inverse is
multiplicative with

\[
 d_\omega(p^k)=-(p^{2\omega}-1)p^{-k\omega}.
\]

Writing `H_omega(x)=x h_omega(x)` and `f_omega(y)=g_omega(1/y)`, one obtains

\[
 H_\omega=\mathcal T_{c_\omega}f_\omega,
 \qquad
 f_\omega=\mathcal T_{d_\omega}H_\omega.
\]

Thus the infinitely many integer singularities are generated by one endpoint
singularity through one positive arithmetic convolution. The inverse is signed
and must be implemented as a conservative Fock transfer, not termwise.

## 4. One primitive removes the local obstruction

Set

\[
 A_\omega(x)=\int_1^x h_\omega(y)dy,
 \qquad
 f_\omega(t)=e^{-t/2}A_\omega(e^t)\mathbf1_{t\ge0}.
\]

Every integer singularity becomes `(x-N)^omega`, hence is locally square
integrable for every `omega>0`. Moreover

\[
 \widehat f_\omega(z)
 =\frac{\Theta_\omega(z)}{1/2-iz}.
\]

The remaining obstruction is global norm exhaustion, not local compactness.

## 5. One vector, then one node

The outer-vector theorem gives

\[
 f_\omega\in L^2(0,\infty)
 \iff
 \Theta_\omega\text{ is inner}.
\]

This reduces the full kernel equality to one source-ordered vector norm
identity (`OVOT_omega`).

The exact Krein--Langer ledger reduces it further. At one fixed interior node
`eta>0`,

\[
 \mathcal K_a^{\rm hyp}(\eta,\eta)
 =\frac{1-|B_a(\eta)|^2}
        {2\eta|B_a(\eta)|^2}.
\]

It vanishes at one interior point exactly when the Blaschke zero-port factor is
constant. Thus one-node arithmetic source norm exhaustion (`ONAE_a`) is enough
to prove the zero-free half-plane at scale `a`.

## 6. Exact current theorem

For a fixed node, take `eta=1`. Construct the explicit arithmetic feature
`Phi_(a,1)` from the safe completed Green/Jordan/Fock source and prove

\[
 \boxed{
 \|\Phi_{a,1}\|_{\rm arithmetic}^2
 =\|k_1^{\rm crit}\|^2
  +\|k_1^{\rm stable}\|^2,
 }
\]

with no unaccounted auxiliary state and no signed same-scale remainder.

Once the arithmetic source norm is independently identified with the exact
model source kernel, this equality forces the complete hyperbolic zero port to
vanish.

For one sequence `a_j downarrow0`, the result implies RH.

## 7. Status

```text
p=2 hard-range regularization             false
one-coordinate Brownian DtN shortcut      false
global Jordan deconvolution                exact
one-primitive local regularization         exact
one-node zero-port detector                exact
arithmetic source identification at node   open
one-node arithmetic exhaustion             open / RH-bearing
Riemann Hypothesis                         unproved
```
