# Buffered-selector shell-average ledger

Date: 2026-08-23

Checkpoint base: 53c3a24b23335e7b4e77a703b2c3c224821ed749

## Outcome

The absolute-edge programme now has an exact finite shell format.  Starting
from a complete actual-pole manifest on an outer rectangle, primary
selectors retain target jets only in a smaller fixed core and cancel every
other outer pole.  The resulting carriers have contour-independent charges
throughout the buffer.

Averaging the counterclockwise rectangles over both side parameters yields

\[
q_H=\frac{1}{\Delta_T\Delta_\eta}
\left(
i\int_{\mathscr V}\!\operatorname{sgn}(x)\omega_\eta H\,dA
-\int_{\mathscr H}\!\operatorname{sgn}(y)\omega_T H\,dA
\right).
\]

The signs, triangular weights, and coordinate placement were replayed by
exact polynomial integration.  Tonelli gives an exact mean absolute-edge
cost and, for each prescribed nonnegative pair
\((\lambda_1,\lambda_2)\), one common raw-regular rectangle for the two
weighted carriers.  The selected rectangle may depend on the pair.

## Exact buffer firewall

For \(F=z^3/3-z^2+1\), the core-only charges change when the uncanceled
events at one and two enter.  The complete-buffer selectors
\(1-z/2\) and \((z-1)(z-2)/2\) cancel those events and leave only the core
pole.  Exact rational arithmetic verifies every residue, charge jump,
congruence, and carrier reduction.

## Exact authentication

    PASS_T105113_BUFFERED_SELECTOR_SHELL_AVERAGE
    18 focused tests in normal and optimized Python
    44e0cffca9d4d680555287e8290cfa645d979dc76b064f0ab6914ace88c7d560

No numerical or heavy computation is used.

## Remaining gate

The ledger pays the full area cost of the products \(W_1F/F'\) and
\(W_2F^2/(F'F'')\); it does not factor away selector growth.  Authenticated
cofinal Xi manifests, quantitative weighted shell bounds, the positive
signed first-moment lower bound, multiplicity control, strict coherence,
RCMV104530, and RH remain open.
