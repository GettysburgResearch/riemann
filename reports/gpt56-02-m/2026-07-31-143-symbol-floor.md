# Positive-path report — Fourier-density symbol floor

Agent: `gpt56-02-m`  
Date: 2026-07-31  
Branch: `agent/gpt56-02-m/143-symbol-floor`  
Stacked base: PR #152  
Status: new exact reductions; no proof of RH

## Audit of the current frontier

The newest repository work has rigorously closed several formerly promising
finite neighborhoods:

- PR #103 proves the strongest atomized PR #71 determinant positive at roughly
  `8.16e-104`;
- PR #105 closes more than one hundred thousand directed nearby rows and refutes
  the remaining high-order midpoint negatives;
- PR #116 closes the complete degree-at-most-14 half-line nonnegative response
  cone on the exact sixteen-node table;
- PR #117/#134 reduce one-node degree extensions to scalar Schur gates, whose
  current midpoint nominations remain positive;
- PR #152 identifies the real positive-path blocker as an ambient lower spectral
  floor for the growing localized Weil operator.

The literature reaches the same boundary. Connes--Consani--Moscovici construct
finite self-adjoint prime operators and propose determinant convergence to
`Xi`; Suzuki constructs the localized self-adjoint form and unconditional
real-zero characteristic functions, but the limiting identification remains
conjectural. Groskin supplies an exact finite Guinand--Weil dictionary and
archimedean tail order, but not a cofinal positivity theorem.

## Ambient result

`L-14316` uses only the support interval. For a normalized time-limited vector,

```text
p_w(xi)=|hat(w)(xi)|^2/(2 pi ||w||^2)
```

is a probability density with height at most `L/(2 pi)`. Minimizing the symbol
against the larger class of all densities satisfying this cap gives the
bathtub lower floor

```text
B_L(s)=sup_G [G-L/(2 pi) integral (G-s)_+].
```

On Suzuki's scaled interval, this is the average of the lowest `pi` units of
exact symbol measure. It is an ambient lower bound, not merely a complement
bound.

The weighted deficit operator also gives

```text
# eigenvalues below G-epsilon
<= L/(2 pi epsilon) integral (G-s)_+.
```

Thus the integrated deficit simultaneously supplies a scalar floor and a
finite weighted low-packet rank cap.

`X-14309` uses only integers and `fractions.Fraction`. Its strict synthetic
control permits symbol value `-1` on `[-1/4,1/4]` and tail `+1`, yet certifies
the whole compression positive with floor `227/333`. Eight adversarial tests
pass.

## Packet-leverage strengthening

`L-14317` replaces the uniform support cap by the exact leverage deficit of an
already-certified radical or finite packet `K`:

```text
c_K(xi)=||P_(K-perp) exp(-i xi x)||^2/(2 pi).
```

Every normalized Fourier density in `K-perp` is bounded by `c_K`, giving

```text
B_K(s)=sup_G [G-integral c_K(xi)(G-s(xi))_+ dxi].
```

For an exact basis with Gram matrix `M` and Fourier vector `b(xi)`,

```text
c_K(xi)=(L-b(xi)^* M^(-1)b(xi))/(2 pi).
```

Thus a complement floor can be certified without constructing a complement
basis.

For the constant radical on `[-1,1]`,

```text
c_0(xi)=(1/pi)(1-(sin xi/xi)^2),
```

which vanishes quadratically at the central frequency. The strict synthetic
control uses symbol `-10` on `[-1/2,1/2]` and tail `+1`:

```text
ordinary L-14316 floor        -833/333 < 0
constant-complement L-14317    1415/1998 > 0.
```

`X-14310` verifies the second result exactly and has eight adversarial tests.
This shows that exact radical removal can turn a failed ambient relaxation into
a positive whole-complement certificate before any prolate block is built.

## Why these results improve PR #152

`L-14311` turns a binary low-symbol set into a generalized-prolate packet and
then requires a finite Schur calculation. `L-14316` retains the depth of every
cell through the integrated deficit; `L-14317` further retains the exact
frequency leverage removed by the known packet.

The new gate sequence is:

```text
ambient integrated-deficit floor
-> constant-radical leverage floor
-> finite weighted/prolate packet leverage floor
-> finite Schur correction only on the unresolved packet.
```

## Global theorem

`T-14305` states that if an unbounded support sequence has exact ambient or
whole-space-composed floors

```text
mu_(a_j) >= F_j,
liminf F_j >= 0,
```

then `T-14302` and support monotonicity imply RH.

This is not yet a proof. The missing statement is a cofinal arithmetic estimate
for the complete Suzuki symbol and, where needed, its exact packet leverage.

## Latest literature signal

The July 2026 numerical realization of Suzuki's operator reports positive
finite spectra and finds that nontrivial-zero information enters through the
prime-symbol error term rather than through the universal archimedean ladder.
The paper explicitly disclaims an RH proof. This supports measuring the complete
symbol's low-volume tail and leverage-weighted deficit, but supplies no directed
cofinal estimate.

## Proposed computation and analytic attack

1. Audit the exact symbol signs directly from Suzuki (4.5)--(4.6).
2. Produce directed cell lower bounds for several increasing supports.
3. Prove a logarithmic tail threshold.
4. Run `X-14309` before constructing any packet.
5. Remove the exact radical and run `X-14310`.
6. If needed, compute leverage bounds for the finite low packet of `L-14311`.
7. Apply `L-14308` only to the unresolved packet.
8. Search for a scalable inequality for the leverage-weighted prime-symbol
   deficit.

Mean-square bounds on individual prime terms are not enough unless the
near-frequency correlations are controlled at the support-dependent scale.

## Claim-allocation correction

PR #152 advanced after this branch point and allocated `L-14312`, `T-14303`, and
`X-14307`. This contribution was reallocated to `L-14316`, `L-14317`,
`T-14305`, `X-14309`, and `X-14310` before review.

## Current verdict

No unconditional proof of RH has been obtained. The contribution is a new
ambient floor and a sharper radical/packet-complement floor. The global blocker
is now a scalar leverage-weighted lower-tail estimate for the exact arithmetic
symbol, plus a finite residual packet only where that estimate fails.
