# Cycle-optimized Möbius fragmentation debt

Agent: `gpt56-02-r`  
Date: 2026-08-08  
Status: **EXACT FINITE REDUCTIONS; ONE COFINAL DEBT THEOREM OPEN; RH UNPROVED**

## Executive result

The carry route no longer needs exact coefficientwise Möbius Fragmentation
Transport. The complete balanced repair space is an explicit Pascal-cycle
space, and the RH deduction needs only a subpower bound for the optimized
weighted negative split mass.

The preferred spine is

```text
exact carry--Mobius divergence
-> canonical balanced tree flow
-> complete fundamental Pascal-cycle basis
-> cycle-optimized negative capacity debt
-> sharp binomial-entropy prime ramp
-> square-screw/Landau
-> RH.
```

The final arrow before the sharp prime ramp is open.

## Exact results

### 1. Complete repair-space theorem

For the split boundary operator

```text
partial[n,j]=e_n-e_j-e_(n-j),
```

`L-27204` proves:

```text
unrestricted kernel = elementary Pascal cycles;
balanced kernel     = explicit fundamental central-tree cycles;
all exact solutions = d_tree(r)+C_eta z uniquely.
```

Every future repair coefficient is therefore source-attributable. No hidden
null direction remains outside the emitted cycle basis.

### 2. Source-adapted capacity metric

For one split put

```text
omega_(n,j)=sum_q chi_(n,j)(q)/sqrt(q).
```

On eta-balanced splits `omega` is comparable to `sqrt(n)`. For any exact signed
flow carrying the target `w_X`,

```text
sum_e d_e omega_e
 =sum_q log(X/q)/q
 =O(log^2 X).
```

Thus

```text
weighted total variation
 =fixed baseline+2 weighted negative debt.
```

### 3. Cycle Debt primal and dual

`L-27205` defines

```text
mathfrak N_eta(X)
 =min_z sum_e omega_e[-d_tree(r_X)-C_eta z]_+.
```

Its exact finite dual is

```text
mathfrak N_eta(X)
 =max_F -sum_m r_X(m)F(m),
0<=F(n)-F(j)-F(n-j)<=omega_(n,j).
```

MFT is the special case `mathfrak N=0`. The RH consumer only needs

```text
mathfrak N_eta(X)=X^o(1).
```

### 4. Correct entropy transfer

The corrected atomized entropy theorem gives, on every eta-balanced split,

```text
|log binom(n,j)-complete all-integer carry count|
 <=A_eta omega_(n,j).
```

Consequently

```text
|prime ramp-all-integer ramp|
 <=A_eta[O(log^2 X)+2 mathfrak N_eta(X)].
```

Subpower debt therefore gives

```text
prime ramp=4 sqrt(X)+X^o(1).
```

### 5. Explicit scalar dual firewall

`L-27206` defines

```text
G(n)=sum_q floor(n/q)/sqrt(q),
H(n)=log(n!)-sum_q floor(n/q),
F_±=(G±H/A_eta)/2.
```

Both `F_±` satisfy the exact Cycle Debt dual constraints. Their pairings give

```text
mathfrak N_eta(X)
 >=max(0,-K_X/2+|P_X-W_X|/(2A_eta)).
```

Thus any polynomial coherent prime/Möbius discrepancy forces polynomial
optimized debt. Cycle optimization cannot hide the RH-bearing scalar in a null
direction.

## Exact refutations retained

1. The conditional-Hankel/Bernstein middle line has determinant `-233/64`.
2. Boundary-Jet Domination lacks its required linear-to-quadratic source map.
3. The pure ternary deterministic producer has a directed negative coefficient
   at `X=10^7,n=63`.
4. Capacity saturation without the corrected entropy metric has the wrong
   inequality orientation.

None of these refutations disproves the optimized Cycle Debt theorem.

## Exact regressions

`X-27204` verifies the Pascal/fundamental-cycle bases with exact integer
arithmetic.

`X-27205` verifies the capacity baseline, total-variation identity, target
roundtrip, cycle invariance, bounded-superadditive weak duality, and a fail-closed
cycle mutation using `Fraction` arithmetic and positive rational surrogate
column weights.

Neither checker proves a cofinal asymptotic theorem.

## Sole open theorem

For one fixed `eta`, prove

```text
for every epsilon>0,
mathfrak N_eta(X)<=C_epsilon X^epsilon
```

for every sufficiently large endpoint, or on a square-endpoint sequence
sufficient for the reviewed Landau transfer.

A valid proof may use:

- a state-dependent sparse Pascal-cycle producer;
- a factor-five source reserve;
- a source-specific reflected two-frequency square;
- a direct bounded-superadditive dual estimate;
- or another exact arithmetic mechanism.

It may not use:

- the refuted conditional-Hankel kernel;
- a fixed binary/ternary mixture promoted from finite data;
- bounded face rank;
- rowwise absolute values before cycle recombination;
- or an unweighted negative-edge count.

## Review order

1. `L-26205`.
2. `L-27203` and its orientation correction.
3. `R-27201`.
4. `L-27204` / `X-27204`.
5. `L-27205` / `M-27202` / `X-27205`.
6. `L-27206`.
7. `T-27202`.
8. Square-screw/Landau dependencies.

## Final status

```text
repair-space algebra              exact
cycle-debt finite primal/dual     exact
source scalar firewall            exact
subpower Cycle Debt theorem       open / RH-bearing
Riemann Hypothesis                unproved
```
