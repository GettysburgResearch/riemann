# Parabolic carry full attack — 2026-08-07

Status: `PROPOSED / ONE ELEMENTARY GLOBAL HINGE REMAINS`  
Issue: #245  
PR: #248  
Frozen base: `d1f72553c73037a3990f8c505d0c260d41a223f4`

## Executive conclusion

The elementary carry route has been reorganized around a new exact convexification.

The previous exact carry inverse asked for coefficientwise positivity and exposed reciprocal-zeta/Mertens behavior directly. The new formulation instead applies two finite differences to both the carry matrix and the average-binomial entropy. This yields a finite prime-power divisor-gradient LP whose dual is exactly the von Mangoldt prime ramp.

An explicit non-arithmetic parabolic seed already carries the full critical objective

```text
4 sqrt(X) - O(log X).
```

Thus RH reduces to repairing only the seed's positive prime-power constraint excess at `O(log^2 X)` objective cost while preserving nonnegativity.

Two candidate closures survive:

1. a monotone divisibility-cover theorem;
2. a tapered divisor-comb primitive-neighbor contraction.

The second route has an exact favorable gcd ledger: noncoprime interactions are automatically nonpositive, and the only harmful cross-interactions satisfy the primitive determinant equations

```text
ad-bq=+-1.
```

A further continuation derives an explicit continuum excess profile and reduces the outer feasibility theorem to finitely many reciprocal cells. Floating reconnaissance indicates the conservative rational cutoff

```text
q >= X/28
```

is already feasible for the uncorrected seed, so the correction need only originate in the fixed low-ratio region.

RH is not proved.

## Durable claim stack

- `L-24501`: exact carry/binomial second differences and the prime-power divisor-gradient LP;
- `L-24502`: explicit parabolic seed, exact flow cost, and monotone cover calculus;
- `L-24503`: tapered divisor-comb matrix, favorable gcd ledger, primitive-neighbor reduction;
- `L-24504`: continuum seed-excess profile;
- `L-24505`: reciprocal-cell closed form and one-critical-point calculus;
- `T-24501`: complete conditional RH chain from PNC;
- `O-24501`: floating LP reconnaissance for the stronger monotone cover.

## Strongest exact identities

For

```text
F_n=(n+1)G_n
```

one has

```text
Delta^2 F_n=(n-1) log(n/(n-1)).
```

For

```text
g_q(n)=(n+1) beta_(nq)
```

one has

```text
Delta^2 g_q(n)
=(n-1)(1_(q|n)-1_(q|n-1)).
```

After double summation by parts the optimization variable is `b_m>=0`, with

```text
v_q(b)=sum_(kq<=X)(b_(kq)-b_(kq+1)),
J_X(b)=sum_m b_m log(m/(m-1)).
```

For prime powers,

```text
J_X(b)=sum_q Lambda(q) v_q(b).
```

Thus every feasible `b` gives a lower bound for the exact prime ramp, and the LP dual `y_q=Lambda(q)` is exact.

## Explicit critical seed

The seed

```text
b0(m)=2 sqrt(m)[log(X/m)-2(1-sqrt(m/X))]
```

is nonnegative and satisfies

```text
J_X(b0) >= 4 sqrt(X)-6 log X+4-8/sqrt(X).
```

No prime input enters its construction.

## Exact correction metrics

For an adjacent flow `F`,

```text
b_F(m)=b0(m)+F_(m-1)-F_m,
```

and

```text
Delta v_q
=sum_j F_j[1_(q|j+1)-2 1_(q|j)+1_(q|j-1)].
```

The exact objective loss is

```text
sum_j F_j log(j^2/(j^2-1)),
```

whose weight is `j^-2+O(j^-4)`.

For a monotone tail correction generated by `alpha_m>=0`,

```text
Delta v_q=-sum_(kq<=X) alpha_(kq),
objective loss=sum_m alpha_m log m.
```

This gives the stronger Divisibility Cover target.

## Primitive-neighbor ledger

For the tapered divisor comb, the matrix entry from source `d` to constraint `q` is `C(q,d)`.

If `(q,d)>1`, then

```text
C(q,d)=-2 sum_(lcm(q,d)|j) phi(j) <= 0.
```

If `(q,d)=1`, then

```text
C(q,d)
=sum_(ad-bq=+-1) phi(ad)
-2 sum_(qd|j) phi(j).
```

Hence all omitted noncoprime residue chains of the rejected Farey proof are favorable here. The only potentially harmful edges are primitive determinant-one neighbors.

## Continuum frontier

Write

```text
b0(m)=sqrt(X) B(m/X),
B(t)=2 sqrt(t)[log(1/t)-2(1-sqrt(t))].
```

For `q/X -> theta`,

```text
sqrt(X)[v_q(b0)-w_X(q)] -> E(theta),
```

where

```text
E(theta)=
-sum_(1<=k<=1/theta)
 [4 sqrt(k theta)-log(k theta)-4]/sqrt(k theta)
-theta^-1/2 log(1/theta).
```

On the reciprocal cell

```text
1/(N+1) < theta <= 1/N,
```

put

```text
H_N=sum_(k<=N) k^-1/2,
A_N=sum_(k<=N) k^-1/2 log k.
```

Then exactly

```text
E(theta)
=theta^-1/2[A_N+(H_N+1)log theta+4H_N]-4N.
```

Each cell has at most one interior critical point. Therefore the proposed outer sign theorem on `theta>=1/28` reduces to finitely many elementary endpoint/critical-point inequalities.

Floating reconnaissance places the final sign transition inside `(1/29,1/28)` near `0.034852033...`. This decimal is not a certificate.

## Stronger monotone-cover reconnaissance

A floating LP with the full tail-capacity constraints was feasible at every tested scale through `X=1500`. Approximate minimum objective losses were

```text
X=100      0.2621
X=200      0.9062
X=500      2.5954
X=800      3.9606
X=1000     4.7683
X=1500     6.5142
```

These are far below `(log X)^2` at the tested scales. This is scheduling evidence only.

A naive estimate by summing every raw positive seed excess separately appears much larger and should not be promoted. The numerical LP succeeds by placing mass on shared multiples, so any proof of the cover theorem must exploit divisor sharing or bounded-overlap packing rather than a rowwise sum.

## Current full-proposal hinge

The cleanest all-scale theorem is now one of the following.

### A. Divisibility Cover

Construct `alpha_m>=0` satisfying

```text
sum_(kq<=X) alpha_(kq) >= e_X(q)
```

for every prime power, the exact seed tail-capacity constraint, and

```text
sum_m alpha_m log m = O(log^2 X).
```

This immediately closes the elementary route and bypasses Jacobi iteration.

### B. Two-stage Primitive-Neighbor Contraction

1. certify the finite reciprocal-cell sign theorem and a uniform remainder, giving seed feasibility for `q>=X/28`;
2. repair only `q<X/28`;
3. use high-order mean-zero divisor-comb cancellation so low-ratio corrections do not reactivate the outer region;
4. control the remaining balanced primitive-neighbor graph and the exact `j^-2` transport cost;
5. preserve `b>=0`.

Either A or B gives

```text
S_X >= 4 sqrt(X)-O(log^2 X),
```

and the repository's existing square-screw/Landau transfer yields RH.

## Mandatory firewall

The exact carry inverse retains the smoothed-Mertens coordinate

```text
b_(X,2)=-sum_(2<=n<=X) mu(n)/sqrt(n) log(X/n).
```

Any closure that takes absolute values before preserving this coherent channel is suspect. Reviewers should demand that the proposed transport/cover theorem be checked against this scalar projection.

## Falsification tests

Reject the proposal if any of the following occurs:

- an error in the double-summation boundary terms;
- a prime-power LP constraint omitted by the Legendre identity;
- a noncoprime divisor-comb edge with positive sign;
- failure of the `1/28` outer sign after proof-grade enclosure;
- a low-ratio correction necessarily reactivates an outer row at polynomial cost;
- a cover necessarily costs a fixed positive power of `X`;
- preservation of `b>=0` fails cofinally;
- the square-screw transfer requires more than the displayed prime-ramp lower bound.

## Final status

This continuation substantially sharpens the elementary route but does not prove the final global correction theorem. PR #248 is intended for adversarial review of the new exact reduction and, especially, for attacks on Divisibility Cover / two-stage PNC.
