# Report — committed 320-bit ladder and the cofinal production fork

Agent: `gpt56-03-r`  
Experiment: `X-20702`  
Parent: PR #208 / Issue #207

## Committed finite result

The complete fixed-point result bundle contains:

```text
verified_height_320.py
configs/verified-height-320.json
artifacts/certificate-p320-manifest.json
artifacts/verification-p320.json
artifacts/SHA256SUMS-verified-height-320
```

The exact replay certifies 17 finite supports and rejects six adjacent controls
without interpreting a failed lower bound as a negative Weil witness.

The finest displayed positive control is

\[
c=15390,\qquad
\lambda_{\min}>1.4677756009791674054\cdot10^{-12}.
\]

## Why this is not the cofinal route

For a fixed finite critical-line block, every evaluation row has size
\(O((\gamma\sqrt{\log c})^{-1})\), so its Gram is \(O(1/\log c)\).
The verified-height unseen-zero radius is
\(\asymp\sqrt c/\log c\). Their ratio tends to zero exponentially in
\(\log c\).

The cofinal computation must therefore abandon the fixed-height absolute tail
and assemble the complete prime-power, polar, and archimedean matrix jointly
with the positive-sector Schur correction.

## Production target

For growing packets and directed max-volume frames, emit

\[
\underline\sigma_{N,j}^2,\qquad
\overline\nu_{N,j},\qquad
\Lambda_{N,j},\qquad
\delta_{N,j},
\]

where \(\overline\nu\) is obtained from the complete prime-side joint corrected
residual, not an absolute unseen-zero sum. The exact target is

\[
\Lambda_{N,j}
(\overline\nu_{N,j}-\underline\sigma_{N,j}^2)_+
+\delta_{N,j}\to0.
\]
