# Global prime-transport attack

Agent: `gpt56-03-u`  
Date: 2026-08-07  
Repository: `gfreund123/riemann`  
Branch: `agent/gpt56-03-r/207-directed-d0001-frame`  
PR: #208  
Classification: new exact full-RH criterion and transport induction; RH not proved

## Executive result

The project had reached a point where many active branches were proving exact
finite algebra around an increasingly small final sign.  I stepped back and
compared the live carrier, screw, pole-free-window, Hilbert-strip, theta/Volterra,
and coefficient-total-positivity routes.

The resulting attack abandons the finite-matrix bottleneck chain and works
straight on Suzuki's explicit prime-power screw function.

For the prime-power prefixes

\[
 P_j=\sum_{q\le q_j}{\Lambda(q)\over\sqrt q},
 \qquad
 Q_j=\sum_{q\le q_j}{\Lambda(q)\log q\over\sqrt q},
\]

and the fixed smooth archimedean term `A`, define

\[
 M_j=Q_j-A_+^*(P_j).
\]

The new theorem `T-20802` proves the exact global identity

\[
 \inf_{t\ge0}\Psi(t)
 =\min\left\{
  m_{\rm init},
  \inf_j M_j
 \right\}.
\]

Thus, after one fixed compact gate,

\[
 \boxed{
 RH\iff M_j\ge0\text{ for every prime-power prefix }j.
 }
\]

A negative `M_j` is not a nomination or a conditional matrix sign.  It gives an
actual negative value of the zeta screw function and therefore a finite RH
disproof witness, subject to the already named Suzuki-normalization review.

## Why the reduction is exact

For each prefix, extend its deposited affine prime term to the whole positive
half-line:

\[
 F_j(t)=A(t)-P_jt+Q_j.
\]

At every `t`, the physically active prefix equals `Psi(t)`.  Every inactive
prefix lies above it: omitted past terms and prematurely included future terms
both add nonnegative hinge distances.  Therefore

\[
 F_j(t)\ge\Psi(t)
\]

for every `j,t`, while equality is attained by the active prefix.  Minimizing
first in `t` and then in `j` gives the exact global identity.

This removes the entire continuum of support points, not merely a mesh error.

## Canonical induction

`L-20808` defines the minimizer map

\[
 \chi(p)=\arg\min_{t\ge\log2}\{A(t)-pt\}
\]

and the curvature barycenter of one mass slice

\[
 \bar\tau_j={1\over w_j}
 \int_{P_{j-1}}^{P_j}\chi(p)dp.
\]

The safety reserve evolves exactly by

\[
 \boxed{
 M_j-M_{j-1}=w_j(\tau_j-\bar\tau_j).
 }
\]

Equivalently, the entire RH sign is one one-dimensional monotone transport
comparison between:

- prime atoms of mass `w_j` at `tau_j=log q_j`; and
- the explicit smooth curvature measure generated by `A''`.

On every physical prime-power cell,

\[
 \Psi(t)=M_j+\mathfrak D_A(t;P_j),
\]

where the Fenchel/Bregman gap is nonnegative.  The corrected energy is exactly
piecewise constant.  There is no residual derivative or cell-interpolation
term left to bound.

## Elementary entropy form

`L-20809` removes the implicit conjugate.  With

\[
 B={1\over2}(\psi(1/4)-\log\pi),
 \qquad C=\pi^2+8G,
\]

one has

\[
 A_+^*(P)
 =2(P-B)\left[
 \log\left({P-B\over2}\right)-1
 \right]+8-{C\over4}+\Delta(P),
\]

with an explicit positive enclosure

\[
 0\le\Delta(P)\le O(P^{-5}).
\]

So the full arithmetic target is the one-sided prime-power entropy inequality

\[
 Q_j\ge
 2(P_j-B)\left[
 \log\left({P_j-B\over2}\right)-1
 \right]+8-{C\over4}+\Delta(P_j).
\]

The pole-sized main terms cancel exactly.  The remaining margin is of constant
scale.  This explains both why ordinary PNT errors do not finish the proof and
why separate estimates of prime, pole, and gamma channels are structurally
wrong.

## First finite reconnaissance

`X-20806` enumerated every prime power through `10^7`, exactly `665,134` rows.
Every ordinary numerical prefix margin was positive.

The record low occurred at the prefix ending in `q=3089`, with the next knot
`3109`.  An 80-decimal-place replay gives

\[
 t_*=8.039063759496271722468699830727102450367521620849410016031469\ldots
\]

and

\[
 M_*=0.027520573353620804820414575069133782697494255916352542166304\ldots
\]

The minimizer lies strictly between `log 3089` and `log 3109`.  This reproduces
the independent prime-knot minimum found on PR #98, despite using a different
global optimization architecture.

The result is empirical, not directed and not cofinal.

## What I deliberately did not do

I did not:

- add another finite carrier dimension;
- certify a larger fixed zero height;
- split the source Schur residual into more blocks;
- claim a long finite scan proves eventual positivity;
- reuse a phase-blind PNT remainder as if it reached the constant-scale margin;
- promote a new equivalent criterion as a proof.

The new work is intended to identify one global arithmetic theorem worth
attacking, not to rename the obstruction again.

## Full attack plan

The preferred completion is a cofinal block-transport theorem.  For consecutive
blocks emit exact lower bounds for

\[
 \Delta_{a:b}
 =\sum_{r=a}^bw_r\tau_r
 -\int_{P_{a-1}}^{P_b}\chi(p)dp
\]

and the maximum internal drawdown.  A proof that the incoming reserve always
covers the drawdown closes every prefix and proves RH.

Two deeper alternatives remain serious:

1. derive an exact positive Selberg-type convolution identity for
   `Q_j-A_+^*(P_j)` after all pole/gamma terms are jointly centered;
2. connect the transport reserve to the positive anti-causal Poisson energy of
   `L-19810/L-19811`, converting arithmetic transport positivity into the
   missing minimum-phase theorem.

## Relation to current literature

The route starts from Masatoshi Suzuki's located screw-function paper
`arXiv:2206.03682v4`.  His canonical-system paper `arXiv:1204.1827` identifies
extension of the completed-zeta canonical system to every positive strip offset
as an RH criterion; the transport reserve is a direct prime-side real-variable
target for the same minimum-phase obstruction.

Wojciech Michałowski's located preprint `arXiv:2607.16795` proves a uniform
cubic tail wedge for consecutive Toeplitz minors of the xi coefficients, while
explicitly leaving the RH-critical complementary region open.  Its certified
saddle and nonlinear-majorant techniques may be useful for a cofinal transport
block, but its proved tail region does not close the present sign.

Marvin B. Freedman's located `arXiv:2606.29555` develops the theta/Volterra
positivity route without claiming RH.  PR #202 has isolated its surviving sign
as theta Dirichlet-to-Neumann accretivity.  The new scalar reserve gives a simple
global quantity against which any proposed accretivity factorization can be
tested.

## SERIOUS RESOLUTION PATH

Yes.  The exact remaining statement is

\[
 \boxed{
 \exists J_0\ \forall j\ge J_0:
 \quad Q_j-A_+^*(P_j)\ge0.
 }
\]

Together with a finite directed prefix and the fixed initial interval, this
proves RH.

This path is serious because:

- it is exactly equivalent to the full pointwise screw criterion;
- every level is a finite prime-power statement;
- the real continuum has been eliminated exactly;
- one negative level is already a counterexample;
- the induction law is explicit and one-dimensional;
- the missing theorem is genuinely arithmetic and cannot be confused with a
  finite numerical gate.

The missing cofinal transport/entropy inequality has not been proved in this
pass.  RH is not claimed solved.
