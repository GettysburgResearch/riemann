# Final affine audit: the bounded-ordinate block is the off-line cardinal defect

Agent: `gpt56-pro-09-s`  
Date: 2026-08-07  
PR: #202  
Launch head: `d8511ae3105c4b20524732455e4e55009091520c`  
Classification: **AFFINE HYBRID PROPOSAL GAPS/BLOCKED; CENTRAL PROFILE GATE IS RH-BEARING**

## 1. Objective

The pass attempted to finish the sole theorem left open in `T-19813`:

```text
regularized line-centered Riemann--von Mangoldt lower estimate
+
actual-minus-line support average
+
bounded/central zero enclosure
-> affine shifted lower coercivity
-> Xi target is the finite ground line.
```

The high-ordinate Bessel, stationary-alias, Airy-fold, endpoint, and support-large-sieve ledgers were not the decisive issue. The obstruction is the bounded/central zero block.

## 2. Exact result

`R-19846` proves that if one off-line centered zero exists, then the even Xi-cardinal quartet gives a fixed negative Hardy-strip vector. Quadratic-log finite Fourier projections preserve its negative Weil value. Thus every cofinal complete finite hierarchy contains unit even vectors `h_j` with

\[
 Q_W(h_j,h_j)\le-\kappa<0.
\]

At the same time, the exact Xi target converges to a global arithmetic radical and has

\[
 Q_W(p_j,p_j)\to0.
\]

Any affine gate

\[
 A_j-\sigma_jI\succeq c_jD_j,
 \qquad D_j\succeq0,
\]

forces `sigma_j<=-kappa`, while a vanishing target upper endpoint forces
`sigma_j->0`. The two conclusions contradict one another.

Therefore the central-block estimate demanded by `L-19865/T-19813` already excludes every off-line zero. It is not a routine finite enclosure left after the high-frequency analysis.

## 3. Why support averaging cannot repair it

For a fixed hypothetical zero, its ordinate is eventually below every moving high-frequency cutoff. Common support translation cancels exactly from the polarized Weil kernel. The actual-minus-line discrepancy at that zero is phase neutral at the support scale and is not covered by the derivative-separated large sieve.

Moving the cutoff does not help: every fixed off-line zero eventually enters the central block. Treating that block by an unspecified exact matrix does not prove the sign needed by the affine theorem.

## 4. Excited-eigenline escape audit

A possible escape would be to isolate the Xi target as an interior eigenline rather than the ground line, allowing off-line cardinal eigenvalues below it. This would require the finite real-zero theorem for an arbitrary isolated simple even eigenvector.

The currently imported CCM interface in `T-14301` is the simple-even **lowest-eigenvalue** interface. The branch audit

```text
reports/gpt56-pro-09-s/2026-08-07-ccm-theorem-5-10-scope-audit.md
```

did not identify a verified theorem that can replace the ground-state hypothesis by an arbitrary interior eigenline. No such strengthening is used in this report.

Without a new real-zero theorem for interior eigenstates, a singular-value or squared-operator isolation of the Xi line does not close the Hurwitz step.

## 5. Cross-route audit

The other current global proposals do not independently remove this obstruction.

- Brownian/canonical-product `SAT` is exactly equivalent to vanishing of the positive off-line variance defect.
- Terminal Selberg--Hankel `BTP(K)` remains an open signed balanced Type-II theorem.
- The first Farey/Mertens cell is already RH-equivalent.
- The theta/Volterra contraction still lacks its theta-specific positive completion; generic Green orthogonality is insufficient.

These routes may still contain useful mathematics, but none supplies an already-proved opposite-sign identity for the central Xi-cardinal block.

## 6. Correct status of the affine stack

Durable:

```text
L-19861 affine one-sided spectral transfer;
L-19862 exact Xi plus exterior-cardinal common reservoir;
L-19863 corrected k>R endpoint treatment;
L-19864 separate geometric fold ledger;
L-19865 abstract regularized affine adapter;
X-19841 exact synthetic affine algebra.
```

Blocked:

```text
source-specific complete affine lower estimate;
bounded/central off-line block;
T-19813 as a completed RH proof.
```

The high-frequency profile mechanisms may prove estimates after the central defect has been excluded, but they do not exclude it.

## 7. Smallest exact remaining theorem

A genuine completion of the affine route requires an unconditional strip-sensitive theorem implying one of the following equivalent consequences:

1. every even off-line Xi-cardinal quartet is absent;
2. the central actual-minus-line block has no negative direction compatible with the complete Fourier hierarchy;
3. the affine gate of `R-19846` holds cofinally with vanishing target excess;
4. RH.

A proof must use new arithmetic, stochastic, canonical-system, or total-positivity input. It cannot be obtained merely by improving the current high-frequency constants.

## 8. Final status

```text
T-19813 affine hybrid composition       VERIFIED CONDITIONAL DEDUCTION
R-19846 central-cardinal obstruction    PROVED
source-specific affine profile theorem  RH-BEARING / OPEN
accepted proof of RH                     NO
```

This report deliberately does not promote the unresolved central sign to a proved lemma.
