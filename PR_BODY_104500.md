## Purpose

Turn the Xi reverse-Rolle programme on PR #714 into exact mathematics.

This branch replaces the false unit-cost extra-extremum count by a sharp
factor-two conservation law, adds the exact complex boundary-winding ledger,
identifies the derivative ladder as one Riccati/Pick recursion, and proves a
new zero-theoretic consequence of the Ki/Gunns–Hughes cosine limit.

**RH remains unproved.**

## Exact results

```text
real adjacent-critical-value count                PROVED EXACT
N_nr(p)-N_nr(p')=2 wrong_extrema(p)               PROVED EXACT
complex off-real/winding transport                 PROVED EXACT
h_(k+1)=h_k-h_k'/h_k                              PROVED EXACT
wrong extrema = negative Pick diagonals            PROVED EXACT
fixed-scaled-box high Xi derivatives real-rooted   PROVED
fixed finite-depth scaled cascade                  PROVED
```

## Corrected RH gate

On one common original-height rectangle,

```text
high derivative off-real count = 0
AND
cumulative wrong-extremum + endpoint + winding charge < 2
  -> Xi off-real count is an even integer below 2
  -> no off-real zero.
```

The two remaining theorems are:

```text
GBOX104500  quantitative cosine control on expanding scaled boxes;
RPCH104500  cumulative Riccati-Pick/winding charge below two.
```

Neither is claimed here.

## Replay

```text
PASS_T104500_XI_RICCATI_ROLLE_EXACT_ALGEBRA
f855b1b266df57938d94694c624449bb895f853b7931dc8c45bff5235d533c27
```

The replay verifies exact rational polynomial/interval/Riccati fixtures only.
It does not evaluate Xi or prove RH.
