## Recovered checkpoint D — zero-motion tangent (T-105400)

For `F_delta=F'+delta F`, a simple critical zero moves with velocity
`-F/F''`. The low-order Pick matrix is therefore the first variation of an
ordinary zero statistic. In the Dirichlet-convolution algebra,

```text
R_L=(L delta_1-Lambda)^(-*)
C_J=(Lambda log)*R_L
partial_L R_L=-R_L*R_L
partial_L C_J=-C_J*R_L.
```

Thus the frozen tangent is the literal source `e_1=-C_J*R_L`. A one-prime
low-rank projection is false: one frequency produces a full-rank modulated
Gaussian Toeplitz matrix.
