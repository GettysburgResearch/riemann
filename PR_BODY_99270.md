## Purpose

Hostilely audit the three most vulnerable points of PR #647 and repair or
bypass each one.

```text
base PR:      #647
base SHA:     306d1c66fe4e848efda0bd78b5475aeeed3d5563
head branch:  review/gpt56-pro/99270-three-vulnerability-hardening
```

**RH remains unproved.**

## Vulnerability 1 — pointwise tail is stronger than necessary

For the scalar factor-67 defect `h`, this packet proves two weaker
conclusion-complete criteria:

```text
integral_[X/A,X] h(t) dt/t >= 0 eventually, for one A>1;

integral_1^X h_-(t) dt/t = O_epsilon(X^epsilon) for every epsilon>0.
```

The logarithmic box has multiplier `(1-A^-s)/s`, which is zero-free in
`Re(s)>0`. Either criterion implies RH by the reconstructed scalar Landau
consumer.

## New source mechanism

With `g(n)=v_67(n)+1`,

```text
beta * g = epsilon,
T(x)=sum_(d<=x) g(d)d^(-1/2)h(x/d).
```

At a negative point this gives an exact positive-overshoot debt identity. The
logarithmic derivative of `G=zeta/(1-67^-z)` supplies positive prime-power
owner probabilities. The new frontier is a source-owned Carleson estimate for
subpower negative mass.

## Vulnerability 2 — unnecessary composition dependencies

The scalar transform is derived directly from `beta` and `T`. The SHARP row
kernel, Hall, Radon-Nikodym child map, random-key tree, PR #648 calibration
coboundary, Volterra frame, score/capacity, and prime-square chains are removed
from the normative conclusion graph and retained only as cross-checks.

## Vulnerability 3 — analytic and finite authentication

- complete proof of the specialized Landau theorem;
- explicit positive-real zeta sign and `s=1/2` cancellation audit;
- exact off-line pole-multiplicity audit;
- second full `10^8` implementation using a global Möbius sieve and independent
  integer square root;
- 100 exact checkpoints and exact agreement with T-99250's minimum and final
  prefix intervals.

```text
PASS_T99270_THREE_VULNERABILITY_HARDENING
2c73659aecec49a110a9dd9ce060d1b9699add76a114e362781b8890aebde0d5
```

Independent producer:

```text
source SHA-256:
13dcc46e3a9fc2f81b09339085d3b5a08cc58f84bbcbdcc917b62abee7e20049

compressed transcript SHA-256:
2ea18a00327ac9ed62e3630f49b5e637b8265f1e89fb50e10f29ff3299edd211

raw transcript SHA-256 after deterministic decompression:
82fdeeeceb25ead369574c927143f14065df5c3d202a973cc608cfd2d93f6e24
```

Byte-identical full replay proof object:

```text
4de591d37e6f10aba5bd24d9102adf14ed3d21810a9ba6e1544c7b1ccfc9b0e8
```

## Exact boundary

```text
zero-free smoothing theorem             proved exact
subpower-negative-mass theorem          proved conditional
positive inverse renewal                proved exact
logarithmic owner                       proved exact
specialized Landau consumer             proved
independent h>0 through 10^8             proved computer-assisted
smoothed global sign                    open / RH-bearing
subpower negative mass                  open / RH-bearing
Riemann Hypothesis                      unproved
```
