# T-105113 buffered-selector shell averaging

This checkpoint freezes a smaller target core inside one completely
authenticated outer actual-pole manifest.  The corresponding primary
selectors cancel every nontarget outer pole, so the weighted carriers

\[
H_1=W_1F/F',\qquad H_2=W_2F^2/(F'F'')
\]

have fixed contour charges on every raw-regular intermediate rectangle:

\[
q_1=-2\pi iA_0,\qquad q_2=2\pi iB_0.
\]

Averaging both side parameters gives the exact signed shell identity with
\(+i\operatorname{sgn}(x)\) on the vertical shell and
\(-\operatorname{sgn}(y)\) on the horizontal shell.  Tonelli then selects,
for each prescribed nonnegative weight pair, one common intermediate
rectangle for both carriers.  The vertical unweighted shell is paired with
\(1/\Delta_T\); the horizontal shell is paired with
\(1/\Delta_\eta\).

The cubic fixture

\[
F=z^3/3-z^2+1
\]

shows why a core-only manifest is insufficient in general for the
holomorphic-buffer construction: its selectors change charge at buffer
events one and two.  The complete-buffer selectors

\[
W_1=1-z/2,\qquad W_2=(z-1)(z-2)/2
\]

reduce the carriers to \(-F/(2z)\) and \(F^2/(4z)\), preserving normalized
charges \(-1/2\) and \(1/4\).

Exact replay:

    python -B experiments/X-105113-buffered-selector-shell-average/tests/test_verify.py
    python -B -O experiments/X-105113-buffered-selector-shell-average/tests/test_verify.py

Expected:

    PASS_T105113_BUFFERED_SELECTOR_SHELL_AVERAGE
    18 focused tests in normal and optimized Python
    digest 44e0cffca9d4d680555287e8290cfa645d979dc76b064f0ab6914ace88c7d560

The proof object uses only exact rational, polynomial, and rectangular
integral arithmetic.  No heavy computation, Xi evaluation, root finding,
cofinal Xi shell estimate, RCMV104530, or RH conclusion is used.
