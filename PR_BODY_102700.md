## Purpose

Continue PR #718 by identifying its completion defect with the audited
half-divisor/ratio-four physical occupancy route.

**RH remains unproved.**

## Exact factorization

```text
beta - beta_square
  = (lambda-lambda_square) * (lambda+lambda_square)

Phi_*
  = 2 A_- *_M A
```

Therefore the fixed completion defect is one exact two-field packet:

```text
H_def = 2 F_- *_M F_+.
```

The minus field has no unit/root coordinate.

## What closes

- labelled half-divisor source energy: polylogarithmic;
- source diagonals: polylogarithmic;
- same-product factor-pair multiplicity: subpower;
- every cofactor prime lies below `sqrt(X)`, so no optional unsquared cofactor
  sector exists;
- `(p,a) -> p a^2` is injective;
- same-owner/different-core overlap: polylogarithmic;
- product ratios outside `[1/16,16]`: exactly absent.

## Exact remaining theorem

```text
HDNC102703:
subpower distinct-product, cross-owner off-diagonal occupancy for the two
exact half-divisor fields.
```

This is the root-free specialization of the BPOE/HHFE physical occupancy
frontier.

```text
HDNC102703   OPEN / RH-BEARING
RH            UNPROVED
```

## Replay

```text
PASS_T102700_DEFECT_HALF_DIVISOR_FACTORIZATION
8daa7b67895f3f9d986d590fc3c2030e8f574e65979a9dbe7e5e58a1a2026352
```
