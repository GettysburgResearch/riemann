## Publication recovery

The previously claimed `T-101500/T-101510` branch and PR were absent from the remote repository. This draft is the actual publication, stacked on PR #697 at exact head `e878c3717cd8124564e5400b2ec5db035e3a4088`.

**The Riemann Hypothesis remains unproved.**

## Exact advances

1. **Matched transfer.** For all real `f,g,tau`,
   ```text
   (f+g)_- <= (f+tau)_- + (g-tau)_-,
   ```
   and the pointwise infimum over `tau` equals `(f+g)_-` exactly. This repairs componentwise negative-part loss by preserving the common carrier before one-sided estimation.
2. **Adaptive completion residual.** If `C_Z=I+R_Z` and `(C_Z F)(X)>=0`, then
   ```text
   F(X)_- <= [(R_Z F)(X)]_+.
   ```
   The completion may depend on `X` because the consumer still sees the fixed detector `F`.
3. **Opposite owner triangularizations.** For commuting prime shifts,
   ```text
   (C-I)E
   ```
   has exact least-owner and greatest-owner telescoping forms: native past plus squared future, or squared past plus native future.
4. **Full-tail carrier preservation.** Every finite completion correction retains the reciprocal-zeta zero at the real carrier. Truncating the native tail before estimating may create a false square-root carrier.
5. **Regional AND-gate.** If the least-owner majorant is subpower on one measurable region and the greatest-owner majorant is subpower on its complement, then the fixed detector has subpower negative mass.

## Exact boundary

```text
matched-transfer identity                    PROVED EXACT
adaptive-completion residual                 PROVED EXACT
least/greatest-owner triangular forms        PROVED EXACT
full-tail real-carrier preservation          PROVED EXACT
regional two-orientation composition         PROVED EXACT
LCOR101510                                    OPEN / RH-BEARING
RCOR101510                                    OPEN / RH-BEARING
Riemann Hypothesis                            UNPROVED
```

## Replay

```text
PASS_T101500_JOINT_COMPENSATED_MATRIX_RECOVERY
RH UNPROVED
```
