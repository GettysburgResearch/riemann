## Purpose

Continue PR #696 at exact head `1abfcaf96ef3a6796fc8b438835e3c20eb0d1abb` and attack its sole half-completed field-energy condition `HHFE102010`.

**The Riemann Hypothesis remains unproved.**

## Exact advance

For

```text
h_U = (mu 1_(n>U)) * eta,
eta(p^k)=binom(2k,k)/4^k,
```

and the ratio-four step

```text
psi(u)=1_(0<u<log2)-sqrt(2)1_(log2<u<2log2),
```

the field energy from PR #696 is exactly

```text
H_U(N)
 = sum_(U<m,n<=N) h_U(m)h_U(n)/sqrt(mn) R(log(m/n)),
```

where `R=psi * reflected(psi)` is explicit and vanishes unless `1/4<m/n<4`.

Its diagonal is

```text
3 log(2) sum_(U<n<=N) h_U(n)^2/n = N^o(1)
```

unconditionally, since `|h_U(n)|<=tau(n)`.

Therefore `HHFE102010` is equivalent to a single signed ratio-four off-diagonal near-collision estimate. No diagonal, Type-I, carrier, second field, or far-ratio term remains.

## Narrow-scale factorization

For `a=2^(1/M)`, the fixed step multiplier factors as

```text
psi_2 = [sum_(r<M) S_(a^r)]
        [sum_(r<M) a^(r/2) S_(a^r)] psi_a.
```

All reconstruction coefficients are nonnegative and have total mass `O(M^2)`. Hence the fixed energy is bounded by `O(M^4)` times the energy of a window supported on the shrinking ratio `a^2=2^(2/M)`. Any subpower refinement depth is admissible.

## Source ownership

Expanding `h_U=b_U*eta`, grouping the two tail divisors by their gcd, and then taking the unique largest prime collapses the two Möbius signs to one cofactor sign. The remaining weights are positive half-divisor coefficients and one explicit signed autocorrelation kernel.

## Frontier

```text
half-divisor source square                 inherited exact
single-field Hardy reduction              inherited exact
full Gram/autocorrelation formula          PROVED EXACT
coefficient diagonal                       CLOSED UNCONDITIONALLY
far multiplicative ratios                  ZERO EXACTLY
narrow-window positive reconstruction      PROVED EXACT
one-sign owner reduction                   PROVED EXACT
HCNC103100 signed near-collision estimate  OPEN / RH-BEARING
Riemann Hypothesis                         UNPROVED
```
