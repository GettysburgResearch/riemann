# T-108100 — Shared-conductor positive occupancy quotient

Base:

```text
PR #765
a30276a5be049749ebb2147f30f000dd5659298b
```

This continuation proves that the positive part of the complete atom-level
shared-fibre Wick operator descends exactly to the occupied residue cells.

For

\[
B_R=R^*SR-dI,\qquad D=RR^*,
\]

put

\[
Q_R=D^{1/2}SD^{1/2}-dI.
\]

Then

\[
B_R\simeq Q_R\oplus(-dI_{\ker R}),
\]

and therefore

\[
(B_R)_+=U(Q_R)_+U^*,
\qquad U=R^*D^{-1/2}.
\]

Thus residue aggregation need not be injective for the conclusion-facing
positive Hodge trace.  All same-cell label/history directions are strictly
negative and contribute nothing to the positive part.

The packet also proves:

* \(\operatorname{tr}B_R=0\);
* \(\operatorname{tr}Q_R=d\dim\ker R\);
* cell-preserving history changes are negative-block gauge symmetries;
* a cell permutation has a bijective atom lift exactly when occupancy is
  constant on each permutation orbit.

The remaining global target is signed conductor recombination of the
low-dimensional quotient currents.

## Replay

```bash
python -B research/riemann-structures/shared_fibre_positive_quotient.py
python -B research/riemann-structures/shared_fibre_positive_quotient.py --check
python -B -m unittest tests.test_shared_fibre_positive_quotient
```

```text
fixed-fibre positive restriction       PROVED EXACT
atom/history positive debt             ELIMINATED
partial-Frobenius occupancy criterion  PROVED EXACT
global signed recombination            OPEN
ONEPLACETRACE / RELTRACE               OPEN
principal binding                      OPEN
RH / GRH                               UNPROVED
```
