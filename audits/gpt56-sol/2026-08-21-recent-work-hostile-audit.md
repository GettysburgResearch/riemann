# Hostile audit of the T100160 / T100180 / T100600 / T100700 packets

Date: 2026-08-21  
Auditor: GPT-5.6 Pro  
Scientific status: **RH unproved**

## Verified genealogy

```text
PR #681  research/gpt56-sol/100160-critical-self-reciprocal-descent
PR #684  research/gpt56-sol/100180-adaptive-critical-squaring
PR #691  research/gpt56-sol/100600-implication-matrix-hybrid-closure
PR #695  research/gpt56-sol/100700-double-owner-coboundary-closure
```

The previously reported PR number for the T100700 branch is therefore **#695**.

## Exact results surviving reconstruction

- T100160 critical prefix/future-tail identity and activation cancellation;
- fully active critical-cube Euler-product identity;
- double-owner least/greatest-prime decomposition;
- double-owner mixed-coboundary and rectangle telescope;
- inverse-free first-transition finite-squaring identity;
- completed-minus-transition critical-carrier cancellation;
- corrected fixed-order completed positivity corridor;
- corrected balanced homotopy on the unsieved critical carrier;
- corrected local transition-kernel positivity.

## Binding corrections

### PR #691

1. The cofactor shift used for finite squaring must be the unrestricted multiplicative shift `V_q`; killing repeated `q` makes `V_q^2=0` and destroys the claimed `q^2` state.
2. A completion/future monomial `S_d f` or `U_d f` is a dilation, not the divisor restriction `sum_m beta(dm)m^-z` of PR #671.
3. `HDRB100603`, `ODSB100604`, and the corresponding positive-renewal implication arrows are withdrawn.
4. Regionwise absolute estimates are replaced by the oriented balanced successor on PR #695.

### PR #681

1. The exponent-`1` coarea boundary is PNT-controlled, not absolutely convergent.
2. The log-budget identity has its exact level and exclusion indices restored and is not promoted to an unweighted contraction.

### PR #684

1. The final critical kernel is noncompact; the inactive prime tail must be included.
2. The universal `log Z>=90` threshold for all critical orders is replaced by a fixed-order sufficiently-large cutoff.
3. Depth-one large-prime geometry holds only for activated products `<=X`.
4. Termwise child scaling cannot be pushed through a signed squared-prime core; the former collar-positivity argument is withdrawn.

### PR #695

1. The homotopy acts once on the unsieved critical carrier; it must not be applied to an already native critical remainder.
2. The duplicate `67` source is two independent labels.
3. The inactive tail is included in the positive-layer budget.
4. The local transition inequality is `w_X(np)>w_X(np^2)`; the former extra shift coefficients double-counted the normalization.
5. The precise derivative-cone equality with a fixed rough prefix was not typed through and is withdrawn; only a conditional method firewall remains.

## Current honest frontier

```text
native source
 -> double-owner mixed coboundary
 -> inverse-free completed-minus-transition homotopy
 -> carrier-preserving balanced physical estimate
 -> negative-mass / compact-wavelet detector
 -> RH.
```

The balanced physical estimate remains open and RH-bearing. No file in these audited branches establishes RH.
