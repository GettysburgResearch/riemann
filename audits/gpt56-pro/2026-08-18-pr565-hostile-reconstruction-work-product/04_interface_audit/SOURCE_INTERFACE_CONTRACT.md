# Required source-interface contract not yet proved

A valid successor theorem would need to define, for each real scale `x` and history `h`, a literal positive source object `P_{x,h}` together with:

1. an unsigned mass map `m(P_{x,h})` equal to the `M` used in the contraction estimate;
2. a signed observation `f(P_{x,h})` equal to the `F` used in the bias estimate, with the parity cocycle applied exactly;
3. a first-owner partition of every source atom into current, terminal, reserve, and recursive-child classes;
4. child embeddings whose images are disjoint source subpackets, not merely scalar comparison terms;
5. exact coefficient identities and scalar/source normalization after low-child recombination;
6. strict rank descent and complete base typing, including the odd-history witness `(X,h,terminal)=(61841,(67),(71,13))`;
7. proof that no reserve, target, or child mass is spent twice;
8. either a genuine Hall/Lorenz-free invariant or the full global primal/dual allocation certificate.

The reconstruction did not find this theorem in the inspected lineage, and did not complete a replacement. This is the first unsupported arrow after the scalar repair.
