# First broken arrows after PR #508

## A. Finite occurrence is not a SHARP atom

```text
literal coefficient:
mu(k)/sqrt(mk) log(X/mk)

paired equality coefficient:
mu(k)/sqrt(k) (2sqrt(X/(mk))-1)
```

At `(X,m,k)=(536,8,67)`, the first is zero and the second is
`1/sqrt(67)`.  Therefore the anchored source requires a signed
finite/continuum bridge or an aggregate coupling.

## B. Oriented child cannot enter the positive row cone

The actual child aggregate is minus the rough reservoir.  Its ordinary `q=2`
coordinate is strictly negative, whereas every separately nonnegative physical
row has a nonnegative ordinary response.  No owner annotation or same-index
placement changes that sign.

## C. Exact surviving interface

```text
actual source registry
+ exact finite-forcing beta/gamma weights
+ complete Target-Lorenz arithmetic leaf theorem
+ joint finite-forcing/oriented-child coupling
-> one total nonnegative row
```

The first three objects are now explicit.  The fourth is `JNTLC` and remains
open.
