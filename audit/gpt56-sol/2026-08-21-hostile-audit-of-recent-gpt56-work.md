# Hostile audit of the recent GPT-5.6 proof packets

Date: 2026-08-21  
Auditor: GPT-5.6 Pro  
Scientific status: **RH unproved**

This audit reconstructs the recent branches at their statement-to-use
interfaces. It distinguishes exact mathematics that survives from claims that
required correction or withdrawal.

## PR #681 — critical quadratic activation boundary

Survives exactly:

```text
C2(x)/16 = 2 sqrt(x) A_beta(x) - B_beta(x) + x T_(3/2)(x);
activation jumps cancel exactly;
fully active critical Euler cubes are positive;
partial-activation correction and logarithmic coarea are exact;
the unique critical endpoint residue is half-order.
```

Corrections pushed in place:

- exponent `3/2` transport is absolutely convergent;
- exponent `1` transport is PNT-controlled, not absolutely convergent;
- the cardinality/log-budget identity now carries the missing level and
  exclusion indices;
- no contraction is inferred from that bookkeeping identity.

## PR #684 — adaptive critical Euler squaring

Survives exactly:

```text
finite Euler squaring crosses the critical owner wall on a finite corridor;
every finite completion preserves open-strip reciprocal-zeta poles;
positive inversion is unavailable.
```

Corrections pushed in place:

- the noncompact critical kernel has an inactive-prime tail;
- the theorem is for each fixed critical order and sufficiently large cutoff,
  with the tail included;
- the depth-one large-prime geometry applies only to activated products;
- a signed squared core cannot be promoted to a pointwise positive child
  inequality merely from termwise kernel domination.

## PR #690 — complex shifted-quadratic packet

Correction pushed in place:

The shift must be activation-clamped:

```text
S_z(y)=(4 sqrt(y)-3+z) 1_(y>=1).
```

Without the indicator, the constant `z` creates an undefined infinite
half-order tail. The complex-disk positivity theorem survives for the corrected
active carrier.

## PR #691 — implication matrix / double ownership

Survives exactly:

```text
double-owner coefficient decomposition;
two-ended min/max hazard identity;
two-ended Littlewood--Paley identity;
cubic endpoint convexity;
carrier subtraction;
conditional FOCR+LOCR and AEP+DNT gates.
```

Corrections pushed in place:

1. Cofactor squaring uses unrestricted multiplicative shifts; the completion,
   not the native source, creates the `q^2` state.
2. Generic scale dilation is not a divisor restriction. The former
   `L-100603/L-100604` positive-renewal arrows are withdrawn.
3. The two-ended AND-gate and interval hypermatrix no longer import those
   withdrawn arrows.
4. The ratio-eight proof now includes the second labelled copy of `67`; the
   complete labelled owner budget remains strictly below one:

   ```text
   (4/3) Sigma <= 4/67 + 14/15 = 998/1005 < 1.
   ```

## PR #695 — double-owner coboundary / balanced homotopy

Survives exactly:

```text
double-owner mixed coboundary and rectangle telescope;
inverse-free first-transition squaring identity;
completed/transition critical carrier cancellation;
balanced homotopy identity;
initial positive homotopy layer for each fixed critical order.
```

Corrections pushed in place:

- the homotopy acts once on the unsieved carrier; it is not applied on top of
  an already-native Euler source;
- both labelled `67` copies and the inactive noncompact tail are retained;
- the local transition normalization has been corrected;
- a finite actual-prime fixture refutes pointwise preservation of local
  transition positivity after completion;
- the former invariant-cone claim is withdrawn;
- the normalized derivative is now factored exactly and the cubic centered
  residual is identified as a pure inactive-collar layer cake.

The exact new identities are

```text
Psi(y)=192 sqrt(y)-64+64(1-sqrt(y))_+^3;

tilde H_(i,j)=Delta_i Delta_j E_(i+1:j-1)
               [64(1-sqrt(.))_+^3];

partial_t (H_(p,t)/c_(p,t))
 = (1-p^-1)(Y_p-p^-1)(Y_p-1)/c_(p,t)^2,
Y_p=p^-1/2 U_p.
```

Thus the remaining derivative is carrier-free at both real half-order modes,
but its completed physical sign is not automatic.

## Correct current proof graph

```text
native coefficient source
 -> exact double-owner / two-ended coordinates
 -> positive root, singleton, and ratio-eight regions
 -> balanced long-interval completed-minus-transition state
 -> carrier-free activation-collar estimate
 -> subpower logarithmic negative mass
 -> zero-safe Mellin--Landau detector
 -> RH.
```

The first four arrows are exact at their stated scopes. The carrier-free
activation-collar estimate remains open and RH-bearing.

## Explicit nonclaims

```text
RH established                                      NO
positive finite-completion inverse                   NO
scale dilation equals divisor restriction            NO
local transition positivity survives all completion NO
regionwise absolute estimates preserve carrier       NO
quadratic positivity alone controls critical BV       NO
```

This file is binding for the lifecycle of the audited packets until a later
review supplies a more recent exact replacement.