# T-105540 — Unimodular Hermitian bank realization

This checkpoint continues T-105530 on dedicated PR #726.

The two-channel bank

```text
w0(X)=I-X/2-X^2/4
w1(X)=X/2
```

is not merely fibrewise positive.  It has the exact Bezout identity

```text
w0(X)+(I+X/2)w1(X)=I
```

and extends to a determinant-one polynomial matrix with a polynomial inverse.
Consequently the bank observation map has a source-owned holomorphic left
inverse on every one-sided Hardy module; it introduces no zeros, no dimension
loss, and no bank-induced strip partial index.

Combining this exact realization with the already proved functional-equation
fold, reciprocal tail, and differentiated freezing estimates closes the
horizontal gate `BANKREAL105530` at strict bandwidth.  The bank anchor is
`I+O(log(T)^-3)`, so the former fixed constant `9139/9216` improves
asymptotically to one.

The ninety-percent route is therefore reduced to a single conclusion-bearing
statement:

```text
MATRIXLERC105541:
  the Gram-normalized negative trace of the complete companion/vertical/
  collar remainder is < (1/20-o(1)) d_T.
```

This estimate, 90%, the public record, and RH remain unproved.
