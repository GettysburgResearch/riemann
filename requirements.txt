# Rigorous ball arithmetic (Arb/FLINT bindings).  Everything in scripts/ is
# certified: every returned object is a ball guaranteed to contain the true
# value.  See the TRUST BOUNDARY block in scripts/certzeta.py for exactly which
# routines are relied upon (zeta itself is NOT one of them).
python-flint>=0.9.0
