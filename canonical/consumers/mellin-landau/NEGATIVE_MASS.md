# Subpower negative mass

Pointwise positivity is stronger than the Mellin-Landau consumer needs.

For one fixed zero-safe detector `f`, the reviewed sufficient condition is

\[
\int_1^T f_-(X)\,\frac{dX}{X}=T^{o(1)}.
\]

Together with the fixed multiplier and specialized Landau theorem, this excludes an off-critical reciprocal-zeta pole.

The graph therefore uses two premises:

```text
VERIFIED analytic negative-mass API
+
OPEN fixed arithmetic negative-mass estimate
->
RH
```

The open estimate is never stored inside the verified API node.
