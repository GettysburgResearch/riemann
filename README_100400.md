# T-100400 — Two surviving closure routes: publication recovery

This add-only packet republishes the exact two-route reconstruction that was
previously described but did not reach GitHub.  It is deliberately assigned a
fresh namespace and branch.

The packet records two conclusion-facing routes:

1. **Quadratic upper-envelope route.**  A shifted-square homotopy gives an
   exact atom-plus-collar formula for the quadratic envelope.  The final gate
   is `QACG100400`.
2. **Minimal ordinary-Möbius wavelet route.**  Unique largest-prime ownership
   removes the smooth sector and leaves one explicit rough bilinear form.  The
   final gate is `LPMW100410`.

A third candidate, the symmetric phase-Hasse symbol, is audited and demoted:
neutral-phase cancellation does not remove the nonzero-phase Euler root.

Neither final gate is proved.  The Riemann Hypothesis remains unproved.
