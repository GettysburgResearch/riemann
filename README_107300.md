# T-107300 — shared-fibre primitive trace object

This packet is stacked on PR #765 at
`a30276a5be049749ebb2147f30f000dd5659298b`.

It proves:

- exact factorization of every native rank-one rectangular shared-fibre Wick
  panel into two one-sided centered forms and two norms;
- a canonical Frobenius-stable mean/primitive augmentation object;
- source-aware Adams compatibility at the finite augmentation-compatible
  scope;
- the exact \(-660d_{\ell,\rho}\) primitive energy of the live
  one-hundred-history panel.

The cofinal rectangle gluing, uniform signed primitive trace and principal
binding remain open.  RH and GRH remain unproved.

Replay:

```bash
python3 experiments/X-107300-shared-fibre-primitive/verify.py \
  --output experiments/X-107300-shared-fibre-primitive/results/verification.json
sha256sum -c T107300_CONTENT_SHA256SUMS
```
