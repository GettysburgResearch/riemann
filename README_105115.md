# T-105115 common safe-rectangle transfer

This checkpoint makes the finite geometry cited by T-105114 a hashed proof
object.  For finitely many closed bad disks it computes the exact merged
coordinate projections, proves the positive-measure safe-product criterion,
and records the worst-case sharp strict gate

\[
\Delta_T>2S,\qquad\Delta_\eta>2S.
\]

It then restricts a prescribed integrable L-105113 shell cost to the safe
product.  The exact parameter-normalization factor is

\[
\kappa=
\frac{\Delta_T\Delta_\eta}
{(\Delta_T-|B_T|)(\Delta_\eta-|B_\eta|)}.
\]

The raw quotient transfer permits boundary zeros of \(F\), but comparison
with the unrestricted shell mean requires full-shell integrability or
removable continuation across unsafe raw events.

Exact replay:

    python -B experiments/X-105115-common-safe-rectangle/tests/test_verify.py
    python -B -O experiments/X-105115-common-safe-rectangle/tests/test_verify.py

Expected:

    PASS_T105115_COMMON_SAFE_RECTANGLE
    16 / 16 in normal and optimized Python
    digest b9e9d4c77045189e1c807b056f5b2e257d22a1bdf45e5fc824a652e710ac7aaf

The proof object uses exact rational interval geometry.  It runs no Xi
evaluation, zero search, contour quadrature, or heavy computation.

The cofinal Xi disk certificate, actual-pole manifests, cost absorption,
RCMV104530, and RH remain open.
