# T-105500 — Native prime-color and tail-pair F1 frontier

This checkpoint continues durable draft PR #730 after the binding QPTI
semiprime correction on PR #719.

```text
base PR #730:  b3114562acbeb8c5890ef7a5fc59eed8db71d29a
PR #719:       59654c02d13545d6c8c0972315db2628e9efa1f6
PR #751:       98af0db6ec7f77d6333a77a3dac53c4698852f43
PR #685:       4f69b7656f42dcb5ff250d13adc9f88e8d18f315
PR #715:       99cc94c48bafd9b96141f7cef9f1e7aa83012747
```

**The Riemann Hypothesis remains unproved.**

## Binding source correction

Corrected PR #719 proves that the harmonic equal-pair Euler–Beta field has a
power-sized negative semiprime main.  Thus `QPTI103112`, `EBD103120`, and the
old completed-source `QPTI <=> BCI <=> HMO` chain are withdrawn.

The balanced F1 source on PR #730 is not that fixed harmonic source.  Its core
coefficient is

\[
b_U=a_U*a_U*\mu=a_U*\nu_U,
\qquad
\nu_U=\mu\mathbf1_{n>U},
\]

and therefore

\[
b_U(c)=0\qquad(c\le U^2).
\]

The QPTI fixed-core semiprime asymptotic cannot be transferred by matching only
the physical shape \(pq c^2\).  What fails on PR #730 is the old
source-identification/conclusion arrow, not its finite Chow or generic
cell/Gram identities.

## Native physical F1 section

A contraction-free ordinary local factorization

\[
(1-\alpha x)(1-\beta x)=1-x
\]

forces \(\{\alpha,\beta\}=\{0,1\}\).  Hence the native physical section is a
prime-color boundary gauge.  For every prime coloring \(\chi\),

\[
\mu=\mu_\chi^+*\mu_\chi^-.
\]

There is no owner-pair/squared-core completion mode.

## Vaughan compression

The balanced ordinary Vaughan source admits the exact identities

\[
a_U=\mathbf1*\nu_U,
\qquad
a_U*\mu=\nu_U,
\qquad
a_U*a_U*\mu=a_U*\nu_U.
\]

More generally,

\[
a_U*a_U*\mu
=(a_U*\mu_\chi^+)*(a_U*\mu_\chi^-).
\]

Both factors vanish below \(U\); their source diagonals and equal-product
multiplicities are subpower.

## Native cross-Hodge coordinate

For the positive ratio-four half-kernel \(A\),

\[
K_1=(D+\tfrac32)(D-\tfrac12)(A*_MA).
\]

The balanced same-\(K_1\) current is therefore the differential image of an
ordinary cross-convolution of the two long native fields.  If

\[
\mathcal D_\chi(x)=\frac14\|F^+_{\chi}-R_xF^-_{\chi}\|_2^2,
\]

then for \(K_2=DK_1\),

\[
\mathcal B_U^{K_2}
=-2D(D+\tfrac32)(D-\tfrac12)\mathcal D_\chi.
\]

Different colorings change \(\mathcal D_\chi\) only by an additive constant,
so the differential current is color-gauge invariant.

## Exact discrete endpoint

The derivative same-\(K_1\) kernel is supported on \([1,16]\) and has four
explicit affine-in-\(\sqrt y\) bands.  For
\(c_n=\mu(n)/\sqrt n\), define

\[
P(x)=\sum_{n\le x}c_n,
\qquad
Q(x)=\sum_{n\le x}\frac{c_n}{\sqrt n},
\qquad
W(x)=3P(x)-4\sqrt xQ(x),
\]

\[
\Delta_4=(I-S)^2(I-\sqrt2S)^2.
\]

Then

\[
G_2(m+)=-\Delta_4W(m).
\]

The left/right jump is the corresponding atomic filter and has weighted cost
\(O(M^{-1/2})\).  Every unit cell is affine in \(\sqrt X\); its exact
logarithmic negative area is an elementary function of the two endpoint
values.

## Correct conclusion chain

Freeze \(U_j=\lfloor2^{j/6}\rfloor\) on each dyadic block.  The derivative
Type-I row is \(O(X^{-1/6})\) and has finite logarithmic mass.  The balanced
row is the native tail-pair mismatch above.  Hence

```text
NATIVEF1XD105504
  <=> NATIVECELL105504
  <=> RH.
```

Both named estimates remain open.

## Replay

```text
PASS_T105500_NATIVE_F1_TAIL_PAIR
e25255643bfd11e9fc931b4484189d0f6178bcf5537f39d734acb0b3da84a386
checks=161264
rh_established=false
```

The replay authenticates finite source identities, prime-color factorization,
Vaughan tail-pair compression, the exact derivative kernel, endpoint/jump
filters, cross-Hodge polarization, and the signed-cell primitive.  It does not
prove either terminal estimate or RH.
