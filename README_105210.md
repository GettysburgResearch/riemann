# T-105210/T-105220 — simple-zero record and quantitative descent gates

This packet has two distinct roles.

1. It imports, source-locks, and reconstructs the deduction behind the
   proposed unconditional bound
   \[
   \liminf_{T\to\infty}
   \frac{N_0^s(T,2T)}{N(T,2T)}
   \ge 0.673008527927\ldots .
   \]
   The finite seven-gap certificate belongs to the pinned external repository
   `ainta/zeta-simple-zeros` at commit
   `040c5e899e658aed7b56a2a87f501798fe10761d`.

2. It proves a new exact multilevel reverse–Rolle theorem. A whole derivative
   block can be paid by one aggregate critical-residue coherence statistic.
   This gives explicit sufficient cutsets for 90% and for density one on the
   critical line.

The external Arb certificate is not silently represented as rerun by the
lightweight checker in this packet. RH remains unproved.
