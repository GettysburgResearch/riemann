## Dedicated durable F1 execution PR

This remains the multi-pass successor to programme PR #727.

**RH remains unproved. The PR remains draft.**

## Binding source audit

The requested checkpoint-four commit `512ff17dd7880313694c20372ca2b84ad2a5c330`
remains in the branch ancestry.

Corrected PR #719 now proves that the harmonic Euler–Beta completion
`QPTI103112/EBD103120` has a power-sized negative semiprime main.  The former
completed-source `QPTI <=> BCI <=> HMO` chain is withdrawn.

This checkpoint proves the source distinction which prevents a second false
promotion:

```text
harmonic QPTI core coefficient:  mu(c);
balanced F1 core coefficient:    b_U(c)=a_U*a_U*mu(c);
b_U(c)=0 for c<=U^2.
```

The semiprime main does not transfer by matching only the atom shape `p*q*c^2`.
The old PR #730 source-to-RH arrow is retired; the finite Chow/Hodge and generic
cell/Gram identities remain exact.

## T-105500 — native F1 repair

### Prime-color boundary compactification

A contraction-free ordinary local factorization of `1-x_p` into two
squarefree factors forces each prime wholly into one factor.  For every prime
coloring,

```text
mu = mu_chi^+ * mu_chi^-.
```

This retains `1/zeta`, has unique squarefree products, and introduces no
owner-pair/squared-core completion mode.

### Vaughan trilinear collapses to a tail pair

For

```text
nu_U = mu 1_(n>U),
a_U  = epsilon - mu_U * 1,
```

one has exactly

```text
a_U = 1 * nu_U,
a_U * mu = nu_U,
a_U * a_U * mu = a_U * nu_U.
```

More generally the balanced source is the cross-product of the two colored
long fields.  Both vanish below `U`; diagonals and equal products are
subpower.

### Cross-Hodge form on the native same-K1 detector

For the positive ratio-four half-kernel `A`,

```text
K1 = (D+3/2)(D-1/2)(A *_M A).
```

If `D_chi(x)` is one quarter of the squared reflected mismatch of the two
colored fields, then for `K2=D K1`,

```text
B_U^K2 = -2 D(D+3/2)(D-1/2) D_chi.
```

Prime color changes only the constant mismatch baseline, so this differential
current is gauge invariant.

### Exact ratio-sixteen Hardy endpoint

The derivative kernel has four explicit bands on `[1,16]`.  With

```text
W(x)=3 sum_(n<=x) mu(n)/sqrt(n)
     -4 sqrt(x) sum_(n<=x) mu(n)/n,
Delta4=(I-S)^2(I-sqrt(2)S)^2,
```

one has

```text
G2(m+) = -Delta4 W(m).
```

The endpoint jump is the atomic version of the same filter and has
`O(M^(-1/2))` weighted cost.  The negative logarithmic area of every integer
cell is an exact elementary endpoint primitive.

### Correct live frontier

With `U_j=floor(2^(j/6))`, the derivative Type-I row is `O(X^(-1/6))`.
The balanced row is exactly the native tail-pair Hodge mismatch.  Therefore

```text
NATIVEF1XD105504
  <=> NATIVECELL105504
  <=> RH.
```

Both estimates remain open.

## Binding supersessions on this branch

`T-105460`, `T-105470`, `T-105480`, and `T-105490` are corrected in place:

```text
their finite/operator identities are retained;
QPTI103112 is marked refuted;
the old completed-source RH arrows are withdrawn;
the balanced b_U estimates are source questions, not declared false;
the independent native RH-equivalent route is T-105500.
```

## Replay

```text
PASS_T105500_NATIVE_F1_TAIL_PAIR
e25255643bfd11e9fc931b4484189d0f6178bcf5537f39d734acb0b3da84a386
checks=161264
rh_established=false
```
