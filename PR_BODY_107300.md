## Shared-conductor relative-trace programme

This durable draft executes programmes #763, #737, and #739 from exact base
PR #765 head `a30276a5be049749ebb2147f30f000dd5659298b`.

**RH and GRH remain unproved.**

### Main advance

The connected squareclass projector is realized by the honest Kummer
augmentation sheaf. Square pullback has exactly one possible nonprincipal
constant constituent per marked place: the even quadratic character.

After removing those explicit rank-one channels, the reduced shared-conductor
augmentation tensor:

- is an actual lisse pure weight-zero object;
- has no constant constituent on the complete clean two-core torus;
- reproduces the native connected trace kernel;
- has exact zero or square-root complete additive-twisted traces.

The full connected source is the reduced object plus at most three explicit
quadratic-resonant rows.

### Live gates

```text
LIVEPUSH107300
AND QRESBIND107300
  -> CBKM106130
  -> BCI102990
  -> RH.
```

Both new gates remain open. The branch does not infer principal binding from a
family average and does not model the two Wick atoms with independent marked
places.

Replay:

```text
PASS_X_107300_SHARED_CONDUCTOR_REDUCED_AUGMENTATION
```
