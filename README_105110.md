# T-105110 oriented-edge cancellation frontier

This checkpoint resolves the first finite layer of the phase-sensitive fork
left open by T-105109.

For an exterior simple pole approaching the interior of a straight edge, the
oriented Cauchy-kernel integral stays bounded even though its supremum and
absolute edge integral diverge.  A finite principal-part decomposition gives
the exact certificate

\[
\left|\int_E Wh\,dz\right|
\le C_{\rm rem}
+\left(\pi+\log\frac\ell\kappa\right)C_{\rm pp}.
\]

The hypotheses are sharp enough to matter: a pole projecting to a corner
causes logarithmic divergence.  Moreover, an explicit real-even family with
one globally fixed critical target, residue one, no exterior critical point,
and optimal selector one has an individual edge larger than \(N^2/30\).
Thus the pole-subtracted remainder is an independent gate.

Exact replay:

    python -B experiments/X-105110-principal-part-edge-cancellation/tests/test_verify.py
    python -B -O experiments/X-105110-principal-part-edge-cancellation/tests/test_verify.py

Expected:

    PASS_T105110_PRINCIPAL_PART_EDGE_CANCELLATION
    18/18 focused tests in normal and optimized Python
    digest 3f0286c2d574e8252fe9903f4f62d86e510275bdf4a1a085e3f232873aec9532

No heavy computation is used.  Xi continuation, exterior principal parts,
weighted residue sums, corner control, pole-subtracted remainders, cofinal
strict coherence, RCMV104530, and RH remain open.
