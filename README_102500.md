# T-102500 — Common mother kernel and covariant scale–phase stress tensor

This packet executes the concrete programme in PR #713.

The main exact result is a compact common mother packet `Phi_*` for the
activation-free critical channel and the corrected same-`K1` wavelet channel:

```text
CV_* = D Phi_*,
XD   = (D+3/2) Phi_*/2,
Phi_*= -2 CV_*/3 + 4 XD/3.
```

The relation is finite, fixed, zero-safe and source-faithful. It is lifted to
the complete labelled Euler source, with the two copies of 67 kept distinct.

The naive Euclidean `(u,theta)` stress tensor is refuted. The correct phase
coordinate is the relative Heisenberg coordinate

```text
Q = u - i sum_l log(p_l) partial_(theta_l),
[P,Q] = -i,  P=-i partial_u.
```

This gives an exact strict determinant reserve. A second exact determinant
identity couples the CV and XD channels, and on every one-octave source shell
gives a uniform energy contraction constant below `0.935`.

What remains is arithmetic rather than analytic:

```text
carrier-recombined physical shell estimate;
source-faithful torus-to-physical occupancy after the regional decomposition.
```

No such estimate is silently assumed.

```text
common multiplier / Bezout       PROVED EXACT
labelled scale–phase lift         PROVED EXACT
Heisenberg determinant reserve    PROVED EXACT
CV/XD channel determinant         PROVED EXACT
carrier/source functoriality      PROVED EXACT
fixed mother Mellin detector      PROVED EXACT
global arithmetic Perron rows     OPEN / RH-BEARING
Riemann Hypothesis                UNPROVED
```
