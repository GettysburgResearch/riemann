# Fixes required before reconciliation

## Integration blockers

1. Obtain an exact-head Lean build and retain output for all requested library and comparator targets. Run both B-owned `#print axioms` files directly and wire them into the authoritative audit.
2. Replace the canonical mapping of `firstOwner_eq_nativeEuler`. Either rename it as a generic scalar-list lemma or formalize L-99601 with labelled occurrences, parity channels, commuting shifts, complete future-prime products, first-owner weights, and disjoint/exhaustive ownership.
3. Downgrade `ARITH.SHARP.SEQUENTIAL_FIRST_OWNER`, `ARITH.SOURCE.TYPING_FIREWALLS`, and `API.MELLIN.SUBPOWER_NEGATIVE_MASS` in `B.tsv` until their exact statements are formalized.
4. Replace the toy RN and normalization witnesses with the exact raw/RN `(16,4,4)` fixture and a quantified `p^-1` versus `p^-1/2` normalization theorem.
5. State the real protection boundary of the role-indexed source API. Public constructors currently permit coefficient extraction and reconstruction at a new role. Fixed/moving detector types likewise do not prove quantifier-order independence.
6. Rebuild the conditional consumer against Reviewer A's exact analytic API. Make source identity, fixed detector choice, numerator noncancellation, Landau boundary hypothesis, holomorphic defect, multiplicity, and reflection assumptions explicit and operative. Remove the false blueprint `uses` edge unless the theorem genuinely consumes the 5:3 result.
7. Complete or explicitly defer the exact wavelet deliverables: `alpha=sqrt(2)` coefficients, ratio-eight support, piecewise kernel, factor-67 negative copy, endpoint conventions, and exact Abel-Mertens frame. Rename the current generic/trivial helpers where their names overstate scope.
8. For the one-field reduction, source-lock the accepted convolution identity separately from the unformalized `b_U/h_U`, ratio-four field, Hardy norm-3, and finite-endpoint localization statements.
9. Publish a B-owned exact source-lock ledger and formal-status report.

## Local corrections

- Remove `ComparatorSmoke` from the default formal library or justify the reversed comparator dependency.
- Add direct axiom prints for response/capacity, normalization, role separation, K0/K1, and comparator solutions.
- Correct the same-K1 registry dependency so the false old edge is recorded as superseded, not a proof dependency.
