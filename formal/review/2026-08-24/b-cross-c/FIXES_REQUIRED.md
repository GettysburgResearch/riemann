# Fixes required before reconciliation

## P0 — Statement and trust blockers

### 1. Replace arbitrary external proposition labels with exact locked propositions

`HighZeroVerified`, `CorrectedSourceLock`, `GroupedC2Convergence`,
`OrbitConventionLocked`, `MultiplicityResidualRetained`, and
`ReciprocalSquareTailControlled` are currently arbitrary `Prop` parameters.

Define exact statements containing the verified height and published theorem,
exact bibliographic/artifact lock, grouped actual-Xi expansion, local `C²`
convergence through two derivatives, reflected-orbit and multiplicity
conventions, residual `(m₀-1)R₀`, and reciprocal-square tail theorem.

The formal source-lock registry must lock those external inputs independently of
the repository claim-file SHA.

### 2. Prove repeated-node reduction

Delete the hypothesis that directly returns the desired `IsPSD3` conclusion on
all repeated-node branches. Model values as evaluations of one function at the
nodes. From positive-node squared equality derive node and value equality, then
use the duplicate-row congruence lemmas and order-two PSD.

### 3. Repair semantic mappings in `C.tsv`

Do not map stronger canonical statements to generic finite lemmas. In
particular, repair or split ONE_ORBIT, CRITICAL_RESERVE_BUDGET, ACTUAL
reciprocal concavity, TP curvature, and PICK_ORDER3. Keep generic finite
implications under new API-level semantic IDs.

### 4. Correct the order-three comparator

The current comparator proves only the ordered-distinct generic implication,
while the QA report marks the full order-three semantic claim as checked. Add a
comparator definitionally matching the repaired headline, including repeated
nodes and concrete external propositions.

### 5. Produce authoritative build evidence

Run the complete frozen-head workflow and archive full `lake build`, all three
challenge/solution builds, registry/source/blueprint/declaration checks,
no-sorry/custom-declaration scan, and raw/parsed `#print axioms` output.

## P1 — Reserve and QA hardening

### 6. Formalize source-specific one-orbit algebra

Add the exact cross-curvature formula, domain hypotheses,
`epsilon = 2 m kappa / (1-kappa)`, and the `epsilon <= 9m/b²` bound. The generic
`hpay` lemma is useful but does not formalize the source theorem.

### 7. Make reserve shares a genuine allocation

Require every share and tail term to be nonnegative. Define one reserve orbit,
one representative per reflected off-line orbit, and multiplicity-aware costs.
Prove the coefficient is consumed once and the leftover is nonnegative.

### 8. Generate QA evidence rather than trusting booleans

Derive comparator coverage from exact theorem-type comparison, axiom coverage
from the registry declaration set, source locks from exact source-lock objects,
and fully qualified declaration types from the Lean environment. Reject unused
or inert external-input bundles.

## P2 — Documentation

### 9. Rewrite `content-C.tex` and `C_OPERATOR_QA.*`

State exactly which finite identities are proved and which source-specific or
analytic theorems remain blocked. Do not describe arbitrary proposition labels
as an exact source lock or the generic payment lemma as the full one-orbit
theorem.
