# Formalization Reviewer B cross-review of Reviewer C

## Executive verdict

```text
Target PR:                 #735
Frozen target head:        4863c31dd497ffe69ea400fe29275decf4cb5d29
Bootstrap base:            573eb6aa42c3d9469462c91c6b3ddfb8ab36d77f
Reviewer C integration:    NOT_INTEGRATION_READY
RH proved:                 NO
```

Reviewer C completed a substantial and valuable **finite algebra layer**. The
two- and three-node determinant algebra, LDL identities, PSD/PD separation,
finite reciprocal-concavity closure, orbit defect formula, finite Q4/Hermite
identities, and the six requested minimal operator firewalls withstand
independent exact replay.

Reviewer C did **not** complete every assigned conclusion-facing task. The
published “actual-Xi order three” theorem does not formalize an actual Xi
object, an exact external verified-height theorem, the grouped zero expansion,
the source-specific reserve payment, or repeated-node reduction. It packages
six arbitrary proposition labels, directly assumes the decisive scalar signs,
and assumes the desired PSD conclusion on the repeated-node branch.

The packet is useful evidence and a strong finite substrate, but is not ready
for formal reconciliation as the canonical order-three theorem.

## Completion audit

Completed at substantive reviewed scope:

- exact two-point Pick determinant algebra;
- exact three-node determinant and divided-difference factorization;
- two- and three-dimensional LDL/congruence identities;
- PSD and PD separation;
- finite positive-sum reciprocal-concavity closure;
- exact off-line orbit defect and critical zero-curvature formulas;
- finite Hermite and Q4 identities;
- six minimal operator/source-blind firewalls;
- existence of all three requested comparator topic families;
- existence of C registry, blueprint, QA report, and QA scripts.

Incomplete or overstated:

- source-specific one-orbit reserve theorem;
- nonnegative one-use reserve allocation;
- exact external high-zero and bibliographic source lock;
- grouped actual-Xi `C²` assembly;
- actual-Xi companion-curvature theorem;
- repeated-node proof;
- comparator coverage of the full headline theorem;
- fail-closed derivation of QA evidence;
- authoritative Lean build and axiom output.

## Exact three-node algebra

The formal convention `H_ij=(x_i p_i+x_j p_j)/(x_i+x_j)`, the barycentric
second divided difference, and `Delta=(t₂-t₁)(t₃-t₁)(t₃-t₂)` match the reviewed
source. The denominator is a product of squared node sums. The two factor signs
each carry one minus sign and cancel.

Independent symbolic reduction confirms the determinant and LDL3 identities.
The formal theorem validly generalizes the source's ordered-positive node scope
to algebraic nondegeneracy, which should be documented. Degenerate nodes are
excluded from the divided-difference theorem and are only handled at generic
matrix scope.

```text
THREE-NODE DETERMINANT: FORMALLY_VERIFIED_WITH_FIXES
PSD/PD SCOPE:           FORMALLY_VERIFIED
```

## Actual-Xi order three

The distinct-node theorem is an exact conditional finite implication from
positive nodes/diagonal values, one strict leading minor, and two nonpositive
curvature divided differences. It concludes PSD only.

However:

1. six external inputs are arbitrary `Prop` parameters;
2. the bundle is unused in reciprocal, companion, and reserve finite proofs;
3. no actual Xi function or grouped zero expansion appears;
4. the curvature signs are direct hypotheses rather than consequences of the
   named external data;
5. the repeated-node hypothesis returns exactly the desired PSD conclusion;
6. the conditional comparator covers only the ordered-distinct theorem.

Thus compilation would establish a generic finite implication, not the reviewed
actual-Xi theorem for every packet of size at most three.

```text
DISTINCT FINITE IMPLICATION:  PROVED_CONDITIONAL
FULL ACTUAL-XI HEADLINE:      STATEMENT_MISMATCH
EXTERNAL SOURCE INPUTS:       EXTERNAL_INPUT_NOT_LOCKED
REPEATED-NODE HANDLING:       INCOMPLETE_PUBLICATION
```

## Reserve accounting

The local defect, critical zero-curvature formula, and generic implication
`defect <= epsilon*cross -> energy(q+epsilon*r)>=0` are correct. The source
L-92101 cross formula, kappa domain, chosen epsilon, and `9m/b²` bound are not
formalized.

The finite leftover theorem does not require nonnegative shares, and the reserve
wrapper has no actual orbit index, multiplicity convention, verified height, or
infinite tail. It does not yet prove that one physical critical orbit is
consumed once without double counting.

```text
LOCAL RESERVE ALGEBRA:        FORMALLY_VERIFIED
SOURCE-SPECIFIC ABSORPTION:   STATEMENT_MISMATCH
GLOBAL RESERVE ACCOUNTING:    STATEMENT_MISMATCH
```

## Firewalls

All requested finite witnesses replay exactly: positive Schur rescue,
coefficient energy versus coherent contraction, inertia/current, hyperbolic
cross block, positive excess/root orientation, and scalar diagonal/polarized
Gram. Their statements are appropriately minimal. The finite heat displacement
theorem is correctly not promoted to the full analytic no-go.

## Comparator and QA audit

- `XiPickThreeNode`: exact finite formula and sorry-free solution.
- `OperatorPositiveSchurRescue`: exact firewall and sorry-free solution.
- `XiPickOrderThreeConditional`: exact only for the ordered-distinct generic
  implication, not the full registered headline.

Static target-patch inspection finds exactly three `sorry`s, one in each
Challenge file, and none in changed trusted solution/library files. No custom
axiom, opaque constant, or unsafe declaration was found in the target patch.

The scripts are useful but not fully fail-closed: report booleans are trusted,
declaration checking is a leaf-name regex, registry declarations are not
mechanically compared with the axiom-audit set, exact theorem types are not
compared with comparator statements, and inert external proposition labels are
not detected.

## Build and trust result

No full target-head build could be established. The audit environment lacks
Lean/Lake and a networked Mathlib cache. PR #735 exposes no workflow run. A
separate exact-head probe PR was created without touching #735, but GitHub
exposed no run or status for it either. This is `BUILD_FAILURE` because required
evidence is absent, not because compilation has been demonstrated to fail.

## Direct answers

### Did C complete every assigned task?

**No.** Most finite algebra and firewall tasks are complete, but exact external
source locking, source-specific reserve absorption, one-use global allocation,
grouped actual-Xi assembly, repeated-node closure, full comparator coverage, and
authoritative build evidence are not.

### Is the order-three theorem stated honestly?

**Not fully.** It correctly concludes PSD rather than PD and exposes scalar
signs, but arbitrary proposition labels are not exact inputs and the
repeated-node premise is the conclusion itself.

### Which exact algebra is sorry-free?

Subject to the missing authoritative build, the changed trusted text gives
sorry-free proofs of the two-/three-node Pick algebra, divided-difference
factorization, LDL2/LDL3, finite PSD/PD lemmas, finite reciprocal-concavity
closure, orbit defect/critical formulas, generic absorption/allocation, finite
Hermite/Q4 identities, and all six finite firewalls. Independent exact symbolic
replay passes the load-bearing identities.

### Which result remains conditional?

Actual-Xi PSD through order three remains conditional, and its current theorem
is weaker than the intended conditional statement. Exact analytic inputs,
reserve bridges, scalar signs, and repeated-node closure remain unproved.

### Are all comparator and QA layers complete?

**No.** All named files exist, but the conditional comparator misses the full
headline and QA evidence is not fully derived fail-closed.

### Is PR #735 ready for reconciliation?

**No.** It is valuable input after P0 repairs and a successful frozen-head
build, but is not currently integration-ready.

### RH proved

**NO.**
