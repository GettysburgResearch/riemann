# Blueprint scaffold

This directory is the human-readable dependency-graph source for the Lean project.
The bootstrap deliberately supplies a lightweight scaffold and a fail-closed semantic-ID validator. A later infrastructure pass may install and publish the full `leanblueprint` web/PDF site.

Open nodes are marked `not ready`; they are never labelled `leanok` merely because a metadata constructor exists.
