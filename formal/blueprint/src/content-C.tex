\section{Finite operator algebra and formal firewalls}

Reviewer C formalizes the stable finite-dimensional operator layer at the
bootstrap source lock.  The central registered declaration is
\texttt{OPERATOR.XI.PICK\_ORDER3}; its Lean theorem is deliberately classified
\emph{proved conditional}.  The corrected published high-zero verification,
the source lock, grouped local $C^2$ convergence, the orbit convention, the
multiplicity residual, the reciprocal-square tail budget, and repeated-node
reduction all occur as explicit hypotheses.  Compilation of the theorem does
not convert any of those hypotheses into a proved analytic input.

\subsection{Pick determinants}

For $F(x)=x p(x^2)$, the finite Pick entry is
\[
  H_{ij}=\frac{x_i p_i+x_j p_j}{x_i+x_j}.
\]
The two-point determinant and the reviewed three-node factorization are proved
by exact field algebra.  The three-node determinant is the product of the
second divided differences of $1/p$ and $tp$, multiplied by the positive
Vandermonde prefactor.  The Lean declarations are
\lean{RiemannFormal.Operator.two_point_pick_identity} and
\lean{RiemannFormal.Operator.three_node_pick_determinant_identity}.

The finite quadratic-form layer distinguishes positive semidefiniteness from
positive definiteness.  An exact LDL congruence proves that a positive first
pivot, a positive leading two-by-two minor, and a nonnegative determinant imply
PSD.  Strict determinant positivity is required for PD.  A zero-matrix fixture
proves that PSD cannot be silently strengthened to PD.

\subsection{Reciprocal concavity}

For a second-order jet $f$, put
\[
  \mathcal E(f)=f f''-2(f')^2.
\]
The cross term for a positive sum has an exact square certificate, yielding
finite positive-sum closure.  The off-line orbit defect, the zero-curvature
critical orbit, one-orbit reserve absorption, finite reserve allocation, and a
finite reciprocal-square tail budget are formalized separately.  The grouped
actual-Xi conclusion remains conditional on the named analytic inputs.

\subsection{Operator no-go theorems}

The trusted refutation modules kernel-check minimal counterexamples showing:
\begin{itemize}
\item a positive Schur complement cannot repair an existing negative direction;
\item an $\ell^2$ coefficient budget does not imply coherent operator contraction;
\item small negative inertia does not bound a positive current;
\item the hyperbolic pole block is not a sum of two positive squares;
\item positive excess does not orient an arbitrary square root;
\item a scalar diagonal does not determine the full polarized Gram matrix.
\end{itemize}
The heat module contains only a finite displacement/parity fixture.  It does
not claim the full Bohr--Weyl uniform-center theorem.  The Q4 module contains
only finite Hermite, filter, and determinant identities; SACF, UOSACF,
fixed-center heat, and the constant-four frontier remain unproved.
