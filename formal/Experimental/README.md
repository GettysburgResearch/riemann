# Experimental formalization work

Incomplete Lean work may live here temporarily, but this directory is excluded from the default `RiemannFormal` build, comparator solutions, release modules, and trusted formal-status registry.

A declaration leaves `Experimental/` only after its exact scientific statement is source-locked, its trusted target is sorry-free, and its formal status has been independently reviewed.
