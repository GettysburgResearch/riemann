## Purpose

Attempt the requested direct proofs of XD and CV against the latest live graph,
and freeze the exact source-faithful disposition.

```text
base main:
677203992eb0168920365ee45ae9db76bfa97dcf
```

**RH remains unproved.**

## Collision-safe publication provenance

This is a namespace-only migration of the verified original `T-102100`
packet to the collision-free `T-103200` family.  The source archive has
SHA-256

```text
f750f416dbfe5ad4e03bac2cef3ae879d2567b756840f6678785d38d211c48bc
```

Own claim, gate, schema, path, and replay identifiers were remapped exactly as
recorded in `integration/2026-08-21/t103200-namespace-migration.json`.
External PR and claim references are unchanged.  The source archive's outer
publisher was not imported because it referenced a patch absent from the
archive; the verified mathematical content is published directly and locked
by `T103200_CONTENT_SHA256SUMS`.

## XD

The previous matrix compared different kernels.  This packet rebuilds
largest-prime and Vaughan coordinates on the same extra-notched kernel `K1` and
proves their terminal gates equivalent modulo `L1(dX/X)`.

It also proves that every fixed notch leaves a power-sized regional prime
carrier.  Carrier recombination must occur before any norm.

## CV

Corrected adaptive squaring has a noncompact inactive tail.  A moving cutoff
changes the literal source by a shifted critical packet, not by its scalar
owner-budget jump.

The transfer is identified exactly with the activation-wavelet channel of the
joint quadratic-wavelet matrix.

## Replay

```bash
python3 experiments/X-103200-xd-cv-carrier-transfer/verify.py \
  --output /tmp/t103200-verification.json
cmp /tmp/t103200-verification.json \
  experiments/X-103200-xd-cv-carrier-transfer/results/verification.json
sha256sum -c T103200_CONTENT_SHA256SUMS
```

```text
PASS_T103200_XD_CV_CARRIER_TRANSFER_HARDENING
8a6f1579f6c63f3c592299218ce50ec60a5de7583f8b67b6bd04aa7ff5391e50
```

## Boundary

```text
CPXD103200   open / RH-equivalent
SCTV103210   open / RH-equivalent
RH           unproved
```
