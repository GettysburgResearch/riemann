# T-104640 — Source-to-inner-factor completion attempt

This packet records one direct attempt to prove the final model-space Gram
estimate in the self-contained \(>90\%\) Xi programme.

## Outcome

The pass did **not** prove more than ninety percent.

It found two binding corrections:

1. the fractional-current residual is equivalent to the shallow
   canonical-correlation charge up to the gamma tail already entered in the
   budget;
2. the seven-grade projected Krylov family contains the exact primal minimizer
   at grade zero, because
   \(Z_{0,H}=P_{K_A}P_{K_B}M_H\).

Thus the advertised finite source-Gram bridge has already inserted the unknown
inner projection.  The higher six grades supply no source-to-inner leverage.

An exact frozen-carrier countermodel based on

\[
F_n(t)=c+\cos(nt)
\]

satisfies the complete `L-104637` Bézout source identity with zero residual,
yet its physical quotient has canonical charge two per period while its
nontrivial pole height tends to zero.

## Retained and withdrawn

```text
self-contained alpha_5 > 49/50 ledger       RETAINED
fractional Calderón identity                 RETAINED
frozen degree-six source transport           RETAINED
variable-carrier source error o(N)           RETAINED
fractional residual as weaker source gate    WITHDRAWN
seven-grade source-to-inner bridge           WITHDRAWN
universal source-to-inner promotion          REFUTED
XIMICRO104640                                 OPEN
more than ninety percent                     UNPROVED
RH                                            UNPROVED
```

The live theorem is now the literal Xi microscopic charge: control the forced
unmatched endpoint index together with the matched phase/nonnormality charge at
the exact budget.  This is equivalent to the previously named shallow
canonical-correlation/fractional-current gates, not a consequence of the
finite source module.

## Replay

```bash
python -B experiments/X-104640-source-inner-factor-firewall/verify.py \
  --output experiments/X-104640-source-inner-factor-firewall/results/verification.json

python -B -m unittest discover \
  -s experiments/X-104640-source-inner-factor-firewall/tests \
  -p "test_*.py" -v
```

Expected:

```text
PASS_T104640_SOURCE_INNER_FACTOR_FIREWALL
afeb3d1091ce59a838821a7fd75112e7414a88e44f8819608989c6e5776842ff
checks=19
more_than_ninety_percent_established=false
rh_established=false
```
