# T-105470 — F1 dyadic-cell and one-Hardy-primitive frontier

This is the second checkpoint of the present ownership pass on durable draft
PR #730.

Frozen inputs:

```text
PR #730  77c4f9e24ead1a89678ffdb975275b914c709d70
PR #719  4146f81e7237d41e2e4a0cb1737511266683e980
PR #751  98af0db6ec7f77d6333a77a3dac53c4698852f43
```

The earlier claimed checkpoint-four commit

```text
512ff17dd7880313694c20372ca2b84ad2a5c330
```

is present remotely and remains in the ancestry.

`T-105460` reduced the F1 route to the total variation of one explicit
reflection-odd Hodge current.  This packet removes the remaining continuous
kernel geometry exactly.

For the fixed derivative-outer kernel `K_L`, every unit integer cell is affine
in `sqrt(X)`:

```text
H_K(X) = A_m + B_m sqrt(X),      m < X < m+1.
```

Its logarithmic L1 mass has an exact closed formula and is uniformly equivalent
to the two endpoint values of the cell.

For physical coefficients `a_n`, define

```text
P(x) = sum_{n<=x} a_n,
Q(x) = sum_{n<=x} a_n/sqrt(n),
W(x) = 2P(x) - sqrt(x) Q(x),
Delta_2 = (I-S)^2 (I-sqrt(2) S),    (Sf)(x)=f(x/2).
```

Then the exact right-endpoint identity is

```text
H_K(m+) = 4 Delta_2 W(m).
```

The left/right endpoint discrepancy is the finite arithmetic jump filter

```text
H_K(m+) - H_K(m-) = 4 Delta_2 a(m).
```

The inherited source-diagonal and equal-product energy makes the weighted sum
of this jump filter `M^(-1/2+o(1))` on a dyadic horizon.  Consequently the
continuous F1 variation gate is equivalent to the single discrete Hardy gate

```text
F1HARDY105470:
  sum_{M<=m<=2M} |Delta_2 W(m)|/m = M^(o(1)).
```

No midpoint or one-sample-per-cell shortcut is valid: an affine cell can vanish
at any prescribed interior sample while retaining positive logarithmic L1
mass.

Scientific status:

```text
unit-cell affine normal form                       PROVED EXACT
closed logarithmic cell integral                   PROVED EXACT
two-endpoint norm equivalence                      PROVED UNIFORMLY
one-Hardy-primitive factorization                  PROVED EXACT
right/left jump filter                             PROVED EXACT
weighted jump ledger                               INHERITED CLOSED
continuous F1VAR <=> discrete F1HARDY              PROVED EXACT MODULO CLOSED LEDGER
one-interior-sample shortcut                       REFUTED EXACTLY
F1HARDY105470                                      OPEN / RH-BEARING
Riemann Hypothesis                                 UNPROVED
```

## Replay

```text
PASS_T105470_F1_DYADIC_HARDY
7241f2c46f70d02c45f0344e20144f5788d6a2302cccbae9c66867000d3400f9

finite checks:       186,004
hostile mutations:       15
```

The retained result object records the proof-object checksum and all finite
checks.  The replay authenticates the algebraic localization and its hostile
firewalls.  It does not prove `F1HARDY105470`, `BCI102990`, or RH.
