# T-105470 — Corrected F1 dyadic Hardy frontier

For the bounded derivative-outer density,

\[
H_K(X)=\sum_na_nK_L(X/n)
\]

is affine in \(\sqrt X\) on every integer cell. With

\[
W(x)=2\sum_{n\le x}a_n-\sqrt x\sum_{n\le x}\frac{a_n}{\sqrt n},
\qquad
\Delta_2=(I-\mathsf S)^2(I-\sqrt2\,\mathsf S),
\]

one has exactly

\[
H_K(m+)=4\Delta_2W(m).
\]

The cell \(L^1\) primitive, two-endpoint equivalence and jump ledger are exact.
Thus

```text
F1HARDY105470:
  sum_(M<=m<=2M)|Delta_2 W(m)|/m = M^(o(1))
```

is the direct bounded-current gate.

The historical equivalence with the differential reflection variation is
superseded. The corrected relation is one-way:

```text
F1VAR105460 = REFSIG106150
  -> F1HARDY105470
  -> RH.
```

See `L-105490--L-105491` and `T-105490`.

```text
integer-cell and Hardy discretization      PROVED EXACT
bounded-current L1 = F1HARDY               PROVED
reflection -> F1HARDY                      PROVED
F1HARDY -> reflection                      NOT CLAIMED
F1HARDY105470                              OPEN / RH-BEARING
Riemann Hypothesis                         UNPROVED
```
