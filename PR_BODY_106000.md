## Purpose

Continue one dedicated execution PR joining three active programmes:

- **#743** — critical scale–phase/common-mother `CV/XD` and physical restriction;
- **#736** — Dirichlet-character completion of the canonical detector;
- **#737** — function-field mirror.

The branch is stacked on the live PR #719 arithmetic programme and remains the
single workspace for later passes on this hybrid family route.

The programme takes seriously the possibility that the missing theorem is a
moment theorem for an `L`-function family not visible from zeta alone, or a
geometric trace law suggested by modularity, Hasse--Weil `L`-functions,
arithmetic geometry and systematic exploration of new examples.

**The Riemann Hypothesis remains unproved.**

## Current source lock

```text
canonical main:       573eb6aa42c3d9469462c91c6b3ddfb8ab36d77f
stacked parent:       PR #719
parent head audited:  c2e82cfdd254a478731f005b3d83b49d3e1e33ea
parent frontier:      HBCQDSP102888
head branch:          research/gpt56-pro/106000-cvxd-lfamily-hybrid-moments
```

The parent now uses a dyadic-frozen Vaughan cutoff and a horizon-safe pair
gauge. Its Type-I row is power-small and its former largest-two
smooth-boundary row is absent. The sole arithmetic source is the coherent
balanced distinct-product current `HBCQDSP102888`.

## Checkpoint 1 — auxiliary completed Dirichlet family

For every odd auxiliary prime `ell != 67`, the packet constructs

```text
F_(chi,ell)
 = (I-ell^(-1/2)S_ell)
   (I-chi(67)67^(-1/2)S_67)
   sum_n mu(n)chi(n)n^(-1/2) Phi_*(X/n).
```

Its principal member is coefficient-exactly the native marked-67 common-mother
detector, and its nonprincipal members contain reciprocal `L(s,chi)` with only
zero-safe finite local factors.

Complete character orthogonality converts

```text
P c^2 versus Q d^2
```

into

```text
QP^(-1) nonsquare mod ell: exact zero;
QP^(-1) square mod ell:    c=+/-tau d mod ell.
```

Quadratic characters erase the core, while general characters retain it through
`chi^2(c)`. Exact separation alone pays family dimension, so auxiliary
principal leverage remains a real obligation.

## Binding order correction

Ramified completion restores the **full Möbius source**. It cannot be quoted
after an arbitrary residual projection. The valid order is

```text
full source
 -> character twist
 -> ramified completion
 -> full carrier recombination
 -> Wick / owner / dyadic-frozen Vaughan functor
 -> balanced residual
 -> positive family moment.
```

`R-106001` proves the opposite order false; `L-106004` gives the corrected
commuting diagram and exact principal recovery of `HBCQDSP102888`.

## Checkpoint 2 — exact square-phase Gauss--Mellin identity

For an odd prime `p`, a Hilbert-valued square-supported packet satisfies

```text
sum_(h!=0) ||F_h||^2
 = (p+1)/(p-1) ||F_0||^2
   + 2p/(p-1) sum_(eta even, eta!=1) ||M_eta||^2.
```

Therefore

```text
||F_0||^2 <= (p-1)/(p+1) sum_(h!=0)||F_h||^2.
```

The coherent nonzero phase sum equals `-F_0`. Generic Cauchy would pay `p-1`;
the exact square-phase geometry gives a strict factor below one. The theorem
tensorizes across any selected owner moduli.

For a clean packet

```text
N=pq a^2,
M=rs b^2,
```

the sharpened bound is

```text
|C_(P,Q)|
 << 1/sqrt(pqrs)
    sqrt(1+rho/A)
    sqrt(1+pi/B),
```

for any opposite owners `rho in {r,s}`, `pi in {p,q}`. Choosing the smaller
opposite owners gives `rho=s`, `pi=q`. All four owner weights are retained; no
phase-cardinality factor remains.

## Checkpoint 3 — a source-paid owner-conductor L-family

The multiplicative change of basis is

```text
eta=chi^2,
chi(P a^2)=chi(P)eta(a).
```

As `chi` ranges modulo an opposite owner prime, `eta` ranges over the even
Dirichlet characters with multiplicity two. The owner-excluded core has

```text
sum_((n,pq)=1) mu(n)eta(n)n^(-2s)
 = L(2s,eta)^(-1)
   /[(1-eta(p)p^(-2s))(1-eta(q)q^(-2s))].
```

The principal and quadratic square roots both map to the native untwisted core.
Together they contribute coefficient `(rho+1)/(rho-1)>1` to the positive
owner-conductor moment. Principal leverage is therefore already paid by the
literal source at this local interface.

A nonquadratic-only estimate is insufficient: deleting the quadratic root
deletes most of the native leverage, while treating it as an independent core
oscillation contradicts the exact square map and the existing reciprocity
firewall.

## Function-field mechanism

Over `F_Q`, the same identity is the exact Kummer--Artin--Schreier Fourier
transform of the square map on `G_m`. It uses finite-field character
orthogonality, not geometric RH.

The open geometric target is to control the incomplete degree-restricted and
Möbius/Vaughan-weighted Kummer traces coherently as owner irreducibles vary,
with conductors, resonant strata and memberwise-versus-average scope explicit.
The desired output is a precise number-field hybrid character or exponential-
sum theorem, not a formal transfer from known function-field RH.

## Current conclusion-facing theorem

Define

```text
SOCM106020:
  the source-weighted coherent moment of the surviving short-core
  horizon-safe balanced packets, expressed in the complete even
  owner-conductor character basis, is subpower.
```

The inherited coherent phase theorem already closes the weighted long-core
sector. Then

```text
SOCM106020
 -> HBCQDSP102888
 -> derivative common-mother subpower negative mass
 -> fixed Mellin--Landau consumer
 -> RH.
```

The function-field discovery target is `FFSOCM106023`.
Neither target, `HBCQDSP102888`, nor RH is proved.

## Exact replays

```text
PASS_X_106000_CVXD_LFAMILY_SQUARECLASS_MOMENTS
exact_checks=376476
proof_object_sha256=a23b1c5a739a5634593df8c6ec750a857dd8690e79c31fc8b195f0859c2786d1

PASS_X_106020_SQUARE_PHASE_OWNER_CONDUCTOR_FAMILY
exact_checks=6306
proof_object_sha256=5bc623c12b447cde059c5a2ff77abde3cdce1b5634e1a6d11836f14f1472eac9
```

The replays check exact finite algebra only. They do not evaluate zeta, any
Dirichlet `L`-function, a Frobenius matrix, the coherent moment, or RH.

## Exact boundary

```text
auxiliary Euler-completed family                   PROVED EXACT
character collision-line reduction                 PROVED EXACT
full-source/residual order correction               PROVED EXACT
horizon-safe balanced residual adapter              PROVED EXACT
square-phase Gauss--Mellin identity                 PROVED EXACT
phase-cardinality removal                           PROVED EXACT
all-owner-weight fixed-packet estimate              PROVED
owner-conductor even reciprocal-L family            PROVED EXACT
local source-paid principal leverage                PROVED EXACT
Kummer--Fourier finite-field mechanism              PROVED EXACT
weighted long-core coherent sector                  INHERITED CLOSED
SOCM106020                                          OPEN / RH-BEARING
FFSOCM106023                                        OPEN / EXPLORATORY
HBCQDSP102888                                       OPEN / RH-BEARING
Riemann Hypothesis                                  UNPROVED
```

This remains the single continuation PR for the three linked programmes.
