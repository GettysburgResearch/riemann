## Purpose

Open one dedicated execution PR joining three active programmes:

- #743 — critical scale–phase/common-mother `CV/XD` and physical restriction;
- #736 — Dirichlet-character completion of the canonical detector;
- #737 — function-field mirror.

This branch is stacked on the live PR #719 arithmetic frontier and will remain
the workspace for subsequent passes on the hybrid family moment.

The programme takes seriously the possibility that the missing input is a
moment theorem for a family not visible from zeta alone, or a geometric trace
law suggested by modularity, arithmetic geometry, and systematic exploration
of new `L`-functions. The purpose of the first pass is therefore to construct
one exact family containing the native zeta detector, not merely to attach
family language to the same one-function estimate.

**The Riemann Hypothesis remains unproved.**

## Frozen start

```text
main:              573eb6aa42c3d9469462c91c6b3ddfb8ab36d77f
arithmetic parent: PR #719
parent head:       20e6bc5d961f00818fac86c9252e2535c5d9821a
branch:            research/gpt56-pro/106000-cvxd-lfamily-hybrid-moments
```

## Why this family

The current PR #719 obstruction is a coherent sum over clean semiprime
squareclasses

```text
N=P c^2,
M=Q d^2.
```

Quadratic characters are insufficient: they erase the square core and the
four-owner Legendre product collapses by reciprocity. The full character family
behaves differently:

```text
chi(P c^2)=chi(P) chi(c)^2.
```

All but the principal and quadratic channels retain a nontrivial core phase.

## Exact Euler-completed family

For every odd prime `ell != 67`, the packet constructs

```text
F_(chi,ell)
 = (I-ell^(-1/2)S_ell)
   (I-chi(67)67^(-1/2)S_67)
   sum_n mu(n)chi(n)n^(-1/2) Phi_*(X/n).
```

Its Mellin transform is

```text
PhiHat_*(z)
(1-ell^(-s))(1-chi(67)67^(-s))/L(s,chi),
s=z+1/2.
```

For the principal character,

```text
L(s,chi_0)=zeta(s)(1-ell^(-s)),
```

so the completed principal member is **exactly**

```text
PhiHat_*(z)(1-67^(-s))/zeta(s),
```

the native common-mother detector. Every finite factor is zero-free in the
open right half-plane.

## Exact family moment

Character orthogonality converts a clean owner/core pair into

```text
P c^2 = Q d^2 mod ell.
```

Therefore:

```text
QP^(-1) nonsquare mod ell:
  the owner pair vanishes exactly;

QP^(-1) square mod ell:
  c = +tau d or -tau d mod ell.
```

The complete physical correlation becomes two linear core-collision lines,
with all inherited nonzero owner/core phases retained. The equal-product
diagonal is already subpower by PR #719 `L-102883`.

## Exact finite-field mechanism

For a finite field of odd cardinality `Q`,

```text
sum_(c!=0) chi(c)^2 psi(a c)
```

has modulus `sqrt(Q)` whenever `chi^2` is nontrivial. Exactly two channels,
principal and quadratic, are exceptional.

On a collision line, the complete additive phase sum is

```text
Q-1  on an explicit resonance;
-1   otherwise.
```

This is the function-field prototype of the missing hybrid moment.

## Binding no-go

Exact owner separation alone pays the family dimension. If `K` owner
squareclasses are made orthogonal, the family feature space has dimension at
least `K`, and positivity alone pays that factor when extracting the principal
member.

Likewise, congruence density `1/ell` is not cancellation: coefficients
concentrated on one collision line saturate it.

A viable proof must use nonquadratic core phases, inherited additive phases,
a genuine amplifier, trace formula, or geometric moment theorem.

## New frontier

For a predeclared weighted family and amplifier, define

```text
PLEV106000:
  principal leverage Lambda_X >= X^(-o(1));

HCLM106000:
  integrated amplified completed-family energy = Y^o(1).
```

Because every principal completed member is the same native detector,

```text
PLEV106000 AND HCLM106000
 -> native common-mother L2 energy is subpower
 -> native negative mass is subpower
 -> fixed Mellin-Landau consumer
 -> RH.
```

This implication is exact. It is a family-moment replacement for the open
`BQSP102870` estimate, not a claim that `BQSP102870` itself has been proved.
Neither premise is proved.

The function-field target `FFHCLM106003` is to identify the actual trace
sheaf/monodromy mechanism for the incomplete Möbius/Vaughan collision lines
and export a precise number-field exponential-sum theorem. It is exploratory,
not a transfer from known function-field RH.

## Exact replay

```text
PASS_X_106000_CVXD_LFAMILY_SQUARECLASS_MOMENTS
exact_checks=376476
proof_object_sha256=a23b1c5a739a5634593df8c6ec750a857dd8690e79c31fc8b195f0859c2786d1
```

The replay checks exact integer, rational and finite-field algebra. It does not
evaluate an `L`-function or prove the open moment.

## Boundary

```text
Euler-completed family / native principal member   PROVED EXACT
reciprocal-L transform and pole retention          PROVED EXACT
character moment / squareclass collision lines     PROVED EXACT
nonquadratic core visibility and Gauss prototype   PROVED EXACT
function-field algebraic mirror                    PROVED EXACT
uniform exact-separation shortcut                  REFUTED
PLEV106000                                         OPEN
HCLM106000                                         OPEN / RH-BEARING
FFHCLM106003                                       OPEN / EXPLORATORY
Riemann Hypothesis                                 UNPROVED
```

This is the single continuation PR for the three linked programmes; later
passes should stay on this branch.
