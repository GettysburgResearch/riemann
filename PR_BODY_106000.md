## Purpose

Continue one dedicated execution PR joining three active programmes:

- **#743** — critical scale–phase/common-mother `CV/XD` and physical restriction;
- **#736** — Dirichlet-character completion of the canonical detector;
- **#737** — function-field mirror.

This branch remains the single workspace for the route. It tests the motivating
possibility that the missing theorem is a moment theorem for an `L`-function
family or a geometric trace law visible only after zeta is placed in a larger
source-faithful family.

**The Riemann Hypothesis remains unproved.**

## Source lock

```text
main:             573eb6aa42c3d9469462c91c6b3ddfb8ab36d77f
parent PR:        #719
parent head:      c2e82cfdd254a478731f005b3d83b49d3e1e33ea
parent frontier:  HBCQDSP102888
```

The parent now has a dyadic-frozen Vaughan cutoff, horizon-safe pair gauge,
power-small Type-I row and no smooth-boundary row. Only the balanced
distinct-product current remains.

## Exact corrections and local theorems

The auxiliary completed Dirichlet family still has the native marked-67 common
mother as its exact principal member. But `R-106001` proves that ramified
completion must be applied to the full Möbius source **before** carrier, owner
and Vaughan residual selection. `L-106004/T-106001` give the corrected ordered
source functor.

For every odd phase prime `p`, the square-supported physical packet satisfies

```text
sum_(h!=0)||F_h||^2
 = (p+1)/(p-1)||F_0||^2
   + 2p/(p-1) sum_(eta even, eta!=1)||M_eta||^2.
```

Hence

```text
||F_0||^2 <= (p-1)/(p+1) sum_(h!=0)||F_h||^2.
```

Generic Cauchy would pay `p-1`; the exact square-phase geometry gives a strict
contraction. This removes all phase-cardinality loss from the fixed owner
packet and retains every literal owner coefficient.

The same identity is:

- an exact even Dirichlet-character moment `eta=chi^2`;
- a source-paid local principal embedding, because the principal and quadratic
  roots combine with coefficient `(p+1)/(p-1)>1`;
- a sharp local physical-occupancy theorem with operator norm squared
  `(p-1)/(p+1)`;
- the Kummer–Artin–Schreier Fourier transform of the square map over finite
  fields.

Local squareclass occupancy is therefore closed. Global `BPOE103300` remains
open because different owner packets can still collide after physical
identification.

## Exact mollified reciprocal-`L` normal form

For owner pair `P=pq`, opposite owner conductor `rho`, and even character
`eta`, define

```text
Z_(P,eta)(w)
 = L(w,eta)(1-eta(p)p^-w)(1-eta(q)q^-w),

M_(U;P,eta)(w)
 = sum_(n<=U,(n,P)=1) mu(n)eta(n)n^-w.
```

The balanced fixed-cutoff Vaughan row is exactly

```text
B_(U;P,eta)(w)
 = (1-M_U Z)^2/Z
 = Z^-1 - 2M_U + M_U^2 Z.
```

The physical source is the finite shell projection of this ratio; that
projection may not be discarded.

With `kappa(u)=K_L(e^u)`, finite-shell Mellin–Plancherel gives

```text
Vhat_(U;P,eta)^S(t)
 = kappahat(t) P^(-1/2-it) B_(U;P,eta)^S(1+2it).
```

The derivative kernel has `kappahat(0)=0`, cancelling the principal ratio’s
possible simple pole at `t=0`.

For fixed conductor `rho`, owner pairs must be split by

```text
sigma = kappa_rho(P) in {+1,-1}.
```

Inside each sector the sharp local leverage survives coherently; recombining
the two sectors costs only an absolute factor. Aggregating them before the
transform destroys the contraction.

## Explicit current frontier

For every `(rho,sigma,eta)`, retain the literal owner, shell, cutoff and source
coefficient and define the spectral amplitude

```text
A_(rho,sigma,eta)(t)
 = sum_i omega_i chi_(rho,eta)(P_i) P_i^(-1/2-it)
   B_(U_i;P_i,eta)^(S_i)(1+2it).
```

The exact positive moment separates into

```text
PCM106030:
  eta=1 principal/quadratic-root mollified owner moment is subpower;

NEM106030:
  eta!=1 even-character mollified family moment is subpower.
```

The conclusion graph is

```text
PCM106030 AND NEM106030
 -> explicit owner-conductor moment is subpower
 -> SOCM106020
 -> HBCQDSP102888
 -> RH.
```

Neither analytic moment, `SOCM106020`, `HBCQDSP102888`, nor RH is proved.
A source-blind large sieve is insufficient because different owner pairs in
the same quadratic class survive on the same collision lines.

## Exact replays

```text
PASS_X_106000_CVXD_LFAMILY_SQUARECLASS_MOMENTS
checks=376476
sha256=a23b1c5a739a5634593df8c6ec750a857dd8690e79c31fc8b195f0859c2786d1

PASS_X_106020_SQUARE_PHASE_OWNER_CONDUCTOR_FAMILY
checks=6426
sha256=63f7cbba47dea60cf308b26e46162b4dc23f54798f7ce408424bf7f1058f4872

PASS_X_106030_MOLLIFIED_OWNER_CONDUCTOR_NORMAL_FORM
checks=38780
sha256=48c20069952efd0ec21e3b23a38c0b78af07e3b010e0dbb346e32014e898398b
```

The replays authenticate exact finite algebra only.

```text
full-source completion order                       CORRECTED / PROVED
square-phase Gauss–Mellin identity                 PROVED EXACT
phase-cardinality removal                           PROVED EXACT
local squareclass physical occupancy                PROVED SHARP
owner-conductor reciprocal-L family                 PROVED EXACT
balanced Vaughan = projected mollifier defect / L  PROVED EXACT
finite-shell Mellin–Plancherel                      PROVED EXACT
owner quadratic-class split                         PROVED EXACT
PCM106030                                           OPEN / RH-BEARING
NEM106030                                           OPEN
SOCM106020                                          OPEN / RH-BEARING
BPOE103300                                          OPEN / RH-BEARING
HBCQDSP102888                                       OPEN / RH-BEARING
Riemann Hypothesis                                  UNPROVED
```
