## Purpose

Continue one dedicated execution PR joining programmes **#743**, **#736**, and
**#737** through the CV/XD, Dirichlet-family, temperature--Kummer, and
function-field coordinates.

**The Riemann Hypothesis remains unproved.**

## Exact source state

```text
canonical main:       573eb6aa42c3d9469462c91c6b3ddfb8ab36d77f
stacked parent PR:    #719
parent head merged:   60285de21fafdd1b3c185ddd19b57191a41c08dd
merge commit:         45e41f8ec19bd2ec2e37e595686b2e7e605f7d47
execution PR:         #751
corrected source lock:
  integration/2026-08-25/t106071-quadratic-owner-residue-source-lock.json
```

The latest parent additions are the primewise-gauge packet
`L-102901--L-102904`, `R-102873`, and `T-102930`.  No HBC or detector input
used by this branch changed.

## Earlier exact layers retained

```text
full-source ramified completion order                PROVED EXACT
square-phase Gauss--Mellin family                    PROVED EXACT
sharp local squareclass occupancy                    PROVED
mollified reciprocal-L normal form                   PROVED EXACT
composite tensor Kummer frame                        PROVED EXACT
twisted complementary-temperature flatness           PROVED EXACT
quadratic roots = owner Kummer charts                PROVED EXACT
temperature tangent = charted native prime current  PROVED EXACT
two-phase collision-line contraction                 PROVED SHARP
```

## Scale-matched source geometry

On a stopped-Vaughan core block `c in [B,8B)`, `L-106070` chooses three palette
primes, all different from `67`, satisfying

```text
16B < ell_j < 256B.
```

A semiprime owner contains at most two palette primes.  A disjoint **linear**
owner colouring selects one absent prime, so the complete coloured block is
coefficientwise unramified and its principal character is literally the native
source.

For fixed owners `P,Q`, every surviving character collision

```text
P c^2 = Q d^2 mod ell
```

is either empty or one of two partial matchings

```text
c = +/- tau d mod ell.
```

Thus repeated-core occupancy inside one fixed owner-pair line is removed
exactly.  The exact block energy

```text
E_(P,B) << X^(o(1))/(P B)
```

pays both the quadratic diagonal conductor cost and the quartic matched-pair
Hilbert--Schmidt tensor.

## Binding retraction

The first versions of `L-106073` and `T-106070` incorrectly used that quartic
tensor to bound the quadratic character-family moment.  `R-106071` proves the
adapter false by homogeneity and an explicit many-owner one-residue fixture.

```text
first L-106073 HBC closure             RETRACTED
first T-106070 full RH proposal         RETRACTED
Riemann Hypothesis                      UNPROVED
```

The exact family object is instead

```text
Q_(B,A)
 = sum_(r != 0) || sum_(P c^2 = r mod ell) Z_(P,c) ||^2.
```

## Corrected frontier

`L-106074` proves exact character Parseval and principal inclusion:

```text
1/(ell-1) sum_chi ||V_chi||^2 = Q_(B,A),
||R_(B,A)||^2 <= (ell-1) Q_(B,A).
```

The diagonal part of `ell*Q_(B,A)` is subpower.  If `nu_r` is the number of
distinct owners in residue cell `r`, `L-106075` proves

```text
||A_r||^2 <= 2 nu_r sum_(P c^2 = r) ||Z_(P,c)||^2.
```

Every cell with `nu_r=X^(o(1))` is therefore closed.  The sole new live packet
is

```text
HQORO106071:
  control ell*||A_r||^2 on cells containing a power-sized coherent family of
  distinct owner packets, with every source label retained exactly once.
```

The conclusion graph is

```text
HQORO106071
 -> QORO106074
 -> HBCQDSP102888
 -> RH.
```

`HQORO106071` remains open and is a finite-field block coordinate of the global
physical occupancy obstruction `BPOE103300`.

## Replays

```text
retained exact checks: 644145

X-106070: 166834 checks
  palette, unramifiedness, core matching, representation and quartic payment

X-106071: 7472 checks
  quadratic residue Gram, homogeneity counterexample, root count and
  low-crowding Cauchy
```

The replays do not prove `HQORO106071`, `BPOE103300`, the Mellin consumer, or
RH.  This PR remains a draft, with the failed closure preserved openly in the
provenance trail.