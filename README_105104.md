# Checkpoint 5 — multiplicity-aware jet-coherence transfer

Checkpoint base:

    eafd6d86055179e5b2fa9b3747ea740b8609ba64

This packet closes the real-variable multiplicity branch left open by
T-105103.

For a noncommon real critical point of odd order
\(r=\operatorname{ord}_c f'\), define

\[
\rho_c^{\mathrm{jet}}
=\frac{r!\,f(c)}{f^{(r+1)}(c)}.
\]

It is the leading \(w^{-r}\) principal coefficient of \(f/f'\). Its sign
classifies good versus wrong flat turns, and the leading coefficient of
\(f^2/(f'f'')\) is \((\rho_c^{\mathrm{jet}})^2/r\).

With \(R_{\mathrm{jet}}\) eligible turns, common derivative-order mass
\(\mathfrak m_{\mathrm{com}}\), and jet coherence
\(\mathfrak C_{\mathrm{jet}}\), the exact transfer is

\[
N_{\mathbb R}^{\mathrm{mult}}(f)
\ge
\mathfrak m_{\mathrm{com}}
+(2\mathfrak C_{\mathrm{jet}}-1)R_{\mathrm{jet}}-1.
\]

The full derivative multiplicity is

\[
N_{\mathbb R}^{\mathrm{mult}}(f')
=\mathfrak m_{\mathrm{com}}+R_{\mathrm{jet}}
+\Delta_{\mathrm{mult}}.
\]

Thus common mass transfers directly, odd noncommon events use jet coherence,
and all neutral/excess multiplicity remains explicit.

Ordinary contour residues cannot supply the new jet data. The exact pair
\(x^6\pm1/64\) has the same endpoint signs, derivative event manifest, and
zero ordinary first/second residue charges, yet has zero versus two real
roots. The leading jet carriers \(\pm1/384\) distinguish it.

Validation:

    PASS_T105104_JET_COHERENCE_REVERSE_ROLLE
    15/15 focused tests in normal and optimized Python
    digest bcd3ac0f644c49e47be6b2e019a57b2e7d2b8b6df14dfec97da854cb44b3bed6

Xi event proportions, multiplicity-defect control, a jet/global-observable
bridge, jet moment estimates, RCMV104530, and RH remain open.
