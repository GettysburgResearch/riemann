# T-101000 — Implication hypergraph for the live RH programme

This packet is an implication matrix, not another renamed scalar criterion.

It distinguishes:

```text
source-faithful producers;
structural reductions;
critical middle estimates;
Mellin/Mertens/Landau detectors;
RH-equivalent detector nodes;
binding no-go edges;
genuine conjunctive hyperedges.
```

The principal exact advances are:

1. critical activation-free variation implies the activation-zero envelope
   deficit by a positive Hardy operator;
2. the rough-largest-prime and balanced-Vaughan terminal gates are equivalent
   modulo absolutely integrable errors;
3. signed phase-Hasse packing is exactly the minimal-wavelet gate, not a third
   independent detector;
4. current proven-only arrows contain no path to RH;
5. the matrix computes the minimal open cut sets and the shortest honest
   conjunctive closure packets.

Scientific status:

```text
implication matrix / alias registry       proved exact at frozen SHAs
new translation theorems                 proved exact
current proven-only RH path               none
Riemann Hypothesis                        unproved
```
