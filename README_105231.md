# T-105231 — Weighted de Branges/Fourier spectral extinction

Status: **active checkpoint on PR #728; RH unproved**  
Created: 2026-08-23

## Exact new bridge

At one reverse--Rolle step, the actual multipole-weighted residue defect has
two simultaneous exterior-square realizations:

\[
\pi^2(NB-A^2)=\|u_a\wedge v_a\|_{\rm dB}^2
\]

and

\[
\rho_i=-\langle x,y_i\rangle_{\rm Fourier}.
\]

The new theorem constructs the explicit contraction carrying the weighted
Fourier pair bundle

\[
\bigoplus_{i<j}\sqrt{w_iw_j}(y_j-y_i)
\]

to the de Branges wedge. It yields

\[
NB-A^2
\le
N\Lambda_{2r}(0)
\|L_w^{1/2}KL_w^{1/2}\|_{\rm op},
\]

with

\[
L_w=W-\frac{ww^T}{N}.
\]

The exact `9/25` integrality threshold from `L-105222` therefore gives a
single conclusion-facing gate, `WSEG105231`.

## Entire-space simplification

Direct Clark-kernel series construct the two actual moment vectors in the
entire de Branges spectral subspace as soon as:

```text
the derivative is simple and real-rooted;
the weighted second residue moment is finite.
```

Canonical-product truncation and a separate Bézout exhaustion are not needed
on this stratum. The quantitative centered spectral estimate remains open.

## Firewall

Equal sample norms and equal uncentered trace can correspond to centered
energies `6` and `0`. The translation-overlap term `w^T K w` is therefore
load bearing; diagonal-only large-sieve estimates cannot close the theorem.

## Replay

```text
PASS_T105231_WEIGHTED_DEBRANGES_FOURIER_INTERTWINER
c94f91870107598a458e00e4e40107c2da7ffce9745db43e4895279593f533d0
```

Four focused unit tests pass in exact rational arithmetic.

## Boundary

```text
finite compound intertwiner              PROPOSED EXACT
direct entire Clark construction         PROPOSED EXACT
spectral-to-extinction implication       PROVED EXACT
WSEG105231 for fixed low Xi orders       OPEN / RH-BEARING
compact endpoint/multiplicity/winding    OPEN / RH-BEARING
Riemann Hypothesis                       UNPROVED
```
