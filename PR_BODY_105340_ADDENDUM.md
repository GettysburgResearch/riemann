## Checkpoint F — oriented shifted-ratio transfer (T-105340)

The shifted-family explicit-formula gate is replaced by one exact ratio

```text
M_alpha=(xi'-alpha xi)/(xi'+alpha xi).
```

For every analytic test primitive `Psi`, the alpha derivative of its oriented
zero divisor at zero is `-2` times the complete low-order Pick functional.
This remains valid at multiple critical points and cancels common xi factors.
The functional equation gives `M_alpha(1-s)=M_alpha(s)^(-1)`, so the two safe
lines fold exactly. On the right safe line the only arithmetic tangent is
`xi/xi'`.

Freezing the archimedean carrier gives the exact Dirichlet resolvent
`(L delta_1-Lambda)^(-*)`; on `sigma>sigma0>1` its omitted coefficient tail is
at most

```text
X^(-(sigma-sigma0))/(|L|-A_abs(sigma0)).
```

Thus shifted-family existence, branch tracking, and the reciprocal coefficient
tail are no longer open. The remaining record-bearing estimate is the direct
Wick-preconditioned safe-line trace/HS comparison with horizontal/taper errors.

The public record and RH remain unproved.
