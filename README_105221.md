# T-105221/T-105222 — additive square-defect descent

Checkpoint two on PR #731.

New exact results:

- wrong extrema are dominated by the quadratic residue defect
  `sum (1+lambda rho)^2`;
- the whole derivative-block loss is additive;
- coherence is the optimized form of this square defect;
- a finite source-matched interpolation removes every adjacent-derivative pole
  from the global contour observable;
- 90% and density one reduce to one combined boundary-flux estimate.

Concrete literature-instantiated target:

```text
K = 3
combined real square defect / N < 0.0173991107...
```

subject to the cited Rezvyakova entry theorem and all window/counting ledgers.
The interpolation boundary estimate is open. Density one and RH are not
established.
