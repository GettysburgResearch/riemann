# T104580 — Stationary horizontal minima and critical-value conservation

This packet weakens the fixed-order `Xi''' -> Xi''` target from a global
Laguerre sign to a sign sampled only at the real zeros of `Xi'''`.

## Exact results

```text
bilateral partition-function variance          PROVED EXACT
horizontal curvature = Laguerre profile        PROVED EXACT
positive-Fourier weighted finite-shift defect  PROVED EXACT
critical-only q -> alpha_2 conversion          PROVED EXACT
critical-value weighted majority               PROVED UNCONDITIONALLY
amplitude regularity -> count majority         PROVED EXACT
```

At a real zero `c` of `Xi'''`,

```text
L_2(c)>=0
iff
h=0 is a horizontal minimum of |Xi''(c-ih)|.
```

If this holds on a density `q`, then

```text
alpha_2 >= (2q-1) alpha_3.
```

The actual Xi function also satisfies

```text
sum_good |Xi''(c)| - sum_wrong |Xi''(c)|
  = (1/2) integral_R |Xi'''(t)| dt > 0.
```

## Open gates

```text
SHMIN104580  critical horizontal-minimum density q>1/2
CSAMP104580  source-faithful critical sampling with loss <1/2
AMPREG104580 critical-value amplitude regularity
```

No gate is claimed proved.  `alpha_2` has not been derived from Conrey's
`alpha_3` theorem in this packet, and RH remains unproved.

## Review order

1. `L-104537`
2. `R-104519`
3. `L-104539`
4. `L-104538`
5. `L-104540`
6. `L-104541`
7. `T-104580`
8. replay, report and handoff
