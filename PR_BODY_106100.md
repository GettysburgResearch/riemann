## Purpose

This is the single persistent execution PR joining:

- **#743** — CV/XD common mother and physical restriction;
- **#736** — Dirichlet-character/L-family completion;
- **#737** — function-field mirror.

The branch is scientifically synchronized to parent PR #719 at
`ec6635b4c7dcd08fe433b7ae7e1d9a8c9495dfcc`.

**The Riemann Hypothesis remains unproved.**

## Binding corrections

```text
T-106070 scale-matched closure       retracted by R-106071;
T-106080 minimum-owner closure        retracted by R-106080;
first T-106090 Q-indexed moment       superseded by R-106095.
```

The valid local reductions from each attempt are retained. No retracted theorem may be cited as an RH proof.

## Inherited parent repair

The current parent has already proved:

```text
critical Hodge class = squarefree Boolean source;
Boolean Type-I global transport;
equal- and one-sided-core absolute closure;
exceptional balanced-owner elimination;
literal physical-product phase packing;
arbitrary incidence-mask stability;
canonical equal-pair owner gauge;
owner/phase gauge decoupling.
```

The clean inherited frontier is `BCI102990`.

## Least-discrepancy triangularization

After exact common-core extraction, every live pair is

```text
N=P*g^2*c^2,
M=Q*g^2*d^2,
c,d>1,
(c,d)=1.
```

Orient it by the smaller of `P^-(c)` and `P^-(d)`. If

```text
ell=P^-(c)<P^-(d),
```

then

```text
ell divides N and not M;
the opposite reduced core is strictly ell-rough;
1=-sum_(h=1)^(ell-1)e_ell(-hM).
```

Every coprime two-sided interaction therefore has one exact triangular anchor/rough-tail coordinate.

## Correct fully amplified L-family

A fixed-Q theorem is only local. `R-106095` proves that separating Q and paying it by a scalar Cauchy weight cancels the reciprocal `1/Q` source energy and leaves a power-sized count of opposite-owner fibres.

The corrected member fixes only `g`, `ell`, and the owner quadratic class `sigma`, then sums **both** coherent dimensions before squaring:

```text
left:   every anchor (c,P,...);
right:  every opposite owner/core atom (Q,d,...).
```

For fixed `g,ell,sigma`,

```text
Z_tilde_(g,ell,sigma,h)(t)
 = sum_anchor conjugate(A_anchor(t))
     sum_(Q,d,...) b_(Q,d,...)(t)e_ell(-h*Q*g^2*d^2).
```

Inside one quadratic class, `Q*g^2=u_sigma*r_Q^2 mod ell`, so the multiplicative family sees the combined variable `r_Q*d`. Exact Gauss orthogonality gives

```text
sum_(h!=0)|Z_tilde_h|^2
 = (ell+1)/(ell-1)|Z_tilde_prin|^2
   + 2ell/(ell-1) sum_(eta even, eta!=1)|Z_tilde_eta|^2.
```

The correct source-dual moment weight is

```text
g^2*ell,
```

with no separate Q weight. The dual sum `sum_(g,ell)1/(g^2*ell)` is polylogarithmic.

## Diagonal progress and exact live gates

`L-106096` pays the **literal atomic diagonal** of the corrected moment. It does not claim that the whole same-anchor block is diagonal.

Only genuine cross-incidence terms remain. Define:

```text
FAPCX106100:
  the off-atomic-diagonal principal part of the fully amplified moment is Y^o(1);

FANEX106100:
  the off-atomic-diagonal nonprincipal even-family part is Y^o(1).
```

The exact implication is

```text
FAPCX106100 AND FANEX106100
 -> corrected fully amplified moment
 -> LDART106090
 -> BCI102990
 -> RH.
```

Both first premises remain open.

## Function-field target

`FFFA106100` asks for the corresponding fully amplified Artin–Schreier/Kummer moment over `F_q[T]`, with both owner and core variables summed before the family square, every constant/resonant constituent classified, and one exportable number-field trace or exponential-sum theorem identified.

## Current packet

```text
L-106090  least-discrepancy triangular incidence;
L-106091  fixed-Q exact rough-tail even-character family;
L-106092  fixed-Q automatic long-core estimate;
R-106095  opposite-owner dimension correction;
L-106095  fully amplified owner/anchor family;
L-106096  literal atomic diagonal is subpower;
T-106100  corrected conclusion-facing frontier;
X-106090  local finite replay;
X-106100  fully amplified correction replay.
```

Replays:

```text
PASS_X_106090_LEAST_DISCREPANCY_ROUGH_TAIL
checks=142924

PASS_X_106100_FULLY_AMPLIFIED_OWNER_ANCHOR
checks=11616
```

They authenticate finite incidence, Ramanujan, character-kernel, squareclass, source-dual and diagonal algebra. They do not prove `FAPCX106100`, `FANEX106100`, `BCI102990`, the Mellin consumer or RH.
