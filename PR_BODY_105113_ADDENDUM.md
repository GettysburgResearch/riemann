## Checkpoint 14 — buffered-selector shell averaging

Exact checkpoint base:
53c3a24b23335e7b4e77a703b2c3c224821ed749.

One complete outer actual-pole manifest can now freeze a strictly smaller
target core across a two-parameter family of intermediate rectangles.
Full zero congruences at every nontarget outer event make the weighted
carriers holomorphic in the buffer, leaving the fixed unnormalized charges

\[
q_1=-2\pi iA_0,\qquad q_2=2\pi iB_0.
\]

The exact contour average has \(+i\operatorname{sgn}(x)\) on the vertical
shell and \(-\operatorname{sgn}(y)\) on the horizontal shell.  Its absolute
Tonelli ledger gives one common good rectangle for both carriers for every
prescribed nonnegative weight pair.  The rectangle may depend on that pair;
no universal rectangle is claimed.  The inverse widths are coordinate
specific: \(1/\Delta_T\) for the vertical shell and
\(1/\Delta_\eta\) for the horizontal shell.

The exact cubic firewall records the raw normalized charge jumps

\[
P:\ -1/2\to-2/3,
\qquad
Q:\ 1/4\to7/36\to2/9.
\]

Complete-buffer selectors instead reduce the carriers to \(-F/(2z)\) and
\(F^2/(4z)\), restoring invariant charges \(-1/2\) and \(1/4\).

Exact replay:

    PASS_T105113_BUFFERED_SELECTOR_SHELL_AVERAGE
    18 focused tests in normal and optimized Python
    digest 44e0cffca9d4d680555287e8290cfa645d979dc76b064f0ab6914ace88c7d560

The finite identity retains the full shell cost of
\(W_1F/F'\) and \(W_2F^2/(F'F'')\).  Complete cofinal Xi manifests,
weighted Xi shell estimates, the positive signed first moment,
multiplicity-defect control, strict coherence, RCMV104530, and RH remain
open.
