# T-105109 quotient-edge frontier

This checkpoint isolates the factor left open after the optimal-selector and
Green–Gram ledgers.

With

\[
\mathscr L=F'/F,
\qquad
\mathscr A=\mathscr L^2+\mathscr L'=F''/F,
\]

the raw carriers are exactly \(1/\mathscr L\) and
\(1/(\mathscr L\mathscr A)\).  Because the optimal selectors have constant
boundary modulus, their weighted boundary suprema are exactly the selector
norm times the raw quotient norm.

The real-even zero-free family

\[
F_s(z)=\exp(z^2/2-z^4/(4s))
\]

keeps the target residues fixed and both optimal selector norms bounded, but
makes both weighted boundary suprema diverge as exterior critical points
approach the unit circle.  The full contour integrals remain fixed, so
phase-sensitive cancellation is left open.

Exact replay:

    python -B experiments/X-105109-log-derivative-edge-obstruction/tests/test_verify.py
    python -B -O experiments/X-105109-log-derivative-edge-obstruction/tests/test_verify.py

Expected:

    PASS_T105109_LOG_DERIVATIVE_EDGE_OBSTRUCTION
    17/17 focused tests in normal and optimized Python
    digest 54829e77cb2aa90192aabdd61f86ff045a04ba3549f82c75384bc9a4e08f2890

No heavy computation is used.  Xi collar/lower-margin estimates,
oriented-edge cancellation, cofinal Green–Gram control, strict coherence,
RCMV104530, and RH remain open.
