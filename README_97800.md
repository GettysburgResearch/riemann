# T-97800 — Growing-prime bilinear tail for the two literal rows

This add-only packet proves a uniform positive finite Euler sieve through a moving cutoff `z_*(X)~log X` and rewrites the remaining full two-row problem as one exact least-prime bilinear correlation with sharp normalization.

```text
FPCB23: Gamma_23(X)<=1 eventually
```

is equivalent to eventual two-row positivity on the frozen PR #579 consumer. It remains open and RH-bearing. RH is unproved.
