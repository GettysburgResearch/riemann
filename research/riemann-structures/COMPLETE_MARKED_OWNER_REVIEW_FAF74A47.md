# Independent review: complete marked owner pushforward

Reviewed scientific commit: `faf74a47dac33927d081a890f2b5a7ead48b869b`.

This review covers the six new native files: `COMPLETE_MARKED_OWNER_PUSHFORWARD.md`, `complete_marked_owner_pushforward.py`, its result and source JSON files, `marked_owner_pushforward_scout.py`, and `tests/test_complete_marked_owner_pushforward.py`. The main agent reports the frozen proof-object hash `8fa0e40d7ed45b7db82a417e9ac7929cdc0eff01c14c4d62ad0502fceba17671`, focused Ruff, producer write/check/optimized-check, and 13 normal plus 13 optimized tests passing.

The reviewer independently read the proof, producer, bounded scout and all 13 test definitions, checked the frozen file inventory, and read the eight exact historical source locks. No computation or test job was run by the reviewer. Execution results above are the main agent's reported results, not independent reruns.

## Corrected source finding

A substantive geometric adapter error was caught before the freeze and repaired. The geometric quotient of the four-root configuration by the two owner-pair swaps has rational points with nonsplit Frobenius-swapped pairs. It therefore does not count precisely the unordered pairs of rational roots used by the literal split-owner sum. Replacing it by an invariant projector would not repair that mismatch.

The frozen note uses the ordered configuration space and an explicit rational counting weight `1/4`. This is not described as an invariant projector or a quotient-scheme point count. For the integral ordered compact pushforward `T_(sigma,tau)`, the trace is `16 K_(sigma,tau)`: four class-indicator units and four ordered representatives per split owner pair. The virtual expression `81 T tensor T - 36 T` then has trace `16 J`, so the rational source observable and physical normalization carry the additional factor `1/16`. The frozen producer metadata and tests reflect those factors. The new `F_5` regression distinguishes the ten split rational-root pairs from the twenty squarefree monic quadratics, which also include ten irreducible quadratics. The repaired parent is consistent; the earlier quotient interpretation must not be reinstated in summaries.

## Mathematical assessment

No blocking error found in the repaired, explicitly restricted theorem. The source is the declared finite-field polynomial Boolean projection with all split degree-two owner pairs, distinct retained marks, all Boolean histories and the literal Wick diagonal. It is not asserted to equal the whole integer carrier current.

I independently reconstructed the four-distinct-owner partition formula from ordered edges. With the two quadratic-character row sums `alpha,beta`, cross sum `omega`, and available-root count `n`, the stated fourth mixed moment and the resulting four class counts are correct. The quadratic-character complete sum `-1` gives the finite excluded-mark reduction. The identity for the sum of squared class counts, and hence the class-summed Wick polynomial, retains the mixed row information rather than replacing the family by its completely class-forgotten linear count.

The Boolean history normalization checks: six surviving balanced histories per side, equal-pair share `1/15`, 36 bilateral histories per owner configuration, and `J=1296 K^2-36 K`. The source-dual and phase factors give the displayed physical prefactor; the corrected integral geometric parent requires its further division by 16. These are not interchangeable diagonals or alternative atomizations.

For the fixed `F_25` point with `a^2=2`, retained marks `0,1,2,3`, and `(lambda,rho)=(a,a+1)`, I checked the quadratic-character signs independently. The original and left-partially-Frobenius rows are `(-1,1,1)` and `(1,3,1)`. The resulting class counts and defect `-336420864` at `q=25` agree with the formulas. Character restriction over odd extensions preserves these signs, proving the cofinal defect by the written polynomial identity, not by extrapolating the finite fixture. Its sign is negative for every declared `n>=19`.

The coupled Kummer label survives the complete split-owner pushforward. This obstructs an isomorphism preserving the stated principal observable and compatible with total Weil structure under the prescribed left partial Frobenius on the common clean locus. It does not assert that every possible observable detects the defect. The full nonzero additive-parameter sum and completely class-forgotten linear count collapse as disclosed. With the physical normalization restored, this particular defect decays at the stated order; it is not a growing bad-moment lower bound.

## Producer and test assessment

The bounded producer authenticates the eight frozen source blobs before importing its owned scout. It compares primitive owner-pair enumeration, independent character-sign counting, sign-category falling factorials and the closed moment formula. All four classes and the stated original, left-partial and total-Frobenius panels are retained. Total Frobenius acts on all coordinates and passes the expected bijection control.

The exact small-field and cyclotomic arithmetic, four-owner distinctness, Boolean allocation census, type and size caps, cofinal formula records, corrected ordered-source factors and nonsplit regression were inspected. Higher odd-extension records use the proved restriction formula; they are not labeled as full large-field enumerations. Source authentication and counterfeit tests are meaningful, but do not formally verify the imported geometric pushforward theorem.

## Scope and residual risks

The corrected result is a non-descent statement for a concrete weighted finite-field source projection and its full class family. It does not bind all carrier recombinations, establish an integer mean-value obstruction, or prove a global cancellation estimate. The rational weighted trace is distinguished from an integral actual sheaf and from the displayed integral virtual realization. Classical character sums, compact pushforward and virtual sheaf operations are not claimed as externally new. The remaining full-carrier transport question stays open.
