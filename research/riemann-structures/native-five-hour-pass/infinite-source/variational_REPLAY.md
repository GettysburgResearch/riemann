# Infinite variational certificate: replay and accepted conclusion

The immutable physical input is commit
`a454106bd685d618f61231549e6fd99bf5b5a1ad`. The consumed L96 Gram
capture has Git blob `78a5a8ae10b065bb32bfe114b9ac3063bf0ed0f1`,
the L32 center-selection capture has blob
`e9363aa64dd54c4d0e81554bc28a0c2c360c0bfd`, its producer has blob
`36ddede447e6190e7712d3afbdae04f50b7ab1d9`, and the physical result note
has blob `544b35f84cb17b8a4b68bec3cf86472371305345`.
The consumer additionally authenticates their LF-normalized SHA256 values
before parsing, as declared in `variational_PREREGISTRATION.md`.

The directed certificate establishes two statements in the original
`L^2(nu)` physical norm on the fixed primes `2,3,5`:

1. the enclosed both-clipped path is the global minimizer over the entire
   family in which `w` is activated last;
2. the literal legal path which raises `w` by `1/100` at `u=99/100` has
   a strictly smaller original energy.

The certified energy change lies in

\[
[-7.68290550556\,10^{-5},-7.68289460076\,10^{-5}],
\]

while the half-gradient first variation is already strictly negative.
Thus no globally optimal infinite path can lie in the whole `w`-last
family. The certificate does not identify the true three-coordinate
minimum.

Before final binding, the coordinator ran `--write`, `--check`, and
optimized `--check`; all passed. The later hardening rerun preserves the
same mathematical intervals and adds typed JSON equality, direct exact
integration of every one of the twenty moment forms, and direct exact
integration of all sixty-four literal factor-two currents on the old and
new terminal paths. An independent reviewer read the proof and complete
consumer and found no blocker. The first refused input-hash result is
preserved as a negative provenance control rather than overwritten.

Current LF-normalized SHA256 bindings, to be replaced by their Git blobs
at the final variational freeze, are:

| File | SHA256 |
|---|---|
| `NATIVE_INFINITY_VARIATIONAL_THEORY.md` | `d8a7945c0dd9baffa4f8a373ddbce4675752b09a9b63fcb47fb7543510bea25c` |
| `variational_PREREGISTRATION.md` | `d262897a3e027417dddc229affacd0e1880b16aa2fd50b37464808f2590159de` |
| `variational_certificate.py` | `aa25f2aec339ca0502f1591d6219bfe9a0b883a7ec19528383ac9c2230db04db` |
| `variational_certificate.verification.json` | `53426798d731eee10ac0e824c22a10d0db1b22eb91916e06adcc858982bd14fb` |
| `variational_certificate.refused_input_hash.v1.json` | `f80d7da06f0223ede844ba48e61cf29a9a75a7b87c1113f24c8d3bbd40902192` |

All source coefficients, the unchanged Mellin measure, root enclosure,
all eight global cone outcomes, literal path points, moment differences
and current differences remain in the artifact. There is no
post-renewal retained-gamma identification, varying-prime assertion or
zeta-function consequence.
