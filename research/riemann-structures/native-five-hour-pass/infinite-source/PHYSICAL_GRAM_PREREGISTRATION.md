# Direct infinite physical Gram acquisition

Status: registered before acquisition; floating reconnaissance is not a certificate.

The target is the affine twenty-moment source at native head
`9421846721cd788ab01615c8b6d459d9de849df7`, in the order of
`ALL_HORIZON_THREE_PRIME_SOURCE_MOMENTS.md`. Its source norm is the ordinary
Euclidean norm of these declared twenty real coordinates, fixed independently
of the physical Gram. The observation is the unchanged L-102880 measure.

Write A(z)=sqrt(1-z^2), D(z)=sqrt(1-z)-A(z). The three holomorphic pair
products are A^2, AD, D^2. Their identities are

    A^2=1-z^2,
    AD=(1-z)sqrt(1+z)-(1-z^2),
    D^2=(1-z)+(1-z^2)-2(1-z)sqrt(1+z).

These functions construct a 27 by 27 covariance in the ORIGINAL observation
measure. Index reshuffling and the exact monomial-current decoder recover
the actual 21 by 21 affine Gram. No coefficient-frame norm is substituted.

Registered reconnaissance truncations are L=16,32. Registered certificate
truncations are L=64,96,128, with 192 or 256-bit Arb ball arithmetic. Each
holomorphic pair product is truncated separately at local exponent L. This
is an approximation of the infinite Gram, NOT a physical-product horizon
and NOT necessarily a Gram of a common truncated source. Certified tails
must be added before any statement about the infinite source.

The finite covariance uses the exact compact autocorrelation of the original
piecewise kernel. Frequency triples are streamed. No infinite numerical
quadrature and no dense three-dimensional permanent array are required.
Power-series coefficient tails use the decreasing exact binomial magnitudes
and geometric q-tail for q=p^(-1/2), for p=2,3,5. Rounding is enclosed by Arb;
no unqualified floating epsilon is accepted as a transcendental certificate.

Acceptance requires source-blob authentication; exact monomial decoder
controls; matching independent literal-path controls; symmetry; original
kernel mass; and all stated tail/interval bounds. Positive definiteness
requires a separate verified matrix certificate, not floating eigenvalues.
Candidate optimizer coordinates are proposed by floating computation and
accepted only by interval root and complete support certificates.

Independent controls use exact monomial currents on power paths with exponent
vectors (1,2,3), (2,3,5), (3,1,4), and a degree-one primitive truncation
`A=1, D=-z/2` at each prime. The latter permits complete direct enumeration
of 64 ordered source records and comparison with the tensor contraction.
It is a calibration object, not the infinite primitive. Replay at L128 is
an independent truncation/precision control of the L96 interval result.

All failed truncations, inadequate bounds and candidate failures are retained.
The producer refuses L>128, precision>256 and dimensions beyond those stated.
The working-set target is below 200 MB; increases require coordination.
