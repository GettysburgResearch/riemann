# Exact refutation of the registered linear dual menu

This certificate is registered after the fixed `K=1,2,3` grid probe
exposed the need for analytic minimization in `w`. It consumes the same
authenticated L96 Gram and encloses the first declared ordered-root
candidate at the exact decimal center

`(-0.08233305700654603,1.1226168381295267,`
`-0.5082335933099136,0.5775184399712018,0.9383761444256082)`

with coordinate radius `1/100000000`. Acceptance requires a strict
five-dimensional Krawczyk enclosure, the declared lower-clipped regimes,
positive profile coefficients, and exact branch equality.

For each fixed K, evaluate the directed early-branch difference
`A_K-q_v(u,f(u))` at one registered rational point:

- K=1: `(u,v,w)=(15/16,31/32,1/32)`;
- K=2: `(u,v,w)=(15/16,501/512,21/1024)`;
- K=3: `(u,v,w)=(15/16,503/512,5/256)`.

The root box must put `u=15/16` strictly before its switch. A strictly
negative upper bound refutes the corresponding global inequality in
`variational_ORDERED_DUAL_THEOREM.md`. All three failures are required;
no replacement K is searched. This does not refute other nonlinear
potentials or prove a descent from the ordered path.

Arb precision is 192 bits. Exact rational inputs have at most 4096 bits.
No matrix exceeds 21 by 21 and no source or Gram acquisition is run.
The final producer has distinct `--write` and `--check` modes and requires
complete exact typed replay. Its earlier direct development invocation is
superseded by the final write/check cycle; the witnesses and all registered
inputs are unchanged.
