# Mixed low/high packet preregistration

Declared before the first mixed-domain acquisition. Fixed target:
one node in[1/2,256], up to three others>=2^30, H>=C/100, high-node confluence.

Fixed analytic constants: endpoint lower3/200, endpoint upper4, row bound36,
high off-diagonal bound1193/1024<2, cross bound1764/32768<1/16,
Re F target1/200, block Schur comparison3/2500>1/1024.

Exact endpoint controls:
Taylor lower degree16 at2303/1000 exceeds10;
Taylor lower degree16 at127/50 exceeds88/7;
Taylor lower degree7 at6 exceeds257;
H10-2303/1000-1/20>57/100.
No numerical Euler-constant, digamma or Xi evaluation.

Fixed source algebra panels, after diagonal square-root similarity:
(1/2,2^30,2^30,2^30),
(1,2^30,2^31,2^40),
(256,2^30,2^31,2^32).
Check the complete row r(B-x)=v and the block similarity by independent exact
matrix multiplication. A separate polynomial functional-calculus control,
f(z)=z^3+2z+1, checks w=r f(B)-f(x)r against direct f(A). The scalar polynomial
is an algebra control, not a replacement for the literal F in the proof.

Authenticate the frozen E4 source/code/artifact and actual theta source before
any inherited helper import; refuse while the E4 exact pin is pending.
New producer mixed_packet_replay.py, artifact mixed_packet_verification.json,
tests test_architecture_e_pass_mixed.py. Exact standard-library rational
arithmetic only;4x4 matrices,4096bit accepted-number cap,2MiB JSON cap.
Expected worker below64MiB; no heavy source job or external zero computation.

--check must rebuild the exact source record and compare canonical typed JSON.
Reject duplicate keys, float/nonfinite tokens, coerced integers, changed source
blobs, altered thresholds/coverage, wrong row signs and the missing factor2.
The final theorem remains a written analytic proof with independent review.

