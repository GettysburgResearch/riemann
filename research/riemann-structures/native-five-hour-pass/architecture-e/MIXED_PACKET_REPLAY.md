# Mixed packet replay record

Status: AUTHOR VALIDATED; exact independent review and freeze pending.

The producer authenticates frozen E4 commit
686df46fab25eadfc4564808f036e2b4fc1e4b3e and the actual theta-source
record before compiling the inherited finite rational helper. It then
reconstructs three declared mixed panels from literal exponential
functions, checks their Gram and derivative source map, and verifies both
the product and triangular forms of the exact block-elimination row.

Author run:

    python mixed_packet_replay.py --write
    python mixed_packet_replay.py --check
    python -O mixed_packet_replay.py --check
    python -m unittest tests/test_architecture_e_pass_mixed.py
    python -O -m unittest tests/test_architecture_e_pass_mixed.py

All three producer modes passed. All 17 tests passed in both interpreter
modes (0.867 s ordinary, 0.843 s optimized). Ruff check/fix and format were
run before the final write. The final proof SHA-256 is
a3ecc3338b955b14aaeaa520da5c7118fd90295d2813d63fb3525fe1e412b3fa.

This replay proves finite source-algebra and exact-bound identities only.
The continuum mixed-node estimate remains the written analytic theorem in
MIXED_LOW_HIGH_PACKET.md. In particular, no finite packet establishes the
actual theta-source positivity, and the polynomial functional-calculus
panel is an algebra control rather than a substitute for the literal
logarithmic derivative.

