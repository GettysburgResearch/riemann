# Endpoint four-jet replay record

Status: INDEPENDENTLY REVIEWED AND FROZEN.

The producer reconstructs the complete exact four-variable polynomial matrix
and all four leading determinant factorizations.  It separately executes the
fixed 100-digit eta-regularized endpoint reconnaissance declared before the
artifact was written.  The artifact binds every owned source byte.

No numerical sign is promoted to an interval statement.  In particular, the
packet does not prove positivity on a neighborhood of `1/2`, a continuum
four-node theorem, or RH.

The root reviewer independently checked the Leibniz matrix, all four symbolic
determinant factors and the regularized endpoint expansion, then reran the
producer and all 11 hostile tests in ordinary and optimized Python.  All four
runs passed with proof SHA-256
`5d5063a176bf3bd4b950e2bb00d4e705435c1266112243f76f367ea4bf50ba44`.
