# Two-moderate/two-high preregistration

Declared before the first bounded E22 replay. Fixed theorem target:
two nodes in [32,256], at most two nodes at least 2^40, separate confluence
inside both clusters, and H >= C/50.

Fixed low-block constants: dimension two; minimum 32; log lower 1/4;
J2=382/3969 and half J2 squared below1/200; digamma below1/10;
weighted frame square52; complete prime bound1/3584 below1/3000;
resulting lower margin217/1500 above1/8. The upper source norm bound is6.

Fixed interface constants: inverse Sylvester-column bound2/y for y>=2^16;
source-column bound46 sqrt(y); column bounds92/sqrt(y),460/sqrt(y);
cross numerator15364 and final norm below1/64 at2^40.
Final operator delta1/100 and relative kernel margin1/50.

Exact algebra panels in square-root diagonally similar rational coordinates:

    (32,32,2^40,2^40)
    (32,256,2^40,2^41)
    (256,256,2^40,2^56)

Reconstruct the complete2x2 Sylvester solution from original matrices; check
both block similarity products, polynomial functional calculus for
f(z)=z^3+2z+1, every literal exponential source function/Gram/derivative,
and the confluent Newton residual. Retain every matrix entry.

New producer two_cluster_replay.py, artifact two_cluster_verification.json,
and tests test_architecture_e_pass_two_cluster.py. Authenticate the frozen
E4 and mixed-packet dependency chain before compiling any inherited helper.
Standard-library Fraction arithmetic only,4x4 matrices,4096-bit accepted
numbers,2MiB JSON and expected below64MiB. No Xi values or zero census.

Strict JSON must reject duplicate keys, floats/nonfinite tokens, bool/int
aliases and altered source bindings. Countercontrols must change a Sylvester
sign, omit a coupling row, remove factor two, alter a constant and breach a
domain/cap. Finite replay does not prove continuum coercivity; the analytic
theorem is TWO_MODERATE_TWO_HIGH.md.

