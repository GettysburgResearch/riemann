# Four-node replay and scope

The author ran four_node_replay.py in --write, --check and optimized --check
modes, and all17 tests in ordinary and optimized Python. The first run used
less than one second per command. Ruff found import ordering only; that was
repaired and the final owned bytes are rebound before freezing. These are
author executions, not an independent execution claim.

The artifact authenticates the integrated order-three source and pinned
PR765/783 mathematical context before constructing data. It reconstructs
literal exponential-polynomial bases from rational Laplace fractions,
integrates their Gram entries exactly, checks actual derivative action,
checks both resolvent products and an independent finite path expansion,
and derives the normalized last Cauchy residual through repeated nodes.
Four preregistered panels include repeated and widely separated nodes.

All scalar bounds are rational: the exponential enclosure, positive
pi-integral identity, resolvent/gamma/prime estimates and final margin.
Prime-power labels are independently factored through32, without replacing
log p by log(p^k). The infinite Euler sum is bounded analytically in the
proof, not reconstructed by that finite owner table.

The fixture contains no sampled Xi value, zero count, approximate eigenvalue,
or finite-to-global PSD inference. The all-node theorem requires the written
proof. The separate general-n note is a proof-only sequel and adds no tests
or finite coverage to this record.

Reproduction from the repository root:

    python research/riemann-structures/native-five-hour-pass/architecture-e/four_node_replay.py --check
    python -O research/riemann-structures/native-five-hour-pass/architecture-e/four_node_replay.py --check
    python -m unittest discover -s tests -p test_architecture_e_pass_four_node.py
    python -O -m unittest discover -s tests -p test_architecture_e_pass_four_node.py

The executable and test are bounded standard-library code with at most4x4
matrices. No external high-zero computation or inherited producer is run.
