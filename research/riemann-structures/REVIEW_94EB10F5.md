# Independent review of the fixed-core owner-range correction

Reviewed scientific commit: `94eb10f59be1f0118d165ac23a31b648600d224e`.

Scope: its four files, containing the mathematical correction, exact producer, artifact and ten tests. I independently read the named archival source statements, reconstructed the corrected Hilbert bound and source counterexample, and read the code and all tests. I checked the frozen file set and final proof after freeze. I did not run computations. Root reports Ruff format/lint, producer write/check/optimized-check, and ten ordinary plus ten optimized tests passing (0.959 and 0.953 seconds). Reported proof object: `6145fcb1b88180448808bfecc29a82a5bcda06b5b87c13f3c6ed27d57be9235e`.

Disposition: the stated range-import defect and correction are supported by the exact sources. No remaining blocker was identified.

L-102958 bounds opposite owner products by the full cores: `P <= 2b`, `Q <= 2a`. With `a=gc`, `b=gd`, these are `P <= 2gd`, `Q <= 2gc`. L-106124 explicitly uses reduced `c,d` but imports the smaller bounds without `g`. Its abstract Hilbert interval argument remains valid under those smaller-range hypotheses; the defect is its uniform application to the full physical source. The proof correctly distinguishes an implied constant depending on a fixed g from the displayed uniform `g^(-2)` saving.

The corrected finite estimate follows directly from complete additive orthogonality and residue-cell multiplicity. The exact capacity is bounded by `ell rho ceil(2gc/ell) ceil(2gd/rho)`, hence by `9g^2 cd`. Substitution of the original coefficient shape and multiplication by the conductor weight leaves `9 G^2 ell rho/(cd)` times the two reciprocal owner sums. There is no additional uniform `g^(-2)` gain. This statement still requires the actual Hilbert norms of any retained extra source labels; it does not supply a complete-gamma norm bound.

The countercontrol is the frozen dense canonical source, with its actual owners, two positive Boolean histories per side, shares, physical indices and Mellin measure unchanged. Here `P,Q` have order `U^2`, while reduced `c,d` and common g have order U. The true full-core bounds hold, whereas `P/d` and `Q/c` grow proportionally to g. Choosing one actual quadratic class is permitted coefficient deletion in the claimed local Hilbert estimate. The source kernel gives principal class energy bounded below by a constant times `1/(log U)^8`.

I independently verified the Gauss normalization. The double quadratic-weighted additive sum equals a scalar of modulus `sqrt(ell rho)` times the class coefficient sum. Hilbert-valued Cauchy therefore gives the comparison factor `ell rho/((ell+1)(rho+1)) >= 9/16` after restoring the original principal normalization. This is sufficient to contradict a uniform `U^(-2+o(1))` bound on the same physical family. No stronger unnormalized coefficient-one Gauss-frame assertion is needed. The explicit horizon `X=16Y=16U^6` makes the comparison with the old subpower parameter precise; no independently enlarged horizon is silently identified with Y.

One scope wording was corrected during review: the old displayed saving would permit common-core summation, but the old note itself leaves global conductor recombination open. The final proof does not attribute a completed global sum to that source. It also preserves the distinction between four literal history amplitudes and their grouped coefficient; the range loss is independent of the known Wick and nonprincipal atomic corrections.

The producer authenticates six exact source blobs before use, reads the frozen sixteen physical pairs and their sixty-four histories, verifies coefficient squares and actual quadratic classes, and checks the true and erroneous range ratios separately. The small integer residue-capacity controls are explicitly elementary rectangles rather than new prime-source fixtures. No large phase enumeration, prime search or numerical Fourier integration is used. The cofinal contradiction is a proof using fixed proportional prime intervals and the already authenticated kernel estimate, not an extrapolation from the fixture.

The separate `fixed-core-range-downstream-audit.md` records the bounded impact review. Later exact Wick identities, direct atomic diagonal calculations and the conditional T-106140 frontier do not use this erroneous local `g^(-2)` import in the inspected arguments. This is not an exhaustive repository-wide dependency audit, and neither this correction nor the canonical control proves the full native principal moment large or refutes an RH reduction.
