# Complete actual-path grids: calibration, heldout and a falsified extrapolation

The fixed executable frozen at
`fd24a4acb3b9a041bce4fb8701d2a887e77d2def` exhausts every actual
monotone grid path in the registered H25 source. Its calibration
contains N=2,3. The unchanged executable then produced the heldout
N=4 artifact frozen at `885fff1b92897d604dbec971a2945c76191c7dfd`.
The original63 ordered factors,45 physical ratios,2ds and original
Mellin metric are retained throughout.

| N | Complete words | Unique minimizing word | (A,B,C/2,D,E,F) | Original energy, approximately |
|---|---:|---|---|---:|
| 2 | 90 | 232355 | (1/4,3/16,1/16,0,0,0) | 158.23051049508 |
| 3 | 1680 | 232323555 | (1/3,13/54,5/54,0,0,0) | 157.563844970868 |
| 4 | 34650 | 232323235555 | (3/8,17/64,7/64,0,0,0) | 157.49597972322044 |

These are certified minima on the complete stated grids. The decimal
values are only summaries of the outward rational intervals. Every
word enters the streaming score digest; each winner is also integrated
directly through all63 literal records and all45 physical ratios.
Both a unique word and a unique occupation class survive on each panel.
The N=4 search did not modify its executable, metric or candidate policy.

The convergence theorem is in `NATIVE_MONOTONE_GRID_CONVERGENCE.md`.
It gives actual source upper bounds and an O(1/N) enclosure of the
all-continuous-monotone-path infimum, with explicit original-norm
constants. The small-grid results do not by themselves identify the
all-path minimizer. In particular N=3 does not refine N=2, even though
its observed minimum happens to be smaller.

## The observed alternating pattern cannot persist at every resolution

The three winners have the form `(23)^N5^N`. For this actual staircase,
direct summation gives

    A=(N-1)/(2N),
    B=(N-1)(4N+1)/(12N^2),
    C/2=(N-1)(2N-1)/(12N^2),   D=E=F=0.                 (1)

Thus these particular staircase fields converge to the synchronized
path v=u followed by w, whose original energy is about158.333344517.
That is strictly larger than the heldout N=4 value. For every N
divisible by4, the N=4 winning path is represented exactly by repeating
each of its steps N/4 times. Consequently its source field and energy
remain available on those finer grids. Continuity of the original
finite Gram expression then proves that `(23)^N5^N` fails to minimize
for all sufficiently large multiples of4.

This is a rigorous limit on extrapolating the three successful finite
patterns. It motivates a different continuous path, rather than an
unsupported claim that alternating activation solves every grid.

## Replay and scope

The final certificate replays both frozen phases and adds independent
controls: all90 words survive a zero metric, a signed cross-term metric
selects exactly A=1/2, all six single-step activation orders have their
independently known moments, and refinement preserves the actual path.
Those synthetic controls test enumeration and scoring; they are never
substituted for the original source metric in the scientific panels.

The coordinator executed the complete calibration and heldout scouts.
Final producer/test validation is pending its serialized queue; no
scientific job was run by the author. Independent readers checked the
whole scout, source approximation proof and fixed search declaration.
This packet makes no all-height or full retained-gamma identification.
