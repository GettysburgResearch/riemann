# Six-hour native research synthesis

The pass produced explicit results for the literal finite-prime path current and its original physical observation. It also identified concrete limits on proposed projections and on one numerical decoder. None of these results identifies the full post-renewal retained-gamma family, takes a limit over all primes, or proves RH.

## The source and the finite optimization problem

For the fixed primes `2,3,5`, the source is

\[
 \Lambda_x(\mathbf z)=\prod_p\bigl((1-x_p)\sqrt{1-z_p^2}
                      +x_p\sqrt{1-z_p}\bigr)=\sum_n a_n(x)\mathbf z^{v(n)}.
\]

Actual coordinatewise monotone paths give `B_(n,m)=2 integral a_m da_n`. Their observed field is the complete sum of `B_(n,m)/sqrt(nm) exp(it log(n/m))` over `nm<=H`, in the original measure `dnu=|kappa_hat|²dt/(2pi)`. Derivative sites, the factor two and every equal-ratio `1/d` alias remain present.

The polynomial source gives twenty exact occupation coordinates on the affine hull of source currents; they are not freely attainable path parameters. The all-horizon decoder is frozen at `b58909695c2feb6cc74af7d4b3b41089b41c4871`. The filtration and bounded physical-rank certificate establish full twenty-dimensional rank from horizon 450 through 900 (`a4d610431d5edaf26b00bae903bb9111837e4c31`).

The horizon-450 optimization uses all 614 literal records and 265 physical ratios. Its accepted certificate, `35ebdcdf060489be7a53e81246094043ec4aa9c0`, identifies the global minimizer among all monotone paths: a clipped affine planar graph `v=clip(lambda+mu*u)`, followed by the last-coordinate activation, with approximately

`lambda=-0.12650381198`, `mu=1.1600902574`, `energy=178.6045826115`.

Exact root enclosures and the complete source-gradient support inequalities prove global optimality; numerical stationarity alone is not the certificate. The oriented image is unique modulo pauses and weak reparametrization. The geometry theorem `83ca3f77516380eaa29fa0f3a76f25ce9ebcc568` controls Hausdorff distance by the cube root of the original energy excess. An actual small detour proves that exponent `1/3` is sharp. This does not assert persistence of that optimizer at infinite horizon.

## Attainable paths constrain projections

The complete pair body is now explicit: for `A=integral v du`, `B=integral uv du`, `C=integral v²du`, every feasible pair satisfies `A/2<=B<=A-A²/2`, and precisely the interval `Cmin(A,B)<=C<=Cmax(A,B)` is attained. The lower envelope has the clipped-affine phases; the upper envelope and all equality profiles follow from a threshold-law/Gini argument. A single completed graph realizes each intermediate value (`6aa24e9c81c5a618ae7b740872f48266a5a86892`). This classifies the pair current, not simultaneous higher-arity compatibility or physical energy.

Reynolds averaging need not preserve actual path attainability. Legal pairs obey `C-A²<=2B-A`; the uniform permutation average of sequential paths has `(A,B,C)=(1/2,1/4,1/2)` and violates it by `1/4`. Its exact distance from the legal pair-moment body in the maximum coordinate norm is `(sqrt(10)-3)/2` (`5e312c7dc81926e637d2d7343f90eba4708587c0`). Separately, all three dual central coordinate-`S3` projectors fail to preserve the actual horizon-25 physical observation kernel (`cb9278bec3844cc3fd8072987a6890f1df32a368`, Section 4). These are specific source and observation obstructions. A proposed common projector must prove its own descent and path compatibility; a symmetry label alone does not supply them.

## Completion, sign and faithfulness

For any fixed finite prime set, the full current exists uniformly:

\[
 F_\infty(t)=2\int\overline S\,dS,\qquad
 \sup_{\gamma,t}|F_\infty-F_H|\le r\sqrt2\,4^r H^{-1/2}.
\]

Original Hilbert energies converge uniformly over paths; their minimum values converge, and minima are attained. The real part is fixed by the endpoints; only the imaginary current varies. Independent local radicals prove faithfulness on the full `4^r` tensor space, hence on the actual curvature directions (`822646ffea23d906c385f0273a8c45693e982c4d`).

The completed imaginary current has the same strict negative sign for every admissible path on `0<t<pi/log(max p)`. An explicit positive gap above the endpoint-only energy follows in the unchanged measure (`059d54f23b39b692c23f375338d9ad443b56b74f`). The endpoint-only real field is therefore not an attainable completed schedule.

## A failed minor and an effective replacement

The old selected twenty-row minor has limiting rank at most fifteen: eight pure-prime rows collapse into at most three directions. Its inverse norm grows at least on the order of `sqrt(H)` whenever it exists, so no honest strict uniform Neumann-tail certificate can succeed for that selection (`78918a6f3297d4c88b3b3c42140eba4c0f11778e`). The old UNKNOWN tail outcomes remain unchanged. The accepted diagnostic `b602ea788ed6a74800c5dd394c118dbc14dd59d2` verifies that the local cutoff remainder is negligible; it does not turn those outcomes into rank failures.

The replacement fixes all 64 ratios `1/(2^a 3^b 5^c)` with `a,b,c in {1,2,3,4}`. Exact local inverse and tail comparisons passed (`0103b95130de0f8151c529a0911a52842732227d`). The registered artifact gives an explicit eventual full-tensor threshold. The separately reviewed analytic refinement in `EFFECTIVE_FULL_SUPPORT_THEOREM.md`, frozen at `ae60a59d103a6696a667be4dbd0721387adc6a77`, improves it to

\[
 H_2=(13604716350000)^2+1.
\]

Every integer horizon at least `H2` therefore distinguishes all 64 tensor coordinates and, in particular, the twenty actual curvature directions. No untested interval below `H2` is filled by this deduction.

The completed event collector, frozen at `7448c5a4eb1cd2ad7c300b7332abf4e39392a138` with proof `02a7065a92e272f6764eb235f78b011c8312358ec914adf994e4019cf8d304a8`, authenticates all 40 components and certifies the old selected minor at every integer `900<=H<=2^48=281474976710656`, with no gaps. Each of the 39 target panels passed write/check/optimized-check before freezing; the collector passed those three modes too. It reconstructs the complete event census and composes the previously verified local updates and modular eliminations; it does not claim a fresh monolithic replay. Combining this with the earlier `450<=H<=900` certificate proves full physical observation rank twenty on the whole interval `450<=H<=2^48`. This is a finite result despite the singular limit of the selected minor. The interval above `2^48` and below the independent replacement bound `H2` remains unproved.

The coordinator reports 423 ordinary and 423 optimized final-packet controls passed. This total is not a claim that every older interrupted contract ran. Exact acquisitions, analytic deductions and unchanged UNKNOWNs remain separately recorded. The strongest next targets are an effective bridge across the remaining finite horizon gap, infinite-horizon path optimization, and an authenticated comparison with the full retained-gamma source. Further canonical positive examples or an assumed common symmetry projector would not settle those obligations.
