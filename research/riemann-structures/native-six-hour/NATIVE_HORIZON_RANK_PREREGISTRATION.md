# A complete bounded horizon atlas for native physical curvature

This is a new acquisition contract, before its calculations. It retains
the original three independent schedules at primes2,3,5 and the literal
half-source product, original2ds current, product cutoff and1/sqrt(nm)
physical observation. It does not synchronize schedules or replace the
physical observation by a polynomial coefficient norm.

The source formula is frozen at ec6635b4c7dcd08fe433b7ae7e1d9a8c9495dfcc
in L-102707-continuous-half-divisor-geodesic-and-polarized-hankel-current.md,
blob6810bcece309b0c54ae6c8fc84b314990004549c. Exact polynomial, edge and
rectangle utilities are taken unchanged from the curvature scout at
6b18fbd9bc0a493e739166e6fcb9fbefd8e4d537, blob
e660bfaa86ca3562189d47c89390984fc1e3117c. Its horizon and elimination caps
are not changed. New bounded index generation and elimination have their
own identity here.

Calibration covers every supported product threshold through30. Held-out
coverage is every supported product threshold from31 through900. Between
two such thresholds no supported ordered record changes, so these panels
represent all integer horizons through900. Calibration must be frozen
before the unchanged executable is used for held-out coverage.

For every horizon retain every ordered pair(n,m),nm<=H supported on the
three primes. Compute the literal polynomial curvature
2 d(lambda_n) wedge d(lambda_m). Its columns have the36 declared possible
monomial/form coordinates: for each form du_i wedge du_j, exponents in
coordinates i,j lie in{0,1}, and the remaining exponent lies in{0,1,2}.
The complete raw coefficient array and its exact rational row basis give
the labelled source rank. Then coalesce ALL records with equal reduced
ratio a/b, using the required weight1/d for(n,m)=(da,db). Keep the complete
coalesced array and compute its rank independently. The remaining factor
1/sqrt(ab) is nonzero and constant within that ratio, so it preserves rank.
Distinct real frequencies are independent in the original observation
space. A small scalar energy is not being inferred from these ranks.

Known calibration: H25 has63 records and both ranks6. The source-theoretic
full three-schedule curvature span has predicted dimension20. No claim
that every fixed-horizon physical map attains20, that its ranks are
monotone, or that its first saturation horizon is known is made before
this campaign. Higher prime powers are retained; they cannot be replaced
by a squarefree-only census for the physical rank question.

To authenticate geometric accessibility, use all36 fixed oriented axis
rectangles: active-coordinate centers1/3,2/3; inactive-coordinate values
1/4,1/2,3/4; active half-width1/48. Their area-evaluation matrix on the
36 monomial/form coordinates must have rank36. At the largest horizon
of each phase select a row basis of the actual source curvature and
verify every one of its forms on all36 rectangles by FOUR independent
literal2ds edge integrals. The common monotone prefix and suffix cancel;
the two routes are actual monotone paths with the same endpoints. These
checks certify the entire coefficient space and then every physical
coalescence, not independently attainable arbitrary moment tuples.

Caps: horizon900,200 distinct thresholds,5000 ordered records,2000 ratios,
36 coordinates, exact rational4096bits, source bytes4MiB, retained artifact
32MiB. Root runs a single worker under64MiB with the established reserve;
no agent runs an additional scientific process. All first-rank jumps,
rank drops and physical/source rank differences are retained. A failed
declared prediction or cap is a recorded failure, not permission to adjust
the input family. This is a finite/source-affine discovery; it identifies
neither the retained-gamma decoder nor a principal-member bound.
