# Complete native curvature-span certificate

Status: the acquisition passed under the root RAM guard and was frozen at `bae7184724093d3589ad16b278dc104322428d6a`. This separate proof-bound producer and test suite are awaiting root execution. The author has not run a scientific job.

The producer authenticates the complete acquisition before importing its executable. It reconstructs the literal three-variable half-source and all 63 ordered records, recomputes the coefficient and observed ranks, and compares the entire result with the frozen discovery. The six coefficient vectors are then independently compared with the proof's complete signed coordinate table; six distinct ratio pivots check the actual physical weight `1/d`, including all zero entries of that pivot matrix.

All six rectangle witnesses retain direct integration along the four native edges and common monotone prefixes and suffixes. Their source and observed ranks, change-of-basis determinant, and reversed-orientation control remain in the acquisition. The certificate binds the full proof, this contract, its producer and tests. Its checker uses canonical typed JSON, not Python numeric equality.

The finite replay verifies the actual source calculations and witnesses. The proof supplies the all-path Stokes inclusion, equality of linear spans, and injectivity under the original measure. No individual monotone path is claimed to attain every vector in that span or its unconstrained least-energy point. The full post-renewal gamma field and T106140 diagonal remain outside this packet.

Root commands: `native_curvature_span_certificate.py --write`, ordinary and optimized `--check`, then ordinary and optimized `tests/test_native_six_hour_curvature_span.py`. No new prime search, field census, or large symbolic package is used.
