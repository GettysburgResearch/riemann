# Preregistered support-functional source controls

Status: proof and experiment declaration only; no new worker has run.
Root coordinates all computation. Authenticate the exact occupation
identity source at `a75f3bbef5f75bb6a4bd7f3c91d051cfb132f3dc` and the
affine physical source at46c8453e before any future producer imports them.
Their exact proof/producer/artifact blobs must be installed before that
producer becomes runnable.

1. Retain the original H25 source, all63 ordered records and x=(A,B,C/2,D,E,F).
   Directly integrate polynomial/axis paths for the occupation definitions
   and verify the integration-by-parts single-w formula, including w
   activations at u=0 or1 and at an interior point of a vertical v-segment.
2. Fix the following eight coefficient tuples(a,b,c,d,e,f) before execution:

   - increasing_target: (1,-4,1,0,0,0), marked(s,t)=(1/2,1/2);
   - decreasing_target: (-3,4,1,1,0,-1/2), marked(1/2,1/2);
   - constant_target_endpoints: (-1,0,1,0,1/2,1), marked(0,1/4)
     and(1,3/4);
   - order_positive: (0,0,0,0,0,1);
   - order_negative: (0,0,0,0,0,-1);
   - concave_interior: (2,-4,-1,1,-2,1/2);
   - concave_flat: (1,0,-1,0,0,0);
   - concave_boundary: (-1,2,-1,1,2,-1).

   The first three calibrate fixed-marked planar minima only; they do not
   claim the global two-dimensional minimum. The last five use the exact
   two-triangle oracle. These cover both clipping ends, a decreasing target,
   c=0/c<0, zero b/e, both tied vertical orders and flat edges. Do not adapt
   the panel to a preferred numerical answer. Use the frozen irregular-one
   path as a separate signed single-w-collapse control for these coefficients.
3. Compare direct integration of every declared optimizer with the closed
   J formulas. Retain rejected counterfeits: clipping a decreasing target
   without isotonic projection, requiring false endpoint values at fixed
   u, dropping the C/2 coordinate conversion, or merging the two vertical
   activation orders on the diagonal s=r.
4. For c<=0, enumerate all vertices and valid stationary candidates of
   the two closed triangles exactly. Treat zero denominators and flat
   edges explicitly. Verify the chosen value by the source-path moment
   calculation. Finite samples do not establish the all-path reduction;
   that conclusion is the separately reviewed theorem.
5. For c>0, initially calibrate the exact planar J formula and explicitly
   chosen rational marked points. Any full two-dimensional critical-point
   solver is a separate registered extension, with exact root isolation,
   degeneracy coverage and certificates. A numerical grid is not an exact
   support oracle and is not an all-path energy proof.
6. Any use of the physical quadratic must retain the frozen original G,g,c0
   and certify equation(7) with outward rational intervals. A support bound
   applies to all actual paths; minimizing over their convex hull is not
   silently promoted to existence of a realizing single path.
7. Initial caps: these eight rational coefficient panels, coefficient
   denominators<=16 and absolute values<=8,6 source moments, at most12
   finite candidates per triangle, polynomial degree<=4 and4096 exact bits.
   All source paths in this initial panel retain the existing occupation
   cap of at most seven points; no general high-complexity clipping path
   is smuggled into the finite replay. Compare every selected path's
   moments with all63 literal source records using the authenticated
   occupation and curvature helpers. Do not replace the source by its
   six coordinates without that comparison.
   No field census, numerical quadrature, adaptive optimizer or new source
   identification is authorized by this declaration.
