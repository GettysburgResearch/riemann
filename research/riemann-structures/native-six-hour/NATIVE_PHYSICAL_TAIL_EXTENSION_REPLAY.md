# Replay of the separately registered physical-tail extension

This packet implements NATIVE_PHYSICAL_TAIL_EXTENSION_PREREGISTRATION.md.
It is a new data-informed campaign, following the two frozen UNKNOWN
outcomes of the earlier campaign. Neither old artifact, old limit nor
old acceptance contract is amended.

The first phase freshly reproduces the complete mathematical result at
\(2^{20}\), including every source and positive-majorant alias record,
all36 row sums, the fixed twenty-coordinate matrix and tails, all local64
tables, the exact inverse, both inverse products, row sums and theta.
The expected mathematical status remains UNKNOWN_TAIL_NOT_CONTRACTIVE.
A successful replay is an execution acceptance, not a contraction pass.

All seven direct frozen sources are authenticated before compilation or
parsing. The old helper is compiled from its exact authenticated bytes
with its original file identity, and authenticates its frozen H900-minor
dependency chain before use. The new code does not call the old panel,
alias enumerator or campaign entry point at a different horizon, and
does not mutate a helper constant. Its new supported-product enumerator
covers the complete box for \(g\le2^{24}\), while all calls to the frozen
local and coefficient functions stay inside their original64/80 limits.

For each ratio the new artifact retains the complete ordered integer
alias list and a framed SHA256 digest of every full signed-source and
positive-majorant record. It also retains the exact accumulated
mathematical data. Every write or check regenerates every alias
contribution; the digest alone is never accepted as a computation.
Calibration compares each contribution directly to the complete old
capture before also comparing the entire compacted result.

The new eighteen controls cover full calibration; authentication before
compile/parse; fixed selection and both historical UNKNOWNs; an independent
literal small alias census and exhaustive larger-box closure; integer
and support refusals; stream source/majorant/order corruption; omitted
aliases and false prefixes; independent literal low-degree curvature
with factor2 and1/g; same-majorant subtraction and both inverse products;
central-binomial local coefficient comparison and unchanged caps; exact
strict/singular contraction outcomes; conservative scope flags; the new
phase's own immutable calibration gate; typed JSON/rationals; complete
body digests; and ratio/domain-separated stream framing.

Only root runs the serialized jobs. Initial commands are:

    python research/riemann-structures/native-six-hour/native_physical_tail_extension.py --phase calibration --write
    python research/riemann-structures/native-six-hour/native_physical_tail_extension.py --phase calibration --check

The optimized check and ordinary/optimized focused test suite use the
same source and retain the128MiB worker target,2GiB reserve,4096-bit
stored-rational cap and32MiB source/16MiB artifact limits.

After root freezes the new calibration artifact and all four owned
files, the registered extension command is:

    python research/riemann-structures/native-six-hour/native_physical_tail_extension.py --phase extension --calibration-commit EXACT_SHA --write

The check uses the same arguments with --check. The phase first checks
the exact new freeze and unchanged owned bytes, then freshly reproduces
calibration before evaluating the sole larger target \(2^{48}\).
There is no free horizon argument.

A strict exact theta below one certifies rank20 at every integer horizon
at least \(2^{48}\) and at infinity for this fixed prime source.
Otherwise the existing UNKNOWN or separate refusal is retained.
Neither outcome changes the historical claims: a success is a sufficient
eventual threshold, not the first faithful horizon, and it does not fill
the intervening untested range. No energy minimum, all-prime completion,
retained-gamma decoder or optimizer-persistence result is inferred.

At implementation handoff no new calibration, extension, formatting or
test job has run. Both the preregistration and its source/cap design
were independently read before implementation. The implementation and
eighteen tests are now ready for independent static review and root's
serialized calibration. No \(2^{48}\) result is asserted.

