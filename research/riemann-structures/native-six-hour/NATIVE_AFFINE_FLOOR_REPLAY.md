# Proof-bound affine physical floor

This separate certificate authenticates the complete proof, preregistration,
scout and discovery at `46c8453e3976d242479202bf4d58489282a64d2a` before
importing executable source. It then reruns the complete original-kernel
scout, including its authenticated curvature replay, all six observed
Gram directions, interval inverse,65 source halfspaces and eight actual
comparison paths. No frozen source file is rewritten.

The certified affine floor lies strictly between102.20 and102.22. The
source-feasible universal bound is strictly above138.77, while the actual
quadratic path has energy strictly below160.56. These enclose the all-path
infimum but neither construct a minimizing path nor identify its value.
The affine minimizer has C<-19, B>3 and E>7, so it is not an admissible
occupation-moment vector.

Additional exact controls recover each comparison path's six coordinates
from literal coefficient rows and verify the reconstruction in all63
records. This is a source-coordinate calculation, not an energy metric.
All65 inequalities are then checked directly on those exact moments.
The fixed ratio16 source row retains amplitude11/256, with zero variation
in every direction. The quadratic path's independently integrated moment
tuple is (1/3,7/30,1/6,1/6,2/15,1/3).

Every numerical assertion uses rational interval endpoints. Approximate
midpoints remain display data. Kernel precision,6x6 systems, source size
and4MiB bounds stay at the preregistered values; the extra exact linear
algebra has six columns and at most63 rows, with4096-bit intermediates.
Canonical JSON checks distinguish numeric types and reject duplicate keys
and nonfinite display values. Source hashes and new owned-file hashes
normalize LF consistently where specified.

Validation status: the coordinator executed focused Ruff formatting/lint,
producer write/check/optimized-check and all16 ordinary plus16 optimized
dedicated tests; all passed. Independent full proof/code/test reading
found no blocker. No scientific computation was run by this certificate's
author. The final note binding and exact frozen-SHA review remain pending.

```text
python research/riemann-structures/native-six-hour/native_affine_floor_certificate.py --write
python research/riemann-structures/native-six-hour/native_affine_floor_certificate.py --check
python -O research/riemann-structures/native-six-hour/native_affine_floor_certificate.py --check
python -m unittest discover -s tests -p test_native_six_hour_affine_floor.py
python -O -m unittest discover -s tests -p test_native_six_hour_affine_floor.py
```
