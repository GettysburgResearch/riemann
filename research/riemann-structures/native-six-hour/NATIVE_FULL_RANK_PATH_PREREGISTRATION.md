# Original-metric path test at the first full source horizon

The new target is H450, selected by the exact filtration theorem:
for primes2,3,5 it is the FIRST product horizon with all20 literal
source-curvature directions. The earlier bounded physical atlas also
found rank20 there. This acquisition computes the complete source and
metric; it does not assign those ranks as a replacement for either.

This is a NEW acquisition identity. It does not alter or extend the
frozen H25/H30/H60 scout. Calibration is H25. After freezing this
executable, declaration and calibration output, its heldout panel is
exactly H450. There are no additional horizons, primes, masks or starts.

All2,3,5-supported prime powers and all ordered pairs nm<=H are retained.
The independent local source coefficient uses its central-binomial
formula, with source-index cap450 and exponent cap8. Complete source
record cap1500, physical-ratio cap600, artifact cap8MiB and worker target
128MiB are fixed before execution. The serialized coordinator retains
its RAM gate and a600second maximum worker duration. No author runs
scientific jobs.

The fixed twenty-moment decoder and pure root/interval helpers may be
loaded from the authenticated frozen H25/H30/H60 source. Its capped
source enumerator, model builder and full-matrix routine are NOT called
or patched. This acquisition owns its complete450-horizon source builder.
Calibration must reproduce all63 actual H25 records, all45 ratios and
the exact original three-coordinate planar Gram.

Instead of storing the full physical Gamma matrix, stream every upper
triangular ratio pair once. For each exact entry, update all four planar
response vectors in BOTH symmetric positions, retaining the diagonal
only once. Retain the complete entry digest, all four exact response
vectors, all rational source columns and the20x4 gradient couplings.
Every physical weight1/sqrt(abcd), ratio alias1/d and original Gamma
entry remains present. An entry that vanishes by the kernel's support
is still included in the complete pair count and digest.

Use the same16 declared starts as the earlier horizon campaign,32
floating seed steps and16 halvings, followed by the unchanged strict
root-box radius2^-30 and outward512-bit rational certification. No
floating value determines root, cone or global acceptance. Every start
and refusal is retained. The full twenty-coordinate gradient is tested
against all eight exact source-cone quadratics. A failed or uncertain
cone remains failed or uncertain and does not itself disprove optimality.

Exactly ONE nearby rational path is independently replayed: the exact
binary center of the first declared start whose root is certified.
Its every ordered source coefficient is integrated literally and its
complete physical energy is recomputed in a separate full upper-pair
stream. This must equal the planar Gram energy. If no root is certified,
there is no such witness and no witness claim. Choosing the first root
does not require its full-source cone to pass. No sixteen-fold repetition
of the expensive physical energy loop is used.

The fixed source controls are all six axis orders, the diagonal and the
same irregular polygon through(1/3,1/4,1/5),(2/3,3/4,2/5). Their complete
ordered records must equal the twenty-moment decoder. This finite test
does not identify the full retained-gamma family, imply all-horizon
optimizer persistence, or change the original observation measure.
