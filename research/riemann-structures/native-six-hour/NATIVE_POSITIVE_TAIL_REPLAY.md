# Independent positive-tail completion certificate

This certificate binds calibration440570c5707e57347987115aeab2ba5fce80d6c5
and unchanged-executable heldoutd0ff1c645653d61afd77d5e9a0d9a79b64816d0f.
The only horizons are25,30,60,900 and2^20. All three fixed primes and every
smooth product cost in each declared prefix are retained. It is a bound
on all monotone primitive paths in the original observation measure.

The acquisition bounds the actual omitted field by a positive coefficient
majorant. It does not evaluate the signed field tail. Its coefficientwise
Amax is different from the smaller aggregate A_actual used for the complete
physical source norm; the certificate checks these roles separately.

The final producer authenticates all nine declared source inputs before
compiling the frozen acquisition or parsing either capture. It replays
both complete phases using the identical executable and checks their
canonical serialized JSON, including diagnostic fields. Duplicate JSON
keys and nonfinite numbers are rejected. The final artifact binds its
producer, this note and all18 test sources.

Independent source controls use a half-source coefficient recurrence,
rather than the acquisition's central-binomial formula. Both endpoint
coefficients give the coefficientwise maximum and absolute derivative.
For EVERY retained product cost, the certificate enumerates every
allocation of its three prime exponents between n and m and sums all
three actual derivative sites with the factor2. This is independent of
the acquisition's factored local-convolution assembly. Multiplicative
heap closure supplies a separate complete smooth-cost census.

Each directed contribution is checked by rational square inequalities
against its exact g_k/sqrt(k). Every complete prefix is resummed from
those intervals, and the tail must subtract that SAME positive prefix
from the closed positive majorant. Positivity can only clip the lower
endpoint. The independent endpoint rate is checked directly against
its exact square73728/H.

A second closed-value route uses256-bit integer root brackets, exact
interval operations, and128 positive log2 terms, compared with the
acquisition's512-bit outward arithmetic and256 terms. It checks all
local sums, both closed total bounds and the original kernel mass.
The exact squared Hilbert bound and product energy bound are separately
checked using the original positive measure. Selecting the smaller of
the positive-tail and endpoint-rate upper bounds does not select a
different path, source, or norm.

The coordinator reported that both acquisition phases passed. Approximate
readouts illustrate the bounds' present size: atH900 the uniform field
upper bound is about2.48075 and the energy/minimum-value error bound is
about6183.3; atH=2^20 they are about0.0446997473 and111.4144292.
These are rigorous upper bounds with loose constants, not measurements
of a signed error or an infinite minimum. Rational endpoints in the
frozen source, rather than these decimal readouts, govern acceptance.

Eighteen final tests cover whole replay, unchanged heldout source,
independent coefficient recurrence, all derivative sites, type-before-cache
validation, complete census, omitted costs, wrong coefficients, false
square enclosures, independent root boundaries, the aggregate/coefficientwise
distinction, prefix/digest corruption, same-majorant subtraction,
understated Hilbert/energy errors, scope/proof promotion, numeric JSON
aliases, duplicate keys, nonfinite data and rational caps.

At author handoff the final producer and18-test execution are pending.
The author ran no scientific job. Later coordinator execution and the
exact-freeze review must be reported separately. No optimizer persistence,
full retained-gamma identification, all-prime limit, or numerical value
of the infinite optimum is asserted.

