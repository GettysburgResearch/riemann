# Complete native synchronization-fiber replay

This separate certificate authenticates all four source objects at
`f97b7e00ba0bd3d6153167b477c44c6f153da10f` before compiling the scout.
It reruns that complete acquisition and compares the entire result.
The original proof, preregistration, scout and discovery remain unchanged.

Independent exact controls reconstruct P from its primitive integer Q,
all five moments, the positive five-dimensional weighted Gram matrix,
the derivative bound and both actual schedules. They retain the four
common original moments without inferring the all-height theorem from
finite data. The full native census has140 ordered pairs at horizon60
and474 at horizon300.

The two original complete integrated sources agree. After synchronization,
ratio3/5 has rational coalesced difference `1/16243587360`, with remaining
physical weight `1/sqrt(15)`. Both pairs(3,5),(6,10) and their distinct
common-factor weights are included. A separate omitted-weight control
fails. The common initial v-activation segment is retained as part of
the actual source, not discarded because it cancels in one difference.

At horizon300 the complete literal columns are independently coalesced,
and exact elimination verifies both literal and physical ranks3 and7.
All seven actual rectangle differences are compared, record by record,
with an independently integrated monomial-area matrix. The rank of this
matrix is also checked, rather than inferred from the printed rank.

Sixteen dedicated tests include coefficient and moment corruption,
the missing initial-segment control, wrong physical weighting, all
rectangle records, exact-rank independence and authentication before
compilation. Complete canonical comparison rejects numeric aliases;
the JSON reader rejects duplicate keys, floats and nonfinite values.

Status: the coordinator completed write/check and optimized-check,
and all sixteen tests passed in both ordinary and optimized modes.
The independent reviewer read the complete proof, scout, certificate
and tests without a mathematical blocker. No scientific job has been
run by this certificate's author. This run-status-only update awaits
the coordinator's final three-mode binding check and freeze.

```text
python research/riemann-structures/native-six-hour/synchronization_fiber_certificate.py --write
python research/riemann-structures/native-six-hour/synchronization_fiber_certificate.py --check
python -O research/riemann-structures/native-six-hour/synchronization_fiber_certificate.py --check
python -m unittest discover -s tests -p test_native_six_hour_synchronization_fiber.py
python -O -m unittest discover -s tests -p test_native_six_hour_synchronization_fiber.py
```
