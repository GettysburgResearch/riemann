# Replay of the fixed physical-minor tail experiment

This packet retains the two declared outcomes without changing the minor,
the local cutoff 64, the positive majorant, or the horizons. The calibration
at 900 and held-out acquisition at 2^20 both completed successfully, but
both mathematical contraction tests returned
`UNKNOWN_TAIL_NOT_CONTRACTIVE`. This is not a rank counterexample.

The exact calibration freeze is
`3ba479241acf9db1554af067d5e2bea85533dde5`; the unchanged held-out freeze is
`179d4c78a81cbe9eb7214c2b5a9eca041d197c46`. The former includes the proof,
preregistration, scout, eighteen tests, and calibration artifact. The
latter fixes the complete held-out artifact. The scout authenticates the
earlier exact source atlas and completion/faithfulness papers before
parsing their data. This final replay authenticates its six direct
files plus the literal horizon-filtration theorem before executing the
frozen scout or parsing either acquisition.

The final producer recomputes both declared panels with the unchanged
scout and compares the entire typed records. It then independently checks
the alias weights `1/g`, all selected source coordinates, positive-prefix
sums, same-majorant subtraction, both inverse products, and every row of
`abs(inverse)*tail`. Its short diagnostics give exact outward intervals
of width 10^-6 for theta; those intervals are display summaries, not the
acceptance rule. The rule remains the exact rational inequality theta<1.

Both actual matrices have exact two-sided inverses. Together with the
source filtration theorem at
`2952d7c0c9fd0fbbbf04fa6321cc2ff4bb392c8a`, this proves physical rank 20 at
the isolated held-out horizon 2^20 as well as at 900. It does not certify
any omitted horizon or an all-future-horizon statement. The earlier atlas
through 900 and the separate nonexplicit eventual-faithfulness theorem
remain valid; this experiment does not fill their intervening gap.

The final artifact retains the entire two acquisitions rather than only
their diagnostics. The producer has its own schema, source bindings,
proof-object digest, and strict numeric-type acceptance. The original
four-file scout contract and its eighteen tests are unchanged.

Root-reported acquisition validation: eighteen ordinary and eighteen
optimized tests passed; calibration completed in 1.72 seconds with about
25 MiB worker memory, and the held-out run completed in about 5 seconds
with about 27.5 MiB. These are successful executions with inconclusive
contraction outcomes. The author ran no scientific jobs. Final producer
and final dedicated-suite validation are pending.
