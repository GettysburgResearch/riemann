# Single-activation replay contract

This final packet follows discovery39d689b7485a423a1e0323a755ffc6a68ee084ed.
The final-independent stage is full subset enumeration at EVERY r=2,...,16,
not only the discovery controls through8. Its hard cap is65536 subsets and
16-by-16 rational matrices. It compares every matrix and all KKT values,
retaining all15 rows and the failed odd-central-three hypothesis.

Bind this contract, SINGLE_NATIVE_ACTIVATION.md, the new producer and tests.
Authenticate the discovery preregistration, source and complete JSON via
commit/blob before source execution; the inherited source reauthenticates
the original all-arity theorem. Reconstruct rather than trust the JSON.
Write and check exact canonical JSON; check also under optimized Python.
No float or precision tolerance is an acceptance rule.

New proof responsibilities are the perturbation bound, explicit threshold,
and cofinal single-active-prime theorem. Finite certificate coverage does
not replace those analytic quantifiers. Root alone runs the RAM-gated jobs.
