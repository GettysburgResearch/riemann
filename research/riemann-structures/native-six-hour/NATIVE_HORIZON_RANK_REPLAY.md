# Final certificate for the complete bounded native horizon atlas

This is a separate certificate of the frozen calibration
7e62a06c97ad4cd621fc70e228cb661913bf0439 and unchanged-executable held-out
acquisition bbdf750a267b5e4c603f99a115c0cac9ec88416f. Its independent
literal upper bound is the source filtration theorem at
2952d7c0c9fd0fbbbf04fa6321cc2ff4bb392c8a. Calibration has18 supported
thresholds through30; held-out coverage has65 further thresholds through900.
No supported ordered record changes between thresholds, so these83 panels
cover every integer horizon from1 through900.

All seven direct source blobs are authenticated before either executable
compilation or discovery parsing. The two frozen acquisition executables
and declarations must be identical, and their working files must retain
the bound LF-normalized contents needed by the acquisition's own hashes.
The unchanged discovery is replayed for both phases and compared as complete
canonical serialized JSON. Boolean/integer/float aliases cannot pass this
comparison; duplicate keys and floating or nonfinite JSON numbers are
rejected.

The final code independently enumerates the2,3,5-supported indices by
prime-power products, reconstructs every ordered record census and every
threshold, and rebuilds every reduced-ratio curvature sum with its actual
1/d weight. It compares all physical rows, not just reported ranks. The
literal dimension is independently calculated from all27 multidegrees:
beta(d)=number of coordinates equal1, except subtract1 when no coordinate
equals2 and the degree is nonzero. This gives all16 exact jumps and first
full literal rank20 at450.

For every panel a separate dense elimination over the fixed prime65521
computes a lower bound for the rank of the complete physical array. A
denominator divisible by that prime is refused, not silently discarded.
The modular lower bound must meet the theorem's literal upper bound and
the two captured rational ranks. Together these certify physical and
literal rank equality at every declared horizon without treating the
theorem's value as an assigned measurement. There is no fallback modulus
or adaptive input change in this packet.

Both unchanged discovery replays also recompute all36 rectangle
interpolation rows and all36 four-edge literal integrals for each selected
source-basis row. Their full retained outputs must match the freezes.
This is actual source accessibility; it does not assert independent
attainability of arbitrary moment coordinates.

The claimed physical equality ends at900. Literal rank saturation beyond
450 follows from the separate theorem, but physical rank at untested
horizons is not promoted to a theorem here. No scalar-energy minimum,
retained-gamma decoder, or principal-member bound is inferred.

Registered final controls are fourteen tests of the complete certificate,
source freezes, all literal jumps, independent coverage, fixed-prime
rank/denominator refusal, sparse rational typing, duplicate/nonfinite JSON,
numeric alias counterfeits, scope flags, omitted thresholds/records,
the wrong1/d alias sum, and corrupted panel coverage/rank. The unchanged
scientific replays run only through the root's serialized worker. Source
bytes are capped32MiB, the compact certificate1MiB, rational sizes4096bits,
source records5000, ratios2000, and matrix width36. The acquisition's
original900 horizon and root reserve remain unchanged.

At author handoff no final-certificate jobs had run. The root had reported
successful calibration and held-out acquisitions, including all954 records
at900. Subsequent final Ruff, producer write/check/optimized-check,
ordinary/optimized tests, and exact-source review results are recorded in
the branch's SIX_HOUR_NATIVE_PROGRESS.md front door. This historical
handoff statement itself asserts no final-certificate pass.
