# Native synchronization-fiber acquisition preregistration

Before running: fix P=t^3(1-t)^2Q with degree(Q)<=4, and solve the exact4x5
moment system for exponents0,1,3,4. Use its unique primitive integer null
vector, highest nonzero coefficient positive. Choose
delta=1/(1+sumabs(coefficients(P'/t^2))).
No scalar is fitted to an observed discrepancy.

Compute all moments0..4, the four original sufficient moments, and
monotonicity certificates for w_plus/minus=t^3+/-deltaP. Reconstruct every
literal source coefficient2int dLambda_n Lambda_m at horizon60 for both
original paths and both synchronized paths. Retain the common initial
v-activation segment and the full ordered pair census. Compare before and
after the corrected1/d rational ratio coalescence, with remaining1/sqrt(ab).
The predicted original difference is identically zero; synchronizedratio
3/5 is predicted nonzero with exact value delta*int(t^2P)/(4sqrt15).
All failures are retained; no new P is selected after a discrepancy.

Held-out constructive test: independently build all original bilinear and
synchronized quadratic-in-u/linear-in-w source polynomials for the complete
horizon300 pair census. Differentiate before reading any predicted ranks.
Record complete polynomial-curvature vectors and physically coalesced
vectors. Predicted literal ranks are3 and7. Physical ranks are measured,
not forced. Use the seven fixed rectangle centers
(1/8,1/4),(3/8,1/4),(5/8,1/4),(7/8,1/4),
(1/8,3/4),(3/8,3/4),(5/8,3/4), all half-width1/32,
for actual common-prefix/suffix monotone path witnesses.

Caps: fixed primes2,3,5; horizons60and300 only; at most2000ordered pairs;
at most2000ratios; exact matrix4x5 or7columns; path-polynomial degree24;
rational numerator/denominator4096bits; artifact8MiB. No prime search,
floating point, field census, unregistered precision, or external CAS.
The finite record/physical ranks do not replace the all-height proof.
Root execution stays serialized under the unchanged RAM reserve.
