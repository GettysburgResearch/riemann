# Exact-freeze review: held-out global path certificates

Reviewed commit: 284ddc66a75387cf77ec7551f93fa990724decf0.
Proof object: c2a5b39280e70ce89300e85d4348b248c057a38c3809504d32a06e8c41e03117.
Conclusion: no blocking source, certificate, or scope issue found.

The reviewer read the complete acquisition source, final producer,
NATIVE_HIGHER_HORIZON_PATH_REPLAY.md, and all 14 test sources, then reread
the frozen formatted producer and inspected the artifact. All four frozen
packet files agree with the current working tree. All eight direct source
Git blobs were checked, including the identical calibration/heldout
executable. All three LF-normalized owned-file SHA256 values were
independently recomputed and agree with the fixture.

The reviewer authored the referenced eight-quadratic proof and
fixed-planar alias proof; this report is not their sole independent
mathematical review. Those inputs received separate team reviews. The
native author independently implemented the acquisition and final
certificate, whose complete source logic was reviewed here.

The original source census, 21 columns including the endpoint constant,
complete 1/d ratio collection and unchanged physical Gamma form are
retained. The certificate authenticates every declared input before
compiling the frozen acquisition or parsing its captures. Whole-phase
replay is compared using canonical serialized JSON, preserving the
bool/int/float distinction. Every registered start and every refusal is
retained.

The independent controls check all 64 ordered multilinear monomial pairs
on 27 fixed power paths, yielding 1728 literal factor-two integrals.
The H60 power-path controls retain all aliases at ratios 2 and 5 and
verify the separate M6=1/5 and M13=1/2 source formulas. The artifact
contains the exact values -134731/115200 and -37/32 after the declared
square-root scaling.

The Krawczyk control retains strict whole-box inclusion, an invertible
rational preconditioner, contraction, and the actual endpoint clipping
regime. Quadratic envelopes are checked at both endpoints and the
interior vertex when applicable; PASS, FAIL and UNKNOWN remain distinct.
The full-source global claim requires the actual eight tests, not merely
the last-activation planar root.

The inspected artifact has horizons 25, 30 and 60 with respectively
63/45, 75/55 and 140/89 ordered records/ratios. All 16 starts at each
horizon have full global certificates and strict lower cone bounds.
Upper clipping is false, true, true. The strict gap in the replay note
follows from the exact gauge, positive planar coefficient, and original
field norm. Equality forces the unique oriented clipped graph and final
w segment, modulo pauses and monotone reparametrization. No unmeasured
rank-20 hypothesis is used.

Execution is coordinator-reported, not reviewer-run: Ruff formatting
and checking, producer write/check/optimized-check, and all 14 tests in
ordinary and optimized Python passed before the exact freeze. The first
heldout attempt stopped at the RAM gate before launching a worker and
had no scientific outcome. The frozen replay note retains its earlier
author-handoff wording that final jobs were pending; this report records
the later coordinator results without changing the frozen packet.

The reviewer performed only source reads and lightweight Git/hash/artifact
inspection; no scientific job was run. The result is at the declared
finite horizons in the original norm. It does not establish optimizer
persistence at arbitrary horizons, a full retained-gamma decoder, or a
different physical normalization.

