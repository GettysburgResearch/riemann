# Affine physical-floor scout preregistration

Pin the complete curvature theorem/acquisition at
6b18fbd9bc0a493e739166e6fcb9fbefd8e4d537 (and its authenticated predecessors).
Reconstruct F_ref by literal native integration along2-then3-then5; retain
all63 records, every coalesced physical ratio and all cross-frequency terms.

Compute the full6x6 Gram matrix, six cross terms, reference energy, fixed
symmetric energy, and all six exact symmetric/odd orthogonality controls.
Solve the one declared affine system with interval elimination; no path
parameters are chosen to force attainability. Report all six unconstrained
coordinates as occupation moments(A,B,C,D,E,F), with C twice coordinate3.

Test the fixed source halfspaces in the proof: both bounds onA,D,F,
C>=0,C<=A,B>=A/2,E>=D/2, bothbounds onA+F-D, and the three tangent families
at t=0,1/8,...,1. Keep every pass/failure/uncertified status, and report
the strongest rigorously certified single-halfspace energy lower bound.
Also test2B-C<=1/3 and all25 square-form halfspaces
2sA+2tB-C<=s^2+st+t^2/3 for s,t=-1,-1/2,0,1/2,1.
Compare with all six fixed complete axis orders, the original straight
diagonal and fixed quadratic
epsilon=(1,0,-1) actual path from frozen tangentc251614fa6ff2367c7c6dd30c1d6b5f33fbc5161.
An affine minimizer is never described as a path without reconstruction.

Exact expressions use onlysqrt(2),sqrt(3),sqrt(5),log2,log3,log5 through the
frozen actual-kernel algorithm. Outward interval operations round each
endpoint to the dyadic2^-192 grid. Input kernel intervals retain their
authenticated48-term logarithm and10^-40 square-root bounds. Intermediates
are bounded by8192bits; serialized rounded endpoints by1024bits.
Caps:63rows, at most63ratios,6x6systems,65halfspaces, artifact4MiB.
No new primes, large numerical package, adaptiveprecision, or extra
operator/source identification. The root serializes jobs under the same
RAM reserve. This scout precedes a distinct proof-bound final certificate.
