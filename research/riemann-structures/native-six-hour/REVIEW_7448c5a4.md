# Independent review of the complete frozen event collection

Reviewed freeze: `7448c5a4eb1cd2ad7c300b7332abf4e39392a138`.
Conclusion: no mathematical or composition-contract blocker found. The
accepted collection certifies rank 20 for the selected physical-current
minor at every integer horizon in

    [900, 281474976710656] = [900, 2^48].

This is a finite-interval result composed from previously verified frozen
components. It is not an infinite-tail or ambient-64 certificate.

## Exact identity and review scope

I read the complete producer, preregistration, replay note and all eighteen
test sources, including the final streamed collector, and inspected the
frozen collection's complete forty-component identity/coverage ledger.
Read-only Git inspection identified these five blobs at the reviewed SHA:

| File | Git blob |
| --- | --- |
| `native_event_minor.py` | `b806a192035d559855be2cae0de6334968dabadc` |
| `NATIVE_EVENT_MINOR_PREREGISTRATION.md` | `d64c92e7da9d029c991f4168bc3e7748fb7761ba` |
| `NATIVE_EVENT_MINOR_REPLAY.md` | `4bc9c7c18caba74da69990de77d63fd5f7feadb8` |
| `tests/test_native_six_hour_event_minor.py` | `9f88d93fc70d31f6ae0ecfb5bc2965d105633a90` |
| `native_event_minor.collection.json` | `3f183d72122d33daa6ea05acd9d3ae92c92b9a06` |

The five working paths had no diff from this freeze. The collection records
proof-object SHA256
`02a7065a92e272f6764eb235f78b011c8312358ec914adf994e4019cf8d304a8`,
all four owned LF-normalized source hashes, forty immutable component
identities, the single certified interval above, and complete registered
chain/coverage flags set to true. The chain starts with calibration
`390b94a5b5a1f87d69cca71ef258df5d74d67ffd` and ends with panel 48 at
`3982fe448b9e75fb842c866d215ec0cc2db3c9f8`.

This reviewer did not rerun a producer, test, elimination, source census,
or the forty panel bodies. The author of this campaign is another agent;
I contributed earlier source-filtration and physical-tail predecessors,
which are explicitly pinned dependencies rather than work being
self-labelled as independently reviewed here.

## Mathematical and acceptance checks

The source update is the literal complete curvature coefficient divided
by its supported alias g. The original derivative factor 2 and physical
1/g coalescence are retained. A selected row changes only at H=g^2 ab.
The complete event census includes zero updates, and all updates with the
same H are applied before rank is tested. Hence a verified event matrix
certifies its entire following constant integer interval, not merely the
event point.

The exact base matrix and full 36-coordinate source rows are reconstructed
against the frozen H900 capture; its inverse is checked in both orders.
Subsequent rational updates have eligible denominators in the two declared
prime fields. The source checks those denominators explicitly, proves the
moduli prime by complete trial division, and retains fresh modular
elimination witnesses. A nonzero modular determinant proves rational rank
20. Primary-field failure invokes only the fixed fallback; failure of both
fields remains UNKNOWN and leaves its integer interval uncertified.

The collector first authenticates and discards every component's raw
bytes, then reauthenticates and parses one component at a time. It checks
unchanged owned/source identities, canonical proof bodies, exact
predecessor commit/hash links, endpoint-state equality, consecutive dyadic
intervals, the independently regenerated complete event-address census,
simultaneous-event grouping, and the resulting union of certified integer
intervals. Strict JSON parsing and canonical comparisons reject duplicate
keys and coercible numeric counterfeits. Only compact predecessor state
and summaries survive each iteration.

The crucial premise is that each frozen component has already been
verified. The collector checks composition of those proofs; it does not
recompute local coefficient updates or modular eliminations. Its explicit
fields correctly say both are false, while full event-census reconstruction
is true. The per-panel check is the operation that freshly reconstructs
that panel's source updates and elimination witnesses. This is not an
acceptance claim for arbitrary unverified JSON carrying plausible rank
flags.

## Execution attribution and limits

The root reports that all thirty-nine target panels passed write, check
and optimized check before their individual freezes. For the final
collector, the root reports write 50.06 seconds, check 50.52 seconds and
optimized check 49.53 seconds, all PASS, with worker memory at most
45.1 MiB. Those are root execution results, not independent reviewer runs.

The full finite interval does not fill any horizon above 2^48. The
historical preregistration's conditional strict infinite-tail extension is
not an achieved conclusion; the separate fixed-minor degeneration theorem
rules out that Neumann strategy for this same singular limiting minor.
Finite rank certificates remain valid despite that limiting degeneration.
The separate effective full-support 64-row theorem uses another selection
and does not silently extend this collection. No scalar-energy minimum,
all-prime limit, retained-gamma decoder or RH conclusion follows here.
