# Preregistered source-metric tangent and joint-path experiment

This experiment continues the exact finite source at
`5d7ea752ee84765a7690bdc36bb2bbcaddb38bb4`: physical horizon25,
primes2,3,5, every one of the63 ordered pairs with nm<=25, all derivative
sites, original measure2ds, physical factor1/sqrt(nm), and original
Mellin measure. No canonical coefficient restriction or replacement
metric will be introduced.

The existing scalar path family is enlarged to

    u_p(s)=s+epsilon_p*s*(1-s).

Each |epsilon_p|<=1 makes the path monotone. The first computation will
construct the original observed3x3 tangent Gram matrix at epsilon=0,
retain its known constant-direction null vector, and test rank exactly2.
The two coordinates are epsilon=(a,b,-a-b). Their metric and energy
gradient determine a unique steepest-descent direction when the metric
is positive definite. The experiment will retain a rank failure.

The fixed admissible finite path panel consists of:

- epsilon=0;
- all six vertices of |a|,|b|,|a+b|<=1;
- the midpoint of each of those six rays;
- the existing one-coordinate controls epsilon2=1, epsilon3=-1/2 and
  epsilon5=-1 from the frozen global certificate (the middle control is
  a certified improving path, not the exact one-coordinate minimizer);
- one rational tangent-metric direction, selected by the following
  declared rule, and its midpoint.

For the last direction use the midpoint of the certified rational
intervals for `-adj(M)*gradient`, normalize its resulting three-coordinate
vector by its maximum absolute coordinate, round the first two
coordinates to the nearest multiple of1/1000 (ties away from zero), set
the third to minus their sum, and rescale once more if a coordinate
exceeds1 in absolute value. This rule is executed once. It will not be
changed to improve a reported energy.

For each path, form the complete native coefficient records before
physical ratio coalescence. Evaluate its original observed energy with
certified rational logarithm bounds. Report every fixed panel result,
including worse paths. A comparison with the frozen best one-coordinate
path is a finite source comparison, not a global path-optimality claim.

The source polynomial cap is four local occupied factors, epsilon degree4,
and s-degree8; the horizon itself gives at most three occupied local
factors for these particular records. Pair count is capped at63, ratio
count at45, and every physical product at25. The computation uses only
the existing exact radical/logarithm arithmetic and source coefficients.
No new prime search or numerical quadrature is allowed.

Two nonlinear controls are declared in advance. Common coordinates
(1/2,1/2,1/2) and(-1/2,-1/2,-1/2) must reproduce the baseline current,
because they are actual common reparameterizations. In contrast compare
(1/2,0,-1/2) with(3/4,1/4,-1/4). At literal record(2,6) their coefficient
difference is predicted to be1/1920; after physical weighting the ratio1/3
coefficient difference is1/(1920 sqrt12). This distinguishes the tangent
augmentation from an unjustified nonlinear affine quotient.

The original current's endpoint product values, exchange-odd variation,
primitive-site and integrated-site provenance remain part of the replay.
No full post-renewal gamma, conductor-weighted principal moment, or new
literal Wick diagonal is inferred from this finite experiment.
