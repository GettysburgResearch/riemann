# Bounded original-source support replay

Eight coefficient panels are fixed in the preregistration. The producer
authenticates occupationa75f3bbe and affine46c8453e before executable import,
then replays the underlying complete curvature source. It retains all63
ordered records for each selected path, compares their literal integral
with the frozen six-moment readout and preserves the C/2 convention.

For c<=0 it enumerates both closed activation triangles, with all vertices,
valid edge stationary points and isolated interior stationary points.
Flat cases keep boundary candidates. The chosen value is realized by an
actual native path; the two vertical orders at tied u are kept separately.
For c>0 the replay calibrates exact fixed-mark planar minima only. It does
not execute the theorem's full two-dimensional real-algebraic solver or
claim those fixed marked points globally minimize the support functional.

The same frozen irregular path supplies signed single-w-collapse controls:
the maximum of Q along its planar segments is found by exact quadratic
candidate evaluation and the actual reinserted w segment is integrated.
This verifies source endpoints and vertical segments without fitting a
new halfspace or replacing the native source by an arbitrary moment tuple.

Validation status: the coordinator reports successful Ruff, producer
write/check/optimized-check, and all16 dedicated tests in ordinary and
optimized modes. No scientific job was run by the author. The theorem,
complete producer and all16 tests received independent static reading
without a blocking finding. Final binding replay and frozen-SHA review
remain pending.

```text
python research/riemann-structures/native-six-hour/native_occupation_support.py --write
python research/riemann-structures/native-six-hour/native_occupation_support.py --check
python -O research/riemann-structures/native-six-hour/native_occupation_support.py --check
python -m unittest discover -s tests -p test_native_six_hour_support.py
python -O -m unittest discover -s tests -p test_native_six_hour_support.py
```
