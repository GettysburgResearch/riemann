# One global geodesic schedule on a complete finite horizon

Recorded before this experiment's first computation on 2026-08-31.

Use exactly the prime panel {2,3,5} and physical product cap nm<=25 in the
L-102707 half-geodesic primitive. Retain all 63 ordered factor pairs supported
on that panel, including the right unit, every prime-power allocation and
every repeated ratio. The left tangent remains root-free. This is a complete
declared finite Euler panel/horizon, not the complete source over all primes
or the post-renewal amplified family.

The primary common deformation is u_3(s)=s+epsilon*s*(1-s), with u_2=u_5=s.
Controls deform prime 2 or prime 5 in the same way. Restrict |epsilon|<=1,
so the schedules remain monotone in [0,1]. Different product tuples do not
receive independent parameters. Include the full chain-rule derivative and
the actual integration measure 2 ds.

Before any norm, retain the physical coefficient 1/sqrt(nm), and combine
equal ratios n/m with their exact amplitudes. Compare the entire ratio field
in the original measure |kappahat|^2dt/(2pi), including all cross-product
interference. No counting-diagonal replacement is allowed.

Also retain four explicitly distinguished diagonal polynomials: the primitive
derivative-site norm in 2 ds, separately integrated-site atoms, integrated
ordered-pair atoms, and ratio-coalesced atoms. Each individual exponential has
nu-norm Gamma(0), which is applied after the appropriate source normalization.
These are resolution diagnostics, not an assertion that any one equals the
later T-106140 Wick diagonal. Report E minus the declared integrated-pair
diagonal as an additional diagnostic, without replacing the full Gram energy.

The field has degree at most two in epsilon and its energy has degree at
most four. Derive each energy coefficient from the original K_L
autocorrelation at rational shifts, using exact multiquadratic arithmetic
on {sqrt2,sqrt3,sqrt5} and certified logarithm intervals. Check whether the
uniform path is stationary and whether any fixed grid choice
epsilon in {plus/minus1/16, plus/minus1/8, plus/minus1/4, plus/minus1/2}
has lower total energy. Zero derivatives or no improving grid point are
valid negative results; do not change the panel or cap to force a positive
answer.

For every K separately, verify that all nonconstant epsilon coefficients
sum to zero and that the constant sum is beta(K)-beta^square(K). Thus all
three experiments preserve the source endpoint and original signed Hankel
current, rather than merely preserving one chosen scalar value.
