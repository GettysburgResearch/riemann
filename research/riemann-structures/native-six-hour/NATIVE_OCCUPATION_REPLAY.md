# Exact native occupation replay

The producer authenticates the frozen curvature source before importing it,
then replays the whole acquisition. The separate proof supplies the all-path
identity, Jensen obstruction and continuum realization; the finite controls
are not extrapolated into those theorems.

Each of20 declared paths retains its entire30-word law, all six moments,
all63 original source records and the corrected physical ratio image.
Ordered integrals include the two iid factorial multiplicities. Word
expectations and direct source integration are separate computations.
The counterfeit event-order point is retained as a failed source candidate.

Root commands: native_occupation_moments.py --write; ordinary and optimized
--check; tests/test_native_six_hour_occupation.py in both modes. Execution
and exact-SHA independent review remain pending at manuscript preparation.
Arithmetic and resource caps are fixed in the preregistration. The checker
reconstructs the entire artifact, rejects duplicate keys/floats/nonfinite
JSON and compares typed canonical JSON. Bound owned files use LF-normalized
hashes. No final gamma decoder or actual energy optimum is asserted.
