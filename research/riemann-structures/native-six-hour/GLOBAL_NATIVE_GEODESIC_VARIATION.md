# One native schedule on the complete finite product horizon

Status: an exact finite-source variation theorem, with a frozen preregistered
discovery and an independent source-curvature certificate.
Scope: prime panel {2,3,5}, physical product cap 25, and the L-102707 primitive.
This is not the full source over every prime or the post-renewal amplified family.

One global admissible schedule improves the original observed energy on
**all 63 ordered factor pairs at once**, with every cross-product term retained.
Changing only the prime-5 schedule from s to s^2 lowers the energy by more
than 3.41. The original energy is between 166.72 and 166.73. No tuple-specific
coefficient or parameter is fitted. A source-defined exterior curvature
explains the change while product collapse kills it exactly.

## 1. A finite source identity with all product tuples present

Let A_25 be the polynomial algebra on the three prime labels, modulo the
monomial ideal generated by physical products exceeding 25. This is a genuine
ideal: multiplication by a nonconstant prime monomial can only increase its
physical index. Coefficient extraction, differentiation in a source parameter
and finite integration commute with this truncation.

Use the exact half-geodesic local factor

\[
 \ell_u(x)=u\sqrt{1-x}+(1-u)\sqrt{1-x^2}.
\]

The permitted indices are
1,2,3,4,5,6,8,9,10,12,15,16,18,20,24,25. There are exactly 63 ordered pairs
(n,m) on this panel with nm<=25. All are retained. The arithmetic coefficient
of the global source Lambda_s at n is a_s(n), and its physical field
coefficient is a_s(n)/sqrt(n). The current coefficient is

\[
 v_{n,m}=2\int_0^1 a'_s(n)a_s(m)\,ds.                            \tag{1}
\]

The left unit is zero, the right unit is present, and each prime derivative
site is kept before summing it into a'_s(n). No probability measure on s is
introduced: the primitive measure is 2 ds.

For every physical product K separately, finite Fubini gives

\[
 \sum_{nm=K}v_{n,m}=\beta(K)-\beta^\square(K).                    \tag{2}
\]

Thus the complete signed Hankel current from L-102707, including its
product kernel and factor 2D-1, is preserved coefficientwise. No scalar
projection has been used in place of (2).

## 2. GV-1: a common admissible deformation and its exact variation

The primary perturbation changes prime 3; the two controls change prime 2
or prime 5. For the chosen prime p set

\[
 u_p(s)=s+\epsilon s(1-s),\qquad u_q(s)=s\quad(q\ne p),\qquad
 -1\le\epsilon\le1.                                             \tag{3}
\]

Every schedule is monotone from zero to one, since
u'_p(s)=1+epsilon(1-2s)>=1-|epsilon|. There is one common parameter for every
source coefficient and product tuple. The full derivative of (3) is used.

Let A(s)=Lambda_s at epsilon=0, and let
B_p(s)=s(1-s) partial_(u_p)Lambda evaluated on the uniform path. Since the
local factor is affine in u_p,

\[
 \Lambda_{s,\epsilon}=A(s)+\epsilon B_p(s),\qquad B_p(0)=B_p(1)=0.
\]

Consequently the integrated ordered tensor has the exact expansion

\[
 V_\epsilon=V_0+\epsilon V_1+\epsilon^2V_2,
\]

where

\[
 V_0=2\int A'\otimes A,\quad
 V_1=2\int(B_p'\otimes A+A'\otimes B_p),\quad
 V_2=2\int B_p'\otimes B_p.                                      \tag{4}
\]

All integrals in (4) are with respect to ds. The factor two is used once.
If J swaps the ordered factors, the endpoint conditions imply

\[
 JV_1=-V_1,\qquad JV_2=-V_2.                                    \tag{5}
\]

Product collapse therefore kills both variation tensors. Equation (2)
holds for every epsilon, and all nonconstant epsilon coefficients of its
left side vanish separately at every K.

## 3. The complete ratio field and original measure

The observed field is

\[
 F_\epsilon(t)=\sum_{nm\le25}\frac{v_{n,m}(\epsilon)}{\sqrt{nm}}
                           e^{it\log(n/m)}
              =F_0(t)+\epsilon F_1(t)+\epsilon^2F_2(t).           \tag{6}
\]

Equal ratios n/m are combined with their actual inverse-square-root
amplitudes. This coalescing is done before the observation and is not a
replacement of the Gram matrix by an atomic diagonal.

Use the original real K_L of L-102880, kappa(u)=K_L(exp u), and
nu=|kappahat(t)|^2dt/(2pi). The measure is even. Equation (5) makes F_1 and
F_2 imaginary-odd; the real-even component of F_0 is unchanged. The full
energy is the polynomial

\[
 E(\epsilon)=\|F_\epsilon\|_{L^2(\nu)}^2
             =\sum_{j=0}^4E_j\epsilon^j,
\]

with exact coefficients

\[
 \begin{aligned}
 E_0&=\|F_0\|^2,\qquad E_1=2\Re\langle F_0,F_1\rangle,\\
 E_2&=\|F_1\|^2+2\Re\langle F_0,F_2\rangle,\\
 E_3&=2\Re\langle F_1,F_2\rangle,\qquad E_4=\|F_2\|^2\ge0.
 \end{aligned}                                                    \tag{7}
\]

All cross-product terms are present. In particular the coefficient E_2
need not be positive merely because E_4 is, and convexity is not assumed.

For two modes r=n/m and s=a/b, the exact Gram entry is Gamma(log(r/s)).
The nine kernel-piece intersections give the same antiderivative as in
NATIVE_ACTIVATION_KERNEL_MINIMIZER.md, now at a general positive rational
shift, not only a rational square. The square-root factors lie in
Q(sqrt2,sqrt3,sqrt5). Each E_j is stored as an explicit sum of an element
of this field and field coefficients times log2, log3 and log5.
Rational interval evaluation uses the actual kernel and physical weights;
no numerical quadrature or surrogate matrix is substituted.

## 4. Diagonal resolutions are not interchangeable

For a derivative site j, let b_(n,m,j)(s,epsilon) denote the arithmetic
integrand before the factor two. The primitive site diagonal is

\[
 D_{\rm primitive}(\epsilon)
 =\sum_{n,m,j}\frac{2}{nm}\int_0^1|b_{n,m,j}(s,\epsilon)|^2ds.
 \tag{8}
\]

Separately integrating each site gives
D_site=sum_(n,m,j)|2 int b_(n,m,j)|^2/(nm). Summing the sites first gives
D_pair=sum_(n,m)|v_(n,m)|^2/(nm). Finally ratio coalescing gives
D_ratio=sum_r|c_r|^2, where c_r is the full physical coefficient of mode r.
Each individual exponential has nu-norm Gamma(0), so the observed atomic
versions are Gamma(0) times these quantities.

The replay preserves all four polynomials. The difference E-Gamma(0)D_pair
is reported only as a centered form at the declared integrated-pair
resolution. None of these diagonals is identified with the complete
T-106140 Wick diagonal. Cross terms required to move between resolutions
are not silently discarded.

## 5. Frozen discovery and certified global improvements

The primary prime, controls, horizon, all 63 factor pairs and parameter grid
were fixed in GLOBAL_GEODESIC_PREREGISTRATION.md before computation.
The grid is epsilon=plus/minus1/16, plus/minus1/8, plus/minus1/4,
plus/minus1/2. A reported improvement must have a strictly positive rational
lower bound for E(0)-E(epsilon). Zero derivatives or no improving grid value
are valid outcomes; the source panel is not changed to force an answer.
The executed source, preregistration and complete discovery output are frozen
at `5d7ea752ee84765a7690bdc36bb2bbcaddb38bb4` before the independent certificate.

For the primary prime 3, the preregistered epsilon=-1/2 gives
`E(0)-E(-1/2)>0.23`. The other controls also have improving directions.
The exact energy coefficients have the following approximate values; all
acceptance statements use rational interval enclosures, not this table.

| Changed prime | E1 | E2 | E3 | E4 |
|---|---:|---:|---:|---:|
| 2 | -4.599260815 | 1.164208310 | 0.041005933 | 0.000944150 |
| 3 | 0.891492114 | 0.856811495 | 0.009994013 | 0.000171499 |
| 5 | 3.707768701 | 0.288522560 | 0 | 0 |

The frozen coefficients permit stronger conclusions without another search:

* For prime 5, the field itself is affine in epsilon. Its quadratic energy
  has strictly positive derivative throughout [-1,1], so the unique minimum
  on this admissible axis is epsilon=-1. This is the actual global schedule
  `(u2,u3,u5)=(s,s,s^2)`, and its improvement exceeds 3.41.
* For prime 2, the energy derivative is strictly negative throughout [-1,1].
  The unique axis minimum is epsilon=1, the global schedule
  `(2s-s^2,s,s)`, and its improvement exceeds 3.39.
* For prime 3, the second derivative exceeds 1.65 throughout [-1,1].
  Its unique axis minimum lies between -53/100 and -52/100.

The prime-5 affine statement has a physical-support proof. A quadratic
variation requires the deformed prime in both factors. At horizon 25 the
only such product is 25. At that product the only varying possible split
is (5,5), whose integral is an endpoint derivative and is constant. Thus
the quadratic field vanishes, rather than merely having an accidentally
small norm. The certificate checks every original record as well.

The boundaries epsilon=plus/minus1 were not in the preregistered grid.
They are postdiscovery conclusions of exact polynomial monotonicity, explicitly
distinguished from the original grid result. None is claimed to minimize over
all possible global paths.

## 6. GV-2: an actual exterior source and its reparametrization quotient

Let u=(u2,u3,u5) be independent source coordinates and Lambda(u) the same
finite half-geodesic product. In the physical-product-truncated ordered
tensor algebra define the one-form

\[
 \alpha_i(u)=2\,\partial_i\Lambda(u)\otimes\Lambda(u).
 \tag{9}
\]

Its antisymmetric derivative is the explicitly source-defined tensor

\[
 \partial_j\alpha_i-\partial_i\alpha_j
 =2\{\partial_i\Lambda\otimes\partial_j\Lambda
       -\partial_j\Lambda\otimes\partial_i\Lambda\}.
 \tag{10}
\]

This is an exterior two-source built from the actual derivatives, including
all prime powers and contractions. It is not a fitted operator. Product
collapse kills (10); exchange reverses its sign. Applying finite-dimensional
Stokes to a surface between two source paths gives their exact current
difference. A convex interpolation of monotone endpoint-preserving paths
remains admissible, so this supplies legal deformation surfaces here.
The ordered sign in (10) fixes the surface orientation convention.

More explicitly, for a fixed path u(s) and endpoint-zero displacement
eta(s), differentiating and integrating by parts gives

\[
 \delta V
 =2\int_0^1\sum_j\eta_j(s)
   \{\dot\Lambda(s)\otimes\partial_j\Lambda(s)
       -\partial_j\Lambda(s)\otimes\dot\Lambda(s)\}\,ds.
 \tag{11}
\]

The replay computes each first variation both from differentiating the
complete source coefficients and independently from (11), before physical
ratio coalescing. The equalities hold record by record on all 63 pairs.

For the uniform path, sum_j partial_j Lambda=dot Lambda. Therefore the
three columns in (11) sum to zero **pointwise in s**, for every common
scalar displacement eta(s). This is the infinitesimal form of a stronger
identity: changing every schedule by the same monotone reparametrization
changes no integrated source coefficient. The derivative factor in `2 ds`
compensates the parameter change; a probability interpretation of ds would
obscure this identity.

Consequently the actual tangent map L from the three prime-parameter
displacements factors through the quotient by their common direction.
Writing a convenient representative projector

\[
 \Pi_{\rm par}=I-\tfrac13\mathbf1\mathbf1^T,
 \qquad L\Pi_{\rm par}=L,\qquad R L\Pi_{\rm par}=R L,
 \tag{12}
\]

gives a genuine source/observation intertwining statement. The quotient is
intrinsic to source reparametrization; the displayed Euclidean representative
is only a convenient choice. It is not asserted to be an orthogonal
projector for the physical energy, nor is it identified with a local slot
Walsh projector, a quadratic-residue projector, or a full native Wick sector.
In particular exterior degree two in (10) is not a claim of Walsh chaos two.

The exact energy first variations accordingly sum to zero. The negative
prime-2 direction and positive prime-3/5 directions are compatible with this
source relation. They show that the uniform path is not stationary for the
original finite observed energy. The variational condition for unrestricted
endpoint-zero perturbations is the vanishing of the pairing of F with the
observed curvature columns in (11), subject to the common-direction relation.
It is a condition on the actual source readout, not on an arbitrary scalar
recurrence fitted after observation.

## 7. Replay, normalization and remaining scope

The independent certificate authenticates the frozen discovery executable,
source bytes, preregistration and exact output. It reconstructs the literal
source arrays, all derivative-site diagonals and the complete observed
quartic, and compares them with the frozen data. It separately derives
(11) and the pointwise common-direction relation from source polynomials.
The actual nonzero algebraic square roots are retained in a multiquadratic
field. Typed canonical JSON prevents integer/float/bool substitutions from
passing fixture equality.

This experiment tests single global deformations on a complete declared
finite primitive. The improving paths do not by themselves transport
carrier/renewal masks, identify the full gamma decoder, or improve a
previously defined amplified principal moment. Those source interfaces
remain distinct from the coefficientwise endpoint identity (2).
