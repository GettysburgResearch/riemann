# Fixed physical minor and absolute future-tail preregistration

This is a new bounded proposal before implementation or execution. The
author runs no scientific process. Root may authorize a dependency-light
exact replay only after review of
`NATIVE_PHYSICAL_MINOR_TAIL_CONTRACTION.md`.

The primary source remains the actual L-102707 half-source and original
product-horizon physical sum. Authenticate the frozen final H900 rank packet
and its complete held-out discovery before parsing the selected panel. Take
its20 rational independent physical rows and20 pivot coordinates verbatim,
including their order. Keep the resulting reduced ratios and coordinate
indices fixed before any new calibration/held-out result. Do not refit the
minor, change its row scales, select new phases, or use a Gamma Gram matrix.

The calibration horizon is exactly900. It must reconstruct the selected
entries independently from literal local coefficients and match the frozen
H900 submatrix. The held-out horizon is exactly2^20, using the same frozen
algorithm and selected ratios/coordinates after calibration is frozen.
Every source alias g<=floor(sqrt(H/(ab))) is retained with its exact1/g
weight. No dense enumeration of integers up to2^20 is needed: enumerate the
three-prime exponent products with g<=1024.

For each selected matrix entry use the exact determinant/product formula,
positive one-prime majorants and **same-majorant prefix subtraction** proved
in the companion note. The local infinite sums use the fixed cutoff64 and
the exact geometric remainder there. Retain the complete local rational
tables, all finite aliases and signed/majorant contributions, the full20-by20
matrix and tail bound, both inverse products, all row sums of
|A_H^(-1)| Tail_H, and their maximum.

The only mathematical success flag is strict theta_H<1. A singular selected
minor records `UNKNOWN_SINGULAR_MINOR`; an invertible noncontractive result
records `UNKNOWN_TAIL_NOT_CONTRACTIVE`. Neither is a counterexample. Keep
both fixed-horizon outcomes; do not adapt the cutoff, horizons or minor after
a failure. An arithmetic or resource-cap refusal is recorded separately
and is not a mathematical outcome.

Proposed fixed limits:3 primes,20 ratios,20 chosen coordinates from the
unchanged36-coordinate source box, local cutoff64, coefficient exponent80,
alias g<=1024, at most200 aliases per ratio, complete matrix20-by20, exact
rational numerator/denominator sizes4096bits, source bytes32MiB and retained
artifact16MiB. All coefficient/type/cap checks precede acceptance. Root owns
the serial worker and its RAM reserve; no second agent starts a job. A new
implementation would have a separate identity and never change the frozen
horizon scout's original degree9/horizon900 limits.

Only a calibration pass at900 closes the all-horizon gap using the earlier
complete83-panel atlas. A held-out-only pass at2^20 leaves901 through2^20-1
unproved. The no-new-compute infinite-source faithfulness theorem remains
independent of either finite sufficient test outcome.

Implementation handoff: `native_physical_tail_scout.py` and the separate18
tests in `tests/test_native_six_hour_physical_tail.py` are now written. No
author job has run. They authenticate the final H900 certificate and held-out
capture, and both completion/faithfulness notes at
`822646ffea23d906c385f0273a8c45693e982c4d`, before parsing source selections.
No frozen executable is imported. The H900 panel independently reconstructs
every coordinate of every selected alias and all36 selected physical rows,
not merely the20 chosen matrix columns, and must retain a verified inverse.
A singular H900 calibration is an acceptance refusal; the singular-minor
UNKNOWN status is permitted only for the later test.

Calibration command: `--phase calibration`. After root freezes its complete
artifact and all four owned files, held-out command:
`--phase heldout --calibration-commit EXACT_SHA`. The held-out gate checks
that the code, declaration, proof and tests are byte-equivalent under LF
normalization to that calibration freeze, then freshly reconstructs and
compares the complete calibration result before running2^20. No source
constant needs editing between phases. Both positive and inconclusive
calibration results are retained. The new scout has no authority to rerun
at a different horizon or to change the fixed minor.

The eighteen controls include independent direct polynomial derivatives,
the originalfactor2 and1/g weight, finite later-term checks against the
geometric remainder, same-majorant subtraction, exact inverse products,
strict boundary/noncontractive/singular outcomes, cached numeric-type
counterfeits, source ordering, complete alias coverage and arithmetic caps.
The4096-bit guards check stored rational arithmetic results; they are not
a measurement of every temporary unreduced integer in Python's Fraction
implementation. Root's process memory guard remains the resource ceiling.
