# Independent certificate for the higher-horizon activation campaign

This new certificate is separate from the registered acquisition. Its
calibration is H25 at69b5322bc872b46d42d4f74e13dcbeb96c1421da and its
unchanged-executable heldout panels are H30 and H60 at
833f5c6eec591bcee722a5c498a5ac46e3d3dd0a. No additional horizon, source mask, start,
or numerical acceptance rule is chosen by this certificate.

Both heldout horizons have a certified global minimum over ALL continuous
monotone primitive source paths, in the original observation norm. Every
one of the16 registered starts passed the root and full-source cone
certificate at each of the three horizons. The table gives approximate
readouts of the exact rational root and energy enclosures, not numerical
acceptance thresholds.

| H | Ordered records / ratios | lambda | mu | Minimum energy | Clipping |
|---|---:|---:|---:|---:|---|
| 25 |63 /45|-0.267524382746|1.206702516665|157.467335272606|lower only|
| 30 |75 /55|-0.220714216365|1.360423376675|160.767320467090|lower and upper|
| 60 |140 /89|-0.320772810619|1.541078813377|173.725425806350|lower and upper|

Thus last activation remains globally optimal in these two heldout
sources even though their additional source moments are retained. The
planar profile changes: at H30 and H60 it reaches v=1 before u=1.
This is a result at the declared finite horizons, not an assumption that
the H25 minimizing path persists unchanged or that energy is monotone
under arbitrary changes of the horizon.

The acquisition retains every2,3,5-supported prime power and every
ordered pair nm<=H, followed by the original1/sqrt(nm) coefficient and
complete ratio collection. It decodes the source into its fixed twenty
moments, evaluates the original Gamma matrix, solves the three-coordinate
last-activation equations and tests the full twenty-coordinate gradient.
The sufficient global cone consists of eight actual univariate
quadratics; PASS, FAIL and UNKNOWN have different meanings. Failure of
that cone is not asserted to disprove a path minimum.

The final producer replays both complete frozen outputs and independently
checks the ordered-record census, all constant-plus20 decoder columns,
and every1/d physical column. It checks all sixteen retained starts,
exact whole root boxes, strict Krawczyk inclusion, nonsingular rational
preconditioners and the distinction between a planar and a full-source
certificate. Each recorded quadratic minimum is independently evaluated
at its endpoints and, when relevant, its interior vertex. A claimed
global certificate requires positive planar curvature and all eight
actual lower-envelope minima to be nonnegative.

In these captures the eight lower minima are strictly positive. Let
delta_H be any positive lower bound for all eight, and let c_H>0 be a
lower bound for the literal planar quadratic coefficient. The same exact
gauge and original Gram identity then give, for every competing path,

    I_H(path)-I_H(path*) >=
      2c_H integral|v(u)-clip(lambda+mu u)|^2du
      +2delta_H(integral w du+integral w dv)
      +||F_H(path)-F_H(path*)||^2.                     (1)

Indeed the affine endpoint tests imply both residual coefficients
P1+wP2 and Q1+wQ2 are at least delta_H throughout the cube. Pointwise
strict convexity of the clipped planar support gives the first term;
the remaining square is exactly the original physical field norm.
Equality forces w last and the continuous clipped graph du-almost
everywhere. Monotonicity excludes an interior vertical jump or a point
away from that continuous graph: either would force disagreement on a
nonzero u interval. Only the required endpoint completions remain.
The minimizing oriented path image is therefore unique, modulo
nondecreasing reparametrizations and pauses. At the two doubly clipped
heldout roots it is the clipped graph, including its two horizontal
pieces, followed by the final w segment.

An independent analytical source control considers every ordered pair of
multilinear monomials on each of27 fixed power paths
(t^a,t^b,t^c), a,b,c in{1,2,3}. Its literal2-times derivative integral is
2d_left/(d_left+d_right), or zero for two constants. All1728 coefficients
must agree with the twenty-moment decoder. This directly checks its
endpoint constants and source factor2, without using polygonal
integration as the comparison route.

A second exact control uses u=v=t,w=t^2. At H60 the complete ratio2
aliases have d=1,2,3,4,5 and the complete ratio5 aliases have d=1,2,3.
Here M6=1/5 and M13=1/2. The actual coalesced coefficients satisfy

    sqrt2 C2=-26831/23040-1/200,
    sqrt5 C5=-53/48-5/96.

These formulas come from the literal records in
`NATIVE_FIXED_PLANAR_LAST_COORDINATE_SUPPORT.md`. They test actual new
source moments after physical collection; no rank prediction is used
to assign them. The comparison concerns fields, not a claimed energy
penalty or a full retained-gamma source.

Fourteen registered final tests cover complete replay, unchanged heldout
source, all-monomial factor2, the physical aliases, bounded power-path
inputs, interior quadratic minima, three-way cone outcomes, false global
claims, root-box/contraction corruptions, retained refusals, omitted
records and incorrect physical columns, scope promotion, numeric aliases
and the declared horizon/exponent caps. The root reported calibration
and unchanged heldout discovery passes, including all48 full cone
certificates; the first heldout attempt had stopped at the RAM gate
before child launch and had no scientific outcome. At final-certificate
author handoff, its own producer and14-test jobs are pending. Their later
status belongs in the branch progress ledger and exact-freeze review.

The observed norm and all source normalizations are unchanged. An
all-horizon global minimum or a full retained-gamma identification is
not inferred from any finite successful panel.
