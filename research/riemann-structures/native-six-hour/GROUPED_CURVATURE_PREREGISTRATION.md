# Grouped native curvature acquisition preregistration

Declared before execution. No scout for these panels has run.
The source theorem is `GROUPED_NATIVE_CURVATURE_ENRICHMENT.md`.
This acquisition will not modify the earlier horizon60/300
synchronization-fiber scout or its registered predictions.

## Fixed panels and source construction

Use the distinct primes(2,3,5,7), and exactly these two groupings:

- (p,q)=(3,1): u-group(2,3,5), w-group(7); predicted rank11.
- (p,q)=(2,2): u-group(2,3), w-group(5,7); predicted rank15.

In each case enumerate all16 squarefree subset indices, including1,
and all256 ordered pairs of those indices. K=210 and every recorded
product is at most44100. This is the complete squarefree source-basis
panel, NOT the complete supported physical-product horizon44100
census. No conclusion about that complete census's physical rank is
registered.

Authenticate the exact L102707, frozen complete-curvature and
occupation sources named in the theorem before reconstruction.
Build each lambda_n from the local binomial formula, multiply the
actual prime factors, then identify equal group schedules. Do not
substitute a desired monomial matrix for the source construction.
Retain every lambda_n, every ordered-pair curvature, and all ordered
source indices. The predicted curvature monomials are the full
rectangle 0<=r<2p, 0<=s<2q with only its top corner omitted.

## Independent controls and witnesses

Differentiate source functions before comparing the prediction.
Build complete rational curvature coefficient columns and perform
exact rank elimination, retaining pivot rows and the matrix digest.
Check separately that source functions span all (p+1)(q+1) monomials;
that the top curvature coefficient vanishes; and that every other
curvature direction is present. A top-corner coefficient falsely
retained as an independent source direction is an explicit negative
control.

Use exactly the (2p) by(2q) center grid
(a/(2p+1),b/(2q+1)), 1<=a<=2p,1<=b<=2q, with the top-right point
removed. Half-width is1/[8(2p+1)(2q+1)]. For each rectangle choose
the common prefix

    (0,0) -> (u_low,0) -> (u_low,w_low)

and common suffix

    (u_high,w_high) -> (1,w_high) -> (1,1).

Compare w-then-u minus u-then-w in the middle. Independently integrate
the literal polarized current2d(lambda_n)lambda_m along all segments
and compare each of the256 differences with direct rectangle
integration of the corresponding curvature. Retain all witness
vectors and their exact rank. Predicted witness ranks are11 and15.
Reverse the ordered path difference as a sign counterfeit.

The acquisition proves no arbitrary occupation-coordinate
attainability, physical coalescence rank, original-kernel metric
claim, or full retained-gamma identification. The all-height source
theorem rests on the polynomial proof, not extrapolation of these
finite panels.

The theorem's additional physical-prefix reconstruction is proof-only:
at reduced ratio a/b, the jump between cutoffs d^2 ab and d^2 ab-1
isolates one actual labelled pair with its factor1/(d sqrt(ab)). No
44100-prefix computation is registered. The required observation is
the family of full physical fields from one common path, not energies
or a single cutoff; no uniform reconstruction bound is claimed.

## Resource and acceptance limits

Two panels only; four fixed primes;16 indices and256 ordered pairs
per panel; at most15 curvature columns and15 rectangle witnesses;
source bidegrees at most(3,2), current/curvature bidegrees at most
(6,4); path polynomial degree at most8; exact rational numerator and
denominator4096bits; artifact8MiB. No prime search, floating point,
external CAS, unregistered precision, or additional physical census.

Any cap violation or failed prediction is retained and reported.
Do not change a grouping, rectangle or prediction after a result.
Root alone runs the scout under the existing serialized RAM guard.
A final certificate, strict JSON acceptance and dedicated tests, if
requested, will be separate from this initial acquisition.

The standalone scout is `grouped_native_curvature_scout.py`. It
authenticates the three declared source objects without importing
another producer. Root may capture its full stdout with:

```text
python research/riemann-structures/native-six-hour/grouped_native_curvature_scout.py --discover
```

All scientific execution remains pending at implementation time.
