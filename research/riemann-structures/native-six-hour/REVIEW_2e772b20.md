# Independent review of the finite native activation faces

Reviewed scientific freeze:
`2e772b2048a00af9b6d59aa594f2e64ec96a5529`.
Reviewer: the separate generalized-L review agent, read-only with respect to
the scientific packet. No mathematical or implementation blocker found.

I read all of `SECOND_ORDER_NATIVE_FACE_SELECTION.md`,
`native_finite_face_certificate.py`, its11 tests, the authenticated finite
face scout, and the literal-prime acquisition producer/preregistration.
I checked the relevant frozen leading-cluster and original-source inputs,
the four direct commit/blob bindings, representative artifact intervals,
all recorded individual second-moment gaps, the scope declarations and
owned-file hashes. The four scientific files agree with the exact freeze.
This was a proof/code/artifact audit, not an independent execution of the
large exact rational calculations or a manual recalculation of every
artifact numerator.

The coordinator confirms successful focused formatting/lint, producer
write/check/optimized-check, and11 tests in ordinary mode plus11 tests in
optimized mode. Those are coordinator executions; I ran no scientific
jobs. Read-only Git comparisons and file hashes were used to bind this
review to the stated source and current LF bytes.

## Mathematical checks

The original kernel has the stated negative quadratic coefficient and
positive bounded third derivative. Substitution of the actual frequency
`2 epsilon Delta` gives the positive `(72/A) epsilon J` correction after
normalizing the minimization problem, and the derivative-count remainder
`240 C_r T^3 epsilon^2/A` is consistent with the complete ordered subset
sum. The four-prime and six-prime individual inactive gradient gaps have
the correct signs and constants. The argument does not mistake a negative
symmetric-direction derivative for all-coordinate KKT feasibility.

The transfer to actual prime tuples correctly strengthens the log-window
error to O(epsilon^3). After normalization this changes gradients only by
O(epsilon^2), so it cannot overwhelm the decisive order-epsilon terms.
Strict tangent concavity from the singleton band and strict positivity of
the original observed Gram form justify the active-face stability and
uniqueness. The cofinal prime argument fixes each epsilon before applying
ordinary PNT; it does not claim a uniform shrinking-window theorem.

The finite KKT numerator is the exact denominator-cleared original-kernel
comparison. Every inactive coordinate is retained. The physical multiplier
`4/(4^r K)` and every one of the `2^r` factor allocations remain present.
The two-stage power paths realize the central activation coordinates with
the original `2 ds` integration. Early activation of other primes does not
delete factor allocations containing them.

## Primitive replay and acceptance

The six primes come from fixed first-prime windows, with complete bounded
trial division, before testing a kernel face. The certificate recomputes
all windows and both original-kernel models. The scout independently agrees
its nine literal overlap integrals with the native closed expression;
cardinality separation and the small-shift domain are exact integer tests.
The new256-bit input cap is explicit and does not weaken the frozen64-bit
helper. Outward512-bit dyadic serialization occurs only after exact KKT
decisions and preserves interval containment; the integer-string safety
limit remains enabled.

Canonical full-payload comparison distinguishes integer and floating JSON
counterfeits. The executable scout is authenticated before compilation,
and the remaining declared source bytes are authenticated before their
results are used. All11 test sources are relevant controls, including
prime acquisition, full coverage, actual KKT signs, physical scaling,
kernel overlap, source paths, cap refusal and typed acceptance.

One portability limitation is explicit: this frozen certificate binds its
owned note, producer and tests by raw-byte SHA256. The reviewed checkout is
LF, and its three hashes match the fixture. A checkout that rewrites those
files to CRLF will fail replay despite retaining the same canonical Git
blobs. This is fail-closed behavior rather than a mathematical flaw or an
acceptance bypass. A separate checkout-attributes change can preserve LF;
this review does not rewrite the frozen packet or silently normalize its
historical hash contract.

The result is finite original-source face selection and a cofinal theorem
under its stated cluster hypotheses. It does not identify the complete
post-renewal gamma, reconstruct an amplified principal member, or prove an
RH/GRH estimate. The absolute improvements remain small at the retained
physical scale.
