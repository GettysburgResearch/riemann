# Independent review of the canonical native preimage

Freeze: `e85e827a9ddd8122d234e36067cac687397b45d7`.
Reviewer: `/root/recent_landscape`, independent of proof and implementation.
Finding: no mathematical, normalization, provenance or replay-acceptance blocker.

I read the entire proof, producer and ten tests; checked the four frozen path
identities and absence of subsequent path-specific differences; and inspected
the artifact's source/measure/count metadata. No scientific job was run by me.
The coordinating agent reports all producer modes and ten tests in each mode
passing. Those execution results are not represented as an independent rerun.

The source diagram has the correct order. All half-geodesic factor allocations
and derivative sites integrate with `2 ds` before product collapse; the two
owners then select the coefficient `mu(D)`. Multiplication by the actual
Vaughan factors, the two owner orders with their separate Beta integrals, and
the history-dependent completion multiplier produce the stated physical
coefficient. The independent half-source convolution is a valid second route
to the same recombined coefficient, not an assertion that its atoms coincide.

The earlier draft's Beta-measure ambiguity is repaired in this freeze:
`(1-theta)dtheta` is the base measure and `theta^core_depth` stays in the
integrand. Squaring therefore doubles that exponent. The producer implements
this convention and distinguishes continuous, integrated-site, history and
fully recombined diagonals. On the zero chart the first three are respectively
`29/25200`, `1/14400`, `1/150` before `1/(NM)`, while the last is zero.

The complete 24 histories expand to 3072 nested bilateral sites with the
declared positive/negative histogram. Completion puts them at a common final
Mellin frequency, so their cancellation survives that observation. It is not
a statement that the raw pre-collapse ratio field vanishes. The positive
fixture uses the same maps and measures and retains 256 nested sites. The
endpoint-colour probability diagonal is correctly separated from the squared
weights of counting atoms.

Exact frozen executable bytes are authenticated before execution, and all
inherited sources are authenticated. Canonical JSON comparison rejects the
integer/float and integer/boolean aliases relevant to a JSON-loaded artifact.
The scope explicitly excludes identification with complete post-renewal gamma,
preservation of primitive ratio norms, and a full principal-family estimate.

| File | Git blob |
| --- | --- |
| `CANONICAL_NATIVE_PREIMAGE.md` | `ba8a7e59a60509b2c85d86f0e22ddbc554a6c4b8` |
| `canonical_native_preimage.py` | `f8a73d28de4db484b5286bca3521164b19efdc84` |
| `canonical_native_preimage.json` | `50c05a2cf3b899b13c476bbf2c5553fa9e42a834` |
| `tests/test_native_six_hour_canonical_preimage.py` | `336d102cce53288a2d1465c033acc2c56f5ae749` |

Artifact proof object:
`cdf3dbe481df2f2ddafe8e32ad8d1d81e0812800cc1a03d61224d3e8d31d3b47`.
No bound source was changed for this report.
