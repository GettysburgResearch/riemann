# Preregistered original-kernel optimization on the synchronized source face

This experiment is declared before its kernel computation. It uses
only the horizon25, primes2,3,5 half-source, all63 ordered factors and
all45 physical ratios, actual2ds, and the original Mellin measure nu.
No coefficients, source windows, or measure are selected from an
optimization outcome.

The actual source subclass is u=v. Its complete attainable occupation
body is0<=D<=1,D/2<=E<=D-D^2/2. Every point has the explicit monotone
piecewise linear path from the synchronization packet. The observed
field is F0+D R0+E R1, where R0=V25,0+V35,0,R1=V25,2 and F0 has
(A,B,C,D,E,F)=(1/2,1/3,1/3,0,0,0).

The scout will compute the exact original-kernel Gram2x2, two linear
terms and the constant, then retain all candidate classes:

1. The unconstrained two-dimensional stationary point, if feasible.
2. The stationary point on the straight lower boundary E=D/2.
3. Every real stationary point on E=D-D^2/2, isolated inside(0,1).
4. Both endpoints(0,0) and(1,1/2).

The upper-boundary derivative is cubic. An outward interval Sturm
sequence will certify its root count and isolation; maximum degree3,
maximum80 dyadic subdivision levels and1024 interval nodes are fixed
caps. A sign that cannot be certified is a retained failure, not a
reason to change the source or invent a root. Exact original interval
arithmetic is outward-rounded at512bits. Roots are isolated to width
at most2^-64. Every candidate is checked against the full convex-body
KKT conditions, including the sign normal to a curved boundary.

Strict positive definiteness of the original two-vector Gram gives
uniqueness once a complete KKT point is certified. The producer will
not infer optimality merely from a grid or from optimizing on the
curved boundary alone.

All of the following comparisons are retained whatever their signs:
the diagonal geodesic(D,E)=(1/2,1/3), the genuine pathw=s^2 with
(D,E)=(1/3,1/4), and the previously fixed joint quadratic schedule
(a,b,c)=(1,0,-1). The latter is not on the synchronized face. A worse
best face energy is a valid outcome, not an omitted experiment.

The result, if certified, is an attained global minimum only within
this actual synchronized source subclass. It is not a full all-path
minimum, a literal Wick-diagonal minimizer, a full gamma decoder, or
a principal-family estimate. The path realization can use five equal
parameter-time segments; the optimized quantity is the stated
recombined observed current, not a reparameterization-invariant claim
about primitive site squares.

A concrete rational path will also be retained. Round the certified
coordinate midpoints to nearest dyadics of denominator2^48; on a
boundary set E by its exact boundary formula. Reconstruct the legal
five-segment path and integrate every original factor independently.
The resulting exact native energy is compared with the certified
minimum, retaining the gap even if it is unexpectedly large. No
second search is permitted to improve this rational witness.
