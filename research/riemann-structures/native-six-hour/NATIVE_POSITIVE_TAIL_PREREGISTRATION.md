# Fixed positive-tail acquisition

Use exactly the completed primitive source on primes2,3,5, the exact
positive coefficient majorant of NATIVE_POSITIVE_COMPLETION_TAIL.md,
and the original kernel mass from the completion theorem.

Calibration panels are H=25,30,60. After freezing that executable,
declaration and output, the SAME executable evaluates heldout
H=900 and H=2^20. No other horizon, prime, coefficient majorant or
arithmetic precision is chosen after results. All smooth product costs
2^a3^b5^c<=the phase maximum are retained with their exponent vector,
exact rational g_k and directed interval contribution g_k/sqrt(k).
Every declared panel uses the complete prefix of this same ordered list.

The local beta_e is computed by the central-binomial formula, independently
of the coefficient recurrence used in earlier source scouts. Exact
convolution gives Amax^2 and D Amax. The finite prefixes retain every
exponent allocation; the closed total uses the positive roots in the
formula for Amax and D. The local degree cap is20, integer cost cap2^20,
retained cost cap4096 and artifact cap8MiB. Worker memory is targeted
below128MiB and the coordinator's RAM reserve remains in force.

All interval operations round outwards to the dyadic lattice2^-512.
Square roots are bounded by integer-square-root inequalities, including
exact endpoint detection. Input and intermediate rational sizes are
capped8192bits. The original log2 is enclosed by256 fixed positive
atanh-series terms at1/3 and its explicit geometric remainder.
No floating result enters acceptance; decimal values, if printed, are
diagnostics only. No physical Gamma matrix or numerical quadrature is
computed.

For every panel retain the closed majorant total, complete prefix,
nonnegative total-minus-prefix enclosure, the independent C0/sqrt(H)
bound, their smaller upper bound, and the induced original Hilbert,
energy and global-minimum-value error bounds. Cphys uses the smaller
aggregate A(q) from the completion theorem, not Amax. The two roles
must remain distinct in both formulas and metadata.

The acquisition also checks the exact source-square coefficient identity
through degree20, all nonnegative local coefficients, and
g_1,g_2,g_4,g_6,g_8=0,1,5/4,2,1. Complete record lists, counts and
digests remain in the output even if the certified tail bound is loose.
No signed observed tail is captured, and no optimality persistence or
full retained-gamma claim is made.
