# Next native gate: extract one complete observed source member

This is a future-work proposal, not an execution record. The aim is an
exact source readout, without assuming its difference from the positive
canonical arithmetic candidate is small. The complete-candidate theorem
and fibre-distribution corollary already rule out that shortcut if the
intended native principal moment is subpower.

**Target.** For one named finite-horizon source occurrence, derive the
entire retained-label coefficient measure of a fixed principal member:

    W_native,iota(t) = integral sum_omega z_iota,omega(t) dmu(omega).

The formula must specify carrier, homotopy parameter, endpoint colour,
owner allocation, renewal ancestry, selector, physical Mellin phase, and
literal Wick atom resolution. Then compare it with the corresponding
canonical member in the SAME conductor-weighted L2(|kappahat|^2 dt/2pi)
space. Do not replace the source measure by a counting diagonal or declare
an empty-colour/no-renewal label to have coefficient one.

**Acquisition path.** Start with the actual literal weights in
86cac1d64364015ec2cc0f8fbb6fc75dc041c12b, L-106026.8 and L-106093,
especially the latter's anchor-dependent masks and final family square.
Use L-106133 for the exact labelled Boolean algebra and L-106134 to
separate its Wick image from ordinary-completion contraction terms. Trace
the latter to ec6635b4c7dcd08fe433b7ae7e1d9a8c9495dfcc, T-102990 and the
L-102951/L-102954 quotient transfers. Their fixed outer/logarithmic-L1
costs must stay labelled until a principal-norm transport is proved.
Exact filenames, blobs, and preserved-observation boundaries are already
listed in NATIVE_DECODER_OBSERVATION_GAP.md. Follow definitions only until
the primitive source integrand and measure are explicit; a cross-reference
to an intended final estimate does not supply them.

**First bounded witness.** Reuse the frozen dense eleven-prime fixture;
no new prime acquisition is needed. Retain all carrier/colour/renewal
terms that contribute to its selected physical indices, not just its
balanced Boolean row. Symbolically integrate the native finite parameter
densities, or give an exact integral formula if no elementary evaluation
exists. Separate physical outputs from mechanism provenance. Replay the
complete tuple coefficient and its literal diagonal before attempting any
large-horizon estimate. The relevant residual may be large and coherent;
that would be useful information rather than a failed small-error test.

**Success criteria.** An exact source-to-readout identity with an explicit
measure and Wick correction; identification of an actual missing term
whose canonical comparison has the required original-norm size; or a
specific source-order/readout obstruction showing the proposed member
cannot represent the original observation. Any numerical fixture must
follow the proved occurrence map, not define it after the fact.

**Hard failure / productive fallback.** If the cited statements do not
define a regional source occurrence or its literal measure, stop that
decoder attempt and record the exact missing definition and first
unjustified equality. Do not invent gamma, import only an outer-L1 bound,
or obtain success by changing the principal norm. A source-exact missing
interface certificate is preferable to another coefficient variant,
ambient-gauge obstruction, or reproof of an already-retracted Q estimate.
