# Independent review of canonical anchor-diagonal saturation

Reviewed scientific commit: `ce8c857a039cde7f0affa0b1abd346e356255a73`.

Scope: its four files, containing the mathematical note, producer, artifact and ten tests. I independently read the relevant old local identities and both prior retractions, reconstructed the new factorization and normalization, and read the complete producer and tests. I then checked the frozen file set and final validation paragraph. I did not run computations. Root reports Ruff, producer write/check/optimized-check, and ten ordinary plus ten optimized tests passing (1.122 and 1.183 seconds), followed by the final binding replay. Reported proof object: `32a232800ee1d56d2b32cc3ea7bfdc634a6d019b3678a4e621e05b0ffbe322fa`.

Disposition: no remaining blocker identified for the explicitly restricted seven-window canonical coefficient model.

The historical status is essential. R-106095 and R-106110 already identify the unpaid opposite-owner count and retract the old complete same-anchor claim in L-106094. I found and read these refutations before the new packet was finalized. The final packet correctly credits that correction and adds an actual cofinal coefficient-source saturation; it does not present the old omission as newly discovered. The earlier fixed-Q estimate and the later dual-amplified operation remain separate.

The source uses the unchanged dense-owner windows, actual physical indices and positive Boolean histories. The aggregated one-sided amplitudes are exactly `1/(3 sqrt(N))` and `1/(3 sqrt(M))` with their native Mellin phases. The principal factor `c_ell=(ell+1)/(ell-1)` is retained. Q is external to the left anchor and is counted exactly once in its actual quadratic class.

At fixed g, ell and P the common Q-phase cancels inside the tail norm. Multiplication by the old external weight `g^2 ell Q` cancels the reciprocal Q coefficient, leaving the exact factorization

`H = (#Q/81) (sum_g g^(-2)) (sum_ell c_ell/ell) (sum_P P^(-1)) S_rho`.

The source kernel bounds give `S_rho` comparable to `(sum_rho 1/rho)^2`, while the ordinary prime number theorem in fixed proportional intervals supplies all counts and reciprocal sums. The resulting order is `Theta(U/log(U)^8)`, equivalently `Theta(Y^(1/6)/log(Y)^8)`. No growing-modulus prime distribution or point evaluation at zero substitutes for the observed norm.

Diagonalizing the right-tail prime instead replaces that last factor by `Gamma(0) sum_rho rho^(-2)` and gives `Theta(log(U)^(-7))`. The literal two-left-history anchor resolution gives H/2, and the full four-history atomic resolution gives the arithmetic atomic quantity divided by four. I checked these factors independently; the growing anchor-diagonal quantity is not obtained by merely relabelling the literal atomic diagonal.

The producer authenticates seven frozen blobs before parsing the dense fixture. It checks the complete sixteen-pair/four-Q rectangle, source classes, physical coefficients, the exact c_ell factor and the direct-versus-factored physical sum. It preserves the explicit distinction between a generic reciprocal-tail algebra control and a newly certified prime-source fixture. The actual frozen prime panel has one rho prime, so it checks the Q-count and normalization but does not purport to demonstrate asymptotic rho coherence; that assertion is proved from the windows and kernel in the note. No executable dependency is imported and no new prime search or phase enumeration is performed.

The restricted coefficient model is not asserted orthogonal in the physical Mellin space. Consequently this review does not certify a lower bound for an unrestricted canonical tail, the full retained-gamma source, the corrected dual-amplified moment, or the native principal moment. The result is a source-specific realization of a known obstruction, with its original observation and history normalization retained, not a new general theorem about RH.
