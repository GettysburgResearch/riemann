# Independent review of the observed subcritical Boolean block

Reviewed scientific commit: `1623f1924c62035918a94bcacf2ccad7d3bb6cf7`.
Review date: 2026-08-31. Reviewer: independent `recent_landscape` agent.

Result: no blocker found for the stated coefficient-projection theorem.
This does not promote it to a theorem about the complete carrier-weighted
source, the quartic Wick observable or RH. I independently read the proof,
source contracts, producer, scout and tests; I ran no computational jobs.

## Frozen scope and provenance

The six reviewed files are `SUBCRITICAL_OBSERVED_BOOLEAN_BLOCK.md`,
`subcritical_observed_boolean_block.py`, its `.sources.json` and `.json`
files, `subcritical_boolean_scout.py`, and
`tests/test_subcritical_observed_boolean_block.py`.
The proof's LF SHA-256 is
`366f4dbb5b920d62090b5988252a5306ad50505c97adb853706899aef9e62d05`.
The complete proof-object identity is
`3385b68dd20c2c10a361eac3afda8aa1f5b3ba767649ed7d7016913650ace388`.

The manifest fixes six exact source blobs across commits
`86cac1d64364015ec2cc0f8fbb6fc75dc041c12b` and
`ec6635b4c7dcd08fe433b7ae7e1d9a8c9495dfcc`. In particular its L-102880
is the logarithmic-derivative detector file with blob
`d7330d114ebba1a7a16e22fa9ba6aa6b5eb7cdd6`, not the unrelated file
sharing that numeric lemma label. The source comparison checks actual
Git blob bytes against compiled identities, rather than trusting hashes
supplied by the manifest itself.

## Independent mathematical checks

* The eight strict prime windows are disjoint for sufficiently large
  dyadic U. They give the stated common core, coprime reduced cores,
  owner disjointness, least-prime orientation and legal equal-pair owner
  gauge. Their rational endpoint bounds put both completed products
  inside `(Y,1.1Y)` with `Y=U^6`. This is not a conversion to the
  deterministic largest-two gauge.
* The frozen Boolean convention is
  `a_U=epsilon-mu_(<=U) star 1_sf`. Thus `a_U(A)=-1` and
  `a_U(B ell)=+1`. Enumerating the complete three-label convolution
  leaves precisely the two displayed histories, each with coefficient
  `-1`; every other allocation vanishes. The same argument applies on
  the right. The bilateral sign is positive because both full one-sided
  coefficients are negative, not because signs were replaced.
* Depth five gives owner share `1/10`. Consequently the one-sided
  coefficients are `-1/(5 sqrt(N))` and `-1/(5 sqrt(M))`, the bilateral
  coefficient is `1/(25 sqrt(NM))`, and its four literal history terms
  each have coefficient `1/(100 sqrt(NM))`. The squared aggregate is
  four times the sum of the four literal squares. That distinction is
  correctly preserved in the code and scope discussion.
* The physical observation is the actual autocorrelation of the frozen
  logarithmic kernel under L-106026 Plancherel. I independently checked
  the three elementary integrals, all jumps and the monotone pieces:
  the supremum is `8 sqrt(2)`, full variation `32 sqrt(2)`, and squared
  norm `(384+128 sqrt(2)) log(2)-288`. The BV translation estimate has
  penalty `512 |v|`, including the factor one-half in the squared
  translation identity. The rational inequalities yield the strict
  lower bound `544/15>36` throughout the stated ratio window.
* The complete phase sum, rather than each individual nonzero phase,
  has Ramanujan product one on this clean restriction. The final text
  makes that distinction. No phase-cardinality factor belongs in the
  linear physical pairing.
* Fixed-proportional-interval PNT gives eight-tuple count
  `U^(25/4)/(log U)^8`. Unique factorization and the disjoint windows
  prevent duplicate ordered interactions. Each weight is comparable
  to `U^-6`, so the observed coefficient restriction has exact order
  `Y^(1/24)/(log Y)^8`.
* The stated unsigned shell estimate is
  `Y^(1+epsilon)/(G^3 C D)`. Both reduced-core scales must be retained;
  throwing them away gives the weaker exponent. The elementary
  `6^k` bound for a k-label Boolean convolution is sufficient for this
  chosen coefficient chart, but does not prove a multiplicity bound
  for an unspecified carrier-word refinement.

The frozen note therefore proves saturation of its unsigned exponent
under the stated source coefficient restriction. It does not prove
positivity after complementary signed pieces are restored. In particular
`J_U` is the pre-family-square physical cross pairing: it must not be
reported as the quartic principal/Wick observable, its diagonal-subtracted
version or its source-dual amplification.

## Code and hostile controls

The published producer verifies 24 fixed prime certificates and does not
repeat a prime search. The Proth criterion and its elementary proof are
valid: every prime divisor must be `1 mod 2^e` and therefore exceeds
`sqrt(n)` under the certified Proth form. Exact trial division is bounded
to the small phase primes. Type, size, exponent and witness requirements
are explicit, and no probable-prime result is mislabeled as a proof.

The 27 allocations per side are genuinely enumerated. The kernel is
recomputed in exact rational `Q(sqrt(2))` arithmetic, including its
endpoint jumps and integral constants. The interval/ratio checks use
integers and fractions. The producer also verifies the exact distinction
between aggregate and literal diagonal squares, and the conversion
`p_iota |z_tilde|^2 = g^2 ell rho c_ell c_rho |z|^2` for the declared
coefficient. That conversion is a bookkeeping check, not a new bound.

I read all 11 tests, including wrong windows despite valid prime inputs,
changed Boolean cutoffs, composite and malformed certificates, exact
quadratic signs, counterfeit numeric values and forged source hashes.
The canonical comparison rejects boolean/float substitutions and NaN.
No necessary acceptance check relies on an `assert` removed by `-O`.
The optional scout is separately bounded and is explicitly only fixture
search; it cannot establish cofinal PNT existence.

## Execution evidence and limits

The root/writer reported the complete serialized Ruff, producer
write/check, optimized check, and 11 ordinary plus 11 optimized tests
passing at the frozen proof-object identity above. The root also reported
the separate bounded scout run. These execution results are reported
evidence, not jobs performed by this independent reviewer.

The infinite prime-counting input and elementary analysis proof are not
machine-proved by the three fixture panels. Full carrier transport,
endpoint/colour assembly, renewal provenance and complementary source
terms remain unbound to this projection. A bound on their signed sum
cannot be invalidated by the positivity of this restriction alone.
The result only defeats a stronger cancellation claim required to hold
uniformly for every such restriction. No external priority, full-source
lower bound, or RH/GRH implication is certified by this review.
