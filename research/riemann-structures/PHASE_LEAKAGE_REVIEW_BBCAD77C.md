# Independent review: native phase leakage and its complete connection

Reviewed scientific commit: `bbcad77cf87f093b2c0513420069f3ee97791789`.

I independently read the complete proof, producer and nine tests, and checked the repaired provenance and Mellin-orientation wording in the frozen note. I did not execute computational jobs. Root and the writer report generation, ordinary/optimized producer checks, Ruff, and nine tests in each mode passing.

The source identity `H'=GE'+G'E`, together with endpoint identity gauges, gives the stated off-sector cancellation after homotopy integration in the original Lebesgue measure. Both selectors and observations must remain independent of the homotopy parameter. The proof correctly retains the integration-by-parts correction for a variable carrier density or projector, and it recomputes the output least-prime selector on the actual output.

I requested a precision repair to the draft's provenance wording. The frozen note now distinguishes common output/spectator coordinates from mechanism and derivative-site labels: cancellation occurs in the recombined output coordinate, while those labels can separately define an atom resolution with a nonzero diagonal. It no longer suggests that distinct literal history coordinates cancel without summation.

For the authenticated selector-changing column, I checked the physical factor 1/3, the common primitive/Mellin phases, the native principal weight, and the three integrated coefficients `(-14,15,-1)/166320`. The density-tau residual is `1/123552`. The observed Gram has zero total sum but positive diagonal. The two-mechanism, three-group and eleven-site diagonals are respectively `2`, `422` and `175/2` times the stated common squared scale. These resolutions must not be exchanged silently, and the note does not do so.

The producer authenticates the exact Euler polynomial and selector-record primitives before executing their bytes. It compares direct polynomial differentiation/integration with beta-integral formulas, checks generic bounded owner/core depths, reconstructs the physical selector transition, and records the common-output Gram and distinct diagonal resolutions. Tests cover the omitted-owner/core/connection residuals, nonconstant measure, endpoints, source-byte refusal and numeric-type caps. I found no remaining blocking defect.

This repairs a specific complete source-path column and explains why the earlier transport-only amplification does not refute the full integrated connection current. It does not identify that current with the complete balanced T-106140 family or its paid diagonal. The global-horizon example also does not occupy one unchanged dyadic shell. The full balanced-family adapter, carrier measure and normal-ordering correction remain open.
