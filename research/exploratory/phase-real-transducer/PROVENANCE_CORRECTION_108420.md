# Provenance correction for continuation 108320

The exact parent of continuation `108320` is

```text
4a78ba328a0d4e2505824b5c55e6239cfbabaa2f
```

as shown by the Git commit graph and the parent field of commit
`16d9c586d2f7439187e756066410622fd51c6d56`.

Two files in the `108320` packet transcribed that parent incorrectly as

```text
4a78ba3ca89dcb63fcccf50d6738f01e28c80c64
```

which is not a repository commit.  The error is provenance-only: it does not
alter a formula, result object, or claim.  This correction is recorded as a
new file rather than silently rewriting the earlier continuation manuscript.
All `108420` metadata use the exact live parent.
