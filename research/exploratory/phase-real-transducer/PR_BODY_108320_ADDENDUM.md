## Checkpoint 108320 — prime knots, curvature--Speiser flow, and soft localization

This checkpoint continues the exact head containing `PFR-T5`, `PFR-T6`, and
`PFR-R2`.  RH remains unproved.

### PFR-T7 — actual-Xi prime-knot spline

The Gamma-resolvent response is now shown to be `C^(m-2)` with universal
prime-power derivative jumps

```text
[R_(a,m)^(m-1)]_(log n)^+ - [R_(a,m)^(m-1)]_(log n)^-
  = (-1)^(m+1) Lambda(n)/sqrt(n).
```

Distributionally `(a-D)^m R_(a,m)` is the critical explicit-formula source.
Thus one real function contains primes as local knots and zero displacement as
its global growth/energy abscissa.

### PFR-T8 — flower curvature is zeta-prime phase acceleration

On every open Hardy petal,

```text
D_H = |zeta'|^2,
-theta' C_H = Re(conj(zeta') zeta''),
-theta' C_H / D_H = Re(zeta''/zeta') = d_t arg zeta'.
```

The positive flower-curvature defect is therefore exactly the positive
variation of the phase of `zeta'`.  A finite polynomial theorem writes the
same field as a signed sum of Poisson kernels from derivative critical points.
The basic curvature identity is known literature; the project contribution is
its exact insertion into the PFR-T6 zero-count ledger.

### PFR-R3 / PFR-T9 — localization boundary

A nonvanishing holomorphic filter cannot alter the infinite-time zero
abscissa: arbitrarily small leakage eventually wins.  Exact hard windows are
already impossible by `PFR-R2`.

A complex Gamma filter nevertheless gives a source-defined two-channel real
signal with explicit coefficient suppression outside a guarded ordinate band
and a rigorous finite-horizon leakage bound.  The next burden is an in-band
lower frame bound; no local zero theorem is claimed.

### Replay

```text
PASS_PFR_PRIME_KNOT_CURVATURE_AND_SOFT_LOCALIZATION
checks=7
proof_object=8548718c9de79bdaffbae1bdb700be52981a1af10f8cb241442d8d4e2626ffbe
RH_UNPROVEN
```
