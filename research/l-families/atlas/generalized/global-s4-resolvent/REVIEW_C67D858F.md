# Independent review of the actual S4 quadratic source

Scientific freeze: `c67d858fc9f7112cf9f0b13049a97d68de0d429b`.

Scope: the six new files in `research/l-families/atlas/generalized/global-s4-resolvent/`: `QUADRATIC_TWIST_SOURCE_AND_DIAGONAL_INERTIA.md`, `quadratic_twist_replay.py`, its test, primitive source, artifact and provenance. I independently reconstructed the geometry and local-factor conventions, read the proof, producer and tests, and checked the final committed coverage wording. I did not execute a test suite or producer. Root reports focused Ruff, complete generation and ordinary/optimized producer checks, and 14 tests in each Python mode passed.

## Mathematical result

No remaining mathematical blocker was found in the declared source class `p>3`, `b!=0`, `256c^3-27b^4!=0`.

- The quadratic cover `t^2=u` is geometrically disjoint from the S4 splitting cover because it ramifies at zero, where the latter is unramified. The joint group is S4 x C2. The 24 simple zeros of u and even infinity poles give genus 49 and anti-invariant H1 rank 60.
- Infinity has diagonal inertia generated by `(double transposition,-1)`. Twisting selects the negative double-transposition subspace. For standard and sign-standard this has dimension two: Frobenius traces are two for Q=1 mod4 and zero for Q=3 mod4, with second trace two in either case. The latter local denominator is `1-T^2`; a vanishing first trace does not remove the stalk. Zero and the nonzero finite branches have the stated scalar-inertia and residue-character rules.
- The four explicit quotient constructions have the stated normalizations: Dchi genus3 with one infinity point; X genus3 with `3+chi(-1)` infinity points; Rchi genus4 with one missing point over z=0 and `1+chi(-1)` over z=infinity; Cchi genus17 with `4(1+chi(-1))` infinity points. The normalized exceptional points matter to the trace identities.
- The anti-invariant determinants have degrees 6,6,4,10. The last is the sign-standard multiplicity space, not its three-times-larger isotypic piece. Their regular multiplicities give 60, agreeing with the cover genus. The conductor calculation `(5d-6chi_t+chi_tt)/2` independently gives the same ranks. Rational orthogonal S4 representations and quadratic twisting preserve the alternating cohomological pairings, so reciprocity and weights come from actual cohomology, not fitted roots.

## Replay and adversarial controls

The producer authenticates the frozen `3d3b5254` helper before import; that helper recursively authenticates the untwisted S4 source, including its artifact. Its exact primitive-source hash fixes the three-panel roster and field limits. The batch inverse routine excludes zero from the product and reconstructs all nonzero inverses with one exponentiation; the Rchi zero point is handled separately.

All fourteen declared fields are completely counted. Quadratic and fourth-power characters are reconstructed in each extension field, not inherited incorrectly from the prime field. Complete local stalk sums are compared with independently counted quotient curves and their projective corrections. The normalized regular-character count for Ztilde is explicitly identified as such; no affine splitting-variety enumeration is claimed.

The p5 degree-ten factor uses its first five traces and predicts the sixth. The p7 examples have only four traces: their degree-ten factor and large Cchi/Ztilde polynomials remain null, with a prefix modulo T^5 retained. The test suite expressly refuses promotion of that prefix. The smaller factors are complete by the proved reciprocity. Large polynomial degrees follow the proved decompositions, not dozens of independently acquired point counts.

Controls include actual S4 permutation characters and diagonal-inertia projectors, first/second Frobenius powers, direct prime-field equations for all four curves, opposite branch-twist signs at u=1,-1 over F7, extension-dependent infinity counts, batch inverses, invalid source strata/types, source forgery and full-factor availability.

## Review correction and limits

The draft coverage sentence could imply an independent quotient-fibre comparison at each u. The final freeze correctly says that every local trace is summed and the complete sum is compared with direct quotient counts; it explicitly disclaims a separate per-u quotient-fibre comparison. This was a reporting precision correction, not a change to the equations or computed data.

This is an actual function-field arithmetic source and ramified twist test for the analytic continuation programme. It is not a number-field transfer, a new RH/GRH theorem, a new proof of the classical Weil theorem, or an external priority claim for Kummer covers, Artin factors or Prym/cohomological constructions. Analytic infinite-grade conclusions require their separately defined parent and domain.
