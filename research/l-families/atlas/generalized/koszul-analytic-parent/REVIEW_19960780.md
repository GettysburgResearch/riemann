# Independent review of the S3 grade-cutoff anomaly

Scientific checkpoint: `19960780cf9582cea2105ddf3e89bee3e2c3b687`.

Reviewed files: `S3_GRADE_CUTOFF_ANOMALY.md`, `S3_CUTOFF_REPLAY.md`, `s3_cutoff_replay.py`, `s3_cutoff.verification.json`, and `tests/test_s3_cutoff.py` in `koszul-analytic-parent`. The checkpoint adds these five files.

## Finding

No unresolved mathematical or implementation blocker was found. The packet establishes an ordering-dependent anomaly for the previously defined actual S3 Lie-grade parent. It carefully distinguishes the whole-grade cutoff from the rational continuation and from an ordinary Fredholm determinant outside its trace-class domain.

## Independent proof checks

I read the entire proof and checked the source characters `F_e=(1+2t)/(1-t)^4`, `F_s=(1-t^2)^-2`, and `F_c=(1-t^3)^-1`. Splitting the source Adams inversion by powers of an element of order `d=2` or `3` gives the displayed primitive-character series. Its leading singular term is `-(1/d) log(1+2t^d)`. The degree support is confined to multiples of `d`, and the stated S3 eigenspace multiplicities follow from these traces and the already authenticated source dimensions.

In the whole-grade logarithm, the first Adams term cuts this singular series at `floor(N/d)`, whereas the `d`th Adams term cuts the identity series at `N`. Their difference is exactly the finite band

`-(1/d) sum_{m=floor(N/d)+1}^N y^m/m`, with `y=-2t^d`.

I checked the analytic remainder argument on `sigma<eta<2^(-1/(d+1))`, where `sigma=2^(-1/d)`. In particular, for order three the second Adams term is controlled using its improved nonidentity radius, and the large-Adams-index tail uses the dimension bound with `2*eta^(d+1)<1`. This is why the proof does not claim the larger disk suggested by the first singular term alone.

At a resonant boundary point `tau^d=-1/2`, the band tends to `log(d)`, producing the factor `d^(-1/d)` relative to `F_g(tau)`. At nonresonant points on that circle the band tends to zero. The compact-parameter transition with `t_N=tau*exp(w/(d*N))` is the corresponding Riemann-sum integral over `[1/d,1]`. The two stated exterior behaviors are restricted to real `y>1` and real `y<-1`; the packet does not assert a uniform divergence classification for all complex exterior points.

I also checked the explicit Cauchy remainder bounds, including the identity remainder, the extra order-three term, and the geometric large-index tail. The general exact-control variant uses `abs(t^d)` in the identity tail rather than retaining the critical value `1/2` away from the boundary.

## Independent code review

I read the complete producer and twenty tests. The producer authenticates the frozen S3 source and runtime before importing it. It constructs exact source traces and root-of-unity eigenspace multiplicities, checks low degrees against the actual frozen Lie quotient, and checks the finite Adams/PBW identity. Large dimensions are represented by multiplicities rather than expanded eigenvalue arrays.

The real logarithm enclosures use rational atanh expansions. Multiplicity weights are applied before outward rounding, avoiding loss of a small logarithm multiplied by a very large multiplicity. Public type and size checks precede cached helper calls. The tests cover source/eigenspace agreement, counterfeit dimensions and radii, exact low products, floor-cutoff behavior, analytic error bounds, both declared exterior behaviors, tiny weighted logarithms, and source-authentication failures.

## Execution evidence and limits

I did not execute the producer or tests. The coordinating agent reports Ruff, producer write/check, optimized check, twenty ordinary tests, and twenty optimized tests all passing at this checkpoint. My independent evidence is the proof, source, implementation, and test-code review described above.

The result concerns a specified source grading and cutoff order. It is not an arithmetic functional equation, a new Riemann hypothesis mechanism, or an assertion that the infinite Lie operator becomes trace class at the larger S3 radius. It supplies a concrete warning that rational continuation, Abel continuation, and a source-ordered boundary limit can differ.
