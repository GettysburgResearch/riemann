# Coherent quadratic place Euler source: exact replay

The [source theorem](COHERENT_QUADRATIC_PLACE_EULER.md) applies the actual
quadratic grading operation to the full place-Euler product, including
new inertia at zero and infinity. It proves that the first main Q^n
coefficient term disappears while the exact elliptic scale Q^(n/4)
survives. The later meromorphic natural boundary remains the unit circle.
These are different statements from the ordinary Lie operator's ideal
domain and the polynomial-growth product over finite cohomological grades.

The runtime authenticates the complete untwisted source theorem and
producer at `65204d3f6e43b737e36c40b090685cbd068e34e6`, and the actual
S3 quadratic source at `efdff67938b4ce8769a3cbaadec359898fec1654`, before
either import. Their frozen dependency chains authenticate the curves,
the homogeneous Lie modules, actual Frobenius histograms and ramification.

Every new primitive recount has field order at most49. The first two
actual extension traces and the established rank-four reciprocal duality
reconstruct the twisted standard and sign quartics. The degree-three
held-out check belongs to the already frozen source packet; it is not
rerun here or relabelled as a new independent computation. No unproved
high-degree polynomial is inferred from a trace prefix.

The low-degree closed-place replay retains the squaring of rational
Frobenius and evaluates the quadratic character in each actual residue
field. It compares the complete coherent Euler coefficients with the
independent finite Lie cohomological factors. The exact second-degree
bad correction is 6b plus the zero correction21,3,0 according to its
old Frobenius class, plus7 or3 at infinity. This differs from the
untwisted correction because the new inertia kills odd first-grade
stalks while retaining the even relation source.

Additional controls derive every new even local numerator from the full
invariant source; check integral finite extraction for both joint-cover
signs; and retain the actual odd/even twist law on the first four Lie
grades. The source first factor has H^1 rank12 and no H^2. The second
grade retains the same P_E(z^2) denominator as the untwisted source.
Reciprocal numerator constant terms9 and3 implement the exact norm
noncollision argument. The sharp coefficient limsup and all-degree
natural boundary are proved, not inferred from these finite checks.

From the repository root with the serialized Python runtime:

```text
python -B research/l-families/atlas/generalized/koszul-analytic-parent/coherent_place_euler_replay.py --check
python -B -O research/l-families/atlas/generalized/koszul-analytic-parent/coherent_place_euler_replay.py --check
python -B -m unittest discover -s research/l-families/atlas/generalized/koszul-analytic-parent/tests -p test_coherent_place_euler.py
python -B -O -m unittest discover -s research/l-families/atlas/generalized/koszul-analytic-parent/tests -p test_coherent_place_euler.py
```

Root executed the serialized focused Ruff checks, producer write/check
and optimized check, and all 26 tests in each normal and optimized mode;
all passed. The source proof, runtime and all controls have independently
been read without a blocking finding. The exact-freeze review follows
the final source binding. The fixture binds all four packet source files
and compares complete typed JSON values.
