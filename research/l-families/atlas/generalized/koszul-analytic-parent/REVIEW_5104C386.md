# Independent review of the coherent quadratic place-Euler source

Reviewed scientific commit: `5104c38614461a3e080c93631b855094d0e5961a`.

Scope: the five-file packet containing `COHERENT_QUADRATIC_PLACE_EULER.md`, its replay note, producer, artifact and 26 tests. I independently read the proof, reconstructed the source and arithmetic calculations, and read the producer and every test before freeze. I then checked the frozen file set and final execution statement. I did not run computations. Root reports Ruff format/lint, final producer write/check/optimized-check, and 26 ordinary plus 26 optimized tests passing (0.942 and 0.939 seconds).

Disposition: no remaining blocker identified in the declared characteristic-greater-than-three source class.

The operation is genuinely the graded algebra `R_n tensor chi^n` on the actual joint S3 and quadratic cover. Its Koszul Lie grade is consequently `M_n tensor chi^n`. This is distinct from twisting every grade once as a module, from the polynomial-growth product over proper cohomology grades, and from treating the original infinite Lie operator as trace class beyond its proved domain.

I checked the complete ramified factors. At the new zero place, the invariant operation deletes odd degrees and gives the three stated even Hilbert series according to the old Frobenius class. At infinity the diagonal inertia again retains even degrees, with the split and nonsplit Frobenius alternatives kept separate. The source has no pole in any local factor inside the unit disk. The finite-group extraction criterion therefore applies to this coherent algebra and gives a single-valued meromorphic source there, with its proved meromorphic natural boundary.

The first four actual Lie grades have the claimed proper factors. The first odd twisted grade has rank-twelve H1 and no H0/H2, so it does not supply the untwisted main pole. Grade two retains the denominator `P_E(z^2)`; grade three is an actual twisted standard quartic; grade four supplies only the later elliptic denominators. Three-grade extraction proves the quarter-power coefficient upper bound. Four-grade extraction goes beyond that circle and identifies its exact poles.

I independently checked the added noncancellation for the two new bad numerators. If a power of an inverse elliptic Weil root were a numerator zero, its reciprocal would be an algebraic integer with norm supported at the characteristic prime. Its minimal polynomial would divide a monic reciprocal source numerator with constant term 9 or 3. This is impossible in characteristic greater than three. Together with the already reviewed old bad and good factors, it proves the exact first pole polynomial, including multiplicities, and hence the coefficient limsup `Q^(1/4)`. This is not a nonoscillating coefficient asymptotic or an RH statement.

The bounded replay authenticates both frozen dependency chains before importing them. It evaluates the quadratic character in each actual extension field and subtracts rational Frobenius squares when extracting degree-two closed places. The source/cohomology degree-two correction is `6 b_1` plus the actual zero correction 21, 3 or 0 and the infinity correction 7 or 3. This tests the new even invariants rather than applying an unramified twist formula at the bad places. The runtime reconstructs the two twisted quartics using two primitive extension traces and the previously proved rank and reciprocal duality. The third-extension held-out check is explicitly inherited from the earlier frozen packet, not claimed as a new run here.

The remaining controls verify every new even numerator, source-derived finite extraction for both quadratic signs, the actual four-grade cohomological factorization, and the retained `P_E(z^2)` denominator. New primitive fields have order at most 49. The actual zero-place panels cover the classes recorded in the fixture; the remaining identity-class numerator is checked as an exact representation-source control, not represented as an additional primitive field panel.

The weighted effective-divisor interpretation is also correctly scoped: the quadratic operation signs old-place coefficients and deletes odd contributions at the new ramified places, so coefficientwise absolute values are bounded by the untwisted positive source. This does not identify the resulting coefficients with those of an unrelated classical L-function.

Classical inputs include Koszul/PBW functoriality, finite-cover cohomology, Weil weights and finite-factor Euler extraction. The reviewed advance is their explicit coherent source binding, complete ramification correction and sharp arithmetic cancellation boundary. No external priority, number-field transfer, enlarged operator ideal, or RH/GRH conclusion is established.
