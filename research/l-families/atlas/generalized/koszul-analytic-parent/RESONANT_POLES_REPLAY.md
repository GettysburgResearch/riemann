# Effective fully resonant pole classification: exact replay

The [source theorem](FINITE_RESONANT_COHERENT_POLES.md) proves an
elementary uniform cancellation bound for every even grade n>=6.
The exact low grades then leave precisely two double poles. Higher
resonant arithmetic points are true zeros and themselves force the
unit-circle natural boundary. The theorem also separates the original
coefficients, a complex principal-part decomposition, and a new finite
cohomological polynomial modification with integer coefficients.

The runtime authenticates the previous source/pole-divisor packet at
`f977b2e9e962b4ad560ce0edfac6dd97fb395d6b` before import. That chain
binds the actual Lie characters, curve cohomology, ramification and
noncancellation needed for the all-grade pole formula.

Direct Möbius/Adams evaluation keeps g^k in every group-character term
and is compared with the frozen Lie-source producer. The replay checks
the explicit n=6 margin1228 and low grades, while the proof's decreasing
normalized bound covers every later grade. No finite positive list is
treated as evidence sufficient for an infinite threshold.

Both equal and opposite fully resonant elliptic signs are retained.
The actual inherited F7-to-F49 source supplies its negative eigenvalue
and the polynomial1+14z^2+49z^4; the existing seven-element primitive
replay is reused, with no additional field or parameter introduced. The source
polynomial operation is explicitly distinguished from principal-part
subtraction, whose residues are complex and are not asserted algebraic.

Rational principal-part examples check opposite poles, parity
cancellation and the integer recurrence. They are declared algebraic
calibrations, not computations of the actual source's residues or
new examples of its full natural boundary. The original coefficient
growth has not been redefined or improved by changing the function.

From the repository root with the serialized Python runtime:

```text
python -B research/l-families/atlas/generalized/koszul-analytic-parent/finite_resonant_poles_replay.py --check
python -B -O research/l-families/atlas/generalized/koszul-analytic-parent/finite_resonant_poles_replay.py --check
python -B -m unittest discover -s research/l-families/atlas/generalized/koszul-analytic-parent/tests -p test_finite_resonant_poles.py
python -B -O -m unittest discover -s research/l-families/atlas/generalized/koszul-analytic-parent/tests -p test_finite_resonant_poles.py
```

Root executed the serialized focused Ruff checks, producer write/check
and optimized check, and all 22 tests in each normal and optimized mode;
all passed. The proof, runtime and all controls have independently been
read without a blocking finding. The exact-freeze review follows the
final source binding. The fixture binds all four packet source files
and checks the complete typed payload.
