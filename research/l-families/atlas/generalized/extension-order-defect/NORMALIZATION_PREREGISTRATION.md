# Preregistered normalization and infinite-base source controls

Status: theorem and finite predicates specified before execution. The root
agent alone runs any bounded worker. No field census, group catalogue or
large polynomial elimination is required.

1. Authenticate the exact C6 module proof/producer freeze, the original
   finite extension-order proof, and the frozen actual Lie multiplicity
   proof/producer used for the ladder. Bind the new theorem, this plan,
   runtime, tests and output. No mutable dependency may be imported.
2. Verify the literal G-weights on all six rank-one Segre coordinates.
   Substitute the four nonfixed coordinates equal to zero into the three
   actual minors, recovering only ce. This proves the fixed R scheme;
   the proof alone transfers its geometric dimension through the quadratic
   quotient. Do not infer reducedness of the fixed A scheme from this test.
3. Check the explicit source chart z=a^2, s=z^3, x=zp, y=z^2q by polynomial
   monomials: x^3=s u, xy=s v, y^3=s^2 w, and the A2 relation. Use the
   finite character/monomial basis to verify all source covariants descend
   in this chart. The proof supplies the all-degree torsor and normalization
   theorem; a finite substitution check is only a calibration.
4. Retain the complete T-origin fibre basis1,p,q,p^2,q^2, its multiplication
   matrices and C3/normalizer actions. Check its length5 against generic
   degree3. Do not identify it with the33-generator homogeneous-vertex
   quotient from the preceding packet, and do not call stabilizer order3
   a divisorial ramification index.
5. Use bounded artificial generator lists to reconstruct the full
   covariant minimal quotient by literal monomials and positive invariant
   divisors. Compare it with length-one nontrivial generators plus
   same-charge length-two monomials, including repeated variables.
   Trivial generators are base variables and opposite charges give
   invariant factors. Infinitely many nontrivial directions are excluded
   by the proof, not inferred from a numerical trend.
6. For the actual ladder independently reconstruct PBW class characters
   through16 from the three source Hilbert series, then compare all grades
   with the authenticated frozen source artifact. Report the exact full
   minimal polynomial (5.1) at cutoffs2,4,8,12, with16 held out. Keep
   multiplicities compressed; do not expand thousands of Lie generators.
   Retain the linear-only lower bound as an explicitly rejected equality.
   The logarithmic asymptotic constant13/144 is a proved source consequence,
   not a finite fit; the proof claims equivalence, not an unsupported
   uniform O(4^j/j) convolution remainder.
7. Integer/Fraction arithmetic only. Cap monomial total length at8, source
   degree at16, output polynomial degree at36 and linear maps at16
   rows/columns. Keep explicit primitive
   type checks and strict canonical JSON acceptance, including bool/int/
   float counterfeits. All field-extension uniqueness is relative to the
   stated inclusion and fraction field; no test promotes Euler equality
   to source uniqueness or selects an analytic completion.
