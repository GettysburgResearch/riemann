# Preregistered actual C6 infinity module replay

Status: proof and finite predicates specified before execution. Root alone
runs the bounded worker. No new field census or symbolic-algebra package
is required. The theorem is in `CYCLIC_INFINITY_INVARIANT_MODULE.md`.

1. Authenticate the frozen finite comparison (0018b73f), general invariant
   base (cfcfa422), and the Segre presentation/Hilbert--Burch source at
   7b320b3a. Bind this note, theorem, producer, test source and artifact.
   Reject missing or mismatched pins before using an imported statement.
   No executable upstream module is needed.
2. Enumerate literal Segre monomials `(m,a,b,c,d)` with row and column
   totals fixed as in equation(3.2), through original grading degree8.
   Keep the actual C6 quadratic projection (m even) and C3 charge separately.
   Reconstruct each nontrivial A-covariant's degree2 basis, degree4 old
   product span and two new generators. Retain the actual monomial lists,
   not just their dimensions. Check higher grades6,8 are generated by the
   proved degree2/4 generators; do not infer the all-grade claim from these
   two held-out grades.
3. Independently enumerate the total C monomials through total degree8 and
   remove products by positive monomials of B'=A^G tensor P^G. Recover all
   33 minimal generators and the polynomial1+12t^4+16t^6+4t^8. Re-run the
   source without the quadratic projection as a declared negative control,
   retaining its17-generator polynomial. Do not call that source the
   actual infinity stalk.
4. Apply the literal nonsplit permutation to every retained basis vector.
   Verify no positive minimal vector is fixed, phi^2=id, and the cycle
   determinants in degrees4,6,8 have exponents6,8,2. The invariant base is
   preserved, so this is an action on a quotient, not merely on selected
   representatives.
5. Multiply both2-by-2 matrices over the free polynomial ring k[u,v,w]
   and retain the exact result `(uw-v^3)I`. Independently substitute
   u=p^3,v=pq,w=q^3 and verify both maps land in the appropriate kernels.
   Use the normal forms with no simultaneous u,w factor to check selected
   weighted-degree8,10,14,16 maps and kernels. The proof supplies exactness
   in all degrees. Do not promote the relative A_i tensor T terms to free
   modules over B'.
6. All arithmetic is integer or exact rational, with no random choices,
   numerical eigensolvers, expanded infinite matrices, or field counting.
   Cap original Segre degree and total generator degree at8 and matrix
   degree at16. Typed argument validation precedes caches. Strict canonical
   JSON comparison uses `allow_nan=False`; explicit bool/int/float and
   missing/extra-key counterfeits must fail acceptance.

Expected controls are specified by the source proof, not tuned after a
run: each nontrivial A character has6 degree2 and2 degree4 generators;
its old degree4 product rank is23 of25; the actual repaired module has33
minimal generators and rank3; the untwisted calibration has17. The selected
P_1 presentation-domain F0 dimensions are3,3,5,5 in degrees8,10,14,16, with
kernel dimensions1,1,2,2. All actual executions and any repaired predicate
must be declared in a separate replay note before the scientific freeze.
