# Absolute Frobenius cutoff: controls fixed before implementation

This is a separate operation-law companion to the grading-genus packet.
Implementation waits for proof review and that packet's exact scientific pin.
No additional curve or field enumeration is permitted.

1. Retain actual source grades4,6,...,16, coefficient cutoff at most64, and
   original Frobenius powers from the same two-by-two matrix. Test e=1,2,3,4
   as the primary family, and e*d≤6 for norm controls d=2,3.
2. Use C_n=n,2n and the divergent nonmonotone choice n for 4|n, n/2 otherwise.
   Record the absolute and extension-relative thresholds separately. Never
   replace ceil(C_n/e) by a floating-point division or a fixed slope.
3. Compare literal proper polynomials times exponential counterterms with
   independent formal trace sums selected by e*h≥C_n.
4. Check the same-sequence norm law, with the root-of-unity sieve acting on
   h, not n*h. Retain a counterfeit that wrongly keeps p_(e,n)=C_n after
   extension. The e=2 coefficients -98 at degree8 and 1372/3 at degree12
   must be matched to the square of the e=1 product after z→z^2.
   Twelve primary norm panels use e=1,2, d=2,3 and the three declared
   policies. Six additional panels use e=1, d=2,3 and xi=-1,0,2, with
   phase xi^d on the left. They make no arbitrary-phase boundary claim.
5. Compare literal threshold composition with direct ceil(C_n/(ed)). For
   actual source ambiguity, retain a pair of thresholds crossing only an
   odd zero trace and a pair crossing a nonzero trace. These bounded controls
   do not replace the proof of the all-e blockwise classification.
6. Retain oriented frame-change counterterms and their additive cyclic norm.
   C_n=n and2n must remain distinguishable even though both are coherent.
7. Authenticate the frozen genus packet and its complete source chain before
   importing any executable. Bind all new proof/contract/producer/test bytes,
   and compare strict canonical JSON with `allow_nan=False`.

All global normal-convergence, natural-boundary, and blockwise classification
statements are proof obligations. A finite sample is not acceptance evidence
for their infinite quantifiers. Root alone runs the bounded replay and tests.
