# Complete higher maps: staged pre-execution contract

The degree-four acquisition is frozen at
`1a5a63f1148fb884e9ae7aae9e9324a40aee9f33`: actual rank1685/nullity65,
55 weight blocks, largest supported block204 by103, and18 exact bits. Its
original-coordinate polynomial-column digest is retained and must recur.
Neither the old declaration nor its scout is changed here.

This new runtime authenticates that acquisition and its actual D1 source before
any executable import. The parent alone runs the following stages:

* **Five:** reconstruct the acquired degree-four columns, then form the
  literal degree-five D1 matrix, with16577 full target rows and7975 columns.
  Predict kernel659. Rank the650 actual variable multiples of the65 acquired
  columns, predict639, and choose20 new quotient classes. Then compute the
  degree-five D2 kernel, predict11, yielding the first D3 columns.
* **Six:** compute the degree-six D2 kernel on3775 domain columns, predict127.
  Its110 old variable multiples have rank110; select17 new D3 classes.
* **Seven:** compute the degree-seven D2 kernel on15400 domain columns,
  predict776. The775 actual lower-D3 multiples have rank775; select one new
  class. This finishes29 D3 columns only if every calculation and composition
  check succeeds.

Every minimal quotient is computed by exact spans, not by subtracting the
predicted dimensions. Every old multiple and every new kernel witness is
substituted in the original graded matrix. Full weight multiplicities must
agree with the independently frozen degree-seven Tor duality:

`D2_4 <-> D1_3`, `D2_5 <-> D1_2`, and
`D3_5,D3_6,D3_7 <-> F0_2,F0_1,F0_0`, with weight complement `(7,7,7)-w`.

Initial elimination caps remain512 rows and512 columns per supported weight
block and4096 rational bits. A preflight reports the actual dimensions before
any out-of-cap elimination; root must authorize and record any enlargement.
The largest full declared free-module basis has at most90000 rows, with at
most16000 domain columns. Full ambient bases and matrices are reproducible
from ordered generator records and lexicographic ten-variable monomials. Their
exact streamed digests, dimensions and complete weight metadata are retained;
the artifact does not duplicate every expanded ambient zero row. All minimal
polynomial maps and original-coordinate kernel witnesses are retained in full.

No scientific computation has been run by the author. A successful five-stage
scout is not permission to assume six/seven pass. The all-degree exactness
argument will use the actual computed minimal classes plus the frozen
projective-dimension/duality theorem and graded Nakayama. This is a marked
resolution, with no claim of a GL3- or factor-S3-equivariant choice of bases.
