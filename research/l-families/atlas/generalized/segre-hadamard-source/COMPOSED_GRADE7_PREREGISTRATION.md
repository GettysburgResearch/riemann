# A separate composition of accepted lower maps with the grade-seven certificate

This is a new execution contract. It does not modify or satisfy the old
52-test streamed640 contract, the old 42-test row-restricted contract, or
the original 26-test continuation contract. Those contracts retain their
own pending execution status. Repeated reserve stops in the old640 runner
occurred while repeating the already accepted grade-six/coordinate
prefix; they are not mathematical failures or accepted grade-seven runs.

The inputs are the complete accepted grade-six discovery
`d3bda0446a379ce0bd8dfe4024f0184451adb1d5`, blob
`b9131081876be381c43237ea42c5695767bd73ae`, and the exhaustive original/lazy
coordinate comparison at `41b74441d26dc69c3c2134cc2b839ce522f9f183`, blob
`1d3b340f50f975b3aa98af23f6d7016ec822632a`. The latter fixes the exact
unchanged streamed640 algorithm, declaration, replay note, and test file.
The accepted grade-six proof includes actual full-row kernels and the
110-dimensional old subspace with seventeen new minimal generators.

The new runner authenticates every consumed source/helper file, the
complete accepted artifacts, and the earlier source chain before parsing
an acquisition or executing a frozen helper. Its lower-stage rank and
minimality conclusions are explicitly imported as proved inputs. It does
not call `verify_cache`, `verified_source`, `coordinate_check`, or any
lower-stage elimination. The exact accepted records are not a dimension
fit: they contain the complete marked polynomial maps and witnesses.

Fresh prefix checks compare F0 and D1 against the exact original
presentation, compare the acquired degree-four map digest, validate all
integer polynomial terms, positive degrees and full weights, and compose
every D1D2 and D2D3 column directly as a polynomial. Full dual weight
checks are repeated. This establishes that the consumed maps, source
conventions and homogeneous original coordinates match the accepted
proofs. It does not independently re-prove the lower-stage rank claims.

Grade seven is then constructed by the unchanged, separately frozen640
algorithm: its lazy original-coordinate matrix has 86,515 target basis
elements and 15,400 domain elements, with maximum 592 columns per
weight. It selects actual original rows using the fixed prime 65521,
then 1000003 only if necessary. Every restricted kernel is certified
against every original row. Primitive independence and the matching
modular rank certify completeness. Failed first-prime attempts remain
recorded. The actual old 775 columns are constructed and their rank is
computed; the predicted one new generator is accepted only after the
exact quotient calculation. No kernel or old-column independence is
assumed from Betti numbers.

The numerical caps remain 640 selected rows/columns, 4096 supported
original rows, 90,000 target addresses, 16,000 domain addresses, and the
4096-bit checks of the frozen algorithm. That bit check covers declared
inputs, stored pivots/witnesses and checked residuals; it is not an
instrumented bound on every transient arithmetic intermediate. Root
retains the 128 MiB worker ceiling and 2 GiB free-memory reserve. No
reserve or mathematical cap is lowered here.

The new checkpoint directory is `composed_grade7_weightblocks`. Its
ownership includes every accepted source identity plus this runner's
algorithm and preregistration hashes. Completed blocks are reused only
after the unchanged full-row verifier checks them. The final artifact
also binds this replay note and the new dedicated test file. Mutable
execution prose does not change the mathematical block identity.

The new producer supports a prefix-only check and the complete scout,
write, and check modes. A prefix result does not assert any new rank or
grade-seven result. A full result preserves the complete accepted lower
records, adds the actual final map and certificates, and repeats direct
polynomial compositions, including the new top column.

Once the actual grade-seven maps and minimal quotient have passed, the
independently reviewed theorem at
`423a25c35996ec5bf2c4dfaac6594d31f89f1081` promotes the constructed complex
to a complete minimal resolution. It uses the independent projective
dimension/Tor support and graded Nakayama in the stated order; it does
not infer exactness merely from the Hilbert numerator. Until that run
passes, this remains a declared composition procedure.
