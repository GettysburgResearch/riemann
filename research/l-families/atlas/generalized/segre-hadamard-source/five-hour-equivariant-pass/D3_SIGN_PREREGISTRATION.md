# Exact factor-sign reduction of the weight744 d3 target

Status: separately preregistered after the full target shape refusal and before any sign-shape or rank acquisition.

The original Q source block512 to336 and every componentwise internal-degree4 W block remain complete. Only the final internal-degree5 weight744 W complex is replaced by its exact factor-S3 sign isotypic subcomplex, because the complete frozen target at weight744 is one copy of factor sign. This proves the complete [744] sign constituent and does not address [555] or claim a global d3.

For each W chain basis element `(wedge,(r1,r2,r3))`, factor S3 fixes the W wedge and permutes the three R records. A sign vector exists exactly when r1,r2,r3 are distinct. Choose the sorted record triple as pivot and take its six-term alternating orbit sum. These vectors form a complete disjoint-pivot basis of the sign isotypic chain space. Build every restricted differential by applying the full coefficient-one W differential to these vectors, reconstructing the exact alternating output, and verifying equality in original coordinates and d squared zero. Refuse if any restricted matrix dimension exceeds512 or if the target H3 quotient is not dimension1 with transposition/three-cycle traces(-1,1).

The first Q kernel and first boundary lifts stay in complete original coordinates. Reduce the first obstruction componentwise in the seven complete internal-degree4 W complexes; retain all vectors surviving to E3 and exact eta2 boundaries. Apply `-delta eta2` in the original internal-degree5 chain group, verify W closure, then apply the exact sign Reynolds projector. Express the projected vector in the complete alternating chain basis and reduce modulo its complete sign boundaries. Retain all source combinations, both lift stages, projected cycles, target boundary witnesses and zero images.

Acceptance statuses are `VERIFIED_NONZERO_D3_744_SIGN`, `VERIFIED_ZERO_D3_744_SIGN`, `VERIFIED_NO_E3_SOURCE_744`, or an exact refusal. A nonzero result must span the one-dimensional target and satisfy direct factor sign actions. Exact rational arithmetic,4096-bit coefficient cap,8192 sparse-term cap,512 matrix cap,16MiB artifact,1GiB working set and240seconds remain unchanged. Negative controls include deleting an alternating orbit term, reversing either lift sign, skipping Reynolds projection, changing a source kernel and falsifying target quotient coordinates. Ordinary and optimized replay are mandatory.
