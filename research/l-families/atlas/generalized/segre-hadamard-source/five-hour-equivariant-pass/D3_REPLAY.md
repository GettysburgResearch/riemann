# Actual d3 replay

Status: `PENDING_TIMEOUT`; there is no d3 rank or nonvanishing result.

Only the complete weight744 block is registered. Shape mode authenticates the complete higher-syzygy packet and all its source chain, then reports the Q-source and every required internal-degree4/5 W chain dimension without elimination. It refuses before rank acquisition if any matrix dimension exceeds512.

The retained shape has Q dimensions512 to336. All seven required internal-degree4 W blocks have maximum dimension401. The internal-degree5 target has chain dimensions [180,763,1143,730,178,9], so the full original block violates the512 cap. No elimination or d3 rank calculation ran. Any isotypic reduction is a separately preregistered continuation and does not rewrite this refusal.

The separately preregistered exact sign-isotypic shape has internal-degree5 dimensions [27,110,150,76,8,0], and all its declared blocks fit the512 cap. Sixteen ordinary source/hostile tests passed for that shape, the complete Q3 kernel, first lift, alternating-basis completeness, original-coordinate differential reconstruction, Reynolds idempotence and the one-dimensional sign H3 target.

Two full sign acquisition attempts did not produce an artifact. The first reached the second-lift assembly and stopped on an omitted local function argument; that call site was repaired and focused tests passed. The second retained the same mathematics and then stopped honestly at the inherited240-second cap while combining second lifts. `d3.verification.json` does not exist. Full modes are now fail-closed with `PENDING_TIMEOUT`; no zero/nonzero image, E3-source dimension or rank may be inferred from this packet. The unsuccessful attempts are not evidence for either outcome.
