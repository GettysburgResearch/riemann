# Declared central top-class source certificate

This is a new theorem-guided contract. Its target is one actual missing
minimal relation class of weight (7,7,7), not the global grade-seven
775-column old-rank calculation or 776-vector kernel census. The frozen
proof CENTRAL_TOP_CLASS_GLOBAL_EXACTNESS.md explains why this smaller
obligation suffices. All older full-census producers and test contracts
remain unchanged and are not claimed completed here.

The composed helper and central promotion proof are frozen at
`ed8c7251719a381abf6d55f57d768d69e76b776d`. Its accepted prefix artifact is
blob `9e057f58f16f4853a65dd152d5c83ca719e829fb`, with proof object
`2301613316c82b81debf817ecf4580a1476e1b78e663dd23a57317a03eda8bb3`.
The separately frozen composed helper supplies exact accepted lower
maps, source authentication and fresh polynomial composition/grading
checks. Its full build is not called. The lower-stage rank and minimality
proofs and exhaustive coordinate comparison are imported as proved
inputs, not rerun. The central runner additionally binds the actual
source finiteness and Tor-character theorem/artifact before helper
execution. Pending freeze placeholders must refuse before any imports.

The new runner enumerates the full original grade-seven F2 domain and
selects every descriptor of weight (7,7,7). Its expected size is 592,
from the independently frozen shape acquisition. The central matrix
retains every original target row and the original addresses. It uses
the unchanged640 row-selection/kernel algorithm on this single block,
including its full-row residual, primitive independence and modular-rank
completeness certificates. No rank or nullity is predeclared here.

Independently enumerate all degree-two monomials multiplying every one
of the eleven accepted degree-five D3 columns, and all ten degree-one
variables multiplying every one of the seventeen accepted degree-six
columns. Retain every resulting central-weight column, including any
dependence. This is the whole old central subspace, not a selected list.
The producer records the original generator, multiplier and every
coefficient in the original F2 coordinates. It checks every old column
against every central-matrix target row.

An exact rational span computes the old central rank. Adjoining the
actual central kernel vectors must produce precisely one new class;
the first such marked vector is retained. Non-membership is established
by exact rational rank increase, not by an uncertified modular rank
comparison. The actual polynomial vector is checked again against D2,
and all 29 final D3 columns undergo the fresh full polynomial composition
and grading checks. The complete central kernel is retained as an
additional check, although the promotion theorem only requires the one
nonzero quotient class.

The artifact states measured central dimensions separately from the
deduced global numbers 775 and 776. After the one class is certified,
graded Nakayama and Tor4=0 in the pinned theorem prove the complete
minimal resolution. They do not constitute execution of the older
full-census contracts or claim an equivariant choice of marked lifts.

All original bounds remain: 640 selected rows/columns, 4096 supported
original rows, 4096-bit checks at the declared stored values, and the
parent's 128 MiB worker ceiling with 2 GiB free-memory reserve. The bit
check is not a bound on every transient arithmetic integer. The new
central checkpoint directory binds the complete original matrix,
source/algorithm/declaration identities and original column addresses.
Failed fixed-prime attempts and incomplete acquisitions remain distinct
from mathematical success. No author computation is authorized.
