# Streamed original-coordinate resolution, new 640 identity

Candidate implementation only; no acquisition or test run has been performed
by its author. Root must first run the bounded classes and the exhaustive
degree-six coordinate comparison under the declared guard. The formerly
pending gate is now bound to the complete accepted old512 degree-six discovery
`d3bda0446a379ce0bd8dfe4024f0184451adb1d5`, with the exact payload and measured
history recorded in the preregistration. No new640 acquisition has run yet.

The producer is `streamed_resolution640.py`, with its separate
`STREAMED_RESOLUTION640_PREREGISTRATION.md`,
`streamed640_weightblocks/`, `streamed640_coordinate.discovery.json`, and
eventual `streamed640_resolution.verification.json`. Tests have the separate
identity `tests/test_segre_hadamard_streamed640.py`. Neither the old 42-test
identity nor the earlier 26-test contract is replaced.

`--coordinate-check` exhaustively compares the actual degree-six source
constructor without kernel elimination. `--scout` emits the complete new
degree-seven payload only after accepted degree-six replay. `--write` and
`--check` produce and replay the final separately bound artifact. The old
degree-six retry command is unchanged:

```text
python research/l-families/atlas/generalized/segre-hadamard-source/row_restricted_resolution.py --scout 6
```

The new tests retain independent polynomial-composition, minimality, complete
weight and source-preservation checks, plus hostile full-row/kernel/ownership
controls. Additional tests cover every lexicographic composition address
through degree five, inactive module generators, complete degree-six matrix
comparison, literal coordinate types, the 592-column case, unchanged old512
refusal, new640 limits, exact old/new eliminator agreement on eligible
matrices, and lazy degree-seven domain shape without a rank claim.

The new method trades repeated sparse-column reconstruction for lower peak
storage. It does not promise that every block will fit the worker guard;
completed certificates permit an unchanged guarded retry after interruption.
The inherited 4096-bit checks cover the declared stored/accepted coefficients
and checked residuals, not every transient arithmetic operation.
`max_exact_bits` is not an instrumented arithmetic peak; the independent
root worker memory guard remains in force.
