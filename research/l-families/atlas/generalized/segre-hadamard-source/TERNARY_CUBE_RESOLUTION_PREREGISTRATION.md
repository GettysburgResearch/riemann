# Actual higher differentials: pre-execution declaration

This is a new bounded construction after the actual first-presentation freeze
`4c635b2ee8d7cf6caa41efde2d2e0c7baea1b787`. No higher differential has yet been
computed by this packet. The first scout is internal degree four only; the
coordinating agent must assess its resource report before any larger stage.

The polynomial base remains the ten-variable Chow base `S=Sym(Sym^3 V)`.
The frozen marked `F0` has 29 generators of degrees `0:1,1:17,2:11`; its actual
85-column `D1` has generator degrees `2:20,3:65`. We use its literal integer
polynomial entries, not a matrix inferred from the Betti table.

1. Authenticate the frozen first-presentation proof, producer and complete
   matrix artifact before interpreting their bytes. No predecessor executable
   needs to be imported or rerun. Check every D1 term's degree, weight,
   coefficient type, target index and positive polynomial degree.
2. Form the actual degree-four map `(F1)_4 -> (F0)_4`. Its full free-module
   dimensions are 1750 columns and 5060 rows. Eliminate only exact rational
   torus-weight blocks, with both row-support and column cap 512 and arithmetic
   cap 4096 bits. Empty rows remain declared in the full module basis even
   though they need not occupy an elimination block.
3. Retain original-coordinate kernel witnesses and directly substitute each
   into the actual D1 matrix. The preregistered prediction is rank 1685,
   nullity 65. Compare its complete weight multiplicities with the duals of
   the frozen 65 degree-three D1 generators: `w -> (7,7,7)-w`. Do not use that
   character to construct the kernel vectors.
4. If root judges the scout safe, extend to degree five. The predicted D1
   kernel dimension is 659. The 650 variable multiples of the degree-four
   kernel should have rank 639, with 11 relations; choose 20 genuinely new
   degree-five classes. These construct all 85 columns of D2 only after the
   actual quotient calculation and its compositions pass.
5. Then construct D3 from actual D2 kernels in degrees five, six and seven,
   with predicted new generators `5:11,6:17,7:1`. At degrees six and seven,
   quotient by the actual lower-generator multiples before selecting new
   classes. The full kernel dimensions are predicted to be 11,127,776.
   Every displayed D3 column must compose to zero under D2.
6. Global exactness and absence of further minimal columns use the frozen
   actual Tor computation, projective dimension three and degree-seven
   duality, followed by graded Nakayama. Finite rank agreement alone is not
   a replacement for that argument. No GL3- or factor-S3-equivariant marked
   splitting is claimed. Minimal generators are weight homogeneous but
   chosen deterministically from literal matrices.

The first scout has no fixture-writing mode and performs no field enumeration,
numerical rank computation, CAS invocation or dense all-degree matrix build.
Later stage caps and artifact/test contracts must be declared before they are
run. The parent runs every nontrivial job; the author has run none.
