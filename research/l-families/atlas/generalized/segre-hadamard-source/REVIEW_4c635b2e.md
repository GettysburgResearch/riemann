# Independent review of the actual ternary-cube presentation

Reviewed scientific freeze: `4c635b2ee8d7cf6caa41efde2d2e0c7baea1b787`.
Reviewer: the extension-order agent, independent of the packet's author.
Conclusion: no mathematical, source-map, or acceptance blocker found in the
scoped first-presentation result.

I read the complete proof, preregistration, replay contract, producer, and all
26 test sources. A read-only Git comparison confirmed that all six scientific
files inspected agree with this exact freeze. I inspected the retained artifact's
source pins, ownership hashes, summary, and representative relation and marked-
section records; I did not manually re-eliminate its full sparse matrices.

The source construction precedes the dimension checks: the ten coefficient-one
content sums multiply literal tensor monomials; weight-blocked rational
elimination retains witnesses in the original free-domain coordinates. The
primitive integer normalization preserves these witnesses. The 200 old cubic
relations are actual variable multiples of the 20 quadratic columns, and the
65 new cubic columns are selected modulo their independently computed span.
Thus the asserted first differential is a retained polynomial map, not a Betti
table or a fitted scalar numerator.

The global promotion in SHP3 is valid with its stated imports. The frozen
low-degree Tor homology together with projective dimension three and the
degree-seven duality excludes further generators and first relations. Graded
Nakayama then promotes the bounded source calculations to the complete minimal
first presentation. It does not make the first differential injective or supply
the two higher differentials. The marked monomial complement is explicitly
separated from an equivariant section, and its permutation counterfeit tests
the actual span of the chosen monomials.

The three source Git objects at
`a895f47628b0bc7c7ee5e0392df2f79c24166f92` resolve to the declared blobs:
`bbd847461b4955a93b4533fa687cdd8724e2347c`,
`795566f6ec91bf68dd7ee2f77450e73afd131032`, and
`310ffc95cb6eaf19281de52dd18d2f2884bc0f49`.
Authentication precedes source-artifact use, and no executable predecessor is
imported. Typed canonical JSON with `allow_nan=False` rejects Boolean, float,
and wrong-integer substitutions even with an unchanged stored digest.

Root reports Ruff, the producer's write/ordinary-check/optimized-check sequence,
and **26 tests in each Python mode passing**. Those are parent-run execution
results, not computations performed by this reviewer. The frozen preregistration
and replay contract retain their pre-execution wording; this later review records
the reported completion without rewriting those bound historical files.

The claimed novelty boundary remains appropriate: this is an explicit marked
calculation in the classical Chow module, not a new general existence theorem,
an untouched rank-three cube case, or an equivariantly selected full resolution.
