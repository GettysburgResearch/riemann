# Balanced Vaughan and half-divisor reduction

The reviewed reduction retains:

- the corrected balanced Vaughan large-divisor identity;
- squarefree and gcd constraints;
- ratio-four two-field factorization;
- the positive half-divisor sequence with `eta*eta=1`;
- reduction to one field with Hardy norm `3`;
- the exact ratio-four Haar autocorrelation Gram;
- an unconditional subpower diagonal.

Canonical use must truncate the finite field before applying the full-line Hardy inequality and use the product `AB` in the Cauchy gate.

The oriented signed off-diagonal near-collision estimate `HCNC` remains open and sufficient for RH through the fixed consumer.
