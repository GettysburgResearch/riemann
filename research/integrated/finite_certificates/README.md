# Finite certificates

## Integrated scope

**Strongest reviewed result:** Robin and finite Pick boxes at exact finite scope.

**First open arrow:** Cofinal/global theorem.

**Relationship to RH:** open nodes: none

**Family state:** `finite`

This packet contains 2 reconciled semantic claim rows. It is a reviewed residency manifest. Exact proof bodies remain at the frozen source PR/head/path recorded in `SOURCE_MANIFEST.tsv`.

## Claims

| Claim | Verdict | Statement | Scope | Required fix | Exact source |
|---|---|---|---|---|---|
| `FINITE.ROBIN.BARRIER` | `RETAINED_HEAVY_CERTIFICATE` | The exact Robin barrier through 5582 with adjacent threshold 5583 is retained as a finite theorem. | finite n through declared barrier | Pin the canonical August certificate path/hash; do not infer a global implication. | registry node (review/2026-08-22/reconciliation/FINAL_COMPUTATIONS.tsv) |
| `FINITE.PICK.BOXES` | `RETAINED_HEAVY_CERTIFICATE` | Two directed complex 8x8 Pick boxes are retained as finite exclusions. | two supplied finite boxes | Keep authenticated primitive rectangles and exact scope; no strict negative Riemann-data witness exists. | registry node (review/2026-08-22/reconciliation/FINAL_COMPUTATIONS.tsv) |

## Reading rule

- `VERIFIED_WITH_FIXES` may be used only with the listed repair.
- `CONDITIONAL_EXACT` does not establish its premises.
- `OPEN_SUFFICIENT_FOR_RH` and `OPEN_RH_EQUIVALENT` remain open.
- Refutations and false edges are retained as mechanism firewalls.
- Family membership never confers a stronger verdict than the claim row.

Canonical registry: `canonical/2026-08-22/claims.tsv` at repository root.
