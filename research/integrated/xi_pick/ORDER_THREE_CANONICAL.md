# Actual-Xi infinitesimal safe Pick positivity through order three

## Canonical statement

Let
\[
\Xi(z)=\xi\!\left(\tfrac12+z\right),\qquad
F(x)=\frac{\Xi'(x)}{\Xi(x)}.
\]
For safe nodes `x_i>1/2`, define
\[
\mathcal H_{ij}=\frac{F(x_i)+F(x_j)}{x_i+x_j}.
\]

The reviewed conclusion is:

\[
\mathcal H\succeq0\qquad\text{for every packet size }N\le3.
\]

The canonical theorem is positive semidefiniteness. Distinct-node positive definiteness is not included without a separate strictness proof.

## Mandatory repairs

- use the corrected Platt-Trudgian source lock and distinguish critical-line verification from simplicity;
- retain the residual `(m_0-1)R_0` if the selected critical orbit has multiplicity `m_0`;
- use one reflected-orbit and multiplicity convention;
- state grouped local `C^2` convergence through two derivatives;
- treat PR #446 and PR #460 as using one shared critical-reserve ledger;
- replace the corrupt historical replay with a new canonical light replay;
- do not describe the external high-zero computation as rerun.

## Boundary

The first unresolved interpolation order is four. All-order positivity remains RH-equivalent.
