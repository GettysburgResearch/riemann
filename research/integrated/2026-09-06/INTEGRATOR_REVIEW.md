# Integrator review and acceptance boundary

Inputs are the seven trees in `integration/2026-09-06/SOURCE_FREEZE.json`. This pass is reconciliation and extraction, not a fresh reproof of every archived claim.

## Arguments reconstructed in this pass

The integrator read C4-X X1–X10 in full and independently checked the endpoint normalization, the direction of the countable coding, genus-zero source expansion, summable constant reserve allocation, paid-prefix identity, C2 passage, scalar monotonicity, two-/three-node determinant algebra and duplicate-node pullback. The finite replay tests empty and finite off-line model sources, the half-node, repeated nodes, all principal minors, the local orbit payment and the three-node factorization on unrelated rational data. The mathematical extraction is **conditional on the stated complete spectral and analytic inputs**. The actual external zero computation and a complete Lean input construction are not rerun or asserted.

The integrator reconstructed C4-M M1–M4: punctured-germ transfer under an invertible analytic multiplier, compact holomorphic domination, the positive Taylor/Tonelli Landau argument and dyadic negative-mass holomorphy. The existing formal total-function predicate is not relabeled a punctured-germ predicate. The reader must retain the tail convention and finite-abscissa hypotheses.

D's finite corrections were checked against their actual algebra: the varying-order difference example; point-spectrum threshold; all principal minors; the wavelet boundary factor; Farkas sign; complex jump dissipation; both boundary excursions; the Fourier normalization; Q4 sector/signed-Hilbert distinction; activation and net-pole conventions. The new P61 finite coefficient formulas were reconstructed independently. D's full replacement campaign is imported at its authenticated source and reported execution boundary, not rerun here.

For the two #792 operator proofs this integration consumes the exact non-author C4 review. The energy-domain and lower-versus-upper Galerkin distinction is retained explicitly. No sampled lower matrix is accepted as a continuum sign certificate.

## Independence

C4 discloses that its author previously served as A. Its newly written repairs were not independent acceptance until separately inspected here, and its agreement with A is not a second vote. B discloses authorship of #790 and supplies no independent acceptance there. B's pass7 review of #793 is narrower than that branch; C4's #792 review is likewise limited to two named proofs. Original account names do not authenticate independent-agent identity.

## Holds and conditional statuses

The decision register retains A and B's source-specific acceptances, supplied repairs, failed promotions and omissions. New theory in an author's own packet without an independent proof review stays held. The growing Poincare relative-error assertions, numerical Epstein inclusion/coverage and several full source campaigns are not promoted. The old formal actual-Xi input is blocked at source fidelity, while its conditional Lean theorem and historical build record remain historical facts.

The inherited graph is changed only conservatively. Raw reviewer edge tables are not automatically joined through colliding short claim IDs. The six new route records identify open leaves and are nontraversable until a complete exact adapter reconciliation is supplied. This is a clear current state with explicit exclusions, not an assertion that the remaining graph-extraction backlog has disappeared.

## Execution

See the integration [validation record](../../../integration/2026-09-06/VALIDATION.md). Fresh local execution covers 576 bounded rational/integer fixtures and eighteen corruption/rejection cases in both normal and optimized Python. Payload-only validation is explicitly not full-checkout source authentication, full graph execution, Lean, or historical campaign replay. Remote review subtrees and proof-copy blobs are independently pinned by Git object identity.
