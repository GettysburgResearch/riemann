## Purpose

Dedicated continuation of the native beta–Chebyshev route from exact PR
#758 head `d79692ece0b7604ad309c459f565b24e9926f5c5`.

This PR now contains two source-locked checkpoints:

```text
T-108000  complete limiting-profile Perron boundary law
T-108002  exact finite first-edge cubic-cusp profile
```

**RH remains unproved. No new zero-free region is claimed.**

## T-108000 — limiting boundary

The limiting Stieltjes profile is positive below \(4i\), has a Plemelj jump
with strictly signed imaginary part above the cut, and has square-root
channels at every odd threshold \(4mi\). It has no zero on either physical
boundary face.

## T-108002 — finite edge closed

Let \(n=r+1\), let \(\Phi_n\) be the exact finite Chebyshev jump profile,
and put

\[
 c_n(\lambda)=4i+\lambda n^{-2/3},\qquad\lambda\ge0.
\]

The new theorem proves

\[
 \boxed{
 n^{-1/3}\Phi_n(c_n(\lambda))
 \longrightarrow
 {\cal A}(\lambda)}
\]

with

\[
 \boxed{
 {\cal A}(\lambda)
 ={36\sqrt2\over\pi^3}e^{-i\pi/4}
 \int_0^\infty u^{-1/2}
 e^{-\pi\lambda u/4+i\pi^3u^3/24}\,du.}
\]

At the exact edge,

\[
 \boxed{
 {\cal A}(0)
 ={12\sqrt2\over\pi^3}
 \left({24\over\pi^3}\right)^{1/6}
 \Gamma(1/6)e^{-i\pi/6}.}
\]

Numerically,

\[
 {\cal A}(0)
 =2.528164832374\ldots-1.459636646527\ldots i.
\]

The large-\(\lambda\) matching law is

\[
 {\cal A}(\lambda)
 ={72\sqrt2\over\pi^3}e^{-i\pi/4}\lambda^{-1/2}
 +O(\lambda^{-7/2}),
\]

which matches the T-108000 Stieltjes threshold coefficient exactly.

## Correction to the scout

The earlier discovery-only scout correctly identified

```text
edge width       n^(-2/3)
edge amplitude   n^(1/3)
```

but its provisional Airy label was too specific. The exact local object is
a **cubic-cusp/Fresnel transform**, arising from

\[
 \pi^3\left({u^3\over24}+{uv^2\over2}\right).
\]

No Airy zero mechanism is available.

## Strategic consequence

The entire universal first-edge geometry is now explicit. It still does not
cancel by itself. The next theorem must retain the literal
primitive-pair/Möbius coefficients together with the finite difference
spectrum before taking any norm:

```text
BETACUSPSOURCE108004

guarded beta primitive-pair source
  -> exact finite cubic-cusp spectrum
  -> signed Möbius/divisor-wavelet superposition
  -> source-specific saving over the universal BV envelope.
```

## Replay

```text
PASS_T108000_BETA_CHEBYSHEV_PERRON_EDGE_BOUNDARY
proof object: 2a7909321f91401f6a6872ba3e11d50f3b6da36097667ab768cb0c3ce53f5046
```

```text
PASS_T108002_BETA_CHEBYSHEV_CUBIC_CUSP_EDGE_PROFILE
```

The T-108002 producer authenticates five exact predecessor blobs and
replays the exact finite jump spectrum at
\(n=32,64,128,256,512\), the Gamma edge constant, a positive-\(\lambda\)
control, and the outer matching law.

```text
limiting physical boundary law        PROVED
finite cubic-cusp edge profile         PROVED
n^(-2/3) width / n^(1/3) height        PROVED
exact Gamma edge constant              PROVED
outer Stieltjes matching               PROVED
beta-source cancellation               OPEN
new zero-free half-plane               NOT PROVED
RH                                     UNPROVED
```
