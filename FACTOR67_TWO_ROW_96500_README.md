# T-96500 — finite source-complete factor-67 two-row candidate

This add-only successor is based on frozen review PR #552 at
`81df9c3f507aba0e5f21187044583a0d6fb90db9`.

It accepts both exact separators from PR #552:

1. a fixed physical-product divisor cube cannot create a cross-knot convex packet;
2. the rough-prefix reservoir lower bound is false at `(P,p,u)=(30,5,2)`.

The replacement does not repair either false mechanism. It uses the exact
coefficient-one factor-67 causal partition in a single labelled positive source
category, expands every unresolved child only until its scale is below 67, and
observes only the two final component rows `j=2,3`. The tree is finite at every
endpoint. Terminal `P_61` differences are realized by the frozen directed
Target–Lorenz/Hall theorem before the row marginal is taken.

The resulting proposed signs

\[
c_X(2)\ge0,\qquad c_X(3)\ge0
\]

feed the exact reciprocal-zeta fixed-row Mellin transforms. The two finite
numerators have no common zero in `Re z>0`, so Landau's theorem supplies the
candidate RH conclusion.

**Scientific status:** proposed complete unconditional proof candidate;
independent hostile reconstruction is required. RH is not treated as
established by publication.
