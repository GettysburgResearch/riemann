# X-91750 — canonical factor-67 one-shot closure regression

Run:

```bash
python3 verify.py certificates/control.json --output /tmp/verification.json
python3 -m unittest discover -s tests -v
python3 -m py_compile verify.py tests/test_verify.py
```

The checker authenticates finite exact algebra only:

```text
common-parent typed and capacity identities;
one owner per source atom and zero child port;
positive-packet hereditary fallback and 16/7 envelope;
one-shot child colours remain inside the final parent row;
Y4 support through q=8192;
all-column/thinning/terminal constants;
strict reserve payment;
one-sided prime-square-moat sign algebra;
ten fail-closed mutations.
```

It does not replay the frozen Hall/profile theorem, analytic endpoint estimates,
PNT prime-square limit, Landau theorem, or prove RH.
