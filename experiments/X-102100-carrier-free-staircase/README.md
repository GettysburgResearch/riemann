# X-102100 exact replay

Run:

```bash
python3 verify.py --output results/verification.json
```

The standard-library checker verifies:

- all survival-weighted mixed-coboundary and staircase identities on an exact rational six-label source;
- the six-box rational Bernstein certificate for the `17/16` Harnack bound;
- a directed rational interval separator for Harnack-cone iteration;
- the common five-box differential-operator algebra;
- the exact Vaughan coefficient identity;
- fail-closed theorem flags.
