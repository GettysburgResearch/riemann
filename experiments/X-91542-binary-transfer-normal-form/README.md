# X-91542 — binary-transfer normal form

This exact symbolic replay checks `L-91542`:

- the native parent = arithmetic residual + contracted child split;
- the binary parent = survival + hazard target telescope;
- the favorable score telescope;
- the exact positive target transferred from residual to child;
- the target-null score surplus;
- the positive physical coordinates of the child correction.

It does not check the finite component-row transfer, affine pushforward,
quantization/collar normalization, or RH.

```bash
python3 verify.py
sha256sum -c SHA256SUMS
```
