# A2 — PROP G UNCONDITIONAL: the smooth far field is killed without zero hypotheses
Lane GRAND/final/A2. Sources: FAR.md (X-105071) SS4.4 + FAILURES F-4, P2_transfer_theorem.md
P2.4-P2.7, DEFORM.md (X-105072). RH: unproved, NOT addressed, never assumed. No anti-conspiracy
hypothesis, no |zeta'(rho)| lower bound: both of Prop G's hypotheses (i),(ii) are ELIMINATED.
Zero inputs: (A1) verified zeros |Im| <= 60 on-line and simple; Riemann-von Mangoldt;
Landau/Titchmarsh local log-zeta expansion; Ingham 1940 density (constants only). All zeta
evaluations are ON contours; no residue at an unknown zero is ever extracted.

VERDICT UP FRONT. Proposition G is replaced by THEOREM A2 (SS4): the Gevrey-smooth far field
Z_far^{sm} satisfies, unconditionally in the zero data (and with no reductio input),
    (i)  |Z_far^{sm}(s)| <= M_sm    on Omega^+ ∩ {0 < h <= 1/6}, all s (pinches included);
    (ii) h int_WIN |D_half[Z_far^{sm}](h+it)|^2 dt <= C_* h -> 0   for h in H_good,
where H_good ⊂ (0,1/6] is explicit with 0 in its closure (complement an explicit union of
intervals of finite total length; empty if no far pinch exists), M_sm, C_* finite explicit.
Hence [P2-FAR](b) for the full Z_far reduces to the CORNER far field alone (SS5), with the
full eps_1 budget intact. Status: proposition-level proof; load-bearing steps verified here,
mechanical uniformity sweeps flagged in SS7. The honest label is PROVED AT PROPOSITION LEVEL,
with the one interface amendment (limsup along H_good) that P2.7 verifiably accepts (SS5.3).

## 0. Conventions
As P2/FAR: a = 3/4 + (3/2)s, s = h+it, s_0 = i gamma_1/3, WIN = {|t - gamma_1/3| <= r_0},
Omega^+ as P2.6. w := a - z (pole-side variable: poles of 1/zeta(a-z) sit AT the zeros rho,
s-independently). Branch points of zeta^{1/2}(a+z) sit at z = rho' - a, i.e. in w-coordinates
at w = 2a - rho' (moving with s). Far pinch: w-pole rho meets w-branch 2a - rho':
    rho + rho' = 2a = 3/2 + 3s  <=>  beta + beta' = 3/2 + 3h,  gamma + gamma' = 3t.       (P)
Pinch point in s: s'_{rho,rho'} := (rho + rho' - 3/2)/3, so Re s' = (beta+beta'-3/2)/3.
L(T) := log T loglog T. delta_flr(T) := c_0/log T (threading clearance floor).

## 1. The Gevrey-2 smooth/corner split and the transform decay
Fix delta in (0, 1/4]. Let theta in C^inf(R), 0 <= theta <= 1, theta = 1 on |u| <= delta/2,
supp theta ⊂ [-delta, delta], theta in Gevrey class G^2 (Denjoy-Carleman: exists; e.g.
theta built from exp(-1/u - 1/(delta-u)) blocks). Define, in u = log r,
    kap_sm(r; s) := kap_0(r; s) [1 - theta(log r) - theta(log(r/2))],   kap_c := kap_0 - kap_sm.
kap_c is supported in r in (1, e^delta] ∪ [2 e^{-delta}, 2] — FAR.md 4.1's corners up to
reparametrization. kap_sm = (compactly supported G^2 part on u in [delta/2, log2 - delta/2])
+ (constant tail) * phi_tail with phi_tail a G^2 step at r = 2e^{-delta/2}-scale.

LEMMA 1 (transform decay). hat-kap_sm(z; s) := int_1^inf kap_sm(r; s) r^{-z} dr/r satisfies,
uniformly for s in Omega^+ ∩ {h <= 1}, |Re z| <= 1:
    |hat-kap_sm(z; s)| <= C_delta exp(-c_delta sqrt(|Im z|))  for |Im z| >= 1,
c_delta > 0 explicit (Gevrey-2 Fourier decay; numerically c ~ 1.5-2 at delta = 1/4,
gevrey_num.py). Proof: compact part = Fourier-Laplace of a G^2 compactly supported function
of u (kap_0(e^u; s) is analytic in u with s-uniform bounds on the compact support, and
analytic x G^2 ⊂ G^2 with uniform constants): classical G^2 decay exp(-c sqrt|xi|).
Tail part: one integration by parts gives (1/z) x [Fourier-Laplace of phi_tail' in G^2_c]:
same decay with an extra 1/|z|. The 1/z kernel pole survives only at z = 0 (local region,
not ours). QED. Note the corner transform hat-kap_c decays only polynomially — the corner
far field is NOT treated by this lane (it is the residual, SS5).

Split B_0 = B_sm + B_c by kernel linearity in P2.3's series (both absolutely convergent for
Re s > 1/6); P2.5's Mellin proof applies verbatim to kap_sm (Lemma 1 gives better-than-|z|^{-2}
line decay), yielding for Re s > 5/24, c = 1/16:
    B_sm(s) = H_0^{sm}(s) + (1/2 pi i) int_{Re z = 1/16} Psi_sm(z,s) dz,
    Psi_sm(z,s) := (3/2) hat-kap_sm(z; s) zeta^{1/2}(a+z) / zeta(a-z),
H_0^{sm} the same finite entire head construction. Split at |Im z| = Z_0 = 40 exactly as
DEFORM: B_sm = H_0^{sm} + Z_loc^{sm} + Z_far^{sm}, with Z_loc^{sm} the cut-hug (DEFORM D2
verbatim with hat-kap_sm in place of (1/z)k(beta_z); its pinch coefficient is c_B^{sm}, and
c_B = c_B^{sm} + c_B^{c} by linearity — the corner keeps its share, SS5.2), the connecting
pieces classical (DEFORM E3, verified-height inputs only), and Z_far^{sm} := the |Im z| >= 40
integral — THE OBJECT OF THIS LANE.

## 2. The threaded contour: unconditional bounds on the contour, never at zeros
LEMMA 2 (local expansion; classical, unconditional). There is an absolute C_1 with: for
|Im w| = T >= 2, -1 <= Re w <= 2, if dist(w, Z(zeta)) >= d > 0 with d <= 1 then
    |1/zeta(w)| <= exp( C_1 log T [ loglog T + log(1/(d log T))_+ ] )  <= exp(C_1' L(T)) for
    d >= delta_flr(T);   and with the nearest zero rho_* factored:
    |1/zeta(w)| <= |w - rho_*|^{-1} exp(C_1' L(T))   for dist(w, Z(zeta)∖{rho_*}) >= delta_flr.
    Also |zeta^{1/2}(v)| <= exp(C_2 log T) on the strip, and near a zero rho':
    |zeta^{1/2}(v)| <= |v - rho'|^{1/2} exp(C_2' log T).
Proof: Titchmarsh (2nd ed.) SS9.6: log zeta(w) = sum_{|w - rho| <= 1} log(w - rho) + O(log T);
the sum has <= C log T terms (RvM box count), each >= log d; the upper bounds for zeta^{1/2}
from |zeta| << T^{C} (convexity) and, near one zero, from |zeta(v)/(v-rho')| << T^{C} on a
disk (max modulus). No lower bound on |zeta'| is used anywhere. QED.

THE THREAD. Fix h in (0, 1/6], t in WIN. In the w-plane the far contour's base is the
vertical line Re w = sigma_c(h) := 11/16 + (3/2)h — t-INDEPENDENT (the brief's s-independence,
realized where possible: only the bounded endpoints w_end = a - (1/16 ∓ 40i) and the
branch-point positions 2a - rho' move with s). Segments:
 (T1) |Im w| <= 60: straight. All zeros here are on Re = 1/2 (A1): clearance >= 3/16;
      |1/zeta| bounded by an explicit compact-region constant (A1)+(A2). No threading.
 (T2) |Im w| > 60: replace the line by a thread Gamma(s): around each zero (or cluster of
      zeros, merged when within 2 delta_flr of each other; cluster diameter <= C c_0 <= 1/4
      by RvM box count) with |beta - sigma_c(h)| < delta_flr, bulge RIGHT keeping the zeros
      LEFT, radius delta_flr — EXCEPT where a branch point b' = 2a - rho' obligated RIGHT
      lies within delta_flr of an obligated-left zero cluster: there the thread passes
      between them at clearance ~ half the pole-branch gap d := |2a - rho - rho'| = 3|s - s'|
      (through the widest gap between adjacent cluster zeros, width >= d/m, m = cluster
      size <= C log T). Branch points with Re(2a - rho') < sigma_c(h) (i.e. beta' >
      13/16 + (3/2)h) are excluded rightward by a corridor: a path to Re w = 2 along a
      Landau-good height (dist >= delta_flr from all ordinates, exists in every unit height
      interval by pigeonhole on the <= C log T ordinates), with that rho''s cut laid rightward
      inside the corridor. All branch cuts point rightward; the thread never crosses a cut;
      branches are propagated continuously along the thread from the anchor.
DEFORMATION IDENTITY. For s with h > 5/24 the straight z-line is the P2.5 representation
(all poles right of it in z = strictly left in w; all branch points left in z = right in w,
since at h = 5/24: beta < 1 = sigma_c and Re(2a - rho') >= 9/8 > 1). Deform h downward:
the swept strip {sigma_c(h) < Re w < sigma_c(5/24)} minus the bulges/corridors contains no
singularity of Psi_sm; Cauchy on height-truncated pieces [Y_n, Y_{n+1}] with Landau-good
horizontal crossings (on which |Psi_sm| <= e^{-c_delta sqrt(Y)} e^{C L(Y)} -> 0) gives, for
every s in Omega^+ with s not a pinch point and the horizontal ray [s, s+inf) meeting no
pinch point (all but countably many t):
    Z_far^{sm}(s) = (1/2 pi i) int_{Gamma(s)} Psi_sm dz    (thread representation),      (TR)
where Z_far^{sm} on 0 < h <= 5/24 is DEFINED as the analytic continuation of the anchor
object along horizontal rays from Re s > 5/24 (this matches D_half's ray geometry and pins
the branch at multivalued points; the continuation exists off the countable pinch set P by
(TR) itself, continued disk-by-disk with thread stability radius ~ half the local clearance).

## 3. Pinch classification, the key on-contour estimate, and the coefficient sum

3.1 WHICH PAIRS CAN PINCH. (P) with h > 0 forces beta + beta' > 3/2; since beta, beta' < 1
(zeta != 0 on Re = 1, classical), BOTH members are strictly off-line: beta = 3/2 + 3h - beta'
> 1/2 and symmetrically. By (A1) both then have |gamma|, |gamma'| > 60. Height match:
gamma + gamma' = 3t in [gamma_1 - 3r_0, gamma_1 + 3r_0], so one ordinate is negative:
gamma = -|gamma|, gamma' = |gamma| + gamma_1 + O(3 r_0) — comparable heights, both > 60.
And max(beta, beta') > 3/4 + (3/2)h > 3/4: every pinch-capable pair contains a zero counted
by N(3/4, T). Ingham (1940, Quart. J. Math. 11, 291-292): N(sigma, T) << T^{3(1-sigma)/
(2-sigma)} log^5 T uniformly for 1/2 <= sigma <= 1; at sigma = 3/4: N(3/4,T) << T^{3/5} log^5 T.
NEAR-pairs (needed for bookkeeping): P_near := {(rho, rho'): beta + beta' > 3/2 - eta_0,
gamma + gamma' in [gamma_1 - 3r_0 - 1, gamma_1 + 3r_0 + 1]}, eta_0 = 1/8; then
max(beta,beta') > 11/16, so P_near is Ingham-counted at sigma = 11/16 (exponent 5/7), and
pairs OUTSIDE P_near keep pole-branch gap d >= eta_0/3: their (1/d)^m enhancement is
<= (3/eta_0)^{C log T} = e^{C'' log T}, absorbed into e^{C L(T)}.

3.2 THE KEY ESTIMATE (why 1/|zeta'| never appears). Near-pinch segment of the thread, pair
(rho, rho') in P_near at gap d = 3|s - s'|, cluster size m >= 1, T ~ gamma'. On the thread
(Lemma 2, all evaluations ON the contour):
  numerator |zeta^{1/2}(2a - w)| <= |2a - w - rho'|^{1/2} e^{C_2' log T}   — VANISHES at the
  branch point (the branch point is a ZERO of zeta, not the v=1 pole: the half power is
  POSITIVE — this single sign is what kills Prop G's hypothesis (ii));
  denominator |1/zeta(w)| <= e^{C_1' L(T)} prod_{cluster} |w - rho_i|^{-1};
  kernel |hat-kap_sm| <= C_delta e^{-c_delta sqrt(T - gamma_1/2 + O(1))}.
Segment integral (near part |w - rho| ~ d/m, length ~ d; far part u in (d, 1), integrand
(d+u)^{1/2} u^{-m}): with kappa_j := C_delta e^{-c_delta sqrt(gamma'_j)} e^{C_3 L(gamma'_j)}
(m^m and all log-power factors absorbed into e^{C_3 L}),
    |near-pinch contribution to Z_far^{sm}(s)|  <= kappa_j (1 + d^{3/2 - m}),          (K1)
    |its s-derivative| (differentiate (TR) under the integral on a thread stable in a disk
    of radius ~ d/6; d/ds hits (3/2)-shifts of log zeta: |zeta'/zeta| <= m/dist + C L(T)/1
    on the thread)                              <= kappa_j (1 + d^{1/2 - m}).          (K2)
For m = 1 (simple pinch): Z_far^{sm} stays BOUNDED through the pinch (K1: d^{1/2} -> 0);
only the derivative blows up, as (s - s')^{-1/2} — F-4's singularity, now identified as a
derivative-level (equivalently: Z has a bounded sqrt-branch (s-s')^{1/2}, its residue-free
on-contour avatar). For m >= 2 (a zero CLUSTER colliding with a branch point) Z itself can
blow up like d^{3/2-m}; handled by the h-exclusion device in 3.4.

3.3 GENERIC (non-pinch) THREAD MASS. Bulges with no branch point within delta_flr: clearance
delta_flr, |integrand| <= e^{C_1' L(T)} e^{-c_delta sqrt T} per unit length; per unit height
<= C log T bulges (RvM); the straight parts likewise. Hence
    M_sm <= M_verified(Z_0..60)  +  C int_60^inf log T e^{C L(T)} e^{-c_delta sqrt T} dT
         =: M_sm(delta) < inf   (explicit; the integral converges for EVERY c_delta > 0
    since sqrt T beats L(T); its size is governed by the crossover T_x of SS6).
M_verified is a computable compact-region constant ((A1)+(A2), clearance 3/16, heights <= 60).

3.4 THE COEFFICIENT SUM AND THE h-EXCLUSION SET. Define over pairs j = (rho, rho') in P_near
(rho' the max-beta member, gamma'_j its height; partner counted by RvM: <= C(r_0) log gamma'_j
choices at matched height):
    Sigma_c := sum_j kappa_j
             <= C int_60^inf log T e^{C_3 L(T)} e^{-c_delta sqrt T} dN(11/16, T)  < inf,
finite by Ingham + partial summation, for every c_delta > 0; EMPTY below the verified height
(both members off-line => heights > 60): the sum's every term requires two unverified off-line
zeros. [Finiteness needs only RvM's N(T) << T log T; Ingham sharpens the constant and the
crossover — record: the density input is a CONSTANT-improver, not the existence input.]
Exclusion set: for pairs with m_j >= 2 set w_j := kappa_j^{1/(2 m_j)} (so w_j >=
e^{-c_delta sqrt(gamma'_j)/(2 m_j)}, sum_j w_j < inf, super-geometrically); for m_j = 1, (K1)
is bounded and no exclusion is needed for Z; for the D_half bound below take additionally
w_j := exp(-1/kappa_j) for m_j = 1 (sum still finite). H_ex := union_j (sigma'_j - w_j,
sigma'_j + w_j), sigma'_j := Re s'_j; H_good := (0, 1/6] ∖ H_ex. Since sum w_j < inf and the
w_j are summable in blocks, H_good can be arranged (shrinking w_j by the dyadic-block device:
cap the total exclusion in (2^{-k-1}, 2^{-k}] at 2^{-k-2}) to meet every interval (0, eps):
0 in closure(H_good). If NO far pinch exists (e.g. if all zeros are on the line — unknown!),
H_ex is empty and every statement below holds for all h.

REVIEW CORRECTIONS (deposit hostile review, findings J1/J2 — applied 2026-08-23; they
LIMIT the theorem's scope, they do not repair-and-keep the original claim):

(J2 — the H_good construction, written out; the "dyadic-block device" one-liner above is
NOT a proof and is inconsistent with the w_j-based bound, since shrinking w_j inflates
the negative-exponent factor w_j^{(3-2m_j)/2}. The correct construction is a fixed-pair /
tail split:) The band 60 <= gamma'_j <= T_x contains FINITELY many pinch-capable pairs.
For each such FIXED pair, as h -> 0 the distance |h - sigma'_j| freezes at sigma'_j > 0
(the pinch abscissa is a fixed positive number), so its contribution to every bound is a
constant C_j — no exclusion interval is needed for band pairs at small h, and the finitely
many C_j sum to a finite constant. Exclusion intervals are needed only for TAIL pairs
(gamma'_j > T_x), where kappa_j <= e^{-c sqrt(T_x)} < 1 and the natural widths
w_j = kappa_j^{1/(2m_j)} are super-geometrically summable AS GIVEN (no shrinking): their
total length below any eps is < eps/2 for eps small, so 0 in closure(H_good) holds with
the UNSHRUNK w_j. This repairs the construction for m_j <= 2.

(J1 — the m_j >= 3 hole; MAJOR, substantive, NOT mechanical: the closing bracket of §4's
proof mislabeled it.) For tail pairs with m_j >= 3, the per-pair L2 bound
kappa_j w_j^{(3-2m_j)/2} = kappa_j^{1/4} needs the FULL width w_j = kappa_j^{1/(2m_j)},
but nothing prevents an adversarial (RvM/Ingham/verified-60-consistent) configuration —
e.g. triple zeros at Ingham-minimal heights with abscissae sigma'_k = 2^{-k} — from
placing its pinch points so that h int_WIN |...|^2 diverges along EVERY h-sequence
(h ~ 2^{-K} vs kappa_K^2 h^{-3} with kappa_K ~ e^{-cK^{5/6}}: 2^{2K} e^{-2cK^{5/6}} -> inf).
Killing this needs zero-density exponent <= 1/2 near sigma = 3/4 (unknown; Ingham gives
3/5) or a cluster-repulsion input. THE THEOREM BELOW IS THEREFORE UNCONDITIONAL FOR PINCH
CLUSTERS m_j <= 2 ONLY; the general case holds modulo the named lemma:

  [CLUSTER-3]  No pinch-capable pair (per §3.1) with cluster multiplicity m_j >= 3 has
               pinch defect < kappa_j^{1/(2 m_j)}.

(Simplicity of high zeros would give m_j = 1 everywhere and discharge it; that is
unproved. [CLUSTER-3] is strictly weaker than the old Prop G anti-conspiracy — it
constrains only m >= 3 clusters, not all pairs — but it IS an input of that flavor.)

## 4. THEOREM A2 (unconditional for pinch clusters m_j <= 2; modulo [CLUSTER-3] otherwise; L2 form)
Let delta in (0, 1/4], and let Z_far^{sm} be SS2's horizontal-ray continuation. Then with
M_sm, Sigma_c, H_good as above (all explicit, no unproved hypotheses; zero inputs: (A1),
RvM, Lemma 2, Ingham constants):
 (a) |Z_far^{sm}(h + it)| <= M_sm + Sigma_c + sum_{j: m_j >= 2} kappa_j d_j(s)^{3/2 - m_j}
     for all s in Omega^+ ∩ {h <= 1/6} off the countable pinch set; in particular Z_far^{sm}
     is BOUNDED on {h in H_good} ∩ {|3(s - s'_j)| >= w_j for the m_j >= 2 pairs}, and at
     every SIMPLE pinch Z_far^{sm} itself stays bounded (only Z' blows up, as (s-s')^{-1/2}).
 (b) |d/ds Z_far^{sm}(s)| <= M'_sm + sum_j kappa_j (1 + d_j(s)^{1/2 - m_j}),
     d_j(s) := 3|s - s'_j|, M'_sm explicit (Cauchy/under-integral differentiation).
 (c) D_half window bound: for h in H_good,
        h int_WIN |D_half[Z_far^{sm}](h + it)|^2 dt  <=  C_4 h (M_sm + M'_sm + Sigma_c)^2
                                                          (1 + C(r_0))  ->  0  (h -> 0).
 (d) The same bound holds for the connecting pieces and H_0^{sm} (classical, bounded), hence
     for the whole of B_sm - Z_loc^{sm} in place of Z_far^{sm}.
Proof. (a) Sum (K1) over pairs plus 3.3's generic mass; the thread representation (TR) holds
off the pinch set along horizontal continuation. (b) Sum (K2) plus the generic derivative
mass (same thread, one extra |zeta'/zeta| <= C L(T) + m/dist factor, absorbed as in (K2)).
(c) The ray estimate: for f = Z_far^{sm} on the ray [s, s+inf), s = h+it, h in H_good:
|f(s+del) - f(s)| <= min(2 sup|f|, int_0^del |f'|), and per pair j:
int_0^del d_j^{1/2 - m_j} du <= C del^{1/2} for m_j = 1 (any offset; int_0^del |u - x|^{-1/2}
du <= 4 sqrt(del)), <= C_j(del, d_j)-power for m_j >= 2. Insert into D_half's del-integral
split at d_j (FAR.md F2's Minkowski pattern pointwise): per-pair contribution
<= C kappa_j (1 + log(1/d_j(s)))   (m_j = 1),   <= C kappa_j (1 + d_j(s)^{3/2 - m_j} +
d_j(s)^{1 - m_j} ... ) <= C kappa_j (1 + d_j(s)^{1 - m_j})  (m_j >= 2; worst term).
L2(WIN, dt) norms: ||log(1/|s - s'|)||_{L2(WIN)} <= C(r_0) UNIFORMLY in the position of s'
(int_{-r_0}^{r_0} log^2(1/|u|) du < inf); for m_j >= 2, int_WIN d_j(t)^{2 - 2 m_j} dt <=
C |h - sigma'_j|^{3 - 2 m_j} <= C w_j^{3 - 2 m_j} on H_good, and with w_j = kappa_j^{1/(2m_j)}:
kappa_j * w_j^{(3 - 2m_j)/2} = kappa_j^{1 - (2m_j - 3)/(4 m_j)} <= kappa_j^{1/4} — summable
(sum_j kappa_j^{1/4} < inf by the same integral as Sigma_c with c_delta/4). Total:
||D_half Z_far^{sm}(h + i.)||_{L2(WIN)} <= C (M_sm + M'_sm + Sigma_c)(1 + C(r_0)) uniformly
on H_good; multiply by h. (d) P2.4(c) on the classical bounded pieces. QED
[CORRECTED per review J1: the closing remark here originally called the m_j >= 2
bookkeeping "mechanical" — that label was WRONG for m_j >= 3, where the exponent
arithmetic hides the substantive hole documented in the REVIEW CORRECTIONS block before
this section ([CLUSTER-3]). The m_j = 1 chain is complete as stated; m_j = 2 is marginal
but closes (kappa_j^2 h^{-1} * h = kappa_j^2, summable with the Ingham height floor).]

## 5. THE REDUCTION: (FAR-WIN) / [P2-FAR] reduce to the corner field alone

5.1 COROLLARY (corner-only [P2-FAR]). Write Z_far = Z_far^{sm} + Z_far^{c} (SS1's split; on
0 < Re s <= 5/24, Z_far^{c} := Z_far - Z_far^{sm} with Z_far the P2.7-step-(3) subtraction
object under R and Z_far^{sm} the SS2 continuation — well-defined off the countable pinch
set, measurable, L2 on window lines). Suppose the CORNER RESIDUAL
    (C-b)  limsup_{h -> 0, h in H_good} h int_WIN |D_half[Z_far^{c}](h+it)|^2 dt
           <= eps_1^2 (2/(3 pi)) |c_B|^2,   some eps_1 < 1.
Then [P2-FAR] holds in P2.7 Remark (iii)'s form along H_good with the SAME eps_1 (Peter-Paul
with Theorem A2(c): for every eta > 0, h||D_half Z_far||^2 <= (1+eta) h||D_half Z_far^c||^2 +
(1+1/eta) h||D_half Z_far^{sm}||^2, second term -> 0), and P2.7 concludes with
c_0' = (1 - eps_1)^2 (4/(3 pi))|c_B|^2. The smooth far field consumes NONE of the eps-budget.

5.2 THE CORNER RESIDUAL, PRECISELY (the surviving open statement). Corner far field:
kernel kap_c supported in r in (1, e^delta] ∪ [2 e^{-delta}, 2]; its window form via F3's
multiplier calculus is FAR.md's (FAR-WIN) restricted to the corner field, with TWO
bookkeeping amendments this lane pins:
  (i) pinch-share: the corner field owns pinch coefficient c_B^{c} = c_B - c_B^{sm}
      (SS1; both are k(1/2)-type cut evaluations of the respective kernel transforms at z*,
      computable to any precision with (A1) inputs only). The corner (FAR-WIN) subtracts
      c_B^{c} (pi x)^{-1/2}-type pinch, not the full c_B one. Numerically the corner window
      share is ~8% (FAR.md 5.1-2): |c_B^{c}| is expected small; not needed by the logic.
  (ii) h-set: limsup along H_good suffices (5.3).
Also: corner-F1 is UNCONDITIONAL (not even R needed): |c_n^{(delta)}| <= 2 Delta(n) and
Hall-Tenenbaum sum Delta(n)^2 << x log^{C_0} x give ||psi_c e^{-hx}||_2^2 << log^{C_0+1}(1/h),
so the F2/F3/F4 machinery runs on the corner field with log-power (not single-log) budgets —
these enter only error terms that vanish after the h-multiplication, so FAR.md Sec 3's
reduction theorem applies to the corner field verbatim with (FAR-WIN)_corner in place of
(FAR-WIN). [Mechanical re-run; flagged, not re-derived here.]

5.3 INTERFACE CHECK (does P2.7 accept the H_good amendment?). P2.7's chain (1)-(4),(6) holds
for EVERY h in (0, 1/2); step (6) multiplies by 2h and takes h -> 0: any sequence h_k -> 0
suffices, and H_good contains such sequences (SS3.4: 0 in closure). Remark (iii)'s far-field
hypothesis is consumed at exactly that step: replacing "limsup_h" by "limsup along H_good"
changes nothing downstream. Likewise FAR.md Theorem 3's proof consumes (FAR-WIN) only in its
Step-3 limsup: same amendment passes through. VERIFIED against both source texts. If no far
pinch exists, H_good = (0, 1/6] and the amendment is vacuous.

## 6. Crossover height and numerics (pairs_num.py, gevrey_num.py; *_out.json)

6.1 Verified-zero census (pairs_num.py, mpmath zetazero, 60 zeros to height 163.03):
139 candidate far-pinch pairs with gamma' - |gamma| in [gamma_1 - 3, gamma_1 + 3] (r_0 = 1);
first 20 listed in pairs_num_out.json (closest height-match: pair (11,16), |Im s' -
gamma_1/3| = 0.0084). EVERY pair has beta + beta' = 1 (all on-line): pinch defect
(3/2 + 3h) - 1 >= 1/2 for all h > 0, and Re s' = -1/6, an ABSOLUTE distance 1/6 left of
Omega^+. Confirms SS3.1: no pair with both heights <= 60 (indeed <= 163) can pinch or even
near-pinch: the theorem's pinch machinery is vacuous below the verified height, exactly as
the both-members-off-line classification predicts.

6.2 Gevrey decay measured (gevrey_num.py): bump exp(-1/u - 1/(delta - u)), delta = 1/4:
|hat-theta(xi)| = 5.8e-10 (xi = 100), 3.9e-40 (xi = 3200); fitted c in exp(-c sqrt xi):
1.48 (theoretical endpoint constant 2, slow logarithmic approach — consistent).

6.3 Crossover T_x (c sqrt T = C L(T)) and the tail integral
I(H) = int_H^inf T^{3/5} log^6 T e^{-c sqrt T + C L(T)} dT (Ingham weight + RvM partner log):
   c=1, C=2:  T_x ~ 960;    I(60) ~ 5e9;   I(T_x) ~ 8e6
   c=1, C=5:  T_x ~ 1.1e4;  I(60) ~ 1e25;  I(T_x) ~ 5e10
   c=2, C=5:  T_x ~ 1.2e3;  I(60) ~ 1e15;  I(T_x) ~ 6e8
All FINITE (machine-verified), as SS3.4 requires; magnitudes are astronomically large — but
Sigma_c and M_sm multiply terms whose window h-mass is O(h): ONLY FINITENESS is load-bearing
(the rate at which h must shrink is affected, the conclusion is not). The crossover exceeds
the verified height 60 for realistic constants: the band [60, T_x] dominates the BOUND
(not the truth: every term there requires two off-line zeros; none are known to exist).
EFFECTIVE TRUNCATION: contributions from pairs above height H are <= I(H): the Gevrey tail
truncates the pair sum at H(target) = ((C L(H) + log(1/target))/c)^2, self-consistently
~ (log(1/target)/c)^2 for small targets. LOW pairs (both <= 60): killed by (A1) (6.1);
HIGH pairs: killed by density + Gevrey; the crossover between the two regimes is exactly
the verified height 60 — the two mechanisms overlap for all realistic constants ONLY through
the finite (large) band [60, T_x], which is where any future improvement (higher verified
height, better density, larger c_delta) directly shrinks the constants.

## 7. Honest status inventory

PROVED HERE (load-bearing, checked): (1) Lemma 1 (Gevrey transform decay, uniform in s);
(2) the w-plane base line is t-independent — the brief's s-independence, realized; (3) the
pinch classification: both members off-line, both heights > verified floor, Ingham-counted
(SS3.1) — machine-checked against real zeros (6.1); (4) THE KEY ESTIMATE (K1): at a simple
far pinch the smooth far field itself stays BOUNDED — the branch point is a zero of zeta,
so the on-contour numerator VANISHES like d^{1/2} and cancels the pole's d^{-1}; F-4's
(s-s')^{-1/2} lives only in the DERIVATIVE. This single observation eliminates Prop G(ii)
(the |zeta'| lower bound): 1/zeta' appears only in residue extraction, and no residue is
ever extracted. (5) Sigma_c < inf unconditionally — Prop G(i) (anti-conspiracy) is replaced
by Ingham-counting + Gevrey damping of whatever pairs exist. (6) Theorem A2(c): the smooth
far field's D_half window h-mass VANISHES along H_good. (7) The interface amendment is
accepted by P2.7 and FAR.md Thm 3 (5.3, checked against the texts).

FLAGGED MECHANICAL (stated, not fully re-derived): (a) the Cauchy bookkeeping of the full
thread deformation (truncation sequence, corridor cuts, branch propagation) — standard but
lengthy; (b) the m_j >= 2 cluster exponent bookkeeping in Theorem A2(c) and the dyadic-block
arrangement of H_ex; (c) the corner-field re-run of FAR.md Sec 3 with log-power budgets and
the c_B^{c} pinch-share (5.2); (d) explicit numeric values of C_1', C_2', C_3 (Titchmarsh
SS9.6 constants) — needed only for the SIZE of M_sm, never for finiteness.

NOT PROVED, NOT CLAIMED: the corner residual (C-b) — the entire surviving content of
[P2-FAR]/(FAR-WIN); any statement about where zeta's zeros are; RH.

WHAT DIED (see FAILURES.md): pointwise form (a) for the smooth far field (false as stated
if any far pinch exists — the derivative genuinely blows up); residue-extraction routes
(reintroduce 1/zeta'); epsilon-SMALLNESS of the smooth part with explicit constants
(unachievable through the [60, T_x] band; fortunately boundedness + vanishing h-mass is
what the architecture consumes).
