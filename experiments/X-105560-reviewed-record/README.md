# X-105560 — Reviewed simple-zero record replay

The replay checks:

- the scalar minimization in the stability-enhanced rank--inertia inequality;
- the sum-free kernel-zero algebra on a rational surrogate;
- every seven-gap global-averaging coefficient;
- a rational alternating-series enclosure of `H0`;
- the exact final comparison with `673/1000`;
- all frozen source identifiers and fail-closed status flags.

It does not rerun the external 707,901-node Arb search. That independent second
replay remains explicitly pending.

```bash
bash experiments/X-105560-reviewed-record/replay.sh
```
