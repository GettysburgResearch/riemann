# X-106100 — Fully amplified owner/anchor repair replay

Run:

```bash
python3 verify.py
```

The replay checks finite exact instances of:

- squareclass normalization when the opposite owner product `Q` varies;
- the even-character kernel after owner and core are combined;
- the failure of a separate `Q`-weighted Cauchy payment;
- the corrected `g^2*ell` atomic-diagonal algebra;
- the finite source-dual Cauchy inequality.

It does not prove the off-diagonal fully amplified moment, `BCI102990`, the
Mellin consumer or RH.
