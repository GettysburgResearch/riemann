# X-103100 exact replay

The replay checks finite coefficient identities underlying the packet:

- `eta*eta=1`;
- `h_U*h_U=a_U*a_U*mu`;
- the symbolic Haar autocorrelation at all breakpoints;
- fixed-to-narrow scale factorization for several refinement depths;
- gcd sign removal and unique largest-prime sign;
- fail-closed theorem status.

It does not prove `HCNC103100` or RH.
