# X-91120 — Heterogeneous corridor Hall firewall

This exact directed replay evaluates the adverse heterogeneous assignment

```text
parameter 4/3 on every even capacity;
parameter 3/2 on every odd demand;
```

at the last factor-54 cell and threshold `31`. It certifies a Hall margin below
`-3/2`.

The result does not contradict the common-parameter Hall theorem. It proves that
paired channel labels cannot be discarded before Hall projection.
