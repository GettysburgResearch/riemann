# X-104640 — Exact source/inner-factor firewall replay

This bounded replay checks:

- the polynomial Bézout identity of `L-104637`;
- the exact \(F_n=c+\cos(nt)\) frozen-source packet;
- zero-residual degree-six source transport;
- the physical quotient factorization with numerator inner degree zero and
  denominator inner degree two;
- grade-zero collapse of the projected seven-grade Krylov family;
- the exact positive ridge excess;
- the two inherited budget constants.

It uses exact `Fraction` arithmetic.  It does not prove the operator-valued
fractional sandwich, Xi asymptotics, `XIMICRO104640`, more than ninety percent,
or RH.
