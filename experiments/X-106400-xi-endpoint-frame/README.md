# X-106400 — Xi endpoint-frame finite replay

Run:

```bash
python3 experiments/X-106400-xi-endpoint-frame/verify.py \
  --output experiments/X-106400-xi-endpoint-frame/results/verification.json
```

Expected classification:

```text
PASS_T106400_XI_ENDPOINT_FRAME_ALGEBRA
```

The replay checks:

- the endpoint-companion product cancellation;
- a finite exact exterior-square Fourier-density fixture;
- the load-bearing square mutation;
- the Frobenius quantile constants;
- the exact conditional fraction `18149/20000 = 0.90745`.

It does **not** evaluate Xi, prove the cofinal entire-window passage, establish
`ROBUSTFRAME106400`, prove ninety percent, density one, or RH.
