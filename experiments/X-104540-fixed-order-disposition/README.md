# X-104540 — exact fixed-order disposition replay

Run:

```bash
python3 verify.py --output /tmp/t104540.json
```

The replay checks only exact rational arithmetic and the elementary analytic
counterexample.  It does not reproduce Conrey's mollifier proof and does not
prove RH.
