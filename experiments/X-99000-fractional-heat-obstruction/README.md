# X-99000 — fractional heat obstruction replay

```bash
python3 verify.py --output results/verification.json
sha256sum -c SHA256SUMS
```

The replay verifies exact algebra and finite fixtures.  The specialized Hankel
transfer and asymptotic analysis are proved in `R-99000`; they are not replaced
by a numerical test.  The replay does not prove PCSCHE or RH.
