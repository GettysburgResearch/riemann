# X-97240 global parity-Hall scalar/Julia replay

Run `python3 verify.py --output results/verification.json`. The replay checks exact finite algebra and interface mutations. It does not prove GABPT or RH.
