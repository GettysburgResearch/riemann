# X-104520 — quantitative reverse-Rolle finite fixtures

Run:

```bash
python3 verify.py --output /tmp/t104520.json
```

The checker verifies:

- the exact canonical Fourier companion derivative chain on rational finite
  atomic measures;
- the quartic no-proportion counterexample;
- that an open vertical-side logarithmic integral is not an integer index;
- model consecutive zero-count ratios above `0.9`.

It does not evaluate Xi, prove the saddle estimates, prove either final
last-defect gate, or prove RH.
