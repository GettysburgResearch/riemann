# X-102500

Standard-library lightweight replay for the common mother and covariant stress
tensor. It checks exact finite algebra and numerical fixtures only. It does not
prove the open arithmetic rows or RH.

Run:

```bash
python3 verify.py --output results/verification.json
```

Expected verdict:

```text
PASS_T102500_COMMON_MOTHER_STRESS_TENSOR
```
