# Production run request

Regenerate the deterministic X-18507 certificate, verify both immutable input bindings, replay the exact Fraction consumer and all seven mutations, and upload the complete proof object.
