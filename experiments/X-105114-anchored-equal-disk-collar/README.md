# X-105114 anchored equal-disk collar replay

This lightweight replay authenticates the exact formula ledger behind
L/T/R/M-105114:

- normalized amplitude and dimensionless spatial constants;
- Jensen multiplicity and nominal equal-disk radius accounting;
- a rational Blaschke-factor and projection fixture;
- the constant-function normalization refutation;
- the smaller raw-quotient exceptional union;
- dependency, content-hash, digest, and open-scope firewalls.

Run:

    python -B tests/test_verify.py
    python -B -O tests/test_verify.py

The replay does not machine-prove Jensen or Borel-Caratheodory, evaluate Xi,
find zeros, or run a numerical minimum-modulus search.
