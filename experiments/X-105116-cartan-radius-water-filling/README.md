# X-105116 Cartan radius water filling

The verifier uses exact fractions to check an interior KKT fixture, a capped
fixture, the common-radius Cartan specialization, fractional safe-shell
slack, and strict-budget nonattainment.

Run:

    python -B tests/test_verify.py
    python -B -O tests/test_verify.py

No numerical optimizer or heavy computation is used.
