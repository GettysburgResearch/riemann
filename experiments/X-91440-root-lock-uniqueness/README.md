# X-91440 — Root-lock uniqueness and dyadic firewall

This replay checks the algebra and sign diagnostics behind `L-91440/R-91441`:

1. the residual derivative, value at zero and zero integral;
2. the numerical enclosures of `tau_*`, `kappa`, `a_*` and the all-prime threshold;
3. absence of sign violations for `R(a_*u)B(u)` on a dense high-precision grid;
4. explicit negative-product witnesses for scales below and above `a_*`;
5. positivity of the `n=2` prime coefficient at `a_*/2` and its negativity at `a_*/4`.

The uniqueness and dyadic inequalities are proved analytically in the claim
files. The numerical grid is diagnostic. No operator domination or RH result is
claimed.
