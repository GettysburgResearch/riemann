# X-102990 — Equal-pair Boolean core normal form

This lightweight replay checks:

```text
canonical equal-pair source weights and energies;
endpoint-color identity EC=M^2-D^2;
first degree of every nontrivial color variance;
owner-independent divisibility of a selected phase label.
```

It does not prove `BCI102990`, the physical coprime-core orientation, the Mellin consumer, or RH.