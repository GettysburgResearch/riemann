# X-91650 — T-91651 foundational replay

Run `python3 verify.py`.

The checker covers the exact coefficient cancellation, the strict `<1/8`
recursive coefficient bound, the corrected directed value `D(67)>1`, and the
finite root coefficient-count gate. Expensive imported finite campaigns are
referenced by blob SHA in the dependency lock and are not rerun here.