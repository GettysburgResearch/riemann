# Canonical experiment ID

The canonical experiment ID for this legacy path is

```text
X-13202
```

The directory name is retained to preserve the already-open trigger PR #135 and
its workflow paths. It must not be registered as `X-12102`.

Canonical analytic claim: `L-13201`.
