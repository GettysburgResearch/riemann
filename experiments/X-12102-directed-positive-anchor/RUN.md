# Provenance-repaired PA1 trigger

This marker launches the X12102 workflow after the audit repair that accepts the
immutable source certificate through its explicitly typed Git blob SHA-1 and
retains the original basis hash in the output manifest.

No sign or counterexample is asserted by this trigger.
