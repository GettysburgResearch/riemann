# X-106700 — MESOTRANS hostile-audit replay

Run:

```bash
python -B experiments/X-106700-mesotransport-audit/verify.py
```

The replay checks:

- the exact adverse/favorable canonical-defect index identity;
- the nonnegative transport and numerator-subfactor debts;
- the exact `c+cos(nt)` two-unit endpoint countermodel;
- the direct simple-fifth-residue transition allowance `863/10000`;
- the existing `3/40 + 11/500 = 97/1000` ledger.

It does not evaluate Xi, prove the arithmetic transport, establish the residue-transition bound, prove ninety percent, or prove RH.
