# X-106090 — Least-discrepancy rough-tail replay

Run:

```bash
python3 verify.py
```

The replay checks finite exact/combinatorial instances of:

- unique least-discrepancy orientation for coprime squarefree cores;
- strict roughness of the opposite reduced core;
- clean physical divisibility and the nonzero Ramanujan phase;
- the exact even-character kernel identity;
- the fixed-fibre dyadic long-core inequality;
- the exact source-dual diagonal-weight cancellation.

It does not authenticate the distinct-anchor amplified moment, `BCI102990`, the
Mellin consumer or RH.
