# X-96600 paper reconstruction

```bash
python3 verify.py --output results/verification.json
```

The checker verifies exact hazard algebra, rank drop, squarefree first-owner factorization, the activated rough-store firewall, the exact two-row common-zero polynomial, and a one-million-endpoint row diagnostic. It does not prove the universal stopping theorem, the frozen AVLT theorem, or RH.
