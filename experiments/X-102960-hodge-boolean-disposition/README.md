# X-102960 — Hodge/Boolean disposition replay

This standard-library checker authenticates the finite algebra behind the
independent disposition of PR #751's `T-106080` proposal.

It verifies:

```text
M(x)^2 differs from 1-x only in degrees >=2;
Boolean restriction commutes with disjoint-support convolution;
squarefree divisor reindexing;
the l1 square-shift transfer mass;
the source-blind labelled-to-physical collapse firewall.
```

It does not replay the parent centered phase theorem, prove the owner-indexed
physical restriction `OICP102960`, accept `T-106080`, or establish RH.
