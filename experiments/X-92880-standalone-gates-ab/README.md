# X-92880 — Standalone Gates A/B authentication replay

Run:

```bash
python3 verify.py --mutations --output results/verification.json
```

The replay checks finite constants, strict inequalities, required paths and the frozen dependency manifest. It does not replay the analytic Hall/profile, endpoint-frame, source-realization or Mellin–Landau theorems and does not prove RH.
