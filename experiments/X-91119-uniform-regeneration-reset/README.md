# X-91119 — Uniform four-state regeneration reset

The exact replay checks

```text
D_p = c_p I + R_p,
```

with `c_p=(1-p^-1/2)/50`, the SHARP observation of `R_p`, the uniform
`(4/3,3/2)` channel corridor, and a synthetic disjoint-branch contraction.

It does not certify the measure-valued least-prime source partition required by
`L-91332`, and it does not independently prove RH.
