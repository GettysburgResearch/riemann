# X-101100 replay

Run:

```bash
python3 verify.py --output results/verification.json
```

The replay checks the exact endpoint homotopy coefficients, the directed
factor-eight reciprocal-prime budget, largest-label matching, negative-part
subadditivity, and fail-closed status flags.
