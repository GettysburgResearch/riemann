# X-100000 replay

```bash
python3 verify.py --limit 200000
```

Expected verdict:

```text
PASS_T100000_CENTERED_BERNSTEIN_HIERARCHY
```

The finite scan is diagnostic only. The replay does not prove the critical
remainder nonnegative and does not establish RH.
