# X-105310 — Low-order critical-residue Pick rank--trace replay

The replay checks only the finite exact spine:

- Gaussian-rational verification of the centered `p/p'` Pick decomposition;
- real-sign and nonreal-pair inertia fixtures;
- confluent multiplicity charge inequalities;
- the exact `0.86864/0.93432 -> 0.0821` nuisance budget;
- Hermitian rank--trace fixtures;
- the `49/51` Cauchy-power model reserve and one-percent perturbation arithmetic;
- the `0.919 -> 0.6738` record-target calculation.

It does not reprove the imported Anthropic theorems, evaluate an Xi explicit
formula, prove `LPRT105310`, establish a new zero proportion, or prove RH.
