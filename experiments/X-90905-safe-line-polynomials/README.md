# X-90905 — One-safe-line Euler weight polynomials

Finite exact/high-precision regression for `L-90906`.

Checks the raising recurrence, coefficient variation, unique positive roots,
strict root monotonicity, the sharp window

```text
2k+2 < tau_k^2 < 2k+3,
```

and the Gamma--Bessel integral representation.

Expected verdict:

```text
PASS_X_90905_SAFE_LINE_POLYNOMIALS
```
