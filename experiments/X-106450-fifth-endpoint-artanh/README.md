# X-106450 — Fifth-endpoint and artanh-source replay

Run:

```bash
python3 experiments/X-106450-fifth-endpoint-artanh/verify.py \
  --output experiments/X-106450-fifth-endpoint-artanh/results/verification.json
```

Expected classification:

```text
PASS_T106450_FIFTH_ENDPOINT_AND_ARTANH_SOURCE_ALGEBRA
```

The replay checks finite polynomial symmetrization, exact rational source
constants, a finite collection of rank-one Loewner-feature Gram fixtures, and
hostile status flags. It does not evaluate Xi, authenticate a cofinal endpoint
bank, prove either signed-tail statement, prove ninety percent, density one, or
RH.