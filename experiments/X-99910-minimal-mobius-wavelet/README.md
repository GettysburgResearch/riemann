# X-99910 — Minimal Möbius-wavelet replay

The verifier checks:

- the exact degree-three annihilator in `Q(sqrt(2))`;
- its double root at one and root at `2^(-1/2)`;
- the positive-resolvent firewalls;
- a rigorous rational interval proof of the negative `X=4` fixture;
- fail-closed scientific status.

It does not prove the open signed Hardy/Carleson estimate.
