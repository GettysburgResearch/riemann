# X-100340 — exact algebra replay

The replay checks:

- hereditary activation-prefix lower and upper bounds;
- largest-label capacity matching;
- the cumulative envelope jump identity;
- Cauchy phase kernel `min(m,n)/max(m,n)`;
- gcd/common-core sign extraction;
- the squarefree common-core Euler product on finite truncations;
- fail-closed status flags.

It does not prove `CDTG100320`, `PRBC100330`, or RH.
