# X-91410 — Cutoff-minor reconnaissance

This directory records discovery data for `L-91411`–`L-91416` and `O-91417`.

The retained JSON is **not** a proof object. It used ordinary floating-point evaluation on a finite sample of rough parameters, integer child endpoints, and all 65 rows. It found no negative actual-cutoff determinant, finite-prefix gap, or cutoff-minor shift-eight Hall margin.

A production verifier must replace this scan by outward rational intervals on every activation/cutoff cell and combine them with the analytic large-`p` reduction in `L-91413`.
