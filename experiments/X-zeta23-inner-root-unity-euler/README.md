# X-zeta23-inner-root-unity-euler

Exact `Fraction` replay for the phase-resolved powered inner Euler frame.

It checks the reflected all-pass identity, strict and exponentially growing
off-line pair determinant, root-of-unity separation of all active local Selberg
terms, and the polynomial deterministic-gauge coefficient.

It certifies finite algebra only. The complete independent-frequency arithmetic
growth bound and RH remain open.
