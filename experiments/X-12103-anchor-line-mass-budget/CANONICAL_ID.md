# Canonical experiment ID

The canonical experiment ID for this legacy path is

```text
X-13203
```

The directory name is retained to preserve committed control bytes and digests.
It must not be registered as `X-12103`.

Canonical analytic claim: `L-13203`.
