# X-101220 truncated Dickman interval replay

The replay checks finite convolution-exponential algebra, the delay/integral identity on a discretized continuum model, representative actual-prime prefixes, and fail-closed theorem flags. The asymptotic convergence theorem itself is proved analytically in `L-101220`; the replay is not a substitute for Mertens' theorem.
