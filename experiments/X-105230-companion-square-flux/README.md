# X-105230 light algebra replay

The replay checks:

- the three residue contributions `1`, `2 lambda rho`, `lambda^2 rho^2`;
- the cubic integration-constant firewall;
- the `delta=1/2` companion roots `i(2+-sqrt(3))` are upper-half-plane;
- the 90% charge rearrangement;
- fail-closed status flags.

It does not evaluate Xi, authenticate canonical-product exhaustion, or prove
`CIBF105230`.
