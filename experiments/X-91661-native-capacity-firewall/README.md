# X-91661 — Native-capacity firewall

This packet records the exact first rough-activation witness from `L-91661`.

```text
P = product of primes through 61
first coprime integer after 1 = 67
X = 135
q = 2
ordinary gap = detail gap = log(135/134)/sqrt(134) > 1/1620
```

The witness concerns the native feasibility dictionary only. It does not prove the Riemann Hypothesis.
