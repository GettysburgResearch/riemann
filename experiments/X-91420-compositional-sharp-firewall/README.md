# X-91420 — Compositional SHARP firewall

Checks:

1. the one-prime identity `w N(r) = w M(r)`;
2. the exact two-factor defect;
3. the general one-shot completion `N(A,B)`;
4. the exact four-state intertwining;
5. strict positivity of sample two-factor defects.

This replay refutes only the arbitrary cascade of completed two-state maps. It
does not prove the four-state reset projection or RH.
