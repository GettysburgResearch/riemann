# X-91411 — Reciprocal-limit shifted-eight transport moment

Status: **EXACT FINITE CERTIFICATE; ANALYTIC TRANSFER SEPARATE**

Run `python3 verify.py` to reproduce the exact common-denominator transport and directed square-root interval result.

Retained verdict:

```text
PASS_P61_RECIPROCAL_LIMIT_HALL_MOMENT
```

The checker proves a limiting prefix margin above `1/100` and a limiting transport moment above `18`. It does not prove the finite-parameter perturbation step or RH.

The earlier local SHA-256 lines were removed because they predated the exact GitHub serialization. A Git-clean digest ledger remains to be generated.
