# Trusted-base production trigger

This child branch exists only to launch the proof-producing workflow already
present on its base branch.

The workflow evaluates unconditional total zeta-zero counts at nine nested exact
windows around the exact PR #71 ordinate, produces the inherited direct
completed-xi rectangles at 192 and 256 bits, reconstructs the L-9303 shell
increments, and checks all declared two-point and cross-Loewner rows.

No sign or counterexample is asserted by this file.
