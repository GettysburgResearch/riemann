# X-91820 — Positive common-parent response-complement replay

This lightweight deterministic checker authenticates the finite composition layer of `T-91821`:

```text
Hall demand exhaustion and source non-overdraw;
Hall residual plus edge-owned bonus identity;
unique least-rough-prime ownership;
causal coefficient identity and recursive mass below 1/8;
one anchored-plus-bulk labelled quantizer;
positive-source / signed-observation type separation;
coverage of physical columns q<K;
129/sqrt(K) nonterminal constant;
581 X^(-3/2) terminal margin;
strict denominator 130 in the common thinning;
positive radix-four inverse;
correct endpoint orientation;
absence of the forbidden continuum-benchmark bridge.
```

## Replay

```bash
cd experiments/X-91820-positive-common-parent-response-complement
python3 verify.py --mutations --output results/verification.json
python3 -m unittest discover -s tests -v
python3 -m py_compile verify.py tests/test_verify.py
sha256sum -c SHA256SUMS
```

Expected verdict:

```text
PASS_T91821_POSITIVE_COMMON_PARENT_RESPONSE_COMPLEMENT
```

Ten hostile mutations must fail closed.

## Scope boundary

The checker does not independently authenticate the directed compact Hall census, component-profile monotonicity, positive packet-cone realization, endpoint density, analytic mismatch/collar estimates, prime-square theorem, Mellin transform, Landau theorem, or RH.
