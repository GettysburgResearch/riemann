# Superseded centered block

These retained artifacts used exactly 256 consecutive Hardy-zero balls and
therefore prove the recorded positive signs for those 256 certified line zeros.
They did not contain unselected exterior guards, so they do not independently
prove that the selected set is globally nearest.

The guarded `../pr71/` run supersedes this directory. It emits 320 consecutive
balls, proves exact separation of the globally nearest 256 from all 64 guards,
and replays both the declared rows and every disjoint fixed-grid minor.
