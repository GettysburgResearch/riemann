# Run the expanded PR #71 zero-deflated direct-xi certificate

This child commit exists solely to launch
`.github/workflows/pr71-zero-deflated-modulus.yml` from a base branch that
already contains the proof-producing workflow.

At the exact ordinate

```text
20225875608341108140435 / 2^32
```

the workflow produces at 192 and 256 bits:

1. the line-empty-slab Hardy-Z/Turing discrepancy certificate;
2. an indexed block of 128 pairwise-disjoint Platt-isolated Hardy-Z zero balls;
3. one direct completed-`xi` table at nine exact dyadic horizontal offsets.

It checks the original bracketing pair and the nearest-zero ladders

```text
2, 4, 8, 16, 32, 64
```

against the same twenty algebraic and logarithmic Loewner rows, with exact
precision nesting at every layer.

No sign is asserted by this marker. A strict negative row or positive
line-empty-slab total-zero discrepancy is a nomination only until independent
backend reproduction and analytic review are complete.
