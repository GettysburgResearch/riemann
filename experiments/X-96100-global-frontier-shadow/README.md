# X-96100

```bash
python3 verify.py --output results/verification.json
```

The replay verifies the exact fixed-product proof failure, the global
rough-reservoir coefficient identity, exact finite capacity algebra, and broad
finite knot diagnostics.  It explicitly does not prove the universal transport
or RH.
