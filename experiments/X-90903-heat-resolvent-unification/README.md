# X-90903 — Heat/resolvent confluent unification

Checks the `y^-2` confluent limit of the safe-Euler resolvent derivatives and its exact Gamma-moment representation by high-precision quadrature.

Expected verdict:

```text
PASS_X_90903_HEAT_RESOLVENT_UNIFICATION
```
