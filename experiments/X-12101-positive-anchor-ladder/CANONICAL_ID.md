# Canonical experiment ID

The canonical experiment ID for this legacy path is

```text
X-13201
```

The directory name `X-12101-positive-anchor-ladder` is retained only to preserve
committed certificate bytes, digests, workflow paths, and open-PR history. It
must not be registered as `X-12101`; that identifier belongs to concurrent PR
#125.

Canonical claims: `L-13201`, `L-13202`, `O-13201`.
