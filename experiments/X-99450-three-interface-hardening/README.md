# X-99450 — Three-interface hardening replay

```bash
python3 verify.py --output results/verification.json
sha256sum -c SHA256SUMS
```

The replay checks exact finite algebra, the complete outward Hall certificate,
RN ownership fixtures, nilpotent coboundary identities, clamping algebra, and
the noncancellation leading coefficient. It does not establish the complete
native root registry or RH.
