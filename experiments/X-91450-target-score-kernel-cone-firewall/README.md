# X-91450 — Target/score kernel cone firewall

This replay checks the exact algebra behind `R-91450/L-91451`:

1. every common positive `W_Psi/W_S` measure satisfies
   `4*score-5*target = 3 sum nu(n)/sqrt(n) >= 0`;
2. the complete small-prime forcing at `x=5` has positive target and score but
   `4*score-5*target=3A<0`;
3. the physical map `(L,R)->(L+2R,2L+R)` has the stated inverse and cone;
4. the pure equality and reserve kernels each have an exact coefficient-one
   geometric child plus positive harmonic slack.

The radical signs used for the `x=5` witness are proved with elementary rational
bounds in `R-91450`. This does not prove the physical all-generation splice or
RH.
