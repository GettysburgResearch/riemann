# X-98710 — Fractional heat Bohr hardening

```bash
python3 verify.py --output results/verification.json
```

The checker verifies exact finite algebra and negative controls. The Gaussian
Selberg--Delange and Bohr--Kronecker steps are proved in `R-98710`; the replay
does not mislabel them as machine proofs.
