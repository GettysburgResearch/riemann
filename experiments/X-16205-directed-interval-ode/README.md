# X-16205 — source-bound directed CCM interval-ODE prefix

This retained prefix records the first parameterized `DIRECTED_INTERVAL_ODE`
replays for the order-zero CCM prolate packet with modes `0,4,8,12`.

The retained scales are

```text
gamma = 2^(12+3j), j=0,1,2
      = 4096, 32768, 262144.
```

For each block the producer enclosed angular separation values, certified the
infinite-Jacobi index by a Schur-tail Sturm calculation, enclosed regular pole
Cauchy data, serialized the exact two-constraint repaired-source definition,
and replayed the result through the unchanged X-16204 consumer.

The prefix is historical evidence for the emitter/consumer interface. The newer
X-16206 production packet supersedes it for active development. In particular,
consumer acceptance did not derive the deterministic-error, complete arithmetic
profile-Gram, or support-average fields from the radial primitive. Those fields
remain the active production moat.

No RH proof is claimed.
