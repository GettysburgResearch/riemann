# X-106110 — Dual-amplified owner-collision replay

Run:

```bash
python3 verify.py
```

The standard-library replay checks finite exact or high-precision instances of:

- the Q-fixed external-weight fibre-dimension firewall;
- amplification over all opposite owners before squaring;
- the sharp squareclass principal embedding;
- the equal-pair semiprime Wick identity;
- complete-character fourth-moment/product-collision orthogonality;
- even-character `+/-` product-collision orthogonality;
- the atomic diagonal inequality `ell/c^2 <= 1/c`.

It does not authenticate `DAPRO106110`, `DAKUM106110`, `BCI102990`, the fixed
Mellin consumer, or the Riemann Hypothesis.
