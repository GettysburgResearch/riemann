# X-92910 upstream-spine reconstruction

```bash
python3 verify.py --mutations --output results/verification.json
```

The replay recomputes the compact Hall/profile inputs through resident directed verifiers, checks the explicit martingale quantizer identities, the `Y_4` recurrence, all constants, required paths, and fail-closed mutations. It authenticates finite/directed interfaces only; independent review of the analytic proofs remains mandatory.
