# B1 — The Levinson converse-Rolle machine, quantified end-to-end

Lane: GRAND/final/B1. Date 2026-08-23. RH is unproved and NOT addressed.
Machine checks: rate_check.py, census_check.py (this dir). Sources read:
T-105060, L-105061/62/63/64/66/67, T-105065, CLAIM_LEDGER.tsv:50, X-105061.

## 0. Verdict in one line

The chain closes ARITHMETICALLY but not USEFULLY: the first end-to-end
explicit constant is `1 - kappa_0 <= 4.5692` (vacuous vs. its own input
0.3275), `Sum_k w_k <= 1.446` is newly explicit (discharging T-105060(d)'s
hypothesis), and the rung-0 self-reference (blocker iii) is PROVED irreducible
for the pointwise-compression method: theta_0 >= 8/5 > 1 for every constant
assignment consistent with the deposited sharpness certificates.

## 1. The classical input, pinned (task item i)

**Conrey I** [J. B. Conrey, "Zeros of derivatives of Riemann's xi-function on
the critical line", J. Number Theory 16 (1983) 49-74; PDF read in-lane,
conrey_I_full.txt]. Corollary (p. 50): with alpha_m = proportion of zeros of
xi^(m) on Re s = 1/2 (short-window liminf, U = T/L^10),
```
alpha_0 > 0.3658, alpha_1 > 0.8137, alpha_2 > 0.9584,
alpha_3 > 0.9873, alpha_4 > 0.9948, alpha_5 > 0.9970,
and  alpha_m = 1 + O(m^{-2})  as m -> infinity.
```
The rate is **m^{-2}**, NOT m^{-1}: the paper's final page (p. 73) proves
`log F_m(1) <= m^{-2}` for large m via Phi = 1/(4m) - 1/(8m^2) + ...,
A^2 ~ m^2, F_m(1) = 1 + O(m^{-2}), and alpha_m >= 1 - log F_m(1).
Machine verification (rate_check.py §1, mpmath dps 40, phi = 1-x, R = 1):
m^2 log F_m(1) = 0.289, 0.376, 0.435, 0.475, 0.503, 0.513, 0.522, 0.523 at
m = 2, 5, 10, 20, 50, 100, 500, 1000 — increasing, < 0.53 <= 1 throughout
(numerically ~ pi/6 = 0.5236; not proved). m_0 in "large m" is unspecified:
tail constants below carry C_Conrey = 1 as a transcription flag (FAILURES F6).
**Conrey II** [JNT 17 (1983) 71-75, read in-lane]: on-line AND simple:
beta_1 > 0.7869, beta_2 > 0.9314, beta_3 > 0.9666, beta_4 > 0.9799,
beta_5 > 0.9863, beta_m = 1 + O(m^{-1}); also the 0.3485 simple-zeros value.
Not needed below (alpha dominates). Repo import status: T-105060 consumed
only the QUALITATIVE kappa_k -> 1; the numerics above are new to the repo,
transcribed literature-unverified in-container (same discipline as Q-0014).
Window-to-cumulative: Conrey's short-window bound implies the cumulative
liminf by disjoint-window additivity — the same (easier) bridge as the
deposited Z23 argument, L-105062 §6. Hence `kappa_k >= alpha_k` and
`kappa_k >= 0.6725007...` (Z23, modulo Z23-UPSTREAM's own review status).

## 2. Theorem Q1 (Sum w_k is explicitly finite — (d)'s hypothesis discharged)

Modulo: W4-W6 (L-105064, proved), Conrey I values + rate (transcribed, with
the m_0 flag), and — variant A only — the Z23-UPSTREAM import row:
```
Variant A (with Z23):     Sum_{k>=0} w_k <= 1.4457   (census_check.py)
Variant B (import-free):  Sum_{k>=0} w_k <= 3.3158
```
via w_k <= (3/(2 c_k))(1 - c_k), c_k = max(alpha_k, 0.6725 [A only]), and for
k >= 6: c_k = 1 - C_Conrey/k^2, tail = (3/2) Sum_{k>=6} 1/(k^2-1) = 0.275.
This is the first explicit numeric bound on Sum w_k in the program: it makes
T-105060(d) a NON-VACUOUS inequality. It does NOT yield a kappa_0 bound (§4).

## 3. Theorem Q2 (the end-to-end explicit constant — honest and vacuous)

Conditional on D_res = 0, where D_res := limsup_T (1/N_0) Sum_{k<=K}
Sum_{residual gaps G of Xi_k} [extra(G) - A'_k W(G)]_+ (regimes R-C1'/R-C2 of
L-105063 §4 / L-105067) — numeric evidence: X-105061 census, T0 = 500, defect
ledger X_0 = X_1 = X_2 = 0, R = 269/269/270/269, no non-real pair observed,
so D_res-contribution = 0 and W_k = 0 for T <= 500 (census_check.py):
```
1 - kappa_0 <= (1 - c_{K+1}) + Sum_{k<=K} max(6,2k) (3/(2 c_k)) (1 - c_k)
            =  4.5692  at the optimum K = 0   (rate_check.py §2a)
```
(K = 1: 6.485; K = 4: 7.016; K -> inf DIVERGES, §5.) Comparison demanded by
the task: 4.5692 > 0.3275 = 1 - 0.6725: the machine's own output is 13.95x
WORSE than the trivial bound riding its input — NOT a new record proportion;
no beating of Zeta23. Import-free variant: 15.79 vs trivial 0.6342. Even at
the forced-minimal constants (§4) the K=0 bound is 0.9655 — still 2.95x above
trivial. These are the first explicit end-to-end constants for this machine.

## 4. Theorem Q3 (blocker (iii) is the irreducible one — proof)

Claim: for the pointwise-compression method — any pricing chain of the form
(P1) per-pair pointwise weight omega(p) <= C_omega (L-105064 W2-type),
(P2) per-gap count cap extra <= A' W (L-105061/63-type),
(P3) proportion inputs kappa_k >= c_k <= 1 —
the master's rung-0 coefficient is theta_0 = A' C_omega/(2 c_0), and the
deposited certificates force
```
C_omega >= 3  (L-105064 §2: sup_p omega(p) = 3, sharpness family realized),
A'      >= 16/15 (L-105063 §3: extra/W = 16/15 extremal, 40-dps replay),
c_0     <= 1,
hence  theta_0 >= (3/2)(16/15) = 8/5 > 1.
```
So the self-improvement restructure (1 - theta_0)(1 - kappa_0) <= tail has a
NEGATIVE left coefficient for EVERY admissible constant set: the fixed point
is vacuous; no constant improvement anywhere in the chain can close rung 0.
Closure would need A' C_omega/2 < c_0 <= 1, i.e. A' C_omega < 2, while the
certificates force A' C_omega >= 16/5 = 3.2. Gap factor: >= 1.6 at the floor,
13.4 at the proved constants (rate_check.py §2b). Moreover (rate_check §2c),
even deleting rung 0 by fiat, at any truncation K >= 1 the k = 1 term alone
is >= 0.3663 > 0.3275 at the best published alpha_1 = 0.8137 (nonvacuity of
that single term would need alpha_1 > 0.8301, not in the literature).
[Review correction K1: at K = 0 with rung 0 deleted no k = 1 term exists and
the output would be (1 - c_1) = 0.1863 — but that route is a fantasy; the
deposited K = 0 chain runs through theta_0 and yields >= 0.9655. Combining
the two cases: the method cannot beat the trivial baseline at ANY truncation
and ANY constants consistent with the deposits.] The only
exits are distributional: average-omega smallness (L-105064 W2'/W7: needs
hypothetical off-line zeros predominantly HIGH, y_p >> local gaps) or the
near-line repulsion [H3] = PR(theta,eps) of T-105065. Blockers (ii) and (iv)
are real but subordinate: fixing either leaves theta_0 >= 8/5 (FAILURES F2-F5).

## 5. Theorem Q4 (tail obstruction at the published rate)

With the deposited regime caps A'_k = max(6, 2k) (2k growth is real: the C-3
depth example, L-105063 §1) and the pinned rate 1 - kappa_k = O(k^{-2}), the
master majorant's k-th term is ~ 2k * (3/2) C/k^2 = 3C/k: the series DIVERGES
harmonically ((3C/c) log K; rate_check §2e), so T-105060(d) with per-rung caps
is unusable and truncation at finite K is forced (optimum K = 0, §3).
Convergence requires 1 - kappa_k = O(k^{-2-delta}) — no such rate is
published, and Conrey's own F_m(1) is intrinsically >~ 0.5/m^2 (§1 numerics),
so his mollifier at R = 1 cannot supply it — or a sublinear cap
A'_k = O(k^{1-delta}) on the shallow ladder (open). Note Sum w_k itself
CONVERGES (§2): divergence is created exactly by the A'_k weights.

## 6. Inventory

conrey_I_full.txt / conrey_II_full.txt (extracted papers), rate_check.py
(all constants; sections 1, 2a-2g), census_check.py (census cross-check +
Sum w_k + asymptote), PLAN.md, FAILURES.md (F1-F7, each a refuted route),
B1.md (this file). Nothing committed; branch untouched.
