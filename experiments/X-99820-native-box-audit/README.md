# X-99820 native-box normalization and Hardy-tail replay

The checker authenticates:

- exact labelled-source coefficients after normalized prime steps;
- the `1/p` versus `1/sqrt(p)` mutation firewall;
- the symbolic half-order collar coefficient;
- the exact discrete Hardy factorization of `min(m,n)^(2 tau)` at `2 tau=1`;
- fail-closed theorem status.

It does not prove the global native-window negative-mass estimate or RH.
