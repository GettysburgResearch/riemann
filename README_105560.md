# T-105560 — Reviewed unconditional simple-zero improvement

This packet converts the proposed stability refinement into a source-qualified
unconditional theorem.

## Exact result

Let

\[
H_0=\frac32-\frac1{\sqrt2}\cot\frac1{\sqrt2}.
\]

Then

\[
\boxed{
\liminf_{T\to\infty}
\frac{N_0^s(T,2T)}{N(T,2T)}
\ge
\frac{1\,345\,000H_0-2\,680}{1\,340\,003}
=0.673008527927557\ldots .
}
\]

The exact rational enclosure in the replay proves the lower bound is larger
than `673/1000`.

## What is new

1. The multiplicity-aware formal theorem is source-locked to the correct file
   `Zeta23/ThmD/Mult.lean`; the similarly valued `Final.lean` theorem concerns
   a different zero-count object.
2. The positive defect discarded in the original rank--inertia proof is
   retained exactly.
3. A machine-free sum-free theorem for the optimized overlap kernel proves a
   strict unconditional improvement even without the finite certificate.
4. The stronger explicit value uses the pinned seven-gap interval theorem at
   `ainta/zeta-simple-zeros@040c5e8`.

The finite certificate was not independently rerun by this connector session;
its verifier source, rounding policy, tree, and retained certificate were
audited and pinned exactly.

```text
strict improvement over H0                   PROVED ANALYTICALLY
67.3008527927% explicit value                 PROVED / COMPUTER-ASSISTED
independent second replay                     NOT COMPLETED
ninety percent                                UNPROVED
Riemann Hypothesis                            UNPROVED
```
