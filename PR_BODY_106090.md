## Purpose

This remains the single persistent execution PR joining:

- **#743** — CV/XD common mother and physical restriction;
- **#736** — Dirichlet-character/L-family completion;
- **#737** — function-field mirror.

The branch is now synchronized scientifically to parent PR #719 at
`ec6635b4c7dcd08fe433b7ae7e1d9a8c9495dfcc`.

**The Riemann Hypothesis remains unproved.**

## Binding corrections retained

The earlier `T-106070` scale-matched closure remains retracted by
`R-106071`.  The former `T-106080` minimum-owner closure remains retracted by
`R-106080`.

Their valid reductions are retained, but neither may be cited as an RH proof.

## Parent repairs now inherited

The current parent proves:

```text
critical Hodge class = native squarefree Boolean source;
Boolean Type-I global transport;
equal- and one-sided-core absolute closure;
exceptional balanced-owner elimination;
literal physical-product phase packing;
arbitrary incidence-mask stability;
canonical equal-pair owner gauge;
owner/phase gauge decoupling.
```

The clean inherited frontier is `BCI102990`.

## New T-106090 repair

After exact common-core extraction, every live pair is

```text
N = P g^2 c^2;
M = Q g^2 d^2;
c,d > 1;
(c,d)=1.
```

Orient it by the smaller of `P^-(c)` and `P^-(d)`.  If

```text
ell=P^-(c)<P^-(d),
```

then:

```text
ell divides N and not M;
the opposite reduced core is strictly ell-rough;
1=-sum_(h=1)^(ell-1) e_ell(-hM).
```

This gives one unique triangular anchor/rough-tail coordinate for every
coprime two-sided interaction.

For one fixed fibre, the rough tail has an exact even-character family and is
automatically long-core.  The local conductor, ramification and family-size
costs are all paid.

## Anchor amplification before squaring

A fixed-anchor Cauchy step would duplicate the opposite tail.  `L-106093`
instead applies Mellin polarization first and forms

```text
Z_(g,ell,Q,sigma,h)(t)
 = sum_anchor conjugate(A_anchor(t)) B_(anchor,h)(t)
```

before the family square.

The exact family identity is

```text
sum_(h!=0)|Z_h|^2
 = (ell+1)/(ell-1)|Z_prin|^2
   + 2ell/(ell-1) sum_(eta even, eta!=1)|Z_eta|^2.
```

The natural source-dual moment has weight

```text
g^2 * ell * Q.
```

Its subpower bound implies `BCI102990` and hence RH.  `L-106094` already pays
the entire same-anchor diagonal.  Only distinct-anchor correlations remain.

## Exact live gates

```text
LDRPCX106090:
  distinct-anchor principal rough-tail moment is subpower;

LDRNEX106090:
  distinct-anchor nonprincipal even-character moment is subpower.
```

The exact implication is

```text
LDRPCX106090
AND LDRNEX106090
 -> LDRTM106090
 -> LDART106090
 -> BCI102990
 -> RH.
```

Both first premises remain open.

## Function-field target

`FFLDRT106090` asks for the corresponding anchor-amplified
Artin--Schreier/Kummer rough-tail moment over `F_q[T]`, including explicit
constant/resonant strata and an exportable number-field trace or
exponential-sum theorem.

## New packet

```text
L-106090  least-discrepancy triangular incidence;
L-106091  exact rough-tail even-character family;
L-106092  fixed-fibre automatic long-core estimate;
L-106093  anchor-amplified Mellin--Gauss normal form;
L-106094  same-anchor diagonal is subpower;
R-106090  fixed-fibre-to-global coherence firewall;
T-106090  corrected L-family frontier;
X-106090  finite exact replay.
```

Replay:

```text
PASS_X_106090_LEAST_DISCREPANCY_ROUGH_TAIL
checks=142924
```

The replay authenticates finite incidence, Ramanujan, character-kernel,
long-core and diagonal-weight algebra.  It does not prove the distinct-anchor
moment, `BCI102990`, the Mellin consumer or RH.
