# Supplement execution boundary

The seven-point inequality was recomputed three times: ordinary mode, optimized
mode and a second ordinary run. Each processed 713315 nodes. All mathematical
result fields and both table/traversal hashes agree; only elapsed time differs.
The final optimized and repeated ordinary processes have retained exit-code-zero
files. The first ordinary run has its terminal PASS and complete result but no
separate exit-code file; it is not the sole execution evidence.

The independent backend uses 128-bit outward integer dyadics for transcendental
primitives and accepting Hessian LDL, with conservatively widened binary64
range minima and nonnegative accumulation. Its CPython floating contract is
recorded in EXECUTION_RECEIPT.json. No Arb or FLINT was installed. A failed
attempt to install python-flint was not replaced by ordinary mpmath signs.

The uncached predecessor was interrupted without a verdict. Caching only exact
point derivatives left all box arithmetic unchanged and made the full search
finish. This is a proof-producing finite continuum computation, not a mesh
sign survey. Original ainta Arb hashes are not claimed reproduced.

The original rank-two checker remains false/failing as recorded in the parent.
The corrected invariant-cone script now completes under -O with exit zero and
its polynomial certificate is byte-identical to the parent's ordinary result.
One extra container-supervised run timed out after PASS without an exit receipt;
only the final recorded exit-zero execution is used for that claim.

1769 finite rational/interval controls and 23 selector/spectral controls pass in
both modes with byte-identical result files. These do not prove the imported
Conrey analytic functional, the zeta trace/mean-square inputs, or a Lean theorem.
No Lean, Comparator, Nanoda, original-PDF page audit, large physical-integral
campaign or actual-Xi zero scan was executed. The scope is the named supplement.

The parent file manifest is regenerated to cover the complete current report
package. The new validator also checks the composite source/claim/edge join.
Mutations test package rejection; they are not proofs that every possible bug
or hostile producer is excluded. See VALIDATOR_RECEIPT.json.
