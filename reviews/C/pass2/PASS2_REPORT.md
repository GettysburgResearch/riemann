# Reviewer C second pass — executive handoff

**Main result:** the 312 missing historical head comparisons are completed, and a concrete formal statement-fidelity defect was discovered: the actual-Xi headline input type is empty at the frozen baseline.

**Publication:** this packet is prepared for PR #798 but not published remotely. The exact existing head remains `7466ad8081101508be7c7acf0065cb2e0944a639`; current GitHub tooling exposes no writes. The patch is confined to `reviews/C/`, with seven first-pass files updated and their exact original bytes preserved. See `PUBLICATION.json` and the downloadable patch application receipt.

## Completed independent coverage

Historical sources: **340/340 real PRs**, comprising 312 fresh observations and 28 inherited first-pass observations. **338 same heads; #568 and #599 different**. Both different head commits predate the release, so source selection—not a post-release mutation—is the correct diagnosis.

#568: two replay paths, old/new source bytes authenticated, equal generated output, retained JSON formatting difference only. No claim-file change or new primitive interval replay.

#599: fourteen added paths; thirteen text files read, four mathematical claim texts independently analyzed, toy output reproduced exactly, and the write-enabled publisher inspected but not run. Its PDF, archive and transitive analytic proof inputs remain unreviewed.

Formal catalog: **31 canonical rows + six API mappings; 34 declaration-bearing entries; 33 distinct declarations in 15 defining modules**. Every listed declaration and body was read. Three canonical rows are open producers without declarations. Full transitive imports, uncataloged declarations and fresh compilation are not covered by that count.

## Most important scientific finding

At the baseline, the shared normalization is the raw totalized product `(1/2) s(s−1) completedRiemannZeta(s)`. Thus its value at `s=1` is zero, its centered value at `x=1/2` is zero, and the node logarithmic-derivative quotient is zero by totalized division. But every actual-Xi input instance requires this node to be strictly positive. Therefore no such instance exists.

Five canonical actual-Xi entries have a source-level nonvacuity failure. The finite algebra and local orbit absorption remain independently scoped. An exact comparator match and a valid Lean proof of the conditional type would not cure empty hypotheses.

A pinned Mathlib identity suggests the correct entire normalization `1/2 + s(s−1)Λ₀(s)/2`. The representation also needs empty/finite off-line orbit cases; an injective natural-number enumeration cannot supply them. The full argument and affected declarations are in `FORMAL_NONVACUITY.md`; the Lean regression is **uncompiled**.

## Reproducibility findings

Six author-script runs reproduce the declared old/new artifacts. Three deliberately corrupted scripts reject normally but emit PASS under `python -O` (six mutation runs). The baseline tactic-query regex misses ten ordinary tactic queries and flags one neutral `apply?x` fixture; a candidate handles the fifteen bounded cases (thirty grep runs). An independent exact finite-algebra checker validates formal-variable ownership, fixed 5:3 factorization and an adverse sign fixture.

The inventory validator is fail-closed in ordinary and optimized Python, verifies its declared evidence boundaries and rejects malformed fixtures. It explicitly does not certify scientific, remote-authentication, all-ref or build completeness.

## Remaining obligations and integration

The completed 340-head denominator is not exhaustive proof review of those PRs or full postcut coverage. All postcut claim splits, branch-only and attachment/transitive closure, full mutable discussion archives, complete formal dependency/build coverage and owner rights/history/access review remain incomplete. No unpublished A/B output was required; their scientific dispositions remain **pending integrator reconciliation**.

The integrator's first narrow task is to reconcile the exact shared Xi source and any independently reviewed repair, then trace its consumers. #599's final packet and all later correction chains need source-qualified reconciliation. Public-release readiness remains **not cleared**.
