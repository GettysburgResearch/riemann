# Reviewer C: selective extraction and repair order

Use `pass3/RETRY_REPORT.md` as the current front door; preserve the prior four reports under `pass3/evidence/PRE_RETRY_*.md`. Keep the existing census source pins and all preceding research bytes unchanged.

First reconcile the observed publication lineage: parent cd8a172c contains seven pass3 files atop e0422804; the old prose's d7a47580 head was not resolved by the retry. The new replay scripts and receipts complete the bounded arithmetic/contract packet, not the exhaustive review allotment.

For X-105560, extract the independent exact scalar calculation and the wrapper-defect evidence together. Rebuild the actual author wrapper, hashes, retained JSON and displayed decimal on an authorized research successor; do not overwrite its frozen past. Keep the external Arb certificate and analytic 269-offset deduction as separate review inputs. Check downstream quoted records at their own source pins.

For actual Xi, apply `pass3/FORMAL_REPAIR_CONTRACT.md` to the shared definitions and every consumer before any formal promotion. Empty/finite off-line spectra, removable points and analytic domain changes require explicit tests. Metadata locks and a successful comparator self-match do not establish nonvacuity.

For manifest closure, require an independent source-root denominator and strict recognized binding schemas before using a PASS as coverage. Retain the old graph's narrower claims. For the majorant, extract only the verified finite ANOVA/matrix ledger; do not promote the declared one-cell subpacket into a complete native fibre.

A at e44b6d072ac82d014717454635df96da9c0e115f and B at 0ffd6bfee7160c424e15578405498fdc52b9fec1 were observed as published review heads. Their scientific dispositions remain pending integrator reconciliation. C does not independently accept its own #792 research. No merge, canonical promotion, formal-source edit or owner-only release action is included.

The preceding detailed extraction inventory is preserved at `pass3/evidence/PRE_RETRY_STRUCTURE_AND_EXTRACTION.md`. Current omissions remain explicit in `pass3/COVERAGE.tsv` and `RELEASE_BLOCKERS.md`.
