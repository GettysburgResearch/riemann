# Extraction plan: new repair identities, never rewritten history

## Highest-priority source repairs

1. **Actual-Xi function and indices.** Preserve the old contradictory definition
   and its source-qualified review. Create a new repair identity for X1/X2. Use
   the pinned Mathlib declaration body, not its inconsistent overview comment.
   All shared ChallengeDeps, Solution and actual consumers must refer to the same
   corrected entire function and multiplicity-complete, empty/finite-capable
   spectrum. The candidate `lean/EntireXiRepair.lean` is not an authorized trusted
   edit and is not compiled.
2. **Assemble rather than assume.** Formalize X3-X8 to construct the numerical
   budget, paid prefixes, C2 source identity and scalar curvature from explicit
   actual spectral assumptions. Keep the complete zero-source binding, all-height
   count and low critical reserve as named dependencies. Do not assign Nonempty
   to the old input or hide source completeness in an arbitrary enumeration.
3. **Mellin analytic library.** M2-M6 are ready as paper-level proof specifications
   for the two missing analytic propositions and their adapters. The punctured
   singularity predicate and M7's minus-infinite-abscissa variant need new IDs.
   Keep the old correct conditional assemblies at their stated narrower scope.
   Revisit the removable reciprocal value at zeta's pole when defining actual
   detector continuations.

## Proposed destinations (not files modified by this review)

| Repair/extract | Suggested destination after independent review |
|---|---|
| Entire xi normalization and off-exception derivative agreement | New formal analytic normalization module and matching shared challenge definition |
| Countable complete source, finite exhaustion, one-use selected reserve | New formal actual-Xi input contract, source-binding theorem and regressions |
| M2-M6 | Formal Mellin/meromorphic library lemmas, replacing arguments only after compilation |
| M7 | New conditional consumer statement with explicit native arithmetic node |
| X3-X8 | Separate reviewed source-specific low-order Pick packet; no order-four promotion |
| O1-O4 | Scoped #792 finite-window coercivity and energy-Schur extract, exact two source blobs retained |
| Synthetic spectrum and compact-Schur controls | Exact refutation/regression appendix, clearly not actual zeros or full kernel sign |

All proposals need explicit new semantic identities chosen by the integrator;
C4 labels are review-local. Do not recycle an existing T/L ID with a stronger
analytic domain or a different definition.

## Former-A and specialist handoff

The predecessor A package and A-authored #793/Architecture-E proofs do not receive
an independent scientific acceptance in this C role. B's automorphic, Segre/Chow,
partial-Frobenius, and full #765 transport questions remain specialist handoffs.
The current #792 reconstruction is confined to its two named proof files and
local operator consequences; no global trace identity or sampled continuum sign
is automatically accepted.

The integrator should join rows by exact source SHA/path/claim, not by short IDs
alone, since historical claim IDs can collide across research branches. Source
updates after the inspected pins need delta review. This packet changes no
canonical or formal acceptance status by itself.
