# Reviewer C: completed bounded retry and explicit remaining holds

Status: completed bounded computational/provenance continuation; exhaustive C allotment incomplete.
Scientific baseline: `8d16f8d9c475db290bc85e53d775b93b9bcdb336`.
Publication parent: `cd8a172c24aaf61fd293b32c5a396334110f0565`.
Scope: independent wrapper/contract replays, actual-Xi statement fidelity, publication repair, and live release-enforcement observations. No RH proof, blanket scientific acceptance, formal build or public-launch clearance.

## 1. Publication recovered, rather than assumed

Fresh PR metadata and the direct branch ref both returned `cd8a172c24aaf61fd293b32c5a396334110f0565`. A direct comparison with `e0422804f098a296aaf2ede615a833d90704a18f` showed one commit containing seven added files, exclusively in `reviews/C/pass3/`. The new code below completes the missing replay/receipt files referenced by the existing `RECORD_ARITHMETIC.md`.

The old PR description instead claimed final head `d7a47580d98c7ebeb6529e76620fdd3a50c91b1d`. Its commit endpoint returned 404 in this retry. This is a failed resolution, not proof that the object never existed. Neither that claimed head nor the earlier conversational claim that only `7466ad...` had been published is an accurate description of the observed branch.

The four previous C front-door reports still said the second pass was prepared but unpushed. They are preserved byte-for-byte as `evidence/PRE_RETRY_*.md`. Their replacements describe this observed publication and its actual limits. The historical census and the entire preceding mathematical research are unchanged.

## 2. X-105560 wrapper: confirmed defects and a valid scalar repair

Source PR #726 at `e8e6d85221a9a85fb2a5f82a1807802f89b06b7b`, producer blob `bd076c05c2e741758286ee3c0d378240c4be884c`, retained JSON blob `41ec02af734f101c0255e200139fa532755847b3`.

The unmodified producer's selected functions run after byte authentication in ordinary and optimized Python. They compute H0+1/2, not H0, and consequently certify a purported proportion lower bound greater than 1. Its claimed alternating remainder is also wrong: the nonconstant H0 terms have one sign. An independent exact Taylor quotient places H0+1/2 strictly above its alleged upper endpoint. Its 50-term lower endpoint also exceeds its 48-term upper endpoint.

The actual loop counts are 1640 and 2592, not the retained 1601 and 2527. The retained producer hash does not match the frozen source. These are defects in the wrapper/retained artifact, not a refutation of the external seven-gap theorem.

For x^2=1/2, enclose C=cos(x) and S=sin(x)/x by adjacent alternating Taylor sums. Since S is positive, H0=3/2-C/S lies in [3/2-C_U/S_L,3/2-C_L/S_U]. Exact rational arithmetic then proves

    0.673008527927779 < (1345000 H0-2680)/1340003 < 0.673008527927780.

The threshold >673/1000 survives. The printed decimal ending 7557 is not the displayed exact formula's decimal expansion. The driver checks six Taylor orders and rejects five invalid order arguments; no floating-point value is used as a bound. Full rational endpoints are recomputed by `corrected(80)`; their deterministic digest and the decimal rational enclosure are retained in the report.

The external 707901-node Arb certificate, the complete sixteen-file author payload, and the analytic seven-window/269-offset adapter were not replayed. This is not a new zero-proportion theorem. A's independently reported hold on the explicit numerical record remains pending integrator reconciliation.

## 3. Manifest closure: integrity is not coverage

The original scanner blob `106e1a300872c78659ba2796b7fb98e08df84ef7` is run unmodified on eleven deterministic, temporary, remote-less Git repositories. All fixtures run in both Python modes: 22 CLI runs and ten successful report roundtrips per driver execution.

Valid explicit and inherited bindings pass. Wrong recognized blob/hash, missing target, duplicate key, floating primitive and unknown implicit binding reject. But zero roots, an empty source list, and a misspelled/unrecognized `sha256` binding all pass with zero recognized edges. In the last fixture the named file does not exist, but the unrecognized record is never treated as an edge.

This establishes a contract boundary, not a missing edge in the real scientific graph. The original documentation expressly excludes all prose/executable dependencies. Its prior independent expected-count regressions are not refuted. An acceptance consumer needs an independently frozen root inventory and a strict schema that rejects unrecognized source-binding forms; a self-consistent empty report cannot certify that inventory.

## 4. Majorant: affirmative bounded verification

Producer blob `77ebc43f92ff21413bd56cda15522f2665825d9f` reconstructs retained output blob `7bef7e8cf814920d81603aee7654d5ae3da8bc47` exactly. An independently formed Kronecker projection checks 1539 rational currents on 2x2, 2x3 and 3x2 grids, in each Python mode. Projection, idempotence, squared norm and ANOVA identity agree.

For d=535600/537151, the declared one-cell majorant is 16d, its positive part is (396/25)d, and its ratio to literal energy is (4/169)d. The source's mathematical claim remains limited to that declared subpacket; no complete native fibre or cross-conductor estimate is inferred.

An out-of-grid key is rejected by `centered_projection` but silently counted by `centered_energy_formula`. This is an input-validation inconsistency outside the declared mathematical domain, not a counterexample to the identity on valid grids. The historical `check_source_blobs` function was not run and is explicitly excluded.

## 5. Formal and release disposition

`FORMAL_REPAIR_CONTRACT.md` records the fresh definition/body reading, the exact `diagonalPositive` field name, and the independently remaining empty/finite-spectrum enumeration defect. The supplied Lean regression remains uncompiled. Existing metadata-lock and dependency-projection lemmas do not construct a nonempty actual-Xi input package.

`OWNER_RELEASE_GATES.md` records private visibility, unprotected main, and a ruleset endpoint denied by the repository feature tier. No access, visibility, workflow or setting was changed.

A at `e44b6d072ac82d014717454635df96da9c0e115f` and B at `0ffd6bfee7160c424e15578405498fdc52b9fec1` are observed published review heads, not scientific acceptances imported by C. Their dispositions require integrator reconciliation. C's prior authorship of #792 excludes an independent C acceptance of that research.

## 6. Completed executions and exclusions

Each of the three new drivers was run normally and under Python -O, with byte-identical reports. Each itself launches both modes of its frozen target. Counts in retained reports describe one driver execution, not distinct theorems; repeating a run does not increase mathematical coverage.

The 566-row census and pass2 consistency validator were also rerun in both modes before replacing the front-door reports. They retain 340 actual historical sources, 338 matches, two differing pins, and 17 rejection fixtures per mode. This was a consistency replay, not a new fetch of 340 heads. The new validator reconstructs that historical four-report view in a temporary copy for repeatability.

Initial record replay startup hit the fixed memory cap through site initialization; isolated `-S` startup repaired the harness without changing the target or increasing the cap. An initial manually copied scanner failed its Git-blob seal and was corrected before execution. These are retained preparation failures, not accepted scientific results.

Still incomplete: exhaustive claim-level splitting of the 79 postcut PRs, branch-only and attachment/transitive closure, complete producer coverage, all uncataloged/upstream formal proofs, fresh Lean/Lake/Comparator/Nanoda builds, all-history secrets/PII and imported license/NOTICE/paper rights, complete live ACL audit, and reconciliation of A/B dispositions. The earlier #599 PDF exclusion remains. The source census is not relabeled to hide these omissions.

## References and replay

Classical series: NIST DLMF 4.19.1, 4.19.2 and 4.19.6, https://dlmf.nist.gov/4.19 . Python optimization semantics: https://docs.python.org/3/using/cmdline.html#cmdoption-O . These references support the series/optimization facts, not the repository-specific verdicts.

From repository root on Linux with Python and Git, run the commands in `../VALIDATION.md`. Scripts do not use repository credentials or dispatch workflows. Reports and hashes certify bounded replay consistency; they are not external authenticity signatures or global proof certificates.
