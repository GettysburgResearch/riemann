# Third-pass closing confirmation and synthesis review

**Review only. No integration or scientific acceptance is performed.** This is an append-only confirmation of the review at `37ea752b07700c7ba76335ba082cb6993caca35f`, not a replacement for it.

## Concurrent publication and exact scope

The session opened PR #827 at `13ac26caae546c8a6481a85b2f61ecc524dbbb27`. While the manuscript reading and independent checks were in progress, that PR advanced to a published third pass at `37ea752b07700c7ba76335ba082cb6993caca35f`. Its report, inspected-file table, remaining-work queue and tree were read before this confirmation. The existing `pass1`, `pass2` and `pass3` directories are preserved, not overwritten or retroactively relabeled.

The principal arguments read here match the 28 `SUBSTANTIVE_MAIN_MANUSCRIPT` rows in the published [pass-three file table](../pass3/FILES.tsv), at exactly its listed commits and blobs. Source-stability was also reread. The additional six synthesis/source-lock files are individually pinned in [SOURCE_REVIEW.json](SOURCE_REVIEW.json). Research manuscripts were read through the connector; this does not assert that all source bytes or a full checkout were acquired locally.

The cumulative **45-packet principal-manuscript coverage** is retained. This confirmation does not create more packets, independent discoveries or extra referee votes. It does not clear every auxiliary file, author program, transitive source, discussion, or later revision. The published third pass's seven-minimum/full-Gram computations and test-method counts are its recorded executions; they were not rerun in this confirmation.

## Additional synthesis dispositions

The full `SYNTHESIS.md` for arithmetic norm transfer, `SYNTHESIS_AND_ATTEMPT.md` for grounding capacity, and `SYNTHESIS.md` for coherent-channel work preserve their mathematical boundaries. Their companion source-lock files explicitly distinguish direct proof reading, metadata orientation and contextual prior work; those labels are not converted into independent acceptance.

- **ANT:** the two stated ending routes preserve either the complete physical residual or the original prime-discrepancy energy. The `Y/log Y` obstruction concerns a coefficient-uniform adapter, not an upper bound for the selected native minimizer. The signed coarse-work cross term and terminal future remain. Its classical `o(Y)` bound is not a fixed power saving.
- **GCP:** the period mean with its Jordan-totient weights is explicitly not the harmonic graph metric. The exact root coupling is only the specified coupling; it is not asserted to equal the full Xi/Weil coupling. The source-domain defect remains an unpaid premise, not a consequence of the gap.
- **CCS:** its squarefree root-Green/secular/asymptotic result overlaps GCP and should be extracted once, crediting both sources. The geometric reservoir and root-shaped Schur test belong to GCP; the literal stopped-Mobius coherent projection, complete edge-energy asymptotic and survival law are separately retained CCS additions. The source's unknown coherent function channel is not discarded.

**Recommendation:** the narrative connecting these exact components is consistent at this inspected scope. The three synthesis-manuscript items in the first P0 auxiliary row of [RESUME](../pass3/RESUME.md) are now read. That does not complete their full source-lock ancestry, application packages, or mathematical upper bounds. No new graph-to-physical implication is approved.

The smaller extraction qualifications in the published report remain necessary: use RC equation (18) for its tight numerical brackets; distinguish BL energy from unsquared norm; specify TE's interval `(0,1/M)` when M is only a support bound. RN's two-jet and RC's one-jet feasible classes must not be merged.

## Independently executed corroboration

[checks.py](checks.py) imports no repository producer. It reconstructs **6,770 bounded exact/directed assertions**, with the full output in [result.json](result.json). Normal and optimized Python runs are byte-identical. This count overlaps the published pass-three evidence and must not be added to it as if every case were distinct.

The checks include the entire 4,095-cell norm-transfer profile plus its infinite-tail upper bound; both 63-subset P13 capacity calculations with directed secular endpoint signs; all three zero-jet constraints of the norm-transfer variation; complete stopped-future/coherent-work identities through N=128; the actual signed annular scalar at m=2; four continuum endpoint budgets; independently enclosed C0 and the complete height-tail constant; the exact four-state Lyapunov identity and positive W3 counterexample; finite support duals; rational balanced-source KKT equations; and terminal/hyperbola identities and both signed countermodels.

The complete profile is enclosed by `[1.7042194500368407, 1.7042194985432249]`. The P13 contrast/anchored intervals reproduce the authors' advertised brackets in both reservoirs. The positive work value at cutoff three is in `[0.0596187785377326, 0.0596187785377327]`. These are finite statements, not an asymptotic upper estimate or a zeta-zero computation.

The supporting external-source reads keep their original hypotheses: qualitative unconditional Mertens cancellation, classical critical-line convexity, the published finite-height verification through 3*10^12, and the explicitly zero-free/RH-conditional approximation inputs. PDF formula/statement pages were visually checked where used. No external numerical campaign or original Balazard-Saias proof was independently replayed.

To reproduce this fixed computation from this directory:

```sh
python3 -I -S -B checks.py > /tmp/riemann-confirm-normal.json
python3 -I -S -B -O checks.py > /tmp/riemann-confirm-optimized.json
cmp result.json /tmp/riemann-confirm-normal.json
cmp /tmp/riemann-confirm-normal.json /tmp/riemann-confirm-optimized.json
```

This program regenerates a fixed result; it is not an accepting parser for an arbitrary author certificate. Its arithmetic/source scope and exact executable/result hashes are in [EXECUTION.json](EXECUTION.json). No author package, new full-checkout validation, Lean build, native Windows run or CI execution is claimed.

## Moving branches and the remaining pass

The closing matching-ref read returned all fifteen watched research heads unchanged, including the branch-only spectral classification. Main remained `f99d9e3908dde4865377c75d9ca051c1f545bf4f`. [CLOSING.json](CLOSING.json) records those observed endpoints and the review-head change. This is not a globally atomic snapshot or an all-history discussion/branch census. Compare the two exact endpoint trees after later pushes; unchanged files retain only their earlier component scope.

The next pass should finish the remaining certificate and package dispositions: fixed-horizon ordinary-input realization and the older ramp/cutoff trials, square-grid Li_2 prefixes, and selected author source/CLI/rejection contracts. It should use existing full numerical replays rather than inflate counts by repeating them. The principal-paper queue is no longer the bottleneck. Each remaining object can be cleared at precise scope, retained as exploratory, or held with a named missing check; solving RH is not a precondition for an honest disposition.

Only afterward prepare a separate integration branch from then-current main, preserving the cumulative public account and contributor changes. Review and validate that exact integration tree before a separately authorized merge. Do not merge the review branch as a shortcut to accepting all research.
