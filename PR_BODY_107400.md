## Xi endpoint-31 source-Pick programme

This durable draft continues programme #744 from exact PR #731 head
`6d440eb6c82d989c046e87e13f9bdabf087f920f`, importing the reviewed actual-Xi
concentration theorem from PR #765 head
`a30276a5be049749ebb2147f30f000dd5659298b`.

**More than \(90\%\), density one, and RH remain unproved.**

### New unconditional results

```text
Conrey alpha_31 > 999/1000;
all fixed odd endpoint source constants = 1/(2K);
K=31 source constant = 1/62;
physical frozen carrier is centrally adapted;
radial Hankel weights transfer exactly by Hardy averaging;
the available physical error margin is 2569/31000.
```

### Exact conclusion gate

```text
XI31TRANSFER107400:
  full physical endpoint-31 Hankel/source-Pick charge
  <= (1/62 + delta + o(1)) N,
  delta < 2569/31000.
```

Then more than \(90\%\) of zeros lie on the critical line.

The attempted complete proof stops at the forced unit topological spectrum.
`R-107400` proves that diagonal source softening and the repaired Hardy transfer
cannot remove that block. A genuinely Xi-specific physical source-to-index
theorem is still required.

Replay:

```text
PASS_X_107400_ENDPOINT31_SOURCE_PICK_REDUCTION
```
