# T-105550 — Companion partial-index and separated-defect frontier

This packet replaces the remaining contour-flux black box by an exact topological
coordinate.  For a real polynomial, after cancelling the common factor of `p`
and `p'`, the zeros of `p'+i delta p` in the two half-planes satisfy

```text
N_plus + N_minus = number of distinct roots of p;
N_plus - N_minus = number of distinct real roots of p.
```

The same identity localizes to regular entire-function windows by the argument
principle and the literal endpoint ledger.  Equivalently, the real-root count is
the winding/Toeplitz index of the companion Cayley ratio.

A second theorem identifies the exact missing quantitative interface.  For a
finite Blaschke quotient `u=B_plus/B_minus`, the bad-half-plane count is
`rank H_u=deg B_minus`.  If the denominator kernels are uniformly conditioned
and the numerator is uniformly nonzero at them, this rank is bounded by the
Hankel Hilbert--Schmidt energy.  Without those hypotheses the statement is
false: a one-zero/one-pole Blaschke dipole has rank one while its Hankel norm
can be arbitrarily small.

Thus the T-105540 signed matrix flux is topologically a partial-index problem,
not an unsigned norm problem.  The exact remaining input is a direct winding
estimate or a source-owned separation theorem for the near-axis companion
modes.

**Ninety percent and RH remain unproved.**
