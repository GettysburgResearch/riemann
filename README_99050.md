# T-99050 — Expand first, score last

This add-only successor to PR #620 removes the remaining artificial arithmetic
score debt from the direct-integral score-free Hall candidate.

The actual rough-prime causal tree is finite. Every edge is an exact positive
physical-row identity. Therefore literal entropy is preserved exactly when it
is evaluated once after the complete tree expansion.

```text
arithmetic recursion score loss   0
fixed top omission               <60
one common thinning              <792
total root-owned score loss      <852
```

RH remains unproved pending hostile reconstruction of the inherited endpoint
frame, all-column comparison, and Mellin--Landau consumer.
