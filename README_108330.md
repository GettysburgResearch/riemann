# T-108330 — decisive disposition of the local Xi-source descent

The exact endpoint-companion and winding identities on this branch remain
valid. This checkpoint resolves whether the presently proved local analytic
package can estimate that winding.

It cannot.

For every `M>=1`, T-108330 constructs a strictly positive smooth Fourier
kernel that:

```text
agrees exactly with the Xi kernel outside a compact set;
retains the complete high-derivative saddle and growing real-zero entry;
retains every fixed-odd carrier-adapted source limit 1/(2K);
has no parent zero on one interval;
has at least M wrong-sign critical points there.
```

Thus the route

```text
positive Xi-tail source + saddle concentration + carrier constants
  -> endpoint-31 minority phase or winding
```

is refuted. The live conclusion coordinate remains
`XI31WIND108320`, but its proof must use a genuinely global Xi mechanism:
the safe-line prime phase, the functional equation and a complete argument-
principle boundary ledger, or another input not invariant under compact
positive source perturbation.

Replay:

```bash
python3 -B experiments/X-108330-arbitrary-defect-firewall/verify.py
```

```text
PASS_T108330_ARBITRARY_DEFECT_THETA_TAIL_FIREWALL
47dcd306f38d61e63ff5e9e779c3db870ed0dd08542909ab630043241e30b8db
```

```text
current local-source closure route   RETIRED BY EXACT NO-GO
XI31WIND108320                       OPEN / REQUIRES GLOBAL ARITHMETIC PHASE
more than 90 percent                 UNPROVED
RH                                   UNPROVED
```
