# T-99980/T-99990 — Two-route closure assault

This packet continues the canonical scalar research graph from PR #668 while
treating PRs #665, #670, and #671 as binding sibling audits.

Two genuinely different routes are pursued:

1. **Quadratic-envelope route.** Replace the critical sign by the one-sided
   upper-envelope theorem
   \[
   \mathfrak H_2(X)\le
   16\frac{1-67^{-3/2}}{\zeta(3/2)}X.
   \]
   Its exact finite activation gate is isolated after proving every one-prime,
   two-prime, and fully coactive block.

2. **Phase-Hasse route.** Average the exact priority flow over all prime orders,
   derive its closed product symbol, and prove that the potential boundary
   annihilates the neutral phase identically. The remaining cross-core theorem
   is a nonzero-phase embedding, not the source-blind residual refuted in PR
   #670 and not either root-containing positive square shown RH-equivalent in
   PR #671.

Both routes are conclusion-complete if their displayed final gates are proved.
Neither final gate is proved in this packet. RH remains unproved.
