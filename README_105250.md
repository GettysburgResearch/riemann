# T-105250 — Square-root polynomial Wick hierarchy

This checkpoint on PR #731 imports the exact full-signature consumer and prime-simplex model from PR #726 at `45e416d932818399045ec7d3ba36e9d6efc50f00`, and replaces the isolated degree-two polynomial by an all-degree hierarchy.

For
\[
P_K(x)=\sum_{j=0}^K [x^j]\sqrt{1-x}\,x^j
      =1-\sum_{j=1}^K b_jx^j,
\qquad
b_j=\frac{\binom{2j}{j}}{4^j(2j-1)},
\]
the exact quotient
\[
\frac{P_K(x)^2}{1-x}=1+\sum_{m\ge K+1}q_{K,m}x^m
\]
has nonnegative coefficients. It removes source degrees `1,...,K`, is an invertible coordinate change in every contraction Banach algebra, and has inverse norm at most
\[
c_K^{-1},\qquad c_K=\binom{2K}{K}4^{-K}\asymp\sqrt{\pi K}.
\]

At the frozen prime-simplex model the residual energy
\[
\mathcal D_K=\sum_{m\ge K+1}q_{K,m}^2\frac{m!}{(2m)!}
\]
tends to zero superfactorially. Thus:

- `K=2` recovers and slightly sharpens the conditional 92.10% frontier;
- transfer for every fixed `K` with trace/HS error tending to zero implies density one by taking `K` after the height limit;
- a fixed nonzero transfer floor cannot imply density one.

The safe-line Dirichlet source has an exact contractive half-line shift representation. Polynomial invertibility and omitted-prime stability are therefore closed at source/safe-line scope without nilpotence or a finite projection edge.

The actual Xi contour/taper transfer remains open. Neither 90%, density one, nor RH is asserted.
