# T-105330 — Shifted-xi-prime deformation bridge for the low-order Pick matrix

This checkpoint continues the actual low-order Levinson programme on draft PR
#726.  It identifies the T-105320 Wick-preconditioned Pick matrix as the
parameter derivative of a shifted xi-prime zero statistic.

For

```text
E_alpha(s)=xi'(s)-alpha xi(s),
```

and a regular contour, differentiation of the argument-principle statistic is
exact:

```text
d/dalpha sum_(E_alpha(rho)=0) H(rho)|_(alpha=0)
  = sum_(xi'(rho)=0) H'(rho) xi(rho)/xi''(rho).
```

Taking `H_ij'=-W^2 phi_i phi_j` gives precisely the Wick-preconditioned
low-order Pick entry.  On the safe-line side,

```text
E_alpha'/E_alpha = L + L'/(L-alpha),
```

so the shifted prime coefficients are `C(N;L-alpha)`.  Differentiating at
`alpha=0`, followed by the exact Hardy division `1/log N`, gives the reciprocal
coefficient family already isolated in T-105320.

Thus the coefficient/source part of `GRAMFREEZE105320` is an exact deformation
of the pinned xi-prime explicit formula, not a new explicit formula from
scratch.

The remaining theorem is a uniform **paired shifted explicit formula** on a
complex alpha disk.  A Cauchy estimate must control the derivative of the
error; a pointwise or real-axis `O(T^-delta)` statement is insufficient.
The shifted family obeys

```text
E_alpha(1-s)=-E_(-alpha)(s),
```

so the correct zero-side object is the oriented `+alpha/-alpha` pair, not an
unsupported symmetric ZeroConfig for one shifted field.

No new zero proportion and no RH theorem is claimed.
