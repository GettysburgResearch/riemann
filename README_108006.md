# T-108006 checkpoint

T-108004 found the full symmetric Perron--Fourier cubic cusp.  T-108006
inserts it into the assembled beta source and proves that beta-pair evenness
selects one directed chirality before any absolute value.

Globally,

\[
\int \widehat R_{r,z}(t)
B_\beta(s-it)B_\beta(s+it)\,dt
=
\int J_r(z+2it)
B_\beta(s-it)B_\beta(s+it)\,dt.
\]

Equivalently the source is one reflected vertical convolution in `omega`.
Locally, the symmetric cusp carrier may be replaced inside the beta integral
by

\[
\mathcal D(\lambda,\tau)=\mathcal A(\lambda+2i\tau),
\]

whose inverse Fourier density is exactly one-sided:

\[
H_\lambda^{\leftarrow}(v)=2H_\lambda(v)1_{v<0}.
\]

The exceptional-67 numerator becomes two exact translated copies of this
one-sided density.  For fixed positive `lambda`, those copies decouple
exponentially; this does not settle the boundary `lambda=0` correlation.

The reciprocal-zeta correlation estimate, outer-frequency control, Perron
boundary shift, and RH remain open.  The next gate is
`BETACHIRALCORR108008`.
