## T105416 addendum — anchor-renormalized flow and scalar outer phase

Exact remote state was audited before this deposit:

```text
PR #729 head:
c47b7d5d8917b96e97687cfc8a516e58f523b0ca

state: open / draft / mergeable
```

The key new identity is

```text
(1/2) d_alpha J_(F,Omega)^(a)(q;alpha)|_0
  = q^T S_(k,Omega)^(a) q.
```

Here `J` is the contour winding of

```text
(F'-alpha F)/(F'+alpha F)
```

against the inverse-power primitive generated by the polynomial square
`q(z^-2)^2`. The test pole at the origin contributes the fixed source resource
`q^T A q`; the shifted critical-zero motion contributes `-q^T C q`. In even
parity the moving central zero must be factored out first.

Consequently:

```text
ZCAP105412                         = first oriented-flow test;
all source-critical capacities    = all oriented-flow tests;
OASH105350 / BRP105220             = nonnegative flow for every test.
```

A second exact theorem shows that, after the complete real/nonpositive critical
pole sign is known, the all-packet boundary matrix gate collapses to one scalar
outer-boundary condition:

```text
liminf_outer Im(F/F') >= 0.
```

The harmonic minimum principle makes `F/F'` Pick. Its Herglotz representation
then supplies every boundary Stieltjes matrix and the scalar endpoint mass
automatically. Equivalently,

```text
d_alpha arg M_alpha^sharp|_0=-2 Im(F/F').
```

Thus the remaining boundary producer is one folded safe-line phase velocity,
exactly the current attacked by the oriented-ratio and one-sided Hardy
programmes.

Exact replay:

```text
PASS_X_105416_ANCHOR_ORIENTED_FLOW
91 exact rational checks
b6798c3b111efb1b8f6b90ae5eafb0291e2704bf7712710fb3e604cb175dfdeb
```

Current scientific boundary:

```text
anchor/critical/flow decomposition          PROVED EXACT
signed-pole maximum-principle reduction     PROVED EXACT
terminal moving-saddle phase                PROPOSED / REVIEW REQUIRED
CRVH105330 complete critical sign           OPEN
SOPV105416 low-order outer phase            OPEN / RH-BEARING
Riemann Hypothesis                          UNPROVEN
```
