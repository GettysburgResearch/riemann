# Hostile-review handoff — live Target–Lorenz endpoint marginal

Begin with the exact obstruction, not the conditional endpoint theorem.

## Primary questions

1. Does `L-94020.5` reproduce the literal finite endpoint-divergence
   coefficient used by the frozen endpoint frame?
2. Is every squarefree occurrence assigned the unique factorization
   `k=d p_1...p_t` with `d|P61` and first rough owner `p_1`?
3. Does the generated `(67,13)` leaf contain exactly the 229 active `P_61`
   atoms and nine child-active atoms?
4. Do the `beta/gamma` records come from one global ordered prime list, rather
   than a cloned parent in every leaf?
5. Does the `q=2` negative-coordinate separator invalidate every branchwise
   positive actual-child implementation?
6. Is the outer `x<67` E/R certificate genuinely directed on all 66 cells?
7. Does `L-94023` keep ordinary `q` and `4q` in the same primal before detail?
8. Has any synthetic fixture or incomplete-template Farkas separator been
   promoted beyond its scope?

## Expected verdict boundary

```text
live source and path marginal                 reviewable exact theorem
outer fibre                                   reviewable directed theorem
actual branchwise children                    exact impossibility theorem
joint native coupling                         open producer specification
native deficit / endpoint implication          conditional only
Riemann Hypothesis                            unproved
```
