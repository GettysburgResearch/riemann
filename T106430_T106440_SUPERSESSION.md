# Supersession notice for T-106430 and T-106440

Status: **BINDING**  
Created: 2026-08-25

The endpoint winding identities, exterior-square numerator identities, signed
Fourier/residue-Gram formulas, companion barycenter, and root-compression
statements in the `T-1064xx` line remain valid at their stated local scopes.

The following conclusion-facing compositions are withdrawn:

```text
T-106430 use of a visible Hankel source cost < 1/600;
T-106440 use of a visible Hankel source cost < 1/982;
the resulting SIGNEDTAIL106430 and EVENST106440 percentage implications as
written with those visible payments.
```

Binding reason (`R-106450`): for a reduced all-pass quotient `U=N/D`, every
analytic vector of the form `Dg` with `Ng in H^2` lies in `ker H_U`:

```text
H_U(Dg)=P_-(Ng)=0.
```

Therefore the denominator-multiplied source cannot furnish a nonzero subspace
of the Hankel initial space.  The source density ratios are retained only as
local numerator/denominator algebra; they do not bound the topological Hankel
charge.

The controlling successors are:

```text
T-106450 / RESGRAM106450:
  retain the literal quotient (N-D)/D and every principal-part weight;

T-106451 / HBSIG106451:
  use the denominator-free Hermite--Bézout full-signature matrix with
  signature R_0-R_4.
```

Exact status:

```text
T-106430 percentage composition   SUPERSEDED
T-106440 percentage composition   SUPERSEDED
RESGRAM106450                      OPEN
HBSIG106451 / HBRT106451           OPEN
90% / density one / RH            UNPROVED
```
