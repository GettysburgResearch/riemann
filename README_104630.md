# T104630 — Fractional-current fifth-endpoint 90% frontier

This add-only continuation of PR #720 joins the independently reconstructed
fixed-order work to the exact fifth-endpoint programme frozen at PR #731 and
the Xi current-profile theorems frozen at PR #729.

The new unconditional results are:

```text
the fifth odd current has an explicit generalized-Maxwell source profile;
that profile is within exp(7 H^2/(3 kappa_0)) of the exponential metric;
the identity admits an exact fractional Calderón resolution;
the unweighted shallow canonical defect is bounded by a scale-wise,
source-weighted primal transport integral plus an explicit tail;
the constants H_0=1/20 and a=1/1000 leave a strict 299/10^6 margin
inside the fifth-endpoint 90% ledger.
```

The remaining theorem is `FRACTRANS104630`: bound the literal scale-wise
current-weighted transport residual by `21/1000 N`. It contains no missing
coverage theorem, no global Gram condition number and no arbitrary fixed
sampling scale.

```text
FRACTRANS104630       OPEN / 90%-BEARING
more than 90%         UNPROVED
RH                    UNPROVED
```
