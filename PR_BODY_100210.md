## Purpose

Continue the minimal ordinary-Möbius wavelet route from PR #675 at exact head `e21383e7522962182491f88301b3cbd375d6d6d0`, while reconciling PR #672's symmetric phase-Hasse flow.

**The Riemann Hypothesis remains unproved.**

## Exact advance

Every exact parity-saturating Hasse flow has the same signed divergence:

```text
B_J(Phi)
 = Delta Phi(empty)
   - sum_A (-1)^|A| w(A) Phi(A).
```

For exponential product tests,

```text
B_J(gamma)
 = Delta - product_i(1-a_i p_i^(i gamma)),
```

so the neutral phase vanishes identically.

After normalizing PR #674's minimal wavelet by `sqrt(X)`, the native activities are exactly `1/p`. For `X>8`, the ordinary-Möbius wavelet is the negative of this signed Hasse divergence.

## Binding firewall

Signed cancellation does not control positive edge variation. An exact two-edge fixture has zero signed boundary and variation one; PR #673 proves the actual priority positive flux is power-sized for every ordering.

Therefore the only viable Hasse continuation must preserve inter-edge cancellation through the compact wavelet observation. Under the exact dictionary, this is the signed cross-core `MWOC99910` frontier, which PR #675 proves RH-equivalent.

## Replay

```text
PASS_X_100210_HASSE_WAVELET_TERMINAL_MAP
570ecdde4e647056e8f1f5ce7a1510189829367ce1ee1dd982d0420e71539c1c
```

## Boundary

```text
flow-independent signed divergence       PROVED EXACT
closed phase product / neutral zero      PROVED EXACT
minimal-wavelet source dictionary        PROVED EXACT
positive variation shortcut              REFUTED
signed phase packing / MWOC99910          OPEN / RH-EQUIVALENT
Riemann Hypothesis                       UNPROVED
```
