# T-104500 — Xi Riccati–Pick reverse-Rolle cascade

This branch is a theorem-producing successor to programme PR #714.

It replaces the false unit-cost extra-extremum count by an exact factor-two
conservation law, adds the missing complex boundary winding, identifies the
exact Riccati/Pick dynamics of the derivative ladder, and derives an
unconditional high-derivative real-rooted-box theorem from the Ki / Gunns–
Hughes cosine limit.

## New exact spine

```text
adjacent critical-value edges
  -> exact factor-two wrong-extremum charge
  -> exact complex winding transport
  -> Riccati/Pick derivative-ratio dynamics
  -> fixed-scaled-box high-derivative real-rootedness
  -> integer-rigidity cascade
```

## Exact result

For generic real polynomials,

```text
nonreal(p)-nonreal(p') = 2 * wrong_extrema(p).
```

For a real entire function on one conjugation-symmetric domain,

```text
O(F_k)
 = O(F_(k+1))
   + 2 E_k
   + B_(k,-)+B_(k,+)
   + W_k-1.
```

The derivative ratio

```text
h_k = -F_(k+1)/F_k
```

satisfies

```text
Laguerre_k = F_k^2 h_k',
h_(k+1) = h_k - h_k'/h_k.
```

Ki's compact cosine theorem implies that every sufficiently high Xi derivative
has only simple real zeros on every fixed scaled compact box, and the same is
true simultaneously for any fixed finite band of subsequent derivatives.

## Remaining RH gates

```text
GBOX104500
  quantitative cosine asymptotics on the expanding boxes corresponding to a
  fixed original height;

RPCH104500
  cumulative wrong-extremum plus boundary-winding charge below two.
```

Their conjunction implies RH by an exact even-integer zero count.  Neither is
proved here.  RH remains unproved.
