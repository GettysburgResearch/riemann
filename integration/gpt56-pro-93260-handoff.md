# Handoff — 93260 hostile cubic-dispersion successor

## Frozen parent

```text
PR #498
6cc0da2fa5711017e260ebdcea4ba8c22e453288
```

## Durable results to review

- `R-93260`: independent reconstruction and corrected two-sided modulus line;
- `L-93261`: exact minimal-two-switch/double-zero kernel theorem;
- `T-93260`: prime-only RH-equivalent scalar after power/gauge removal;
- `L-93262`: critical radix-four Abel compression to one switch;
- `L-93263`: a positive compact Peano potential whose log curvature is `W`;
- `R-93264`: scalar one-sign scale-filter firewalls;
- `L-93265`: `O(log Y/Y)` forcing and complete small-divisor closure.

## Smallest remaining arithmetic statement

Prove square-root cancellation in

\[
\sum_{\sqrt X<d\le X/2}\mu(d)F_W(X/d),
\]

keeping the complete elementary kernel `F_W` intact. Absolute values, generic
block counts, or a source-blind large sieve do not suffice.

A genuinely new continuation should attempt one of:

1. a bilinear dispersion identity for the large divisor range;
2. a vector pair of one-switch kernels whose common mode restores the double
   Mellin zero;
3. a sharp convergence theorem for the positive potential `U_Lambda`, using
   structure beyond positivity alone;
4. a carrier-coupled Mobius estimate tied to the First-Hermite ordinate;
5. an exact arithmetic transport that preserves the signed `F_W` forcing.

## Boundary

RH remains unproved. Do not merge this packet as a proof claim before
independent reconstruction.
