# Integration handoff — Issue #258 / Prime-Anchored Digit Transport

Agent: `gpt56-pro-22`  
Branch: `agent/gpt56-pro-22/258-prime-anchored-digit-transport`  
Base: PR #250 at `f179ab93e7e0dc748e0e9fb9e5a6800b80b6d224`  
Status: **FULL PROPOSAL; PRODUCTION SIGNED DIPOLE FLOW AND FRAME OPEN; RH UNPROVED**

## New exact components

```text
L-25801  top-depth prime-anchor normal form
L-25802  positive nonmultiple-count kernel
L-25803  reciprocal-free potential and fixed-scale dipole
L-25804  two-frequency adjacent-flow identity
L-25805  prime-anchor carry cluster descent
L-25806  dyadic bit-layer and parity interface
L-25807  carry-cost to physical-flow frame adapter
R-25801  no-go and scope firewalls
R-25802  absolute recovery-wedge shortcut rejected
D-25801  fail-closed PADT certificate
T-25801  PADT family implies RH conditionally
M-25801  production and review protocol
X-25801  exact synthetic algebra regression
```

## Corrected spine

```text
reviewed two-frequency local block
-> exact finite packet and terminal closure
-> top source T=Lambda*r_V^(K-1)
-> positive anchor g_(K-1)*mu_(>V)^(K-1)
-> dyadic nonmultiple depletion
-> reciprocal-free potential Z=a_Q*T
-> fixed-scale dipole D=(epsilon-delta_Q)T=mu_V*Z
-> signed adjacent/divisor transport of mu_V
-> complete same-scale prime-power cluster solve
-> two-frequency Transport Frame certificate
-> E_D(J)<=exp((eta_K+o(1))J)[1+lower]
-> T_J=Q^-1/2 T_(J-logQ)+D_J
-> strict recurrence with eta_K->0
-> RH.
```

## Important correction

Do not use

```text
T=H_Q*mu_V*Z
```

with a current-block estimate for `Z` and one absolute `mu_V` charge. At shifted
blocks near `J-log V`, the same `V` has full top-order capacity. This shortcut
is rejected in `R-25802`.

The proof-facing theorem estimates the complete signed dipole

```text
D=(epsilon-delta_Q)T=mu_V*Z
```

directly and uses the exact fixed-scale physical recurrence.

## Exact regression

```text
PASS_EXACT_TOP_TRANSPOSE_DIGIT_DEPLETION_AND_FLOW_ALGEBRA
cases                                   6
dyadic layer rows                    3584
all mismatch counts                     0
mutation tests                          7/7
proof-object SHA-256
cfae02e722f534195e9bfef00065e5457b0da1e5a18a96589ea0e9560aa2a7b3
```

## Immediate reviewer target

Construct the complete `K=4` top-source graph with:

- every lower-depth divisor-allocation sibling;
- one dyadic fixed-scale depletion;
- marked prime-power anchors;
- exact adjacent incidence;
- same-scale cluster solves;
- the full `L-9518` physical Gram;
- parity and fixed-ratio mutations.

The proposal fails upon any missing sibling, any undeclared same-scale child,
any physical generalized eigenvalue growing like `exp(cJ)`, or any lost
Mertens/parity mutation.

## Exact boundary

```text
exact source and depletion algebra            proposed complete
exact dyadic/parity adapter                    proposed complete
exact flow/cluster/frame schemas               proposed complete
production signed source flow                  open
production Transport Frame LMI                 open
PADT(K) family                                 open
PADT(K) family -> RH                           proposed complete conditional
Riemann Hypothesis                             unproved
```
