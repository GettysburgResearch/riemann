# Integrator and successor handoff — PR #510 versus PR #522

## Frozen heads

```text
PR #509 e01daee9cdfea35d2a7d2591f1df6c8080084119
PR #510 b596b1abae9213366aab4cf3f2c911ff8cfab725
PR #522 3e8949af3e23b8563bfbc0cf8846820cc6c66d76
PR #508 4ae97dffd1f76ed3244b8f3028560ffa80663caf
review #516 2492bd48f8e2bafacfc04fc9237c06219b8c9bf8
```

## Verdict

Neither sibling closes the live positive coupling.

```text
PR #510 first failure:
  literal occurrence coefficient is rough-scaled twice.

PR #522 first failure:
  occurrence-to-path normalization is not defined consistently.

Independent blocker:
  complete Target-Lorenz tail remains unproved after review #516.

Later blocker:
  top/bottom anchored source is output and also charged as omitted.
```

## Strongest retained results

```text
hybrid finite/Volterra source identity;
bulk rank-one incidence marginals;
PR #503 type firewall;
paired P61 whole-packet orientation;
abstract Target-Lorenz leaf compiler;
one common-row q/4q/detail formalism;
conditional all-column and Y4 arithmetic;
one-sided endpoint orientation.
```

## Required successor theorem

Publish one exact anchored occurrence map

\[
\mathcal A_X\longrightarrow\bigsqcup_\omega\mathcal L_\omega
\]

with:

```text
full native input coefficient;
one owner path;
rough coefficient applied exactly once;
paired orientation;
certified Target-Lorenz leaf for every parameter and row;
output/omission split before physical placement;
one common quantizer;
source and output marginals checked from actual arithmetic data.
```

The replay must reconstruct all path coefficients from repository definitions. One selected `(67,15)` leaf is insufficient.

## Proposed repairs, not verified

1. Route fully weighted literal occurrences deterministically, or start before the rough factor and let `L-91362` supply it. Do not combine the conventions.
2. Replace PR #508's failed transcendental enclosure contract with an inclusion-certified backend or exact primitive intervals.
3. Split anchored source into output, bottom omission and top omission before `Gamma_A`.
4. Replay the full live path family and all Target-Lorenz rows.
5. Revert PR #522 root `README.md` to the PR #509 version; retain the packet README under `standalone/`.

All repairs remain **PROPOSED**. RH remains unproved.
