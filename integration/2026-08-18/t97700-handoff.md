# T-97700 handoff

The threshold-flexible source ledger is exact, but every stopping schedule with
`L log L=o(log log X)` is eventually negative. This includes PR #578's
`O(log log log X)` schedule and refutes `LAPBR67`.

The next attack should work in the critical rough-depth window and prove or
separate `CSHT67`. Do not return to local leaf positivity or l1 child budgets.
