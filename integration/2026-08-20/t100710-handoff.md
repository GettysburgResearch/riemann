# T-100710 successor handoff

Freeze:

```text
base PR #693
base head 2cc70798b453ce4e5059dcc25d8d3fb7c38e172e
```

Exact disposition:

```text
short-sector asymptotic        proved
SCME100704                      refuted
long-sector asymptotic         proved
LRNM100704                      proved
RH                              unproved
```

The next branch must preserve the exact cancellation

```text
-kappa_0 sqrt(X)/log X  +  kappa_0 sqrt(X)/log X.
```

Do not take an absolute value, positive part, or separate Hilbert norm of the
short region before this carrier cancels.

Recommended next targets:

1. a centered two-component cross-region Gram after projecting away the known
   carrier direction `(-1,+1)`;
2. an exact source-chaos projection that removes the complete prime carrier
   before the owner-ratio partition while retaining the reciprocal-zeta
   detector.
