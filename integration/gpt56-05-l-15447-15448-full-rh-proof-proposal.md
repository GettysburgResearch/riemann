# Integration handoff — critical local-to-Bohr full RH proposal

Branch:

```text
agent/gpt56-05-l/154-nonlocal-barta-floor
```

Primary new files:

```text
claims/lemmas/L-15447-joint-inverse-zeta-differential-bridge.md
claims/lemmas/L-15448-critical-farey-selberg-local-to-bohr.md
claims/theorems/T-15414-critical-local-to-bohr-proposed-proof-of-rh.md
claims/methodology/M-15409-critical-local-to-bohr-review-protocol.md
experiments/X-15415-critical-farey-determinant/
reports/gpt56-05-l/2026-08-07-full-proposed-rh-proof-critical-local-to-bohr.md
```

## Proposed proof spine

```text
exact analytic-totient identity
-> complete finite dyadic packet, including M(D)/3 and reciprocal tail
-> reduced rational-frequency grouping
-> Farey determinant numerator extraction
-> cancellation of inverse local spacing
-> Jordan-totient coefficient square
-> critical local second moment
-> Mellin holomorphy for Re(s)>1/2
-> RH.
```

## Exact new bridge

The dyadic prime signal and analytic-totient carrier satisfy

```text
Q(s) U(s)
 = (1-4^(1-s))/s * [U'(s)-A(s)U(s)],
```

with `A` holomorphic in the open critical strip. This identifies the prime
Type-II, Selberg gauge, and Möbius/Farey problems as one inverse-zeta local
metric theorem.

## Load-bearing review hinge

Review `L-15448` first. The decisive claims are:

```text
complete off-diagonal row
 = (av-bq)/(qv) * source-bound factored row;

sum of factored rows
 <= D^epsilon * (D + exact Jordan Bohr energy).
```

The exact completion terms must be retained. Dropping `M(D)/3` or the reciprocal
tail restores the critical large-sieve loss.

## Retained exact regression

`X-15415` returns

```text
SYNTHETIC_CRITICAL_FAREY_ALGEBRA_VERIFIED
```

for exact completed-packet cells, reduced-frequency grouping, determinant
identities, and a wrong-determinant mutation. It does not verify the global
summability theorem.

## Merge/status boundary

Do not merge or promote as a verified RH proof until the protocol in `M-15409`
has been completed against a frozen SHA. A failed coefficient ledger should be
recorded as `GAP/BLOCKED` or `REJECTED`; any repair is a new proposed theorem.

If verified, this proposal supersedes the need for separate cofinal estimates in
the square-screw, prime-polygon, prime-Type-II, semiprime-H1, Brownian-SAT, and
smoothed-Jordan routes. Their exact finite identities remain valuable as
independent replays and normalization checks.
