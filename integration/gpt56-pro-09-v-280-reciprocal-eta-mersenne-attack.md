# Integration handoff — reciprocal eta / Mersenne boundary attack

Branch:

```text
agent/gpt56-pro-09-v/280-central-cascade-global-attack
```

Parent:

```text
PR #280
research/gpt56-sol/277-central-carry-cascade
6a2195f4c173dfc3db1ac42596dce58706062b28
```

## New claim stack

```text
R-28001  exact product-six refutation of all-order continuum monotonicity
L-28001  central Neumann resolvent = 1/eta
L-28002  reciprocal-eta carry image = dyadic staircase
L-28004  central split collapses source to Mersenne boundary rows
L-28003  reflected eta Selberg Hermitian identity
T-28001  reflected Mersenne Boundary Recurrence conditional RH proposal
M-28001  fail-closed review protocol
X-28001  exact finite algebra replay
```

## Disposition of parent results

Retain:

- exact central residual formula;
- unconditional first and second positive packing stages;
- exact finite support-halving signed saturation.

Withdraw as a dependency:

- positivity/decreasingness of every continuum central iterate;
- the claim that every later sign defect is only the `-1` lattice commutator.

The first continuum defect is the exact coefficient `-2` at
`6=2*3=3*2`.

## New global interface

The complete cascade source is not opaque:

```text
central all-stage resolvent
 =1/eta(s)
 =1/[(1-2^(1-s)) zeta(s)].
```

Its complete atomized carry image is the dyadic staircase of `L-28002`.  On the
central split it equals one except at the Mersenne rows `2P-1`, where the charge
is `1-P`.

The preferred next review target is therefore `RMBR`, not the old DCCS wording.
The reflected eta identity supplies the correct Hermitian square, but no
critical boundary recurrence is claimed proved.

## Merge boundary

The exact algebra and refutation may be integrated independently after review.
`T-28001` must remain a conditional proposal until `RMBR` is proved.

```text
accepted RH proof: NO
```
