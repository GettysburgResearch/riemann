# T104590 handoff

## Read order

1. `L-104542`
2. `L-104543`
3. `L-104544`
4. `T-104590`
5. replay and report

## Two next attacks

### Phase lane

Estimate

\[
\Delta\arg\left[
\xi''+\frac{a}{\log T}\xi'''
\right]
\]

on the critical line with the same mollifier and normalization used in the
fixed-order Conrey theorem.

The parameter freedom in `L-104542` means `a` may be optimized after the exact
count reduction, but before any hypothetical zero is selected.

### Moment lane

Choose one holomorphic gamma normalizer `m_T` and evaluate

\[
\frac{1}{2\pi i}\int
m_T^2\Xi''^2\,\frac{\Xi''''}{\Xi'''}
\]

on a thin strip, together with the exact weighted variation first moment.

The target is

\[
D_T^2>\frac12R_3(T)B_T
\]

with an explicit positive margin.

## Firewalls

- A positive weighted amplitude majority is not by itself a count majority.
- The phase drift must use a continuous unwrapped branch.
- The marked contour must exclude nonreal derivative zeros before the
  thin-strip limit is taken.
- Neither route may import the independent `alpha_2>0.9584` theorem.

RH remains unproved.
