# T104540 handoff

## Controlling conclusions

```text
Conrey alpha_2 > 0.9584                    unconditional imported theorem
Conrey alpha_3 > 0.9873                    unconditional imported theorem
“99% at xi'''”                             approximate wording only
alpha_3 alone -> positive alpha_2           impossible without extra structure
Conrey two-level good-extrema fraction      > 9665/9873
RCMV104530                                  still open
RH                                          unproved
```

## Integration actions

1. Register the Conrey fixed-order table as an imported historical theorem.
2. Replace every literal “>99% for xi'''” statement by “>98.73%” unless a
   different counting convention is explicitly sourced.
3. Mark `R-104514` binding for every proportion-only reverse-Rolle claim.
4. Retain `T-104530` only as a conditional transfer API.
5. Do not describe `T-104530` as an unconditional conversion of the known
   `xi'''` row.
6. Preserve `L-104526` as the strongest exact orientation consequence of the
   already-known two-level table.

## Source pin

```text
J. B. Conrey,
Zeros of derivatives of Riemann's xi-function on the critical line,
J. Number Theory 16 (1983), 49–74,
DOI 10.1016/0022-314X(83)90031-8.
```

Before canonical integration, pin the exact page/table in a retained primary
PDF.  The present packet records the standard published numerical table and
its DOI without redistributing the copyrighted paper.
