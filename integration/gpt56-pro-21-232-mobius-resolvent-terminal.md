# Integration handoff — Issue #232 corrected proposal

## Frozen disposition

The independent review of commit

```text
0211053679e1b5f524a9238093e64d2e7a4128e3
```

classified the `STC(K)`-only proposal `GAP/BLOCKED`.  Nothing in the later
commits retroactively verifies that frozen theorem.

## Retain with corrected scope

```text
L-23201  exact finite Möbius resolvent; packet dictionary only a schema
L-23202  scalar high-order Mertens equivalence; no packet decoder claimed
L-23203  abstract conditional complexity induction
L-23204  scalar Selberg adapter; packet application blocked without source map
T-23201  frozen proposal, marked superseded
M-23201  frozen review protocol, marked superseded
X-23201  synthetic smoke test only
```

## Add canonical corrected stack

```text
R-23201  refutation of the STC-only proof boundary
L-23205  terminal lattice Euler cancellation
L-23206  corrected Type-I complexity reduction with delta=1/5
L-23207  source-specific balanced Type-II certificate BTP(K)
T-23202  corrected full RH proposal
M-23202  fail-closed balanced Type-II review protocol
X-23202  exact terminal-Euler/BTP synthetic regression
```

## Base and frozen inputs

Use PR #158 at

```text
9ee33527aef3acbb281ebad367aeb1e51652d006
```

for the safe Hardy transfer, high-order null quotient, exact finite
Heath--Brown packet, fixed-reserve partition, and scale-contraction theorem.

The first-cell Mertens decoder is imported only at its reviewed scalar scope
from PR #229.  The generic cluster-operator refutation on PR #231 remains a
mandatory firewall.

The corrected proof does not depend on a packet/global Selberg source
identification.

## Canonical proposal

```text
safe high-order prime signal
-> exact finite signed packet
-> fixed reserve delta=1/5, K>=6
-> corrected Type-I complexity reduction
-> terminal Euler cancellation
-> signed balanced Type-II theorem BTP(K)
-> strict scale contraction
-> RH.
```

## Promotion rule

Promote only after an unbounded source-bound family proves the complete
balanced packet recurrence and

```text
epsilon_K -> 0
```

or the tensor analogue

```text
epsilon_K/(1-kappa_K) -> 0.
```

A finite ladder, generic operator bound, unsigned packet estimate, or aggregate
Selberg identity without a packet source map is insufficient.

## Public status

```text
CORRECTED FULL PROPOSAL
TERMINAL TYPE-I FAMILY PROPOSED CLOSED
BALANCED TYPE-II THEOREM OPEN
RH UNPROVED
```