# Integration handoff — PR #304 terminal-source refutation

## Frozen input

```text
PR #304
78b75fc17e27334a9950018528c1c6e083d74820
```

## Retain

- the adjacent-tree load identity `L_q(E_h)=1_(q|h+1)`;
- the finite carry/divergence normal form at its exact algebraic scope;
- any non-absolute signed or physical-normal continuation not using the rejected atomic ledger.

## Reject

- treating `A_k(q,s)e_(2k)-B_k(q,s)e_(2k+1)` as a column-independent divisor source;
- the polylogarithmic zeroth Euler atomic ledger;
- the claim that correcting the source by Möbius inversion rescues the ledger;
- every terminal recurrence obtained by summing absolute adjacent-tree source costs.

## New controlling theorem

`R-30404` proves for the unique correctly inverted complete critical boundary source

```text
||sigma_N||_at > N/480.
```

The lower bound is inherited by the sum of the atomic norms of every exact source decomposition.

## Required future architecture

A continuation must combine bulk and boundary before taking a norm:

```text
analytic bulk + complete boundary
-> exact independent-frequency normal form
-> signed/lower-scale cancellation
-> recurrence.
```

It must fail closed on the common-tail cell

```text
floor((N+1)/4)+1 <= m <= floor(N/3),
```

where the correct Möbius inversion has only its identity term and every critical boundary coefficient is positive.

## Status

```text
absolute terminal closure    false
coupled boundary route       open
RH                           unproved
```
