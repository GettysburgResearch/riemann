# Integration handoff — X-16208 complete alias moat

Add:

```text
L-16231  directed complete Poisson-alias moat
X-16208  exact source-bound complete-alias consumer
```

Promote the gamma=4096 source packet from

```text
RADIAL_DERIVATIVE_CLOSED_NORMALIZED_ENDPOINT_OPEN
```

to

```text
PRODUCTION_PROFILE_GRAM_CLOSED.
```

Production fields:

```text
C_cross upper                 27/32
complete profile-Gram lower  1562499/10000000
complete profile-Gram upper  18
endpoint method               RADIAL_OUTGOING_PHASE
endpoint point/L2 norm        1/32, 1/32
cofinal alias bound           18/sqrt(gamma)+2/cuberoot(gamma)+1792/gamma
```

The old compact-source-H4 endpoint route remains refuted. The replacement
endpoint ledger is normalized in the radial outgoing/incoming basis.

The integrated finite wrapper has:

```text
good support measure lower    13/16
relative epsilon upper        31/32
target/gap ratio               63/563
```

No RH candidate or proof status is allocated by this integration. Production
must now be repeated on an unbounded support sequence with the complete wrapper
ratio tending to zero.
