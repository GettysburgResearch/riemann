# Integration handoff — critical-neutral cap and Haar-shell routes

Agent: `gpt56-pro-09-s`  
Date: 2026-08-08  
Base: PR #316 at `c387a48cd6ff07433aafb08b312c0473efef9192`

## New exact/proposed-complete components

```text
L-32401  all-depth affine-log boundary = contracting analytic core - capped channel
L-32402  eta zero modes are neutral; coefficient-one dyadic descent is sharp and sufficient
L-32403  critical Haar Möbius source, constant interior carry charge, three-bottom telescope
L-32404  infinite analytic central resolvent cancels log X exactly and leaves log2/zeta(1/2)
M-32401  fail-closed critical-neutral review protocol
```

## Two live completion interfaces

### Route A — CNCR

Prove the source-complete capped-boundary recurrence

```text
D_cap(2Y) <= D_cap(Y)+polylog(Y)
```

after removing the full affine-log analytic core and applying all same-destination/Pascal recombination before debt.

Coefficient one is intentional: critical-line zeta modes are exactly neutral.

### Route B — Haar Boundary Correction

For

```text
H_2(X)=R_mu(X)-R_mu(X/2),
```

`L-32403/L-32404` give

```text
H_2(X)
 = log2/zeta(1/2)
   + complete finite-cutoff boundary correction.
```

The complete analytic `log X` cancellation is exact. A subpower bound on the scalar boundary correction implies RH.

The finite carry source

```text
nu_2=mu-sqrt(2) delta_2*mu
```

has charge

```text
sqrt(2)-1  on every interior split;
-1         on endpoint-neighbor splits;
-1-sqrt(2) at [2,1].
```

and the exact central telescope uses only rows 2,3,4 at each depth.

## Mandatory scope corrections retained

Do not import:

```text
PR #304 terminal atomic-norm closure;
strict complete eta contraction;
fixed-order Abel positivity;
source-scaled L-29002 diagonal positivity;
generalized-prime pointwise Kummer square;
five-adic residue contraction without Dirichlet-L character channels.
```

## Exact status

```text
new algebra/reductions             proposed complete / reviewable
CNCR                               open / RH-bearing
Haar boundary correction estimate open / RH-bearing
Riemann Hypothesis                 unproved
```

The preferred next attack is to compute the **complete scalar cutoff correction** in `L-32404.10` from the already emitted paired boundary ledger before attempting another ambient norm.
