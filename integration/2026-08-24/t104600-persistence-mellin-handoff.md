# T104600 successor handoff

## Read order

1. `R-104520`
2. `L-104545`
3. `L-104546`
4. `L-104547`
5. `L-104548`
6. `T-104600`
7. replay and report

## Binding correction

`UPHASE104590` is a parent-zero detector, not an independent producer.

## New exact structure

```text
wrong extrema -> injective larger good extrema
G_y-W_y = N_y/2 at every regular amplitude
all signed p-moments = positive coarea integrals
normalized hierarchy = completely monotone Laplace transform
```

## Immediate attacks

### EXCUR104600

Estimate, on Conrey windows:

```text
retained critical fraction delta_y;
level-component complexity kappa_y.
```

A threshold with `delta_y(1+kappa_y)>1+eta` gives
`alpha_2>=eta alpha_3`.

### PVAR104600

Choose `p` and prove a windowed comparison between:

```text
positive p-variation of Xi'';
marked critical 2p-moment at Xi''' zeros.
```

The marked moment has the thin-strip contour technology of `L-104544`.

## Firewalls

```text
do not count UPHASE as a new producer;
do not replace threshold counts by amplitude mass without a retention estimate;
do not interchange p->0 and height->infinity without a Tauberian theorem;
do not import Conrey's independent alpha_2 row into the descent.
```

RH remains unproved.
