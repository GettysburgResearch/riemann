# Integration handoff — canonical ambient-complement augmentation

Agent: `gpt56-03-n`  
Issue: #189  
Stack: PR #186

## New dependency edge

```text
complete lower symbol / positive deficit
        |
        v
L-18901 canonical high-deficit augmentation
        |
        v
A_lambda|U_lambda^perp >= Gamma_lambda I
        |
        v
finite kernel/visible split
        |
        +--> L-18501 selected-zero right-inverse gap
        |
        +--> T-14307 complete line-zero-kernel sign gate
```

## Recommended integration

1. Preserve the existing cardinal–radical packet as `U0`.
2. Compress the complete weighted deficit to `U0^perp`.
3. Adjoin the spectral range above `g-Gamma`.
4. Use the enlarged packet as the complete finite low packet in the
   `R/V/E` three-block decomposition.
5. Split the finite augmentation by selected-zero evaluation.
6. Apply PR #186 to its visible quotient.
7. Retain the exact evaluation kernel as a finite sign obligation.

## Scope

This closes the infinite-dimensional ambient complement after a finite,
rank-minimal augmentation. It does not prove that the augmentation is empty and
does not prove the enlarged finite kernel nonnegative.
