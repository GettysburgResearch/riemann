# Required fixes before canonical extraction

**Scientific status:** RH remains unproved.
**Readiness:** `READY_WITH_EXPLICIT_EXCLUSIONS`.

## P0 — Mellin–Landau contract

1. Keep the fixed detector/source/smoothing width independent of the zero and horizon.
2. Separate initial convergence from meromorphic continuation.
3. State local integrability, nonzero density, finite abscissa, positive-real removability, multiplicity, and functional-equation reflection.
4. Every RH edge must contain its open arithmetic premise explicitly.
5. Prefer fixed rows 2,3 or the fixed 5:3 scalar; retain direct-main `L-96000/L-96001` only as analytic predecessors.

## P0 — actual-Xi Pick order three

1. Replace the corrupt historical PR #446 checker with a new canonical replay; preserve the old defect.
2. Repair the external source lock and distinguish line verification, simplicity, zero counts, and tail bounds.
3. Use one consistent upper-half/reflected-orbit convention and multiplicity convention.
4. Spend one coefficient unit of the selected critical orbit and retain any residual multiplicity.
5. Treat PRs #446 and #460 as one shared reserve ledger.
6. Prove grouped local `C^2` convergence through two derivatives.
7. Canonicalize PSD through size three; omit distinct-node strictness unless separately proved.

## P0 — graph and aliases

1. Premises are JSON arrays; no free-form delimiter is accepted.
2. Every premise/conclusion is a registered node.
3. Conditional and RH-equivalent estimates are inactive until explicitly supplied.
4. The old K0/K1 wavelet–Vaughan edge remains false; only same-K1 translation is active.
5. Bare numerical theorem IDs never determine identity; use exact PR/head/path/internal-ID.

## P1 — Reviewer C final assets

1. Preserve the finite/local scope of Farkas, Y4 repair/null gauge, live marginal, Volterra Green, and direct-integration APIs.
2. Do not turn a finite primal/Farkas formulation into a feasibility theorem.
3. Keep Q4 phase-locked sublinear results distinct from uniform-center/all-order claims.
4. Use the exact PR #386 and #600 paths for Fourier–Goldbach and the critical-zero filter barrier.
5. Sequence #417 is an administrative no-object gap, not an unreviewed theorem.
6. Issue archaeology supplies genealogy only unless corroborated by exact claim/review objects.

## P1 — direct-main packets

1. Select one Cauchy Hardy/Fourier orientation; label its conjugate anti-causal and do not infer output sign before repair.
2. Exclude the direct-main P79 `L-91350` checker and false score surplus; retain exact split identities only.
3. Treat `L-91355` as mass accounting with packet typing open.
4. Keep `T-96000` gap-blocked by the absent simultaneous native all-row producer.
5. Separate `L-99930/L-99931` unconditional results from `T-99930`'s RH-equivalent critical estimate.

## Heavy artifacts

No heavy campaign was rerun. Use exactly:

```text
RETAINED ARTIFACT HASH VERIFIED
METHOD/CONTRACT AUDITED
HEAVY CAMPAIGN NOT RE-RUN
```

Finite and external computations support only the exact theorem and scope recorded in `FINAL_COMPUTATIONS.tsv`.
