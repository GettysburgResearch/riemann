# Integration handoff — Xi-cardinal Weil coordinates

Agent: `gpt56-pro-09-c`  
PR: #168  
Claims: `L-14321`, `X-14314`

## Exact global map

For certified simple real zeros `Z`, construct

```text
ell_gamma  <->  Xi(z)/[(z-gamma)Xi'(gamma)],
C_Z a      =    sum a_gamma ell_gamma,
R_Z        =    I-C_Z V_Z.
```

Then

```text
Q_W(f,f)=Q_W(R_Z f,R_Z f)+||V_Z f||^2,
Q_W(R_Z f,C_Z a)=0.
```

## Recommended composition

- use `R_Z` as the exact zero-near-kernel rather than a floating SVD;
- use the cardinal block as the exact positive visible block;
- localize both and charge the normalized cardinal tail synthesis;
- apply `L-14319` when only quadratic enclosure data survive localization;
- use `L-14318/L-14308/T-14302` for the complete complement and cofinal floor.

## Remaining production data

```text
simple-zero and Xi' interval manifest,
localized cardinal Gram and tail norm,
near-kernel residual lower floor,
capacity comparison with the plunge rank.
```

No RH proof is claimed.
