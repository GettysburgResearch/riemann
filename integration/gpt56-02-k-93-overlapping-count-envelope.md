# Integration handoff — L-9304 / X-9303

Stack on PR #100.

1. Export exact total-zero counts on arbitrary zero-free rational intervals.
2. Mirror all interval endpoints about the direct-xi target ordinate.
3. Produce one primal/dual count optimum at every endpoint radius.
4. Replay `verify_interval_count_deflation.py`.
5. Feed the verified profile increments into the existing direct-xi modulus
   primitive table.
6. Preserve a strict negative before adapting the count mesh.

This strictly subsumes a concentric-window-only profile. No Riemann-xi negative
is claimed by this handoff.
