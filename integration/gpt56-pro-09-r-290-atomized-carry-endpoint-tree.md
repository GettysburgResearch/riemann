# Integration handoff — atomized carry frame and endpoint-tree route

Branch:

```text
agent/gpt56-pro-09-r/281-carry-window-selberg-kummer
```

## Preferred dependencies

```text
PR #241  independent-frequency reflected block
PR #269  omega_2 factor-five source and carry wavelets
PR #272  Pascal split-chain/cycle algebra
PR #289  averaged carry commutator and prime annulus
```

No full-proof status is inherited from any dependency.

## New canonical files

```text
L-29001  atomized Nyman/carry frame and exact physical carry Gram
L-29002  Selberg-Kummer pointwise interior reserve
L-29003  endpoint balanced-tree commutator
R-29001  fixed Abel counterexamples through order eight
T-29001  ETSR conditional RH proposal
M-29001  fail-closed review protocol
X-29001  exact finite regression
```

## Suggested integration order

1. retain PR #289's scalar prime-annulus coordinate as the averaged member of
   the atomized bank;
2. import `L-29001` after the source normalization in PR #269;
3. import `L-29002` as an ordinary-prime carry theorem, without silently
   generalizing it to `Lambda_omega`;
4. import `L-29003` after PR #272's split-chain definitions;
5. mark fixed ambient Abel closure as unavailable using `R-29001`;
6. keep `T-29001` conditional until a production `ETSR` object exists.

## Key scope boundaries

- `L-29001` removes the **map**, not the energy estimate.
- `L-29002` is an ordinary von Mangoldt theorem.  The actual filtered source
  still needs complete recombination.
- `L-29003` preserves carry columns and entropy, but its tree expansion is
  signed.
- `ETSR` is the unresolved arithmetic theorem.

## Recommended next production task

Freeze one balanced interval, for example

```text
eta=1/4.
```

For one logarithmic block:

1. enumerate all breakpoints of `Z_(X,m)(theta)`;
2. form the exact rational carry-position Gram;
3. attach the complete independent-frequency reflected source;
4. separate the `L-29002` interior reserve;
5. emit the endpoint coefficient sequence;
6. apply the exact tree Abel transform;
7. measure the signed first variation and every lower-scale destination;
8. test the coefficient-one recurrence without taking rowwise absolute values.

A finite successful block is only a schema test.  The theorem requires a
source-bound symbolic recurrence or an unbounded certified family.
