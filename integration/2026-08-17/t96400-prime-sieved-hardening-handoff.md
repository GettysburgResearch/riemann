# T-96400 integration handoff

## Canonical disposition

Do not integrate `L-94200` as a proved prime-sieved positivity theorem.
Retain its row-spline formula, but mark the fixed-product `FRONTIER-CHAIN`
proof invalid by `R-96400`.

Do not integrate `L-96101` or `L-96200` as complete global transports.  Their
block capacity uses all integers while their declared positive store is
\(P\)-rough.

Retain from PR #542:

* the exact fixed-row reciprocal-zeta transform;
* analyticity on the positive real axis;
* the Mellin--Landau architecture.

Prefer the exact rows-\(2,3\) noncancellation in `L-96401` to the large-row
asymptotic.

## Strongest current prime-sieved spine

```text
canonical row formula
 -> exact two-row coefficient / Riesz state             L-96400
 -> integer-knot reduction and recurrence               L-96400
 -> exact two-row reciprocal-zeta consumer               L-96401
 -> TRP23                                                OPEN
 -> RH                                                   conditional
```

## Next producer

Prove eventual positivity of the two integer sequences \(c_N(2),c_N(3)\).
`M-96400` records three noncircular avenues: an exact prefix-debt theorem, a
source-correct large-prime-core transport, or a finite-dimensional invariant
cone.

## Status

```text
complete unconditional proof: not obtained
new exact refutations:        yes
new exact reduction:          yes
Riemann Hypothesis:           unproved
```
