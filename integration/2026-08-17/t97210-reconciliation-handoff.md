# T-97210 reconciliation handoff

Use PR #567 as the canonical fail-closed parity-covariant frontier.

Do not cite an unpublished terminal-checkerboard theorem, a blanket
“forest-incidence minors are positive” statement, or a completed global Hall
theorem.

A future complete successor must prove, in one common ordering:

1. total nonnegativity or the exact required sign-regularity of the literal
   owner-incidence matrix;
2. the parity-corrected terminal target/scalar checkerboard across all
   activation cells and boundaries;
3. the endpoint Lorenz inequality
   \(\mathcal L_E(T_O)\ge R_O\) with complete source capacities.

Only then may `L-97210` and `L-97211` be composed to the scalar Mellin–Landau
consumer.
