# Integration handoff — Issue #138 audit

## Add

```text
R-13801  local zero census does not refute a global functional
R-13802  O-5613 false frontier extension
R-13803  complex-center targeted Li family refuted as written
L-13801  saturated Hardy-Z sign chain with endpoint gates
L-13802  fixed-witness zero accounting
L-13803  corrected Hermite-Hankel inertia
M-13801  forced-progress and interface-equivalence audit
X-13801  exact locality/saturation process controls
X-13802  exact Hermite/Li algebra regressions
```

## Correct existing records

```text
O-5609  -> local off-line-zero interpretations excluded;
           global candidate-retirement conclusion retracted

O-5613  -> independent sub-frontier replay;
           frontier-extension conclusion refuted

T-0001  -> central PSD criterion retained;
           signature/inertia formula corrected by L-13803

T-0003  -> classical Li route retained;
           first-order amplification constant divided by two

T-0004  -> arbitrary-complex-center coefficient criterion blocked;
           conformal geometry and separate Re(xi'/xi) route retained
```

## Process changes

1. Require a `LOCALITY GATE` in every candidate-retirement record based on a
   finite zero census.
2. Require explicit endpoint semantics in every count/sign saturation packet.
3. Require a `FORCED-DIRECTION AUDIT` and `EQUIVALENCE AUDIT` before a metric is
   used to rank expensive computations.
4. Apply zero-accounting tail fractions only after the exact witness is frozen.
5. Distinguish special-function assembly independence from backend/library
   independence.
6. Do not order Taylor coefficients at a complex basepoint unless a real
   symmetrization and its RH criterion are explicitly proved.
7. Use the correct Hermite inertia convention in all box certificates.

## Separate operational patch

The positive-anchor base branch has been patched directly with a typed
provenance wrapper and tests. Trigger PR #139 requests the corrected PA1 run.

The older Claude bootstrap corrections are also published directly against
`claude/riemann-repo-development-h4vg4v` in PR #141.

## Candidate registry

No candidate is added. Any candidate marked refuted solely because its sample
ordinate lies inside the O-5608 slab should be restored to its previous exact
numerical status unless a route-specific locality theorem is attached.

No candidate may rely on T-0004's complex-center `lambda_n^(alpha)` positivity
until a matching generalized Li theorem is supplied.
