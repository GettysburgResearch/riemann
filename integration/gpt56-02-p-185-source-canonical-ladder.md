# Integration handoff — X-18506 source-canonical ladder

Use `X-18506` as the growing-packet replacement for the ad hoc `X-18505` basis.
The packet is defined by the exact source functional, not by floating
singular/eigenvectors.

A passing level emits `Delta=0` and can be fed directly to `T-18504`.  The radical
row remains separate.  Do not infer cofinality from the finite ladder.

The next mathematical target is the scale law

```text
inf_j log(c_j) * lambda_min(S_W(c_j,N_j),G_W) > 0
```

for an unbounded source-canonical schedule, or an equivalent directed family
whose output is independently replayable at arbitrary `j`.
