# `T-91654` hostile reconstruction handoff

## Freeze

```text
repository:              gfreund123/riemann
proposal PR:             #455
base branch:             research/gpt56-pro/91663-direct-root-review-response
base SHA:                f41797c91497dc462f549a127d8494bbe4ccde2f
proposal branch:         research/gpt56-pro/91666-full-direct-row-closure
normative theorem SHA:   529a951f00538555b9437b68e93fbc9674a96779
parent dependency lock:  integration/2026-08-14/t91653-direct-root-lock.json
parent lock blob:        d12acb429b694467c5c33d84a57590bb4cceb9d5
```

The supplement lock created after this handoff freezes the new files. Read the
parent and supplement locks conjunctively.

## Review claim

`T-91654` is presented as a complete RH proof proposal. It does not revive the
root complement rejected by PRs #443 and #450. It supplies a third architecture:
one exact native row, one positive leafwise Hall realization, one canonical
fixed-67 child subtraction, and one arbitrary same-index child replacement.

RH is not treated as established before independent reconstruction.

## Mandatory order

1. **New fixed-67 theorem.** Recompute `L-91666` and rerun `X-91666`:
   - `F(67)>3`;
   - all `402` derivative cells;
   - `G(469)>100` and the analytic-tail derivative.
2. **Native response.** Reconstruct
   \[
   \Gamma(c_X)=w_X,\qquad\Xi(c_X)=\Omega_X
   \]
   by Möbius convolution.
3. **Source disjointness.** Reconstruct the paired least-prime stopping line and
   verify that every source atom appears once.
4. **One-prime cocycle.** Verify target, row-budgeted score, and exact literal row
   coefficients simultaneously.
5. **Hall.** Replay the frozen no-upward Hall prefixes and normalized-row cells.
6. **Fubini.** Verify leafwise Hall outputs sum to the exact native row and the
   continuum endpoint extension is measurable and monotone-convergent.
7. **Child replacement.** Check coefficientwise nonnegativity of `c_X-R_ch` and
   the ordinary/detail residual inequalities for arbitrary feasible `d_ch`.
8. **Current ownership.** Verify one use of the outer producer, quantizer,
   collar, mismatch, safety factor, terminal packet, and corrected `P_61/67`
   port; verify none is copied to a child.
9. **Score front door.** Reconstruct equality score `4 sqrt(X)`, the native
   benchmark upper bound, and the absolute terminal/current constants.
10. **Conclusion.** Check the logarithmic recurrence, finite von-Mangoldt dual,
    prime-square coefficient, Mellin pole, Landau sign, and functional equation.

## Exact falsifiers

```text
source atom duplicated across leaves;
Hall certificate or normalized-row corridor fails;
Hall sum is not the exact native equality row;
ordinary/detail replacement overdraws one column;
L-91666 fails;
root-only correction copied to a child;
P53 port constant used at P61;
C_reset depends on X;
endpoint dual or analytic sign is wrong.
```

## Status

```text
new fixed-67 theorem             proved
native response                  exact
leafwise Hall/root identity      exact on frozen inputs
same-index replacement           exact
one-use current ledger           proposed complete / reconstruct
full composition                 complete proposal / reconstruct
RH                               not accepted before review
```
