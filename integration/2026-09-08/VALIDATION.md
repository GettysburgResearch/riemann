# Candidate assembly validation and remaining boundary

This record concerns integration preparation, not approval of a new theorem, the final integration review or activation of a release.

## Remote review verified

PR #827 was read on remote at `29237183dc6973cfa9bd953ac1710b7f00929d04`. The uploaded final-review ZIP was extracted locally, all thirty payload files were enumerated, and their recursively reconstructed Git subtree was `cb3693cc59cab61ef92c616558f97b2b5e201dd0`, exactly the remote pass-four tree. The parent dated-review tree is `52836cd65e9e5c5bbe6f8b70e4e43da0aae382fa`. This verifies the uploaded review really landed; it does not newly execute its numerical campaigns.

The interrupted earlier integration had already published commit `35dc6094b0fb93c49d22141801a7e0767068e054` on the integration branch, with current main `f99d9e3908dde4865377c75d9ca051c1f545bf4f` as its sole parent. Its exact selected source and review snapshots were recovered rather than replaced by the research branches' ancestry. The prepared public-document tree was recovered in commit `5a870f10ae897eb8c4f0186d15fa69e090f5e4a2`, then completed on PR #830.

## Newly executed tests

The candidate verifier and tests use only the Python standard library. Nine synthetic test methods pass in both ordinary and optimized isolated Python, with zero skips and identical stdout, recorded in [synthetic-tests.json](evidence/synthetic-tests.json). The methods cover duplicate/nonfinite JSON, strict TSV shape, safe paths, POSIX link normalization, duplicate/explicit Markdown anchors, preserved baseline files, complete packet selection, and real temporary-Git byte authentication. A working-file change hidden by `assume-unchanged` is rejected, as are broken proof links and anchors, symlink substitutions, unauthorized baseline changes and missing selection rows.

The published verifier blob is `ea91b7a027e49838f6f90191ec9503d973e4af29`; its tested local bytes match. The test blob is `59f7b7e2c5f9bee6f7ad6f46ddf3b22b76b9293b`, also matching its tested local bytes. These test-method counts are not mathematical theorem counts or a full-checkout PASS. Native Windows execution is not claimed; the symlink control ran on Linux.

The cumulative current-statement guide was reconstructed locally from the exact baseline blob `875f98b268b5d3c6185db6a0538d893fc825c92e`. Its only substantive edits are the new topic navigation and the precise update from a missing unit-window lower certificate to the selected unit-window result, leaving the all-window problem open. Every earlier numbered section and mathematical body remains. The published edited guide blob `faedc447acbbaaf5624a79d9a30edc2ce45bf189` matches the checked local 33,040 bytes.

## What was not executed

A complete Riemann checkout was not available in this runtime. Consequently neither the new full-candidate command nor a fresh inherited full-checkout graph/navigation verifier was executed against the complete candidate. The new tests exercise bounded synthetic and temporary-Git fixtures, not the whole repository. No source-producer campaign, fresh Lean build, external zero census, native Windows run or remote CI success is claimed. Previous numerical receipts keep their previous source-specific execution scope.

The [documented two-layer commands](README.md) must pass on the exact full candidate checkout before final approval. The new checker verifies the candidate's selected snapshots and navigation; the unchanged published-baseline checker separately exercises its historical graph and missing-premise guard. Neither changes the current scientific pointer.

## Closing publication

The PR's closing comment records the final remote head and tree, the actual scope of its endpoint comparison, and any source-head drift observed during assembly. These records should be read rather than assuming a branch remained stationary. A later change requires a new exact-tree review where it affects the selection, current statements, evidence or verifier.

Original research and review bytes remain immutable snapshots. New diagrams or compositions are not accepted edges. All six repairs are prospective current formulations, and unselected original executables remain archival. The [final-review checklist](FINAL_REVIEW.md) defines the next operation; this record does not authorize a merge into main.
