# T-99100 integrator handoff

## Intended stack

```text
base PR:     #624
base branch: research/gpt56-pro/99000-c4mbi67-primitive-prefix-billion
base head:   0fa032faedc0fa9a47a87c41a69c0db141b1dea2
```

T-99100 is add-only relative to that head.

## New closure target

```text
H_67(t) = C(t) - C(t/67)/sqrt(67)
H_67(t) >= 0 for every t >= 2,000,000,001
```

This one tail statement plus the retained finite base implies global `C>=0`,
C4MBI67, and then RH through T-99001.

## What is proved

- exact factor-67 dilation descent;
- exact local-square Dirichlet coefficient formula;
- exact anchored duplicate-67 threshold-complex identity;
- exact outward-rounded `H_67>=0` certificate through `2*10^9`;
- derived `C>=0` certificate through `2*10^9`.

## What is not proved

- the tail at or above `2,000,000,001`;
- global C4MBI67;
- RH.

## Recommended next attack

Work on the anchored complex from L-99102, not the unsigned complex of T-99000.
A successful weighted discrete-Morse or source-Hall construction must reserve
one 67-anchor activity and transport every remaining negative face without
changing the literal `b_67` coefficient.
