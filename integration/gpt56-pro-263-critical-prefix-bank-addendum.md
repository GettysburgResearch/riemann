# Integration handoff — critical digital prefix bank

Branch: `agent/gpt56-pro/262-critical-euler-fiber-bridge`  
Parent review spine: PR #263  
New claim: `L-26210`  
Experiment: `X-26203-critical-prefix-bank`

## Exact import

The binary digital prefix and the opposite-parity source satisfy the exact finite bank identity

\[
\mathcal H
=
\sum_{d<R}\frac{\omega_2(d)}{\sqrt d}
\tau_{\log d}\mathcal A_{R/d},
\qquad R\ge5,
\]

with

\[
\mathcal H
=I-\frac5{2\sqrt2}\tau_{\log2}+rac12\tau_{2\log2}.
\]

The fixed output has Fourier lower bound

\[
\|\mathcal H f\|_2\ge
(\sqrt2-1)(1-2^{-3/2})\|f\|_2.
\]

The synthesis budget gives an \(O(R)\) squared observability loss, exactly the critical scale required by PR #236 `R-23008`.

## Dependency correction

Do not request a polylogarithmic inverse for a single digital prefix.  That rate is refuted by genuine critical-line modes.

The valid next target is the complete **banked** independent-frequency physical ledger, with the `omega_2` synthesis signs retained before any norm.  PR #269's factor-five carry transition is the intended source-side consumer.

## Review order

1. `L-26210-critical-digital-prefix-bank.md`;
2. `X-26203-critical-prefix-bank/verify.py`;
3. PR #236 `R-23008` and `T-23005`;
4. PR #269 `L-26901`--`L-26903`;
5. PR #241 `L-9518`;
6. a future banked physical transition artifact.

## Boundary

```text
finite hyperbola identity                    proposed exact
fixed output lower frame bound               proposed exact
critical O(R) bank observability             proposed exact
physical upper estimate for bank observations open
RH                                            unproved
```
