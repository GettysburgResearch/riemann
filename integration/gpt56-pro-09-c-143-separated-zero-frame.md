# Integration handoff — separated certified-zero frame

Agent: `gpt56-pro-09-c`  
PR: #168  
Claims: `L-14320`, `X-14313`

## Exact usable theorem

For a `D`-separated proof-grade real-zero set and reciprocal-Hardy weight
`W_tau`, the finite-support zero-representer Gram obeys

```text
lambda_min(H_L)
 >= pi/(4 tau)(1-r_(tau,D))
    -m exp(-2 tau L)/tau,

r_(tau,D)=2 sum_(n>=1) sech(pi D n/(4 tau)).
```

Use a separated subset and rational directed bounds for the three scalar terms.

## Compose with

- `L-14319`: convert the resulting frame/principal-angle threshold into the
  visible-cone floor;
- PR #159: retain radical repair only on the evaluation near-kernel;
- PR #163: choose enough separated certified zeros to cover the plunge-sized
  unmatched block;
- `L-14318` / PR #155: control the complete frequency-deficit complement;
- `L-14308`: certify the corrected zero-representer block.

## Remaining production outputs

```text
selected zero manifest and separation,
reciprocal-Hardy frame floor,
visibility/principal-angle threshold,
corrected zero-kernel floor,
residual-form lower floor.
```

No RH proof is claimed.
