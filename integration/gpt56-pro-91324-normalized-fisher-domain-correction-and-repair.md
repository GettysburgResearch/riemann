# Integration correction — normalized Fisher domain, bounded repair, and physical-core closure

## Freeze

```text
parent PR:      #400
child PR:       #429
initial child:  80ba1f2958ab6e561dc5e91430ffd21c05d02f11
session date:   2026-08-13
RH:             unproved
```

Read the current child head from GitHub after publication; this correction was
added after the initial exact Hardy-geometry commit.

## I. Normative correction to `L-91316`

`R-91324` proves that the normalized phase feature

```text
h_(a,t)(Y)
 = exp(-itY)/varphi_a(-t)
   - exp(itY)/varphi_a(t)
```

cannot be used as the declared Bochner
`L2(P_a;H2)` source operator on a dense boundary core.

On the safe line,

```text
|varphi_a(t)| <= polynomial(t) exp(-pi |t|/4),
```

while

```text
E |h_(a,t)|^2 >= 1/|varphi_a(t)|^2
```

for all sufficiently large `|t|`. Any boundary function in the natural raw
multiplier domain would therefore have two-sided exponential `L2` decay. The
Paley--Wiener support theorem plus analytic continuation across a strip forces
such an `H2` function to vanish. Hence

```text
Dom(raw normalized feature multiplier) = {0}.
```

The following are retained:

```text
finite-carrier covariance identity of L-91312;
model-space shape/Hankel identity of L-91316.7;
Hardy delay geometry of L-91322 and L-91323.
```

The normalized dense-core feature map, its contraction inequality, and its
score-orthogonal positive reserve are not retained as Hilbert-space statements.
Sections of `L-91322` and `L-91323` that invoke that old operator are superseded
by `R-91324`--`L-91326`; their independent delay colligations remain exact.

## II. Exact bounded repair

`L-91325` renormalizes before integrating:

```text
k_t(Y)
 = varphi_a(t) h_(a,t)(Y)
 = Theta_a(t)^(-1) exp(-itY) - exp(itY).
```

Then

```text
E k_t = 0;
|k_t(Y)| <= 2;
K_a(s,t) = varphi_a(s) conjugate(varphi_a(t)) H_a(s,t) >= 0;
0 <= K_a(t,t) <= 4;
K_a(t,t) -> 2.
```

The corrected random-source map is bounded on the entire Suzuki model space.
The score observation remains a coisometry with an exact orthogonal reserve.
The missing amplification is isolated as

```text
T_(varphi,a) f = P_+(f/varphi_a),
```

on its dense maximal `L2` domain. This reciprocal-amplitude Toeplitz leg is
unbounded. The corrected exact factorization is

```text
J_a
 = a sqrt(2 Var_a(Y))
   T_(varphi,a) C_a B_a.
```

Thus ordinary data processing cannot prove the tangent domination: the
reciprocal completed-xi amplitude is the load-bearing analytic leg.

## III. Physical delayed Cauchy domain is nevertheless exact

`L-91326` uses

```text
omega(t) = (1+log(2+|t|))^2.
```

The weight belongs to `A2`, so every Riesz projection and every inner
model-space projection is bounded on `L2(omega dt)`. On the safe line,

```text
|a partial_a log Theta_a(t)| <= C_a(1+log(2+|t|)).
```

Every raw delayed rational Cauchy input decays like `|t|^-3`; its forced
resident projection from `L-91323` therefore lies in `L2(omega dt)` and hence
in the corrected domain

```text
D_a = {g in K_(Theta_a):
       (a partial_a log Theta_a) U_(Theta_a) g in L2}.
```

All finite carrier-delay packets, the reflected packet, and the canonical
bridge orientation components lie in this domain. The corrected factorization
is therefore legitimate on the actual conclusion-producing physical core.

## IV. Exact regression

```text
experiments/X-91324-renormalized-fisher-feature/
PASS_RENORMALIZED_FISHER_FEATURE
```

The verifier uses exact Gaussian-rational arithmetic on a three-point
exponential family and checks:

```text
k=varphi h;
centering of h and k;
score-covariance derivative identity;
K=|varphi|^2 H;
pointwise |k|^2<=4.
```

No floating point is used. The analytic exponential-growth obstruction and
the weighted-domain theorem are proved symbolically, not inferred from this
finite regression.

## V. Correct live endpoint

```text
delayed half-line form core                         PROVED
canonical two-sided bridge geometry                 PROVED
resident raw-delay dilation                         PROVED
full physical model/amplitude/leakage colligation   PROVED
normalized Fisher-Hankel dense Bochner core         REFUTED
bounded renormalized source feature                 PROVED
reciprocal-xi Toeplitz amplification                EXPLICIT, UNBOUNDED
physical delayed Cauchy/bridge packet in domain     PROVED
structured reciprocal-xi observability estimate    OPEN / RH-BEARING
mixed-orientation and bridge arithmetic block       OPEN
Riemann Hypothesis                                  UNPROVED
```

The next proof attempt must estimate the unbounded reciprocal-xi leg on the
special score-covariance range generated by the completed source. It must not
reuse the refuted global contraction.
