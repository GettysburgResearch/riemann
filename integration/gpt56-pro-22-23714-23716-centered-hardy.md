# Integration handoff — centered base-five Hardy correction

## Frozen correction boundary

The following earlier energy targets are not part of the live proposal:

```text
T-23704 uncentered HCRE(5)    refuted by R-23706;
L-23714 centered little-o     too strong by R-23707.
```

Retain their exact algebra only where explicitly imported.

## Current front door

```text
L-23709  cumulative shell and transform
L-23715  bounded centered digital forcing
L-23716  centered digital energy descent
T-23705  centered digital Hardy proof candidate
M-23702  review protocol
```

The single open theorem is `CDHB(5)`:

```text
sup_(0<sigma<=sigma0)
sigma integral_R
 |(z+1/2)(h(z)-h(0))/z^2|^2 dtau < infinity.
```

## Required dependencies

```text
PR #241  correct reflected algebra and all-line/two-frequency scope
PR #234  Euler-aligned inverse and positive generalized primes
PR #248  endpoint-projected Green algebra and scalar-mode firewall
```

## Do not import

```text
PR #243 conditional-Hankel spline;
PR #239 bounded-rank Möbius contagion;
old T-23704 little-o energy;
global Green coercivity without scalar projection;
a simple-zero assumption;
a finite positive ladder.
```

## Exact conditional chain

```text
CDHB(5)
-> centered continuous energy O(T)
-> centered discrete boundary energy O(log y)
-> digital descent Z(y)>=-O(sqrt(log y))
-> C(y)=a5 log y+Z(y)>0 eventually
-> Landau pole exclusion
-> RH.
```

## Production obligation

The missing proof object must bind the complete `p=5` source to a reflected Hermitian reserve and prove a strict anchored Hardy/Poincare inequality with one uniform constant. If the target norm appears unchanged on the forcing side, the proof has not advanced beyond a tautology.
