# Integration handoff — dyadic parity-dipole source-specific proposal

Branch:

```text
agent/gpt56-pro-source-specific/262-dyadic-parity-dipole
```

## Import order

1. PR #241 `L-9518` — two-frequency reflected physical block.
2. PR #248 `L-24501`, `L-24517`, `L-24520` — carry/prime transport algebra.
3. PR #236 `L-23011`, `L-23012` — dyadic digital source.
4. `L-26201` — exact opposite-parity carry dipole.
5. `L-26202` — positive inverse, Selberg data, and digital collapse.
6. `L-26203` — exact bipartite transport and Hall dual.
7. `O-26201` — logarithmic scalar firewall.
8. `T-26201` — proposed full chain.
9. `M-26201` and `X-26201` — review and finite replay.

## What is new

The source

```text
omega_2=mu-(3/2)tau_2 mu+(1/2)tau_4 mu
```

simultaneously gives:

```text
compact carry dipole on [m,4m),
positive Dirichlet inverse,
positive generalized prime weights,
three-atom binary-digit dual,
stable equivalence to the dyadic Mertens shell.
```

The exact positive block

```text
delta b_m=t 1_(A<m<=B),  A<B, t>=0
```

moves prime residual from divisors of `A` to divisors of `B`, preserves carry
coefficient nonnegativity, and increases the objective.

## Proof boundary

The finite algebra and the transport-to-RH composition are proposed complete.
The symbolic Reflected Dyadic Hall reserve identity is open and RH-bearing.

Do not merge this proposal as a proof of RH unless that identity is emitted,
reviewed, and shown to reproduce the logarithmic prime-ramp witness exactly.
