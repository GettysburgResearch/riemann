# Cumulative account: focused preservation and reading-path follow-up

Baseline: `ea4da0b4993c9b25e5a2abdd73c0186a6be581da` (the merged cumulative account).
Scope: editorial preservation, current reading paths and bounded verifier tests.
No new mathematical acceptance, strengthened theorem, or exhaustive review is implied.

## What was checked

The remote comparison from `81b4fd9453c3f75c76cec66b8f79d96904a7a316` to this baseline contains exactly fifteen Markdown paths: thirteen modifications and two additions, with no deletion or move. Original mathematical proofs, claim/edge tables, source freezes, review archives, certificates, trusted Lean code and workflows were not removed or modified by that rewrite.

The follow-up compared the cumulative results/index and current statement guide with the earlier research index, integrated family inventory, source/correction records and the later analytic supplement. It checked the major arithmetic, fixed-detector, wavelet/Q4, xi, operator/heat, family and certificate reading routes. This was a targeted human-account check, not a census of every unreviewed branch or an independent reconstruction of every proof.

## Omissions and ambiguities repaired

- High-derivative safe-ray entry, finite pole selectors and the fifth Conrey trial are now explicit in the results/index. Controlling scope: `reviews/A/supplement/CLAIMS.tsv`, AS-012–021, and `REPORT.md` S04–S06. The frozen companion parameter, stated high-order regime, analytic imports and missing low-order descent remain essential.
- External comparison work is discoverable by name: LongGapsBetweenPrimes, PrimeGaps186, Lamzouri interfaces and the conditional Catalan majorant critique. Controlling scope: the same supplement, AS-022–030 and S07–S10. A reconstructed paper chain is not a fresh formal build; the three PrimeGaps186 inputs are not discharged; the Catalan critique is neither a rationality nor an irrationality result. These entries do not claim the external theorems as newly authored project results.
- The frozen Robin certificate programme through `10^54` is linked again. Its earlier `VERIFIED WITH FIXES` disposition in `integration/2026-08-01/ledger/batch-A.tsv` is retained alongside the later restriction in `reviews/D-pass4/INTEGRATOR_ACTIONS.tsv`. Missing complete streams and shared arithmetic-backend assurance are not waived. No enlarged traversal range is accepted by this editorial change.
- The undated xi, heat and conjunctive manifests now begin with their current qualifications. Their original bodies and historical status cells are retained verbatim after the notice. The notices prevent the old point-spectrum, whole-complement and conditional-premise shorthand from overriding the corrected human account.
- The verifier's default navigation list now includes `research/README.md` and `research/integrated/CURRENT_RESULTS.md` (26 pages rather than 24). Previously the README linked to the new statement guide, but the guide's own links were not in the default scan. All review/history/payload pins and the graph resolver are unchanged.

## Executed checks and their boundary

The exact originals of the five edited scientific/navigation documents and both checker files were retrieved and matched to their Git blob hashes before editing. Local comparison confirms that the results and research-index changes are insertions only, and that each of the three historical manifest bodies is retained byte-for-byte. Edited Markdown was checked for whitespace and display-math delimiter errors; newly added source destinations and explicit anchor references were checked against the retrieved source records. This is not an external-link or all-history crawl.

All **31 synthetic hardening tests** pass in normal and optimized Python on Linux, with identical JSON results and no skips. The two new tests enforce inclusion of the cumulative pages in both navigation and authentication scope, and demonstrate that a broken link *inside* the statement guide fails the real default scanner. Running the new tests against the exact old wrapper produces the expected failures; they are not merely new tests that would also pass with the omission present.

```json
{"tests":31,"failures":0,"errors":0,"skipped":0,"marker":"PASS_SYNTHETIC_HARDENING_CONTRACTS","real_riemann_checkout":false,"remote_ci":false,"lean_build":false,"rh_proved":false}
```

The hardening checksum manifest is updated only for the two changed checker files. Earlier execution receipts remain historical rather than being relabeled as new runs.

Direct Git access in this runtime failed DNS resolution. No complete Riemann checkout was obtained, so no new `PASS_AUTHENTICATED_SCOPED_RELEASE`, native Windows run, Lean build, historical numerical campaign or CI result is claimed. The earlier full-checkout receipt in PR #802 remains tied to its tested tree, not this follow-up. The supplied default full-checkout command will additionally authenticate and scan the cumulative guide when run on the new committed checkout.

Repository visibility, licensing, access controls, Actions settings, active research branches, mathematical registries and the scientific acceptance baseline are unchanged. RH remains unproved.
