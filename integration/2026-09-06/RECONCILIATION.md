# Reconciliation decisions and precedence

## What controls

Use the exact source version, not the newest PR title or the strongest status word. A later reconstruction closes an earlier hold only for the identified statement, source, arithmetic class and execution. Preserve old failures and unperformed tests. A new certificate may replace evidence without making the old verifier valid.

A and B's review units, C's census rows, D's multi-pass dispositions, inspected files and executed fixtures have different denominators. They are not added into a theorem count. Review claim IDs are qualified by packet; original source IDs require PR/head/path. The machine record retains all raw fields.

## Resolved intersections

**A and C: seven-point record.** The C3 critique of X105560 remains valid. A's supplement supplies a different independent continuum certificate and the relevant analytic reconstruction. The corrected 269 expression is retained; no stale original wrapper or unrun external certificate is rehabilitated. The ratio/proportion theorem retains all analytic premises.

**A/B/C: authorship.** C4 explicitly discloses prior A authorship. It is not a second independent acceptance of A or PR793/Architecture E. B excludes its own PR790. B supplies a non-author pass7 review of PR793; C4 supplies non-author review of the two named PR792 operator proofs. Do not enlarge either reading boundary to a whole branch. The integrator separately reconstructed the C4 entire-Xi normalization, conditional prefix/curvature/Pick argument, and the exact finite algebra identified in its new replay.

**C and D: formal status.** D's inspection of conditional consumer types and old runner evidence does not answer C's source-nonvacuity objection. Preserve the valid conditional formal theorem and its historical build record, but block the claim that its old actual-Xi input is an inhabited implementation. The new paper repair and uncompiled candidates remain separate from trusted Lean code.

**D passes 1–4: baseline repairs.** The final action list adds D-F16–18; it does not erase D-F01–15. The suffix-field and corrected Poisson/Hardy arguments address different wavelet energies. The new P61 campaign replaces the old enclosure and evidence. Pick68's locator repair plus finite-box reconstruction does not create a new primitive high-ordinate computation.

**A: claimed refutations.** Refutations receive the same scrutiny as positive theorems. The completed Euler–Beta semiprime obstruction is source-specific. The alleged cofinal 90-percent-gate refutation is not accepted from the false lower inequality and finite phase data. Its exact mesh algebra and diagnostics survive.

**B: large structural packets.** Retain generic/specialized degree distinctions, the torsion exception, corrected HPL sign and higher-page zigzags, integrated-density replacement and precise graph cutoff. Do not promote the held native Poincare precision claims, Epstein numerical certification, or unrerun Chow/census data to unconditional current theorems.

## Baseline and after-freeze coverage

The old 139-claim/36-edge release is immutable. The current overlay adds repairs or blocks original uses; it never strengthens inherited reachability. The new unbound frontier edges are explicitly nontraversable. Detailed reviewer edge records stay available for the next exact adapter-level extraction instead of being joined through ambiguous numerical identifiers.

C's broad coverage, branch-only/attachment archaeology and transitive dependency work remain as recorded. A's later #568/#599 source-specific work does not turn all earlier sources into fresh reviews. Later changes after the exact frozen heads belong to the next delta. This is an explicit exclusion, not an inference that absent work is false or nonexistent.
