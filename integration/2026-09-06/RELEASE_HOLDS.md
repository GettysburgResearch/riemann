# Remaining holds — do not conflate their categories

## Scientific/source backlog

The native arithmetic signs, coefficient-resolved all-order positivity, low-order Xi descent and principal binding are open research, not packaging failures. The native Poincare precision and specified analytic-source adapters remain proof/review holds. Epstein directed endpoints and full contours, Chow-map closure, torsion/graph censuses, native Gram collectors and unreplayed source suites retain their exact evidence limitations. Architecture-E and PR790 components need the independent review actually required by their source-specific verdicts, not an assumption based on other agents' agreement.

C's complete post-release claim split, branch-only/attachment census and transitive source closure were not completed. The current summaries make no claim of exhaustive fresh scientific coverage. Public exploration need not wait for every theorem to be proved; unreviewed material must stay labeled.

## Formal implementation before a new formal release

Replace the empty raw actual-Xi input and its off-line enumeration, construct the complete corrected inputs and consumers together, implement the new analytic propositions, bind imported sources, regenerate registries/locks and perform the exact Lean build, comparator/type and axiom audit. Candidate files in `reviews/` are not trusted imports. Historical builds are not new executions of this repaired source.

## Owner-only public-launch checks

Confirm repository-history secrets/PII review, imported-material licensing/attribution/NOTICE and any paper redistribution rights; verify intended collaborator/team access and bypass permissions; configure and test the desired PR/code-owner/force-push/deletion protections; verify the public contributor workflow. These checks were not completed by this integration, and no visibility, permissions or branch settings were changed.

The absence of a completed audit is not evidence of a discovered secret or a rights violation. This file records what is unverified. Scientific review does not authorize a privileged workflow or settle access policy.

## Execution boundary

This integrator's bounded rational and rejection checks are in [VALIDATION](VALIDATION.md). Do not treat a workflow definition, an unobserved run, or a source hash alone as successful full-checkout validation. Existing source-reading and reviewer-run receipts remain separate from fresh execution.
