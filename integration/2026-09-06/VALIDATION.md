# Validation record and precise execution scope

## Executed locally in this integration

The integrator ran, from the new source bytes:

```text
python3 independent_fixtures.py
python3 -O independent_fixtures.py
python3 -I -S validate.py --payload-only --output <normal-output>
python3 -I -S -O validate.py --payload-only --output <optimized-output>
```

The normal and optimized mathematical outputs are byte-identical. There are **576 exact rational/integer fixtures**: entire normalization and endpoint distinctions; constant reserve budget; local paid-orbit jets; source monotonicity; three-node determinant factorization; empty/finite off-line model PSD including half-nodes and repetitions; Farkas sign; principal-minor criterion; varying-order filter counterexample; Poisson boundary normalization; Galerkin upper-bound direction; elementary prime sign; and 400 finite P61 coefficient comparisons through n=200.

The validator also rejects **14 payload corruptions, two malformed JSON inputs and two broken graph controls**, in both interpreter modes. These exercise absent reviews/findings, duplicate identifiers, false status flags, Boolean/float aliases, missing evidence, unsafe paths, a silently traversable frontier and a collapsed or dangling producer premise. A count is not a theorem tally.

Receipts are in `validation/independent.normal.json`, `validation/independent.optimized.json`, `validation/payload.normal.json`, and `validation/payload.optimized.json`. These record the limited mode explicitly. No floating-point computation is presented as directed certification.

## Remote source authentication

The seven archived review trees are grafted by their exact Git tree identities from A #795, B #796, C #798 and all four D #799 passes. The ten mathematical proof extracts are copied by exact existing blob identities. Historical research branches and the previous dated release are unchanged. The final publication receipt identifies the actual integration head/tree and readback, separately from self-referential committed files.

## Full-checkout command provided, not claimed executed here

```text
python3 -I -S integration/2026-09-06/validate.py --output /tmp/riemann-current
python3 -I -S -O integration/2026-09-06/validate.py --output /tmp/riemann-current-O
diff -r /tmp/riemann-current /tmp/riemann-current-O
```

This mode authenticates actual checkout review trees, the historical 139-row claim and 36-row edge blobs, evidence files, proof copies, review tables and census; resolves the conservative current claim/edge view; and verifies that corrected reachability is a subset of prior reachability with no reviewed-only path to RH. It emits complete current registries rather than silently modifying historical tables.

A bounded read-only workflow is supplied with pinned actions, exact-head checkout, no persistent credentials and no submodules. In this integration session GitHub returned **no workflow run** for the staging PR or its explicit integration-branch push. A workflow definition is not a successful run. Unless a later exact-head receipt explicitly supersedes this paragraph, full-checkout validation and remote CI are **NOT EXECUTED / NOT OBSERVED**, not PASS. The local runtime did not contain a complete authenticated checkout.

## Not executed or inferred

No Lean build, comparator, independent-kernel or fresh axiom audit; no high-ordinate Xi primitive production; no P61 million-endpoint/divisor-tail rerun; no full Chow, Epstein, graph, torsion or native Gram campaign; no complete transitive-source archaeology; and no public-launch clearance. Reviewer-run receipts are preserved and attributed, not added to the integrator's fresh execution count.

The current graph policy has a separate elementary safety argument: this integration changes inherited proved statuses only to equally or more restrictive states and disables, never adds, traversable edges. Its new frontier edges are nontraversable. Hence it cannot introduce a new reviewed-only path that was absent from the unchanged prior graph. This monotonicity argument does not claim execution of the full new resolver.
