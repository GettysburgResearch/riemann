# Integrator and successor handoff — live PR #481 intended-chain review

## Freeze

```text
review cutoff UTC:      2026-08-15T00:55:54Z
main at review start:   9c7538559d7f56c2914b39aed5a1fb3fbf7ce131
proposal PR:            #481
proposal branch:        research/gpt56-pro/91726-pr476-native-slack-repair
reviewed proposal head: 005ae49723898d8661d407a0433a6d69cb6d6efc
proposal base:          518b6a5ec2b49b7decbd4c2e349d0ee5b26bfc9b
```

## Headline disposition

```text
actual-mass child target contraction         CLOSED WITH MINOR PROVENANCE FIX
small physical columns q<K                   CLOSED ARITHMETICALLY
activation-knot score removal                CLOSED MEASURE-THEORETICALLY
physical root target mass                    <3020 ON FROZEN MEASURE INPUT
native slack cocycle                         EXACT CONDITIONAL
positive descendant envelope                 EXACT CONDITIONAL
fully corrected root packet equality         OPEN / FIRST PRODUCER GAP
complete common-port demand                  OPEN
terminal/base/port exact Y4 pairing           OPEN
J_Lambda-4sqrt(X) <4log(X) bridge             RH-BEARING / CIRCULAR INPUT
external endpoint chain transitive lock       OPEN
T-91724/T-91725                               UNPROVEN / GAP
Riemann Hypothesis                            UNPROVEN
```

## Intended chain

Use the dedicated file

```text
integration/2026-08-15/pr481-intended-proof-dag.md
```

as the canonical mathematical graph. Do not use PR #480, PR #482, `R-91726`, or `R-91727` as theorem antecedents. They are review and lifecycle records only.

## Strongest verified live repairs

1. `L-91732` derives the actual weighted child target-mass premise and supplies both aggregate-list and variable-list normalized forms.
2. `L-91733` gives a genuine retained-cell cumulative mismatch seed and closes every integer physical column at the arithmetic-bound level.
3. `L-91734` uses absolute continuity rather than an unsupported pointwise score majorant at activation knots.
4. `L-91737` proves a physical target-mass bound below `3020` on the frozen endpoint-measure interpretation.
5. `L-91727/L-91736` give the correct native slack cocycle once the root identity is supplied.
6. The positive descendants correctly use `T-91312`; the root endpoint frame is not recursively recharged.

## First theorem to produce

Construct one explicit complete packet equality

\[
\mathcal N_X
=\mathcal C_X^{\rm fully\ corrected}
+\mathcal R_X^{\rm root\ slack}
+\sum_b\beta_bU_b\mathcal P_b
\]

with all of the following shown in the same formulas:

```text
source occurrence ownership;
component row;
ordinary response at q;
ordinary response at 4q;
radix-four detail;
literal score;
terminal and finite-base packets;
complete 2x2 common-port demand;
full child capacities;
nonnegative root slack.
```

This theorem must reconcile partial activation collars with the retained-cell quadrature seed. A collar in the quotient variable generally cuts an integer endpoint cell, so the full-cell error `epsilon_X(n)` is not automatically the error of the retained subcell.

## Root-cost instruction

Do not use

\[
J_\Lambda(X)-4\sqrt X<4\log X
\]

as an unconditional bridge. The review audit proves that any eventual one-sided `O(log X)` upper bound of this form implies RH by the Mellin transform and Landau’s theorem.

Instead compute

\[
\delta_{\rm root}(X)=\langle Y_4,r_X\rangle
\]

directly from the exact root slack vector. This requires explicit `Y_4` pairing of:

```text
retained mismatch and collar;
terminal omission/taper;
finite base packet;
complete common-port correction;
any remaining refinement/correction packet.
```

## Common-port instruction

The abstract statement

\[
0\preceq D_s\preceq P_s
\Longrightarrow
0\preceq\int D_s\preceq\int P_s
\]

is verified. The missing object is `D_s`: export the exact matrix demand of every current correction and prove their sum is the demand covered by the one uncolored root port. Do not infer a matrix-port inequality from scalar ordinary/detail overfill estimates.

## Child normalization instruction

Prefer the variable-list actual-mass form in `L-91732`. If the aggregate global-list form is retained, define each child operator on the exact first-owner source slice; “prime active” must not export one rough monomial to two same-generation children.

## Endpoint chain instruction

Freeze and reconstruct the exact external files:

```text
PR #353 L-90020 and T-90011;
PR #352 L-90004 and T-90008;
all PNT, meromorphic-continuation, contour, Landau and endpoint-interpolation dependencies.
```

The finite dual `F_Lambda<=Delta` has the correct orientation. The external chain is not reached until the producer closes.

## Integration recommendation

```text
integrate local algebraic repairs as route infrastructure;
do not integrate T-91724/T-91725 as proof-level theorems;
retain PR #482 as a frozen review of the earlier head only;
retain the benchmark-bridge audit as an urgent circularity firewall;
request one successor containing the explicit root packet and direct Y4 audit.
```

## Review packet

```text
reports/integration-wave/20260815-pr481-intended-chain-review.md
audits/integration-wave/20260815-pr481-claim-status.tsv
audits/integration-wave/20260815-pr481-benchmark-bridge-circularity.md
integration/2026-08-15/pr481-intended-proof-dag.md
integration/2026-08-15/pr481-intended-chain-review-handoff.md
```
