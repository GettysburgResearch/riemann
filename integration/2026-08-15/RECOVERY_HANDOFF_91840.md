# Recovery handoff — T-91840

The earlier report of branch
`research/gpt56-pro/91840-pr484-exact-root-composition` was not backed by a
remote ref. This packet is the recovered durable version.

It is intentionally stacked on PR #487 rather than the stale PR #481 head so
that exact Hall integration, whole-cell support and the bounded one-shot native
slack are inherited rather than duplicated.

Review the new files in numerical order and then reconstruct the exact imports
listed in `t91840-pr484-exact-root-composition-lock.json`.
