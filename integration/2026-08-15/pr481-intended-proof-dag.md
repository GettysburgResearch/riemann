# Canonical intended proof DAG for the live PR #481 packet

**Proposal head:** `005ae49723898d8661d407a0433a6d69cb6d6efc`  
**Purpose:** separate the intended mathematics from historical review-response lineage.

## Lifecycle rule

PRs #480 and #482 are reviews of earlier frozen heads. Their claims may be cited as falsifiers or lifecycle explanations, but they are not antecedents of the mathematical proof.

The controlling mathematical chain should be read as follows.

```text
A. Native endpoint datum

L-91377 full Möbius row
  -> ordinary capacity w_X
  -> detail capacity Omega_X
  -> literal benchmark J_Lambda(X)

L-91378 positive radix-four dual
  -> Delta_X(d)=J_Lambda(X)-H(d)
  -> Delta_X(d)=sum_q Y4(q)[Omega_X(q)-Xi_d(q)]

B. Positive factor-67 root fibers

L-91107 endpoint density L(x)
  + L-91682 normalized row monotonicity
  + L-91690 factor-67 target Hall
  + L-91688 first-owner rough provenance
  -> positive target-exact residual source
  -> nonnegative Hall row bonus
  -> score superordination
  -> exact total-row transparency

L-91674 positive endpoint integration
  -> one labelled common parent measure

C. One-time finite native root realization

bottom/top/knot restrictions
  + L-91734 retained-cell positive refinement
  + one global quantizer
  + L-91733 retained-cell mismatch seed
  + square-root thinning
  + terminal omission
  + finite base packet
  + L-91725 one common uncolored port
  -> [OPEN PRODUCER]

Required exact output:

Omega_X
 = Xi(d_X^cur)
   + r_X
   + sum_b beta_b U_b Omega(P_b),
r_X>=0.

This is the first live open arrow.

D. Actual-mass child decomposition

L-91650 exact causal coefficients
  + L-91658 same-index child functor
  + L-91375 target nonexpansivity
  + L-91732 target-mass Tonelli/normalization
  -> source-owned children
  -> Y_b<=X/67+C0
  -> sum_b beta_b<1/8

E. Exact native slack cocycle

required root identity
  + L-91727/L-91736
  -> s_X=r_X+sum_b beta_b U_b s_b
  -> Delta_X=delta_root(X)+sum_b beta_b Delta_b

F. Root and descendant costs

L-91737 m(root)<3020
  + T-91312 positive causal descendant envelope
  -> complete descendant contribution O(1)

Required noncircular root theorem:
  delta_root(X)=<Y4,r_X>=O(log X)

Current L-91735 route through
  J_Lambda(X)-4sqrt(X)<4log(X)
  is RH-bearing and cannot be used as a pre-RH input.

G. Endpoint conclusion

Delta_X=o(log^2 X)
  + T-91313 finite dual
  -> F_Lambda(X)=o(log^2 X)

L-90020/T-90011 prime-square moat
  -> prime endpoint eventually negative

L-90004/T-90008 Mellin-Landau consumer
  -> no zero with Re rho>1/2

functional equation
  -> RH.
```

## Exact current status

```text
native datum and Y4 dual                         VERIFIED
factor-67 local Hall/source algebra              VERIFIED / FROZEN INPUTS
actual-mass child contraction                    VERIFIED WITH FIXES
all-column local constants                       VERIFIED WITH FIXES
activation collar score theorem                  VERIFIED WITH FIXES
root physical target mass <3020                  VERIFIED WITH FIXES
native slack cocycle                             VERIFIED CONDITIONAL
positive descendant envelope                     VERIFIED CONDITIONAL
fully corrected root identity                    UNPROVEN / GAP
complete common-port demand                      UNPROVEN / GAP
terminal/base/port exact Y4 cost                  UNPROVEN / GAP
J_Lambda-4sqrt(X) benchmark bridge                RH-BEARING / UNPROVEN
external endpoint chain lock                      INCOMPLETE
T-91724/T-91725                                   UNPROVEN / GAP
Riemann Hypothesis                                UNPROVEN
```
