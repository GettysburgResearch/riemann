# Integration handoff — boundary-source cancellation

Branch:

```text
research/gpt56-sol/304-boundary-source-cancellation
```

Parent:

```text
PR #304 @ 78b75fc17e27334a9950018528c1c6e083d74820
```

## Disposition

Do not use PR #304 as an RH proof.  `R-30501` proves that the stopped-power
boundary divisor source has square-root atomic norm at least `N/240`.

Retain:

```text
PR #286 shifted analytic tower and 6/7 interior reserve;
PR #272 exact finite central saturation and Cycle-Debt consumer;
PR #303 source/edge type correction;
PR #304 adjacent-tree identities and O(sqrt(n)) single-atom map.
```

Replace the positive stopped-layer/terminal-atomic composition by:

```text
complete logarithmic endpoint profile
-> exact finite shifted log operator
-> cancellation before Möbius inversion or positive parts
-> weighted log-jet renewal.
```

## New exact entries

```text
R-30501  linear atomic boundary lower bound
L-30501  exact central entropy objective
L-30502  exact normalized finite operator and 9/10 reserve
T-30501  CPLJR conditional completion
X-30501  exact rational certificates
```

## Next mathematical target

Construct the complete finite upper-triangular derivative/endpoint transition
matrix in the state of `T-30501`.  Its two diagonal reserves are already
strict:

```text
current value     9/10
faster powers     6/7
```

The remaining work is the explicit coupling, not review reconstruction of an
undeclared source map.  Until that matrix is proved, RH remains unproved.
