## Checkpoint H — boundary-tame triangular Wick frontier

The zero-free exponential `K=2` factor crosses 90% in the frozen model but
introduces a stronger horizontal-growth cost.  This checkpoint replaces it by
the triangular source unit

```text
P_2(x)=1-x/2-x^2/8.
```

In every finite nilpotent projected source algebra,

```text
P_2(x)^2/(1-x)=1+x^3/8+(9/64) sum_{m>=4} x^m,
```

so degrees one and two vanish by an exact invertible congruence.  The remaining
one-sided energy is `<1/7000`, giving frozen reserve `>3500/3501`.  A 99/101
actual-Xi transfer would imply

```text
liminf N0/N >= 3654811/3968189
              = 0.921027451...
```

The pointwise-polynomial shortcut is explicitly rejected: `P_2` has zeros.
The open gate `TRIWXFER105510` is the projected-source-to-actual-contour edge,
one/two-copy Gram transport, and final 99/101 error budget.  Ninety percent,
the public record, and RH remain unproved.
