# T-105520 — physical half-order normalization and the signed strip-transfer cut

This packet is a hostile continuation of the conditional ninety-percent work on
PR #726.

It proves four things.

1. The frozen Hermitian symbol must contain the half-order factor
   `n^(-1/2-it)`.  The earlier display without `n^-1/2` was inconsistent with
   its own mean-square formula.
2. If the physical product cutoff is `exp(alpha L)`, the prime-simplex energy
   is `D_K(alpha)`, not the unscaled `D_K(1)`.
3. At the full physical scale `alpha=2`, the degree-four square-root Wick
   polynomial still has residual energy below `1/7000`.
4. The safe-line resolvent has an exact accretive factorization.  A one-sided
   negative-trace estimate for the strip-transfer remainder, rather than a
   two-sided 99/101 comparison, is enough for ninety percent.

The packet does **not** prove that negative-trace estimate.  Ninety percent and
RH remain unproved.
