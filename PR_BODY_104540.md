## T104540 fixed-order exactification

The previous T104530 theorem is a valid conditional API but does not
unconditionally convert the known `xi'''` line proportion into a `xi''`
proportion.

The exact historical disposition is:

```text
Conrey alpha_2 > 0.9584   (xi'')
Conrey alpha_3 > 0.9873   (xi''')
```

The second-derivative bound is direct.  A proportion-only reverse implication
is impossible even for even real Cartwright entire functions, by
`f(z)=2+cos z`.

Combining both independent Conrey rows with the exact factor-two reverse-Rolle
identity proves that at least `9665/9873 > 97.8932%` of the certified line zeros
of `xi'''` are correctly oriented Rolle-generating extrema for `xi''`.

`RCMV104530` and RH remain open.
