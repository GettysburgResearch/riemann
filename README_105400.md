# T-105400 — F1 Frobenius–Hodge execution branch

Exact base:

```text
programme PR #727
71782530d175162040a8c182a4cb1602d2b23821
```

This is the dedicated theorem-producing continuation and will remain open for
several passes.

Checkpoint 1 proves:

```text
native Euler source = twisted prime-box corner boundary;
activation = tropical section of the fixed source box;
completion tangent = one divisor coefficient per labelled prime;
least/greatest owner = opposite shellings of one boundary;
finite source-twisted toric Hodge index = exact;
uniform normalized primitive gap = 1;
three filtered SHARP rays = one primitive mixed intersection;
optimal trace constant = 769/2.
```

The sole remaining theorem in this first geometry is the cofinal arithmetic
trace/occupancy estimate `F1PE105403`, equivalently subpower packing of the
root-free primitive three-ray energy.

```text
F1PE105403: OPEN / RH-BEARING
Riemann Hypothesis: UNPROVED
```
