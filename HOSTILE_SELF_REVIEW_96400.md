# Hostile self-review — T-96400

## Disposition of the requested blocker theorem

The packet does not assert `B_X=o(log^4 X)`.  `B_X` belongs to the packing
compiler, while the full arithmetic loss contains the independent term
`F_Lambda=J_Lambda-P_Lambda`.  Even a proof of NEDB would not close RH through
that consumer.

## First load-bearing new theorem

The first genuinely new arrow is `L-96400`: the signed stopping-line expansion
must be an exact projective incidence identity, with each debit/credit edge
cancelled before source labels are forgotten.  A mere coefficient-sum or
schema replay does not prove this.

## Second load-bearing new theorem

`L-96401` must use the same terminal Hall coefficients in target, score, row 2
and row 3.  The forced `q=2` score obstruction remains mandatory: a row-only
bonus cannot be promoted to a complete source packet.

## Downstream theorem

The fixed-row Mellin calculation is useful only after eventual one-sign is
established.  The no-common-zero polynomial identity alone does not imply RH.

## Status

`T-96400` is a complete candidate proof proposal for reconstruction, not an
accepted proof.  Any failure of either source-incidence equality or terminal
two-row positivity rejects the conclusion.
