# T102100 — Carrier-free staircase matrix

This packet continues the joint quadratic–wavelet matrix of PR #697 and the audited double-owner/coboundary work of PRs #691 and #695.

Main new results:

```text
physical survival-weighted interval matrix = exact mixed coboundary;
monotone supercritical matrix regions telescope to one-owner boundaries;
certified cubic Harnack constant improves 4/3 to 17/16;
positive interval width improves A<e^(3/4) to A<e^(16/17);
one-prime Harnack cone iteration is refuted by a directed exact fixture;
a common safe filter removes carriers and Type-I terms in both bridge channels;
both channels are differential coordinates of one positive five-box spline;
one vector Vaughan identity leaves one balanced two-channel source packet.
```

The remaining theorem is `CFBB102100`, a source-faithful subcritical 2x2 Perron estimate on that balanced vector. It is open. RH is unproved.
