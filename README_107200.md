# T-107200 — Xi Cauchy-layer defect and saddle concentration

This packet is stacked on PR #767 at
`cdad9e88097db4925c3bbbf731f7d1ae1a9e6c57`.

It proves:

- an exact Cauchy-layer representation of the complete multiplicity-sensitive
  reverse–Rolle defect;
- the exact logarithmic-curvature flux under differentiation;
- the positive-frequency Xi derivative saddle and Gaussian concentration;
- simple real zeros of high Xi derivatives through a growing full-critical-
  strip window.

It reduces the remaining fixed-order \(90\%\) theorem to the exact screened
curvature estimate `XISCREEN107200`.  That estimate, \(90\%\), density one and
RH remain unproved.

Replay:

```bash
python3 experiments/X-107200-xi-cauchy-saddle/verify.py \
  --output experiments/X-107200-xi-cauchy-saddle/results/verification.json
sha256sum -c T107200_CONTENT_SHA256SUMS
```
