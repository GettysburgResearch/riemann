# T-104100 root-return / root-free packing quotient

This is the collision-safe recovery of the previously unpublished two-key
implication-matrix packet.  It is reconstructed on the current audited
joint-matrix genealogy, not presented as a byte-identical migration.

The mathematical core is the exact scalar absorption

```text
Q=|g|^2+E,
|g|^2 <= theta Q+R,
0<=theta<1

=>

(1-theta)|g|^2 <= E+R.
```

Consequently, a subpower reciprocal moat together with subpower root-free
packing gives subpower negative mass for one fixed detector.  The inherited
Mellin--Landau consumer then gives RH.

The two arithmetic inputs `SORR104100` and `RFCP104100` remain open.  RH is not
proved.

Replay:

```bash
python3 experiments/X-104100-root-return-quotient/verify.py
python3 experiments/X-104100-root-return-quotient/tests/test_verify.py
sha256sum -c T104100_CONTENT_SHA256SUMS
```
