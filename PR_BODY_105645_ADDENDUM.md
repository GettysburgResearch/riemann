## T105645 addendum — balanced parent/derivative Turán spectral flow

**The Riemann Hypothesis remains unproved.**

For every real entire `F`,

```text
(F'^2-FF'')/(FF') = d/dz log(F/F').
```

The two-rung height all-pass

```text
R_(F,H)
 = F(x+iH)F'(x-iH)/(F(x-iH)F'(x+iH))
```

has degree

```text
N_(F')(H)-N_F(H)
```

and phase velocity

```text
partial_H arg R_(F,H)
 =2 Re[(F'^2-FF'')/(FF')](x+iH).
```

Hence parent zeros and derivative zeros are opposite residues of one actual
Turán current.  At a boundary crossing, the continuous phase flow is completed
by the exact rank-one/confluent model-space atom from T105640.

For Xi, the numerator is the already-proved positive exterior-square source.
The remaining theorem is the signed physical estimate before division by
`Xi Xi'` and before source/endpoint pieces are separated:

```text
BSTF105645 — balanced signed Turan flow.
```

This replaces a separate parent-atom gate and a separate derivative-collar gate
by one signed parent-minus-derivative spectral flow.  The physical denominator
phase remains load bearing; RH is not claimed.
