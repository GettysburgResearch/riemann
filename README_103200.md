# T-103200 — XD/CV carrier-transfer hardening

This packet records the exact result of a direct attempt to prove the two
minimal-cut classes from PR #692.

The requested unconditional proofs do **not** survive hostile source typing.

```text
XD:
  the first matrix compared largest-prime ownership on K0 with Vaughan on K1;
  the corrected same-K1 hybrid is exact;
  every fixed notch still leaves a deterministic prime carrier of
  order sqrt(X)/log^(m+1)(X);
  taking regional norms before recombination is power-lossy.

CV:
  corrected adaptive squaring gives a positive completed corridor;
  its critical kernel is noncompact and has an inactive tail;
  moving the cutoff changes the literal source by a shifted critical packet;
  owner-budget variation does not bound that source-transfer variation.
```

The packet proves the exact normalization, carrier asymptotics, same-kernel
hybrid, moving-cutoff transfer identity, and the bridge to the
quadratic–wavelet activation channel.

```text
CPXD103200  open / RH-equivalent
SCTV103210  open / RH-equivalent
RH          unproved
```

## Publication provenance

The verified source archive used the already occupied `T-102100` namespace.
This repository copy is its mechanical `T-103200` migration; the exact map,
source-archive hash, and publisher omission are frozen in
`integration/2026-08-21/t103200-namespace-migration.json`.
