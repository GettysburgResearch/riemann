# Frozen imports for T-92930

The proposal branch is stacked on PR #500 at exact head
`d73c1e7a1a482cac31581211a84db43cc34c824e`.  Dependencies already present on
that base are pinned by Git blob SHA.  The PR #496 two-ledger/terminal-child
comparison and PR #495 native Volterra normalization are pinned to their exact
external heads in `IMPORT_MANIFEST.json`.

No moving branch prose is a theorem antecedent.  Review fails closed on any path,
blob, source-head or type mismatch.
