# Frozen imports for T-92880

All dependencies are present on the PR #488 base and pinned by Git blob SHA in `IMPORT_MANIFEST.json`. The new PR is stacked directly on that exact branch. Reviewers should fail closed if the PR #488 head moves without regenerating this manifest.
