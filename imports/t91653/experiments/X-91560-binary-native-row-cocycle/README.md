# X-91560 — binary native row cocycle

This exact replay supports `L-91560`.

It checks three logically separate layers.

## 1. One-prime three-ledger algebra

Using exact SymPy expressions, it verifies that

```text
raw arithmetic residual + exact raw child
 = controlled survival + controlled hazard
```

simultaneously in:

```text
the SHARP target;
the row-budgeted native score;
a formal literal component-row symbol.
```

It also verifies the favorable excess binary score

```text
r(1-r)(z-1).
```

## 2. Least-prime source provenance

For twelve ordered rough-prime labels, the script enumerates all `4,095`
nonempty squarefree monomials and places each in the unique bucket indexed by
its least label.  The buckets are disjoint, exhaustive, and preserve the exact
signed coefficient dictionary.

This is a finite replay of the combinatorial provenance used by the controlled
cocycle induction.  It is not a proof of any analytic Hall inequality.

## 3. Signed-source linearity

A small exact rational test verifies that the pointwise cocycle remains exact
after multiplication by signed source coefficients and summation.

```bash
python3 verify.py
```

The replay does not certify the imported Hall margins, physical component-row
values, live-parent normalization, final capacity, or RH.
