# Frozen inputs for T-91850

These files are exact Git-blob copies from immutable commits recorded per file in `IMPORT_MANIFEST.json`: fifteen from frozen PR #488 head `9acd381fa168db02a03646ab16851daebbf4d0fd`, plus three historical dependencies already pinned by the PR #488 provenance locks. They are included so a reviewer can reconstruct the interfaces without following moving branches. Vendoring authenticates provenance, not correctness.

Count: `18`.
