# Frozen imports for T-93900

The branch is stacked on exact PR #507 head
`dfaa70cd2eefcabbf6717e3da060792904c7f357`. Cross-branch theorem inputs are
pinned by path, commit, and Git blob SHA in `IMPORT_MANIFEST.json`. A copied
blob is a freeze, not independent proof; reviewers must reconstruct the stated
scope.
