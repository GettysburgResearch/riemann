# Frozen inputs for T-94100

The successor is based on PR #511 at the exact immutable head recorded in
`IMPORT_MANIFEST.json`. The new finite endpoint-detail compiler is proved in the
packet and does not import the factor-67 Hall, Target-Lorenz tail, endpoint
quantizer, child-capacity, or port producers. The imported files fix the native
normalization, carry coefficient, positive dual, and conditional endpoint
consumer.

The `R-92940` review-side obstruction is pinned by exact commit and blob. No
conclusion depends on treating that refutation as a positive producer.
