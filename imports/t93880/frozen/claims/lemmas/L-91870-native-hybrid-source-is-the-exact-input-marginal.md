# L-91870 — The native hybrid finite/Volterra source is the exact arithmetic input marginal

Claim ID: `L-91870`  
Status: **PROVED EXACT SOURCE/OBSERVATION IDENTITY**  
Created: 2026-08-15  
Primary frozen input: `L-91760` at PR #495 head `50f45b46cbe3c471d6e702c41c7ef178b530e1ab`  
RH status: **unproved**

## 1. Native signed occurrence space

For `X>=1`, define

\[
 \ell_x(k)=k^{-1/2}\bigl(2\sqrt{x/k}-1\bigr)>0
 \qquad(k\le x),
\tag{L-91870.1}
\]

and let `p_s>=0` be the infinitesimal component packet generated by

\[
 g_s(m)=\left(\sqrt m-\frac m{\sqrt s}\right)\mathbf1_{m\le s}.
\tag{L-91870.2}
\]

The paired native source is the pair of positive measures

\[
 d\Sigma_X^\pm(s,k)
 =\frac2s\,\mathbf1_{\mu(k)=\pm1}\,
   \ell_{X/s}(k)\,ds,
 \qquad 1\le s\le X,\ k\le X/s.
\tag{L-91870.3}
\]

Each occurrence carries the complete label `(s,k,sign)`. No rough-monoid copy
has been added.

## 2. Exact native continuum observation

The one-colour Volterra identity gives, before component-row observation,

\[
 \overline b_X^\star
 =\int_1^X\frac2s
   \sum_{k\le X/s}\mu(k)\ell_{X/s}(k)g_s\,ds.
\tag{L-91870.4}
\]

Applying the positive component map yields

\[
\boxed{
 \overline c_X
 =\int p_s\,d\Sigma_X^+
  -\int p_s\,d\Sigma_X^-.
}
\tag{L-91870.5}
\]

Thus the bulk signed input marginal is exactly the retained native Möbius Volterra source. It is not `D_(P61,X)` and contains no independent reservoir copy. The complementary anchored marginal is supplied by the literal finite source in Section 4.

## 3. Exact finite/native comparison

Let `E_X=b_X^star-overline b_X^star` be the genuine finite/continuum defect.
Then

\[
\boxed{c_X=\overline c_X+\mathcal R E_X.}
\tag{L-91870.6}
\]

For every ordinary column and every radix-four detail column,

\[
\boxed{
 w_X(q)=C_{\overline c_X}(q)+v_q(E_X),
}
\tag{L-91870.7}
\]

\[
\boxed{
 \Omega_X(q)=\Xi_{\overline c_X}(q)+\mathcal D_4v_q(E_X).
}
\tag{L-91870.8}
\]

The signed defect is observation data, not positive source.

## 4. Exact hybrid finite/continuum decomposition

Let

\[
 K=\lfloor X/67\rfloor+1,
 \qquad W=10000,
\]

and let

\[
 \mathcal I_X=\{m\in\mathbb Z:K+2\le m\le X-W-3\}
\tag{L-91870.9}
\]

be the finite set of complete integer cells sent through the continuum bulk.
Split the exact finite native divergence into the disjoint cell owners

\[
 d_X^\star=d_{X,A}^\star+d_{X,I}^\star,
\qquad
 d_{X,I}^\star(m)=\mathbf1_{\mathcal I_X}(m)d_X^\star(m).
\tag{L-91870.10}
\]

Let `c_(X,A)` be the exact finite component row generated by the complementary
anchored cells. Let `bar c_(X,I)` be the Volterra component row obtained by
integrating only over the same retained cells, and let `E_X^I` be the retained
cell defect of `L-91733`. Then the native finite row has the exact hybrid
identity

\[
\boxed{
 c_X=c_{X,A}+\overline c_{X,I}+\mathcal R E_X^I.
}
\tag{L-91870.11}
\]

The positive/signed types are:

```text
anchored source:
    literal finite native occurrences (m,k) with coefficient
    |mu(k)|(km)^(-1/2) log(X/(km));

bulk source:
    native Volterra occurrences (s,k) restricted to complete cells;

comparison ledger:
    the signed retained-cell defect R E_X^I.
```

Every arithmetic colour is a native Möbius colour. No independent rough-monoid
copy is present.

## 5. Exact input-marginal firewall

The physical compiler therefore has two separately auditable native input
marginals:

1. the paired finite Möbius marginal on the anchored cells;
2. the paired Volterra Möbius marginal on the retained bulk cells.

Their observations, together with the signed defect, equal `c_X` by
(L-91870.11). The compiler may not replace either marginal by the canonical
rough lift. It may not describe the signed defect as positive source.

For the bulk Hall coupling, the odd and even incidence marginals must equal the
corresponding restrictions of `Sigma_X^-` and `Sigma_X^+`. For the anchored
stopping-line coupling, the leaf incidence marginals must equal the exact finite
native occurrences. After thinning, retained incidence plus an explicit discard
must recover each original marginal.

```text
anchored native marginal                 exact finite Mobius source
bulk native marginal                     exact Volterra Mobius source
finite native observation                anchored + bulk + signed defect
rough-lift parent substitution            forbidden
signed defect as source                   forbidden
```
