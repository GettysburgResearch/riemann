# Frozen imports

`IMPORT_MANIFEST.json` is normative. Blob and tree SHAs are copied without
reinterpretation. The one-command publisher materializes them under this
directory and verifies their Git object IDs.
