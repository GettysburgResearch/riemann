# T-94120 imports

The four JSON objects under `94020-live-source` are byte-for-byte copies of the
prior deterministic live-source packet.  Their SHA-256 values are generated in
`IMPORT_SHA256SUMS` and rechecked by X-94120.

Frozen remote theorem heads are recorded in the integration lock.  No sibling
branch is amended.
