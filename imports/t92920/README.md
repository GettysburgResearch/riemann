# Frozen imports for T-92920

The proposal is stacked on PR #500 at exact head
`d73c1e7a1a482cac31581211a84db43cc34c824e`.  PR #496 remains frozen at
`96f8a6b3cc3d474217633e16d4caa490a0aae518` for parallel review.

`IMPORT_MANIFEST.json` pins the exact paths and Git blob SHA-1 values used by
the successor.  The two PR #495 Volterra files are independent normalization
cross-checks and are not silently substituted for the paired stopping-line
compiler.
