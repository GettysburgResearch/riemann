# T-105200 — Xi natural-scale residue coherence

**Scientific status:** proposed unconditional analytic advance; independent
review required; `CRDB105200` and RH remain unproved.

This dedicated branch continues the Xi reverse-Rolle programme from PRs
#716, #720 and #723.

## Main results

```text
positive Xi tilted measure
 -> uniform complex Gaussian saddle on |x|~sqrt(m/log m)
 -> every derivative m>=M real-rooted in one common natural box
 -> critical residues = -w_m^-2(1+o(1))
 -> high-tail residue coherence = 1-o(1).
```

The exact reverse-Rolle conservation law then gives the cumulative defect
budget

```text
off-real zeros
 <= terminal high-derivative defect
  + 2 sum R_j(1-C_j)
  + exact endpoint/winding charge.
```

The terminal high-derivative defect vanishes unconditionally at derivative
order `O(T^2 log T)` for a height-`T` rectangle. The remaining finite low-order
budget is `CRDB105200`.

## Load-bearing files

1. `claims/lemmas/L-105200-natural-scale-gaussian-saddle-for-xi-derivatives.md`
2. `claims/lemmas/L-105201-natural-height-real-rooted-tail.md`
3. `claims/lemmas/L-105202-monochromatic-critical-residues-in-the-high-derivative-tail.md`
4. `claims/lemmas/L-105203-coherence-defect-budget-for-reverse-rolle-descent.md`
5. `claims/refutations/R-105200-high-derivative-coherence-does-not-descend-for-free.md`
6. `claims/theorems/T-105200-natural-scale-xi-residue-coherence-frontier.md`
7. `claims/methodology/M-105200-natural-scale-residue-coherence-review-contract.md`
8. `experiments/X-105200-natural-scale-residue-coherence/`

## Replay

```bash
python -B experiments/X-105200-natural-scale-residue-coherence/verify.py \
  --output experiments/X-105200-natural-scale-residue-coherence/results/verification.json
```

Expected:

```text
PASS_X_105200_NATURAL_SCALE_RESIDUE_COHERENCE
```

The replay checks exact finite algebra only. It records
`analytic_gaussian_saddle_proved_by_replay=false`,
`low_order_coherence_budget_proved=false`, and `rh_established=false`.
