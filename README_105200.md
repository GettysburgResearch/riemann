# T-105200/T-105220 — Xi Poisson–Bézout residue localization

Status: **active multi-pass research programme; RH unproved**  
Initial base: PR #723 at `51c5619ef3c4b793de756b1c359b39df2ac35466`  
Integrated main consulted: `852d8aa05c701ea7818ce8a50543e68987fef5cc`

## Checkpoint 1 — common cubic Poisson weight

The original coordinate is

\[
\Omega_{a,T}(x)=\left(\frac{T^2}{(x-a)^2+T^2}\right)^3.
\]

For a finite polynomial it simultaneously measures critical count, first
residue moment and second residue moment. A canonical polynomial Bézout
correction removes the adjacent-derivative debt pointwise. The corresponding
Xi safe-line carriers have the coherence-one main scaling

```text
localized count       ~ 3 T Lambda(T) / 8;
localized first mass  ~ 3 T / [8 Lambda(T)];
localized second mass ~ 3 T / [8 Lambda(T)^3].
```

The entire Bézout interpolation and nonreal-critical corrections remain open.

## Checkpoint 2 — value-only moving-centre multipole weight

The new positive weight is

\[
\Omega_a(x)=
\frac{36}{((x-a)^2+1)((x-a)^2+4)((x-a)^2+9)}.
\]

Its partial fractions use only the six safe points

```text
a +/- i,  a +/- 2i,  a +/- 3i.
```

The count, first moment and raw second moment are exact **value-only** boundary
functionals. After the Xi functional equation, every evaluation lies on one of
the fixed lines `Re(s)=3/2,5/2,7/2`. For every fixed derivative order,

\[
\mathcal N_k\asymp\ell,
\qquad
\mathcal A_k\asymp\ell^{-1},
\qquad
\mathcal B_k\asymp\ell^{-3},
\]

and

\[
\boxed{
\mathcal N_k\mathcal B_k-\mathcal A_k^2=O_k(\ell^{-4}).
}
\]

Thus the uncorrected normalized carrier quotient is `1+O_k(ell^-2)`.

The exact weighted extinction lemma is

\[
\frac{NB-A^2}{B}<\frac9{25}
\quad\Longrightarrow\quad
\text{no wrong extremum in }[a-1,a+1].
\]

The real defect equals the safe-line defect plus one explicit signed
combination of the nonreal count, first and second residue corrections and the
adjacent-derivative debt. The conclusion-bearing statement is therefore

```text
MCRC105220:
  that exact combined correction is o(ell^-3), uniformly at large centres.
```

A rational quartic fixture proves that raw boundary coherence can exceed one
while the actual real coherence is below one. The corrections may not be
dropped.

## Additional analytic input

The standard positive Xi theta kernel gives one common anchor

\[
\Xi^{(m)}(-i)\ne0\qquad(m\ge0)
\]

and the explicit fixed-order growth envelope

\[
|\Xi^{(m)}(z)|
\le
4\pi^2m!\zeta(3/2)\pi^{-q/2}\Gamma(q/2),
\qquad q=|\Im z|+11/2.
\]

This closes the common-anchor and generic disk-growth inputs in the latest
PR #723 Cartan-collar handoff, but not selector-cost absorption.

## Exact status

```text
common cubic Poisson localization          proved finite
finite Bézout debt elimination             proved finite
positive multipole localizer               proposed exact finite theorem
value-only safe-line carrier asymptotic    proposed unconditional theorem
formal defect O(ell^-4)                    exact algebra
weighted unit-interval extinction          proposed exact theorem
common Xi derivative anchor                proposed unconditional theorem
fixed-order Xi disk growth                 proposed unconditional theorem
MCRC105220 correction cancellation         open / RH-bearing
finite low-height endpoint/winding ledger  open / RH-bearing
Riemann Hypothesis                         unproved
```

This branch remains the dedicated home for later passes. New work must preserve
the distinction between complete safe-line carriers and actual real-critical
moments.
