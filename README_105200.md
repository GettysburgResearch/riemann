# T-105200 — Xi Poisson–Bézout residue localization

Status: **active multi-pass research programme; RH unproved**  
Base: PR #723 at `51c5619ef3c4b793de756b1c359b39df2ac35466`  
Integrated main consulted: `852d8aa05c701ea7818ce8a50543e68987fef5cc`

## Purpose

Continue the Xi reverse–Rolle programme after PRs #720 and #723.  The new
coordinate is a common positive rational localizer

\[
\Omega_{a,T}(x)=\left(\frac{T^2}{(x-a)^2+T^2}\right)^3.
\]

For a finite polynomial, the same localizer simultaneously measures:

- the number of critical points;
- the first derivative-ratio residue moment;
- the second derivative-ratio residue moment.

All three quantities become exact boundary functionals at the two points
`a +/- iT`.  A canonical polynomial Bézout correction removes the
second-derivative cross-residue debt of PR #723 without changing any residue at
a zero of `p'`.

For the Riemann Xi function, the boundary points lie on the zero-free real
axis after the functional equation.  Fixed-order Stirling asymptotics give the
coherence-one main scaling

```text
localized count       ~ 3 T Lambda(T) / 8;
localized first mass  ~ 3 T / [8 Lambda(T)];
localized second mass ~ 3 T / [8 Lambda(T)^3].
```

Here `Lambda=(log xi)'(T+1/2)`.

## Exact status

```text
common-weight rational localization       proved for finite polynomials
Bézout elimination of p'' debt            proved for finite polynomials
off-axis Xi boundary dictionary           proved exact
fixed-order zero-free-axis main asymptotic proved
entire Bézout interpolation / exhaustion  open
nonreal-critical correction               open
low-derivative residue coherence           open
Riemann Hypothesis                         unproved
```

This branch is intended to remain the dedicated home for several continuation
passes.  New strengthenings must preserve the explicit distinction between
finite-polynomial identities, entire-function passage, correction estimates,
and RH consequences.
