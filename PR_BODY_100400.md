## Publication recovery

This PR is the actual remote publication of the previously promised
"two surviving closure routes" packet.  The earlier branch/PR claim did not
land; this recovery uses a fresh `T-100400` namespace and a collision-free
branch.

**RH remains unproved.**

## Route A — exact shifted-square atom/collar homotopy

For `-1 <= c <= 0`, with `lambda_c=(3-c)^2/9`, the packet proves

```text
lambda_c^-1 Q_c(lambda_c X) = H_2(X) + P_c(X),
```

and the complete future identity

```text
E_2(X)
 = X(1+c)^2 sum_(n>lambda_c X) beta(n)n^-3/2
 + (3-c)X int_(lambda_c X)^infty L_c(t)dt/t^2
 + P_c(X).
```

At `c=0` the collar vanishes but the atomic tail remains.  At `c=-1`
the atoms vanish but the ratio-`16/9` collar remains and is already negative at
`X=2`.  The exact remaining gate is `QACG100400`.

## Route B — minimal-wavelet largest-prime reduction

For the ratio-eight ordinary-Möbius wavelet, every nonunit source integer is
spent exactly once by its largest prime.  Choosing

```text
Y_X=(log X)^(3/2)
```

makes the complete `Y_X`-smooth sector `X^(-1/6+o(1))`.  The sole remaining
term is the explicit rough largest-prime bilinear form `LPMW100410`.

## Phase-Hasse audit

The exact identity

```text
S_B(gamma) = 1/2 [T_B(gamma)+s_B-P_B(gamma)]
```

shows that neutral-phase cancellation retains `-P_B(gamma)/2` at nonzero
phase.  The phase-Hasse symbol is therefore not an independent root-free
closure route.

## Replay

```bash
python3 experiments/X-100400-two-route/verify.py \
  --output experiments/X-100400-two-route/results/verification.json
python3 -m unittest discover -s experiments/X-100400-two-route/tests -v
sha256sum -c T100400_CONTENT_SHA256SUMS
```

Expected:

```text
PASS_T100400_TWO_SURVIVING_CLOSURE_ROUTES_RECOVERY
```

```text
QACG100400       OPEN / RH-BEARING
LPMW100410       OPEN / RH-BEARING
Riemann Hypothesis UNPROVED
```
