## Purpose

Correct the conclusion-facing normalization error in PR #685 and push the two
routes that retain genuine arithmetic leverage.

```text
base PR #685:
4f69b7656f42dcb5ff250d13adc9f88e8d18f315
```

**Riemann Hypothesis remains unproved.**

## Binding correction

PR #685's cell primitive computes the negative mass of
`E_2(X)/X`, not of `E_2(X)`.  Division by `X` shifts the reciprocal-zeta poles
out of the positive Landau half-plane.  `R-100400` freezes the correction.

## Route A

Use the activation-zero shifted quadratic carrier.  The resulting envelope:

- has an exact all-integer kernel;
- has a zero-safe Mellin transform;
- has no activation atoms;
- is a global `C1` quadratic spline in `sqrt(X)`;
- has every actual native Euler block through four labels positive.

The remaining all-order gate is `FAEG100401`, equivalently the unnormalized
negative-mass estimate `ACAD100400`.

## Route B

The symmetric phase-Hasse symbol is rewritten as one exact two-parameter
divergence.  Its degree-zero and degree-one sectors vanish, and the complete
free labelled phase packet is polylogarithmic.

The remaining theorem `PHPC100410` is the literal half-order physical
cross-core collapse.

## Replay

```text
PASS_T100400_CORRECTED_TWO_ROUTE_ENDGAME
```

## Boundary

```text
FAEG100401 / ACAD100400   open
PHPC100410                open
Riemann Hypothesis        unproved
```
