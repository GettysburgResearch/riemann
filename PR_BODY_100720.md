## Purpose

Continue the audited double-owner coboundary route from PR #695 at exact head
`7b61f6b1998aefa185d0e5beff18e5a456451299`, preserving its balanced
completed-minus-transition cancellation.

**RH remains unproved.**

## Main exact result

The centered critical cubic has the exact hinge/carrier decomposition

```text
Psi(t^2)
 =384 integral_0^1 (t-s)_+(1-s)ds
 =192t-64+64(1-t)_+^3.
```

Every double-owner interval is therefore one explicit positive deep carrier
plus one compact cubic collar. Its derivative is a positive coarea of the
compensated reciprocal-Mobius prefix

```text
A_P(x)
-p^(-1/2)A_P(x/p)
-q^(-1/2)A_P(x/q)
+(pq)^(-1/2)A_P(x/(pq)).
```

Tao's elementary semigroup theorem gives a uniform derivative bound; the exact
third derivative has summable `d^-2` / prime-`3/2` source mass.

A new level-pairing argument proves every interval with

```text
sum_(p<ell<q) 1/ell + p^(-1/2)+q^(-1/2) < 1
```

is nonnegative at every scale. Hence, for every fixed `A<e`, all sufficiently
large intervals `q<=p^A` are closed pointwise.

## Exact adaptive AND gate

Taylor expansion from the activation origin and from the completed deep end
gives two source-owned certificates `L,R` with

```text
H_- <= min(L,R).
```

The raw integral of `L` is power-sized after the collar vanishes, so it is
explicitly refuted. The corrected gate spends `L` only where `L<=R` and `R`
only where `R<L`.

```text
APCC100723 AND DPCC100723 -> RH.
```

`APCC` is assigned to compensated finite-prefix/activation tools. `DPCC` is
assigned to PR #695's audited mixed-coboundary rectangle telescope and balanced
homotopy. The withdrawn divisor-renewal shortcuts from PR #691 are not used.

## Replay

```text
PASS_T100720_CUBIC_HINGE_ROUGH_PREFIX_ALGEBRA

a05a9ed1c455a87115424ec8d4bfb0ec1fc8acc94ce0f36f012c4c35be88f0ba
```

## Boundary

```text
cubic hinge/coarea/third variation           proved
positive interval region A<e                 proved asymptotically
adaptive two-certificate composition         proved
APCC100723                                    open / RH-bearing
DPCC100723                                    open / RH-bearing
Riemann Hypothesis                            unproved
```
