# Packet 105000 — hazard budget and conservation trichotomy

One theorem replacing the serial alpha-defect autopsies
(`R-97600`, `R-99600/01`, `R-99440`, `R-99260`, `R-97610`, `R-97500`,
`R-99800`):

* **Budget** (`T-105000` A.1): sign-paid first-order delivery of any
  admissible scheme obeys `Σ_p d_p Q_{Y/p}(j) ≤ Q_Y(j)`.
* **Price** (`T-105000` A.1′/A.2, `L-105001`): native delivery costs
  `(1+o(1))/p` per rough prime — invariant under the `R-99820`
  normalization cocycle — and the total price `Π(Y) ≤ Σ_{67≤p≤Y} 1/p`
  diverges; the binding `T`-form budget crosses 1 in `(578906, 584375)`
  (interval-certified), the row-form near `(5·10^6, 7·10^6)`.
* **Exposure** (`T-105000` A.3): every conserving scheme carries
  sign-unpaid demand `≥ (Π−1)Q_Y(j) ≍ √Y(log log Y − O(1))` with a
  positive-real Mellin pole at `s = 1/2` — not typeable as a
  bounded/holomorphic defect.
* **Dichotomy + typing** (`T-105000 §4`): proved — every admissible scheme
  either fails conservation (the corpses, cell A) or carries CHILD-typed
  exposure of the stated size and Mellin type; typology over the executed
  corpus — that exposure is (B) FCHD67-class deep currents (exact schemes;
  the demand is the native problem) or (C) global cross-history coupling
  (CPSL67/GPC67/block-L², RH-bearing/RH-equivalent). Exits from the axioms
  are enumerated.
* **Adjudication** (`R-105000`): inside the axioms, `R-99260 §2`'s third
  repair option ("direct positive representation of the actual Möbius
  marginal") collapses into the first (a sign theorem) with its minimum
  burden computed.
* **Structural lemma** (`L-105002`): RN thinning is branch-submultiplicative
  with exact deficit identity — why free one-prime currents do not compose
  to Euler products.

Verification: `experiments/X-105000-hazard-budget/verify.py`
(`ALL CHECKS PASSED`; fixtures mirrored in `M-105000 §3`).

Scope: method-space classification. **RH is unproved and is not addressed.**
