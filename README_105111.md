# T-105111 certified absolute-edge frontier

This checkpoint turns the lower-margin branch left open by T-105109 into two
finite, auditable certificates.

For \(G=L=F'/F\) or \(G=LA=(F'/F)(F''/F)\), an anchor and the exact downward
logarithmic drop give

\[
\inf_E|G|=|G(z_*)|e^{-D_G}.
\]

Total logarithmic-modulus variation gives a sufficient relaxation.  A second,
noncircular route covers the edge by disks and requires positive
center-minus-derivative slacks.

Both formats are sharp and independent of finite anchor data.  An explicit
real-even zero-free family has one globally fixed critical target, residue
and first optimal selector one, and fixed first/product denominator values at
every prescribed anchor, while both denominators collapse between anchors.

Exact replay:

    python -B experiments/X-105111-anchored-disk-margin-certificate/tests/test_verify.py
    python -B -O experiments/X-105111-anchored-disk-margin-certificate/tests/test_verify.py

Expected:

    PASS_T105111_ANCHORED_DISK_MARGIN_CERTIFICATE
    19 focused tests in normal and optimized Python
    digest 7bafdd3d8fb379e8d8de2cd30df8b96331378d7e4f244bb801439493a2e132cd

No heavy computation is used.  Xi anchor/disk data, cofinal variation,
selector absorption, the complementary cancellation debts, strict coherence,
RCMV104530, and RH remain open.
