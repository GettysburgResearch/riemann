# T-99900 — Native normalized SHARP box and half-order collar firewall

Exact base:

```text
PR #664
14692244bdaef90793a0c2a1a9bfd6e6b4bb1a2e
```

This successor audits the final ratio-67 window interface against the literal
conclusion-facing box source.

The normalized box is

\[
\frac{(\mathcal S_{67}h)(X)}{\sqrt X}
 =\sum_{n\le X}\frac{\beta_{67}(n)}n
   \Phi\!\left(\frac Xn\right).
\]

Therefore adjoining a native prime acts by `I-p^-1 U_p`. Applying
`I-p^-1/2 U_p` to the *normalized* potential changes the source. The native
collar slope is a ratio-67 **half-order** Möbius window, not the unweighted
Mertens window stated in `L-99819`.

The packet also rewrites the Cauchy–Poisson square exactly as a one-dimensional
Hardy tail energy. These are exact repairs and reductions; neither the
half-order window estimate, `GPMOC99800`, nor RH is proved.
