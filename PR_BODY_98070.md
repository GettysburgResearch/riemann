## T98070 deterministic successor to PR #608

Frozen source:

```text
PR:     #608
branch: agent/recursive-dickman-threshold-closure
head:   f362acf56bbbbd183976b6377fbe883193886e1a
tree:   33387b767da8a6c69d824f5bfee33c864d9cc299
```

Collision note: `98060` is occupied by the distinct
`agent/98060-curvature-cutoff-coboundary` branch. This packet therefore uses
`98070` throughout; `integration/2026-08-18/t98070-namespace-migration.json`
records the mechanical mapping.

Main results:

- fixed rough reciprocal-Möbius prefixes cannot be eventually one-signed;
- Euler integer discrepancy is a strict Hausdorff moment sequence;
- exact Hurwitz fractional-cell reset;
- finite positive two-mode dyadic target filters are impossible;
- subpower logarithmic scalar negative mass implies RH;
- exact one-sided prefix-exceedance reduction;
- exact source-faithful log-energy Type-II kernel;
- vanishing Lorenz hinge gives a `c_* L(X)` squarefree-shell reserve;
- fractional reciprocal-Julia root hierarchy;
- exact two-adic scalar/target bridge.

RH remains unproved.

Replay:

```bash
bash experiments/X-98070-negative-mass-euler-hurwitz/replay.sh
sha256sum -c T98070_CONTENT_SHA256SUMS
```

The current p=67 scanner was replayed through `10^6`, giving
`Q67=2.55967670964739158` at `Y=536`. The bundled legacy `10^8` record is
quarantined as incompatible with the current scanner and supplies no evidence.

Scope note: the exact finite identities and replay are retained, while the
Mellin/Landau transfer, vanishing-hinge shell interface, DPE67, VHL67,
cross-kernel estimate, GPC67, and RH remain open to independent review.
